module Aws
  module EC2
    class Client

      getter endpoint : String
      getter endpoint_headers : Hash(String, String)
      getter region : String
      getter credentials : Aws::Runtime::Credentials
      getter transport : Aws::Runtime::Transport
      getter runtime : Aws::Runtime::Client

      def initialize(
        endpoint : String? = nil,
        region : String? = nil,
        credentials : Aws::Runtime::Credentials? = nil,
        profile : String? = nil,
        use_fips : Bool? = nil,
        use_dualstack : Bool? = nil,
        transport : Aws::Runtime::Transport = Aws::Runtime::HttpTransport.new
      )
        @transport = transport
        @region = region || Aws::Runtime::Defaults.resolve_region(profile, @transport)
        @credentials = credentials || Aws::Runtime::Defaults.resolve_credentials(profile, @transport)
        endpoint_provider = Aws::Runtime::EndpointProvider.new(Model::ENDPOINT_PREFIX, Model::ENDPOINT_RULE_SET_JSON)
        endpoint_result = endpoint_provider.resolve(@region, endpoint, use_fips, use_dualstack)
        @endpoint = endpoint_result.url
        @endpoint_headers = endpoint_result.headers
        @runtime = Aws::Runtime::Client.new(@endpoint, @region, Model::SIGNING_NAME, @credentials, @transport, @endpoint_headers)
      end

      # Accepts an Elastic IP address transfer. For more information, see Accept a transferred Elastic IP
      # address in the Amazon VPC User Guide .

      def accept_address_transfer(
        address : String,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::AcceptAddressTransferResult

        input = Types::AcceptAddressTransferRequest.new(address: address, dry_run: dry_run, tag_specifications: tag_specifications)
        accept_address_transfer(input)
      end

      def accept_address_transfer(input : Types::AcceptAddressTransferRequest) : Types::AcceptAddressTransferResult
        request = Protocol::Ec2.build_request(Model::ACCEPT_ADDRESS_TRANSFER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AcceptAddressTransferResult, "AcceptAddressTransfer")
      end

      # Accepts a request to assign billing of the available capacity of a shared Capacity Reservation to
      # your account. For more information, see Billing assignment for shared Amazon EC2 Capacity
      # Reservations .

      def accept_capacity_reservation_billing_ownership(
        capacity_reservation_id : String,
        dry_run : Bool? = nil
      ) : Types::AcceptCapacityReservationBillingOwnershipResult

        input = Types::AcceptCapacityReservationBillingOwnershipRequest.new(capacity_reservation_id: capacity_reservation_id, dry_run: dry_run)
        accept_capacity_reservation_billing_ownership(input)
      end

      def accept_capacity_reservation_billing_ownership(input : Types::AcceptCapacityReservationBillingOwnershipRequest) : Types::AcceptCapacityReservationBillingOwnershipResult
        request = Protocol::Ec2.build_request(Model::ACCEPT_CAPACITY_RESERVATION_BILLING_OWNERSHIP, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AcceptCapacityReservationBillingOwnershipResult, "AcceptCapacityReservationBillingOwnership")
      end

      # Accepts the Convertible Reserved Instance exchange quote described in the
      # GetReservedInstancesExchangeQuote call.

      def accept_reserved_instances_exchange_quote(
        reserved_instance_ids : Array(String),
        dry_run : Bool? = nil,
        target_configurations : Array(Types::TargetConfigurationRequest)? = nil
      ) : Types::AcceptReservedInstancesExchangeQuoteResult

        input = Types::AcceptReservedInstancesExchangeQuoteRequest.new(reserved_instance_ids: reserved_instance_ids, dry_run: dry_run, target_configurations: target_configurations)
        accept_reserved_instances_exchange_quote(input)
      end

      def accept_reserved_instances_exchange_quote(input : Types::AcceptReservedInstancesExchangeQuoteRequest) : Types::AcceptReservedInstancesExchangeQuoteResult
        request = Protocol::Ec2.build_request(Model::ACCEPT_RESERVED_INSTANCES_EXCHANGE_QUOTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AcceptReservedInstancesExchangeQuoteResult, "AcceptReservedInstancesExchangeQuote")
      end

      # Accepts a request to associate subnets with a transit gateway multicast domain.

      def accept_transit_gateway_multicast_domain_associations(
        dry_run : Bool? = nil,
        subnet_ids : Array(String)? = nil,
        transit_gateway_attachment_id : String? = nil,
        transit_gateway_multicast_domain_id : String? = nil
      ) : Types::AcceptTransitGatewayMulticastDomainAssociationsResult

        input = Types::AcceptTransitGatewayMulticastDomainAssociationsRequest.new(dry_run: dry_run, subnet_ids: subnet_ids, transit_gateway_attachment_id: transit_gateway_attachment_id, transit_gateway_multicast_domain_id: transit_gateway_multicast_domain_id)
        accept_transit_gateway_multicast_domain_associations(input)
      end

      def accept_transit_gateway_multicast_domain_associations(input : Types::AcceptTransitGatewayMulticastDomainAssociationsRequest) : Types::AcceptTransitGatewayMulticastDomainAssociationsResult
        request = Protocol::Ec2.build_request(Model::ACCEPT_TRANSIT_GATEWAY_MULTICAST_DOMAIN_ASSOCIATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AcceptTransitGatewayMulticastDomainAssociationsResult, "AcceptTransitGatewayMulticastDomainAssociations")
      end

      # Accepts a transit gateway peering attachment request. The peering attachment must be in the
      # pendingAcceptance state.

      def accept_transit_gateway_peering_attachment(
        transit_gateway_attachment_id : String,
        dry_run : Bool? = nil
      ) : Types::AcceptTransitGatewayPeeringAttachmentResult

        input = Types::AcceptTransitGatewayPeeringAttachmentRequest.new(transit_gateway_attachment_id: transit_gateway_attachment_id, dry_run: dry_run)
        accept_transit_gateway_peering_attachment(input)
      end

      def accept_transit_gateway_peering_attachment(input : Types::AcceptTransitGatewayPeeringAttachmentRequest) : Types::AcceptTransitGatewayPeeringAttachmentResult
        request = Protocol::Ec2.build_request(Model::ACCEPT_TRANSIT_GATEWAY_PEERING_ATTACHMENT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AcceptTransitGatewayPeeringAttachmentResult, "AcceptTransitGatewayPeeringAttachment")
      end

      # Accepts a request to attach a VPC to a transit gateway. The VPC attachment must be in the
      # pendingAcceptance state. Use DescribeTransitGatewayVpcAttachments to view your pending VPC
      # attachment requests. Use RejectTransitGatewayVpcAttachment to reject a VPC attachment request.

      def accept_transit_gateway_vpc_attachment(
        transit_gateway_attachment_id : String,
        dry_run : Bool? = nil
      ) : Types::AcceptTransitGatewayVpcAttachmentResult

        input = Types::AcceptTransitGatewayVpcAttachmentRequest.new(transit_gateway_attachment_id: transit_gateway_attachment_id, dry_run: dry_run)
        accept_transit_gateway_vpc_attachment(input)
      end

      def accept_transit_gateway_vpc_attachment(input : Types::AcceptTransitGatewayVpcAttachmentRequest) : Types::AcceptTransitGatewayVpcAttachmentResult
        request = Protocol::Ec2.build_request(Model::ACCEPT_TRANSIT_GATEWAY_VPC_ATTACHMENT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AcceptTransitGatewayVpcAttachmentResult, "AcceptTransitGatewayVpcAttachment")
      end

      # Accepts connection requests to your VPC endpoint service.

      def accept_vpc_endpoint_connections(
        service_id : String,
        vpc_endpoint_ids : Array(String),
        dry_run : Bool? = nil
      ) : Types::AcceptVpcEndpointConnectionsResult

        input = Types::AcceptVpcEndpointConnectionsRequest.new(service_id: service_id, vpc_endpoint_ids: vpc_endpoint_ids, dry_run: dry_run)
        accept_vpc_endpoint_connections(input)
      end

      def accept_vpc_endpoint_connections(input : Types::AcceptVpcEndpointConnectionsRequest) : Types::AcceptVpcEndpointConnectionsResult
        request = Protocol::Ec2.build_request(Model::ACCEPT_VPC_ENDPOINT_CONNECTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AcceptVpcEndpointConnectionsResult, "AcceptVpcEndpointConnections")
      end

      # Accept a VPC peering connection request. To accept a request, the VPC peering connection must be in
      # the pending-acceptance state, and you must be the owner of the peer VPC. Use
      # DescribeVpcPeeringConnections to view your outstanding VPC peering connection requests. For an
      # inter-Region VPC peering connection request, you must accept the VPC peering connection in the
      # Region of the accepter VPC.

      def accept_vpc_peering_connection(
        vpc_peering_connection_id : String,
        dry_run : Bool? = nil
      ) : Types::AcceptVpcPeeringConnectionResult

        input = Types::AcceptVpcPeeringConnectionRequest.new(vpc_peering_connection_id: vpc_peering_connection_id, dry_run: dry_run)
        accept_vpc_peering_connection(input)
      end

      def accept_vpc_peering_connection(input : Types::AcceptVpcPeeringConnectionRequest) : Types::AcceptVpcPeeringConnectionResult
        request = Protocol::Ec2.build_request(Model::ACCEPT_VPC_PEERING_CONNECTION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AcceptVpcPeeringConnectionResult, "AcceptVpcPeeringConnection")
      end

      # Advertises an IPv4 or IPv6 address range that is provisioned for use with your Amazon Web Services
      # resources through bring your own IP addresses (BYOIP). You can perform this operation at most once
      # every 10 seconds, even if you specify different address ranges each time. We recommend that you stop
      # advertising the BYOIP CIDR from other locations when you advertise it from Amazon Web Services. To
      # minimize down time, you can configure your Amazon Web Services resources to use an address from a
      # BYOIP CIDR before it is advertised, and then simultaneously stop advertising it from the current
      # location and start advertising it through Amazon Web Services. It can take a few minutes before
      # traffic to the specified addresses starts routing to Amazon Web Services because of BGP propagation
      # delays.

      def advertise_byoip_cidr(
        cidr : String,
        asn : String? = nil,
        dry_run : Bool? = nil,
        network_border_group : String? = nil
      ) : Types::AdvertiseByoipCidrResult

        input = Types::AdvertiseByoipCidrRequest.new(cidr: cidr, asn: asn, dry_run: dry_run, network_border_group: network_border_group)
        advertise_byoip_cidr(input)
      end

      def advertise_byoip_cidr(input : Types::AdvertiseByoipCidrRequest) : Types::AdvertiseByoipCidrResult
        request = Protocol::Ec2.build_request(Model::ADVERTISE_BYOIP_CIDR, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AdvertiseByoipCidrResult, "AdvertiseByoipCidr")
      end

      # Allocates an Elastic IP address to your Amazon Web Services account. After you allocate the Elastic
      # IP address you can associate it with an instance or network interface. After you release an Elastic
      # IP address, it is released to the IP address pool and can be allocated to a different Amazon Web
      # Services account. You can allocate an Elastic IP address from one of the following address pools:
      # Amazon's pool of IPv4 addresses Public IPv4 address range that you own and bring to your Amazon Web
      # Services account using Bring Your Own IP Addresses (BYOIP) An IPv4 IPAM pool with an Amazon-provided
      # or BYOIP public IPv4 address range IPv4 addresses from your on-premises network made available for
      # use with an Outpost using a customer-owned IP address pool (CoIP pool) For more information, see
      # Elastic IP Addresses in the Amazon EC2 User Guide . If you release an Elastic IP address, you might
      # be able to recover it. You cannot recover an Elastic IP address that you released after it is
      # allocated to another Amazon Web Services account. To attempt to recover an Elastic IP address that
      # you released, specify it in this operation. You can allocate a carrier IP address which is a public
      # IP address from a telecommunication carrier, to a network interface which resides in a subnet in a
      # Wavelength Zone (for example an EC2 instance).

      def allocate_address(
        address : String? = nil,
        customer_owned_ipv4_pool : String? = nil,
        domain : String? = nil,
        dry_run : Bool? = nil,
        ipam_pool_id : String? = nil,
        network_border_group : String? = nil,
        public_ipv4_pool : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::AllocateAddressResult

        input = Types::AllocateAddressRequest.new(address: address, customer_owned_ipv4_pool: customer_owned_ipv4_pool, domain: domain, dry_run: dry_run, ipam_pool_id: ipam_pool_id, network_border_group: network_border_group, public_ipv4_pool: public_ipv4_pool, tag_specifications: tag_specifications)
        allocate_address(input)
      end

      def allocate_address(input : Types::AllocateAddressRequest) : Types::AllocateAddressResult
        request = Protocol::Ec2.build_request(Model::ALLOCATE_ADDRESS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AllocateAddressResult, "AllocateAddress")
      end

      # Allocates a Dedicated Host to your account. At a minimum, specify the supported instance type or
      # instance family, the Availability Zone in which to allocate the host, and the number of hosts to
      # allocate.

      def allocate_hosts(
        asset_ids : Array(String)? = nil,
        auto_placement : String? = nil,
        availability_zone : String? = nil,
        availability_zone_id : String? = nil,
        client_token : String? = nil,
        host_maintenance : String? = nil,
        host_recovery : String? = nil,
        instance_family : String? = nil,
        instance_type : String? = nil,
        outpost_arn : String? = nil,
        quantity : Int32? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::AllocateHostsResult

        input = Types::AllocateHostsRequest.new(asset_ids: asset_ids, auto_placement: auto_placement, availability_zone: availability_zone, availability_zone_id: availability_zone_id, client_token: client_token, host_maintenance: host_maintenance, host_recovery: host_recovery, instance_family: instance_family, instance_type: instance_type, outpost_arn: outpost_arn, quantity: quantity, tag_specifications: tag_specifications)
        allocate_hosts(input)
      end

      def allocate_hosts(input : Types::AllocateHostsRequest) : Types::AllocateHostsResult
        request = Protocol::Ec2.build_request(Model::ALLOCATE_HOSTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AllocateHostsResult, "AllocateHosts")
      end

      # Allocate a CIDR from an IPAM pool. The Region you use should be the IPAM pool locale. The locale is
      # the Amazon Web Services Region where this IPAM pool is available for allocations. In IPAM, an
      # allocation is a CIDR assignment from an IPAM pool to another IPAM pool or to a resource. For more
      # information, see Allocate CIDRs in the Amazon VPC IPAM User Guide . This action creates an
      # allocation with strong consistency. The returned CIDR will not overlap with any other allocations
      # from the same pool.

      def allocate_ipam_pool_cidr(
        ipam_pool_id : String,
        allowed_cidrs : Array(String)? = nil,
        cidr : String? = nil,
        client_token : String? = nil,
        description : String? = nil,
        disallowed_cidrs : Array(String)? = nil,
        dry_run : Bool? = nil,
        netmask_length : Int32? = nil,
        preview_next_cidr : Bool? = nil
      ) : Types::AllocateIpamPoolCidrResult

        input = Types::AllocateIpamPoolCidrRequest.new(ipam_pool_id: ipam_pool_id, allowed_cidrs: allowed_cidrs, cidr: cidr, client_token: client_token, description: description, disallowed_cidrs: disallowed_cidrs, dry_run: dry_run, netmask_length: netmask_length, preview_next_cidr: preview_next_cidr)
        allocate_ipam_pool_cidr(input)
      end

      def allocate_ipam_pool_cidr(input : Types::AllocateIpamPoolCidrRequest) : Types::AllocateIpamPoolCidrResult
        request = Protocol::Ec2.build_request(Model::ALLOCATE_IPAM_POOL_CIDR, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AllocateIpamPoolCidrResult, "AllocateIpamPoolCidr")
      end

      # Applies a security group to the association between the target network and the Client VPN endpoint.
      # This action replaces the existing security groups with the specified security groups.

      def apply_security_groups_to_client_vpn_target_network(
        client_vpn_endpoint_id : String,
        security_group_ids : Array(String),
        vpc_id : String,
        dry_run : Bool? = nil
      ) : Types::ApplySecurityGroupsToClientVpnTargetNetworkResult

        input = Types::ApplySecurityGroupsToClientVpnTargetNetworkRequest.new(client_vpn_endpoint_id: client_vpn_endpoint_id, security_group_ids: security_group_ids, vpc_id: vpc_id, dry_run: dry_run)
        apply_security_groups_to_client_vpn_target_network(input)
      end

      def apply_security_groups_to_client_vpn_target_network(input : Types::ApplySecurityGroupsToClientVpnTargetNetworkRequest) : Types::ApplySecurityGroupsToClientVpnTargetNetworkResult
        request = Protocol::Ec2.build_request(Model::APPLY_SECURITY_GROUPS_TO_CLIENT_VPN_TARGET_NETWORK, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ApplySecurityGroupsToClientVpnTargetNetworkResult, "ApplySecurityGroupsToClientVpnTargetNetwork")
      end

      # Assigns the specified IPv6 addresses to the specified network interface. You can specify specific
      # IPv6 addresses, or you can specify the number of IPv6 addresses to be automatically assigned from
      # the subnet's IPv6 CIDR block range. You can assign as many IPv6 addresses to a network interface as
      # you can assign private IPv4 addresses, and the limit varies by instance type. You must specify
      # either the IPv6 addresses or the IPv6 address count in the request. You can optionally use Prefix
      # Delegation on the network interface. You must specify either the IPV6 Prefix Delegation prefixes, or
      # the IPv6 Prefix Delegation count. For information, see Assigning prefixes to network interfaces in
      # the Amazon EC2 User Guide .

      def assign_ipv6_addresses(
        network_interface_id : String,
        ipv6_address_count : Int32? = nil,
        ipv6_addresses : Array(String)? = nil,
        ipv6_prefix_count : Int32? = nil,
        ipv6_prefixes : Array(String)? = nil
      ) : Types::AssignIpv6AddressesResult

        input = Types::AssignIpv6AddressesRequest.new(network_interface_id: network_interface_id, ipv6_address_count: ipv6_address_count, ipv6_addresses: ipv6_addresses, ipv6_prefix_count: ipv6_prefix_count, ipv6_prefixes: ipv6_prefixes)
        assign_ipv6_addresses(input)
      end

      def assign_ipv6_addresses(input : Types::AssignIpv6AddressesRequest) : Types::AssignIpv6AddressesResult
        request = Protocol::Ec2.build_request(Model::ASSIGN_IPV6_ADDRESSES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AssignIpv6AddressesResult, "AssignIpv6Addresses")
      end

      # Assigns the specified secondary private IP addresses to the specified network interface. You can
      # specify specific secondary IP addresses, or you can specify the number of secondary IP addresses to
      # be automatically assigned from the subnet's CIDR block range. The number of secondary IP addresses
      # that you can assign to an instance varies by instance type. For more information about Elastic IP
      # addresses, see Elastic IP Addresses in the Amazon EC2 User Guide . When you move a secondary private
      # IP address to another network interface, any Elastic IP address that is associated with the IP
      # address is also moved. Remapping an IP address is an asynchronous operation. When you move an IP
      # address from one network interface to another, check network/interfaces/macs/mac/local-ipv4s in the
      # instance metadata to confirm that the remapping is complete. You must specify either the IP
      # addresses or the IP address count in the request. You can optionally use Prefix Delegation on the
      # network interface. You must specify either the IPv4 Prefix Delegation prefixes, or the IPv4 Prefix
      # Delegation count. For information, see Assigning prefixes to network interfaces in the Amazon EC2
      # User Guide .

      def assign_private_ip_addresses(
        network_interface_id : String,
        allow_reassignment : Bool? = nil,
        ipv4_prefix_count : Int32? = nil,
        ipv4_prefixes : Array(String)? = nil,
        private_ip_addresses : Array(String)? = nil,
        secondary_private_ip_address_count : Int32? = nil
      ) : Types::AssignPrivateIpAddressesResult

        input = Types::AssignPrivateIpAddressesRequest.new(network_interface_id: network_interface_id, allow_reassignment: allow_reassignment, ipv4_prefix_count: ipv4_prefix_count, ipv4_prefixes: ipv4_prefixes, private_ip_addresses: private_ip_addresses, secondary_private_ip_address_count: secondary_private_ip_address_count)
        assign_private_ip_addresses(input)
      end

      def assign_private_ip_addresses(input : Types::AssignPrivateIpAddressesRequest) : Types::AssignPrivateIpAddressesResult
        request = Protocol::Ec2.build_request(Model::ASSIGN_PRIVATE_IP_ADDRESSES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AssignPrivateIpAddressesResult, "AssignPrivateIpAddresses")
      end

      # Assigns private IPv4 addresses to a private NAT gateway. For more information, see Work with NAT
      # gateways in the Amazon VPC User Guide .

      def assign_private_nat_gateway_address(
        nat_gateway_id : String,
        dry_run : Bool? = nil,
        private_ip_address_count : Int32? = nil,
        private_ip_addresses : Array(String)? = nil
      ) : Types::AssignPrivateNatGatewayAddressResult

        input = Types::AssignPrivateNatGatewayAddressRequest.new(nat_gateway_id: nat_gateway_id, dry_run: dry_run, private_ip_address_count: private_ip_address_count, private_ip_addresses: private_ip_addresses)
        assign_private_nat_gateway_address(input)
      end

      def assign_private_nat_gateway_address(input : Types::AssignPrivateNatGatewayAddressRequest) : Types::AssignPrivateNatGatewayAddressResult
        request = Protocol::Ec2.build_request(Model::ASSIGN_PRIVATE_NAT_GATEWAY_ADDRESS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AssignPrivateNatGatewayAddressResult, "AssignPrivateNatGatewayAddress")
      end

      # Associates an Elastic IP address, or carrier IP address (for instances that are in subnets in
      # Wavelength Zones) with an instance or a network interface. Before you can use an Elastic IP address,
      # you must allocate it to your account. If the Elastic IP address is already associated with a
      # different instance, it is disassociated from that instance and associated with the specified
      # instance. If you associate an Elastic IP address with an instance that has an existing Elastic IP
      # address, the existing address is disassociated from the instance, but remains allocated to your
      # account. [Subnets in Wavelength Zones] You can associate an IP address from the telecommunication
      # carrier to the instance or network interface. You cannot associate an Elastic IP address with an
      # interface in a different network border group. This is an idempotent operation. If you perform the
      # operation more than once, Amazon EC2 doesn't return an error, and you may be charged for each time
      # the Elastic IP address is remapped to the same instance. For more information, see the Elastic IP
      # Addresses section of Amazon EC2 Pricing .

      def associate_address(
        allocation_id : String? = nil,
        allow_reassociation : Bool? = nil,
        dry_run : Bool? = nil,
        instance_id : String? = nil,
        network_interface_id : String? = nil,
        private_ip_address : String? = nil,
        public_ip : String? = nil
      ) : Types::AssociateAddressResult

        input = Types::AssociateAddressRequest.new(allocation_id: allocation_id, allow_reassociation: allow_reassociation, dry_run: dry_run, instance_id: instance_id, network_interface_id: network_interface_id, private_ip_address: private_ip_address, public_ip: public_ip)
        associate_address(input)
      end

      def associate_address(input : Types::AssociateAddressRequest) : Types::AssociateAddressResult
        request = Protocol::Ec2.build_request(Model::ASSOCIATE_ADDRESS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AssociateAddressResult, "AssociateAddress")
      end

      # Initiates a request to assign billing of the unused capacity of a shared Capacity Reservation to a
      # consumer account that is consolidated under the same Amazon Web Services organizations payer
      # account. For more information, see Billing assignment for shared Amazon EC2 Capacity Reservations .

      def associate_capacity_reservation_billing_owner(
        capacity_reservation_id : String,
        unused_reservation_billing_owner_id : String,
        dry_run : Bool? = nil
      ) : Types::AssociateCapacityReservationBillingOwnerResult

        input = Types::AssociateCapacityReservationBillingOwnerRequest.new(capacity_reservation_id: capacity_reservation_id, unused_reservation_billing_owner_id: unused_reservation_billing_owner_id, dry_run: dry_run)
        associate_capacity_reservation_billing_owner(input)
      end

      def associate_capacity_reservation_billing_owner(input : Types::AssociateCapacityReservationBillingOwnerRequest) : Types::AssociateCapacityReservationBillingOwnerResult
        request = Protocol::Ec2.build_request(Model::ASSOCIATE_CAPACITY_RESERVATION_BILLING_OWNER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AssociateCapacityReservationBillingOwnerResult, "AssociateCapacityReservationBillingOwner")
      end

      # Associates a target network with a Client VPN endpoint. A target network is a subnet in a VPC. You
      # can associate multiple subnets from the same VPC with a Client VPN endpoint. You can associate only
      # one subnet in each Availability Zone. We recommend that you associate at least two subnets to
      # provide Availability Zone redundancy. If you specified a VPC when you created the Client VPN
      # endpoint or if you have previous subnet associations, the specified subnet must be in the same VPC.
      # To specify a subnet that's in a different VPC, you must first modify the Client VPN endpoint (
      # ModifyClientVpnEndpoint ) and change the VPC that's associated with it.

      def associate_client_vpn_target_network(
        client_vpn_endpoint_id : String,
        subnet_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil
      ) : Types::AssociateClientVpnTargetNetworkResult

        input = Types::AssociateClientVpnTargetNetworkRequest.new(client_vpn_endpoint_id: client_vpn_endpoint_id, subnet_id: subnet_id, client_token: client_token, dry_run: dry_run)
        associate_client_vpn_target_network(input)
      end

      def associate_client_vpn_target_network(input : Types::AssociateClientVpnTargetNetworkRequest) : Types::AssociateClientVpnTargetNetworkResult
        request = Protocol::Ec2.build_request(Model::ASSOCIATE_CLIENT_VPN_TARGET_NETWORK, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AssociateClientVpnTargetNetworkResult, "AssociateClientVpnTargetNetwork")
      end

      # Associates a set of DHCP options (that you've previously created) with the specified VPC, or
      # associates no DHCP options with the VPC. After you associate the options with the VPC, any existing
      # instances and all new instances that you launch in that VPC use the options. You don't need to
      # restart or relaunch the instances. They automatically pick up the changes within a few hours,
      # depending on how frequently the instance renews its DHCP lease. You can explicitly renew the lease
      # using the operating system on the instance. For more information, see DHCP option sets in the Amazon
      # VPC User Guide .

      def associate_dhcp_options(
        dhcp_options_id : String,
        vpc_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::AssociateDhcpOptionsRequest.new(dhcp_options_id: dhcp_options_id, vpc_id: vpc_id, dry_run: dry_run)
        associate_dhcp_options(input)
      end

      def associate_dhcp_options(input : Types::AssociateDhcpOptionsRequest) : Nil
        request = Protocol::Ec2.build_request(Model::ASSOCIATE_DHCP_OPTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Associates an Identity and Access Management (IAM) role with an Certificate Manager (ACM)
      # certificate. This enables the certificate to be used by the ACM for Nitro Enclaves application
      # inside an enclave. For more information, see Certificate Manager for Nitro Enclaves in the Amazon
      # Web Services Nitro Enclaves User Guide . When the IAM role is associated with the ACM certificate,
      # the certificate, certificate chain, and encrypted private key are placed in an Amazon S3 location
      # that only the associated IAM role can access. The private key of the certificate is encrypted with
      # an Amazon Web Services managed key that has an attached attestation-based key policy. To enable the
      # IAM role to access the Amazon S3 object, you must grant it permission to call s3:GetObject on the
      # Amazon S3 bucket returned by the command. To enable the IAM role to access the KMS key, you must
      # grant it permission to call kms:Decrypt on the KMS key returned by the command. For more
      # information, see Grant the role permission to access the certificate and encryption key in the
      # Amazon Web Services Nitro Enclaves User Guide .

      def associate_enclave_certificate_iam_role(
        certificate_arn : String,
        role_arn : String,
        dry_run : Bool? = nil
      ) : Types::AssociateEnclaveCertificateIamRoleResult

        input = Types::AssociateEnclaveCertificateIamRoleRequest.new(certificate_arn: certificate_arn, role_arn: role_arn, dry_run: dry_run)
        associate_enclave_certificate_iam_role(input)
      end

      def associate_enclave_certificate_iam_role(input : Types::AssociateEnclaveCertificateIamRoleRequest) : Types::AssociateEnclaveCertificateIamRoleResult
        request = Protocol::Ec2.build_request(Model::ASSOCIATE_ENCLAVE_CERTIFICATE_IAM_ROLE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AssociateEnclaveCertificateIamRoleResult, "AssociateEnclaveCertificateIamRole")
      end

      # Associates an IAM instance profile with a running or stopped instance. You cannot associate more
      # than one IAM instance profile with an instance.

      def associate_iam_instance_profile(
        iam_instance_profile : Types::IamInstanceProfileSpecification,
        instance_id : String
      ) : Types::AssociateIamInstanceProfileResult

        input = Types::AssociateIamInstanceProfileRequest.new(iam_instance_profile: iam_instance_profile, instance_id: instance_id)
        associate_iam_instance_profile(input)
      end

      def associate_iam_instance_profile(input : Types::AssociateIamInstanceProfileRequest) : Types::AssociateIamInstanceProfileResult
        request = Protocol::Ec2.build_request(Model::ASSOCIATE_IAM_INSTANCE_PROFILE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AssociateIamInstanceProfileResult, "AssociateIamInstanceProfile")
      end

      # Associates one or more targets with an event window. Only one type of target (instance IDs,
      # Dedicated Host IDs, or tags) can be specified with an event window. For more information, see Define
      # event windows for scheduled events in the Amazon EC2 User Guide .

      def associate_instance_event_window(
        association_target : Types::InstanceEventWindowAssociationRequest,
        instance_event_window_id : String,
        dry_run : Bool? = nil
      ) : Types::AssociateInstanceEventWindowResult

        input = Types::AssociateInstanceEventWindowRequest.new(association_target: association_target, instance_event_window_id: instance_event_window_id, dry_run: dry_run)
        associate_instance_event_window(input)
      end

      def associate_instance_event_window(input : Types::AssociateInstanceEventWindowRequest) : Types::AssociateInstanceEventWindowResult
        request = Protocol::Ec2.build_request(Model::ASSOCIATE_INSTANCE_EVENT_WINDOW, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AssociateInstanceEventWindowResult, "AssociateInstanceEventWindow")
      end

      # Associates your Autonomous System Number (ASN) with a BYOIP CIDR that you own in the same Amazon Web
      # Services Region. For more information, see Tutorial: Bring your ASN to IPAM in the Amazon VPC IPAM
      # guide . After the association succeeds, the ASN is eligible for advertisement. You can view the
      # association with DescribeByoipCidrs . You can advertise the CIDR with AdvertiseByoipCidr .

      def associate_ipam_byoasn(
        asn : String,
        cidr : String,
        dry_run : Bool? = nil
      ) : Types::AssociateIpamByoasnResult

        input = Types::AssociateIpamByoasnRequest.new(asn: asn, cidr: cidr, dry_run: dry_run)
        associate_ipam_byoasn(input)
      end

      def associate_ipam_byoasn(input : Types::AssociateIpamByoasnRequest) : Types::AssociateIpamByoasnResult
        request = Protocol::Ec2.build_request(Model::ASSOCIATE_IPAM_BYOASN, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AssociateIpamByoasnResult, "AssociateIpamByoasn")
      end

      # Associates an IPAM resource discovery with an Amazon VPC IPAM. A resource discovery is an IPAM
      # component that enables IPAM to manage and monitor resources that belong to the owning account.

      def associate_ipam_resource_discovery(
        ipam_id : String,
        ipam_resource_discovery_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::AssociateIpamResourceDiscoveryResult

        input = Types::AssociateIpamResourceDiscoveryRequest.new(ipam_id: ipam_id, ipam_resource_discovery_id: ipam_resource_discovery_id, client_token: client_token, dry_run: dry_run, tag_specifications: tag_specifications)
        associate_ipam_resource_discovery(input)
      end

      def associate_ipam_resource_discovery(input : Types::AssociateIpamResourceDiscoveryRequest) : Types::AssociateIpamResourceDiscoveryResult
        request = Protocol::Ec2.build_request(Model::ASSOCIATE_IPAM_RESOURCE_DISCOVERY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AssociateIpamResourceDiscoveryResult, "AssociateIpamResourceDiscovery")
      end

      # Associates Elastic IP addresses (EIPs) and private IPv4 addresses with a public NAT gateway. For
      # more information, see Work with NAT gateways in the Amazon VPC User Guide . By default, you can
      # associate up to 2 Elastic IP addresses per public NAT gateway. You can increase the limit by
      # requesting a quota adjustment. For more information, see Elastic IP address quotas in the Amazon VPC
      # User Guide . When you associate an EIP or secondary EIPs with a public NAT gateway, the network
      # border group of the EIPs must match the network border group of the Availability Zone (AZ) that the
      # public NAT gateway is in. If it's not the same, the EIP will fail to associate. You can see the
      # network border group for the subnet's AZ by viewing the details of the subnet. Similarly, you can
      # view the network border group of an EIP by viewing the details of the EIP address. For more
      # information about network border groups and EIPs, see Allocate an Elastic IP address in the Amazon
      # VPC User Guide .

      def associate_nat_gateway_address(
        allocation_ids : Array(String),
        nat_gateway_id : String,
        availability_zone : String? = nil,
        availability_zone_id : String? = nil,
        dry_run : Bool? = nil,
        private_ip_addresses : Array(String)? = nil
      ) : Types::AssociateNatGatewayAddressResult

        input = Types::AssociateNatGatewayAddressRequest.new(allocation_ids: allocation_ids, nat_gateway_id: nat_gateway_id, availability_zone: availability_zone, availability_zone_id: availability_zone_id, dry_run: dry_run, private_ip_addresses: private_ip_addresses)
        associate_nat_gateway_address(input)
      end

      def associate_nat_gateway_address(input : Types::AssociateNatGatewayAddressRequest) : Types::AssociateNatGatewayAddressResult
        request = Protocol::Ec2.build_request(Model::ASSOCIATE_NAT_GATEWAY_ADDRESS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AssociateNatGatewayAddressResult, "AssociateNatGatewayAddress")
      end

      # Associates a route server with a VPC to enable dynamic route updates. A route server association is
      # the connection established between a route server and a VPC. For more information see Dynamic
      # routing in your VPC with VPC Route Server in the Amazon VPC User Guide .

      def associate_route_server(
        route_server_id : String,
        vpc_id : String,
        dry_run : Bool? = nil
      ) : Types::AssociateRouteServerResult

        input = Types::AssociateRouteServerRequest.new(route_server_id: route_server_id, vpc_id: vpc_id, dry_run: dry_run)
        associate_route_server(input)
      end

      def associate_route_server(input : Types::AssociateRouteServerRequest) : Types::AssociateRouteServerResult
        request = Protocol::Ec2.build_request(Model::ASSOCIATE_ROUTE_SERVER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AssociateRouteServerResult, "AssociateRouteServer")
      end

      # Associates a subnet in your VPC or an internet gateway or virtual private gateway attached to your
      # VPC with a route table in your VPC. This association causes traffic from the subnet or gateway to be
      # routed according to the routes in the route table. The action returns an association ID, which you
      # need in order to disassociate the route table later. A route table can be associated with multiple
      # subnets. For more information, see Route tables in the Amazon VPC User Guide .

      def associate_route_table(
        route_table_id : String,
        dry_run : Bool? = nil,
        gateway_id : String? = nil,
        public_ipv4_pool : String? = nil,
        subnet_id : String? = nil
      ) : Types::AssociateRouteTableResult

        input = Types::AssociateRouteTableRequest.new(route_table_id: route_table_id, dry_run: dry_run, gateway_id: gateway_id, public_ipv4_pool: public_ipv4_pool, subnet_id: subnet_id)
        associate_route_table(input)
      end

      def associate_route_table(input : Types::AssociateRouteTableRequest) : Types::AssociateRouteTableResult
        request = Protocol::Ec2.build_request(Model::ASSOCIATE_ROUTE_TABLE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AssociateRouteTableResult, "AssociateRouteTable")
      end

      # Associates a security group with another VPC in the same Region. This enables you to use the same
      # security group with network interfaces and instances in the specified VPC. The VPC you want to
      # associate the security group with must be in the same Region. You can associate the security group
      # with another VPC if your account owns the VPC or if the VPC was shared with you. You must own the
      # security group. You cannot use this feature with default security groups. You cannot use this
      # feature with the default VPC.

      def associate_security_group_vpc(
        group_id : String,
        vpc_id : String,
        dry_run : Bool? = nil
      ) : Types::AssociateSecurityGroupVpcResult

        input = Types::AssociateSecurityGroupVpcRequest.new(group_id: group_id, vpc_id: vpc_id, dry_run: dry_run)
        associate_security_group_vpc(input)
      end

      def associate_security_group_vpc(input : Types::AssociateSecurityGroupVpcRequest) : Types::AssociateSecurityGroupVpcResult
        request = Protocol::Ec2.build_request(Model::ASSOCIATE_SECURITY_GROUP_VPC, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AssociateSecurityGroupVpcResult, "AssociateSecurityGroupVpc")
      end

      # Associates a CIDR block with your subnet. You can only associate a single IPv6 CIDR block with your
      # subnet.

      def associate_subnet_cidr_block(
        subnet_id : String,
        ipv6_cidr_block : String? = nil,
        ipv6_ipam_pool_id : String? = nil,
        ipv6_netmask_length : Int32? = nil
      ) : Types::AssociateSubnetCidrBlockResult

        input = Types::AssociateSubnetCidrBlockRequest.new(subnet_id: subnet_id, ipv6_cidr_block: ipv6_cidr_block, ipv6_ipam_pool_id: ipv6_ipam_pool_id, ipv6_netmask_length: ipv6_netmask_length)
        associate_subnet_cidr_block(input)
      end

      def associate_subnet_cidr_block(input : Types::AssociateSubnetCidrBlockRequest) : Types::AssociateSubnetCidrBlockResult
        request = Protocol::Ec2.build_request(Model::ASSOCIATE_SUBNET_CIDR_BLOCK, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AssociateSubnetCidrBlockResult, "AssociateSubnetCidrBlock")
      end

      # Associates the specified subnets and transit gateway attachments with the specified transit gateway
      # multicast domain. The transit gateway attachment must be in the available state before you can add a
      # resource. Use DescribeTransitGatewayAttachments to see the state of the attachment.

      def associate_transit_gateway_multicast_domain(
        subnet_ids : Array(String),
        transit_gateway_attachment_id : String,
        transit_gateway_multicast_domain_id : String,
        dry_run : Bool? = nil
      ) : Types::AssociateTransitGatewayMulticastDomainResult

        input = Types::AssociateTransitGatewayMulticastDomainRequest.new(subnet_ids: subnet_ids, transit_gateway_attachment_id: transit_gateway_attachment_id, transit_gateway_multicast_domain_id: transit_gateway_multicast_domain_id, dry_run: dry_run)
        associate_transit_gateway_multicast_domain(input)
      end

      def associate_transit_gateway_multicast_domain(input : Types::AssociateTransitGatewayMulticastDomainRequest) : Types::AssociateTransitGatewayMulticastDomainResult
        request = Protocol::Ec2.build_request(Model::ASSOCIATE_TRANSIT_GATEWAY_MULTICAST_DOMAIN, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AssociateTransitGatewayMulticastDomainResult, "AssociateTransitGatewayMulticastDomain")
      end

      # Associates the specified transit gateway attachment with a transit gateway policy table.

      def associate_transit_gateway_policy_table(
        transit_gateway_attachment_id : String,
        transit_gateway_policy_table_id : String,
        dry_run : Bool? = nil
      ) : Types::AssociateTransitGatewayPolicyTableResult

        input = Types::AssociateTransitGatewayPolicyTableRequest.new(transit_gateway_attachment_id: transit_gateway_attachment_id, transit_gateway_policy_table_id: transit_gateway_policy_table_id, dry_run: dry_run)
        associate_transit_gateway_policy_table(input)
      end

      def associate_transit_gateway_policy_table(input : Types::AssociateTransitGatewayPolicyTableRequest) : Types::AssociateTransitGatewayPolicyTableResult
        request = Protocol::Ec2.build_request(Model::ASSOCIATE_TRANSIT_GATEWAY_POLICY_TABLE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AssociateTransitGatewayPolicyTableResult, "AssociateTransitGatewayPolicyTable")
      end

      # Associates the specified attachment with the specified transit gateway route table. You can
      # associate only one route table with an attachment.

      def associate_transit_gateway_route_table(
        transit_gateway_attachment_id : String,
        transit_gateway_route_table_id : String,
        dry_run : Bool? = nil
      ) : Types::AssociateTransitGatewayRouteTableResult

        input = Types::AssociateTransitGatewayRouteTableRequest.new(transit_gateway_attachment_id: transit_gateway_attachment_id, transit_gateway_route_table_id: transit_gateway_route_table_id, dry_run: dry_run)
        associate_transit_gateway_route_table(input)
      end

      def associate_transit_gateway_route_table(input : Types::AssociateTransitGatewayRouteTableRequest) : Types::AssociateTransitGatewayRouteTableResult
        request = Protocol::Ec2.build_request(Model::ASSOCIATE_TRANSIT_GATEWAY_ROUTE_TABLE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AssociateTransitGatewayRouteTableResult, "AssociateTransitGatewayRouteTable")
      end

      # Associates a branch network interface with a trunk network interface. Before you create the
      # association, use CreateNetworkInterface command and set the interface type to trunk . You must also
      # create a network interface for each branch network interface that you want to associate with the
      # trunk network interface.

      def associate_trunk_interface(
        branch_interface_id : String,
        trunk_interface_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        gre_key : Int32? = nil,
        vlan_id : Int32? = nil
      ) : Types::AssociateTrunkInterfaceResult

        input = Types::AssociateTrunkInterfaceRequest.new(branch_interface_id: branch_interface_id, trunk_interface_id: trunk_interface_id, client_token: client_token, dry_run: dry_run, gre_key: gre_key, vlan_id: vlan_id)
        associate_trunk_interface(input)
      end

      def associate_trunk_interface(input : Types::AssociateTrunkInterfaceRequest) : Types::AssociateTrunkInterfaceResult
        request = Protocol::Ec2.build_request(Model::ASSOCIATE_TRUNK_INTERFACE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AssociateTrunkInterfaceResult, "AssociateTrunkInterface")
      end

      # Associates a CIDR block with your VPC. You can associate a secondary IPv4 CIDR block, an
      # Amazon-provided IPv6 CIDR block, or an IPv6 CIDR block from an IPv6 address pool that you
      # provisioned through bring your own IP addresses ( BYOIP ). You must specify one of the following in
      # the request: an IPv4 CIDR block, an IPv6 pool, or an Amazon-provided IPv6 CIDR block. For more
      # information about associating CIDR blocks with your VPC and applicable restrictions, see IP
      # addressing for your VPCs and subnets in the Amazon VPC User Guide .

      def associate_vpc_cidr_block(
        vpc_id : String,
        amazon_provided_ipv6_cidr_block : Bool? = nil,
        cidr_block : String? = nil,
        ipv4_ipam_pool_id : String? = nil,
        ipv4_netmask_length : Int32? = nil,
        ipv6_cidr_block : String? = nil,
        ipv6_cidr_block_network_border_group : String? = nil,
        ipv6_ipam_pool_id : String? = nil,
        ipv6_netmask_length : Int32? = nil,
        ipv6_pool : String? = nil
      ) : Types::AssociateVpcCidrBlockResult

        input = Types::AssociateVpcCidrBlockRequest.new(vpc_id: vpc_id, amazon_provided_ipv6_cidr_block: amazon_provided_ipv6_cidr_block, cidr_block: cidr_block, ipv4_ipam_pool_id: ipv4_ipam_pool_id, ipv4_netmask_length: ipv4_netmask_length, ipv6_cidr_block: ipv6_cidr_block, ipv6_cidr_block_network_border_group: ipv6_cidr_block_network_border_group, ipv6_ipam_pool_id: ipv6_ipam_pool_id, ipv6_netmask_length: ipv6_netmask_length, ipv6_pool: ipv6_pool)
        associate_vpc_cidr_block(input)
      end

      def associate_vpc_cidr_block(input : Types::AssociateVpcCidrBlockRequest) : Types::AssociateVpcCidrBlockResult
        request = Protocol::Ec2.build_request(Model::ASSOCIATE_VPC_CIDR_BLOCK, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AssociateVpcCidrBlockResult, "AssociateVpcCidrBlock")
      end

      # This action is deprecated. Links an EC2-Classic instance to a ClassicLink-enabled VPC through one or
      # more of the VPC security groups. You cannot link an EC2-Classic instance to more than one VPC at a
      # time. You can only link an instance that's in the running state. An instance is automatically
      # unlinked from a VPC when it's stopped - you can link it to the VPC again when you restart it. After
      # you've linked an instance, you cannot change the VPC security groups that are associated with it. To
      # change the security groups, you must first unlink the instance, and then link it again. Linking your
      # instance to a VPC is sometimes referred to as attaching your instance.

      def attach_classic_link_vpc(
        groups : Array(String),
        instance_id : String,
        vpc_id : String,
        dry_run : Bool? = nil
      ) : Types::AttachClassicLinkVpcResult

        input = Types::AttachClassicLinkVpcRequest.new(groups: groups, instance_id: instance_id, vpc_id: vpc_id, dry_run: dry_run)
        attach_classic_link_vpc(input)
      end

      def attach_classic_link_vpc(input : Types::AttachClassicLinkVpcRequest) : Types::AttachClassicLinkVpcResult
        request = Protocol::Ec2.build_request(Model::ATTACH_CLASSIC_LINK_VPC, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AttachClassicLinkVpcResult, "AttachClassicLinkVpc")
      end

      # Attaches an internet gateway or a virtual private gateway to a VPC, enabling connectivity between
      # the internet and the VPC. For more information, see Internet gateways in the Amazon VPC User Guide .

      def attach_internet_gateway(
        internet_gateway_id : String,
        vpc_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::AttachInternetGatewayRequest.new(internet_gateway_id: internet_gateway_id, vpc_id: vpc_id, dry_run: dry_run)
        attach_internet_gateway(input)
      end

      def attach_internet_gateway(input : Types::AttachInternetGatewayRequest) : Nil
        request = Protocol::Ec2.build_request(Model::ATTACH_INTERNET_GATEWAY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Attaches a network interface to an instance.

      def attach_network_interface(
        device_index : Int32,
        instance_id : String,
        network_interface_id : String,
        dry_run : Bool? = nil,
        ena_queue_count : Int32? = nil,
        ena_srd_specification : Types::EnaSrdSpecification? = nil,
        network_card_index : Int32? = nil
      ) : Types::AttachNetworkInterfaceResult

        input = Types::AttachNetworkInterfaceRequest.new(device_index: device_index, instance_id: instance_id, network_interface_id: network_interface_id, dry_run: dry_run, ena_queue_count: ena_queue_count, ena_srd_specification: ena_srd_specification, network_card_index: network_card_index)
        attach_network_interface(input)
      end

      def attach_network_interface(input : Types::AttachNetworkInterfaceRequest) : Types::AttachNetworkInterfaceResult
        request = Protocol::Ec2.build_request(Model::ATTACH_NETWORK_INTERFACE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AttachNetworkInterfaceResult, "AttachNetworkInterface")
      end

      # Attaches the specified Amazon Web Services Verified Access trust provider to the specified Amazon
      # Web Services Verified Access instance.

      def attach_verified_access_trust_provider(
        verified_access_instance_id : String,
        verified_access_trust_provider_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil
      ) : Types::AttachVerifiedAccessTrustProviderResult

        input = Types::AttachVerifiedAccessTrustProviderRequest.new(verified_access_instance_id: verified_access_instance_id, verified_access_trust_provider_id: verified_access_trust_provider_id, client_token: client_token, dry_run: dry_run)
        attach_verified_access_trust_provider(input)
      end

      def attach_verified_access_trust_provider(input : Types::AttachVerifiedAccessTrustProviderRequest) : Types::AttachVerifiedAccessTrustProviderResult
        request = Protocol::Ec2.build_request(Model::ATTACH_VERIFIED_ACCESS_TRUST_PROVIDER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AttachVerifiedAccessTrustProviderResult, "AttachVerifiedAccessTrustProvider")
      end

      # Attaches an Amazon EBS volume to a running or stopped instance, and exposes it to the instance with
      # the specified device name. The maximum number of Amazon EBS volumes that you can attach to an
      # instance depends on the instance type. If you exceed the volume attachment limit for an instance
      # type, the attachment request fails with the AttachmentLimitExceeded error. For more information, see
      # Instance volume limits . After you attach an EBS volume, you must make it available for use. For
      # more information, see Make an EBS volume available for use . If a volume has an Amazon Web Services
      # Marketplace product code: The volume can be attached only to a stopped instance. Amazon Web Services
      # Marketplace product codes are copied from the volume to the instance. You must be subscribed to the
      # product. The instance type and operating system of the instance must support the product. For
      # example, you can't detach a volume from a Windows instance and attach it to a Linux instance. For
      # more information, see Attach an Amazon EBS volume to an instance in the Amazon EBS User Guide .

      def attach_volume(
        device : String,
        instance_id : String,
        volume_id : String,
        dry_run : Bool? = nil
      ) : Types::VolumeAttachment

        input = Types::AttachVolumeRequest.new(device: device, instance_id: instance_id, volume_id: volume_id, dry_run: dry_run)
        attach_volume(input)
      end

      def attach_volume(input : Types::AttachVolumeRequest) : Types::VolumeAttachment
        request = Protocol::Ec2.build_request(Model::ATTACH_VOLUME, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::VolumeAttachment, "AttachVolume")
      end

      # Attaches an available virtual private gateway to a VPC. You can attach one virtual private gateway
      # to one VPC at a time. For more information, see Amazon Web Services Site-to-Site VPN in the Amazon
      # Web Services Site-to-Site VPN User Guide .

      def attach_vpn_gateway(
        vpc_id : String,
        vpn_gateway_id : String,
        dry_run : Bool? = nil
      ) : Types::AttachVpnGatewayResult

        input = Types::AttachVpnGatewayRequest.new(vpc_id: vpc_id, vpn_gateway_id: vpn_gateway_id, dry_run: dry_run)
        attach_vpn_gateway(input)
      end

      def attach_vpn_gateway(input : Types::AttachVpnGatewayRequest) : Types::AttachVpnGatewayResult
        request = Protocol::Ec2.build_request(Model::ATTACH_VPN_GATEWAY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AttachVpnGatewayResult, "AttachVpnGateway")
      end

      # Adds an ingress authorization rule to a Client VPN endpoint. Ingress authorization rules act as
      # firewall rules that grant access to networks. You must configure ingress authorization rules to
      # enable clients to access resources in Amazon Web Services or on-premises networks.

      def authorize_client_vpn_ingress(
        client_vpn_endpoint_id : String,
        target_network_cidr : String,
        access_group_id : String? = nil,
        authorize_all_groups : Bool? = nil,
        client_token : String? = nil,
        description : String? = nil,
        dry_run : Bool? = nil
      ) : Types::AuthorizeClientVpnIngressResult

        input = Types::AuthorizeClientVpnIngressRequest.new(client_vpn_endpoint_id: client_vpn_endpoint_id, target_network_cidr: target_network_cidr, access_group_id: access_group_id, authorize_all_groups: authorize_all_groups, client_token: client_token, description: description, dry_run: dry_run)
        authorize_client_vpn_ingress(input)
      end

      def authorize_client_vpn_ingress(input : Types::AuthorizeClientVpnIngressRequest) : Types::AuthorizeClientVpnIngressResult
        request = Protocol::Ec2.build_request(Model::AUTHORIZE_CLIENT_VPN_INGRESS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AuthorizeClientVpnIngressResult, "AuthorizeClientVpnIngress")
      end

      # Adds the specified outbound (egress) rules to a security group. An outbound rule permits instances
      # to send traffic to the specified IPv4 or IPv6 address ranges, the IP address ranges specified by a
      # prefix list, or the instances that are associated with a source security group. For more
      # information, see Security group rules . You must specify exactly one of the following destinations:
      # an IPv4 or IPv6 address range, a prefix list, or a security group. You must specify a protocol for
      # each rule (for example, TCP). If the protocol is TCP or UDP, you must also specify a port or port
      # range. If the protocol is ICMP or ICMPv6, you must also specify the ICMP type and code. Rule changes
      # are propagated to instances associated with the security group as quickly as possible. However, a
      # small delay might occur. For examples of rules that you can add to security groups for specific
      # access scenarios, see Security group rules for different use cases in the Amazon EC2 User Guide .
      # For information about security group quotas, see Amazon VPC quotas in the Amazon VPC User Guide .

      def authorize_security_group_egress(
        group_id : String,
        cidr_ip : String? = nil,
        dry_run : Bool? = nil,
        from_port : Int32? = nil,
        ip_permissions : Array(Types::IpPermission)? = nil,
        ip_protocol : String? = nil,
        source_security_group_name : String? = nil,
        source_security_group_owner_id : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        to_port : Int32? = nil
      ) : Types::AuthorizeSecurityGroupEgressResult

        input = Types::AuthorizeSecurityGroupEgressRequest.new(group_id: group_id, cidr_ip: cidr_ip, dry_run: dry_run, from_port: from_port, ip_permissions: ip_permissions, ip_protocol: ip_protocol, source_security_group_name: source_security_group_name, source_security_group_owner_id: source_security_group_owner_id, tag_specifications: tag_specifications, to_port: to_port)
        authorize_security_group_egress(input)
      end

      def authorize_security_group_egress(input : Types::AuthorizeSecurityGroupEgressRequest) : Types::AuthorizeSecurityGroupEgressResult
        request = Protocol::Ec2.build_request(Model::AUTHORIZE_SECURITY_GROUP_EGRESS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AuthorizeSecurityGroupEgressResult, "AuthorizeSecurityGroupEgress")
      end

      # Adds the specified inbound (ingress) rules to a security group. An inbound rule permits instances to
      # receive traffic from the specified IPv4 or IPv6 address range, the IP address ranges that are
      # specified by a prefix list, or the instances that are associated with a destination security group.
      # For more information, see Security group rules . You must specify exactly one of the following
      # sources: an IPv4 or IPv6 address range, a prefix list, or a security group. You must specify a
      # protocol for each rule (for example, TCP). If the protocol is TCP or UDP, you must also specify a
      # port or port range. If the protocol is ICMP or ICMPv6, you must also specify the ICMP/ICMPv6 type
      # and code. Rule changes are propagated to instances associated with the security group as quickly as
      # possible. However, a small delay might occur. For examples of rules that you can add to security
      # groups for specific access scenarios, see Security group rules for different use cases in the Amazon
      # EC2 User Guide . For more information about security group quotas, see Amazon VPC quotas in the
      # Amazon VPC User Guide .

      def authorize_security_group_ingress(
        cidr_ip : String? = nil,
        dry_run : Bool? = nil,
        from_port : Int32? = nil,
        group_id : String? = nil,
        group_name : String? = nil,
        ip_permissions : Array(Types::IpPermission)? = nil,
        ip_protocol : String? = nil,
        source_security_group_name : String? = nil,
        source_security_group_owner_id : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        to_port : Int32? = nil
      ) : Types::AuthorizeSecurityGroupIngressResult

        input = Types::AuthorizeSecurityGroupIngressRequest.new(cidr_ip: cidr_ip, dry_run: dry_run, from_port: from_port, group_id: group_id, group_name: group_name, ip_permissions: ip_permissions, ip_protocol: ip_protocol, source_security_group_name: source_security_group_name, source_security_group_owner_id: source_security_group_owner_id, tag_specifications: tag_specifications, to_port: to_port)
        authorize_security_group_ingress(input)
      end

      def authorize_security_group_ingress(input : Types::AuthorizeSecurityGroupIngressRequest) : Types::AuthorizeSecurityGroupIngressResult
        request = Protocol::Ec2.build_request(Model::AUTHORIZE_SECURITY_GROUP_INGRESS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::AuthorizeSecurityGroupIngressResult, "AuthorizeSecurityGroupIngress")
      end

      # Bundles an Amazon instance store-backed Windows instance. During bundling, only the root device
      # volume (C:\) is bundled. Data on other instance store volumes is not preserved. This action is not
      # applicable for Linux/Unix instances or Windows instances that are backed by Amazon EBS.

      def bundle_instance(
        instance_id : String,
        storage : Types::Storage,
        dry_run : Bool? = nil
      ) : Types::BundleInstanceResult

        input = Types::BundleInstanceRequest.new(instance_id: instance_id, storage: storage, dry_run: dry_run)
        bundle_instance(input)
      end

      def bundle_instance(input : Types::BundleInstanceRequest) : Types::BundleInstanceResult
        request = Protocol::Ec2.build_request(Model::BUNDLE_INSTANCE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::BundleInstanceResult, "BundleInstance")
      end

      # Cancels a bundling operation for an instance store-backed Windows instance.

      def cancel_bundle_task(
        bundle_id : String,
        dry_run : Bool? = nil
      ) : Types::CancelBundleTaskResult

        input = Types::CancelBundleTaskRequest.new(bundle_id: bundle_id, dry_run: dry_run)
        cancel_bundle_task(input)
      end

      def cancel_bundle_task(input : Types::CancelBundleTaskRequest) : Types::CancelBundleTaskResult
        request = Protocol::Ec2.build_request(Model::CANCEL_BUNDLE_TASK, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CancelBundleTaskResult, "CancelBundleTask")
      end

      # Cancels the specified Capacity Reservation, releases the reserved capacity, and changes the Capacity
      # Reservation's state to cancelled . You can cancel a Capacity Reservation that is in the following
      # states: assessing active and there is no commitment duration or the commitment duration has elapsed.
      # You can't cancel a future-dated Capacity Reservation during the commitment duration. You can't
      # modify or cancel a Capacity Block. For more information, see Capacity Blocks for ML . If a
      # future-dated Capacity Reservation enters the delayed state, the commitment duration is waived, and
      # you can cancel it as soon as it enters the active state. Instances running in the reserved capacity
      # continue running until you stop them. Stopped instances that target the Capacity Reservation can no
      # longer launch. Modify these instances to either target a different Capacity Reservation, launch
      # On-Demand Instance capacity, or run in any open Capacity Reservation that has matching attributes
      # and sufficient capacity.

      def cancel_capacity_reservation(
        capacity_reservation_id : String,
        dry_run : Bool? = nil
      ) : Types::CancelCapacityReservationResult

        input = Types::CancelCapacityReservationRequest.new(capacity_reservation_id: capacity_reservation_id, dry_run: dry_run)
        cancel_capacity_reservation(input)
      end

      def cancel_capacity_reservation(input : Types::CancelCapacityReservationRequest) : Types::CancelCapacityReservationResult
        request = Protocol::Ec2.build_request(Model::CANCEL_CAPACITY_RESERVATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CancelCapacityReservationResult, "CancelCapacityReservation")
      end

      # Cancels one or more Capacity Reservation Fleets. When you cancel a Capacity Reservation Fleet, the
      # following happens: The Capacity Reservation Fleet's status changes to cancelled . The individual
      # Capacity Reservations in the Fleet are cancelled. Instances running in the Capacity Reservations at
      # the time of cancelling the Fleet continue to run in shared capacity. The Fleet stops creating new
      # Capacity Reservations.

      def cancel_capacity_reservation_fleets(
        capacity_reservation_fleet_ids : Array(String),
        dry_run : Bool? = nil
      ) : Types::CancelCapacityReservationFleetsResult

        input = Types::CancelCapacityReservationFleetsRequest.new(capacity_reservation_fleet_ids: capacity_reservation_fleet_ids, dry_run: dry_run)
        cancel_capacity_reservation_fleets(input)
      end

      def cancel_capacity_reservation_fleets(input : Types::CancelCapacityReservationFleetsRequest) : Types::CancelCapacityReservationFleetsResult
        request = Protocol::Ec2.build_request(Model::CANCEL_CAPACITY_RESERVATION_FLEETS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CancelCapacityReservationFleetsResult, "CancelCapacityReservationFleets")
      end

      # Cancels an active conversion task. The task can be the import of an instance or volume. The action
      # removes all artifacts of the conversion, including a partially uploaded volume or instance. If the
      # conversion is complete or is in the process of transferring the final disk image, the command fails
      # and returns an exception.

      def cancel_conversion_task(
        conversion_task_id : String,
        dry_run : Bool? = nil,
        reason_message : String? = nil
      ) : Nil

        input = Types::CancelConversionRequest.new(conversion_task_id: conversion_task_id, dry_run: dry_run, reason_message: reason_message)
        cancel_conversion_task(input)
      end

      def cancel_conversion_task(input : Types::CancelConversionRequest) : Nil
        request = Protocol::Ec2.build_request(Model::CANCEL_CONVERSION_TASK, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Cancels the generation of an account status report. You can only cancel a report while it has the
      # running status. Reports with other statuses ( complete , cancelled , or error ) can't be canceled.
      # For more information, see Generating the account status report for declarative policies in the
      # Amazon Web Services Organizations User Guide .

      def cancel_declarative_policies_report(
        report_id : String,
        dry_run : Bool? = nil
      ) : Types::CancelDeclarativePoliciesReportResult

        input = Types::CancelDeclarativePoliciesReportRequest.new(report_id: report_id, dry_run: dry_run)
        cancel_declarative_policies_report(input)
      end

      def cancel_declarative_policies_report(input : Types::CancelDeclarativePoliciesReportRequest) : Types::CancelDeclarativePoliciesReportResult
        request = Protocol::Ec2.build_request(Model::CANCEL_DECLARATIVE_POLICIES_REPORT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CancelDeclarativePoliciesReportResult, "CancelDeclarativePoliciesReport")
      end

      # Cancels an active export task. The request removes all artifacts of the export, including any
      # partially-created Amazon S3 objects. If the export task is complete or is in the process of
      # transferring the final disk image, the command fails and returns an error.

      def cancel_export_task(
        export_task_id : String
      ) : Nil

        input = Types::CancelExportTaskRequest.new(export_task_id: export_task_id)
        cancel_export_task(input)
      end

      def cancel_export_task(input : Types::CancelExportTaskRequest) : Nil
        request = Protocol::Ec2.build_request(Model::CANCEL_EXPORT_TASK, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Removes your Amazon Web Services account from the launch permissions for the specified AMI. For more
      # information, see Cancel having an AMI shared with your Amazon Web Services account in the Amazon EC2
      # User Guide .

      def cancel_image_launch_permission(
        image_id : String,
        dry_run : Bool? = nil
      ) : Types::CancelImageLaunchPermissionResult

        input = Types::CancelImageLaunchPermissionRequest.new(image_id: image_id, dry_run: dry_run)
        cancel_image_launch_permission(input)
      end

      def cancel_image_launch_permission(input : Types::CancelImageLaunchPermissionRequest) : Types::CancelImageLaunchPermissionResult
        request = Protocol::Ec2.build_request(Model::CANCEL_IMAGE_LAUNCH_PERMISSION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CancelImageLaunchPermissionResult, "CancelImageLaunchPermission")
      end

      # Cancels an in-process import virtual machine or import snapshot task.

      def cancel_import_task(
        cancel_reason : String? = nil,
        dry_run : Bool? = nil,
        import_task_id : String? = nil
      ) : Types::CancelImportTaskResult

        input = Types::CancelImportTaskRequest.new(cancel_reason: cancel_reason, dry_run: dry_run, import_task_id: import_task_id)
        cancel_import_task(input)
      end

      def cancel_import_task(input : Types::CancelImportTaskRequest) : Types::CancelImportTaskResult
        request = Protocol::Ec2.build_request(Model::CANCEL_IMPORT_TASK, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CancelImportTaskResult, "CancelImportTask")
      end

      # Cancels the specified Reserved Instance listing in the Reserved Instance Marketplace. For more
      # information, see Sell in the Reserved Instance Marketplace in the Amazon EC2 User Guide .

      def cancel_reserved_instances_listing(
        reserved_instances_listing_id : String
      ) : Types::CancelReservedInstancesListingResult

        input = Types::CancelReservedInstancesListingRequest.new(reserved_instances_listing_id: reserved_instances_listing_id)
        cancel_reserved_instances_listing(input)
      end

      def cancel_reserved_instances_listing(input : Types::CancelReservedInstancesListingRequest) : Types::CancelReservedInstancesListingResult
        request = Protocol::Ec2.build_request(Model::CANCEL_RESERVED_INSTANCES_LISTING, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CancelReservedInstancesListingResult, "CancelReservedInstancesListing")
      end

      # Cancels the specified Spot Fleet requests. After you cancel a Spot Fleet request, the Spot Fleet
      # launches no new instances. You must also specify whether a canceled Spot Fleet request should
      # terminate its instances. If you choose to terminate the instances, the Spot Fleet request enters the
      # cancelled_terminating state. Otherwise, the Spot Fleet request enters the cancelled_running state
      # and the instances continue to run until they are interrupted or you terminate them manually.
      # Terminating an instance is permanent and irreversible. After you terminate an instance, you can no
      # longer connect to it, and it can't be recovered. All attached Amazon EBS volumes that are configured
      # to be deleted on termination are also permanently deleted and can't be recovered. All data stored on
      # instance store volumes is permanently lost. For more information, see How instance termination works
      # . Before you terminate an instance, ensure that you have backed up all data that you need to retain
      # after the termination to persistent storage. Restrictions You can delete up to 100 fleets in a
      # single request. If you exceed the specified number, no fleets are deleted.

      def cancel_spot_fleet_requests(
        spot_fleet_request_ids : Array(String),
        terminate_instances : Bool,
        dry_run : Bool? = nil
      ) : Types::CancelSpotFleetRequestsResponse

        input = Types::CancelSpotFleetRequestsRequest.new(spot_fleet_request_ids: spot_fleet_request_ids, terminate_instances: terminate_instances, dry_run: dry_run)
        cancel_spot_fleet_requests(input)
      end

      def cancel_spot_fleet_requests(input : Types::CancelSpotFleetRequestsRequest) : Types::CancelSpotFleetRequestsResponse
        request = Protocol::Ec2.build_request(Model::CANCEL_SPOT_FLEET_REQUESTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CancelSpotFleetRequestsResponse, "CancelSpotFleetRequests")
      end

      # Cancels one or more Spot Instance requests. Canceling a Spot Instance request does not terminate
      # running Spot Instances associated with the request.

      def cancel_spot_instance_requests(
        spot_instance_request_ids : Array(String),
        dry_run : Bool? = nil
      ) : Types::CancelSpotInstanceRequestsResult

        input = Types::CancelSpotInstanceRequestsRequest.new(spot_instance_request_ids: spot_instance_request_ids, dry_run: dry_run)
        cancel_spot_instance_requests(input)
      end

      def cancel_spot_instance_requests(input : Types::CancelSpotInstanceRequestsRequest) : Types::CancelSpotInstanceRequestsResult
        request = Protocol::Ec2.build_request(Model::CANCEL_SPOT_INSTANCE_REQUESTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CancelSpotInstanceRequestsResult, "CancelSpotInstanceRequests")
      end

      # Determines whether a product code is associated with an instance. This action can only be used by
      # the owner of the product code. It is useful when a product code owner must verify whether another
      # user's instance is eligible for support.

      def confirm_product_instance(
        instance_id : String,
        product_code : String,
        dry_run : Bool? = nil
      ) : Types::ConfirmProductInstanceResult

        input = Types::ConfirmProductInstanceRequest.new(instance_id: instance_id, product_code: product_code, dry_run: dry_run)
        confirm_product_instance(input)
      end

      def confirm_product_instance(input : Types::ConfirmProductInstanceRequest) : Types::ConfirmProductInstanceResult
        request = Protocol::Ec2.build_request(Model::CONFIRM_PRODUCT_INSTANCE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ConfirmProductInstanceResult, "ConfirmProductInstance")
      end

      # Copies the specified Amazon FPGA Image (AFI) to the current Region.

      def copy_fpga_image(
        source_fpga_image_id : String,
        source_region : String,
        client_token : String? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        name : String? = nil
      ) : Types::CopyFpgaImageResult

        input = Types::CopyFpgaImageRequest.new(source_fpga_image_id: source_fpga_image_id, source_region: source_region, client_token: client_token, description: description, dry_run: dry_run, name: name)
        copy_fpga_image(input)
      end

      def copy_fpga_image(input : Types::CopyFpgaImageRequest) : Types::CopyFpgaImageResult
        request = Protocol::Ec2.build_request(Model::COPY_FPGA_IMAGE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CopyFpgaImageResult, "CopyFpgaImage")
      end

      # Initiates an AMI copy operation. You must specify the source AMI ID and both the source and
      # destination locations. The copy operation must be initiated in the destination Region. CopyImage
      # supports the following source to destination copies: Region to Region Region to Outpost Parent
      # Region to Local Zone Local Zone to parent Region Between Local Zones with the same parent Region
      # (only supported for certain Local Zones) CopyImage does not support the following source to
      # destination copies: Local Zone to non-parent Regions Between Local Zones with different parent
      # Regions Local Zone to Outpost Outpost to Local Zone Outpost to Region Between Outposts Within same
      # Outpost Cross-partition copies (use CreateStoreImageTask instead) Destination specification Region
      # to Region: The destination Region is the Region in which you initiate the copy operation. Region to
      # Outpost: Specify the destination using the DestinationOutpostArn parameter (the ARN of the Outpost)
      # Region to Local Zone, and Local Zone to Local Zone copies: Specify the destination using the
      # DestinationAvailabilityZone parameter (the name of the destination Local Zone) or
      # DestinationAvailabilityZoneId parameter (the ID of the destination Local Zone). Snapshot encryption
      # Region to Outpost: Backing snapshots copied to an Outpost are encrypted by default using the default
      # encryption key for the Region or the key that you specify. Outposts do not support unencrypted
      # snapshots. Region to Local Zone, and Local Zone to Local Zone: Not all Local Zones require encrypted
      # snapshots. In Local Zones that require encrypted snapshots, backing snapshots are automatically
      # encrypted during copy. In Local Zones where encryption is not required, snapshots retain their
      # original encryption state (encrypted or unencrypted) by default. For more information, including the
      # required permissions for copying an AMI, see Copy an Amazon EC2 AMI in the Amazon EC2 User Guide .

      def copy_image(
        name : String,
        source_image_id : String,
        source_region : String,
        client_token : String? = nil,
        copy_image_tags : Bool? = nil,
        description : String? = nil,
        destination_availability_zone : String? = nil,
        destination_availability_zone_id : String? = nil,
        destination_outpost_arn : String? = nil,
        dry_run : Bool? = nil,
        encrypted : Bool? = nil,
        kms_key_id : String? = nil,
        snapshot_copy_completion_duration_minutes : Int64? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CopyImageResult

        input = Types::CopyImageRequest.new(name: name, source_image_id: source_image_id, source_region: source_region, client_token: client_token, copy_image_tags: copy_image_tags, description: description, destination_availability_zone: destination_availability_zone, destination_availability_zone_id: destination_availability_zone_id, destination_outpost_arn: destination_outpost_arn, dry_run: dry_run, encrypted: encrypted, kms_key_id: kms_key_id, snapshot_copy_completion_duration_minutes: snapshot_copy_completion_duration_minutes, tag_specifications: tag_specifications)
        copy_image(input)
      end

      def copy_image(input : Types::CopyImageRequest) : Types::CopyImageResult
        request = Protocol::Ec2.build_request(Model::COPY_IMAGE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CopyImageResult, "CopyImage")
      end

      # Creates an exact copy of an Amazon EBS snapshot. The location of the source snapshot determines
      # whether you can copy it or not, and the allowed destinations for the snapshot copy. If the source
      # snapshot is in a Region, you can copy it within that Region, to another Region, to an Outpost
      # associated with that Region, or to a Local Zone in that Region. If the source snapshot is in a Local
      # Zone, you can copy it within that Local Zone, to another Local Zone in the same zone group, or to
      # the parent Region of the Local Zone. If the source snapshot is on an Outpost, you can't copy it.
      # When copying snapshots to a Region, the encryption outcome for the snapshot copy depends on the
      # Amazon EBS encryption by default setting for the destination Region, the encryption status of the
      # source snapshot, and the encryption parameters you specify in the request. For more information, see
      # Encryption and snapshot copying . Snapshots copied to an Outpost must be encrypted. Unencrypted
      # snapshots are not supported on Outposts. For more information, Amazon EBS local snapshots on
      # Outposts . Snapshots copies have an arbitrary source volume ID. Do not use this volume ID for any
      # purpose. For more information, see Copy an Amazon EBS snapshot in the Amazon EBS User Guide .

      def copy_snapshot(
        source_region : String,
        source_snapshot_id : String,
        completion_duration_minutes : Int32? = nil,
        description : String? = nil,
        destination_availability_zone : String? = nil,
        destination_outpost_arn : String? = nil,
        destination_region : String? = nil,
        dry_run : Bool? = nil,
        encrypted : Bool? = nil,
        kms_key_id : String? = nil,
        presigned_url : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CopySnapshotResult

        input = Types::CopySnapshotRequest.new(source_region: source_region, source_snapshot_id: source_snapshot_id, completion_duration_minutes: completion_duration_minutes, description: description, destination_availability_zone: destination_availability_zone, destination_outpost_arn: destination_outpost_arn, destination_region: destination_region, dry_run: dry_run, encrypted: encrypted, kms_key_id: kms_key_id, presigned_url: presigned_url, tag_specifications: tag_specifications)
        copy_snapshot(input)
      end

      def copy_snapshot(input : Types::CopySnapshotRequest) : Types::CopySnapshotResult
        request = Protocol::Ec2.build_request(Model::COPY_SNAPSHOT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CopySnapshotResult, "CopySnapshot")
      end

      # Creates a crash-consistent, point-in-time copy of an existing Amazon EBS volume within the same
      # Availability Zone. The volume copy can be attached to an Amazon EC2 instance once it reaches the
      # available state. For more information, see Copy an Amazon EBS volume .

      def copy_volumes(
        source_volume_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        iops : Int32? = nil,
        multi_attach_enabled : Bool? = nil,
        size : Int32? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        throughput : Int32? = nil,
        volume_type : String? = nil
      ) : Types::CopyVolumesResult

        input = Types::CopyVolumesRequest.new(source_volume_id: source_volume_id, client_token: client_token, dry_run: dry_run, iops: iops, multi_attach_enabled: multi_attach_enabled, size: size, tag_specifications: tag_specifications, throughput: throughput, volume_type: volume_type)
        copy_volumes(input)
      end

      def copy_volumes(input : Types::CopyVolumesRequest) : Types::CopyVolumesResult
        request = Protocol::Ec2.build_request(Model::COPY_VOLUMES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CopyVolumesResult, "CopyVolumes")
      end

      # Creates a new data export configuration for EC2 Capacity Manager. This allows you to automatically
      # export capacity usage data to an S3 bucket on a scheduled basis. The exported data includes metrics
      # for On-Demand, Spot, and Capacity Reservations usage across your organization.

      def create_capacity_manager_data_export(
        output_format : String,
        s3_bucket_name : String,
        schedule : String,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        s3_bucket_prefix : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateCapacityManagerDataExportResult

        input = Types::CreateCapacityManagerDataExportRequest.new(output_format: output_format, s3_bucket_name: s3_bucket_name, schedule: schedule, client_token: client_token, dry_run: dry_run, s3_bucket_prefix: s3_bucket_prefix, tag_specifications: tag_specifications)
        create_capacity_manager_data_export(input)
      end

      def create_capacity_manager_data_export(input : Types::CreateCapacityManagerDataExportRequest) : Types::CreateCapacityManagerDataExportResult
        request = Protocol::Ec2.build_request(Model::CREATE_CAPACITY_MANAGER_DATA_EXPORT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateCapacityManagerDataExportResult, "CreateCapacityManagerDataExport")
      end

      # Creates a new Capacity Reservation with the specified attributes. Capacity Reservations enable you
      # to reserve capacity for your Amazon EC2 instances in a specific Availability Zone for any duration.
      # You can create a Capacity Reservation at any time, and you can choose when it starts. You can create
      # a Capacity Reservation for immediate use or you can request a Capacity Reservation for a future
      # date. For more information, see Reserve compute capacity with On-Demand Capacity Reservations in the
      # Amazon EC2 User Guide . Your request to create a Capacity Reservation could fail if: Amazon EC2 does
      # not have sufficient capacity. In this case, try again at a later time, try in a different
      # Availability Zone, or request a smaller Capacity Reservation. If your workload is flexible across
      # instance types and sizes, try with different instance attributes. The requested quantity exceeds
      # your On-Demand Instance quota. In this case, increase your On-Demand Instance quota for the
      # requested instance type and try again. For more information, see Amazon EC2 Service Quotas in the
      # Amazon EC2 User Guide .

      def create_capacity_reservation(
        instance_count : Int32,
        instance_platform : String,
        instance_type : String,
        availability_zone : String? = nil,
        availability_zone_id : String? = nil,
        client_token : String? = nil,
        commitment_duration : Int64? = nil,
        delivery_preference : String? = nil,
        dry_run : Bool? = nil,
        ebs_optimized : Bool? = nil,
        end_date : Time? = nil,
        end_date_type : String? = nil,
        ephemeral_storage : Bool? = nil,
        instance_match_criteria : String? = nil,
        outpost_arn : String? = nil,
        placement_group_arn : String? = nil,
        start_date : Time? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        tenancy : String? = nil
      ) : Types::CreateCapacityReservationResult

        input = Types::CreateCapacityReservationRequest.new(instance_count: instance_count, instance_platform: instance_platform, instance_type: instance_type, availability_zone: availability_zone, availability_zone_id: availability_zone_id, client_token: client_token, commitment_duration: commitment_duration, delivery_preference: delivery_preference, dry_run: dry_run, ebs_optimized: ebs_optimized, end_date: end_date, end_date_type: end_date_type, ephemeral_storage: ephemeral_storage, instance_match_criteria: instance_match_criteria, outpost_arn: outpost_arn, placement_group_arn: placement_group_arn, start_date: start_date, tag_specifications: tag_specifications, tenancy: tenancy)
        create_capacity_reservation(input)
      end

      def create_capacity_reservation(input : Types::CreateCapacityReservationRequest) : Types::CreateCapacityReservationResult
        request = Protocol::Ec2.build_request(Model::CREATE_CAPACITY_RESERVATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateCapacityReservationResult, "CreateCapacityReservation")
      end

      # Create a new Capacity Reservation by splitting the capacity of the source Capacity Reservation. The
      # new Capacity Reservation will have the same attributes as the source Capacity Reservation except for
      # tags. The source Capacity Reservation must be active and owned by your Amazon Web Services account.

      def create_capacity_reservation_by_splitting(
        instance_count : Int32,
        source_capacity_reservation_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateCapacityReservationBySplittingResult

        input = Types::CreateCapacityReservationBySplittingRequest.new(instance_count: instance_count, source_capacity_reservation_id: source_capacity_reservation_id, client_token: client_token, dry_run: dry_run, tag_specifications: tag_specifications)
        create_capacity_reservation_by_splitting(input)
      end

      def create_capacity_reservation_by_splitting(input : Types::CreateCapacityReservationBySplittingRequest) : Types::CreateCapacityReservationBySplittingResult
        request = Protocol::Ec2.build_request(Model::CREATE_CAPACITY_RESERVATION_BY_SPLITTING, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateCapacityReservationBySplittingResult, "CreateCapacityReservationBySplitting")
      end

      # Creates a Capacity Reservation Fleet. For more information, see Create a Capacity Reservation Fleet
      # in the Amazon EC2 User Guide .

      def create_capacity_reservation_fleet(
        instance_type_specifications : Array(Types::ReservationFleetInstanceSpecification),
        total_target_capacity : Int32,
        allocation_strategy : String? = nil,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        end_date : Time? = nil,
        instance_match_criteria : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        tenancy : String? = nil
      ) : Types::CreateCapacityReservationFleetResult

        input = Types::CreateCapacityReservationFleetRequest.new(instance_type_specifications: instance_type_specifications, total_target_capacity: total_target_capacity, allocation_strategy: allocation_strategy, client_token: client_token, dry_run: dry_run, end_date: end_date, instance_match_criteria: instance_match_criteria, tag_specifications: tag_specifications, tenancy: tenancy)
        create_capacity_reservation_fleet(input)
      end

      def create_capacity_reservation_fleet(input : Types::CreateCapacityReservationFleetRequest) : Types::CreateCapacityReservationFleetResult
        request = Protocol::Ec2.build_request(Model::CREATE_CAPACITY_RESERVATION_FLEET, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateCapacityReservationFleetResult, "CreateCapacityReservationFleet")
      end

      # Creates a carrier gateway. For more information about carrier gateways, see Carrier gateways in the
      # Amazon Web Services Wavelength Developer Guide .

      def create_carrier_gateway(
        vpc_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateCarrierGatewayResult

        input = Types::CreateCarrierGatewayRequest.new(vpc_id: vpc_id, client_token: client_token, dry_run: dry_run, tag_specifications: tag_specifications)
        create_carrier_gateway(input)
      end

      def create_carrier_gateway(input : Types::CreateCarrierGatewayRequest) : Types::CreateCarrierGatewayResult
        request = Protocol::Ec2.build_request(Model::CREATE_CARRIER_GATEWAY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateCarrierGatewayResult, "CreateCarrierGateway")
      end

      # Creates a Client VPN endpoint. A Client VPN endpoint is the resource you create and configure to
      # enable and manage client VPN sessions. It is the destination endpoint at which all client VPN
      # sessions are terminated.

      def create_client_vpn_endpoint(
        authentication_options : Array(Types::ClientVpnAuthenticationRequest),
        connection_log_options : Types::ConnectionLogOptions,
        server_certificate_arn : String,
        client_cidr_block : String? = nil,
        client_connect_options : Types::ClientConnectOptions? = nil,
        client_login_banner_options : Types::ClientLoginBannerOptions? = nil,
        client_route_enforcement_options : Types::ClientRouteEnforcementOptions? = nil,
        client_token : String? = nil,
        description : String? = nil,
        disconnect_on_session_timeout : Bool? = nil,
        dns_servers : Array(String)? = nil,
        dry_run : Bool? = nil,
        endpoint_ip_address_type : String? = nil,
        security_group_ids : Array(String)? = nil,
        self_service_portal : String? = nil,
        session_timeout_hours : Int32? = nil,
        split_tunnel : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        traffic_ip_address_type : String? = nil,
        transport_protocol : String? = nil,
        vpc_id : String? = nil,
        vpn_port : Int32? = nil
      ) : Types::CreateClientVpnEndpointResult

        input = Types::CreateClientVpnEndpointRequest.new(authentication_options: authentication_options, connection_log_options: connection_log_options, server_certificate_arn: server_certificate_arn, client_cidr_block: client_cidr_block, client_connect_options: client_connect_options, client_login_banner_options: client_login_banner_options, client_route_enforcement_options: client_route_enforcement_options, client_token: client_token, description: description, disconnect_on_session_timeout: disconnect_on_session_timeout, dns_servers: dns_servers, dry_run: dry_run, endpoint_ip_address_type: endpoint_ip_address_type, security_group_ids: security_group_ids, self_service_portal: self_service_portal, session_timeout_hours: session_timeout_hours, split_tunnel: split_tunnel, tag_specifications: tag_specifications, traffic_ip_address_type: traffic_ip_address_type, transport_protocol: transport_protocol, vpc_id: vpc_id, vpn_port: vpn_port)
        create_client_vpn_endpoint(input)
      end

      def create_client_vpn_endpoint(input : Types::CreateClientVpnEndpointRequest) : Types::CreateClientVpnEndpointResult
        request = Protocol::Ec2.build_request(Model::CREATE_CLIENT_VPN_ENDPOINT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateClientVpnEndpointResult, "CreateClientVpnEndpoint")
      end

      # Adds a route to a network to a Client VPN endpoint. Each Client VPN endpoint has a route table that
      # describes the available destination network routes. Each route in the route table specifies the path
      # for traﬃc to speciﬁc resources or networks.

      def create_client_vpn_route(
        client_vpn_endpoint_id : String,
        destination_cidr_block : String,
        target_vpc_subnet_id : String,
        client_token : String? = nil,
        description : String? = nil,
        dry_run : Bool? = nil
      ) : Types::CreateClientVpnRouteResult

        input = Types::CreateClientVpnRouteRequest.new(client_vpn_endpoint_id: client_vpn_endpoint_id, destination_cidr_block: destination_cidr_block, target_vpc_subnet_id: target_vpc_subnet_id, client_token: client_token, description: description, dry_run: dry_run)
        create_client_vpn_route(input)
      end

      def create_client_vpn_route(input : Types::CreateClientVpnRouteRequest) : Types::CreateClientVpnRouteResult
        request = Protocol::Ec2.build_request(Model::CREATE_CLIENT_VPN_ROUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateClientVpnRouteResult, "CreateClientVpnRoute")
      end

      # Creates a range of customer-owned IP addresses.

      def create_coip_cidr(
        cidr : String,
        coip_pool_id : String,
        dry_run : Bool? = nil
      ) : Types::CreateCoipCidrResult

        input = Types::CreateCoipCidrRequest.new(cidr: cidr, coip_pool_id: coip_pool_id, dry_run: dry_run)
        create_coip_cidr(input)
      end

      def create_coip_cidr(input : Types::CreateCoipCidrRequest) : Types::CreateCoipCidrResult
        request = Protocol::Ec2.build_request(Model::CREATE_COIP_CIDR, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateCoipCidrResult, "CreateCoipCidr")
      end

      # Creates a pool of customer-owned IP (CoIP) addresses.

      def create_coip_pool(
        local_gateway_route_table_id : String,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateCoipPoolResult

        input = Types::CreateCoipPoolRequest.new(local_gateway_route_table_id: local_gateway_route_table_id, dry_run: dry_run, tag_specifications: tag_specifications)
        create_coip_pool(input)
      end

      def create_coip_pool(input : Types::CreateCoipPoolRequest) : Types::CreateCoipPoolResult
        request = Protocol::Ec2.build_request(Model::CREATE_COIP_POOL, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateCoipPoolResult, "CreateCoipPool")
      end

      # Provides information to Amazon Web Services about your customer gateway device. The customer gateway
      # device is the appliance at your end of the VPN connection. You must provide the IP address of the
      # customer gateway device’s external interface. The IP address must be static and can be behind a
      # device performing network address translation (NAT). For devices that use Border Gateway Protocol
      # (BGP), you can also provide the device's BGP Autonomous System Number (ASN). You can use an existing
      # ASN assigned to your network. If you don't have an ASN already, you can use a private ASN. For more
      # information, see Customer gateway options for your Site-to-Site VPN connection in the Amazon Web
      # Services Site-to-Site VPN User Guide . To create more than one customer gateway with the same VPN
      # type, IP address, and BGP ASN, specify a unique device name for each customer gateway. An identical
      # request returns information about the existing customer gateway; it doesn't create a new customer
      # gateway.

      def create_customer_gateway(
        type : String,
        bgp_asn : Int32? = nil,
        bgp_asn_extended : Int64? = nil,
        certificate_arn : String? = nil,
        device_name : String? = nil,
        dry_run : Bool? = nil,
        ip_address : String? = nil,
        public_ip : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateCustomerGatewayResult

        input = Types::CreateCustomerGatewayRequest.new(type: type, bgp_asn: bgp_asn, bgp_asn_extended: bgp_asn_extended, certificate_arn: certificate_arn, device_name: device_name, dry_run: dry_run, ip_address: ip_address, public_ip: public_ip, tag_specifications: tag_specifications)
        create_customer_gateway(input)
      end

      def create_customer_gateway(input : Types::CreateCustomerGatewayRequest) : Types::CreateCustomerGatewayResult
        request = Protocol::Ec2.build_request(Model::CREATE_CUSTOMER_GATEWAY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateCustomerGatewayResult, "CreateCustomerGateway")
      end

      # Creates a default subnet with a size /20 IPv4 CIDR block in the specified Availability Zone in your
      # default VPC. You can have only one default subnet per Availability Zone. For more information, see
      # Create a default subnet in the Amazon VPC User Guide .

      def create_default_subnet(
        availability_zone : String? = nil,
        availability_zone_id : String? = nil,
        dry_run : Bool? = nil,
        ipv6_native : Bool? = nil
      ) : Types::CreateDefaultSubnetResult

        input = Types::CreateDefaultSubnetRequest.new(availability_zone: availability_zone, availability_zone_id: availability_zone_id, dry_run: dry_run, ipv6_native: ipv6_native)
        create_default_subnet(input)
      end

      def create_default_subnet(input : Types::CreateDefaultSubnetRequest) : Types::CreateDefaultSubnetResult
        request = Protocol::Ec2.build_request(Model::CREATE_DEFAULT_SUBNET, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateDefaultSubnetResult, "CreateDefaultSubnet")
      end

      # Creates a default VPC with a size /16 IPv4 CIDR block and a default subnet in each Availability
      # Zone. For more information about the components of a default VPC, see Default VPCs in the Amazon VPC
      # User Guide . You cannot specify the components of the default VPC yourself. If you deleted your
      # previous default VPC, you can create a default VPC. You cannot have more than one default VPC per
      # Region.

      def create_default_vpc(
        dry_run : Bool? = nil
      ) : Types::CreateDefaultVpcResult

        input = Types::CreateDefaultVpcRequest.new(dry_run: dry_run)
        create_default_vpc(input)
      end

      def create_default_vpc(input : Types::CreateDefaultVpcRequest) : Types::CreateDefaultVpcResult
        request = Protocol::Ec2.build_request(Model::CREATE_DEFAULT_VPC, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateDefaultVpcResult, "CreateDefaultVpc")
      end

      # Delegates ownership of the Amazon EBS root volume for an Apple silicon Mac instance to an
      # administrative user.

      def create_delegate_mac_volume_ownership_task(
        instance_id : String,
        mac_credentials : String,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateDelegateMacVolumeOwnershipTaskResult

        input = Types::CreateDelegateMacVolumeOwnershipTaskRequest.new(instance_id: instance_id, mac_credentials: mac_credentials, client_token: client_token, dry_run: dry_run, tag_specifications: tag_specifications)
        create_delegate_mac_volume_ownership_task(input)
      end

      def create_delegate_mac_volume_ownership_task(input : Types::CreateDelegateMacVolumeOwnershipTaskRequest) : Types::CreateDelegateMacVolumeOwnershipTaskResult
        request = Protocol::Ec2.build_request(Model::CREATE_DELEGATE_MAC_VOLUME_OWNERSHIP_TASK, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateDelegateMacVolumeOwnershipTaskResult, "CreateDelegateMacVolumeOwnershipTask")
      end

      # Creates a custom set of DHCP options. After you create a DHCP option set, you associate it with a
      # VPC. After you associate a DHCP option set with a VPC, all existing and newly launched instances in
      # the VPC use this set of DHCP options. The following are the individual DHCP options you can specify.
      # For more information, see DHCP option sets in the Amazon VPC User Guide . domain-name - If you're
      # using AmazonProvidedDNS in us-east-1 , specify ec2.internal . If you're using AmazonProvidedDNS in
      # any other Region, specify region.compute.internal . Otherwise, specify a custom domain name. This
      # value is used to complete unqualified DNS hostnames. Some Linux operating systems accept multiple
      # domain names separated by spaces. However, Windows and other Linux operating systems treat the value
      # as a single domain, which results in unexpected behavior. If your DHCP option set is associated with
      # a VPC that has instances running operating systems that treat the value as a single domain, specify
      # only one domain name. domain-name-servers - The IP addresses of up to four DNS servers, or
      # AmazonProvidedDNS. To specify multiple domain name servers in a single parameter, separate the IP
      # addresses using commas. To have your instances receive custom DNS hostnames as specified in
      # domain-name , you must specify a custom DNS server. ntp-servers - The IP addresses of up to eight
      # Network Time Protocol (NTP) servers (four IPv4 addresses and four IPv6 addresses).
      # netbios-name-servers - The IP addresses of up to four NetBIOS name servers. netbios-node-type - The
      # NetBIOS node type (1, 2, 4, or 8). We recommend that you specify 2. Broadcast and multicast are not
      # supported. For more information about NetBIOS node types, see RFC 2132 .
      # ipv6-address-preferred-lease-time - A value (in seconds, minutes, hours, or years) for how
      # frequently a running instance with an IPv6 assigned to it goes through DHCPv6 lease renewal.
      # Acceptable values are between 140 and 2147483647 seconds (approximately 68 years). If no value is
      # entered, the default lease time is 140 seconds. If you use long-term addressing for EC2 instances,
      # you can increase the lease time and avoid frequent lease renewal requests. Lease renewal typically
      # occurs when half of the lease time has elapsed.

      def create_dhcp_options(
        dhcp_configurations : Array(Types::NewDhcpConfiguration),
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateDhcpOptionsResult

        input = Types::CreateDhcpOptionsRequest.new(dhcp_configurations: dhcp_configurations, dry_run: dry_run, tag_specifications: tag_specifications)
        create_dhcp_options(input)
      end

      def create_dhcp_options(input : Types::CreateDhcpOptionsRequest) : Types::CreateDhcpOptionsResult
        request = Protocol::Ec2.build_request(Model::CREATE_DHCP_OPTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateDhcpOptionsResult, "CreateDhcpOptions")
      end

      # [IPv6 only] Creates an egress-only internet gateway for your VPC. An egress-only internet gateway is
      # used to enable outbound communication over IPv6 from instances in your VPC to the internet, and
      # prevents hosts outside of your VPC from initiating an IPv6 connection with your instance.

      def create_egress_only_internet_gateway(
        vpc_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateEgressOnlyInternetGatewayResult

        input = Types::CreateEgressOnlyInternetGatewayRequest.new(vpc_id: vpc_id, client_token: client_token, dry_run: dry_run, tag_specifications: tag_specifications)
        create_egress_only_internet_gateway(input)
      end

      def create_egress_only_internet_gateway(input : Types::CreateEgressOnlyInternetGatewayRequest) : Types::CreateEgressOnlyInternetGatewayResult
        request = Protocol::Ec2.build_request(Model::CREATE_EGRESS_ONLY_INTERNET_GATEWAY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateEgressOnlyInternetGatewayResult, "CreateEgressOnlyInternetGateway")
      end

      # Creates an EC2 Fleet that contains the configuration information for On-Demand Instances and Spot
      # Instances. Instances are launched immediately if there is available capacity. A single EC2 Fleet can
      # include multiple launch specifications that vary by instance type, AMI, Availability Zone, or
      # subnet. For more information, see EC2 Fleet in the Amazon EC2 User Guide .

      def create_fleet(
        launch_template_configs : Array(Types::FleetLaunchTemplateConfigRequest),
        target_capacity_specification : Types::TargetCapacitySpecificationRequest,
        client_token : String? = nil,
        context : String? = nil,
        dry_run : Bool? = nil,
        excess_capacity_termination_policy : String? = nil,
        on_demand_options : Types::OnDemandOptionsRequest? = nil,
        replace_unhealthy_instances : Bool? = nil,
        spot_options : Types::SpotOptionsRequest? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        terminate_instances_with_expiration : Bool? = nil,
        type : String? = nil,
        valid_from : Time? = nil,
        valid_until : Time? = nil
      ) : Types::CreateFleetResult

        input = Types::CreateFleetRequest.new(launch_template_configs: launch_template_configs, target_capacity_specification: target_capacity_specification, client_token: client_token, context: context, dry_run: dry_run, excess_capacity_termination_policy: excess_capacity_termination_policy, on_demand_options: on_demand_options, replace_unhealthy_instances: replace_unhealthy_instances, spot_options: spot_options, tag_specifications: tag_specifications, terminate_instances_with_expiration: terminate_instances_with_expiration, type: type, valid_from: valid_from, valid_until: valid_until)
        create_fleet(input)
      end

      def create_fleet(input : Types::CreateFleetRequest) : Types::CreateFleetResult
        request = Protocol::Ec2.build_request(Model::CREATE_FLEET, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateFleetResult, "CreateFleet")
      end

      # Creates one or more flow logs to capture information about IP traffic for a specific network
      # interface, subnet, or VPC. Flow log data for a monitored network interface is recorded as flow log
      # records, which are log events consisting of fields that describe the traffic flow. For more
      # information, see Flow log records in the Amazon VPC User Guide . When publishing to CloudWatch Logs,
      # flow log records are published to a log group, and each network interface has a unique log stream in
      # the log group. When publishing to Amazon S3, flow log records for all of the monitored network
      # interfaces are published to a single log file object that is stored in the specified bucket. For
      # more information, see VPC Flow Logs in the Amazon VPC User Guide .

      def create_flow_logs(
        resource_ids : Array(String),
        resource_type : String,
        client_token : String? = nil,
        deliver_cross_account_role : String? = nil,
        deliver_logs_permission_arn : String? = nil,
        destination_options : Types::DestinationOptionsRequest? = nil,
        dry_run : Bool? = nil,
        log_destination : String? = nil,
        log_destination_type : String? = nil,
        log_format : String? = nil,
        log_group_name : String? = nil,
        max_aggregation_interval : Int32? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        traffic_type : String? = nil
      ) : Types::CreateFlowLogsResult

        input = Types::CreateFlowLogsRequest.new(resource_ids: resource_ids, resource_type: resource_type, client_token: client_token, deliver_cross_account_role: deliver_cross_account_role, deliver_logs_permission_arn: deliver_logs_permission_arn, destination_options: destination_options, dry_run: dry_run, log_destination: log_destination, log_destination_type: log_destination_type, log_format: log_format, log_group_name: log_group_name, max_aggregation_interval: max_aggregation_interval, tag_specifications: tag_specifications, traffic_type: traffic_type)
        create_flow_logs(input)
      end

      def create_flow_logs(input : Types::CreateFlowLogsRequest) : Types::CreateFlowLogsResult
        request = Protocol::Ec2.build_request(Model::CREATE_FLOW_LOGS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateFlowLogsResult, "CreateFlowLogs")
      end

      # Creates an Amazon FPGA Image (AFI) from the specified design checkpoint (DCP). The create operation
      # is asynchronous. To verify that the AFI was successfully created and is ready for use, check the
      # output logs. An AFI contains the FPGA bitstream that is ready to download to an FPGA. You can
      # securely deploy an AFI on multiple FPGA-accelerated instances. For more information, see the Amazon
      # Web Services FPGA Hardware Development Kit .

      def create_fpga_image(
        input_storage_location : Types::StorageLocation,
        client_token : String? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        logs_storage_location : Types::StorageLocation? = nil,
        name : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateFpgaImageResult

        input = Types::CreateFpgaImageRequest.new(input_storage_location: input_storage_location, client_token: client_token, description: description, dry_run: dry_run, logs_storage_location: logs_storage_location, name: name, tag_specifications: tag_specifications)
        create_fpga_image(input)
      end

      def create_fpga_image(input : Types::CreateFpgaImageRequest) : Types::CreateFpgaImageResult
        request = Protocol::Ec2.build_request(Model::CREATE_FPGA_IMAGE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateFpgaImageResult, "CreateFpgaImage")
      end

      # Creates an Amazon EBS-backed AMI from an Amazon EBS-backed instance that is either running or
      # stopped. If you customized your instance with instance store volumes or Amazon EBS volumes in
      # addition to the root device volume, the new AMI contains block device mapping information for those
      # volumes. When you launch an instance from this new AMI, the instance automatically launches with
      # those additional volumes. The location of the source instance determines where you can create the
      # snapshots of the AMI: If the source instance is in a Region, you must create the snapshots in the
      # same Region as the instance. If the source instance is in a Local Zone, you can create the snapshots
      # in the same Local Zone or in its parent Region. For more information, see Create an Amazon
      # EBS-backed AMI in the Amazon Elastic Compute Cloud User Guide .

      def create_image(
        instance_id : String,
        name : String,
        block_device_mappings : Array(Types::BlockDeviceMapping)? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        no_reboot : Bool? = nil,
        snapshot_location : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateImageResult

        input = Types::CreateImageRequest.new(instance_id: instance_id, name: name, block_device_mappings: block_device_mappings, description: description, dry_run: dry_run, no_reboot: no_reboot, snapshot_location: snapshot_location, tag_specifications: tag_specifications)
        create_image(input)
      end

      def create_image(input : Types::CreateImageRequest) : Types::CreateImageResult
        request = Protocol::Ec2.build_request(Model::CREATE_IMAGE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateImageResult, "CreateImage")
      end

      # Creates a report that shows how your image is used across other Amazon Web Services accounts. The
      # report provides visibility into which accounts are using the specified image, and how many resources
      # (EC2 instances or launch templates) are referencing it. For more information, see View your AMI
      # usage in the Amazon EC2 User Guide .

      def create_image_usage_report(
        image_id : String,
        resource_types : Array(Types::ImageUsageResourceTypeRequest),
        account_ids : Array(String)? = nil,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateImageUsageReportResult

        input = Types::CreateImageUsageReportRequest.new(image_id: image_id, resource_types: resource_types, account_ids: account_ids, client_token: client_token, dry_run: dry_run, tag_specifications: tag_specifications)
        create_image_usage_report(input)
      end

      def create_image_usage_report(input : Types::CreateImageUsageReportRequest) : Types::CreateImageUsageReportResult
        request = Protocol::Ec2.build_request(Model::CREATE_IMAGE_USAGE_REPORT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateImageUsageReportResult, "CreateImageUsageReport")
      end

      # Creates an EC2 Instance Connect Endpoint. An EC2 Instance Connect Endpoint allows you to connect to
      # an instance, without requiring the instance to have a public IPv4 or public IPv6 address. For more
      # information, see Connect to your instances using EC2 Instance Connect Endpoint in the Amazon EC2
      # User Guide .

      def create_instance_connect_endpoint(
        subnet_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        ip_address_type : String? = nil,
        preserve_client_ip : Bool? = nil,
        security_group_ids : Array(String)? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateInstanceConnectEndpointResult

        input = Types::CreateInstanceConnectEndpointRequest.new(subnet_id: subnet_id, client_token: client_token, dry_run: dry_run, ip_address_type: ip_address_type, preserve_client_ip: preserve_client_ip, security_group_ids: security_group_ids, tag_specifications: tag_specifications)
        create_instance_connect_endpoint(input)
      end

      def create_instance_connect_endpoint(input : Types::CreateInstanceConnectEndpointRequest) : Types::CreateInstanceConnectEndpointResult
        request = Protocol::Ec2.build_request(Model::CREATE_INSTANCE_CONNECT_ENDPOINT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateInstanceConnectEndpointResult, "CreateInstanceConnectEndpoint")
      end

      # Creates an event window in which scheduled events for the associated Amazon EC2 instances can run.
      # You can define either a set of time ranges or a cron expression when creating the event window, but
      # not both. All event window times are in UTC. You can create up to 200 event windows per Amazon Web
      # Services Region. When you create the event window, targets (instance IDs, Dedicated Host IDs, or
      # tags) are not yet associated with it. To ensure that the event window can be used, you must
      # associate one or more targets with it by using the AssociateInstanceEventWindow API. Event windows
      # are applicable only for scheduled events that stop, reboot, or terminate instances. Event windows
      # are not applicable for: Expedited scheduled events and network maintenance events. Unscheduled
      # maintenance such as AutoRecovery and unplanned reboots. For more information, see Define event
      # windows for scheduled events in the Amazon EC2 User Guide .

      def create_instance_event_window(
        cron_expression : String? = nil,
        dry_run : Bool? = nil,
        name : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        time_ranges : Array(Types::InstanceEventWindowTimeRangeRequest)? = nil
      ) : Types::CreateInstanceEventWindowResult

        input = Types::CreateInstanceEventWindowRequest.new(cron_expression: cron_expression, dry_run: dry_run, name: name, tag_specifications: tag_specifications, time_ranges: time_ranges)
        create_instance_event_window(input)
      end

      def create_instance_event_window(input : Types::CreateInstanceEventWindowRequest) : Types::CreateInstanceEventWindowResult
        request = Protocol::Ec2.build_request(Model::CREATE_INSTANCE_EVENT_WINDOW, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateInstanceEventWindowResult, "CreateInstanceEventWindow")
      end

      # Exports a running or stopped instance to an Amazon S3 bucket. For information about the
      # prerequisites for your Amazon S3 bucket, supported operating systems, image formats, and known
      # limitations for the types of instances you can export, see Exporting an instance as a VM Using VM
      # Import/Export in the VM Import/Export User Guide .

      def create_instance_export_task(
        export_to_s3_task : Types::ExportToS3TaskSpecification,
        instance_id : String,
        target_environment : String,
        description : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateInstanceExportTaskResult

        input = Types::CreateInstanceExportTaskRequest.new(export_to_s3_task: export_to_s3_task, instance_id: instance_id, target_environment: target_environment, description: description, tag_specifications: tag_specifications)
        create_instance_export_task(input)
      end

      def create_instance_export_task(input : Types::CreateInstanceExportTaskRequest) : Types::CreateInstanceExportTaskResult
        request = Protocol::Ec2.build_request(Model::CREATE_INSTANCE_EXPORT_TASK, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateInstanceExportTaskResult, "CreateInstanceExportTask")
      end

      # Creates an internet gateway for use with a VPC. After creating the internet gateway, you attach it
      # to a VPC using AttachInternetGateway . For more information, see Internet gateways in the Amazon VPC
      # User Guide .

      def create_internet_gateway(
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateInternetGatewayResult

        input = Types::CreateInternetGatewayRequest.new(dry_run: dry_run, tag_specifications: tag_specifications)
        create_internet_gateway(input)
      end

      def create_internet_gateway(input : Types::CreateInternetGatewayRequest) : Types::CreateInternetGatewayResult
        request = Protocol::Ec2.build_request(Model::CREATE_INTERNET_GATEWAY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateInternetGatewayResult, "CreateInternetGateway")
      end

      # Creates an interruptible Capacity Reservation by specifying the number of unused instances you want
      # to allocate from your source reservation. This helps you make unused capacity available for other
      # workloads within your account while maintaining control to reclaim it.

      def create_interruptible_capacity_reservation_allocation(
        capacity_reservation_id : String,
        instance_count : Int32,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateInterruptibleCapacityReservationAllocationResult

        input = Types::CreateInterruptibleCapacityReservationAllocationRequest.new(capacity_reservation_id: capacity_reservation_id, instance_count: instance_count, client_token: client_token, dry_run: dry_run, tag_specifications: tag_specifications)
        create_interruptible_capacity_reservation_allocation(input)
      end

      def create_interruptible_capacity_reservation_allocation(input : Types::CreateInterruptibleCapacityReservationAllocationRequest) : Types::CreateInterruptibleCapacityReservationAllocationResult
        request = Protocol::Ec2.build_request(Model::CREATE_INTERRUPTIBLE_CAPACITY_RESERVATION_ALLOCATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateInterruptibleCapacityReservationAllocationResult, "CreateInterruptibleCapacityReservationAllocation")
      end

      # Create an IPAM. Amazon VPC IP Address Manager (IPAM) is a VPC feature that you can use to automate
      # your IP address management workflows including assigning, tracking, troubleshooting, and auditing IP
      # addresses across Amazon Web Services Regions and accounts throughout your Amazon Web Services
      # Organization. For more information, see Create an IPAM in the Amazon VPC IPAM User Guide .

      def create_ipam(
        client_token : String? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        enable_private_gua : Bool? = nil,
        metered_account : String? = nil,
        operating_regions : Array(Types::AddIpamOperatingRegion)? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        tier : String? = nil
      ) : Types::CreateIpamResult

        input = Types::CreateIpamRequest.new(client_token: client_token, description: description, dry_run: dry_run, enable_private_gua: enable_private_gua, metered_account: metered_account, operating_regions: operating_regions, tag_specifications: tag_specifications, tier: tier)
        create_ipam(input)
      end

      def create_ipam(input : Types::CreateIpamRequest) : Types::CreateIpamResult
        request = Protocol::Ec2.build_request(Model::CREATE_IPAM, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateIpamResult, "CreateIpam")
      end

      # Create a verification token. A verification token is an Amazon Web Services-generated random value
      # that you can use to prove ownership of an external resource. For example, you can use a verification
      # token to validate that you control a public IP address range when you bring an IP address range to
      # Amazon Web Services (BYOIP).

      def create_ipam_external_resource_verification_token(
        ipam_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateIpamExternalResourceVerificationTokenResult

        input = Types::CreateIpamExternalResourceVerificationTokenRequest.new(ipam_id: ipam_id, client_token: client_token, dry_run: dry_run, tag_specifications: tag_specifications)
        create_ipam_external_resource_verification_token(input)
      end

      def create_ipam_external_resource_verification_token(input : Types::CreateIpamExternalResourceVerificationTokenRequest) : Types::CreateIpamExternalResourceVerificationTokenResult
        request = Protocol::Ec2.build_request(Model::CREATE_IPAM_EXTERNAL_RESOURCE_VERIFICATION_TOKEN, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateIpamExternalResourceVerificationTokenResult, "CreateIpamExternalResourceVerificationToken")
      end

      # Creates an IPAM policy. An IPAM policy is a set of rules that define how public IPv4 addresses from
      # IPAM pools are allocated to Amazon Web Services resources. Each rule maps an Amazon Web Services
      # service to IPAM pools that the service will use to get IP addresses. A single policy can have
      # multiple rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run out of
      # addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied to an
      # individual Amazon Web Services account or an entity within Amazon Web Services Organizations. For
      # more information, see Define public IPv4 allocation strategy with IPAM policies in the Amazon VPC
      # IPAM User Guide .

      def create_ipam_policy(
        ipam_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateIpamPolicyResult

        input = Types::CreateIpamPolicyRequest.new(ipam_id: ipam_id, client_token: client_token, dry_run: dry_run, tag_specifications: tag_specifications)
        create_ipam_policy(input)
      end

      def create_ipam_policy(input : Types::CreateIpamPolicyRequest) : Types::CreateIpamPolicyResult
        request = Protocol::Ec2.build_request(Model::CREATE_IPAM_POLICY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateIpamPolicyResult, "CreateIpamPolicy")
      end

      # Create an IP address pool for Amazon VPC IP Address Manager (IPAM). In IPAM, a pool is a collection
      # of contiguous IP addresses CIDRs. Pools enable you to organize your IP addresses according to your
      # routing and security needs. For example, if you have separate routing and security needs for
      # development and production applications, you can create a pool for each. For more information, see
      # Create a top-level pool in the Amazon VPC IPAM User Guide .

      def create_ipam_pool(
        address_family : String,
        ipam_scope_id : String,
        allocation_default_netmask_length : Int32? = nil,
        allocation_max_netmask_length : Int32? = nil,
        allocation_min_netmask_length : Int32? = nil,
        allocation_resource_tags : Array(Types::RequestIpamResourceTag)? = nil,
        auto_import : Bool? = nil,
        aws_service : String? = nil,
        client_token : String? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        locale : String? = nil,
        public_ip_source : String? = nil,
        publicly_advertisable : Bool? = nil,
        source_ipam_pool_id : String? = nil,
        source_resource : Types::IpamPoolSourceResourceRequest? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateIpamPoolResult

        input = Types::CreateIpamPoolRequest.new(address_family: address_family, ipam_scope_id: ipam_scope_id, allocation_default_netmask_length: allocation_default_netmask_length, allocation_max_netmask_length: allocation_max_netmask_length, allocation_min_netmask_length: allocation_min_netmask_length, allocation_resource_tags: allocation_resource_tags, auto_import: auto_import, aws_service: aws_service, client_token: client_token, description: description, dry_run: dry_run, locale: locale, public_ip_source: public_ip_source, publicly_advertisable: publicly_advertisable, source_ipam_pool_id: source_ipam_pool_id, source_resource: source_resource, tag_specifications: tag_specifications)
        create_ipam_pool(input)
      end

      def create_ipam_pool(input : Types::CreateIpamPoolRequest) : Types::CreateIpamPoolResult
        request = Protocol::Ec2.build_request(Model::CREATE_IPAM_POOL, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateIpamPoolResult, "CreateIpamPool")
      end

      # Creates an IPAM prefix list resolver. An IPAM prefix list resolver is a component that manages the
      # synchronization between IPAM's CIDR selection rules and customer-managed prefix lists. It automates
      # connectivity configurations by selecting CIDRs from IPAM's database based on your business logic and
      # synchronizing them with prefix lists used in resources such as VPC route tables and security groups.
      # For more information about IPAM prefix list resolver, see Automate prefix list updates with IPAM in
      # the Amazon VPC IPAM User Guide .

      def create_ipam_prefix_list_resolver(
        address_family : String,
        ipam_id : String,
        client_token : String? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        rules : Array(Types::IpamPrefixListResolverRuleRequest)? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateIpamPrefixListResolverResult

        input = Types::CreateIpamPrefixListResolverRequest.new(address_family: address_family, ipam_id: ipam_id, client_token: client_token, description: description, dry_run: dry_run, rules: rules, tag_specifications: tag_specifications)
        create_ipam_prefix_list_resolver(input)
      end

      def create_ipam_prefix_list_resolver(input : Types::CreateIpamPrefixListResolverRequest) : Types::CreateIpamPrefixListResolverResult
        request = Protocol::Ec2.build_request(Model::CREATE_IPAM_PREFIX_LIST_RESOLVER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateIpamPrefixListResolverResult, "CreateIpamPrefixListResolver")
      end

      # Creates an IPAM prefix list resolver target. An IPAM prefix list resolver target is an association
      # between a specific customer-managed prefix list and an IPAM prefix list resolver. The target enables
      # the resolver to synchronize CIDRs selected by its rules into the specified prefix list, which can
      # then be referenced in Amazon Web Services resources. For more information about IPAM prefix list
      # resolver, see Automate prefix list updates with IPAM in the Amazon VPC IPAM User Guide .

      def create_ipam_prefix_list_resolver_target(
        ipam_prefix_list_resolver_id : String,
        prefix_list_id : String,
        prefix_list_region : String,
        track_latest_version : Bool,
        client_token : String? = nil,
        desired_version : Int64? = nil,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateIpamPrefixListResolverTargetResult

        input = Types::CreateIpamPrefixListResolverTargetRequest.new(ipam_prefix_list_resolver_id: ipam_prefix_list_resolver_id, prefix_list_id: prefix_list_id, prefix_list_region: prefix_list_region, track_latest_version: track_latest_version, client_token: client_token, desired_version: desired_version, dry_run: dry_run, tag_specifications: tag_specifications)
        create_ipam_prefix_list_resolver_target(input)
      end

      def create_ipam_prefix_list_resolver_target(input : Types::CreateIpamPrefixListResolverTargetRequest) : Types::CreateIpamPrefixListResolverTargetResult
        request = Protocol::Ec2.build_request(Model::CREATE_IPAM_PREFIX_LIST_RESOLVER_TARGET, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateIpamPrefixListResolverTargetResult, "CreateIpamPrefixListResolverTarget")
      end

      # Creates an IPAM resource discovery. A resource discovery is an IPAM component that enables IPAM to
      # manage and monitor resources that belong to the owning account.

      def create_ipam_resource_discovery(
        client_token : String? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        operating_regions : Array(Types::AddIpamOperatingRegion)? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateIpamResourceDiscoveryResult

        input = Types::CreateIpamResourceDiscoveryRequest.new(client_token: client_token, description: description, dry_run: dry_run, operating_regions: operating_regions, tag_specifications: tag_specifications)
        create_ipam_resource_discovery(input)
      end

      def create_ipam_resource_discovery(input : Types::CreateIpamResourceDiscoveryRequest) : Types::CreateIpamResourceDiscoveryResult
        request = Protocol::Ec2.build_request(Model::CREATE_IPAM_RESOURCE_DISCOVERY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateIpamResourceDiscoveryResult, "CreateIpamResourceDiscovery")
      end

      # Create an IPAM scope. In IPAM, a scope is the highest-level container within IPAM. An IPAM contains
      # two default scopes. Each scope represents the IP space for a single network. The private scope is
      # intended for all private IP address space. The public scope is intended for all public IP address
      # space. Scopes enable you to reuse IP addresses across multiple unconnected networks without causing
      # IP address overlap or conflict. For more information, see Add a scope in the Amazon VPC IPAM User
      # Guide .

      def create_ipam_scope(
        ipam_id : String,
        client_token : String? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        external_authority_configuration : Types::ExternalAuthorityConfiguration? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateIpamScopeResult

        input = Types::CreateIpamScopeRequest.new(ipam_id: ipam_id, client_token: client_token, description: description, dry_run: dry_run, external_authority_configuration: external_authority_configuration, tag_specifications: tag_specifications)
        create_ipam_scope(input)
      end

      def create_ipam_scope(input : Types::CreateIpamScopeRequest) : Types::CreateIpamScopeResult
        request = Protocol::Ec2.build_request(Model::CREATE_IPAM_SCOPE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateIpamScopeResult, "CreateIpamScope")
      end

      # Creates an ED25519 or 2048-bit RSA key pair with the specified name and in the specified format.
      # Amazon EC2 stores the public key and displays the private key for you to save to a file. The private
      # key is returned as an unencrypted PEM encoded PKCS#1 private key or an unencrypted PPK formatted
      # private key for use with PuTTY. If a key with the specified name already exists, Amazon EC2 returns
      # an error. The key pair returned to you is available only in the Amazon Web Services Region in which
      # you create it. If you prefer, you can create your own key pair using a third-party tool and upload
      # it to any Region using ImportKeyPair . You can have up to 5,000 key pairs per Amazon Web Services
      # Region. For more information, see Amazon EC2 key pairs in the Amazon EC2 User Guide .

      def create_key_pair(
        key_name : String,
        dry_run : Bool? = nil,
        key_format : String? = nil,
        key_type : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::KeyPair

        input = Types::CreateKeyPairRequest.new(key_name: key_name, dry_run: dry_run, key_format: key_format, key_type: key_type, tag_specifications: tag_specifications)
        create_key_pair(input)
      end

      def create_key_pair(input : Types::CreateKeyPairRequest) : Types::KeyPair
        request = Protocol::Ec2.build_request(Model::CREATE_KEY_PAIR, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::KeyPair, "CreateKeyPair")
      end

      # Creates a launch template. A launch template contains the parameters to launch an instance. When you
      # launch an instance using RunInstances , you can specify a launch template instead of providing the
      # launch parameters in the request. For more information, see Store instance launch parameters in
      # Amazon EC2 launch templates in the Amazon EC2 User Guide . To clone an existing launch template as
      # the basis for a new launch template, use the Amazon EC2 console. The API, SDKs, and CLI do not
      # support cloning a template. For more information, see Create a launch template from an existing
      # launch template in the Amazon EC2 User Guide .

      def create_launch_template(
        launch_template_data : Types::RequestLaunchTemplateData,
        launch_template_name : String,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        operator : Types::OperatorRequest? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        version_description : String? = nil
      ) : Types::CreateLaunchTemplateResult

        input = Types::CreateLaunchTemplateRequest.new(launch_template_data: launch_template_data, launch_template_name: launch_template_name, client_token: client_token, dry_run: dry_run, operator: operator, tag_specifications: tag_specifications, version_description: version_description)
        create_launch_template(input)
      end

      def create_launch_template(input : Types::CreateLaunchTemplateRequest) : Types::CreateLaunchTemplateResult
        request = Protocol::Ec2.build_request(Model::CREATE_LAUNCH_TEMPLATE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateLaunchTemplateResult, "CreateLaunchTemplate")
      end

      # Creates a new version of a launch template. You must specify an existing launch template, either by
      # name or ID. You can determine whether the new version inherits parameters from a source version, and
      # add or overwrite parameters as needed. Launch template versions are numbered in the order in which
      # they are created. You can't specify, change, or replace the numbering of launch template versions.
      # Launch templates are immutable; after you create a launch template, you can't modify it. Instead,
      # you can create a new version of the launch template that includes the changes that you require. For
      # more information, see Modify a launch template (manage launch template versions) in the Amazon EC2
      # User Guide .

      def create_launch_template_version(
        launch_template_data : Types::RequestLaunchTemplateData,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        launch_template_id : String? = nil,
        launch_template_name : String? = nil,
        resolve_alias : Bool? = nil,
        source_version : String? = nil,
        version_description : String? = nil
      ) : Types::CreateLaunchTemplateVersionResult

        input = Types::CreateLaunchTemplateVersionRequest.new(launch_template_data: launch_template_data, client_token: client_token, dry_run: dry_run, launch_template_id: launch_template_id, launch_template_name: launch_template_name, resolve_alias: resolve_alias, source_version: source_version, version_description: version_description)
        create_launch_template_version(input)
      end

      def create_launch_template_version(input : Types::CreateLaunchTemplateVersionRequest) : Types::CreateLaunchTemplateVersionResult
        request = Protocol::Ec2.build_request(Model::CREATE_LAUNCH_TEMPLATE_VERSION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateLaunchTemplateVersionResult, "CreateLaunchTemplateVersion")
      end

      # Creates a static route for the specified local gateway route table. You must specify one of the
      # following targets: LocalGatewayVirtualInterfaceGroupId NetworkInterfaceId

      def create_local_gateway_route(
        local_gateway_route_table_id : String,
        destination_cidr_block : String? = nil,
        destination_prefix_list_id : String? = nil,
        dry_run : Bool? = nil,
        local_gateway_virtual_interface_group_id : String? = nil,
        network_interface_id : String? = nil
      ) : Types::CreateLocalGatewayRouteResult

        input = Types::CreateLocalGatewayRouteRequest.new(local_gateway_route_table_id: local_gateway_route_table_id, destination_cidr_block: destination_cidr_block, destination_prefix_list_id: destination_prefix_list_id, dry_run: dry_run, local_gateway_virtual_interface_group_id: local_gateway_virtual_interface_group_id, network_interface_id: network_interface_id)
        create_local_gateway_route(input)
      end

      def create_local_gateway_route(input : Types::CreateLocalGatewayRouteRequest) : Types::CreateLocalGatewayRouteResult
        request = Protocol::Ec2.build_request(Model::CREATE_LOCAL_GATEWAY_ROUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateLocalGatewayRouteResult, "CreateLocalGatewayRoute")
      end

      # Creates a local gateway route table.

      def create_local_gateway_route_table(
        local_gateway_id : String,
        dry_run : Bool? = nil,
        mode : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateLocalGatewayRouteTableResult

        input = Types::CreateLocalGatewayRouteTableRequest.new(local_gateway_id: local_gateway_id, dry_run: dry_run, mode: mode, tag_specifications: tag_specifications)
        create_local_gateway_route_table(input)
      end

      def create_local_gateway_route_table(input : Types::CreateLocalGatewayRouteTableRequest) : Types::CreateLocalGatewayRouteTableResult
        request = Protocol::Ec2.build_request(Model::CREATE_LOCAL_GATEWAY_ROUTE_TABLE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateLocalGatewayRouteTableResult, "CreateLocalGatewayRouteTable")
      end

      # Creates a local gateway route table virtual interface group association.

      def create_local_gateway_route_table_virtual_interface_group_association(
        local_gateway_route_table_id : String,
        local_gateway_virtual_interface_group_id : String,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateLocalGatewayRouteTableVirtualInterfaceGroupAssociationResult

        input = Types::CreateLocalGatewayRouteTableVirtualInterfaceGroupAssociationRequest.new(local_gateway_route_table_id: local_gateway_route_table_id, local_gateway_virtual_interface_group_id: local_gateway_virtual_interface_group_id, dry_run: dry_run, tag_specifications: tag_specifications)
        create_local_gateway_route_table_virtual_interface_group_association(input)
      end

      def create_local_gateway_route_table_virtual_interface_group_association(input : Types::CreateLocalGatewayRouteTableVirtualInterfaceGroupAssociationRequest) : Types::CreateLocalGatewayRouteTableVirtualInterfaceGroupAssociationResult
        request = Protocol::Ec2.build_request(Model::CREATE_LOCAL_GATEWAY_ROUTE_TABLE_VIRTUAL_INTERFACE_GROUP_ASSOCIATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateLocalGatewayRouteTableVirtualInterfaceGroupAssociationResult, "CreateLocalGatewayRouteTableVirtualInterfaceGroupAssociation")
      end

      # Associates the specified VPC with the specified local gateway route table.

      def create_local_gateway_route_table_vpc_association(
        local_gateway_route_table_id : String,
        vpc_id : String,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateLocalGatewayRouteTableVpcAssociationResult

        input = Types::CreateLocalGatewayRouteTableVpcAssociationRequest.new(local_gateway_route_table_id: local_gateway_route_table_id, vpc_id: vpc_id, dry_run: dry_run, tag_specifications: tag_specifications)
        create_local_gateway_route_table_vpc_association(input)
      end

      def create_local_gateway_route_table_vpc_association(input : Types::CreateLocalGatewayRouteTableVpcAssociationRequest) : Types::CreateLocalGatewayRouteTableVpcAssociationResult
        request = Protocol::Ec2.build_request(Model::CREATE_LOCAL_GATEWAY_ROUTE_TABLE_VPC_ASSOCIATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateLocalGatewayRouteTableVpcAssociationResult, "CreateLocalGatewayRouteTableVpcAssociation")
      end

      # Create a virtual interface for a local gateway.

      def create_local_gateway_virtual_interface(
        local_address : String,
        local_gateway_virtual_interface_group_id : String,
        outpost_lag_id : String,
        peer_address : String,
        vlan : Int32,
        dry_run : Bool? = nil,
        peer_bgp_asn : Int32? = nil,
        peer_bgp_asn_extended : Int64? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateLocalGatewayVirtualInterfaceResult

        input = Types::CreateLocalGatewayVirtualInterfaceRequest.new(local_address: local_address, local_gateway_virtual_interface_group_id: local_gateway_virtual_interface_group_id, outpost_lag_id: outpost_lag_id, peer_address: peer_address, vlan: vlan, dry_run: dry_run, peer_bgp_asn: peer_bgp_asn, peer_bgp_asn_extended: peer_bgp_asn_extended, tag_specifications: tag_specifications)
        create_local_gateway_virtual_interface(input)
      end

      def create_local_gateway_virtual_interface(input : Types::CreateLocalGatewayVirtualInterfaceRequest) : Types::CreateLocalGatewayVirtualInterfaceResult
        request = Protocol::Ec2.build_request(Model::CREATE_LOCAL_GATEWAY_VIRTUAL_INTERFACE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateLocalGatewayVirtualInterfaceResult, "CreateLocalGatewayVirtualInterface")
      end

      # Create a local gateway virtual interface group.

      def create_local_gateway_virtual_interface_group(
        local_gateway_id : String,
        dry_run : Bool? = nil,
        local_bgp_asn : Int32? = nil,
        local_bgp_asn_extended : Int64? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateLocalGatewayVirtualInterfaceGroupResult

        input = Types::CreateLocalGatewayVirtualInterfaceGroupRequest.new(local_gateway_id: local_gateway_id, dry_run: dry_run, local_bgp_asn: local_bgp_asn, local_bgp_asn_extended: local_bgp_asn_extended, tag_specifications: tag_specifications)
        create_local_gateway_virtual_interface_group(input)
      end

      def create_local_gateway_virtual_interface_group(input : Types::CreateLocalGatewayVirtualInterfaceGroupRequest) : Types::CreateLocalGatewayVirtualInterfaceGroupResult
        request = Protocol::Ec2.build_request(Model::CREATE_LOCAL_GATEWAY_VIRTUAL_INTERFACE_GROUP, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateLocalGatewayVirtualInterfaceGroupResult, "CreateLocalGatewayVirtualInterfaceGroup")
      end

      # Creates a System Integrity Protection (SIP) modification task to configure the SIP settings for an
      # x86 Mac instance or Apple silicon Mac instance. For more information, see Configure SIP for Amazon
      # EC2 instances in the Amazon EC2 User Guide . When you configure the SIP settings for your instance,
      # you can either enable or disable all SIP settings, or you can specify a custom SIP configuration
      # that selectively enables or disables specific SIP settings. If you implement a custom configuration,
      # connect to the instance and verify the settings to ensure that your requirements are properly
      # implemented and functioning as intended. SIP configurations might change with macOS updates. We
      # recommend that you review custom SIP settings after any macOS version upgrade to ensure continued
      # compatibility and proper functionality of your security configurations. To enable or disable all SIP
      # settings, use the MacSystemIntegrityProtectionStatus parameter only. For example, to enable all SIP
      # settings, specify the following: MacSystemIntegrityProtectionStatus=enabled To specify a custom
      # configuration that selectively enables or disables specific SIP settings, use the
      # MacSystemIntegrityProtectionStatus parameter to enable or disable all SIP settings, and then use the
      # MacSystemIntegrityProtectionConfiguration parameter to specify exceptions. In this case, the
      # exceptions you specify for MacSystemIntegrityProtectionConfiguration override the value you specify
      # for MacSystemIntegrityProtectionStatus . For example, to enable all SIP settings, except
      # NvramProtections , specify the following: MacSystemIntegrityProtectionStatus=enabled
      # MacSystemIntegrityProtectionConfigurationRequest "NvramProtections=disabled"

      def create_mac_system_integrity_protection_modification_task(
        instance_id : String,
        mac_system_integrity_protection_status : String,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        mac_credentials : String? = nil,
        mac_system_integrity_protection_configuration : Types::MacSystemIntegrityProtectionConfigurationRequest? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateMacSystemIntegrityProtectionModificationTaskResult

        input = Types::CreateMacSystemIntegrityProtectionModificationTaskRequest.new(instance_id: instance_id, mac_system_integrity_protection_status: mac_system_integrity_protection_status, client_token: client_token, dry_run: dry_run, mac_credentials: mac_credentials, mac_system_integrity_protection_configuration: mac_system_integrity_protection_configuration, tag_specifications: tag_specifications)
        create_mac_system_integrity_protection_modification_task(input)
      end

      def create_mac_system_integrity_protection_modification_task(input : Types::CreateMacSystemIntegrityProtectionModificationTaskRequest) : Types::CreateMacSystemIntegrityProtectionModificationTaskResult
        request = Protocol::Ec2.build_request(Model::CREATE_MAC_SYSTEM_INTEGRITY_PROTECTION_MODIFICATION_TASK, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateMacSystemIntegrityProtectionModificationTaskResult, "CreateMacSystemIntegrityProtectionModificationTask")
      end

      # Creates a managed prefix list. You can specify entries for the prefix list. Each entry consists of a
      # CIDR block and an optional description.

      def create_managed_prefix_list(
        address_family : String,
        max_entries : Int32,
        prefix_list_name : String,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        entries : Array(Types::AddPrefixListEntry)? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateManagedPrefixListResult

        input = Types::CreateManagedPrefixListRequest.new(address_family: address_family, max_entries: max_entries, prefix_list_name: prefix_list_name, client_token: client_token, dry_run: dry_run, entries: entries, tag_specifications: tag_specifications)
        create_managed_prefix_list(input)
      end

      def create_managed_prefix_list(input : Types::CreateManagedPrefixListRequest) : Types::CreateManagedPrefixListResult
        request = Protocol::Ec2.build_request(Model::CREATE_MANAGED_PREFIX_LIST, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateManagedPrefixListResult, "CreateManagedPrefixList")
      end

      # Creates a NAT gateway in the specified subnet. This action creates a network interface in the
      # specified subnet with a private IP address from the IP address range of the subnet. You can create
      # either a public NAT gateway or a private NAT gateway. With a public NAT gateway, internet-bound
      # traffic from a private subnet can be routed to the NAT gateway, so that instances in a private
      # subnet can connect to the internet. With a private NAT gateway, private communication is routed
      # across VPCs and on-premises networks through a transit gateway or virtual private gateway. Common
      # use cases include running large workloads behind a small pool of allowlisted IPv4 addresses,
      # preserving private IPv4 addresses, and communicating between overlapping networks. For more
      # information, see NAT gateways in the Amazon VPC User Guide . When you create a public NAT gateway
      # and assign it an EIP or secondary EIPs, the network border group of the EIPs must match the network
      # border group of the Availability Zone (AZ) that the public NAT gateway is in. If it's not the same,
      # the NAT gateway will fail to launch. You can see the network border group for the subnet's AZ by
      # viewing the details of the subnet. Similarly, you can view the network border group of an EIP by
      # viewing the details of the EIP address. For more information about network border groups and EIPs,
      # see Allocate an Elastic IP address in the Amazon VPC User Guide .

      def create_nat_gateway(
        allocation_id : String? = nil,
        availability_mode : String? = nil,
        availability_zone_addresses : Array(Types::AvailabilityZoneAddress)? = nil,
        client_token : String? = nil,
        connectivity_type : String? = nil,
        dry_run : Bool? = nil,
        private_ip_address : String? = nil,
        secondary_allocation_ids : Array(String)? = nil,
        secondary_private_ip_address_count : Int32? = nil,
        secondary_private_ip_addresses : Array(String)? = nil,
        subnet_id : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        vpc_id : String? = nil
      ) : Types::CreateNatGatewayResult

        input = Types::CreateNatGatewayRequest.new(allocation_id: allocation_id, availability_mode: availability_mode, availability_zone_addresses: availability_zone_addresses, client_token: client_token, connectivity_type: connectivity_type, dry_run: dry_run, private_ip_address: private_ip_address, secondary_allocation_ids: secondary_allocation_ids, secondary_private_ip_address_count: secondary_private_ip_address_count, secondary_private_ip_addresses: secondary_private_ip_addresses, subnet_id: subnet_id, tag_specifications: tag_specifications, vpc_id: vpc_id)
        create_nat_gateway(input)
      end

      def create_nat_gateway(input : Types::CreateNatGatewayRequest) : Types::CreateNatGatewayResult
        request = Protocol::Ec2.build_request(Model::CREATE_NAT_GATEWAY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateNatGatewayResult, "CreateNatGateway")
      end

      # Creates a network ACL in a VPC. Network ACLs provide an optional layer of security (in addition to
      # security groups) for the instances in your VPC. For more information, see Network ACLs in the Amazon
      # VPC User Guide .

      def create_network_acl(
        vpc_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateNetworkAclResult

        input = Types::CreateNetworkAclRequest.new(vpc_id: vpc_id, client_token: client_token, dry_run: dry_run, tag_specifications: tag_specifications)
        create_network_acl(input)
      end

      def create_network_acl(input : Types::CreateNetworkAclRequest) : Types::CreateNetworkAclResult
        request = Protocol::Ec2.build_request(Model::CREATE_NETWORK_ACL, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateNetworkAclResult, "CreateNetworkAcl")
      end

      # Creates an entry (a rule) in a network ACL with the specified rule number. Each network ACL has a
      # set of numbered ingress rules and a separate set of numbered egress rules. When determining whether
      # a packet should be allowed in or out of a subnet associated with the ACL, we process the entries in
      # the ACL according to the rule numbers, in ascending order. Each network ACL has a set of ingress
      # rules and a separate set of egress rules. We recommend that you leave room between the rule numbers
      # (for example, 100, 110, 120, ...), and not number them one right after the other (for example, 101,
      # 102, 103, ...). This makes it easier to add a rule between existing ones without having to renumber
      # the rules. After you add an entry, you can't modify it; you must either replace it, or create an
      # entry and delete the old one. For more information about network ACLs, see Network ACLs in the
      # Amazon VPC User Guide .

      def create_network_acl_entry(
        egress : Bool,
        network_acl_id : String,
        protocol : String,
        rule_action : String,
        rule_number : Int32,
        cidr_block : String? = nil,
        dry_run : Bool? = nil,
        icmp_type_code : Types::IcmpTypeCode? = nil,
        ipv6_cidr_block : String? = nil,
        port_range : Types::PortRange? = nil
      ) : Nil

        input = Types::CreateNetworkAclEntryRequest.new(egress: egress, network_acl_id: network_acl_id, protocol: protocol, rule_action: rule_action, rule_number: rule_number, cidr_block: cidr_block, dry_run: dry_run, icmp_type_code: icmp_type_code, ipv6_cidr_block: ipv6_cidr_block, port_range: port_range)
        create_network_acl_entry(input)
      end

      def create_network_acl_entry(input : Types::CreateNetworkAclEntryRequest) : Nil
        request = Protocol::Ec2.build_request(Model::CREATE_NETWORK_ACL_ENTRY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Creates a Network Access Scope. Amazon Web Services Network Access Analyzer enables cloud networking
      # and cloud operations teams to verify that their networks on Amazon Web Services conform to their
      # network security and governance objectives. For more information, see the Amazon Web Services
      # Network Access Analyzer Guide .

      def create_network_insights_access_scope(
        client_token : String,
        dry_run : Bool? = nil,
        exclude_paths : Array(Types::AccessScopePathRequest)? = nil,
        match_paths : Array(Types::AccessScopePathRequest)? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateNetworkInsightsAccessScopeResult

        input = Types::CreateNetworkInsightsAccessScopeRequest.new(client_token: client_token, dry_run: dry_run, exclude_paths: exclude_paths, match_paths: match_paths, tag_specifications: tag_specifications)
        create_network_insights_access_scope(input)
      end

      def create_network_insights_access_scope(input : Types::CreateNetworkInsightsAccessScopeRequest) : Types::CreateNetworkInsightsAccessScopeResult
        request = Protocol::Ec2.build_request(Model::CREATE_NETWORK_INSIGHTS_ACCESS_SCOPE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateNetworkInsightsAccessScopeResult, "CreateNetworkInsightsAccessScope")
      end

      # Creates a path to analyze for reachability. Reachability Analyzer enables you to analyze and debug
      # network reachability between two resources in your virtual private cloud (VPC). For more
      # information, see the Reachability Analyzer Guide .

      def create_network_insights_path(
        client_token : String,
        protocol : String,
        source : String,
        destination : String? = nil,
        destination_ip : String? = nil,
        destination_port : Int32? = nil,
        dry_run : Bool? = nil,
        filter_at_destination : Types::PathRequestFilter? = nil,
        filter_at_source : Types::PathRequestFilter? = nil,
        source_ip : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateNetworkInsightsPathResult

        input = Types::CreateNetworkInsightsPathRequest.new(client_token: client_token, protocol: protocol, source: source, destination: destination, destination_ip: destination_ip, destination_port: destination_port, dry_run: dry_run, filter_at_destination: filter_at_destination, filter_at_source: filter_at_source, source_ip: source_ip, tag_specifications: tag_specifications)
        create_network_insights_path(input)
      end

      def create_network_insights_path(input : Types::CreateNetworkInsightsPathRequest) : Types::CreateNetworkInsightsPathResult
        request = Protocol::Ec2.build_request(Model::CREATE_NETWORK_INSIGHTS_PATH, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateNetworkInsightsPathResult, "CreateNetworkInsightsPath")
      end

      # Creates a network interface in the specified subnet. The number of IP addresses you can assign to a
      # network interface varies by instance type. For more information about network interfaces, see
      # Elastic network interfaces in the Amazon EC2 User Guide .

      def create_network_interface(
        subnet_id : String,
        client_token : String? = nil,
        connection_tracking_specification : Types::ConnectionTrackingSpecificationRequest? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        enable_primary_ipv6 : Bool? = nil,
        groups : Array(String)? = nil,
        interface_type : String? = nil,
        ipv4_prefix_count : Int32? = nil,
        ipv4_prefixes : Array(Types::Ipv4PrefixSpecificationRequest)? = nil,
        ipv6_address_count : Int32? = nil,
        ipv6_addresses : Array(Types::InstanceIpv6Address)? = nil,
        ipv6_prefix_count : Int32? = nil,
        ipv6_prefixes : Array(Types::Ipv6PrefixSpecificationRequest)? = nil,
        operator : Types::OperatorRequest? = nil,
        private_ip_address : String? = nil,
        private_ip_addresses : Array(Types::PrivateIpAddressSpecification)? = nil,
        secondary_private_ip_address_count : Int32? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateNetworkInterfaceResult

        input = Types::CreateNetworkInterfaceRequest.new(subnet_id: subnet_id, client_token: client_token, connection_tracking_specification: connection_tracking_specification, description: description, dry_run: dry_run, enable_primary_ipv6: enable_primary_ipv6, groups: groups, interface_type: interface_type, ipv4_prefix_count: ipv4_prefix_count, ipv4_prefixes: ipv4_prefixes, ipv6_address_count: ipv6_address_count, ipv6_addresses: ipv6_addresses, ipv6_prefix_count: ipv6_prefix_count, ipv6_prefixes: ipv6_prefixes, operator: operator, private_ip_address: private_ip_address, private_ip_addresses: private_ip_addresses, secondary_private_ip_address_count: secondary_private_ip_address_count, tag_specifications: tag_specifications)
        create_network_interface(input)
      end

      def create_network_interface(input : Types::CreateNetworkInterfaceRequest) : Types::CreateNetworkInterfaceResult
        request = Protocol::Ec2.build_request(Model::CREATE_NETWORK_INTERFACE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateNetworkInterfaceResult, "CreateNetworkInterface")
      end

      # Grants an Amazon Web Services-authorized account permission to attach the specified network
      # interface to an instance in their account. You can grant permission to a single Amazon Web Services
      # account only, and only one account at a time.

      def create_network_interface_permission(
        network_interface_id : String,
        permission : String,
        aws_account_id : String? = nil,
        aws_service : String? = nil,
        dry_run : Bool? = nil
      ) : Types::CreateNetworkInterfacePermissionResult

        input = Types::CreateNetworkInterfacePermissionRequest.new(network_interface_id: network_interface_id, permission: permission, aws_account_id: aws_account_id, aws_service: aws_service, dry_run: dry_run)
        create_network_interface_permission(input)
      end

      def create_network_interface_permission(input : Types::CreateNetworkInterfacePermissionRequest) : Types::CreateNetworkInterfacePermissionResult
        request = Protocol::Ec2.build_request(Model::CREATE_NETWORK_INTERFACE_PERMISSION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateNetworkInterfacePermissionResult, "CreateNetworkInterfacePermission")
      end

      # Creates a placement group in which to launch instances. The strategy of the placement group
      # determines how the instances are organized within the group. A cluster placement group is a logical
      # grouping of instances within a single Availability Zone that benefit from low network latency, high
      # network throughput. A spread placement group places instances on distinct hardware. A partition
      # placement group places groups of instances in different partitions, where instances in one partition
      # do not share the same hardware with instances in another partition. For more information, see
      # Placement groups in the Amazon EC2 User Guide .

      def create_placement_group(
        dry_run : Bool? = nil,
        group_name : String? = nil,
        linked_group_id : String? = nil,
        partition_count : Int32? = nil,
        spread_level : String? = nil,
        strategy : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreatePlacementGroupResult

        input = Types::CreatePlacementGroupRequest.new(dry_run: dry_run, group_name: group_name, linked_group_id: linked_group_id, partition_count: partition_count, spread_level: spread_level, strategy: strategy, tag_specifications: tag_specifications)
        create_placement_group(input)
      end

      def create_placement_group(input : Types::CreatePlacementGroupRequest) : Types::CreatePlacementGroupResult
        request = Protocol::Ec2.build_request(Model::CREATE_PLACEMENT_GROUP, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreatePlacementGroupResult, "CreatePlacementGroup")
      end

      # Creates a public IPv4 address pool. A public IPv4 pool is an EC2 IP address pool required for the
      # public IPv4 CIDRs that you own and bring to Amazon Web Services to manage with IPAM. IPv6 addresses
      # you bring to Amazon Web Services, however, use IPAM pools only. To monitor the status of pool
      # creation, use DescribePublicIpv4Pools .

      def create_public_ipv4_pool(
        dry_run : Bool? = nil,
        network_border_group : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreatePublicIpv4PoolResult

        input = Types::CreatePublicIpv4PoolRequest.new(dry_run: dry_run, network_border_group: network_border_group, tag_specifications: tag_specifications)
        create_public_ipv4_pool(input)
      end

      def create_public_ipv4_pool(input : Types::CreatePublicIpv4PoolRequest) : Types::CreatePublicIpv4PoolResult
        request = Protocol::Ec2.build_request(Model::CREATE_PUBLIC_IPV4_POOL, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreatePublicIpv4PoolResult, "CreatePublicIpv4Pool")
      end

      # Replaces the EBS-backed root volume for a running instance with a new volume that is restored to the
      # original root volume's launch state, that is restored to a specific snapshot taken from the original
      # root volume, or that is restored from an AMI that has the same key characteristics as that of the
      # instance. For more information, see Replace a root volume in the Amazon EC2 User Guide .

      def create_replace_root_volume_task(
        instance_id : String,
        client_token : String? = nil,
        delete_replaced_root_volume : Bool? = nil,
        dry_run : Bool? = nil,
        image_id : String? = nil,
        snapshot_id : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        volume_initialization_rate : Int64? = nil
      ) : Types::CreateReplaceRootVolumeTaskResult

        input = Types::CreateReplaceRootVolumeTaskRequest.new(instance_id: instance_id, client_token: client_token, delete_replaced_root_volume: delete_replaced_root_volume, dry_run: dry_run, image_id: image_id, snapshot_id: snapshot_id, tag_specifications: tag_specifications, volume_initialization_rate: volume_initialization_rate)
        create_replace_root_volume_task(input)
      end

      def create_replace_root_volume_task(input : Types::CreateReplaceRootVolumeTaskRequest) : Types::CreateReplaceRootVolumeTaskResult
        request = Protocol::Ec2.build_request(Model::CREATE_REPLACE_ROOT_VOLUME_TASK, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateReplaceRootVolumeTaskResult, "CreateReplaceRootVolumeTask")
      end

      # Creates a listing for Amazon EC2 Standard Reserved Instances to be sold in the Reserved Instance
      # Marketplace. You can submit one Standard Reserved Instance listing at a time. To get a list of your
      # Standard Reserved Instances, you can use the DescribeReservedInstances operation. Only Standard
      # Reserved Instances can be sold in the Reserved Instance Marketplace. Convertible Reserved Instances
      # cannot be sold. The Reserved Instance Marketplace matches sellers who want to resell Standard
      # Reserved Instance capacity that they no longer need with buyers who want to purchase additional
      # capacity. Reserved Instances bought and sold through the Reserved Instance Marketplace work like any
      # other Reserved Instances. To sell your Standard Reserved Instances, you must first register as a
      # seller in the Reserved Instance Marketplace. After completing the registration process, you can
      # create a Reserved Instance Marketplace listing of some or all of your Standard Reserved Instances,
      # and specify the upfront price to receive for them. Your Standard Reserved Instance listings then
      # become available for purchase. To view the details of your Standard Reserved Instance listing, you
      # can use the DescribeReservedInstancesListings operation. For more information, see Sell in the
      # Reserved Instance Marketplace in the Amazon EC2 User Guide .

      def create_reserved_instances_listing(
        client_token : String,
        instance_count : Int32,
        price_schedules : Array(Types::PriceScheduleSpecification),
        reserved_instances_id : String
      ) : Types::CreateReservedInstancesListingResult

        input = Types::CreateReservedInstancesListingRequest.new(client_token: client_token, instance_count: instance_count, price_schedules: price_schedules, reserved_instances_id: reserved_instances_id)
        create_reserved_instances_listing(input)
      end

      def create_reserved_instances_listing(input : Types::CreateReservedInstancesListingRequest) : Types::CreateReservedInstancesListingResult
        request = Protocol::Ec2.build_request(Model::CREATE_RESERVED_INSTANCES_LISTING, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateReservedInstancesListingResult, "CreateReservedInstancesListing")
      end

      # Starts a task that restores an AMI from an Amazon S3 object that was previously created by using
      # CreateStoreImageTask . To use this API, you must have the required permissions. For more
      # information, see Permissions for storing and restoring AMIs using S3 in the Amazon EC2 User Guide .
      # For more information, see Store and restore an AMI using S3 in the Amazon EC2 User Guide .

      def create_restore_image_task(
        bucket : String,
        object_key : String,
        dry_run : Bool? = nil,
        name : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateRestoreImageTaskResult

        input = Types::CreateRestoreImageTaskRequest.new(bucket: bucket, object_key: object_key, dry_run: dry_run, name: name, tag_specifications: tag_specifications)
        create_restore_image_task(input)
      end

      def create_restore_image_task(input : Types::CreateRestoreImageTaskRequest) : Types::CreateRestoreImageTaskResult
        request = Protocol::Ec2.build_request(Model::CREATE_RESTORE_IMAGE_TASK, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateRestoreImageTaskResult, "CreateRestoreImageTask")
      end

      # Creates a route in a route table within a VPC. You must specify either a destination CIDR block or a
      # prefix list ID. You must also specify exactly one of the resources from the parameter list. When
      # determining how to route traffic, we use the route with the most specific match. For example,
      # traffic is destined for the IPv4 address 192.0.2.3 , and the route table includes the following two
      # IPv4 routes: 192.0.2.0/24 (goes to some target A) 192.0.2.0/28 (goes to some target B) Both routes
      # apply to the traffic destined for 192.0.2.3 . However, the second route in the list covers a smaller
      # number of IP addresses and is therefore more specific, so we use that route to determine where to
      # target the traffic. For more information about route tables, see Route tables in the Amazon VPC User
      # Guide .

      def create_route(
        route_table_id : String,
        carrier_gateway_id : String? = nil,
        core_network_arn : String? = nil,
        destination_cidr_block : String? = nil,
        destination_ipv6_cidr_block : String? = nil,
        destination_prefix_list_id : String? = nil,
        dry_run : Bool? = nil,
        egress_only_internet_gateway_id : String? = nil,
        gateway_id : String? = nil,
        instance_id : String? = nil,
        local_gateway_id : String? = nil,
        nat_gateway_id : String? = nil,
        network_interface_id : String? = nil,
        odb_network_arn : String? = nil,
        transit_gateway_id : String? = nil,
        vpc_endpoint_id : String? = nil,
        vpc_peering_connection_id : String? = nil
      ) : Types::CreateRouteResult

        input = Types::CreateRouteRequest.new(route_table_id: route_table_id, carrier_gateway_id: carrier_gateway_id, core_network_arn: core_network_arn, destination_cidr_block: destination_cidr_block, destination_ipv6_cidr_block: destination_ipv6_cidr_block, destination_prefix_list_id: destination_prefix_list_id, dry_run: dry_run, egress_only_internet_gateway_id: egress_only_internet_gateway_id, gateway_id: gateway_id, instance_id: instance_id, local_gateway_id: local_gateway_id, nat_gateway_id: nat_gateway_id, network_interface_id: network_interface_id, odb_network_arn: odb_network_arn, transit_gateway_id: transit_gateway_id, vpc_endpoint_id: vpc_endpoint_id, vpc_peering_connection_id: vpc_peering_connection_id)
        create_route(input)
      end

      def create_route(input : Types::CreateRouteRequest) : Types::CreateRouteResult
        request = Protocol::Ec2.build_request(Model::CREATE_ROUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateRouteResult, "CreateRoute")
      end

      # Creates a new route server to manage dynamic routing in a VPC. Amazon VPC Route Server simplifies
      # routing for traffic between workloads that are deployed within a VPC and its internet gateways. With
      # this feature, VPC Route Server dynamically updates VPC and internet gateway route tables with your
      # preferred IPv4 or IPv6 routes to achieve routing fault tolerance for those workloads. This enables
      # you to automatically reroute traffic within a VPC, which increases the manageability of VPC routing
      # and interoperability with third-party workloads. Route server supports the follow route table types:
      # VPC route tables not associated with subnets Subnet route tables Internet gateway route tables Route
      # server does not support route tables associated with virtual private gateways. To propagate routes
      # into a transit gateway route table, use Transit Gateway Connect . For more information see Dynamic
      # routing in your VPC with VPC Route Server in the Amazon VPC User Guide .

      def create_route_server(
        amazon_side_asn : Int64,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        persist_routes : String? = nil,
        persist_routes_duration : Int64? = nil,
        sns_notifications_enabled : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateRouteServerResult

        input = Types::CreateRouteServerRequest.new(amazon_side_asn: amazon_side_asn, client_token: client_token, dry_run: dry_run, persist_routes: persist_routes, persist_routes_duration: persist_routes_duration, sns_notifications_enabled: sns_notifications_enabled, tag_specifications: tag_specifications)
        create_route_server(input)
      end

      def create_route_server(input : Types::CreateRouteServerRequest) : Types::CreateRouteServerResult
        request = Protocol::Ec2.build_request(Model::CREATE_ROUTE_SERVER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateRouteServerResult, "CreateRouteServer")
      end

      # Creates a new endpoint for a route server in a specified subnet. A route server endpoint is an
      # Amazon Web Services-managed component inside a subnet that facilitates BGP (Border Gateway Protocol)
      # connections between your route server and your BGP peers. For more information see Dynamic routing
      # in your VPC with VPC Route Server in the Amazon VPC User Guide .

      def create_route_server_endpoint(
        route_server_id : String,
        subnet_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateRouteServerEndpointResult

        input = Types::CreateRouteServerEndpointRequest.new(route_server_id: route_server_id, subnet_id: subnet_id, client_token: client_token, dry_run: dry_run, tag_specifications: tag_specifications)
        create_route_server_endpoint(input)
      end

      def create_route_server_endpoint(input : Types::CreateRouteServerEndpointRequest) : Types::CreateRouteServerEndpointResult
        request = Protocol::Ec2.build_request(Model::CREATE_ROUTE_SERVER_ENDPOINT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateRouteServerEndpointResult, "CreateRouteServerEndpoint")
      end

      # Creates a new BGP peer for a specified route server endpoint. A route server peer is a session
      # between a route server endpoint and the device deployed in Amazon Web Services (such as a firewall
      # appliance or other network security function running on an EC2 instance). The device must meet these
      # requirements: Have an elastic network interface in the VPC Support BGP (Border Gateway Protocol) Can
      # initiate BGP sessions For more information see Dynamic routing in your VPC with VPC Route Server in
      # the Amazon VPC User Guide .

      def create_route_server_peer(
        bgp_options : Types::RouteServerBgpOptionsRequest,
        peer_address : String,
        route_server_endpoint_id : String,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateRouteServerPeerResult

        input = Types::CreateRouteServerPeerRequest.new(bgp_options: bgp_options, peer_address: peer_address, route_server_endpoint_id: route_server_endpoint_id, dry_run: dry_run, tag_specifications: tag_specifications)
        create_route_server_peer(input)
      end

      def create_route_server_peer(input : Types::CreateRouteServerPeerRequest) : Types::CreateRouteServerPeerResult
        request = Protocol::Ec2.build_request(Model::CREATE_ROUTE_SERVER_PEER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateRouteServerPeerResult, "CreateRouteServerPeer")
      end

      # Creates a route table for the specified VPC. After you create a route table, you can add routes and
      # associate the table with a subnet. For more information, see Route tables in the Amazon VPC User
      # Guide .

      def create_route_table(
        vpc_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateRouteTableResult

        input = Types::CreateRouteTableRequest.new(vpc_id: vpc_id, client_token: client_token, dry_run: dry_run, tag_specifications: tag_specifications)
        create_route_table(input)
      end

      def create_route_table(input : Types::CreateRouteTableRequest) : Types::CreateRouteTableResult
        request = Protocol::Ec2.build_request(Model::CREATE_ROUTE_TABLE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateRouteTableResult, "CreateRouteTable")
      end

      # Creates a security group. A security group acts as a virtual firewall for your instance to control
      # inbound and outbound traffic. For more information, see Amazon EC2 security groups in the Amazon EC2
      # User Guide and Security groups for your VPC in the Amazon VPC User Guide . When you create a
      # security group, you specify a friendly name of your choice. You can't have two security groups for
      # the same VPC with the same name. You have a default security group for use in your VPC. If you don't
      # specify a security group when you launch an instance, the instance is launched into the appropriate
      # default security group. A default security group includes a default rule that grants instances
      # unrestricted network access to each other. You can add or remove rules from your security groups
      # using AuthorizeSecurityGroupIngress , AuthorizeSecurityGroupEgress , RevokeSecurityGroupIngress ,
      # and RevokeSecurityGroupEgress . For more information about VPC security group limits, see Amazon VPC
      # Limits .

      def create_security_group(
        description : String,
        group_name : String,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        vpc_id : String? = nil
      ) : Types::CreateSecurityGroupResult

        input = Types::CreateSecurityGroupRequest.new(description: description, group_name: group_name, dry_run: dry_run, tag_specifications: tag_specifications, vpc_id: vpc_id)
        create_security_group(input)
      end

      def create_security_group(input : Types::CreateSecurityGroupRequest) : Types::CreateSecurityGroupResult
        request = Protocol::Ec2.build_request(Model::CREATE_SECURITY_GROUP, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateSecurityGroupResult, "CreateSecurityGroup")
      end

      # Creates a snapshot of an EBS volume and stores it in Amazon S3. You can use snapshots for backups,
      # to make copies of EBS volumes, and to save data before shutting down an instance. The location of
      # the source EBS volume determines where you can create the snapshot. If the source volume is in a
      # Region, you must create the snapshot in the same Region as the volume. If the source volume is in a
      # Local Zone, you can create the snapshot in the same Local Zone or in its parent Amazon Web Services
      # Region. If the source volume is on an Outpost, you can create the snapshot on the same Outpost or in
      # its parent Amazon Web Services Region. When a snapshot is created, any Amazon Web Services
      # Marketplace product codes that are associated with the source volume are propagated to the snapshot.
      # You can take a snapshot of an attached volume that is in use. However, snapshots only capture data
      # that has been written to your Amazon EBS volume at the time the snapshot command is issued; this
      # might exclude any data that has been cached by any applications or the operating system. If you can
      # pause any file systems on the volume long enough to take a snapshot, your snapshot should be
      # complete. However, if you cannot pause all file writes to the volume, you should unmount the volume
      # from within the instance, issue the snapshot command, and then remount the volume to ensure a
      # consistent and complete snapshot. You may remount and use your volume while the snapshot status is
      # pending . When you create a snapshot for an EBS volume that serves as a root device, we recommend
      # that you stop the instance before taking the snapshot. Snapshots that are taken from encrypted
      # volumes are automatically encrypted. Volumes that are created from encrypted snapshots are also
      # automatically encrypted. Your encrypted volumes and any associated snapshots always remain
      # protected. For more information, see Amazon EBS encryption in the Amazon EBS User Guide .

      def create_snapshot(
        volume_id : String,
        description : String? = nil,
        dry_run : Bool? = nil,
        location : String? = nil,
        outpost_arn : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::Snapshot

        input = Types::CreateSnapshotRequest.new(volume_id: volume_id, description: description, dry_run: dry_run, location: location, outpost_arn: outpost_arn, tag_specifications: tag_specifications)
        create_snapshot(input)
      end

      def create_snapshot(input : Types::CreateSnapshotRequest) : Types::Snapshot
        request = Protocol::Ec2.build_request(Model::CREATE_SNAPSHOT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::Snapshot, "CreateSnapshot")
      end

      # Creates crash-consistent snapshots of multiple EBS volumes attached to an Amazon EC2 instance.
      # Volumes are chosen by specifying an instance. Each volume attached to the specified instance will
      # produce one snapshot that is crash-consistent across the instance. You can include all of the
      # volumes currently attached to the instance, or you can exclude the root volume or specific data
      # (non-root) volumes from the multi-volume snapshot set. The location of the source instance
      # determines where you can create the snapshots. If the source instance is in a Region, you must
      # create the snapshots in the same Region as the instance. If the source instance is in a Local Zone,
      # you can create the snapshots in the same Local Zone or in its parent Amazon Web Services Region. If
      # the source instance is on an Outpost, you can create the snapshots on the same Outpost or in its
      # parent Amazon Web Services Region.

      def create_snapshots(
        instance_specification : Types::InstanceSpecification,
        copy_tags_from_source : String? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        location : String? = nil,
        outpost_arn : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateSnapshotsResult

        input = Types::CreateSnapshotsRequest.new(instance_specification: instance_specification, copy_tags_from_source: copy_tags_from_source, description: description, dry_run: dry_run, location: location, outpost_arn: outpost_arn, tag_specifications: tag_specifications)
        create_snapshots(input)
      end

      def create_snapshots(input : Types::CreateSnapshotsRequest) : Types::CreateSnapshotsResult
        request = Protocol::Ec2.build_request(Model::CREATE_SNAPSHOTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateSnapshotsResult, "CreateSnapshots")
      end

      # Creates a data feed for Spot Instances, enabling you to view Spot Instance usage logs. You can
      # create one data feed per Amazon Web Services account. For more information, see Spot Instance data
      # feed in the Amazon EC2 User Guide .

      def create_spot_datafeed_subscription(
        bucket : String,
        dry_run : Bool? = nil,
        prefix : String? = nil
      ) : Types::CreateSpotDatafeedSubscriptionResult

        input = Types::CreateSpotDatafeedSubscriptionRequest.new(bucket: bucket, dry_run: dry_run, prefix: prefix)
        create_spot_datafeed_subscription(input)
      end

      def create_spot_datafeed_subscription(input : Types::CreateSpotDatafeedSubscriptionRequest) : Types::CreateSpotDatafeedSubscriptionResult
        request = Protocol::Ec2.build_request(Model::CREATE_SPOT_DATAFEED_SUBSCRIPTION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateSpotDatafeedSubscriptionResult, "CreateSpotDatafeedSubscription")
      end

      # Stores an AMI as a single object in an Amazon S3 bucket. To use this API, you must have the required
      # permissions. For more information, see Permissions for storing and restoring AMIs using S3 in the
      # Amazon EC2 User Guide . For more information, see Store and restore an AMI using S3 in the Amazon
      # EC2 User Guide .

      def create_store_image_task(
        bucket : String,
        image_id : String,
        dry_run : Bool? = nil,
        s3_object_tags : Array(Types::S3ObjectTag)? = nil
      ) : Types::CreateStoreImageTaskResult

        input = Types::CreateStoreImageTaskRequest.new(bucket: bucket, image_id: image_id, dry_run: dry_run, s3_object_tags: s3_object_tags)
        create_store_image_task(input)
      end

      def create_store_image_task(input : Types::CreateStoreImageTaskRequest) : Types::CreateStoreImageTaskResult
        request = Protocol::Ec2.build_request(Model::CREATE_STORE_IMAGE_TASK, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateStoreImageTaskResult, "CreateStoreImageTask")
      end

      # Creates a subnet in the specified VPC. For an IPv4 only subnet, specify an IPv4 CIDR block. If the
      # VPC has an IPv6 CIDR block, you can create an IPv6 only subnet or a dual stack subnet instead. For
      # an IPv6 only subnet, specify an IPv6 CIDR block. For a dual stack subnet, specify both an IPv4 CIDR
      # block and an IPv6 CIDR block. A subnet CIDR block must not overlap the CIDR block of an existing
      # subnet in the VPC. After you create a subnet, you can't change its CIDR block. The allowed size for
      # an IPv4 subnet is between a /28 netmask (16 IP addresses) and a /16 netmask (65,536 IP addresses).
      # Amazon Web Services reserves both the first four and the last IPv4 address in each subnet's CIDR
      # block. They're not available for your use. If you've associated an IPv6 CIDR block with your VPC,
      # you can associate an IPv6 CIDR block with a subnet when you create it. If you add more than one
      # subnet to a VPC, they're set up in a star topology with a logical router in the middle. When you
      # stop an instance in a subnet, it retains its private IPv4 address. It's therefore possible to have a
      # subnet with no running instances (they're all stopped), but no remaining IP addresses available. For
      # more information, see Subnets in the Amazon VPC User Guide .

      def create_subnet(
        vpc_id : String,
        availability_zone : String? = nil,
        availability_zone_id : String? = nil,
        cidr_block : String? = nil,
        dry_run : Bool? = nil,
        ipv4_ipam_pool_id : String? = nil,
        ipv4_netmask_length : Int32? = nil,
        ipv6_cidr_block : String? = nil,
        ipv6_ipam_pool_id : String? = nil,
        ipv6_native : Bool? = nil,
        ipv6_netmask_length : Int32? = nil,
        outpost_arn : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateSubnetResult

        input = Types::CreateSubnetRequest.new(vpc_id: vpc_id, availability_zone: availability_zone, availability_zone_id: availability_zone_id, cidr_block: cidr_block, dry_run: dry_run, ipv4_ipam_pool_id: ipv4_ipam_pool_id, ipv4_netmask_length: ipv4_netmask_length, ipv6_cidr_block: ipv6_cidr_block, ipv6_ipam_pool_id: ipv6_ipam_pool_id, ipv6_native: ipv6_native, ipv6_netmask_length: ipv6_netmask_length, outpost_arn: outpost_arn, tag_specifications: tag_specifications)
        create_subnet(input)
      end

      def create_subnet(input : Types::CreateSubnetRequest) : Types::CreateSubnetResult
        request = Protocol::Ec2.build_request(Model::CREATE_SUBNET, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateSubnetResult, "CreateSubnet")
      end

      # Creates a subnet CIDR reservation. For more information, see Subnet CIDR reservations in the Amazon
      # VPC User Guide and Manage prefixes for your network interfaces in the Amazon EC2 User Guide .

      def create_subnet_cidr_reservation(
        cidr : String,
        reservation_type : String,
        subnet_id : String,
        description : String? = nil,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateSubnetCidrReservationResult

        input = Types::CreateSubnetCidrReservationRequest.new(cidr: cidr, reservation_type: reservation_type, subnet_id: subnet_id, description: description, dry_run: dry_run, tag_specifications: tag_specifications)
        create_subnet_cidr_reservation(input)
      end

      def create_subnet_cidr_reservation(input : Types::CreateSubnetCidrReservationRequest) : Types::CreateSubnetCidrReservationResult
        request = Protocol::Ec2.build_request(Model::CREATE_SUBNET_CIDR_RESERVATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateSubnetCidrReservationResult, "CreateSubnetCidrReservation")
      end

      # Adds or overwrites only the specified tags for the specified Amazon EC2 resource or resources. When
      # you specify an existing tag key, the value is overwritten with the new value. Each resource can have
      # a maximum of 50 tags. Each tag consists of a key and optional value. Tag keys must be unique per
      # resource. For more information about tags, see Tag your Amazon EC2 resources in the Amazon Elastic
      # Compute Cloud User Guide . For more information about creating IAM policies that control users'
      # access to resources based on tags, see Supported resource-level permissions for Amazon EC2 API
      # actions in the Amazon Elastic Compute Cloud User Guide .

      def create_tags(
        resources : Array(String),
        tags : Array(Types::Tag),
        dry_run : Bool? = nil
      ) : Nil

        input = Types::CreateTagsRequest.new(resources: resources, tags: tags, dry_run: dry_run)
        create_tags(input)
      end

      def create_tags(input : Types::CreateTagsRequest) : Nil
        request = Protocol::Ec2.build_request(Model::CREATE_TAGS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Creates a Traffic Mirror filter. A Traffic Mirror filter is a set of rules that defines the traffic
      # to mirror. By default, no traffic is mirrored. To mirror traffic, use CreateTrafficMirrorFilterRule
      # to add Traffic Mirror rules to the filter. The rules you add define what traffic gets mirrored. You
      # can also use ModifyTrafficMirrorFilterNetworkServices to mirror supported network services.

      def create_traffic_mirror_filter(
        client_token : String? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateTrafficMirrorFilterResult

        input = Types::CreateTrafficMirrorFilterRequest.new(client_token: client_token, description: description, dry_run: dry_run, tag_specifications: tag_specifications)
        create_traffic_mirror_filter(input)
      end

      def create_traffic_mirror_filter(input : Types::CreateTrafficMirrorFilterRequest) : Types::CreateTrafficMirrorFilterResult
        request = Protocol::Ec2.build_request(Model::CREATE_TRAFFIC_MIRROR_FILTER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateTrafficMirrorFilterResult, "CreateTrafficMirrorFilter")
      end

      # Creates a Traffic Mirror filter rule. A Traffic Mirror rule defines the Traffic Mirror source
      # traffic to mirror. You need the Traffic Mirror filter ID when you create the rule.

      def create_traffic_mirror_filter_rule(
        destination_cidr_block : String,
        rule_action : String,
        rule_number : Int32,
        source_cidr_block : String,
        traffic_direction : String,
        traffic_mirror_filter_id : String,
        client_token : String? = nil,
        description : String? = nil,
        destination_port_range : Types::TrafficMirrorPortRangeRequest? = nil,
        dry_run : Bool? = nil,
        protocol : Int32? = nil,
        source_port_range : Types::TrafficMirrorPortRangeRequest? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateTrafficMirrorFilterRuleResult

        input = Types::CreateTrafficMirrorFilterRuleRequest.new(destination_cidr_block: destination_cidr_block, rule_action: rule_action, rule_number: rule_number, source_cidr_block: source_cidr_block, traffic_direction: traffic_direction, traffic_mirror_filter_id: traffic_mirror_filter_id, client_token: client_token, description: description, destination_port_range: destination_port_range, dry_run: dry_run, protocol: protocol, source_port_range: source_port_range, tag_specifications: tag_specifications)
        create_traffic_mirror_filter_rule(input)
      end

      def create_traffic_mirror_filter_rule(input : Types::CreateTrafficMirrorFilterRuleRequest) : Types::CreateTrafficMirrorFilterRuleResult
        request = Protocol::Ec2.build_request(Model::CREATE_TRAFFIC_MIRROR_FILTER_RULE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateTrafficMirrorFilterRuleResult, "CreateTrafficMirrorFilterRule")
      end

      # Creates a Traffic Mirror session. A Traffic Mirror session actively copies packets from a Traffic
      # Mirror source to a Traffic Mirror target. Create a filter, and then assign it to the session to
      # define a subset of the traffic to mirror, for example all TCP traffic. The Traffic Mirror source and
      # the Traffic Mirror target (monitoring appliances) can be in the same VPC, or in a different VPC
      # connected via VPC peering or a transit gateway. By default, no traffic is mirrored. Use
      # CreateTrafficMirrorFilter to create filter rules that specify the traffic to mirror.

      def create_traffic_mirror_session(
        network_interface_id : String,
        session_number : Int32,
        traffic_mirror_filter_id : String,
        traffic_mirror_target_id : String,
        client_token : String? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        packet_length : Int32? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        virtual_network_id : Int32? = nil
      ) : Types::CreateTrafficMirrorSessionResult

        input = Types::CreateTrafficMirrorSessionRequest.new(network_interface_id: network_interface_id, session_number: session_number, traffic_mirror_filter_id: traffic_mirror_filter_id, traffic_mirror_target_id: traffic_mirror_target_id, client_token: client_token, description: description, dry_run: dry_run, packet_length: packet_length, tag_specifications: tag_specifications, virtual_network_id: virtual_network_id)
        create_traffic_mirror_session(input)
      end

      def create_traffic_mirror_session(input : Types::CreateTrafficMirrorSessionRequest) : Types::CreateTrafficMirrorSessionResult
        request = Protocol::Ec2.build_request(Model::CREATE_TRAFFIC_MIRROR_SESSION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateTrafficMirrorSessionResult, "CreateTrafficMirrorSession")
      end

      # Creates a target for your Traffic Mirror session. A Traffic Mirror target is the destination for
      # mirrored traffic. The Traffic Mirror source and the Traffic Mirror target (monitoring appliances)
      # can be in the same VPC, or in different VPCs connected via VPC peering or a transit gateway. A
      # Traffic Mirror target can be a network interface, a Network Load Balancer, or a Gateway Load
      # Balancer endpoint. To use the target in a Traffic Mirror session, use CreateTrafficMirrorSession .

      def create_traffic_mirror_target(
        client_token : String? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        gateway_load_balancer_endpoint_id : String? = nil,
        network_interface_id : String? = nil,
        network_load_balancer_arn : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateTrafficMirrorTargetResult

        input = Types::CreateTrafficMirrorTargetRequest.new(client_token: client_token, description: description, dry_run: dry_run, gateway_load_balancer_endpoint_id: gateway_load_balancer_endpoint_id, network_interface_id: network_interface_id, network_load_balancer_arn: network_load_balancer_arn, tag_specifications: tag_specifications)
        create_traffic_mirror_target(input)
      end

      def create_traffic_mirror_target(input : Types::CreateTrafficMirrorTargetRequest) : Types::CreateTrafficMirrorTargetResult
        request = Protocol::Ec2.build_request(Model::CREATE_TRAFFIC_MIRROR_TARGET, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateTrafficMirrorTargetResult, "CreateTrafficMirrorTarget")
      end

      # Creates a transit gateway. You can use a transit gateway to interconnect your virtual private clouds
      # (VPC) and on-premises networks. After the transit gateway enters the available state, you can attach
      # your VPCs and VPN connections to the transit gateway. To attach your VPCs, use
      # CreateTransitGatewayVpcAttachment . To attach a VPN connection, use CreateCustomerGateway to create
      # a customer gateway and specify the ID of the customer gateway and the ID of the transit gateway in a
      # call to CreateVpnConnection . When you create a transit gateway, we create a default transit gateway
      # route table and use it as the default association route table and the default propagation route
      # table. You can use CreateTransitGatewayRouteTable to create additional transit gateway route tables.
      # If you disable automatic route propagation, we do not create a default transit gateway route table.
      # You can use EnableTransitGatewayRouteTablePropagation to propagate routes from a resource attachment
      # to a transit gateway route table. If you disable automatic associations, you can use
      # AssociateTransitGatewayRouteTable to associate a resource attachment with a transit gateway route
      # table.

      def create_transit_gateway(
        description : String? = nil,
        dry_run : Bool? = nil,
        options : Types::TransitGatewayRequestOptions? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateTransitGatewayResult

        input = Types::CreateTransitGatewayRequest.new(description: description, dry_run: dry_run, options: options, tag_specifications: tag_specifications)
        create_transit_gateway(input)
      end

      def create_transit_gateway(input : Types::CreateTransitGatewayRequest) : Types::CreateTransitGatewayResult
        request = Protocol::Ec2.build_request(Model::CREATE_TRANSIT_GATEWAY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateTransitGatewayResult, "CreateTransitGateway")
      end

      # Creates a Connect attachment from a specified transit gateway attachment. A Connect attachment is a
      # GRE-based tunnel attachment that you can use to establish a connection between a transit gateway and
      # an appliance. A Connect attachment uses an existing VPC or Amazon Web Services Direct Connect
      # attachment as the underlying transport mechanism.

      def create_transit_gateway_connect(
        options : Types::CreateTransitGatewayConnectRequestOptions,
        transport_transit_gateway_attachment_id : String,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateTransitGatewayConnectResult

        input = Types::CreateTransitGatewayConnectRequest.new(options: options, transport_transit_gateway_attachment_id: transport_transit_gateway_attachment_id, dry_run: dry_run, tag_specifications: tag_specifications)
        create_transit_gateway_connect(input)
      end

      def create_transit_gateway_connect(input : Types::CreateTransitGatewayConnectRequest) : Types::CreateTransitGatewayConnectResult
        request = Protocol::Ec2.build_request(Model::CREATE_TRANSIT_GATEWAY_CONNECT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateTransitGatewayConnectResult, "CreateTransitGatewayConnect")
      end

      # Creates a Connect peer for a specified transit gateway Connect attachment between a transit gateway
      # and an appliance. The peer address and transit gateway address must be the same IP address family
      # (IPv4 or IPv6). For more information, see Connect peers in the Amazon Web Services Transit Gateways
      # Guide .

      def create_transit_gateway_connect_peer(
        inside_cidr_blocks : Array(String),
        peer_address : String,
        transit_gateway_attachment_id : String,
        bgp_options : Types::TransitGatewayConnectRequestBgpOptions? = nil,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        transit_gateway_address : String? = nil
      ) : Types::CreateTransitGatewayConnectPeerResult

        input = Types::CreateTransitGatewayConnectPeerRequest.new(inside_cidr_blocks: inside_cidr_blocks, peer_address: peer_address, transit_gateway_attachment_id: transit_gateway_attachment_id, bgp_options: bgp_options, dry_run: dry_run, tag_specifications: tag_specifications, transit_gateway_address: transit_gateway_address)
        create_transit_gateway_connect_peer(input)
      end

      def create_transit_gateway_connect_peer(input : Types::CreateTransitGatewayConnectPeerRequest) : Types::CreateTransitGatewayConnectPeerResult
        request = Protocol::Ec2.build_request(Model::CREATE_TRANSIT_GATEWAY_CONNECT_PEER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateTransitGatewayConnectPeerResult, "CreateTransitGatewayConnectPeer")
      end

      # Creates a metering policy for a transit gateway to track and measure network traffic.

      def create_transit_gateway_metering_policy(
        transit_gateway_id : String,
        dry_run : Bool? = nil,
        middlebox_attachment_ids : Array(String)? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateTransitGatewayMeteringPolicyResult

        input = Types::CreateTransitGatewayMeteringPolicyRequest.new(transit_gateway_id: transit_gateway_id, dry_run: dry_run, middlebox_attachment_ids: middlebox_attachment_ids, tag_specifications: tag_specifications)
        create_transit_gateway_metering_policy(input)
      end

      def create_transit_gateway_metering_policy(input : Types::CreateTransitGatewayMeteringPolicyRequest) : Types::CreateTransitGatewayMeteringPolicyResult
        request = Protocol::Ec2.build_request(Model::CREATE_TRANSIT_GATEWAY_METERING_POLICY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateTransitGatewayMeteringPolicyResult, "CreateTransitGatewayMeteringPolicy")
      end

      # Creates an entry in a transit gateway metering policy to define traffic measurement rules.

      def create_transit_gateway_metering_policy_entry(
        metered_account : String,
        policy_rule_number : Int32,
        transit_gateway_metering_policy_id : String,
        destination_cidr_block : String? = nil,
        destination_port_range : String? = nil,
        destination_transit_gateway_attachment_id : String? = nil,
        destination_transit_gateway_attachment_type : String? = nil,
        dry_run : Bool? = nil,
        protocol : String? = nil,
        source_cidr_block : String? = nil,
        source_port_range : String? = nil,
        source_transit_gateway_attachment_id : String? = nil,
        source_transit_gateway_attachment_type : String? = nil
      ) : Types::CreateTransitGatewayMeteringPolicyEntryResult

        input = Types::CreateTransitGatewayMeteringPolicyEntryRequest.new(metered_account: metered_account, policy_rule_number: policy_rule_number, transit_gateway_metering_policy_id: transit_gateway_metering_policy_id, destination_cidr_block: destination_cidr_block, destination_port_range: destination_port_range, destination_transit_gateway_attachment_id: destination_transit_gateway_attachment_id, destination_transit_gateway_attachment_type: destination_transit_gateway_attachment_type, dry_run: dry_run, protocol: protocol, source_cidr_block: source_cidr_block, source_port_range: source_port_range, source_transit_gateway_attachment_id: source_transit_gateway_attachment_id, source_transit_gateway_attachment_type: source_transit_gateway_attachment_type)
        create_transit_gateway_metering_policy_entry(input)
      end

      def create_transit_gateway_metering_policy_entry(input : Types::CreateTransitGatewayMeteringPolicyEntryRequest) : Types::CreateTransitGatewayMeteringPolicyEntryResult
        request = Protocol::Ec2.build_request(Model::CREATE_TRANSIT_GATEWAY_METERING_POLICY_ENTRY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateTransitGatewayMeteringPolicyEntryResult, "CreateTransitGatewayMeteringPolicyEntry")
      end

      # Creates a multicast domain using the specified transit gateway. The transit gateway must be in the
      # available state before you create a domain. Use DescribeTransitGateways to see the state of transit
      # gateway.

      def create_transit_gateway_multicast_domain(
        transit_gateway_id : String,
        dry_run : Bool? = nil,
        options : Types::CreateTransitGatewayMulticastDomainRequestOptions? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateTransitGatewayMulticastDomainResult

        input = Types::CreateTransitGatewayMulticastDomainRequest.new(transit_gateway_id: transit_gateway_id, dry_run: dry_run, options: options, tag_specifications: tag_specifications)
        create_transit_gateway_multicast_domain(input)
      end

      def create_transit_gateway_multicast_domain(input : Types::CreateTransitGatewayMulticastDomainRequest) : Types::CreateTransitGatewayMulticastDomainResult
        request = Protocol::Ec2.build_request(Model::CREATE_TRANSIT_GATEWAY_MULTICAST_DOMAIN, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateTransitGatewayMulticastDomainResult, "CreateTransitGatewayMulticastDomain")
      end

      # Requests a transit gateway peering attachment between the specified transit gateway (requester) and
      # a peer transit gateway (accepter). The peer transit gateway can be in your account or a different
      # Amazon Web Services account. After you create the peering attachment, the owner of the accepter
      # transit gateway must accept the attachment request.

      def create_transit_gateway_peering_attachment(
        peer_account_id : String,
        peer_region : String,
        peer_transit_gateway_id : String,
        transit_gateway_id : String,
        dry_run : Bool? = nil,
        options : Types::CreateTransitGatewayPeeringAttachmentRequestOptions? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateTransitGatewayPeeringAttachmentResult

        input = Types::CreateTransitGatewayPeeringAttachmentRequest.new(peer_account_id: peer_account_id, peer_region: peer_region, peer_transit_gateway_id: peer_transit_gateway_id, transit_gateway_id: transit_gateway_id, dry_run: dry_run, options: options, tag_specifications: tag_specifications)
        create_transit_gateway_peering_attachment(input)
      end

      def create_transit_gateway_peering_attachment(input : Types::CreateTransitGatewayPeeringAttachmentRequest) : Types::CreateTransitGatewayPeeringAttachmentResult
        request = Protocol::Ec2.build_request(Model::CREATE_TRANSIT_GATEWAY_PEERING_ATTACHMENT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateTransitGatewayPeeringAttachmentResult, "CreateTransitGatewayPeeringAttachment")
      end

      # Creates a transit gateway policy table.

      def create_transit_gateway_policy_table(
        transit_gateway_id : String,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateTransitGatewayPolicyTableResult

        input = Types::CreateTransitGatewayPolicyTableRequest.new(transit_gateway_id: transit_gateway_id, dry_run: dry_run, tag_specifications: tag_specifications)
        create_transit_gateway_policy_table(input)
      end

      def create_transit_gateway_policy_table(input : Types::CreateTransitGatewayPolicyTableRequest) : Types::CreateTransitGatewayPolicyTableResult
        request = Protocol::Ec2.build_request(Model::CREATE_TRANSIT_GATEWAY_POLICY_TABLE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateTransitGatewayPolicyTableResult, "CreateTransitGatewayPolicyTable")
      end

      # Creates a reference (route) to a prefix list in a specified transit gateway route table.

      def create_transit_gateway_prefix_list_reference(
        prefix_list_id : String,
        transit_gateway_route_table_id : String,
        blackhole : Bool? = nil,
        dry_run : Bool? = nil,
        transit_gateway_attachment_id : String? = nil
      ) : Types::CreateTransitGatewayPrefixListReferenceResult

        input = Types::CreateTransitGatewayPrefixListReferenceRequest.new(prefix_list_id: prefix_list_id, transit_gateway_route_table_id: transit_gateway_route_table_id, blackhole: blackhole, dry_run: dry_run, transit_gateway_attachment_id: transit_gateway_attachment_id)
        create_transit_gateway_prefix_list_reference(input)
      end

      def create_transit_gateway_prefix_list_reference(input : Types::CreateTransitGatewayPrefixListReferenceRequest) : Types::CreateTransitGatewayPrefixListReferenceResult
        request = Protocol::Ec2.build_request(Model::CREATE_TRANSIT_GATEWAY_PREFIX_LIST_REFERENCE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateTransitGatewayPrefixListReferenceResult, "CreateTransitGatewayPrefixListReference")
      end

      # Creates a static route for the specified transit gateway route table.

      def create_transit_gateway_route(
        destination_cidr_block : String,
        transit_gateway_route_table_id : String,
        blackhole : Bool? = nil,
        dry_run : Bool? = nil,
        transit_gateway_attachment_id : String? = nil
      ) : Types::CreateTransitGatewayRouteResult

        input = Types::CreateTransitGatewayRouteRequest.new(destination_cidr_block: destination_cidr_block, transit_gateway_route_table_id: transit_gateway_route_table_id, blackhole: blackhole, dry_run: dry_run, transit_gateway_attachment_id: transit_gateway_attachment_id)
        create_transit_gateway_route(input)
      end

      def create_transit_gateway_route(input : Types::CreateTransitGatewayRouteRequest) : Types::CreateTransitGatewayRouteResult
        request = Protocol::Ec2.build_request(Model::CREATE_TRANSIT_GATEWAY_ROUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateTransitGatewayRouteResult, "CreateTransitGatewayRoute")
      end

      # Creates a route table for the specified transit gateway.

      def create_transit_gateway_route_table(
        transit_gateway_id : String,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateTransitGatewayRouteTableResult

        input = Types::CreateTransitGatewayRouteTableRequest.new(transit_gateway_id: transit_gateway_id, dry_run: dry_run, tag_specifications: tag_specifications)
        create_transit_gateway_route_table(input)
      end

      def create_transit_gateway_route_table(input : Types::CreateTransitGatewayRouteTableRequest) : Types::CreateTransitGatewayRouteTableResult
        request = Protocol::Ec2.build_request(Model::CREATE_TRANSIT_GATEWAY_ROUTE_TABLE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateTransitGatewayRouteTableResult, "CreateTransitGatewayRouteTable")
      end

      # Advertises a new transit gateway route table.

      def create_transit_gateway_route_table_announcement(
        peering_attachment_id : String,
        transit_gateway_route_table_id : String,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateTransitGatewayRouteTableAnnouncementResult

        input = Types::CreateTransitGatewayRouteTableAnnouncementRequest.new(peering_attachment_id: peering_attachment_id, transit_gateway_route_table_id: transit_gateway_route_table_id, dry_run: dry_run, tag_specifications: tag_specifications)
        create_transit_gateway_route_table_announcement(input)
      end

      def create_transit_gateway_route_table_announcement(input : Types::CreateTransitGatewayRouteTableAnnouncementRequest) : Types::CreateTransitGatewayRouteTableAnnouncementResult
        request = Protocol::Ec2.build_request(Model::CREATE_TRANSIT_GATEWAY_ROUTE_TABLE_ANNOUNCEMENT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateTransitGatewayRouteTableAnnouncementResult, "CreateTransitGatewayRouteTableAnnouncement")
      end

      # Attaches the specified VPC to the specified transit gateway. If you attach a VPC with a CIDR range
      # that overlaps the CIDR range of a VPC that is already attached, the new VPC CIDR range is not
      # propagated to the default propagation route table. To send VPC traffic to an attached transit
      # gateway, add a route to the VPC route table using CreateRoute .

      def create_transit_gateway_vpc_attachment(
        subnet_ids : Array(String),
        transit_gateway_id : String,
        vpc_id : String,
        dry_run : Bool? = nil,
        options : Types::CreateTransitGatewayVpcAttachmentRequestOptions? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateTransitGatewayVpcAttachmentResult

        input = Types::CreateTransitGatewayVpcAttachmentRequest.new(subnet_ids: subnet_ids, transit_gateway_id: transit_gateway_id, vpc_id: vpc_id, dry_run: dry_run, options: options, tag_specifications: tag_specifications)
        create_transit_gateway_vpc_attachment(input)
      end

      def create_transit_gateway_vpc_attachment(input : Types::CreateTransitGatewayVpcAttachmentRequest) : Types::CreateTransitGatewayVpcAttachmentResult
        request = Protocol::Ec2.build_request(Model::CREATE_TRANSIT_GATEWAY_VPC_ATTACHMENT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateTransitGatewayVpcAttachmentResult, "CreateTransitGatewayVpcAttachment")
      end

      # An Amazon Web Services Verified Access endpoint is where you define your application along with an
      # optional endpoint-level access policy.

      def create_verified_access_endpoint(
        attachment_type : String,
        endpoint_type : String,
        verified_access_group_id : String,
        application_domain : String? = nil,
        cidr_options : Types::CreateVerifiedAccessEndpointCidrOptions? = nil,
        client_token : String? = nil,
        description : String? = nil,
        domain_certificate_arn : String? = nil,
        dry_run : Bool? = nil,
        endpoint_domain_prefix : String? = nil,
        load_balancer_options : Types::CreateVerifiedAccessEndpointLoadBalancerOptions? = nil,
        network_interface_options : Types::CreateVerifiedAccessEndpointEniOptions? = nil,
        policy_document : String? = nil,
        rds_options : Types::CreateVerifiedAccessEndpointRdsOptions? = nil,
        security_group_ids : Array(String)? = nil,
        sse_specification : Types::VerifiedAccessSseSpecificationRequest? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateVerifiedAccessEndpointResult

        input = Types::CreateVerifiedAccessEndpointRequest.new(attachment_type: attachment_type, endpoint_type: endpoint_type, verified_access_group_id: verified_access_group_id, application_domain: application_domain, cidr_options: cidr_options, client_token: client_token, description: description, domain_certificate_arn: domain_certificate_arn, dry_run: dry_run, endpoint_domain_prefix: endpoint_domain_prefix, load_balancer_options: load_balancer_options, network_interface_options: network_interface_options, policy_document: policy_document, rds_options: rds_options, security_group_ids: security_group_ids, sse_specification: sse_specification, tag_specifications: tag_specifications)
        create_verified_access_endpoint(input)
      end

      def create_verified_access_endpoint(input : Types::CreateVerifiedAccessEndpointRequest) : Types::CreateVerifiedAccessEndpointResult
        request = Protocol::Ec2.build_request(Model::CREATE_VERIFIED_ACCESS_ENDPOINT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateVerifiedAccessEndpointResult, "CreateVerifiedAccessEndpoint")
      end

      # An Amazon Web Services Verified Access group is a collection of Amazon Web Services Verified Access
      # endpoints who's associated applications have similar security requirements. Each instance within a
      # Verified Access group shares an Verified Access policy. For example, you can group all Verified
      # Access instances associated with "sales" applications together and use one common Verified Access
      # policy.

      def create_verified_access_group(
        verified_access_instance_id : String,
        client_token : String? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        policy_document : String? = nil,
        sse_specification : Types::VerifiedAccessSseSpecificationRequest? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateVerifiedAccessGroupResult

        input = Types::CreateVerifiedAccessGroupRequest.new(verified_access_instance_id: verified_access_instance_id, client_token: client_token, description: description, dry_run: dry_run, policy_document: policy_document, sse_specification: sse_specification, tag_specifications: tag_specifications)
        create_verified_access_group(input)
      end

      def create_verified_access_group(input : Types::CreateVerifiedAccessGroupRequest) : Types::CreateVerifiedAccessGroupResult
        request = Protocol::Ec2.build_request(Model::CREATE_VERIFIED_ACCESS_GROUP, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateVerifiedAccessGroupResult, "CreateVerifiedAccessGroup")
      end

      # An Amazon Web Services Verified Access instance is a regional entity that evaluates application
      # requests and grants access only when your security requirements are met.

      def create_verified_access_instance(
        cidr_endpoints_custom_sub_domain : String? = nil,
        client_token : String? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        fips_enabled : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateVerifiedAccessInstanceResult

        input = Types::CreateVerifiedAccessInstanceRequest.new(cidr_endpoints_custom_sub_domain: cidr_endpoints_custom_sub_domain, client_token: client_token, description: description, dry_run: dry_run, fips_enabled: fips_enabled, tag_specifications: tag_specifications)
        create_verified_access_instance(input)
      end

      def create_verified_access_instance(input : Types::CreateVerifiedAccessInstanceRequest) : Types::CreateVerifiedAccessInstanceResult
        request = Protocol::Ec2.build_request(Model::CREATE_VERIFIED_ACCESS_INSTANCE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateVerifiedAccessInstanceResult, "CreateVerifiedAccessInstance")
      end

      # A trust provider is a third-party entity that creates, maintains, and manages identity information
      # for users and devices. When an application request is made, the identity information sent by the
      # trust provider is evaluated by Verified Access before allowing or denying the application request.

      def create_verified_access_trust_provider(
        policy_reference_name : String,
        trust_provider_type : String,
        client_token : String? = nil,
        description : String? = nil,
        device_options : Types::CreateVerifiedAccessTrustProviderDeviceOptions? = nil,
        device_trust_provider_type : String? = nil,
        dry_run : Bool? = nil,
        native_application_oidc_options : Types::CreateVerifiedAccessNativeApplicationOidcOptions? = nil,
        oidc_options : Types::CreateVerifiedAccessTrustProviderOidcOptions? = nil,
        sse_specification : Types::VerifiedAccessSseSpecificationRequest? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        user_trust_provider_type : String? = nil
      ) : Types::CreateVerifiedAccessTrustProviderResult

        input = Types::CreateVerifiedAccessTrustProviderRequest.new(policy_reference_name: policy_reference_name, trust_provider_type: trust_provider_type, client_token: client_token, description: description, device_options: device_options, device_trust_provider_type: device_trust_provider_type, dry_run: dry_run, native_application_oidc_options: native_application_oidc_options, oidc_options: oidc_options, sse_specification: sse_specification, tag_specifications: tag_specifications, user_trust_provider_type: user_trust_provider_type)
        create_verified_access_trust_provider(input)
      end

      def create_verified_access_trust_provider(input : Types::CreateVerifiedAccessTrustProviderRequest) : Types::CreateVerifiedAccessTrustProviderResult
        request = Protocol::Ec2.build_request(Model::CREATE_VERIFIED_ACCESS_TRUST_PROVIDER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateVerifiedAccessTrustProviderResult, "CreateVerifiedAccessTrustProvider")
      end

      # Creates an EBS volume that can be attached to an instance in the same Availability Zone. You can
      # create a new empty volume or restore a volume from an EBS snapshot. Any Amazon Web Services
      # Marketplace product codes from the snapshot are propagated to the volume. You can create encrypted
      # volumes. Encrypted volumes must be attached to instances that support Amazon EBS encryption. Volumes
      # that are created from encrypted snapshots are also automatically encrypted. For more information,
      # see Amazon EBS encryption in the Amazon EBS User Guide . You can tag your volumes during creation.
      # For more information, see Tag your Amazon EC2 resources in the Amazon EC2 User Guide . For more
      # information, see Create an Amazon EBS volume in the Amazon EBS User Guide .

      def create_volume(
        availability_zone : String? = nil,
        availability_zone_id : String? = nil,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        encrypted : Bool? = nil,
        iops : Int32? = nil,
        kms_key_id : String? = nil,
        multi_attach_enabled : Bool? = nil,
        operator : Types::OperatorRequest? = nil,
        outpost_arn : String? = nil,
        size : Int32? = nil,
        snapshot_id : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        throughput : Int32? = nil,
        volume_initialization_rate : Int32? = nil,
        volume_type : String? = nil
      ) : Types::Volume

        input = Types::CreateVolumeRequest.new(availability_zone: availability_zone, availability_zone_id: availability_zone_id, client_token: client_token, dry_run: dry_run, encrypted: encrypted, iops: iops, kms_key_id: kms_key_id, multi_attach_enabled: multi_attach_enabled, operator: operator, outpost_arn: outpost_arn, size: size, snapshot_id: snapshot_id, tag_specifications: tag_specifications, throughput: throughput, volume_initialization_rate: volume_initialization_rate, volume_type: volume_type)
        create_volume(input)
      end

      def create_volume(input : Types::CreateVolumeRequest) : Types::Volume
        request = Protocol::Ec2.build_request(Model::CREATE_VOLUME, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::Volume, "CreateVolume")
      end

      # Creates a VPC with the specified CIDR blocks. A VPC must have an associated IPv4 CIDR block. You can
      # choose an IPv4 CIDR block or an IPAM-allocated IPv4 CIDR block. You can optionally associate an IPv6
      # CIDR block with a VPC. You can choose an IPv6 CIDR block, an Amazon-provided IPv6 CIDR block, an
      # IPAM-allocated IPv6 CIDR block, or an IPv6 CIDR block that you brought to Amazon Web Services. For
      # more information, see IP addressing for your VPCs and subnets in the Amazon VPC User Guide . By
      # default, each instance that you launch in the VPC has the default DHCP options, which include only a
      # default DNS server that we provide (AmazonProvidedDNS). For more information, see DHCP option sets
      # in the Amazon VPC User Guide . You can specify DNS options and tenancy for a VPC when you create it.
      # You can't change the tenancy of a VPC after you create it. For more information, see VPC
      # configuration options in the Amazon VPC User Guide .

      def create_vpc(
        amazon_provided_ipv6_cidr_block : Bool? = nil,
        cidr_block : String? = nil,
        dry_run : Bool? = nil,
        instance_tenancy : String? = nil,
        ipv4_ipam_pool_id : String? = nil,
        ipv4_netmask_length : Int32? = nil,
        ipv6_cidr_block : String? = nil,
        ipv6_cidr_block_network_border_group : String? = nil,
        ipv6_ipam_pool_id : String? = nil,
        ipv6_netmask_length : Int32? = nil,
        ipv6_pool : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        vpc_encryption_control : Types::VpcEncryptionControlConfiguration? = nil
      ) : Types::CreateVpcResult

        input = Types::CreateVpcRequest.new(amazon_provided_ipv6_cidr_block: amazon_provided_ipv6_cidr_block, cidr_block: cidr_block, dry_run: dry_run, instance_tenancy: instance_tenancy, ipv4_ipam_pool_id: ipv4_ipam_pool_id, ipv4_netmask_length: ipv4_netmask_length, ipv6_cidr_block: ipv6_cidr_block, ipv6_cidr_block_network_border_group: ipv6_cidr_block_network_border_group, ipv6_ipam_pool_id: ipv6_ipam_pool_id, ipv6_netmask_length: ipv6_netmask_length, ipv6_pool: ipv6_pool, tag_specifications: tag_specifications, vpc_encryption_control: vpc_encryption_control)
        create_vpc(input)
      end

      def create_vpc(input : Types::CreateVpcRequest) : Types::CreateVpcResult
        request = Protocol::Ec2.build_request(Model::CREATE_VPC, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateVpcResult, "CreateVpc")
      end

      # Create a VPC Block Public Access (BPA) exclusion. A VPC BPA exclusion is a mode that can be applied
      # to a single VPC or subnet that exempts it from the account’s BPA mode and will allow bidirectional
      # or egress-only access. You can create BPA exclusions for VPCs and subnets even when BPA is not
      # enabled on the account to ensure that there is no traffic disruption to the exclusions when VPC BPA
      # is turned on. To learn more about VPC BPA, see Block public access to VPCs and subnets in the Amazon
      # VPC User Guide .

      def create_vpc_block_public_access_exclusion(
        internet_gateway_exclusion_mode : String,
        dry_run : Bool? = nil,
        subnet_id : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        vpc_id : String? = nil
      ) : Types::CreateVpcBlockPublicAccessExclusionResult

        input = Types::CreateVpcBlockPublicAccessExclusionRequest.new(internet_gateway_exclusion_mode: internet_gateway_exclusion_mode, dry_run: dry_run, subnet_id: subnet_id, tag_specifications: tag_specifications, vpc_id: vpc_id)
        create_vpc_block_public_access_exclusion(input)
      end

      def create_vpc_block_public_access_exclusion(input : Types::CreateVpcBlockPublicAccessExclusionRequest) : Types::CreateVpcBlockPublicAccessExclusionResult
        request = Protocol::Ec2.build_request(Model::CREATE_VPC_BLOCK_PUBLIC_ACCESS_EXCLUSION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateVpcBlockPublicAccessExclusionResult, "CreateVpcBlockPublicAccessExclusion")
      end

      # Creates a VPC Encryption Control configuration for a specified VPC. VPC Encryption Control enables
      # you to enforce encryption for all data in transit within and between VPCs to meet compliance
      # requirements for standards like HIPAA, FedRAMP, and PCI DSS. For more information, see Enforce VPC
      # encryption in transit in the Amazon VPC User Guide .

      def create_vpc_encryption_control(
        vpc_id : String,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateVpcEncryptionControlResult

        input = Types::CreateVpcEncryptionControlRequest.new(vpc_id: vpc_id, dry_run: dry_run, tag_specifications: tag_specifications)
        create_vpc_encryption_control(input)
      end

      def create_vpc_encryption_control(input : Types::CreateVpcEncryptionControlRequest) : Types::CreateVpcEncryptionControlResult
        request = Protocol::Ec2.build_request(Model::CREATE_VPC_ENCRYPTION_CONTROL, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateVpcEncryptionControlResult, "CreateVpcEncryptionControl")
      end

      # Creates a VPC endpoint. A VPC endpoint provides a private connection between the specified VPC and
      # the specified endpoint service. You can use an endpoint service provided by Amazon Web Services, an
      # Amazon Web Services Marketplace Partner, or another Amazon Web Services account. For more
      # information, see the Amazon Web Services PrivateLink User Guide .

      def create_vpc_endpoint(
        vpc_id : String,
        client_token : String? = nil,
        dns_options : Types::DnsOptionsSpecification? = nil,
        dry_run : Bool? = nil,
        ip_address_type : String? = nil,
        policy_document : String? = nil,
        private_dns_enabled : Bool? = nil,
        resource_configuration_arn : String? = nil,
        route_table_ids : Array(String)? = nil,
        security_group_ids : Array(String)? = nil,
        service_name : String? = nil,
        service_network_arn : String? = nil,
        service_region : String? = nil,
        subnet_configurations : Array(Types::SubnetConfiguration)? = nil,
        subnet_ids : Array(String)? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        vpc_endpoint_type : String? = nil
      ) : Types::CreateVpcEndpointResult

        input = Types::CreateVpcEndpointRequest.new(vpc_id: vpc_id, client_token: client_token, dns_options: dns_options, dry_run: dry_run, ip_address_type: ip_address_type, policy_document: policy_document, private_dns_enabled: private_dns_enabled, resource_configuration_arn: resource_configuration_arn, route_table_ids: route_table_ids, security_group_ids: security_group_ids, service_name: service_name, service_network_arn: service_network_arn, service_region: service_region, subnet_configurations: subnet_configurations, subnet_ids: subnet_ids, tag_specifications: tag_specifications, vpc_endpoint_type: vpc_endpoint_type)
        create_vpc_endpoint(input)
      end

      def create_vpc_endpoint(input : Types::CreateVpcEndpointRequest) : Types::CreateVpcEndpointResult
        request = Protocol::Ec2.build_request(Model::CREATE_VPC_ENDPOINT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateVpcEndpointResult, "CreateVpcEndpoint")
      end

      # Creates a connection notification for a specified VPC endpoint or VPC endpoint service. A connection
      # notification notifies you of specific endpoint events. You must create an SNS topic to receive
      # notifications. For more information, see Creating an Amazon SNS topic in the Amazon SNS Developer
      # Guide . You can create a connection notification for interface endpoints only.

      def create_vpc_endpoint_connection_notification(
        connection_events : Array(String),
        connection_notification_arn : String,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        service_id : String? = nil,
        vpc_endpoint_id : String? = nil
      ) : Types::CreateVpcEndpointConnectionNotificationResult

        input = Types::CreateVpcEndpointConnectionNotificationRequest.new(connection_events: connection_events, connection_notification_arn: connection_notification_arn, client_token: client_token, dry_run: dry_run, service_id: service_id, vpc_endpoint_id: vpc_endpoint_id)
        create_vpc_endpoint_connection_notification(input)
      end

      def create_vpc_endpoint_connection_notification(input : Types::CreateVpcEndpointConnectionNotificationRequest) : Types::CreateVpcEndpointConnectionNotificationResult
        request = Protocol::Ec2.build_request(Model::CREATE_VPC_ENDPOINT_CONNECTION_NOTIFICATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateVpcEndpointConnectionNotificationResult, "CreateVpcEndpointConnectionNotification")
      end

      # Creates a VPC endpoint service to which service consumers (Amazon Web Services accounts, users, and
      # IAM roles) can connect. Before you create an endpoint service, you must create one of the following
      # for your service: A Network Load Balancer . Service consumers connect to your service using an
      # interface endpoint. A Gateway Load Balancer . Service consumers connect to your service using a
      # Gateway Load Balancer endpoint. If you set the private DNS name, you must prove that you own the
      # private DNS domain name. For more information, see the Amazon Web Services PrivateLink Guide .

      def create_vpc_endpoint_service_configuration(
        acceptance_required : Bool? = nil,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        gateway_load_balancer_arns : Array(String)? = nil,
        network_load_balancer_arns : Array(String)? = nil,
        private_dns_name : String? = nil,
        supported_ip_address_types : Array(String)? = nil,
        supported_regions : Array(String)? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateVpcEndpointServiceConfigurationResult

        input = Types::CreateVpcEndpointServiceConfigurationRequest.new(acceptance_required: acceptance_required, client_token: client_token, dry_run: dry_run, gateway_load_balancer_arns: gateway_load_balancer_arns, network_load_balancer_arns: network_load_balancer_arns, private_dns_name: private_dns_name, supported_ip_address_types: supported_ip_address_types, supported_regions: supported_regions, tag_specifications: tag_specifications)
        create_vpc_endpoint_service_configuration(input)
      end

      def create_vpc_endpoint_service_configuration(input : Types::CreateVpcEndpointServiceConfigurationRequest) : Types::CreateVpcEndpointServiceConfigurationResult
        request = Protocol::Ec2.build_request(Model::CREATE_VPC_ENDPOINT_SERVICE_CONFIGURATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateVpcEndpointServiceConfigurationResult, "CreateVpcEndpointServiceConfiguration")
      end

      # Requests a VPC peering connection between two VPCs: a requester VPC that you own and an accepter VPC
      # with which to create the connection. The accepter VPC can belong to another Amazon Web Services
      # account and can be in a different Region to the requester VPC. The requester VPC and accepter VPC
      # cannot have overlapping CIDR blocks. Limitations and rules apply to a VPC peering connection. For
      # more information, see the VPC peering limitations in the VPC Peering Guide . The owner of the
      # accepter VPC must accept the peering request to activate the peering connection. The VPC peering
      # connection request expires after 7 days, after which it cannot be accepted or rejected. If you
      # create a VPC peering connection request between VPCs with overlapping CIDR blocks, the VPC peering
      # connection has a status of failed .

      def create_vpc_peering_connection(
        vpc_id : String,
        dry_run : Bool? = nil,
        peer_owner_id : String? = nil,
        peer_region : String? = nil,
        peer_vpc_id : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateVpcPeeringConnectionResult

        input = Types::CreateVpcPeeringConnectionRequest.new(vpc_id: vpc_id, dry_run: dry_run, peer_owner_id: peer_owner_id, peer_region: peer_region, peer_vpc_id: peer_vpc_id, tag_specifications: tag_specifications)
        create_vpc_peering_connection(input)
      end

      def create_vpc_peering_connection(input : Types::CreateVpcPeeringConnectionRequest) : Types::CreateVpcPeeringConnectionResult
        request = Protocol::Ec2.build_request(Model::CREATE_VPC_PEERING_CONNECTION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateVpcPeeringConnectionResult, "CreateVpcPeeringConnection")
      end

      # Creates a VPN concentrator that aggregates multiple VPN connections to a transit gateway.

      def create_vpn_concentrator(
        type : String,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        transit_gateway_id : String? = nil
      ) : Types::CreateVpnConcentratorResult

        input = Types::CreateVpnConcentratorRequest.new(type: type, dry_run: dry_run, tag_specifications: tag_specifications, transit_gateway_id: transit_gateway_id)
        create_vpn_concentrator(input)
      end

      def create_vpn_concentrator(input : Types::CreateVpnConcentratorRequest) : Types::CreateVpnConcentratorResult
        request = Protocol::Ec2.build_request(Model::CREATE_VPN_CONCENTRATOR, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateVpnConcentratorResult, "CreateVpnConcentrator")
      end

      # Creates a VPN connection between an existing virtual private gateway or transit gateway and a
      # customer gateway. The supported connection type is ipsec.1 . The response includes information that
      # you need to give to your network administrator to configure your customer gateway. We strongly
      # recommend that you use HTTPS when calling this operation because the response contains sensitive
      # cryptographic information for configuring your customer gateway device. If you decide to shut down
      # your VPN connection for any reason and later create a new VPN connection, you must reconfigure your
      # customer gateway with the new information returned from this call. This is an idempotent operation.
      # If you perform the operation more than once, Amazon EC2 doesn't return an error. For more
      # information, see Amazon Web Services Site-to-Site VPN in the Amazon Web Services Site-to-Site VPN
      # User Guide .

      def create_vpn_connection(
        customer_gateway_id : String,
        type : String,
        dry_run : Bool? = nil,
        options : Types::VpnConnectionOptionsSpecification? = nil,
        pre_shared_key_storage : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        transit_gateway_id : String? = nil,
        vpn_concentrator_id : String? = nil,
        vpn_gateway_id : String? = nil
      ) : Types::CreateVpnConnectionResult

        input = Types::CreateVpnConnectionRequest.new(customer_gateway_id: customer_gateway_id, type: type, dry_run: dry_run, options: options, pre_shared_key_storage: pre_shared_key_storage, tag_specifications: tag_specifications, transit_gateway_id: transit_gateway_id, vpn_concentrator_id: vpn_concentrator_id, vpn_gateway_id: vpn_gateway_id)
        create_vpn_connection(input)
      end

      def create_vpn_connection(input : Types::CreateVpnConnectionRequest) : Types::CreateVpnConnectionResult
        request = Protocol::Ec2.build_request(Model::CREATE_VPN_CONNECTION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateVpnConnectionResult, "CreateVpnConnection")
      end

      # Creates a static route associated with a VPN connection between an existing virtual private gateway
      # and a VPN customer gateway. The static route allows traffic to be routed from the virtual private
      # gateway to the VPN customer gateway. For more information, see Amazon Web Services Site-to-Site VPN
      # in the Amazon Web Services Site-to-Site VPN User Guide .

      def create_vpn_connection_route(
        destination_cidr_block : String,
        vpn_connection_id : String
      ) : Nil

        input = Types::CreateVpnConnectionRouteRequest.new(destination_cidr_block: destination_cidr_block, vpn_connection_id: vpn_connection_id)
        create_vpn_connection_route(input)
      end

      def create_vpn_connection_route(input : Types::CreateVpnConnectionRouteRequest) : Nil
        request = Protocol::Ec2.build_request(Model::CREATE_VPN_CONNECTION_ROUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Creates a virtual private gateway. A virtual private gateway is the endpoint on the VPC side of your
      # VPN connection. You can create a virtual private gateway before creating the VPC itself. For more
      # information, see Amazon Web Services Site-to-Site VPN in the Amazon Web Services Site-to-Site VPN
      # User Guide .

      def create_vpn_gateway(
        type : String,
        amazon_side_asn : Int64? = nil,
        availability_zone : String? = nil,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::CreateVpnGatewayResult

        input = Types::CreateVpnGatewayRequest.new(type: type, amazon_side_asn: amazon_side_asn, availability_zone: availability_zone, dry_run: dry_run, tag_specifications: tag_specifications)
        create_vpn_gateway(input)
      end

      def create_vpn_gateway(input : Types::CreateVpnGatewayRequest) : Types::CreateVpnGatewayResult
        request = Protocol::Ec2.build_request(Model::CREATE_VPN_GATEWAY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::CreateVpnGatewayResult, "CreateVpnGateway")
      end

      # Deletes an existing Capacity Manager data export configuration. This stops future scheduled exports
      # but does not delete previously exported files from S3.

      def delete_capacity_manager_data_export(
        capacity_manager_data_export_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteCapacityManagerDataExportResult

        input = Types::DeleteCapacityManagerDataExportRequest.new(capacity_manager_data_export_id: capacity_manager_data_export_id, dry_run: dry_run)
        delete_capacity_manager_data_export(input)
      end

      def delete_capacity_manager_data_export(input : Types::DeleteCapacityManagerDataExportRequest) : Types::DeleteCapacityManagerDataExportResult
        request = Protocol::Ec2.build_request(Model::DELETE_CAPACITY_MANAGER_DATA_EXPORT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteCapacityManagerDataExportResult, "DeleteCapacityManagerDataExport")
      end

      # Deletes a carrier gateway. If you do not delete the route that contains the carrier gateway as the
      # Target, the route is a blackhole route. For information about how to delete a route, see DeleteRoute
      # .

      def delete_carrier_gateway(
        carrier_gateway_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteCarrierGatewayResult

        input = Types::DeleteCarrierGatewayRequest.new(carrier_gateway_id: carrier_gateway_id, dry_run: dry_run)
        delete_carrier_gateway(input)
      end

      def delete_carrier_gateway(input : Types::DeleteCarrierGatewayRequest) : Types::DeleteCarrierGatewayResult
        request = Protocol::Ec2.build_request(Model::DELETE_CARRIER_GATEWAY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteCarrierGatewayResult, "DeleteCarrierGateway")
      end

      # Deletes the specified Client VPN endpoint. You must disassociate all target networks before you can
      # delete a Client VPN endpoint.

      def delete_client_vpn_endpoint(
        client_vpn_endpoint_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteClientVpnEndpointResult

        input = Types::DeleteClientVpnEndpointRequest.new(client_vpn_endpoint_id: client_vpn_endpoint_id, dry_run: dry_run)
        delete_client_vpn_endpoint(input)
      end

      def delete_client_vpn_endpoint(input : Types::DeleteClientVpnEndpointRequest) : Types::DeleteClientVpnEndpointResult
        request = Protocol::Ec2.build_request(Model::DELETE_CLIENT_VPN_ENDPOINT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteClientVpnEndpointResult, "DeleteClientVpnEndpoint")
      end

      # Deletes a route from a Client VPN endpoint. You can only delete routes that you manually added using
      # the CreateClientVpnRoute action. You cannot delete routes that were automatically added when
      # associating a subnet. To remove routes that have been automatically added, disassociate the target
      # subnet from the Client VPN endpoint.

      def delete_client_vpn_route(
        client_vpn_endpoint_id : String,
        destination_cidr_block : String,
        dry_run : Bool? = nil,
        target_vpc_subnet_id : String? = nil
      ) : Types::DeleteClientVpnRouteResult

        input = Types::DeleteClientVpnRouteRequest.new(client_vpn_endpoint_id: client_vpn_endpoint_id, destination_cidr_block: destination_cidr_block, dry_run: dry_run, target_vpc_subnet_id: target_vpc_subnet_id)
        delete_client_vpn_route(input)
      end

      def delete_client_vpn_route(input : Types::DeleteClientVpnRouteRequest) : Types::DeleteClientVpnRouteResult
        request = Protocol::Ec2.build_request(Model::DELETE_CLIENT_VPN_ROUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteClientVpnRouteResult, "DeleteClientVpnRoute")
      end

      # Deletes a range of customer-owned IP addresses.

      def delete_coip_cidr(
        cidr : String,
        coip_pool_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteCoipCidrResult

        input = Types::DeleteCoipCidrRequest.new(cidr: cidr, coip_pool_id: coip_pool_id, dry_run: dry_run)
        delete_coip_cidr(input)
      end

      def delete_coip_cidr(input : Types::DeleteCoipCidrRequest) : Types::DeleteCoipCidrResult
        request = Protocol::Ec2.build_request(Model::DELETE_COIP_CIDR, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteCoipCidrResult, "DeleteCoipCidr")
      end

      # Deletes a pool of customer-owned IP (CoIP) addresses.

      def delete_coip_pool(
        coip_pool_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteCoipPoolResult

        input = Types::DeleteCoipPoolRequest.new(coip_pool_id: coip_pool_id, dry_run: dry_run)
        delete_coip_pool(input)
      end

      def delete_coip_pool(input : Types::DeleteCoipPoolRequest) : Types::DeleteCoipPoolResult
        request = Protocol::Ec2.build_request(Model::DELETE_COIP_POOL, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteCoipPoolResult, "DeleteCoipPool")
      end

      # Deletes the specified customer gateway. You must delete the VPN connection before you can delete the
      # customer gateway.

      def delete_customer_gateway(
        customer_gateway_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::DeleteCustomerGatewayRequest.new(customer_gateway_id: customer_gateway_id, dry_run: dry_run)
        delete_customer_gateway(input)
      end

      def delete_customer_gateway(input : Types::DeleteCustomerGatewayRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DELETE_CUSTOMER_GATEWAY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Deletes the specified set of DHCP options. You must disassociate the set of DHCP options before you
      # can delete it. You can disassociate the set of DHCP options by associating either a new set of
      # options or the default set of options with the VPC.

      def delete_dhcp_options(
        dhcp_options_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::DeleteDhcpOptionsRequest.new(dhcp_options_id: dhcp_options_id, dry_run: dry_run)
        delete_dhcp_options(input)
      end

      def delete_dhcp_options(input : Types::DeleteDhcpOptionsRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DELETE_DHCP_OPTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Deletes an egress-only internet gateway.

      def delete_egress_only_internet_gateway(
        egress_only_internet_gateway_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteEgressOnlyInternetGatewayResult

        input = Types::DeleteEgressOnlyInternetGatewayRequest.new(egress_only_internet_gateway_id: egress_only_internet_gateway_id, dry_run: dry_run)
        delete_egress_only_internet_gateway(input)
      end

      def delete_egress_only_internet_gateway(input : Types::DeleteEgressOnlyInternetGatewayRequest) : Types::DeleteEgressOnlyInternetGatewayResult
        request = Protocol::Ec2.build_request(Model::DELETE_EGRESS_ONLY_INTERNET_GATEWAY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteEgressOnlyInternetGatewayResult, "DeleteEgressOnlyInternetGateway")
      end

      # Deletes the specified EC2 Fleet request. After you delete an EC2 Fleet request, it launches no new
      # instances. You must also specify whether a deleted EC2 Fleet request should terminate its instances.
      # If you choose to terminate the instances, the EC2 Fleet request enters the deleted_terminating
      # state. Otherwise, it enters the deleted_running state, and the instances continue to run until they
      # are interrupted or you terminate them manually. A deleted instant fleet with running instances is
      # not supported. When you delete an instant fleet, Amazon EC2 automatically terminates all its
      # instances. For fleets with more than 1000 instances, the deletion request might fail. If your fleet
      # has more than 1000 instances, first terminate most of the instances manually, leaving 1000 or fewer.
      # Then delete the fleet, and the remaining instances will be terminated automatically. Terminating an
      # instance is permanent and irreversible. After you terminate an instance, you can no longer connect
      # to it, and it can't be recovered. All attached Amazon EBS volumes that are configured to be deleted
      # on termination are also permanently deleted and can't be recovered. All data stored on instance
      # store volumes is permanently lost. For more information, see How instance termination works . Before
      # you terminate an instance, ensure that you have backed up all data that you need to retain after the
      # termination to persistent storage. Restrictions You can delete up to 25 fleets of type instant in a
      # single request. You can delete up to 100 fleets of type maintain or request in a single request. You
      # can delete up to 125 fleets in a single request, provided you do not exceed the quota for each fleet
      # type, as specified above. If you exceed the specified number of fleets to delete, no fleets are
      # deleted. For more information, see Delete an EC2 Fleet request and the instances in the fleet in the
      # Amazon EC2 User Guide .

      def delete_fleets(
        fleet_ids : Array(String),
        terminate_instances : Bool,
        dry_run : Bool? = nil
      ) : Types::DeleteFleetsResult

        input = Types::DeleteFleetsRequest.new(fleet_ids: fleet_ids, terminate_instances: terminate_instances, dry_run: dry_run)
        delete_fleets(input)
      end

      def delete_fleets(input : Types::DeleteFleetsRequest) : Types::DeleteFleetsResult
        request = Protocol::Ec2.build_request(Model::DELETE_FLEETS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteFleetsResult, "DeleteFleets")
      end

      # Deletes one or more flow logs.

      def delete_flow_logs(
        flow_log_ids : Array(String),
        dry_run : Bool? = nil
      ) : Types::DeleteFlowLogsResult

        input = Types::DeleteFlowLogsRequest.new(flow_log_ids: flow_log_ids, dry_run: dry_run)
        delete_flow_logs(input)
      end

      def delete_flow_logs(input : Types::DeleteFlowLogsRequest) : Types::DeleteFlowLogsResult
        request = Protocol::Ec2.build_request(Model::DELETE_FLOW_LOGS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteFlowLogsResult, "DeleteFlowLogs")
      end

      # Deletes the specified Amazon FPGA Image (AFI).

      def delete_fpga_image(
        fpga_image_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteFpgaImageResult

        input = Types::DeleteFpgaImageRequest.new(fpga_image_id: fpga_image_id, dry_run: dry_run)
        delete_fpga_image(input)
      end

      def delete_fpga_image(input : Types::DeleteFpgaImageRequest) : Types::DeleteFpgaImageResult
        request = Protocol::Ec2.build_request(Model::DELETE_FPGA_IMAGE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteFpgaImageResult, "DeleteFpgaImage")
      end

      # Deletes the specified image usage report. For more information, see View your AMI usage in the
      # Amazon EC2 User Guide .

      def delete_image_usage_report(
        report_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteImageUsageReportResult

        input = Types::DeleteImageUsageReportRequest.new(report_id: report_id, dry_run: dry_run)
        delete_image_usage_report(input)
      end

      def delete_image_usage_report(input : Types::DeleteImageUsageReportRequest) : Types::DeleteImageUsageReportResult
        request = Protocol::Ec2.build_request(Model::DELETE_IMAGE_USAGE_REPORT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteImageUsageReportResult, "DeleteImageUsageReport")
      end

      # Deletes the specified EC2 Instance Connect Endpoint.

      def delete_instance_connect_endpoint(
        instance_connect_endpoint_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteInstanceConnectEndpointResult

        input = Types::DeleteInstanceConnectEndpointRequest.new(instance_connect_endpoint_id: instance_connect_endpoint_id, dry_run: dry_run)
        delete_instance_connect_endpoint(input)
      end

      def delete_instance_connect_endpoint(input : Types::DeleteInstanceConnectEndpointRequest) : Types::DeleteInstanceConnectEndpointResult
        request = Protocol::Ec2.build_request(Model::DELETE_INSTANCE_CONNECT_ENDPOINT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteInstanceConnectEndpointResult, "DeleteInstanceConnectEndpoint")
      end

      # Deletes the specified event window. For more information, see Define event windows for scheduled
      # events in the Amazon EC2 User Guide .

      def delete_instance_event_window(
        instance_event_window_id : String,
        dry_run : Bool? = nil,
        force_delete : Bool? = nil
      ) : Types::DeleteInstanceEventWindowResult

        input = Types::DeleteInstanceEventWindowRequest.new(instance_event_window_id: instance_event_window_id, dry_run: dry_run, force_delete: force_delete)
        delete_instance_event_window(input)
      end

      def delete_instance_event_window(input : Types::DeleteInstanceEventWindowRequest) : Types::DeleteInstanceEventWindowResult
        request = Protocol::Ec2.build_request(Model::DELETE_INSTANCE_EVENT_WINDOW, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteInstanceEventWindowResult, "DeleteInstanceEventWindow")
      end

      # Deletes the specified internet gateway. You must detach the internet gateway from the VPC before you
      # can delete it.

      def delete_internet_gateway(
        internet_gateway_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::DeleteInternetGatewayRequest.new(internet_gateway_id: internet_gateway_id, dry_run: dry_run)
        delete_internet_gateway(input)
      end

      def delete_internet_gateway(input : Types::DeleteInternetGatewayRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DELETE_INTERNET_GATEWAY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Delete an IPAM. Deleting an IPAM removes all monitored data associated with the IPAM including the
      # historical data for CIDRs. For more information, see Delete an IPAM in the Amazon VPC IPAM User
      # Guide .

      def delete_ipam(
        ipam_id : String,
        cascade : Bool? = nil,
        dry_run : Bool? = nil
      ) : Types::DeleteIpamResult

        input = Types::DeleteIpamRequest.new(ipam_id: ipam_id, cascade: cascade, dry_run: dry_run)
        delete_ipam(input)
      end

      def delete_ipam(input : Types::DeleteIpamRequest) : Types::DeleteIpamResult
        request = Protocol::Ec2.build_request(Model::DELETE_IPAM, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteIpamResult, "DeleteIpam")
      end

      # Delete a verification token. A verification token is an Amazon Web Services-generated random value
      # that you can use to prove ownership of an external resource. For example, you can use a verification
      # token to validate that you control a public IP address range when you bring an IP address range to
      # Amazon Web Services (BYOIP).

      def delete_ipam_external_resource_verification_token(
        ipam_external_resource_verification_token_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteIpamExternalResourceVerificationTokenResult

        input = Types::DeleteIpamExternalResourceVerificationTokenRequest.new(ipam_external_resource_verification_token_id: ipam_external_resource_verification_token_id, dry_run: dry_run)
        delete_ipam_external_resource_verification_token(input)
      end

      def delete_ipam_external_resource_verification_token(input : Types::DeleteIpamExternalResourceVerificationTokenRequest) : Types::DeleteIpamExternalResourceVerificationTokenResult
        request = Protocol::Ec2.build_request(Model::DELETE_IPAM_EXTERNAL_RESOURCE_VERIFICATION_TOKEN, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteIpamExternalResourceVerificationTokenResult, "DeleteIpamExternalResourceVerificationToken")
      end

      # Deletes an IPAM policy. An IPAM policy is a set of rules that define how public IPv4 addresses from
      # IPAM pools are allocated to Amazon Web Services resources. Each rule maps an Amazon Web Services
      # service to IPAM pools that the service will use to get IP addresses. A single policy can have
      # multiple rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run out of
      # addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied to an
      # individual Amazon Web Services account or an entity within Amazon Web Services Organizations.

      def delete_ipam_policy(
        ipam_policy_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteIpamPolicyResult

        input = Types::DeleteIpamPolicyRequest.new(ipam_policy_id: ipam_policy_id, dry_run: dry_run)
        delete_ipam_policy(input)
      end

      def delete_ipam_policy(input : Types::DeleteIpamPolicyRequest) : Types::DeleteIpamPolicyResult
        request = Protocol::Ec2.build_request(Model::DELETE_IPAM_POLICY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteIpamPolicyResult, "DeleteIpamPolicy")
      end

      # Delete an IPAM pool. You cannot delete an IPAM pool if there are allocations in it or CIDRs
      # provisioned to it. To release allocations, see ReleaseIpamPoolAllocation . To deprovision pool
      # CIDRs, see DeprovisionIpamPoolCidr . For more information, see Delete a pool in the Amazon VPC IPAM
      # User Guide .

      def delete_ipam_pool(
        ipam_pool_id : String,
        cascade : Bool? = nil,
        dry_run : Bool? = nil
      ) : Types::DeleteIpamPoolResult

        input = Types::DeleteIpamPoolRequest.new(ipam_pool_id: ipam_pool_id, cascade: cascade, dry_run: dry_run)
        delete_ipam_pool(input)
      end

      def delete_ipam_pool(input : Types::DeleteIpamPoolRequest) : Types::DeleteIpamPoolResult
        request = Protocol::Ec2.build_request(Model::DELETE_IPAM_POOL, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteIpamPoolResult, "DeleteIpamPool")
      end

      # Deletes an IPAM prefix list resolver. Before deleting a resolver, you must first delete all resolver
      # targets associated with it.

      def delete_ipam_prefix_list_resolver(
        ipam_prefix_list_resolver_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteIpamPrefixListResolverResult

        input = Types::DeleteIpamPrefixListResolverRequest.new(ipam_prefix_list_resolver_id: ipam_prefix_list_resolver_id, dry_run: dry_run)
        delete_ipam_prefix_list_resolver(input)
      end

      def delete_ipam_prefix_list_resolver(input : Types::DeleteIpamPrefixListResolverRequest) : Types::DeleteIpamPrefixListResolverResult
        request = Protocol::Ec2.build_request(Model::DELETE_IPAM_PREFIX_LIST_RESOLVER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteIpamPrefixListResolverResult, "DeleteIpamPrefixListResolver")
      end

      # Deletes an IPAM prefix list resolver target. This removes the association between the resolver and
      # the managed prefix list, stopping automatic CIDR synchronization. For more information about IPAM
      # prefix list resolver, see Automate prefix list updates with IPAM in the Amazon VPC IPAM User Guide .

      def delete_ipam_prefix_list_resolver_target(
        ipam_prefix_list_resolver_target_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteIpamPrefixListResolverTargetResult

        input = Types::DeleteIpamPrefixListResolverTargetRequest.new(ipam_prefix_list_resolver_target_id: ipam_prefix_list_resolver_target_id, dry_run: dry_run)
        delete_ipam_prefix_list_resolver_target(input)
      end

      def delete_ipam_prefix_list_resolver_target(input : Types::DeleteIpamPrefixListResolverTargetRequest) : Types::DeleteIpamPrefixListResolverTargetResult
        request = Protocol::Ec2.build_request(Model::DELETE_IPAM_PREFIX_LIST_RESOLVER_TARGET, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteIpamPrefixListResolverTargetResult, "DeleteIpamPrefixListResolverTarget")
      end

      # Deletes an IPAM resource discovery. A resource discovery is an IPAM component that enables IPAM to
      # manage and monitor resources that belong to the owning account.

      def delete_ipam_resource_discovery(
        ipam_resource_discovery_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteIpamResourceDiscoveryResult

        input = Types::DeleteIpamResourceDiscoveryRequest.new(ipam_resource_discovery_id: ipam_resource_discovery_id, dry_run: dry_run)
        delete_ipam_resource_discovery(input)
      end

      def delete_ipam_resource_discovery(input : Types::DeleteIpamResourceDiscoveryRequest) : Types::DeleteIpamResourceDiscoveryResult
        request = Protocol::Ec2.build_request(Model::DELETE_IPAM_RESOURCE_DISCOVERY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteIpamResourceDiscoveryResult, "DeleteIpamResourceDiscovery")
      end

      # Delete the scope for an IPAM. You cannot delete the default scopes. For more information, see Delete
      # a scope in the Amazon VPC IPAM User Guide .

      def delete_ipam_scope(
        ipam_scope_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteIpamScopeResult

        input = Types::DeleteIpamScopeRequest.new(ipam_scope_id: ipam_scope_id, dry_run: dry_run)
        delete_ipam_scope(input)
      end

      def delete_ipam_scope(input : Types::DeleteIpamScopeRequest) : Types::DeleteIpamScopeResult
        request = Protocol::Ec2.build_request(Model::DELETE_IPAM_SCOPE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteIpamScopeResult, "DeleteIpamScope")
      end

      # Deletes the specified key pair, by removing the public key from Amazon EC2.

      def delete_key_pair(
        dry_run : Bool? = nil,
        key_name : String? = nil,
        key_pair_id : String? = nil
      ) : Types::DeleteKeyPairResult

        input = Types::DeleteKeyPairRequest.new(dry_run: dry_run, key_name: key_name, key_pair_id: key_pair_id)
        delete_key_pair(input)
      end

      def delete_key_pair(input : Types::DeleteKeyPairRequest) : Types::DeleteKeyPairResult
        request = Protocol::Ec2.build_request(Model::DELETE_KEY_PAIR, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteKeyPairResult, "DeleteKeyPair")
      end

      # Deletes a launch template. Deleting a launch template deletes all of its versions.

      def delete_launch_template(
        dry_run : Bool? = nil,
        launch_template_id : String? = nil,
        launch_template_name : String? = nil
      ) : Types::DeleteLaunchTemplateResult

        input = Types::DeleteLaunchTemplateRequest.new(dry_run: dry_run, launch_template_id: launch_template_id, launch_template_name: launch_template_name)
        delete_launch_template(input)
      end

      def delete_launch_template(input : Types::DeleteLaunchTemplateRequest) : Types::DeleteLaunchTemplateResult
        request = Protocol::Ec2.build_request(Model::DELETE_LAUNCH_TEMPLATE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteLaunchTemplateResult, "DeleteLaunchTemplate")
      end

      # Deletes one or more versions of a launch template. You can't delete the default version of a launch
      # template; you must first assign a different version as the default. If the default version is the
      # only version for the launch template, you must delete the entire launch template using
      # DeleteLaunchTemplate . You can delete up to 200 launch template versions in a single request. To
      # delete more than 200 versions in a single request, use DeleteLaunchTemplate , which deletes the
      # launch template and all of its versions. For more information, see Delete a launch template version
      # in the Amazon EC2 User Guide .

      def delete_launch_template_versions(
        versions : Array(String),
        dry_run : Bool? = nil,
        launch_template_id : String? = nil,
        launch_template_name : String? = nil
      ) : Types::DeleteLaunchTemplateVersionsResult

        input = Types::DeleteLaunchTemplateVersionsRequest.new(versions: versions, dry_run: dry_run, launch_template_id: launch_template_id, launch_template_name: launch_template_name)
        delete_launch_template_versions(input)
      end

      def delete_launch_template_versions(input : Types::DeleteLaunchTemplateVersionsRequest) : Types::DeleteLaunchTemplateVersionsResult
        request = Protocol::Ec2.build_request(Model::DELETE_LAUNCH_TEMPLATE_VERSIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteLaunchTemplateVersionsResult, "DeleteLaunchTemplateVersions")
      end

      # Deletes the specified route from the specified local gateway route table.

      def delete_local_gateway_route(
        local_gateway_route_table_id : String,
        destination_cidr_block : String? = nil,
        destination_prefix_list_id : String? = nil,
        dry_run : Bool? = nil
      ) : Types::DeleteLocalGatewayRouteResult

        input = Types::DeleteLocalGatewayRouteRequest.new(local_gateway_route_table_id: local_gateway_route_table_id, destination_cidr_block: destination_cidr_block, destination_prefix_list_id: destination_prefix_list_id, dry_run: dry_run)
        delete_local_gateway_route(input)
      end

      def delete_local_gateway_route(input : Types::DeleteLocalGatewayRouteRequest) : Types::DeleteLocalGatewayRouteResult
        request = Protocol::Ec2.build_request(Model::DELETE_LOCAL_GATEWAY_ROUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteLocalGatewayRouteResult, "DeleteLocalGatewayRoute")
      end

      # Deletes a local gateway route table.

      def delete_local_gateway_route_table(
        local_gateway_route_table_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteLocalGatewayRouteTableResult

        input = Types::DeleteLocalGatewayRouteTableRequest.new(local_gateway_route_table_id: local_gateway_route_table_id, dry_run: dry_run)
        delete_local_gateway_route_table(input)
      end

      def delete_local_gateway_route_table(input : Types::DeleteLocalGatewayRouteTableRequest) : Types::DeleteLocalGatewayRouteTableResult
        request = Protocol::Ec2.build_request(Model::DELETE_LOCAL_GATEWAY_ROUTE_TABLE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteLocalGatewayRouteTableResult, "DeleteLocalGatewayRouteTable")
      end

      # Deletes a local gateway route table virtual interface group association.

      def delete_local_gateway_route_table_virtual_interface_group_association(
        local_gateway_route_table_virtual_interface_group_association_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteLocalGatewayRouteTableVirtualInterfaceGroupAssociationResult

        input = Types::DeleteLocalGatewayRouteTableVirtualInterfaceGroupAssociationRequest.new(local_gateway_route_table_virtual_interface_group_association_id: local_gateway_route_table_virtual_interface_group_association_id, dry_run: dry_run)
        delete_local_gateway_route_table_virtual_interface_group_association(input)
      end

      def delete_local_gateway_route_table_virtual_interface_group_association(input : Types::DeleteLocalGatewayRouteTableVirtualInterfaceGroupAssociationRequest) : Types::DeleteLocalGatewayRouteTableVirtualInterfaceGroupAssociationResult
        request = Protocol::Ec2.build_request(Model::DELETE_LOCAL_GATEWAY_ROUTE_TABLE_VIRTUAL_INTERFACE_GROUP_ASSOCIATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteLocalGatewayRouteTableVirtualInterfaceGroupAssociationResult, "DeleteLocalGatewayRouteTableVirtualInterfaceGroupAssociation")
      end

      # Deletes the specified association between a VPC and local gateway route table.

      def delete_local_gateway_route_table_vpc_association(
        local_gateway_route_table_vpc_association_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteLocalGatewayRouteTableVpcAssociationResult

        input = Types::DeleteLocalGatewayRouteTableVpcAssociationRequest.new(local_gateway_route_table_vpc_association_id: local_gateway_route_table_vpc_association_id, dry_run: dry_run)
        delete_local_gateway_route_table_vpc_association(input)
      end

      def delete_local_gateway_route_table_vpc_association(input : Types::DeleteLocalGatewayRouteTableVpcAssociationRequest) : Types::DeleteLocalGatewayRouteTableVpcAssociationResult
        request = Protocol::Ec2.build_request(Model::DELETE_LOCAL_GATEWAY_ROUTE_TABLE_VPC_ASSOCIATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteLocalGatewayRouteTableVpcAssociationResult, "DeleteLocalGatewayRouteTableVpcAssociation")
      end

      # Deletes the specified local gateway virtual interface.

      def delete_local_gateway_virtual_interface(
        local_gateway_virtual_interface_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteLocalGatewayVirtualInterfaceResult

        input = Types::DeleteLocalGatewayVirtualInterfaceRequest.new(local_gateway_virtual_interface_id: local_gateway_virtual_interface_id, dry_run: dry_run)
        delete_local_gateway_virtual_interface(input)
      end

      def delete_local_gateway_virtual_interface(input : Types::DeleteLocalGatewayVirtualInterfaceRequest) : Types::DeleteLocalGatewayVirtualInterfaceResult
        request = Protocol::Ec2.build_request(Model::DELETE_LOCAL_GATEWAY_VIRTUAL_INTERFACE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteLocalGatewayVirtualInterfaceResult, "DeleteLocalGatewayVirtualInterface")
      end

      # Delete the specified local gateway interface group.

      def delete_local_gateway_virtual_interface_group(
        local_gateway_virtual_interface_group_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteLocalGatewayVirtualInterfaceGroupResult

        input = Types::DeleteLocalGatewayVirtualInterfaceGroupRequest.new(local_gateway_virtual_interface_group_id: local_gateway_virtual_interface_group_id, dry_run: dry_run)
        delete_local_gateway_virtual_interface_group(input)
      end

      def delete_local_gateway_virtual_interface_group(input : Types::DeleteLocalGatewayVirtualInterfaceGroupRequest) : Types::DeleteLocalGatewayVirtualInterfaceGroupResult
        request = Protocol::Ec2.build_request(Model::DELETE_LOCAL_GATEWAY_VIRTUAL_INTERFACE_GROUP, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteLocalGatewayVirtualInterfaceGroupResult, "DeleteLocalGatewayVirtualInterfaceGroup")
      end

      # Deletes the specified managed prefix list. You must first remove all references to the prefix list
      # in your resources.

      def delete_managed_prefix_list(
        prefix_list_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteManagedPrefixListResult

        input = Types::DeleteManagedPrefixListRequest.new(prefix_list_id: prefix_list_id, dry_run: dry_run)
        delete_managed_prefix_list(input)
      end

      def delete_managed_prefix_list(input : Types::DeleteManagedPrefixListRequest) : Types::DeleteManagedPrefixListResult
        request = Protocol::Ec2.build_request(Model::DELETE_MANAGED_PREFIX_LIST, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteManagedPrefixListResult, "DeleteManagedPrefixList")
      end

      # Deletes the specified NAT gateway. Deleting a public NAT gateway disassociates its Elastic IP
      # address, but does not release the address from your account. Deleting a NAT gateway does not delete
      # any NAT gateway routes in your route tables.

      def delete_nat_gateway(
        nat_gateway_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteNatGatewayResult

        input = Types::DeleteNatGatewayRequest.new(nat_gateway_id: nat_gateway_id, dry_run: dry_run)
        delete_nat_gateway(input)
      end

      def delete_nat_gateway(input : Types::DeleteNatGatewayRequest) : Types::DeleteNatGatewayResult
        request = Protocol::Ec2.build_request(Model::DELETE_NAT_GATEWAY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteNatGatewayResult, "DeleteNatGateway")
      end

      # Deletes the specified network ACL. You can't delete the ACL if it's associated with any subnets. You
      # can't delete the default network ACL.

      def delete_network_acl(
        network_acl_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::DeleteNetworkAclRequest.new(network_acl_id: network_acl_id, dry_run: dry_run)
        delete_network_acl(input)
      end

      def delete_network_acl(input : Types::DeleteNetworkAclRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DELETE_NETWORK_ACL, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Deletes the specified ingress or egress entry (rule) from the specified network ACL.

      def delete_network_acl_entry(
        egress : Bool,
        network_acl_id : String,
        rule_number : Int32,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::DeleteNetworkAclEntryRequest.new(egress: egress, network_acl_id: network_acl_id, rule_number: rule_number, dry_run: dry_run)
        delete_network_acl_entry(input)
      end

      def delete_network_acl_entry(input : Types::DeleteNetworkAclEntryRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DELETE_NETWORK_ACL_ENTRY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Deletes the specified Network Access Scope.

      def delete_network_insights_access_scope(
        network_insights_access_scope_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteNetworkInsightsAccessScopeResult

        input = Types::DeleteNetworkInsightsAccessScopeRequest.new(network_insights_access_scope_id: network_insights_access_scope_id, dry_run: dry_run)
        delete_network_insights_access_scope(input)
      end

      def delete_network_insights_access_scope(input : Types::DeleteNetworkInsightsAccessScopeRequest) : Types::DeleteNetworkInsightsAccessScopeResult
        request = Protocol::Ec2.build_request(Model::DELETE_NETWORK_INSIGHTS_ACCESS_SCOPE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteNetworkInsightsAccessScopeResult, "DeleteNetworkInsightsAccessScope")
      end

      # Deletes the specified Network Access Scope analysis.

      def delete_network_insights_access_scope_analysis(
        network_insights_access_scope_analysis_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteNetworkInsightsAccessScopeAnalysisResult

        input = Types::DeleteNetworkInsightsAccessScopeAnalysisRequest.new(network_insights_access_scope_analysis_id: network_insights_access_scope_analysis_id, dry_run: dry_run)
        delete_network_insights_access_scope_analysis(input)
      end

      def delete_network_insights_access_scope_analysis(input : Types::DeleteNetworkInsightsAccessScopeAnalysisRequest) : Types::DeleteNetworkInsightsAccessScopeAnalysisResult
        request = Protocol::Ec2.build_request(Model::DELETE_NETWORK_INSIGHTS_ACCESS_SCOPE_ANALYSIS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteNetworkInsightsAccessScopeAnalysisResult, "DeleteNetworkInsightsAccessScopeAnalysis")
      end

      # Deletes the specified network insights analysis.

      def delete_network_insights_analysis(
        network_insights_analysis_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteNetworkInsightsAnalysisResult

        input = Types::DeleteNetworkInsightsAnalysisRequest.new(network_insights_analysis_id: network_insights_analysis_id, dry_run: dry_run)
        delete_network_insights_analysis(input)
      end

      def delete_network_insights_analysis(input : Types::DeleteNetworkInsightsAnalysisRequest) : Types::DeleteNetworkInsightsAnalysisResult
        request = Protocol::Ec2.build_request(Model::DELETE_NETWORK_INSIGHTS_ANALYSIS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteNetworkInsightsAnalysisResult, "DeleteNetworkInsightsAnalysis")
      end

      # Deletes the specified path.

      def delete_network_insights_path(
        network_insights_path_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteNetworkInsightsPathResult

        input = Types::DeleteNetworkInsightsPathRequest.new(network_insights_path_id: network_insights_path_id, dry_run: dry_run)
        delete_network_insights_path(input)
      end

      def delete_network_insights_path(input : Types::DeleteNetworkInsightsPathRequest) : Types::DeleteNetworkInsightsPathResult
        request = Protocol::Ec2.build_request(Model::DELETE_NETWORK_INSIGHTS_PATH, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteNetworkInsightsPathResult, "DeleteNetworkInsightsPath")
      end

      # Deletes the specified network interface. You must detach the network interface before you can delete
      # it.

      def delete_network_interface(
        network_interface_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::DeleteNetworkInterfaceRequest.new(network_interface_id: network_interface_id, dry_run: dry_run)
        delete_network_interface(input)
      end

      def delete_network_interface(input : Types::DeleteNetworkInterfaceRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DELETE_NETWORK_INTERFACE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Deletes a permission for a network interface. By default, you cannot delete the permission if the
      # account for which you're removing the permission has attached the network interface to an instance.
      # However, you can force delete the permission, regardless of any attachment.

      def delete_network_interface_permission(
        network_interface_permission_id : String,
        dry_run : Bool? = nil,
        force : Bool? = nil
      ) : Types::DeleteNetworkInterfacePermissionResult

        input = Types::DeleteNetworkInterfacePermissionRequest.new(network_interface_permission_id: network_interface_permission_id, dry_run: dry_run, force: force)
        delete_network_interface_permission(input)
      end

      def delete_network_interface_permission(input : Types::DeleteNetworkInterfacePermissionRequest) : Types::DeleteNetworkInterfacePermissionResult
        request = Protocol::Ec2.build_request(Model::DELETE_NETWORK_INTERFACE_PERMISSION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteNetworkInterfacePermissionResult, "DeleteNetworkInterfacePermission")
      end

      # Deletes the specified placement group. You must terminate all instances in the placement group
      # before you can delete the placement group. For more information, see Placement groups in the Amazon
      # EC2 User Guide .

      def delete_placement_group(
        group_name : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::DeletePlacementGroupRequest.new(group_name: group_name, dry_run: dry_run)
        delete_placement_group(input)
      end

      def delete_placement_group(input : Types::DeletePlacementGroupRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DELETE_PLACEMENT_GROUP, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Delete a public IPv4 pool. A public IPv4 pool is an EC2 IP address pool required for the public IPv4
      # CIDRs that you own and bring to Amazon Web Services to manage with IPAM. IPv6 addresses you bring to
      # Amazon Web Services, however, use IPAM pools only.

      def delete_public_ipv4_pool(
        pool_id : String,
        dry_run : Bool? = nil,
        network_border_group : String? = nil
      ) : Types::DeletePublicIpv4PoolResult

        input = Types::DeletePublicIpv4PoolRequest.new(pool_id: pool_id, dry_run: dry_run, network_border_group: network_border_group)
        delete_public_ipv4_pool(input)
      end

      def delete_public_ipv4_pool(input : Types::DeletePublicIpv4PoolRequest) : Types::DeletePublicIpv4PoolResult
        request = Protocol::Ec2.build_request(Model::DELETE_PUBLIC_IPV4_POOL, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeletePublicIpv4PoolResult, "DeletePublicIpv4Pool")
      end

      # Deletes the queued purchases for the specified Reserved Instances.

      def delete_queued_reserved_instances(
        reserved_instances_ids : Array(String),
        dry_run : Bool? = nil
      ) : Types::DeleteQueuedReservedInstancesResult

        input = Types::DeleteQueuedReservedInstancesRequest.new(reserved_instances_ids: reserved_instances_ids, dry_run: dry_run)
        delete_queued_reserved_instances(input)
      end

      def delete_queued_reserved_instances(input : Types::DeleteQueuedReservedInstancesRequest) : Types::DeleteQueuedReservedInstancesResult
        request = Protocol::Ec2.build_request(Model::DELETE_QUEUED_RESERVED_INSTANCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteQueuedReservedInstancesResult, "DeleteQueuedReservedInstances")
      end

      # Deletes the specified route from the specified route table.

      def delete_route(
        route_table_id : String,
        destination_cidr_block : String? = nil,
        destination_ipv6_cidr_block : String? = nil,
        destination_prefix_list_id : String? = nil,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::DeleteRouteRequest.new(route_table_id: route_table_id, destination_cidr_block: destination_cidr_block, destination_ipv6_cidr_block: destination_ipv6_cidr_block, destination_prefix_list_id: destination_prefix_list_id, dry_run: dry_run)
        delete_route(input)
      end

      def delete_route(input : Types::DeleteRouteRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DELETE_ROUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Deletes the specified route server. Amazon VPC Route Server simplifies routing for traffic between
      # workloads that are deployed within a VPC and its internet gateways. With this feature, VPC Route
      # Server dynamically updates VPC and internet gateway route tables with your preferred IPv4 or IPv6
      # routes to achieve routing fault tolerance for those workloads. This enables you to automatically
      # reroute traffic within a VPC, which increases the manageability of VPC routing and interoperability
      # with third-party workloads. Route server supports the follow route table types: VPC route tables not
      # associated with subnets Subnet route tables Internet gateway route tables Route server does not
      # support route tables associated with virtual private gateways. To propagate routes into a transit
      # gateway route table, use Transit Gateway Connect . For more information see Dynamic routing in your
      # VPC with VPC Route Server in the Amazon VPC User Guide .

      def delete_route_server(
        route_server_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteRouteServerResult

        input = Types::DeleteRouteServerRequest.new(route_server_id: route_server_id, dry_run: dry_run)
        delete_route_server(input)
      end

      def delete_route_server(input : Types::DeleteRouteServerRequest) : Types::DeleteRouteServerResult
        request = Protocol::Ec2.build_request(Model::DELETE_ROUTE_SERVER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteRouteServerResult, "DeleteRouteServer")
      end

      # Deletes the specified route server endpoint. A route server endpoint is an Amazon Web
      # Services-managed component inside a subnet that facilitates BGP (Border Gateway Protocol)
      # connections between your route server and your BGP peers.

      def delete_route_server_endpoint(
        route_server_endpoint_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteRouteServerEndpointResult

        input = Types::DeleteRouteServerEndpointRequest.new(route_server_endpoint_id: route_server_endpoint_id, dry_run: dry_run)
        delete_route_server_endpoint(input)
      end

      def delete_route_server_endpoint(input : Types::DeleteRouteServerEndpointRequest) : Types::DeleteRouteServerEndpointResult
        request = Protocol::Ec2.build_request(Model::DELETE_ROUTE_SERVER_ENDPOINT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteRouteServerEndpointResult, "DeleteRouteServerEndpoint")
      end

      # Deletes the specified BGP peer from a route server. A route server peer is a session between a route
      # server endpoint and the device deployed in Amazon Web Services (such as a firewall appliance or
      # other network security function running on an EC2 instance). The device must meet these
      # requirements: Have an elastic network interface in the VPC Support BGP (Border Gateway Protocol) Can
      # initiate BGP sessions

      def delete_route_server_peer(
        route_server_peer_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteRouteServerPeerResult

        input = Types::DeleteRouteServerPeerRequest.new(route_server_peer_id: route_server_peer_id, dry_run: dry_run)
        delete_route_server_peer(input)
      end

      def delete_route_server_peer(input : Types::DeleteRouteServerPeerRequest) : Types::DeleteRouteServerPeerResult
        request = Protocol::Ec2.build_request(Model::DELETE_ROUTE_SERVER_PEER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteRouteServerPeerResult, "DeleteRouteServerPeer")
      end

      # Deletes the specified route table. You must disassociate the route table from any subnets before you
      # can delete it. You can't delete the main route table.

      def delete_route_table(
        route_table_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::DeleteRouteTableRequest.new(route_table_id: route_table_id, dry_run: dry_run)
        delete_route_table(input)
      end

      def delete_route_table(input : Types::DeleteRouteTableRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DELETE_ROUTE_TABLE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Deletes a security group. If you attempt to delete a security group that is associated with an
      # instance or network interface, is referenced by another security group in the same VPC, or has a VPC
      # association, the operation fails with DependencyViolation .

      def delete_security_group(
        dry_run : Bool? = nil,
        group_id : String? = nil,
        group_name : String? = nil
      ) : Types::DeleteSecurityGroupResult

        input = Types::DeleteSecurityGroupRequest.new(dry_run: dry_run, group_id: group_id, group_name: group_name)
        delete_security_group(input)
      end

      def delete_security_group(input : Types::DeleteSecurityGroupRequest) : Types::DeleteSecurityGroupResult
        request = Protocol::Ec2.build_request(Model::DELETE_SECURITY_GROUP, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteSecurityGroupResult, "DeleteSecurityGroup")
      end

      # Deletes the specified snapshot. When you make periodic snapshots of a volume, the snapshots are
      # incremental, and only the blocks on the device that have changed since your last snapshot are saved
      # in the new snapshot. When you delete a snapshot, only the data not needed for any other snapshot is
      # removed. So regardless of which prior snapshots have been deleted, all active snapshots will have
      # access to all the information needed to restore the volume. You cannot delete a snapshot of the root
      # device of an EBS volume used by a registered AMI. You must first deregister the AMI before you can
      # delete the snapshot. For more information, see Delete an Amazon EBS snapshot in the Amazon EBS User
      # Guide .

      def delete_snapshot(
        snapshot_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::DeleteSnapshotRequest.new(snapshot_id: snapshot_id, dry_run: dry_run)
        delete_snapshot(input)
      end

      def delete_snapshot(input : Types::DeleteSnapshotRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DELETE_SNAPSHOT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Deletes the data feed for Spot Instances.

      def delete_spot_datafeed_subscription(
        dry_run : Bool? = nil
      ) : Nil

        input = Types::DeleteSpotDatafeedSubscriptionRequest.new(dry_run: dry_run)
        delete_spot_datafeed_subscription(input)
      end

      def delete_spot_datafeed_subscription(input : Types::DeleteSpotDatafeedSubscriptionRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DELETE_SPOT_DATAFEED_SUBSCRIPTION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Deletes the specified subnet. You must terminate all running instances in the subnet before you can
      # delete the subnet.

      def delete_subnet(
        subnet_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::DeleteSubnetRequest.new(subnet_id: subnet_id, dry_run: dry_run)
        delete_subnet(input)
      end

      def delete_subnet(input : Types::DeleteSubnetRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DELETE_SUBNET, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Deletes a subnet CIDR reservation.

      def delete_subnet_cidr_reservation(
        subnet_cidr_reservation_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteSubnetCidrReservationResult

        input = Types::DeleteSubnetCidrReservationRequest.new(subnet_cidr_reservation_id: subnet_cidr_reservation_id, dry_run: dry_run)
        delete_subnet_cidr_reservation(input)
      end

      def delete_subnet_cidr_reservation(input : Types::DeleteSubnetCidrReservationRequest) : Types::DeleteSubnetCidrReservationResult
        request = Protocol::Ec2.build_request(Model::DELETE_SUBNET_CIDR_RESERVATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteSubnetCidrReservationResult, "DeleteSubnetCidrReservation")
      end

      # Deletes the specified set of tags from the specified set of resources. To list the current tags, use
      # DescribeTags . For more information about tags, see Tag your Amazon EC2 resources in the Amazon
      # Elastic Compute Cloud User Guide .

      def delete_tags(
        resources : Array(String),
        dry_run : Bool? = nil,
        tags : Array(Types::Tag)? = nil
      ) : Nil

        input = Types::DeleteTagsRequest.new(resources: resources, dry_run: dry_run, tags: tags)
        delete_tags(input)
      end

      def delete_tags(input : Types::DeleteTagsRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DELETE_TAGS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Deletes the specified Traffic Mirror filter. You cannot delete a Traffic Mirror filter that is in
      # use by a Traffic Mirror session.

      def delete_traffic_mirror_filter(
        traffic_mirror_filter_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteTrafficMirrorFilterResult

        input = Types::DeleteTrafficMirrorFilterRequest.new(traffic_mirror_filter_id: traffic_mirror_filter_id, dry_run: dry_run)
        delete_traffic_mirror_filter(input)
      end

      def delete_traffic_mirror_filter(input : Types::DeleteTrafficMirrorFilterRequest) : Types::DeleteTrafficMirrorFilterResult
        request = Protocol::Ec2.build_request(Model::DELETE_TRAFFIC_MIRROR_FILTER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteTrafficMirrorFilterResult, "DeleteTrafficMirrorFilter")
      end

      # Deletes the specified Traffic Mirror rule.

      def delete_traffic_mirror_filter_rule(
        traffic_mirror_filter_rule_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteTrafficMirrorFilterRuleResult

        input = Types::DeleteTrafficMirrorFilterRuleRequest.new(traffic_mirror_filter_rule_id: traffic_mirror_filter_rule_id, dry_run: dry_run)
        delete_traffic_mirror_filter_rule(input)
      end

      def delete_traffic_mirror_filter_rule(input : Types::DeleteTrafficMirrorFilterRuleRequest) : Types::DeleteTrafficMirrorFilterRuleResult
        request = Protocol::Ec2.build_request(Model::DELETE_TRAFFIC_MIRROR_FILTER_RULE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteTrafficMirrorFilterRuleResult, "DeleteTrafficMirrorFilterRule")
      end

      # Deletes the specified Traffic Mirror session.

      def delete_traffic_mirror_session(
        traffic_mirror_session_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteTrafficMirrorSessionResult

        input = Types::DeleteTrafficMirrorSessionRequest.new(traffic_mirror_session_id: traffic_mirror_session_id, dry_run: dry_run)
        delete_traffic_mirror_session(input)
      end

      def delete_traffic_mirror_session(input : Types::DeleteTrafficMirrorSessionRequest) : Types::DeleteTrafficMirrorSessionResult
        request = Protocol::Ec2.build_request(Model::DELETE_TRAFFIC_MIRROR_SESSION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteTrafficMirrorSessionResult, "DeleteTrafficMirrorSession")
      end

      # Deletes the specified Traffic Mirror target. You cannot delete a Traffic Mirror target that is in
      # use by a Traffic Mirror session.

      def delete_traffic_mirror_target(
        traffic_mirror_target_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteTrafficMirrorTargetResult

        input = Types::DeleteTrafficMirrorTargetRequest.new(traffic_mirror_target_id: traffic_mirror_target_id, dry_run: dry_run)
        delete_traffic_mirror_target(input)
      end

      def delete_traffic_mirror_target(input : Types::DeleteTrafficMirrorTargetRequest) : Types::DeleteTrafficMirrorTargetResult
        request = Protocol::Ec2.build_request(Model::DELETE_TRAFFIC_MIRROR_TARGET, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteTrafficMirrorTargetResult, "DeleteTrafficMirrorTarget")
      end

      # Deletes the specified transit gateway.

      def delete_transit_gateway(
        transit_gateway_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteTransitGatewayResult

        input = Types::DeleteTransitGatewayRequest.new(transit_gateway_id: transit_gateway_id, dry_run: dry_run)
        delete_transit_gateway(input)
      end

      def delete_transit_gateway(input : Types::DeleteTransitGatewayRequest) : Types::DeleteTransitGatewayResult
        request = Protocol::Ec2.build_request(Model::DELETE_TRANSIT_GATEWAY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteTransitGatewayResult, "DeleteTransitGateway")
      end

      # Deletes the specified Connect attachment. You must first delete any Connect peers for the
      # attachment.

      def delete_transit_gateway_connect(
        transit_gateway_attachment_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteTransitGatewayConnectResult

        input = Types::DeleteTransitGatewayConnectRequest.new(transit_gateway_attachment_id: transit_gateway_attachment_id, dry_run: dry_run)
        delete_transit_gateway_connect(input)
      end

      def delete_transit_gateway_connect(input : Types::DeleteTransitGatewayConnectRequest) : Types::DeleteTransitGatewayConnectResult
        request = Protocol::Ec2.build_request(Model::DELETE_TRANSIT_GATEWAY_CONNECT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteTransitGatewayConnectResult, "DeleteTransitGatewayConnect")
      end

      # Deletes the specified Connect peer.

      def delete_transit_gateway_connect_peer(
        transit_gateway_connect_peer_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteTransitGatewayConnectPeerResult

        input = Types::DeleteTransitGatewayConnectPeerRequest.new(transit_gateway_connect_peer_id: transit_gateway_connect_peer_id, dry_run: dry_run)
        delete_transit_gateway_connect_peer(input)
      end

      def delete_transit_gateway_connect_peer(input : Types::DeleteTransitGatewayConnectPeerRequest) : Types::DeleteTransitGatewayConnectPeerResult
        request = Protocol::Ec2.build_request(Model::DELETE_TRANSIT_GATEWAY_CONNECT_PEER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteTransitGatewayConnectPeerResult, "DeleteTransitGatewayConnectPeer")
      end

      # Deletes a transit gateway metering policy.

      def delete_transit_gateway_metering_policy(
        transit_gateway_metering_policy_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteTransitGatewayMeteringPolicyResult

        input = Types::DeleteTransitGatewayMeteringPolicyRequest.new(transit_gateway_metering_policy_id: transit_gateway_metering_policy_id, dry_run: dry_run)
        delete_transit_gateway_metering_policy(input)
      end

      def delete_transit_gateway_metering_policy(input : Types::DeleteTransitGatewayMeteringPolicyRequest) : Types::DeleteTransitGatewayMeteringPolicyResult
        request = Protocol::Ec2.build_request(Model::DELETE_TRANSIT_GATEWAY_METERING_POLICY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteTransitGatewayMeteringPolicyResult, "DeleteTransitGatewayMeteringPolicy")
      end

      # Deletes an entry from a transit gateway metering policy.

      def delete_transit_gateway_metering_policy_entry(
        policy_rule_number : Int32,
        transit_gateway_metering_policy_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteTransitGatewayMeteringPolicyEntryResult

        input = Types::DeleteTransitGatewayMeteringPolicyEntryRequest.new(policy_rule_number: policy_rule_number, transit_gateway_metering_policy_id: transit_gateway_metering_policy_id, dry_run: dry_run)
        delete_transit_gateway_metering_policy_entry(input)
      end

      def delete_transit_gateway_metering_policy_entry(input : Types::DeleteTransitGatewayMeteringPolicyEntryRequest) : Types::DeleteTransitGatewayMeteringPolicyEntryResult
        request = Protocol::Ec2.build_request(Model::DELETE_TRANSIT_GATEWAY_METERING_POLICY_ENTRY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteTransitGatewayMeteringPolicyEntryResult, "DeleteTransitGatewayMeteringPolicyEntry")
      end

      # Deletes the specified transit gateway multicast domain.

      def delete_transit_gateway_multicast_domain(
        transit_gateway_multicast_domain_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteTransitGatewayMulticastDomainResult

        input = Types::DeleteTransitGatewayMulticastDomainRequest.new(transit_gateway_multicast_domain_id: transit_gateway_multicast_domain_id, dry_run: dry_run)
        delete_transit_gateway_multicast_domain(input)
      end

      def delete_transit_gateway_multicast_domain(input : Types::DeleteTransitGatewayMulticastDomainRequest) : Types::DeleteTransitGatewayMulticastDomainResult
        request = Protocol::Ec2.build_request(Model::DELETE_TRANSIT_GATEWAY_MULTICAST_DOMAIN, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteTransitGatewayMulticastDomainResult, "DeleteTransitGatewayMulticastDomain")
      end

      # Deletes a transit gateway peering attachment.

      def delete_transit_gateway_peering_attachment(
        transit_gateway_attachment_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteTransitGatewayPeeringAttachmentResult

        input = Types::DeleteTransitGatewayPeeringAttachmentRequest.new(transit_gateway_attachment_id: transit_gateway_attachment_id, dry_run: dry_run)
        delete_transit_gateway_peering_attachment(input)
      end

      def delete_transit_gateway_peering_attachment(input : Types::DeleteTransitGatewayPeeringAttachmentRequest) : Types::DeleteTransitGatewayPeeringAttachmentResult
        request = Protocol::Ec2.build_request(Model::DELETE_TRANSIT_GATEWAY_PEERING_ATTACHMENT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteTransitGatewayPeeringAttachmentResult, "DeleteTransitGatewayPeeringAttachment")
      end

      # Deletes the specified transit gateway policy table.

      def delete_transit_gateway_policy_table(
        transit_gateway_policy_table_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteTransitGatewayPolicyTableResult

        input = Types::DeleteTransitGatewayPolicyTableRequest.new(transit_gateway_policy_table_id: transit_gateway_policy_table_id, dry_run: dry_run)
        delete_transit_gateway_policy_table(input)
      end

      def delete_transit_gateway_policy_table(input : Types::DeleteTransitGatewayPolicyTableRequest) : Types::DeleteTransitGatewayPolicyTableResult
        request = Protocol::Ec2.build_request(Model::DELETE_TRANSIT_GATEWAY_POLICY_TABLE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteTransitGatewayPolicyTableResult, "DeleteTransitGatewayPolicyTable")
      end

      # Deletes a reference (route) to a prefix list in a specified transit gateway route table.

      def delete_transit_gateway_prefix_list_reference(
        prefix_list_id : String,
        transit_gateway_route_table_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteTransitGatewayPrefixListReferenceResult

        input = Types::DeleteTransitGatewayPrefixListReferenceRequest.new(prefix_list_id: prefix_list_id, transit_gateway_route_table_id: transit_gateway_route_table_id, dry_run: dry_run)
        delete_transit_gateway_prefix_list_reference(input)
      end

      def delete_transit_gateway_prefix_list_reference(input : Types::DeleteTransitGatewayPrefixListReferenceRequest) : Types::DeleteTransitGatewayPrefixListReferenceResult
        request = Protocol::Ec2.build_request(Model::DELETE_TRANSIT_GATEWAY_PREFIX_LIST_REFERENCE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteTransitGatewayPrefixListReferenceResult, "DeleteTransitGatewayPrefixListReference")
      end

      # Deletes the specified route from the specified transit gateway route table.

      def delete_transit_gateway_route(
        destination_cidr_block : String,
        transit_gateway_route_table_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteTransitGatewayRouteResult

        input = Types::DeleteTransitGatewayRouteRequest.new(destination_cidr_block: destination_cidr_block, transit_gateway_route_table_id: transit_gateway_route_table_id, dry_run: dry_run)
        delete_transit_gateway_route(input)
      end

      def delete_transit_gateway_route(input : Types::DeleteTransitGatewayRouteRequest) : Types::DeleteTransitGatewayRouteResult
        request = Protocol::Ec2.build_request(Model::DELETE_TRANSIT_GATEWAY_ROUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteTransitGatewayRouteResult, "DeleteTransitGatewayRoute")
      end

      # Deletes the specified transit gateway route table. If there are any route tables associated with the
      # transit gateway route table, you must first run DisassociateRouteTable before you can delete the
      # transit gateway route table. This removes any route tables associated with the transit gateway route
      # table.

      def delete_transit_gateway_route_table(
        transit_gateway_route_table_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteTransitGatewayRouteTableResult

        input = Types::DeleteTransitGatewayRouteTableRequest.new(transit_gateway_route_table_id: transit_gateway_route_table_id, dry_run: dry_run)
        delete_transit_gateway_route_table(input)
      end

      def delete_transit_gateway_route_table(input : Types::DeleteTransitGatewayRouteTableRequest) : Types::DeleteTransitGatewayRouteTableResult
        request = Protocol::Ec2.build_request(Model::DELETE_TRANSIT_GATEWAY_ROUTE_TABLE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteTransitGatewayRouteTableResult, "DeleteTransitGatewayRouteTable")
      end

      # Advertises to the transit gateway that a transit gateway route table is deleted.

      def delete_transit_gateway_route_table_announcement(
        transit_gateway_route_table_announcement_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteTransitGatewayRouteTableAnnouncementResult

        input = Types::DeleteTransitGatewayRouteTableAnnouncementRequest.new(transit_gateway_route_table_announcement_id: transit_gateway_route_table_announcement_id, dry_run: dry_run)
        delete_transit_gateway_route_table_announcement(input)
      end

      def delete_transit_gateway_route_table_announcement(input : Types::DeleteTransitGatewayRouteTableAnnouncementRequest) : Types::DeleteTransitGatewayRouteTableAnnouncementResult
        request = Protocol::Ec2.build_request(Model::DELETE_TRANSIT_GATEWAY_ROUTE_TABLE_ANNOUNCEMENT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteTransitGatewayRouteTableAnnouncementResult, "DeleteTransitGatewayRouteTableAnnouncement")
      end

      # Deletes the specified VPC attachment.

      def delete_transit_gateway_vpc_attachment(
        transit_gateway_attachment_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteTransitGatewayVpcAttachmentResult

        input = Types::DeleteTransitGatewayVpcAttachmentRequest.new(transit_gateway_attachment_id: transit_gateway_attachment_id, dry_run: dry_run)
        delete_transit_gateway_vpc_attachment(input)
      end

      def delete_transit_gateway_vpc_attachment(input : Types::DeleteTransitGatewayVpcAttachmentRequest) : Types::DeleteTransitGatewayVpcAttachmentResult
        request = Protocol::Ec2.build_request(Model::DELETE_TRANSIT_GATEWAY_VPC_ATTACHMENT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteTransitGatewayVpcAttachmentResult, "DeleteTransitGatewayVpcAttachment")
      end

      # Delete an Amazon Web Services Verified Access endpoint.

      def delete_verified_access_endpoint(
        verified_access_endpoint_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil
      ) : Types::DeleteVerifiedAccessEndpointResult

        input = Types::DeleteVerifiedAccessEndpointRequest.new(verified_access_endpoint_id: verified_access_endpoint_id, client_token: client_token, dry_run: dry_run)
        delete_verified_access_endpoint(input)
      end

      def delete_verified_access_endpoint(input : Types::DeleteVerifiedAccessEndpointRequest) : Types::DeleteVerifiedAccessEndpointResult
        request = Protocol::Ec2.build_request(Model::DELETE_VERIFIED_ACCESS_ENDPOINT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteVerifiedAccessEndpointResult, "DeleteVerifiedAccessEndpoint")
      end

      # Delete an Amazon Web Services Verified Access group.

      def delete_verified_access_group(
        verified_access_group_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil
      ) : Types::DeleteVerifiedAccessGroupResult

        input = Types::DeleteVerifiedAccessGroupRequest.new(verified_access_group_id: verified_access_group_id, client_token: client_token, dry_run: dry_run)
        delete_verified_access_group(input)
      end

      def delete_verified_access_group(input : Types::DeleteVerifiedAccessGroupRequest) : Types::DeleteVerifiedAccessGroupResult
        request = Protocol::Ec2.build_request(Model::DELETE_VERIFIED_ACCESS_GROUP, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteVerifiedAccessGroupResult, "DeleteVerifiedAccessGroup")
      end

      # Delete an Amazon Web Services Verified Access instance.

      def delete_verified_access_instance(
        verified_access_instance_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil
      ) : Types::DeleteVerifiedAccessInstanceResult

        input = Types::DeleteVerifiedAccessInstanceRequest.new(verified_access_instance_id: verified_access_instance_id, client_token: client_token, dry_run: dry_run)
        delete_verified_access_instance(input)
      end

      def delete_verified_access_instance(input : Types::DeleteVerifiedAccessInstanceRequest) : Types::DeleteVerifiedAccessInstanceResult
        request = Protocol::Ec2.build_request(Model::DELETE_VERIFIED_ACCESS_INSTANCE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteVerifiedAccessInstanceResult, "DeleteVerifiedAccessInstance")
      end

      # Delete an Amazon Web Services Verified Access trust provider.

      def delete_verified_access_trust_provider(
        verified_access_trust_provider_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil
      ) : Types::DeleteVerifiedAccessTrustProviderResult

        input = Types::DeleteVerifiedAccessTrustProviderRequest.new(verified_access_trust_provider_id: verified_access_trust_provider_id, client_token: client_token, dry_run: dry_run)
        delete_verified_access_trust_provider(input)
      end

      def delete_verified_access_trust_provider(input : Types::DeleteVerifiedAccessTrustProviderRequest) : Types::DeleteVerifiedAccessTrustProviderResult
        request = Protocol::Ec2.build_request(Model::DELETE_VERIFIED_ACCESS_TRUST_PROVIDER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteVerifiedAccessTrustProviderResult, "DeleteVerifiedAccessTrustProvider")
      end

      # Deletes the specified EBS volume. The volume must be in the available state (not attached to an
      # instance). The volume can remain in the deleting state for several minutes. For more information,
      # see Delete an Amazon EBS volume in the Amazon EBS User Guide .

      def delete_volume(
        volume_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::DeleteVolumeRequest.new(volume_id: volume_id, dry_run: dry_run)
        delete_volume(input)
      end

      def delete_volume(input : Types::DeleteVolumeRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DELETE_VOLUME, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Deletes the specified VPC. You must detach or delete all gateways and resources that are associated
      # with the VPC before you can delete it. For example, you must terminate all instances running in the
      # VPC, delete all security groups associated with the VPC (except the default one), delete all route
      # tables associated with the VPC (except the default one), and so on. When you delete the VPC, it
      # deletes the default security group, network ACL, and route table for the VPC. If you created a flow
      # log for the VPC that you are deleting, note that flow logs for deleted VPCs are eventually
      # automatically removed.

      def delete_vpc(
        vpc_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::DeleteVpcRequest.new(vpc_id: vpc_id, dry_run: dry_run)
        delete_vpc(input)
      end

      def delete_vpc(input : Types::DeleteVpcRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DELETE_VPC, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Delete a VPC Block Public Access (BPA) exclusion. A VPC BPA exclusion is a mode that can be applied
      # to a single VPC or subnet that exempts it from the account’s BPA mode and will allow bidirectional
      # or egress-only access. You can create BPA exclusions for VPCs and subnets even when BPA is not
      # enabled on the account to ensure that there is no traffic disruption to the exclusions when VPC BPA
      # is turned on. To learn more about VPC BPA, see Block public access to VPCs and subnets in the Amazon
      # VPC User Guide .

      def delete_vpc_block_public_access_exclusion(
        exclusion_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteVpcBlockPublicAccessExclusionResult

        input = Types::DeleteVpcBlockPublicAccessExclusionRequest.new(exclusion_id: exclusion_id, dry_run: dry_run)
        delete_vpc_block_public_access_exclusion(input)
      end

      def delete_vpc_block_public_access_exclusion(input : Types::DeleteVpcBlockPublicAccessExclusionRequest) : Types::DeleteVpcBlockPublicAccessExclusionResult
        request = Protocol::Ec2.build_request(Model::DELETE_VPC_BLOCK_PUBLIC_ACCESS_EXCLUSION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteVpcBlockPublicAccessExclusionResult, "DeleteVpcBlockPublicAccessExclusion")
      end

      # Deletes a VPC Encryption Control configuration. This removes the encryption policy enforcement from
      # the specified VPC. For more information, see Enforce VPC encryption in transit in the Amazon VPC
      # User Guide .

      def delete_vpc_encryption_control(
        vpc_encryption_control_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteVpcEncryptionControlResult

        input = Types::DeleteVpcEncryptionControlRequest.new(vpc_encryption_control_id: vpc_encryption_control_id, dry_run: dry_run)
        delete_vpc_encryption_control(input)
      end

      def delete_vpc_encryption_control(input : Types::DeleteVpcEncryptionControlRequest) : Types::DeleteVpcEncryptionControlResult
        request = Protocol::Ec2.build_request(Model::DELETE_VPC_ENCRYPTION_CONTROL, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteVpcEncryptionControlResult, "DeleteVpcEncryptionControl")
      end

      # Deletes the specified VPC endpoint connection notifications.

      def delete_vpc_endpoint_connection_notifications(
        connection_notification_ids : Array(String),
        dry_run : Bool? = nil
      ) : Types::DeleteVpcEndpointConnectionNotificationsResult

        input = Types::DeleteVpcEndpointConnectionNotificationsRequest.new(connection_notification_ids: connection_notification_ids, dry_run: dry_run)
        delete_vpc_endpoint_connection_notifications(input)
      end

      def delete_vpc_endpoint_connection_notifications(input : Types::DeleteVpcEndpointConnectionNotificationsRequest) : Types::DeleteVpcEndpointConnectionNotificationsResult
        request = Protocol::Ec2.build_request(Model::DELETE_VPC_ENDPOINT_CONNECTION_NOTIFICATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteVpcEndpointConnectionNotificationsResult, "DeleteVpcEndpointConnectionNotifications")
      end

      # Deletes the specified VPC endpoint service configurations. Before you can delete an endpoint service
      # configuration, you must reject any Available or PendingAcceptance interface endpoint connections
      # that are attached to the service.

      def delete_vpc_endpoint_service_configurations(
        service_ids : Array(String),
        dry_run : Bool? = nil
      ) : Types::DeleteVpcEndpointServiceConfigurationsResult

        input = Types::DeleteVpcEndpointServiceConfigurationsRequest.new(service_ids: service_ids, dry_run: dry_run)
        delete_vpc_endpoint_service_configurations(input)
      end

      def delete_vpc_endpoint_service_configurations(input : Types::DeleteVpcEndpointServiceConfigurationsRequest) : Types::DeleteVpcEndpointServiceConfigurationsResult
        request = Protocol::Ec2.build_request(Model::DELETE_VPC_ENDPOINT_SERVICE_CONFIGURATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteVpcEndpointServiceConfigurationsResult, "DeleteVpcEndpointServiceConfigurations")
      end

      # Deletes the specified VPC endpoints. When you delete a gateway endpoint, we delete the endpoint
      # routes in the route tables for the endpoint. When you delete a Gateway Load Balancer endpoint, we
      # delete its endpoint network interfaces. You can only delete Gateway Load Balancer endpoints when the
      # routes that are associated with the endpoint are deleted. When you delete an interface endpoint, we
      # delete its endpoint network interfaces.

      def delete_vpc_endpoints(
        vpc_endpoint_ids : Array(String),
        dry_run : Bool? = nil
      ) : Types::DeleteVpcEndpointsResult

        input = Types::DeleteVpcEndpointsRequest.new(vpc_endpoint_ids: vpc_endpoint_ids, dry_run: dry_run)
        delete_vpc_endpoints(input)
      end

      def delete_vpc_endpoints(input : Types::DeleteVpcEndpointsRequest) : Types::DeleteVpcEndpointsResult
        request = Protocol::Ec2.build_request(Model::DELETE_VPC_ENDPOINTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteVpcEndpointsResult, "DeleteVpcEndpoints")
      end

      # Deletes a VPC peering connection. Either the owner of the requester VPC or the owner of the accepter
      # VPC can delete the VPC peering connection if it's in the active state. The owner of the requester
      # VPC can delete a VPC peering connection in the pending-acceptance state. You cannot delete a VPC
      # peering connection that's in the failed or rejected state.

      def delete_vpc_peering_connection(
        vpc_peering_connection_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteVpcPeeringConnectionResult

        input = Types::DeleteVpcPeeringConnectionRequest.new(vpc_peering_connection_id: vpc_peering_connection_id, dry_run: dry_run)
        delete_vpc_peering_connection(input)
      end

      def delete_vpc_peering_connection(input : Types::DeleteVpcPeeringConnectionRequest) : Types::DeleteVpcPeeringConnectionResult
        request = Protocol::Ec2.build_request(Model::DELETE_VPC_PEERING_CONNECTION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteVpcPeeringConnectionResult, "DeleteVpcPeeringConnection")
      end

      # Deletes the specified VPN concentrator.

      def delete_vpn_concentrator(
        vpn_concentrator_id : String,
        dry_run : Bool? = nil
      ) : Types::DeleteVpnConcentratorResult

        input = Types::DeleteVpnConcentratorRequest.new(vpn_concentrator_id: vpn_concentrator_id, dry_run: dry_run)
        delete_vpn_concentrator(input)
      end

      def delete_vpn_concentrator(input : Types::DeleteVpnConcentratorRequest) : Types::DeleteVpnConcentratorResult
        request = Protocol::Ec2.build_request(Model::DELETE_VPN_CONCENTRATOR, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeleteVpnConcentratorResult, "DeleteVpnConcentrator")
      end

      # Deletes the specified VPN connection. If you're deleting the VPC and its associated components, we
      # recommend that you detach the virtual private gateway from the VPC and delete the VPC before
      # deleting the VPN connection. If you believe that the tunnel credentials for your VPN connection have
      # been compromised, you can delete the VPN connection and create a new one that has new keys, without
      # needing to delete the VPC or virtual private gateway. If you create a new VPN connection, you must
      # reconfigure the customer gateway device using the new configuration information returned with the
      # new VPN connection ID. For certificate-based authentication, delete all Certificate Manager (ACM)
      # private certificates used for the Amazon Web Services-side tunnel endpoints for the VPN connection
      # before deleting the VPN connection.

      def delete_vpn_connection(
        vpn_connection_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::DeleteVpnConnectionRequest.new(vpn_connection_id: vpn_connection_id, dry_run: dry_run)
        delete_vpn_connection(input)
      end

      def delete_vpn_connection(input : Types::DeleteVpnConnectionRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DELETE_VPN_CONNECTION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Deletes the specified static route associated with a VPN connection between an existing virtual
      # private gateway and a VPN customer gateway. The static route allows traffic to be routed from the
      # virtual private gateway to the VPN customer gateway.

      def delete_vpn_connection_route(
        destination_cidr_block : String,
        vpn_connection_id : String
      ) : Nil

        input = Types::DeleteVpnConnectionRouteRequest.new(destination_cidr_block: destination_cidr_block, vpn_connection_id: vpn_connection_id)
        delete_vpn_connection_route(input)
      end

      def delete_vpn_connection_route(input : Types::DeleteVpnConnectionRouteRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DELETE_VPN_CONNECTION_ROUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Deletes the specified virtual private gateway. You must first detach the virtual private gateway
      # from the VPC. Note that you don't need to delete the virtual private gateway if you plan to delete
      # and recreate the VPN connection between your VPC and your network.

      def delete_vpn_gateway(
        vpn_gateway_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::DeleteVpnGatewayRequest.new(vpn_gateway_id: vpn_gateway_id, dry_run: dry_run)
        delete_vpn_gateway(input)
      end

      def delete_vpn_gateway(input : Types::DeleteVpnGatewayRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DELETE_VPN_GATEWAY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Releases the specified address range that you provisioned for use with your Amazon Web Services
      # resources through bring your own IP addresses (BYOIP) and deletes the corresponding address pool.
      # Before you can release an address range, you must stop advertising it and you must not have any IP
      # addresses allocated from its address range.

      def deprovision_byoip_cidr(
        cidr : String,
        dry_run : Bool? = nil
      ) : Types::DeprovisionByoipCidrResult

        input = Types::DeprovisionByoipCidrRequest.new(cidr: cidr, dry_run: dry_run)
        deprovision_byoip_cidr(input)
      end

      def deprovision_byoip_cidr(input : Types::DeprovisionByoipCidrRequest) : Types::DeprovisionByoipCidrResult
        request = Protocol::Ec2.build_request(Model::DEPROVISION_BYOIP_CIDR, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeprovisionByoipCidrResult, "DeprovisionByoipCidr")
      end

      # Deprovisions your Autonomous System Number (ASN) from your Amazon Web Services account. This action
      # can only be called after any BYOIP CIDR associations are removed from your Amazon Web Services
      # account with DisassociateIpamByoasn . For more information, see Tutorial: Bring your ASN to IPAM in
      # the Amazon VPC IPAM guide .

      def deprovision_ipam_byoasn(
        asn : String,
        ipam_id : String,
        dry_run : Bool? = nil
      ) : Types::DeprovisionIpamByoasnResult

        input = Types::DeprovisionIpamByoasnRequest.new(asn: asn, ipam_id: ipam_id, dry_run: dry_run)
        deprovision_ipam_byoasn(input)
      end

      def deprovision_ipam_byoasn(input : Types::DeprovisionIpamByoasnRequest) : Types::DeprovisionIpamByoasnResult
        request = Protocol::Ec2.build_request(Model::DEPROVISION_IPAM_BYOASN, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeprovisionIpamByoasnResult, "DeprovisionIpamByoasn")
      end

      # Deprovision a CIDR provisioned from an IPAM pool. If you deprovision a CIDR from a pool that has a
      # source pool, the CIDR is recycled back into the source pool. For more information, see Deprovision
      # pool CIDRs in the Amazon VPC IPAM User Guide .

      def deprovision_ipam_pool_cidr(
        ipam_pool_id : String,
        cidr : String? = nil,
        dry_run : Bool? = nil
      ) : Types::DeprovisionIpamPoolCidrResult

        input = Types::DeprovisionIpamPoolCidrRequest.new(ipam_pool_id: ipam_pool_id, cidr: cidr, dry_run: dry_run)
        deprovision_ipam_pool_cidr(input)
      end

      def deprovision_ipam_pool_cidr(input : Types::DeprovisionIpamPoolCidrRequest) : Types::DeprovisionIpamPoolCidrResult
        request = Protocol::Ec2.build_request(Model::DEPROVISION_IPAM_POOL_CIDR, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeprovisionIpamPoolCidrResult, "DeprovisionIpamPoolCidr")
      end

      # Deprovision a CIDR from a public IPv4 pool.

      def deprovision_public_ipv4_pool_cidr(
        cidr : String,
        pool_id : String,
        dry_run : Bool? = nil
      ) : Types::DeprovisionPublicIpv4PoolCidrResult

        input = Types::DeprovisionPublicIpv4PoolCidrRequest.new(cidr: cidr, pool_id: pool_id, dry_run: dry_run)
        deprovision_public_ipv4_pool_cidr(input)
      end

      def deprovision_public_ipv4_pool_cidr(input : Types::DeprovisionPublicIpv4PoolCidrRequest) : Types::DeprovisionPublicIpv4PoolCidrResult
        request = Protocol::Ec2.build_request(Model::DEPROVISION_PUBLIC_IPV4_POOL_CIDR, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeprovisionPublicIpv4PoolCidrResult, "DeprovisionPublicIpv4PoolCidr")
      end

      # Deregisters the specified AMI. A deregistered AMI can't be used to launch new instances. If a
      # deregistered EBS-backed AMI matches a Recycle Bin retention rule, it moves to the Recycle Bin for
      # the specified retention period. It can be restored before its retention period expires, after which
      # it is permanently deleted. If the deregistered AMI doesn't match a retention rule, it is permanently
      # deleted immediately. For more information, see Recover deleted Amazon EBS snapshots and EBS-backed
      # AMIs with Recycle Bin in the Amazon EBS User Guide . When deregistering an EBS-backed AMI, you can
      # optionally delete its associated snapshots at the same time. However, if a snapshot is associated
      # with multiple AMIs, it won't be deleted even if specified for deletion, although the AMI will still
      # be deregistered. Deregistering an AMI does not delete the following: Instances already launched from
      # the AMI. You'll continue to incur usage costs for the instances until you terminate them. For
      # EBS-backed AMIs: Snapshots that are associated with multiple AMIs. You'll continue to incur snapshot
      # storage costs. For instance store-backed AMIs: The files uploaded to Amazon S3 during AMI creation.
      # You'll continue to incur S3 storage costs. For more information, see Deregister an Amazon EC2 AMI in
      # the Amazon EC2 User Guide .

      def deregister_image(
        image_id : String,
        delete_associated_snapshots : Bool? = nil,
        dry_run : Bool? = nil
      ) : Types::DeregisterImageResult

        input = Types::DeregisterImageRequest.new(image_id: image_id, delete_associated_snapshots: delete_associated_snapshots, dry_run: dry_run)
        deregister_image(input)
      end

      def deregister_image(input : Types::DeregisterImageRequest) : Types::DeregisterImageResult
        request = Protocol::Ec2.build_request(Model::DEREGISTER_IMAGE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeregisterImageResult, "DeregisterImage")
      end

      # Deregisters tag keys to prevent tags that have the specified tag keys from being included in
      # scheduled event notifications for resources in the Region.

      def deregister_instance_event_notification_attributes(
        instance_tag_attribute : Types::DeregisterInstanceTagAttributeRequest,
        dry_run : Bool? = nil
      ) : Types::DeregisterInstanceEventNotificationAttributesResult

        input = Types::DeregisterInstanceEventNotificationAttributesRequest.new(instance_tag_attribute: instance_tag_attribute, dry_run: dry_run)
        deregister_instance_event_notification_attributes(input)
      end

      def deregister_instance_event_notification_attributes(input : Types::DeregisterInstanceEventNotificationAttributesRequest) : Types::DeregisterInstanceEventNotificationAttributesResult
        request = Protocol::Ec2.build_request(Model::DEREGISTER_INSTANCE_EVENT_NOTIFICATION_ATTRIBUTES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeregisterInstanceEventNotificationAttributesResult, "DeregisterInstanceEventNotificationAttributes")
      end

      # Deregisters the specified members (network interfaces) from the transit gateway multicast group.

      def deregister_transit_gateway_multicast_group_members(
        dry_run : Bool? = nil,
        group_ip_address : String? = nil,
        network_interface_ids : Array(String)? = nil,
        transit_gateway_multicast_domain_id : String? = nil
      ) : Types::DeregisterTransitGatewayMulticastGroupMembersResult

        input = Types::DeregisterTransitGatewayMulticastGroupMembersRequest.new(dry_run: dry_run, group_ip_address: group_ip_address, network_interface_ids: network_interface_ids, transit_gateway_multicast_domain_id: transit_gateway_multicast_domain_id)
        deregister_transit_gateway_multicast_group_members(input)
      end

      def deregister_transit_gateway_multicast_group_members(input : Types::DeregisterTransitGatewayMulticastGroupMembersRequest) : Types::DeregisterTransitGatewayMulticastGroupMembersResult
        request = Protocol::Ec2.build_request(Model::DEREGISTER_TRANSIT_GATEWAY_MULTICAST_GROUP_MEMBERS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeregisterTransitGatewayMulticastGroupMembersResult, "DeregisterTransitGatewayMulticastGroupMembers")
      end

      # Deregisters the specified sources (network interfaces) from the transit gateway multicast group.

      def deregister_transit_gateway_multicast_group_sources(
        dry_run : Bool? = nil,
        group_ip_address : String? = nil,
        network_interface_ids : Array(String)? = nil,
        transit_gateway_multicast_domain_id : String? = nil
      ) : Types::DeregisterTransitGatewayMulticastGroupSourcesResult

        input = Types::DeregisterTransitGatewayMulticastGroupSourcesRequest.new(dry_run: dry_run, group_ip_address: group_ip_address, network_interface_ids: network_interface_ids, transit_gateway_multicast_domain_id: transit_gateway_multicast_domain_id)
        deregister_transit_gateway_multicast_group_sources(input)
      end

      def deregister_transit_gateway_multicast_group_sources(input : Types::DeregisterTransitGatewayMulticastGroupSourcesRequest) : Types::DeregisterTransitGatewayMulticastGroupSourcesResult
        request = Protocol::Ec2.build_request(Model::DEREGISTER_TRANSIT_GATEWAY_MULTICAST_GROUP_SOURCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DeregisterTransitGatewayMulticastGroupSourcesResult, "DeregisterTransitGatewayMulticastGroupSources")
      end

      # Describes attributes of your Amazon Web Services account. The following are the supported account
      # attributes: default-vpc : The ID of the default VPC for your account, or none . max-instances : This
      # attribute is no longer supported. The returned value does not reflect your actual vCPU limit for
      # running On-Demand Instances. For more information, see On-Demand Instance Limits in the Amazon
      # Elastic Compute Cloud User Guide . max-elastic-ips : The maximum number of Elastic IP addresses that
      # you can allocate. supported-platforms : This attribute is deprecated. vpc-max-elastic-ips : The
      # maximum number of Elastic IP addresses that you can allocate. vpc-max-security-groups-per-interface
      # : The maximum number of security groups that you can assign to a network interface. The order of the
      # elements in the response, including those within nested structures, might vary. Applications should
      # not assume the elements appear in a particular order.

      def describe_account_attributes(
        attribute_names : Array(String)? = nil,
        dry_run : Bool? = nil
      ) : Types::DescribeAccountAttributesResult

        input = Types::DescribeAccountAttributesRequest.new(attribute_names: attribute_names, dry_run: dry_run)
        describe_account_attributes(input)
      end

      def describe_account_attributes(input : Types::DescribeAccountAttributesRequest) : Types::DescribeAccountAttributesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_ACCOUNT_ATTRIBUTES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeAccountAttributesResult, "DescribeAccountAttributes")
      end

      # Describes an Elastic IP address transfer. For more information, see Transfer Elastic IP addresses in
      # the Amazon VPC User Guide . When you transfer an Elastic IP address, there is a two-step handshake
      # between the source and transfer Amazon Web Services accounts. When the source account starts the
      # transfer, the transfer account has seven days to accept the Elastic IP address transfer. During
      # those seven days, the source account can view the pending transfer by using this action. After seven
      # days, the transfer expires and ownership of the Elastic IP address returns to the source account.
      # Accepted transfers are visible to the source account for 14 days after the transfers have been
      # accepted.

      def describe_address_transfers(
        allocation_ids : Array(String)? = nil,
        dry_run : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeAddressTransfersResult

        input = Types::DescribeAddressTransfersRequest.new(allocation_ids: allocation_ids, dry_run: dry_run, max_results: max_results, next_token: next_token)
        describe_address_transfers(input)
      end

      def describe_address_transfers(input : Types::DescribeAddressTransfersRequest) : Types::DescribeAddressTransfersResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_ADDRESS_TRANSFERS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeAddressTransfersResult, "DescribeAddressTransfers")
      end

      # Describes the specified Elastic IP addresses or all of your Elastic IP addresses.

      def describe_addresses(
        allocation_ids : Array(String)? = nil,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        public_ips : Array(String)? = nil
      ) : Types::DescribeAddressesResult

        input = Types::DescribeAddressesRequest.new(allocation_ids: allocation_ids, dry_run: dry_run, filters: filters, public_ips: public_ips)
        describe_addresses(input)
      end

      def describe_addresses(input : Types::DescribeAddressesRequest) : Types::DescribeAddressesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_ADDRESSES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeAddressesResult, "DescribeAddresses")
      end

      # Describes the attributes of the specified Elastic IP addresses. For requirements, see Using reverse
      # DNS for email applications .

      def describe_addresses_attribute(
        allocation_ids : Array(String)? = nil,
        attribute : String? = nil,
        dry_run : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeAddressesAttributeResult

        input = Types::DescribeAddressesAttributeRequest.new(allocation_ids: allocation_ids, attribute: attribute, dry_run: dry_run, max_results: max_results, next_token: next_token)
        describe_addresses_attribute(input)
      end

      def describe_addresses_attribute(input : Types::DescribeAddressesAttributeRequest) : Types::DescribeAddressesAttributeResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_ADDRESSES_ATTRIBUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeAddressesAttributeResult, "DescribeAddressesAttribute")
      end

      # Describes the longer ID format settings for all resource types in a specific Region. This request is
      # useful for performing a quick audit to determine whether a specific Region is fully opted in for
      # longer IDs (17-character IDs). This request only returns information about resource types that
      # support longer IDs. The following resource types support longer IDs: bundle | conversion-task |
      # customer-gateway | dhcp-options | elastic-ip-allocation | elastic-ip-association | export-task |
      # flow-log | image | import-task | instance | internet-gateway | network-acl | network-acl-association
      # | network-interface | network-interface-attachment | prefix-list | reservation | route-table |
      # route-table-association | security-group | snapshot | subnet | subnet-cidr-block-association |
      # volume | vpc | vpc-cidr-block-association | vpc-endpoint | vpc-peering-connection | vpn-connection |
      # vpn-gateway .

      def describe_aggregate_id_format(
        dry_run : Bool? = nil
      ) : Types::DescribeAggregateIdFormatResult

        input = Types::DescribeAggregateIdFormatRequest.new(dry_run: dry_run)
        describe_aggregate_id_format(input)
      end

      def describe_aggregate_id_format(input : Types::DescribeAggregateIdFormatRequest) : Types::DescribeAggregateIdFormatResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_AGGREGATE_ID_FORMAT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeAggregateIdFormatResult, "DescribeAggregateIdFormat")
      end

      # Describes the Availability Zones, Local Zones, and Wavelength Zones that are available to you. For
      # more information about Availability Zones, Local Zones, and Wavelength Zones, see Regions and zones
      # in the Amazon EC2 User Guide . The order of the elements in the response, including those within
      # nested structures, might vary. Applications should not assume the elements appear in a particular
      # order.

      def describe_availability_zones(
        all_availability_zones : Bool? = nil,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        zone_ids : Array(String)? = nil,
        zone_names : Array(String)? = nil
      ) : Types::DescribeAvailabilityZonesResult

        input = Types::DescribeAvailabilityZonesRequest.new(all_availability_zones: all_availability_zones, dry_run: dry_run, filters: filters, zone_ids: zone_ids, zone_names: zone_names)
        describe_availability_zones(input)
      end

      def describe_availability_zones(input : Types::DescribeAvailabilityZonesRequest) : Types::DescribeAvailabilityZonesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_AVAILABILITY_ZONES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeAvailabilityZonesResult, "DescribeAvailabilityZones")
      end

      # Describes the current Infrastructure Performance metric subscriptions.

      def describe_aws_network_performance_metric_subscriptions(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeAwsNetworkPerformanceMetricSubscriptionsResult

        input = Types::DescribeAwsNetworkPerformanceMetricSubscriptionsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_aws_network_performance_metric_subscriptions(input)
      end

      def describe_aws_network_performance_metric_subscriptions(input : Types::DescribeAwsNetworkPerformanceMetricSubscriptionsRequest) : Types::DescribeAwsNetworkPerformanceMetricSubscriptionsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_AWS_NETWORK_PERFORMANCE_METRIC_SUBSCRIPTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeAwsNetworkPerformanceMetricSubscriptionsResult, "DescribeAwsNetworkPerformanceMetricSubscriptions")
      end

      # Describes the specified bundle tasks or all of your bundle tasks. Completed bundle tasks are listed
      # for only a limited time. If your bundle task is no longer in the list, you can still register an AMI
      # from it. Just use RegisterImage with the Amazon S3 bucket name and image manifest name you provided
      # to the bundle task. The order of the elements in the response, including those within nested
      # structures, might vary. Applications should not assume the elements appear in a particular order.

      def describe_bundle_tasks(
        bundle_ids : Array(String)? = nil,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil
      ) : Types::DescribeBundleTasksResult

        input = Types::DescribeBundleTasksRequest.new(bundle_ids: bundle_ids, dry_run: dry_run, filters: filters)
        describe_bundle_tasks(input)
      end

      def describe_bundle_tasks(input : Types::DescribeBundleTasksRequest) : Types::DescribeBundleTasksResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_BUNDLE_TASKS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeBundleTasksResult, "DescribeBundleTasks")
      end

      # Describes the IP address ranges that were provisioned for use with Amazon Web Services resources
      # through through bring your own IP addresses (BYOIP).

      def describe_byoip_cidrs(
        max_results : Int32,
        dry_run : Bool? = nil,
        next_token : String? = nil
      ) : Types::DescribeByoipCidrsResult

        input = Types::DescribeByoipCidrsRequest.new(max_results: max_results, dry_run: dry_run, next_token: next_token)
        describe_byoip_cidrs(input)
      end

      def describe_byoip_cidrs(input : Types::DescribeByoipCidrsRequest) : Types::DescribeByoipCidrsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_BYOIP_CIDRS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeByoipCidrsResult, "DescribeByoipCidrs")
      end

      # Describes the events for the specified Capacity Block extension during the specified time.

      def describe_capacity_block_extension_history(
        capacity_reservation_ids : Array(String)? = nil,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeCapacityBlockExtensionHistoryResult

        input = Types::DescribeCapacityBlockExtensionHistoryRequest.new(capacity_reservation_ids: capacity_reservation_ids, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_capacity_block_extension_history(input)
      end

      def describe_capacity_block_extension_history(input : Types::DescribeCapacityBlockExtensionHistoryRequest) : Types::DescribeCapacityBlockExtensionHistoryResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_CAPACITY_BLOCK_EXTENSION_HISTORY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeCapacityBlockExtensionHistoryResult, "DescribeCapacityBlockExtensionHistory")
      end

      # Describes Capacity Block extension offerings available for purchase in the Amazon Web Services
      # Region that you're currently using.

      def describe_capacity_block_extension_offerings(
        capacity_block_extension_duration_hours : Int32,
        capacity_reservation_id : String,
        dry_run : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeCapacityBlockExtensionOfferingsResult

        input = Types::DescribeCapacityBlockExtensionOfferingsRequest.new(capacity_block_extension_duration_hours: capacity_block_extension_duration_hours, capacity_reservation_id: capacity_reservation_id, dry_run: dry_run, max_results: max_results, next_token: next_token)
        describe_capacity_block_extension_offerings(input)
      end

      def describe_capacity_block_extension_offerings(input : Types::DescribeCapacityBlockExtensionOfferingsRequest) : Types::DescribeCapacityBlockExtensionOfferingsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_CAPACITY_BLOCK_EXTENSION_OFFERINGS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeCapacityBlockExtensionOfferingsResult, "DescribeCapacityBlockExtensionOfferings")
      end

      # Describes Capacity Block offerings available for purchase in the Amazon Web Services Region that
      # you're currently using. With Capacity Blocks, you can purchase a specific GPU instance type or EC2
      # UltraServer for a period of time. To search for an available Capacity Block offering, you specify a
      # reservation duration and instance count.

      def describe_capacity_block_offerings(
        capacity_duration_hours : Int32,
        dry_run : Bool? = nil,
        end_date_range : Time? = nil,
        instance_count : Int32? = nil,
        instance_type : String? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        start_date_range : Time? = nil,
        ultraserver_count : Int32? = nil,
        ultraserver_type : String? = nil
      ) : Types::DescribeCapacityBlockOfferingsResult

        input = Types::DescribeCapacityBlockOfferingsRequest.new(capacity_duration_hours: capacity_duration_hours, dry_run: dry_run, end_date_range: end_date_range, instance_count: instance_count, instance_type: instance_type, max_results: max_results, next_token: next_token, start_date_range: start_date_range, ultraserver_count: ultraserver_count, ultraserver_type: ultraserver_type)
        describe_capacity_block_offerings(input)
      end

      def describe_capacity_block_offerings(input : Types::DescribeCapacityBlockOfferingsRequest) : Types::DescribeCapacityBlockOfferingsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_CAPACITY_BLOCK_OFFERINGS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeCapacityBlockOfferingsResult, "DescribeCapacityBlockOfferings")
      end

      # Describes the availability of capacity for the specified Capacity blocks, or all of your Capacity
      # Blocks.

      def describe_capacity_block_status(
        capacity_block_ids : Array(String)? = nil,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeCapacityBlockStatusResult

        input = Types::DescribeCapacityBlockStatusRequest.new(capacity_block_ids: capacity_block_ids, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_capacity_block_status(input)
      end

      def describe_capacity_block_status(input : Types::DescribeCapacityBlockStatusRequest) : Types::DescribeCapacityBlockStatusResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_CAPACITY_BLOCK_STATUS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeCapacityBlockStatusResult, "DescribeCapacityBlockStatus")
      end

      # Describes details about Capacity Blocks in the Amazon Web Services Region that you're currently
      # using.

      def describe_capacity_blocks(
        capacity_block_ids : Array(String)? = nil,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeCapacityBlocksResult

        input = Types::DescribeCapacityBlocksRequest.new(capacity_block_ids: capacity_block_ids, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_capacity_blocks(input)
      end

      def describe_capacity_blocks(input : Types::DescribeCapacityBlocksRequest) : Types::DescribeCapacityBlocksResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_CAPACITY_BLOCKS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeCapacityBlocksResult, "DescribeCapacityBlocks")
      end

      # Describes one or more Capacity Manager data export configurations. Returns information about export
      # settings, delivery status, and recent export activity.

      def describe_capacity_manager_data_exports(
        capacity_manager_data_export_ids : Array(String)? = nil,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeCapacityManagerDataExportsResult

        input = Types::DescribeCapacityManagerDataExportsRequest.new(capacity_manager_data_export_ids: capacity_manager_data_export_ids, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_capacity_manager_data_exports(input)
      end

      def describe_capacity_manager_data_exports(input : Types::DescribeCapacityManagerDataExportsRequest) : Types::DescribeCapacityManagerDataExportsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_CAPACITY_MANAGER_DATA_EXPORTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeCapacityManagerDataExportsResult, "DescribeCapacityManagerDataExports")
      end

      # Describes a request to assign the billing of the unused capacity of a Capacity Reservation. For more
      # information, see Billing assignment for shared Amazon EC2 Capacity Reservations .

      def describe_capacity_reservation_billing_requests(
        role : String,
        capacity_reservation_ids : Array(String)? = nil,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeCapacityReservationBillingRequestsResult

        input = Types::DescribeCapacityReservationBillingRequestsRequest.new(role: role, capacity_reservation_ids: capacity_reservation_ids, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_capacity_reservation_billing_requests(input)
      end

      def describe_capacity_reservation_billing_requests(input : Types::DescribeCapacityReservationBillingRequestsRequest) : Types::DescribeCapacityReservationBillingRequestsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_CAPACITY_RESERVATION_BILLING_REQUESTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeCapacityReservationBillingRequestsResult, "DescribeCapacityReservationBillingRequests")
      end

      # Describes one or more Capacity Reservation Fleets.

      def describe_capacity_reservation_fleets(
        capacity_reservation_fleet_ids : Array(String)? = nil,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeCapacityReservationFleetsResult

        input = Types::DescribeCapacityReservationFleetsRequest.new(capacity_reservation_fleet_ids: capacity_reservation_fleet_ids, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_capacity_reservation_fleets(input)
      end

      def describe_capacity_reservation_fleets(input : Types::DescribeCapacityReservationFleetsRequest) : Types::DescribeCapacityReservationFleetsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_CAPACITY_RESERVATION_FLEETS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeCapacityReservationFleetsResult, "DescribeCapacityReservationFleets")
      end

      # Describes a tree-based hierarchy that represents the physical host placement of your pending or
      # active Capacity Reservations within an Availability Zone or Local Zone. You can use this information
      # to determine the relative proximity of your capacity within the Amazon Web Services network before
      # it is launched and use this information to allocate capacity together to support your tightly
      # coupled workloads. Capacity Reservation topology is supported for specific instance types only. For
      # more information, see Prerequisites for Amazon EC2 instance topology in the Amazon EC2 User Guide .
      # The Amazon EC2 API follows an eventual consistency model due to the distributed nature of the system
      # supporting it. As a result, when you call the DescribeCapacityReservationTopology API command
      # immediately after launching instances, the response might return a null value for capacityBlockId
      # because the data might not have fully propagated across all subsystems. For more information, see
      # Eventual consistency in the Amazon EC2 API in the Amazon EC2 Developer Guide . For more information,
      # see Amazon EC2 topology in the Amazon EC2 User Guide .

      def describe_capacity_reservation_topology(
        capacity_reservation_ids : Array(String)? = nil,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeCapacityReservationTopologyResult

        input = Types::DescribeCapacityReservationTopologyRequest.new(capacity_reservation_ids: capacity_reservation_ids, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_capacity_reservation_topology(input)
      end

      def describe_capacity_reservation_topology(input : Types::DescribeCapacityReservationTopologyRequest) : Types::DescribeCapacityReservationTopologyResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_CAPACITY_RESERVATION_TOPOLOGY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeCapacityReservationTopologyResult, "DescribeCapacityReservationTopology")
      end

      # Describes one or more of your Capacity Reservations. The results describe only the Capacity
      # Reservations in the Amazon Web Services Region that you're currently using.

      def describe_capacity_reservations(
        capacity_reservation_ids : Array(String)? = nil,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeCapacityReservationsResult

        input = Types::DescribeCapacityReservationsRequest.new(capacity_reservation_ids: capacity_reservation_ids, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_capacity_reservations(input)
      end

      def describe_capacity_reservations(input : Types::DescribeCapacityReservationsRequest) : Types::DescribeCapacityReservationsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_CAPACITY_RESERVATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeCapacityReservationsResult, "DescribeCapacityReservations")
      end

      # Describes one or more of your carrier gateways.

      def describe_carrier_gateways(
        carrier_gateway_ids : Array(String)? = nil,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeCarrierGatewaysResult

        input = Types::DescribeCarrierGatewaysRequest.new(carrier_gateway_ids: carrier_gateway_ids, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_carrier_gateways(input)
      end

      def describe_carrier_gateways(input : Types::DescribeCarrierGatewaysRequest) : Types::DescribeCarrierGatewaysResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_CARRIER_GATEWAYS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeCarrierGatewaysResult, "DescribeCarrierGateways")
      end

      # This action is deprecated. Describes your linked EC2-Classic instances. This request only returns
      # information about EC2-Classic instances linked to a VPC through ClassicLink. You cannot use this
      # request to return information about other instances.

      def describe_classic_link_instances(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        instance_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeClassicLinkInstancesResult

        input = Types::DescribeClassicLinkInstancesRequest.new(dry_run: dry_run, filters: filters, instance_ids: instance_ids, max_results: max_results, next_token: next_token)
        describe_classic_link_instances(input)
      end

      def describe_classic_link_instances(input : Types::DescribeClassicLinkInstancesRequest) : Types::DescribeClassicLinkInstancesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_CLASSIC_LINK_INSTANCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeClassicLinkInstancesResult, "DescribeClassicLinkInstances")
      end

      # Describes the authorization rules for a specified Client VPN endpoint.

      def describe_client_vpn_authorization_rules(
        client_vpn_endpoint_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeClientVpnAuthorizationRulesResult

        input = Types::DescribeClientVpnAuthorizationRulesRequest.new(client_vpn_endpoint_id: client_vpn_endpoint_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_client_vpn_authorization_rules(input)
      end

      def describe_client_vpn_authorization_rules(input : Types::DescribeClientVpnAuthorizationRulesRequest) : Types::DescribeClientVpnAuthorizationRulesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_CLIENT_VPN_AUTHORIZATION_RULES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeClientVpnAuthorizationRulesResult, "DescribeClientVpnAuthorizationRules")
      end

      # Describes active client connections and connections that have been terminated within the last 60
      # minutes for the specified Client VPN endpoint.

      def describe_client_vpn_connections(
        client_vpn_endpoint_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeClientVpnConnectionsResult

        input = Types::DescribeClientVpnConnectionsRequest.new(client_vpn_endpoint_id: client_vpn_endpoint_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_client_vpn_connections(input)
      end

      def describe_client_vpn_connections(input : Types::DescribeClientVpnConnectionsRequest) : Types::DescribeClientVpnConnectionsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_CLIENT_VPN_CONNECTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeClientVpnConnectionsResult, "DescribeClientVpnConnections")
      end

      # Describes one or more Client VPN endpoints in the account.

      def describe_client_vpn_endpoints(
        client_vpn_endpoint_ids : Array(String)? = nil,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeClientVpnEndpointsResult

        input = Types::DescribeClientVpnEndpointsRequest.new(client_vpn_endpoint_ids: client_vpn_endpoint_ids, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_client_vpn_endpoints(input)
      end

      def describe_client_vpn_endpoints(input : Types::DescribeClientVpnEndpointsRequest) : Types::DescribeClientVpnEndpointsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_CLIENT_VPN_ENDPOINTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeClientVpnEndpointsResult, "DescribeClientVpnEndpoints")
      end

      # Describes the routes for the specified Client VPN endpoint.

      def describe_client_vpn_routes(
        client_vpn_endpoint_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeClientVpnRoutesResult

        input = Types::DescribeClientVpnRoutesRequest.new(client_vpn_endpoint_id: client_vpn_endpoint_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_client_vpn_routes(input)
      end

      def describe_client_vpn_routes(input : Types::DescribeClientVpnRoutesRequest) : Types::DescribeClientVpnRoutesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_CLIENT_VPN_ROUTES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeClientVpnRoutesResult, "DescribeClientVpnRoutes")
      end

      # Describes the target networks associated with the specified Client VPN endpoint.

      def describe_client_vpn_target_networks(
        client_vpn_endpoint_id : String,
        association_ids : Array(String)? = nil,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeClientVpnTargetNetworksResult

        input = Types::DescribeClientVpnTargetNetworksRequest.new(client_vpn_endpoint_id: client_vpn_endpoint_id, association_ids: association_ids, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_client_vpn_target_networks(input)
      end

      def describe_client_vpn_target_networks(input : Types::DescribeClientVpnTargetNetworksRequest) : Types::DescribeClientVpnTargetNetworksResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_CLIENT_VPN_TARGET_NETWORKS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeClientVpnTargetNetworksResult, "DescribeClientVpnTargetNetworks")
      end

      # Describes the specified customer-owned address pools or all of your customer-owned address pools.

      def describe_coip_pools(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        pool_ids : Array(String)? = nil
      ) : Types::DescribeCoipPoolsResult

        input = Types::DescribeCoipPoolsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, pool_ids: pool_ids)
        describe_coip_pools(input)
      end

      def describe_coip_pools(input : Types::DescribeCoipPoolsRequest) : Types::DescribeCoipPoolsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_COIP_POOLS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeCoipPoolsResult, "DescribeCoipPools")
      end

      # Describes the specified conversion tasks or all your conversion tasks. For more information, see the
      # VM Import/Export User Guide . For information about the import manifest referenced by this API
      # action, see VM Import Manifest .

      def describe_conversion_tasks(
        conversion_task_ids : Array(String)? = nil,
        dry_run : Bool? = nil
      ) : Types::DescribeConversionTasksResult

        input = Types::DescribeConversionTasksRequest.new(conversion_task_ids: conversion_task_ids, dry_run: dry_run)
        describe_conversion_tasks(input)
      end

      def describe_conversion_tasks(input : Types::DescribeConversionTasksRequest) : Types::DescribeConversionTasksResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_CONVERSION_TASKS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeConversionTasksResult, "DescribeConversionTasks")
      end

      # Describes one or more of your VPN customer gateways. For more information, see Amazon Web Services
      # Site-to-Site VPN in the Amazon Web Services Site-to-Site VPN User Guide .

      def describe_customer_gateways(
        customer_gateway_ids : Array(String)? = nil,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil
      ) : Types::DescribeCustomerGatewaysResult

        input = Types::DescribeCustomerGatewaysRequest.new(customer_gateway_ids: customer_gateway_ids, dry_run: dry_run, filters: filters)
        describe_customer_gateways(input)
      end

      def describe_customer_gateways(input : Types::DescribeCustomerGatewaysRequest) : Types::DescribeCustomerGatewaysResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_CUSTOMER_GATEWAYS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeCustomerGatewaysResult, "DescribeCustomerGateways")
      end

      # Describes the metadata of an account status report, including the status of the report. To view the
      # full report, download it from the Amazon S3 bucket where it was saved. Reports are accessible only
      # when they have the complete status. Reports with other statuses ( running , cancelled , or error )
      # are not available in the S3 bucket. For more information about downloading objects from an S3
      # bucket, see Downloading objects in the Amazon Simple Storage Service User Guide . For more
      # information, see Generating the account status report for declarative policies in the Amazon Web
      # Services Organizations User Guide .

      def describe_declarative_policies_reports(
        dry_run : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        report_ids : Array(String)? = nil
      ) : Types::DescribeDeclarativePoliciesReportsResult

        input = Types::DescribeDeclarativePoliciesReportsRequest.new(dry_run: dry_run, max_results: max_results, next_token: next_token, report_ids: report_ids)
        describe_declarative_policies_reports(input)
      end

      def describe_declarative_policies_reports(input : Types::DescribeDeclarativePoliciesReportsRequest) : Types::DescribeDeclarativePoliciesReportsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_DECLARATIVE_POLICIES_REPORTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeDeclarativePoliciesReportsResult, "DescribeDeclarativePoliciesReports")
      end

      # Describes your DHCP option sets. The default is to describe all your DHCP option sets.
      # Alternatively, you can specify specific DHCP option set IDs or filter the results to include only
      # the DHCP option sets that match specific criteria. For more information, see DHCP option sets in the
      # Amazon VPC User Guide .

      def describe_dhcp_options(
        dhcp_options_ids : Array(String)? = nil,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeDhcpOptionsResult

        input = Types::DescribeDhcpOptionsRequest.new(dhcp_options_ids: dhcp_options_ids, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_dhcp_options(input)
      end

      def describe_dhcp_options(input : Types::DescribeDhcpOptionsRequest) : Types::DescribeDhcpOptionsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_DHCP_OPTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeDhcpOptionsResult, "DescribeDhcpOptions")
      end

      # Describes your egress-only internet gateways. The default is to describe all your egress-only
      # internet gateways. Alternatively, you can specify specific egress-only internet gateway IDs or
      # filter the results to include only the egress-only internet gateways that match specific criteria.

      def describe_egress_only_internet_gateways(
        dry_run : Bool? = nil,
        egress_only_internet_gateway_ids : Array(String)? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeEgressOnlyInternetGatewaysResult

        input = Types::DescribeEgressOnlyInternetGatewaysRequest.new(dry_run: dry_run, egress_only_internet_gateway_ids: egress_only_internet_gateway_ids, filters: filters, max_results: max_results, next_token: next_token)
        describe_egress_only_internet_gateways(input)
      end

      def describe_egress_only_internet_gateways(input : Types::DescribeEgressOnlyInternetGatewaysRequest) : Types::DescribeEgressOnlyInternetGatewaysResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_EGRESS_ONLY_INTERNET_GATEWAYS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeEgressOnlyInternetGatewaysResult, "DescribeEgressOnlyInternetGateways")
      end

      # Amazon Elastic Graphics reached end of life on January 8, 2024. Describes the Elastic Graphics
      # accelerator associated with your instances.

      def describe_elastic_gpus(
        dry_run : Bool? = nil,
        elastic_gpu_ids : Array(String)? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeElasticGpusResult

        input = Types::DescribeElasticGpusRequest.new(dry_run: dry_run, elastic_gpu_ids: elastic_gpu_ids, filters: filters, max_results: max_results, next_token: next_token)
        describe_elastic_gpus(input)
      end

      def describe_elastic_gpus(input : Types::DescribeElasticGpusRequest) : Types::DescribeElasticGpusResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_ELASTIC_GPUS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeElasticGpusResult, "DescribeElasticGpus")
      end

      # Describes the specified export image tasks or all of your export image tasks.

      def describe_export_image_tasks(
        dry_run : Bool? = nil,
        export_image_task_ids : Array(String)? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeExportImageTasksResult

        input = Types::DescribeExportImageTasksRequest.new(dry_run: dry_run, export_image_task_ids: export_image_task_ids, filters: filters, max_results: max_results, next_token: next_token)
        describe_export_image_tasks(input)
      end

      def describe_export_image_tasks(input : Types::DescribeExportImageTasksRequest) : Types::DescribeExportImageTasksResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_EXPORT_IMAGE_TASKS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeExportImageTasksResult, "DescribeExportImageTasks")
      end

      # Describes the specified export instance tasks or all of your export instance tasks.

      def describe_export_tasks(
        export_task_ids : Array(String)? = nil,
        filters : Array(Types::Filter)? = nil
      ) : Types::DescribeExportTasksResult

        input = Types::DescribeExportTasksRequest.new(export_task_ids: export_task_ids, filters: filters)
        describe_export_tasks(input)
      end

      def describe_export_tasks(input : Types::DescribeExportTasksRequest) : Types::DescribeExportTasksResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_EXPORT_TASKS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeExportTasksResult, "DescribeExportTasks")
      end

      # Describe details for Windows AMIs that are configured for Windows fast launch.

      def describe_fast_launch_images(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        image_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeFastLaunchImagesResult

        input = Types::DescribeFastLaunchImagesRequest.new(dry_run: dry_run, filters: filters, image_ids: image_ids, max_results: max_results, next_token: next_token)
        describe_fast_launch_images(input)
      end

      def describe_fast_launch_images(input : Types::DescribeFastLaunchImagesRequest) : Types::DescribeFastLaunchImagesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_FAST_LAUNCH_IMAGES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeFastLaunchImagesResult, "DescribeFastLaunchImages")
      end

      # Describes the state of fast snapshot restores for your snapshots.

      def describe_fast_snapshot_restores(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeFastSnapshotRestoresResult

        input = Types::DescribeFastSnapshotRestoresRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_fast_snapshot_restores(input)
      end

      def describe_fast_snapshot_restores(input : Types::DescribeFastSnapshotRestoresRequest) : Types::DescribeFastSnapshotRestoresResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_FAST_SNAPSHOT_RESTORES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeFastSnapshotRestoresResult, "DescribeFastSnapshotRestores")
      end

      # Describes the events for the specified EC2 Fleet during the specified time. EC2 Fleet events are
      # delayed by up to 30 seconds before they can be described. This ensures that you can query by the
      # last evaluated time and not miss a recorded event. EC2 Fleet events are available for 48 hours. For
      # more information, see Monitor fleet events using Amazon EventBridge in the Amazon EC2 User Guide .

      def describe_fleet_history(
        fleet_id : String,
        start_time : Time,
        dry_run : Bool? = nil,
        event_type : String? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeFleetHistoryResult

        input = Types::DescribeFleetHistoryRequest.new(fleet_id: fleet_id, start_time: start_time, dry_run: dry_run, event_type: event_type, max_results: max_results, next_token: next_token)
        describe_fleet_history(input)
      end

      def describe_fleet_history(input : Types::DescribeFleetHistoryRequest) : Types::DescribeFleetHistoryResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_FLEET_HISTORY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeFleetHistoryResult, "DescribeFleetHistory")
      end

      # Describes the running instances for the specified EC2 Fleet. Currently, DescribeFleetInstances does
      # not support fleets of type instant . Instead, use DescribeFleets , specifying the instant fleet ID
      # in the request. For more information, see Describe your EC2 Fleet in the Amazon EC2 User Guide .

      def describe_fleet_instances(
        fleet_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeFleetInstancesResult

        input = Types::DescribeFleetInstancesRequest.new(fleet_id: fleet_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_fleet_instances(input)
      end

      def describe_fleet_instances(input : Types::DescribeFleetInstancesRequest) : Types::DescribeFleetInstancesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_FLEET_INSTANCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeFleetInstancesResult, "DescribeFleetInstances")
      end

      # Describes the specified EC2 Fleet or all of your EC2 Fleets. If a fleet is of type instant , you
      # must specify the fleet ID in the request, otherwise the fleet does not appear in the response. For
      # more information, see Describe your EC2 Fleet in the Amazon EC2 User Guide .

      def describe_fleets(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        fleet_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeFleetsResult

        input = Types::DescribeFleetsRequest.new(dry_run: dry_run, filters: filters, fleet_ids: fleet_ids, max_results: max_results, next_token: next_token)
        describe_fleets(input)
      end

      def describe_fleets(input : Types::DescribeFleetsRequest) : Types::DescribeFleetsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_FLEETS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeFleetsResult, "DescribeFleets")
      end

      # Describes one or more flow logs. To view the published flow log records, you must view the log
      # destination. For example, the CloudWatch Logs log group, the Amazon S3 bucket, or the Kinesis Data
      # Firehose delivery stream.

      def describe_flow_logs(
        dry_run : Bool? = nil,
        filter : Array(Types::Filter)? = nil,
        flow_log_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeFlowLogsResult

        input = Types::DescribeFlowLogsRequest.new(dry_run: dry_run, filter: filter, flow_log_ids: flow_log_ids, max_results: max_results, next_token: next_token)
        describe_flow_logs(input)
      end

      def describe_flow_logs(input : Types::DescribeFlowLogsRequest) : Types::DescribeFlowLogsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_FLOW_LOGS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeFlowLogsResult, "DescribeFlowLogs")
      end

      # Describes the specified attribute of the specified Amazon FPGA Image (AFI).

      def describe_fpga_image_attribute(
        attribute : String,
        fpga_image_id : String,
        dry_run : Bool? = nil
      ) : Types::DescribeFpgaImageAttributeResult

        input = Types::DescribeFpgaImageAttributeRequest.new(attribute: attribute, fpga_image_id: fpga_image_id, dry_run: dry_run)
        describe_fpga_image_attribute(input)
      end

      def describe_fpga_image_attribute(input : Types::DescribeFpgaImageAttributeRequest) : Types::DescribeFpgaImageAttributeResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_FPGA_IMAGE_ATTRIBUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeFpgaImageAttributeResult, "DescribeFpgaImageAttribute")
      end

      # Describes the Amazon FPGA Images (AFIs) available to you. These include public AFIs, private AFIs
      # that you own, and AFIs owned by other Amazon Web Services accounts for which you have load
      # permissions.

      def describe_fpga_images(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        fpga_image_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        owners : Array(String)? = nil
      ) : Types::DescribeFpgaImagesResult

        input = Types::DescribeFpgaImagesRequest.new(dry_run: dry_run, filters: filters, fpga_image_ids: fpga_image_ids, max_results: max_results, next_token: next_token, owners: owners)
        describe_fpga_images(input)
      end

      def describe_fpga_images(input : Types::DescribeFpgaImagesRequest) : Types::DescribeFpgaImagesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_FPGA_IMAGES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeFpgaImagesResult, "DescribeFpgaImages")
      end

      # Describes the Dedicated Host reservations that are available to purchase. The results describe all
      # of the Dedicated Host reservation offerings, including offerings that might not match the instance
      # family and Region of your Dedicated Hosts. When purchasing an offering, ensure that the instance
      # family and Region of the offering matches that of the Dedicated Hosts with which it is to be
      # associated. For more information about supported instance types, see Dedicated Hosts in the Amazon
      # EC2 User Guide .

      def describe_host_reservation_offerings(
        filter : Array(Types::Filter)? = nil,
        max_duration : Int32? = nil,
        max_results : Int32? = nil,
        min_duration : Int32? = nil,
        next_token : String? = nil,
        offering_id : String? = nil
      ) : Types::DescribeHostReservationOfferingsResult

        input = Types::DescribeHostReservationOfferingsRequest.new(filter: filter, max_duration: max_duration, max_results: max_results, min_duration: min_duration, next_token: next_token, offering_id: offering_id)
        describe_host_reservation_offerings(input)
      end

      def describe_host_reservation_offerings(input : Types::DescribeHostReservationOfferingsRequest) : Types::DescribeHostReservationOfferingsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_HOST_RESERVATION_OFFERINGS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeHostReservationOfferingsResult, "DescribeHostReservationOfferings")
      end

      # Describes reservations that are associated with Dedicated Hosts in your account.

      def describe_host_reservations(
        filter : Array(Types::Filter)? = nil,
        host_reservation_id_set : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeHostReservationsResult

        input = Types::DescribeHostReservationsRequest.new(filter: filter, host_reservation_id_set: host_reservation_id_set, max_results: max_results, next_token: next_token)
        describe_host_reservations(input)
      end

      def describe_host_reservations(input : Types::DescribeHostReservationsRequest) : Types::DescribeHostReservationsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_HOST_RESERVATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeHostReservationsResult, "DescribeHostReservations")
      end

      # Describes the specified Dedicated Hosts or all your Dedicated Hosts. The results describe only the
      # Dedicated Hosts in the Region you're currently using. All listed instances consume capacity on your
      # Dedicated Host. Dedicated Hosts that have recently been released are listed with the state released
      # .

      def describe_hosts(
        filter : Array(Types::Filter)? = nil,
        host_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeHostsResult

        input = Types::DescribeHostsRequest.new(filter: filter, host_ids: host_ids, max_results: max_results, next_token: next_token)
        describe_hosts(input)
      end

      def describe_hosts(input : Types::DescribeHostsRequest) : Types::DescribeHostsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_HOSTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeHostsResult, "DescribeHosts")
      end

      # Describes your IAM instance profile associations.

      def describe_iam_instance_profile_associations(
        association_ids : Array(String)? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeIamInstanceProfileAssociationsResult

        input = Types::DescribeIamInstanceProfileAssociationsRequest.new(association_ids: association_ids, filters: filters, max_results: max_results, next_token: next_token)
        describe_iam_instance_profile_associations(input)
      end

      def describe_iam_instance_profile_associations(input : Types::DescribeIamInstanceProfileAssociationsRequest) : Types::DescribeIamInstanceProfileAssociationsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_IAM_INSTANCE_PROFILE_ASSOCIATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeIamInstanceProfileAssociationsResult, "DescribeIamInstanceProfileAssociations")
      end

      # Describes the ID format settings for your resources on a per-Region basis, for example, to view
      # which resource types are enabled for longer IDs. This request only returns information about
      # resource types whose ID formats can be modified; it does not return information about other resource
      # types. The following resource types support longer IDs: bundle | conversion-task | customer-gateway
      # | dhcp-options | elastic-ip-allocation | elastic-ip-association | export-task | flow-log | image |
      # import-task | instance | internet-gateway | network-acl | network-acl-association |
      # network-interface | network-interface-attachment | prefix-list | reservation | route-table |
      # route-table-association | security-group | snapshot | subnet | subnet-cidr-block-association |
      # volume | vpc | vpc-cidr-block-association | vpc-endpoint | vpc-peering-connection | vpn-connection |
      # vpn-gateway . These settings apply to the IAM user who makes the request; they do not apply to the
      # entire Amazon Web Services account. By default, an IAM user defaults to the same settings as the
      # root user, unless they explicitly override the settings by running the ModifyIdFormat command.
      # Resources created with longer IDs are visible to all IAM users, regardless of these settings and
      # provided that they have permission to use the relevant Describe command for the resource type.

      def describe_id_format(
        resource : String? = nil
      ) : Types::DescribeIdFormatResult

        input = Types::DescribeIdFormatRequest.new(resource: resource)
        describe_id_format(input)
      end

      def describe_id_format(input : Types::DescribeIdFormatRequest) : Types::DescribeIdFormatResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_ID_FORMAT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeIdFormatResult, "DescribeIdFormat")
      end

      # Describes the ID format settings for resources for the specified IAM user, IAM role, or root user.
      # For example, you can view the resource types that are enabled for longer IDs. This request only
      # returns information about resource types whose ID formats can be modified; it does not return
      # information about other resource types. For more information, see Resource IDs in the Amazon Elastic
      # Compute Cloud User Guide . The following resource types support longer IDs: bundle | conversion-task
      # | customer-gateway | dhcp-options | elastic-ip-allocation | elastic-ip-association | export-task |
      # flow-log | image | import-task | instance | internet-gateway | network-acl | network-acl-association
      # | network-interface | network-interface-attachment | prefix-list | reservation | route-table |
      # route-table-association | security-group | snapshot | subnet | subnet-cidr-block-association |
      # volume | vpc | vpc-cidr-block-association | vpc-endpoint | vpc-peering-connection | vpn-connection |
      # vpn-gateway . These settings apply to the principal specified in the request. They do not apply to
      # the principal that makes the request.

      def describe_identity_id_format(
        principal_arn : String,
        resource : String? = nil
      ) : Types::DescribeIdentityIdFormatResult

        input = Types::DescribeIdentityIdFormatRequest.new(principal_arn: principal_arn, resource: resource)
        describe_identity_id_format(input)
      end

      def describe_identity_id_format(input : Types::DescribeIdentityIdFormatRequest) : Types::DescribeIdentityIdFormatResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_IDENTITY_ID_FORMAT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeIdentityIdFormatResult, "DescribeIdentityIdFormat")
      end

      # Describes the specified attribute of the specified AMI. You can specify only one attribute at a
      # time. The order of the elements in the response, including those within nested structures, might
      # vary. Applications should not assume the elements appear in a particular order.

      def describe_image_attribute(
        attribute : String,
        image_id : String,
        dry_run : Bool? = nil
      ) : Types::ImageAttribute

        input = Types::DescribeImageAttributeRequest.new(attribute: attribute, image_id: image_id, dry_run: dry_run)
        describe_image_attribute(input)
      end

      def describe_image_attribute(input : Types::DescribeImageAttributeRequest) : Types::ImageAttribute
        request = Protocol::Ec2.build_request(Model::DESCRIBE_IMAGE_ATTRIBUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ImageAttribute, "DescribeImageAttribute")
      end

      # Describes your Amazon Web Services resources that are referencing the specified images. For more
      # information, see Identify your resources referencing specified AMIs in the Amazon EC2 User Guide .

      def describe_image_references(
        image_ids : Array(String),
        dry_run : Bool? = nil,
        include_all_resource_types : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        resource_types : Array(Types::ResourceTypeRequest)? = nil
      ) : Types::DescribeImageReferencesResult

        input = Types::DescribeImageReferencesRequest.new(image_ids: image_ids, dry_run: dry_run, include_all_resource_types: include_all_resource_types, max_results: max_results, next_token: next_token, resource_types: resource_types)
        describe_image_references(input)
      end

      def describe_image_references(input : Types::DescribeImageReferencesRequest) : Types::DescribeImageReferencesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_IMAGE_REFERENCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeImageReferencesResult, "DescribeImageReferences")
      end

      # Describes the entries in image usage reports, showing how your images are used across other Amazon
      # Web Services accounts. For more information, see View your AMI usage in the Amazon EC2 User Guide .

      def describe_image_usage_report_entries(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        image_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        report_ids : Array(String)? = nil
      ) : Types::DescribeImageUsageReportEntriesResult

        input = Types::DescribeImageUsageReportEntriesRequest.new(dry_run: dry_run, filters: filters, image_ids: image_ids, max_results: max_results, next_token: next_token, report_ids: report_ids)
        describe_image_usage_report_entries(input)
      end

      def describe_image_usage_report_entries(input : Types::DescribeImageUsageReportEntriesRequest) : Types::DescribeImageUsageReportEntriesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_IMAGE_USAGE_REPORT_ENTRIES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeImageUsageReportEntriesResult, "DescribeImageUsageReportEntries")
      end

      # Describes the configuration and status of image usage reports, filtered by report IDs or image IDs.
      # For more information, see View your AMI usage in the Amazon EC2 User Guide .

      def describe_image_usage_reports(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        image_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        report_ids : Array(String)? = nil
      ) : Types::DescribeImageUsageReportsResult

        input = Types::DescribeImageUsageReportsRequest.new(dry_run: dry_run, filters: filters, image_ids: image_ids, max_results: max_results, next_token: next_token, report_ids: report_ids)
        describe_image_usage_reports(input)
      end

      def describe_image_usage_reports(input : Types::DescribeImageUsageReportsRequest) : Types::DescribeImageUsageReportsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_IMAGE_USAGE_REPORTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeImageUsageReportsResult, "DescribeImageUsageReports")
      end

      # Describes the specified images (AMIs, AKIs, and ARIs) available to you or all of the images
      # available to you. The images available to you include public images, private images that you own,
      # and private images owned by other Amazon Web Services accounts for which you have explicit launch
      # permissions. Recently deregistered images appear in the returned results for a short interval and
      # then return empty results. After all instances that reference a deregistered AMI are terminated,
      # specifying the ID of the image will eventually return an error indicating that the AMI ID cannot be
      # found. When Allowed AMIs is set to enabled , only allowed images are returned in the results, with
      # the imageAllowed field set to true for each image. In audit-mode , the imageAllowed field is set to
      # true for images that meet the account's Allowed AMIs criteria, and false for images that don't meet
      # the criteria. For more information, see Allowed AMIs . The Amazon EC2 API follows an eventual
      # consistency model. This means that the result of an API command you run that creates or modifies
      # resources might not be immediately available to all subsequent commands you run. For guidance on how
      # to manage eventual consistency, see Eventual consistency in the Amazon EC2 API in the Amazon EC2
      # Developer Guide . We strongly recommend using only paginated requests. Unpaginated requests are
      # susceptible to throttling and timeouts. The order of the elements in the response, including those
      # within nested structures, might vary. Applications should not assume the elements appear in a
      # particular order.

      def describe_images(
        dry_run : Bool? = nil,
        executable_users : Array(String)? = nil,
        filters : Array(Types::Filter)? = nil,
        image_ids : Array(String)? = nil,
        include_deprecated : Bool? = nil,
        include_disabled : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        owners : Array(String)? = nil
      ) : Types::DescribeImagesResult

        input = Types::DescribeImagesRequest.new(dry_run: dry_run, executable_users: executable_users, filters: filters, image_ids: image_ids, include_deprecated: include_deprecated, include_disabled: include_disabled, max_results: max_results, next_token: next_token, owners: owners)
        describe_images(input)
      end

      def describe_images(input : Types::DescribeImagesRequest) : Types::DescribeImagesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_IMAGES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeImagesResult, "DescribeImages")
      end

      # Displays details about an import virtual machine or import snapshot tasks that are already created.

      def describe_import_image_tasks(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        import_task_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeImportImageTasksResult

        input = Types::DescribeImportImageTasksRequest.new(dry_run: dry_run, filters: filters, import_task_ids: import_task_ids, max_results: max_results, next_token: next_token)
        describe_import_image_tasks(input)
      end

      def describe_import_image_tasks(input : Types::DescribeImportImageTasksRequest) : Types::DescribeImportImageTasksResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_IMPORT_IMAGE_TASKS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeImportImageTasksResult, "DescribeImportImageTasks")
      end

      # Describes your import snapshot tasks.

      def describe_import_snapshot_tasks(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        import_task_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeImportSnapshotTasksResult

        input = Types::DescribeImportSnapshotTasksRequest.new(dry_run: dry_run, filters: filters, import_task_ids: import_task_ids, max_results: max_results, next_token: next_token)
        describe_import_snapshot_tasks(input)
      end

      def describe_import_snapshot_tasks(input : Types::DescribeImportSnapshotTasksRequest) : Types::DescribeImportSnapshotTasksResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_IMPORT_SNAPSHOT_TASKS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeImportSnapshotTasksResult, "DescribeImportSnapshotTasks")
      end

      # Describes the specified attribute of the specified instance. You can specify only one attribute at a
      # time. Available attributes include SQL license exemption configuration for instances registered with
      # the SQL LE service.

      def describe_instance_attribute(
        attribute : String,
        instance_id : String,
        dry_run : Bool? = nil
      ) : Types::InstanceAttribute

        input = Types::DescribeInstanceAttributeRequest.new(attribute: attribute, instance_id: instance_id, dry_run: dry_run)
        describe_instance_attribute(input)
      end

      def describe_instance_attribute(input : Types::DescribeInstanceAttributeRequest) : Types::InstanceAttribute
        request = Protocol::Ec2.build_request(Model::DESCRIBE_INSTANCE_ATTRIBUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::InstanceAttribute, "DescribeInstanceAttribute")
      end

      # Describes the specified EC2 Instance Connect Endpoints or all EC2 Instance Connect Endpoints.

      def describe_instance_connect_endpoints(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        instance_connect_endpoint_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeInstanceConnectEndpointsResult

        input = Types::DescribeInstanceConnectEndpointsRequest.new(dry_run: dry_run, filters: filters, instance_connect_endpoint_ids: instance_connect_endpoint_ids, max_results: max_results, next_token: next_token)
        describe_instance_connect_endpoints(input)
      end

      def describe_instance_connect_endpoints(input : Types::DescribeInstanceConnectEndpointsRequest) : Types::DescribeInstanceConnectEndpointsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_INSTANCE_CONNECT_ENDPOINTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeInstanceConnectEndpointsResult, "DescribeInstanceConnectEndpoints")
      end

      # Describes the credit option for CPU usage of the specified burstable performance instances. The
      # credit options are standard and unlimited . If you do not specify an instance ID, Amazon EC2 returns
      # burstable performance instances with the unlimited credit option, as well as instances that were
      # previously configured as T2, T3, and T3a with the unlimited credit option. For example, if you
      # resize a T2 instance, while it is configured as unlimited , to an M4 instance, Amazon EC2 returns
      # the M4 instance. If you specify one or more instance IDs, Amazon EC2 returns the credit option (
      # standard or unlimited ) of those instances. If you specify an instance ID that is not valid, such as
      # an instance that is not a burstable performance instance, an error is returned. Recently terminated
      # instances might appear in the returned results. This interval is usually less than one hour. If an
      # Availability Zone is experiencing a service disruption and you specify instance IDs in the affected
      # zone, or do not specify any instance IDs at all, the call fails. If you specify only instance IDs in
      # an unaffected zone, the call works normally. For more information, see Burstable performance
      # instances in the Amazon EC2 User Guide .

      def describe_instance_credit_specifications(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        instance_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeInstanceCreditSpecificationsResult

        input = Types::DescribeInstanceCreditSpecificationsRequest.new(dry_run: dry_run, filters: filters, instance_ids: instance_ids, max_results: max_results, next_token: next_token)
        describe_instance_credit_specifications(input)
      end

      def describe_instance_credit_specifications(input : Types::DescribeInstanceCreditSpecificationsRequest) : Types::DescribeInstanceCreditSpecificationsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_INSTANCE_CREDIT_SPECIFICATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeInstanceCreditSpecificationsResult, "DescribeInstanceCreditSpecifications")
      end

      # Describes the tag keys that are registered to appear in scheduled event notifications for resources
      # in the current Region.

      def describe_instance_event_notification_attributes(
        dry_run : Bool? = nil
      ) : Types::DescribeInstanceEventNotificationAttributesResult

        input = Types::DescribeInstanceEventNotificationAttributesRequest.new(dry_run: dry_run)
        describe_instance_event_notification_attributes(input)
      end

      def describe_instance_event_notification_attributes(input : Types::DescribeInstanceEventNotificationAttributesRequest) : Types::DescribeInstanceEventNotificationAttributesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_INSTANCE_EVENT_NOTIFICATION_ATTRIBUTES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeInstanceEventNotificationAttributesResult, "DescribeInstanceEventNotificationAttributes")
      end

      # Describes the specified event windows or all event windows. If you specify event window IDs, the
      # output includes information for only the specified event windows. If you specify filters, the output
      # includes information for only those event windows that meet the filter criteria. If you do not
      # specify event windows IDs or filters, the output includes information for all event windows, which
      # can affect performance. We recommend that you use pagination to ensure that the operation returns
      # quickly and successfully. For more information, see Define event windows for scheduled events in the
      # Amazon EC2 User Guide .

      def describe_instance_event_windows(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        instance_event_window_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeInstanceEventWindowsResult

        input = Types::DescribeInstanceEventWindowsRequest.new(dry_run: dry_run, filters: filters, instance_event_window_ids: instance_event_window_ids, max_results: max_results, next_token: next_token)
        describe_instance_event_windows(input)
      end

      def describe_instance_event_windows(input : Types::DescribeInstanceEventWindowsRequest) : Types::DescribeInstanceEventWindowsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_INSTANCE_EVENT_WINDOWS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeInstanceEventWindowsResult, "DescribeInstanceEventWindows")
      end

      # Describes the AMI that was used to launch an instance, even if the AMI is deprecated, deregistered,
      # made private (no longer public or shared with your account), or not allowed. If you specify instance
      # IDs, the output includes information for only the specified instances. If you specify filters, the
      # output includes information for only those instances that meet the filter criteria. If you do not
      # specify instance IDs or filters, the output includes information for all instances, which can affect
      # performance. If you specify an instance ID that is not valid, an instance that doesn't exist, or an
      # instance that you do not own, an error ( InvalidInstanceID.NotFound ) is returned. Recently
      # terminated instances might appear in the returned results. This interval is usually less than one
      # hour. In the rare case where an Availability Zone is experiencing a service disruption and you
      # specify instance IDs that are in the affected Availability Zone, or do not specify any instance IDs
      # at all, the call fails. If you specify only instance IDs that are in an unaffected Availability
      # Zone, the call works normally. The order of the elements in the response, including those within
      # nested structures, might vary. Applications should not assume the elements appear in a particular
      # order.

      def describe_instance_image_metadata(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        instance_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeInstanceImageMetadataResult

        input = Types::DescribeInstanceImageMetadataRequest.new(dry_run: dry_run, filters: filters, instance_ids: instance_ids, max_results: max_results, next_token: next_token)
        describe_instance_image_metadata(input)
      end

      def describe_instance_image_metadata(input : Types::DescribeInstanceImageMetadataRequest) : Types::DescribeInstanceImageMetadataResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_INSTANCE_IMAGE_METADATA, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeInstanceImageMetadataResult, "DescribeInstanceImageMetadata")
      end

      # Describes the historical SQL Server High Availability states for Amazon EC2 instances that are
      # enabled for Amazon EC2 High Availability for SQL Server monitoring.

      def describe_instance_sql_ha_history_states(
        dry_run : Bool? = nil,
        end_time : Time? = nil,
        filters : Array(Types::Filter)? = nil,
        instance_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        start_time : Time? = nil
      ) : Types::DescribeInstanceSqlHaHistoryStatesResult

        input = Types::DescribeInstanceSqlHaHistoryStatesRequest.new(dry_run: dry_run, end_time: end_time, filters: filters, instance_ids: instance_ids, max_results: max_results, next_token: next_token, start_time: start_time)
        describe_instance_sql_ha_history_states(input)
      end

      def describe_instance_sql_ha_history_states(input : Types::DescribeInstanceSqlHaHistoryStatesRequest) : Types::DescribeInstanceSqlHaHistoryStatesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_INSTANCE_SQL_HA_HISTORY_STATES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeInstanceSqlHaHistoryStatesResult, "DescribeInstanceSqlHaHistoryStates")
      end

      # Describes the SQL Server High Availability states for Amazon EC2 instances that are enabled for
      # Amazon EC2 High Availability for SQL Server monitoring.

      def describe_instance_sql_ha_states(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        instance_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeInstanceSqlHaStatesResult

        input = Types::DescribeInstanceSqlHaStatesRequest.new(dry_run: dry_run, filters: filters, instance_ids: instance_ids, max_results: max_results, next_token: next_token)
        describe_instance_sql_ha_states(input)
      end

      def describe_instance_sql_ha_states(input : Types::DescribeInstanceSqlHaStatesRequest) : Types::DescribeInstanceSqlHaStatesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_INSTANCE_SQL_HA_STATES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeInstanceSqlHaStatesResult, "DescribeInstanceSqlHaStates")
      end

      # Describes the status of the specified instances or all of your instances. By default, only running
      # instances are described, unless you specifically indicate to return the status of all instances.
      # Instance status includes the following components: Status checks - Amazon EC2 performs status checks
      # on running EC2 instances to identify hardware and software issues. For more information, see Status
      # checks for your instances and Troubleshoot instances with failed status checks in the Amazon EC2
      # User Guide . Scheduled events - Amazon EC2 can schedule events (such as reboot, stop, or terminate)
      # for your instances related to hardware issues, software updates, or system maintenance. For more
      # information, see Scheduled events for your instances in the Amazon EC2 User Guide . Instance state -
      # You can manage your instances from the moment you launch them through their termination. For more
      # information, see Instance lifecycle in the Amazon EC2 User Guide . The Amazon EC2 API follows an
      # eventual consistency model. This means that the result of an API command you run that creates or
      # modifies resources might not be immediately available to all subsequent commands you run. For
      # guidance on how to manage eventual consistency, see Eventual consistency in the Amazon EC2 API in
      # the Amazon EC2 Developer Guide . The order of the elements in the response, including those within
      # nested structures, might vary. Applications should not assume the elements appear in a particular
      # order.

      def describe_instance_status(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        include_all_instances : Bool? = nil,
        instance_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeInstanceStatusResult

        input = Types::DescribeInstanceStatusRequest.new(dry_run: dry_run, filters: filters, include_all_instances: include_all_instances, instance_ids: instance_ids, max_results: max_results, next_token: next_token)
        describe_instance_status(input)
      end

      def describe_instance_status(input : Types::DescribeInstanceStatusRequest) : Types::DescribeInstanceStatusResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_INSTANCE_STATUS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeInstanceStatusResult, "DescribeInstanceStatus")
      end

      # Describes a tree-based hierarchy that represents the physical host placement of your EC2 instances
      # within an Availability Zone or Local Zone. You can use this information to determine the relative
      # proximity of your EC2 instances within the Amazon Web Services network to support your tightly
      # coupled workloads. Instance topology is supported for specific instance types only. For more
      # information, see Prerequisites for Amazon EC2 instance topology in the Amazon EC2 User Guide . The
      # Amazon EC2 API follows an eventual consistency model due to the distributed nature of the system
      # supporting it. As a result, when you call the DescribeInstanceTopology API command immediately after
      # launching instances, the response might return a null value for capacityBlockId because the data
      # might not have fully propagated across all subsystems. For more information, see Eventual
      # consistency in the Amazon EC2 API in the Amazon EC2 Developer Guide . For more information, see
      # Amazon EC2 topology in the Amazon EC2 User Guide .

      def describe_instance_topology(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        group_names : Array(String)? = nil,
        instance_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeInstanceTopologyResult

        input = Types::DescribeInstanceTopologyRequest.new(dry_run: dry_run, filters: filters, group_names: group_names, instance_ids: instance_ids, max_results: max_results, next_token: next_token)
        describe_instance_topology(input)
      end

      def describe_instance_topology(input : Types::DescribeInstanceTopologyRequest) : Types::DescribeInstanceTopologyResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_INSTANCE_TOPOLOGY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeInstanceTopologyResult, "DescribeInstanceTopology")
      end

      # Lists the instance types that are offered for the specified location. If no location is specified,
      # the default is to list the instance types that are offered in the current Region.

      def describe_instance_type_offerings(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        location_type : String? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeInstanceTypeOfferingsResult

        input = Types::DescribeInstanceTypeOfferingsRequest.new(dry_run: dry_run, filters: filters, location_type: location_type, max_results: max_results, next_token: next_token)
        describe_instance_type_offerings(input)
      end

      def describe_instance_type_offerings(input : Types::DescribeInstanceTypeOfferingsRequest) : Types::DescribeInstanceTypeOfferingsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_INSTANCE_TYPE_OFFERINGS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeInstanceTypeOfferingsResult, "DescribeInstanceTypeOfferings")
      end

      # Describes the specified instance types. By default, all instance types for the current Region are
      # described. Alternatively, you can filter the results.

      def describe_instance_types(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        instance_types : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeInstanceTypesResult

        input = Types::DescribeInstanceTypesRequest.new(dry_run: dry_run, filters: filters, instance_types: instance_types, max_results: max_results, next_token: next_token)
        describe_instance_types(input)
      end

      def describe_instance_types(input : Types::DescribeInstanceTypesRequest) : Types::DescribeInstanceTypesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_INSTANCE_TYPES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeInstanceTypesResult, "DescribeInstanceTypes")
      end

      # Describes the specified instances or all instances. If you specify instance IDs, the output includes
      # information for only the specified instances. If you specify filters, the output includes
      # information for only those instances that meet the filter criteria. If you do not specify instance
      # IDs or filters, the output includes information for all instances, which can affect performance. We
      # recommend that you use pagination to ensure that the operation returns quickly and successfully. The
      # response includes SQL license exemption status information for instances registered with the SQL LE
      # service, providing visibility into license exemption configuration and status. If you specify an
      # instance ID that is not valid, an error is returned. If you specify an instance that you do not own,
      # it is not included in the output. Recently terminated instances might appear in the returned
      # results. This interval is usually less than one hour. If you describe instances in the rare case
      # where an Availability Zone is experiencing a service disruption and you specify instance IDs that
      # are in the affected zone, or do not specify any instance IDs at all, the call fails. If you describe
      # instances and specify only instance IDs that are in an unaffected zone, the call works normally. The
      # Amazon EC2 API follows an eventual consistency model. This means that the result of an API command
      # you run that creates or modifies resources might not be immediately available to all subsequent
      # commands you run. For guidance on how to manage eventual consistency, see Eventual consistency in
      # the Amazon EC2 API in the Amazon EC2 Developer Guide . We strongly recommend using only paginated
      # requests. Unpaginated requests are susceptible to throttling and timeouts. The order of the elements
      # in the response, including those within nested structures, might vary. Applications should not
      # assume the elements appear in a particular order.

      def describe_instances(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        instance_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeInstancesResult

        input = Types::DescribeInstancesRequest.new(dry_run: dry_run, filters: filters, instance_ids: instance_ids, max_results: max_results, next_token: next_token)
        describe_instances(input)
      end

      def describe_instances(input : Types::DescribeInstancesRequest) : Types::DescribeInstancesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_INSTANCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeInstancesResult, "DescribeInstances")
      end

      # Describes your internet gateways. The default is to describe all your internet gateways.
      # Alternatively, you can specify specific internet gateway IDs or filter the results to include only
      # the internet gateways that match specific criteria.

      def describe_internet_gateways(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        internet_gateway_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeInternetGatewaysResult

        input = Types::DescribeInternetGatewaysRequest.new(dry_run: dry_run, filters: filters, internet_gateway_ids: internet_gateway_ids, max_results: max_results, next_token: next_token)
        describe_internet_gateways(input)
      end

      def describe_internet_gateways(input : Types::DescribeInternetGatewaysRequest) : Types::DescribeInternetGatewaysResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_INTERNET_GATEWAYS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeInternetGatewaysResult, "DescribeInternetGateways")
      end

      # Describes your Autonomous System Numbers (ASNs), their provisioning statuses, and the BYOIP CIDRs
      # with which they are associated. For more information, see Tutorial: Bring your ASN to IPAM in the
      # Amazon VPC IPAM guide .

      def describe_ipam_byoasn(
        dry_run : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeIpamByoasnResult

        input = Types::DescribeIpamByoasnRequest.new(dry_run: dry_run, max_results: max_results, next_token: next_token)
        describe_ipam_byoasn(input)
      end

      def describe_ipam_byoasn(input : Types::DescribeIpamByoasnRequest) : Types::DescribeIpamByoasnResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_IPAM_BYOASN, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeIpamByoasnResult, "DescribeIpamByoasn")
      end

      # Describe verification tokens. A verification token is an Amazon Web Services-generated random value
      # that you can use to prove ownership of an external resource. For example, you can use a verification
      # token to validate that you control a public IP address range when you bring an IP address range to
      # Amazon Web Services (BYOIP).

      def describe_ipam_external_resource_verification_tokens(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        ipam_external_resource_verification_token_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeIpamExternalResourceVerificationTokensResult

        input = Types::DescribeIpamExternalResourceVerificationTokensRequest.new(dry_run: dry_run, filters: filters, ipam_external_resource_verification_token_ids: ipam_external_resource_verification_token_ids, max_results: max_results, next_token: next_token)
        describe_ipam_external_resource_verification_tokens(input)
      end

      def describe_ipam_external_resource_verification_tokens(input : Types::DescribeIpamExternalResourceVerificationTokensRequest) : Types::DescribeIpamExternalResourceVerificationTokensResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_IPAM_EXTERNAL_RESOURCE_VERIFICATION_TOKENS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeIpamExternalResourceVerificationTokensResult, "DescribeIpamExternalResourceVerificationTokens")
      end

      # Describes one or more IPAM policies. An IPAM policy is a set of rules that define how public IPv4
      # addresses from IPAM pools are allocated to Amazon Web Services resources. Each rule maps an Amazon
      # Web Services service to IPAM pools that the service will use to get IP addresses. A single policy
      # can have multiple rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run
      # out of addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied
      # to an individual Amazon Web Services account or an entity within Amazon Web Services Organizations.

      def describe_ipam_policies(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        ipam_policy_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeIpamPoliciesResult

        input = Types::DescribeIpamPoliciesRequest.new(dry_run: dry_run, filters: filters, ipam_policy_ids: ipam_policy_ids, max_results: max_results, next_token: next_token)
        describe_ipam_policies(input)
      end

      def describe_ipam_policies(input : Types::DescribeIpamPoliciesRequest) : Types::DescribeIpamPoliciesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_IPAM_POLICIES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeIpamPoliciesResult, "DescribeIpamPolicies")
      end

      # Get information about your IPAM pools.

      def describe_ipam_pools(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        ipam_pool_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeIpamPoolsResult

        input = Types::DescribeIpamPoolsRequest.new(dry_run: dry_run, filters: filters, ipam_pool_ids: ipam_pool_ids, max_results: max_results, next_token: next_token)
        describe_ipam_pools(input)
      end

      def describe_ipam_pools(input : Types::DescribeIpamPoolsRequest) : Types::DescribeIpamPoolsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_IPAM_POOLS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeIpamPoolsResult, "DescribeIpamPools")
      end

      # Describes one or more IPAM prefix list resolver Targets. Use this operation to view the
      # configuration and status of resolver targets.

      def describe_ipam_prefix_list_resolver_targets(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        ipam_prefix_list_resolver_id : String? = nil,
        ipam_prefix_list_resolver_target_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeIpamPrefixListResolverTargetsResult

        input = Types::DescribeIpamPrefixListResolverTargetsRequest.new(dry_run: dry_run, filters: filters, ipam_prefix_list_resolver_id: ipam_prefix_list_resolver_id, ipam_prefix_list_resolver_target_ids: ipam_prefix_list_resolver_target_ids, max_results: max_results, next_token: next_token)
        describe_ipam_prefix_list_resolver_targets(input)
      end

      def describe_ipam_prefix_list_resolver_targets(input : Types::DescribeIpamPrefixListResolverTargetsRequest) : Types::DescribeIpamPrefixListResolverTargetsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_IPAM_PREFIX_LIST_RESOLVER_TARGETS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeIpamPrefixListResolverTargetsResult, "DescribeIpamPrefixListResolverTargets")
      end

      # Describes one or more IPAM prefix list resolvers. Use this operation to view the configuration,
      # status, and properties of your resolvers.

      def describe_ipam_prefix_list_resolvers(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        ipam_prefix_list_resolver_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeIpamPrefixListResolversResult

        input = Types::DescribeIpamPrefixListResolversRequest.new(dry_run: dry_run, filters: filters, ipam_prefix_list_resolver_ids: ipam_prefix_list_resolver_ids, max_results: max_results, next_token: next_token)
        describe_ipam_prefix_list_resolvers(input)
      end

      def describe_ipam_prefix_list_resolvers(input : Types::DescribeIpamPrefixListResolversRequest) : Types::DescribeIpamPrefixListResolversResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_IPAM_PREFIX_LIST_RESOLVERS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeIpamPrefixListResolversResult, "DescribeIpamPrefixListResolvers")
      end

      # Describes IPAM resource discoveries. A resource discovery is an IPAM component that enables IPAM to
      # manage and monitor resources that belong to the owning account.

      def describe_ipam_resource_discoveries(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        ipam_resource_discovery_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeIpamResourceDiscoveriesResult

        input = Types::DescribeIpamResourceDiscoveriesRequest.new(dry_run: dry_run, filters: filters, ipam_resource_discovery_ids: ipam_resource_discovery_ids, max_results: max_results, next_token: next_token)
        describe_ipam_resource_discoveries(input)
      end

      def describe_ipam_resource_discoveries(input : Types::DescribeIpamResourceDiscoveriesRequest) : Types::DescribeIpamResourceDiscoveriesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_IPAM_RESOURCE_DISCOVERIES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeIpamResourceDiscoveriesResult, "DescribeIpamResourceDiscoveries")
      end

      # Describes resource discovery association with an Amazon VPC IPAM. An associated resource discovery
      # is a resource discovery that has been associated with an IPAM..

      def describe_ipam_resource_discovery_associations(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        ipam_resource_discovery_association_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeIpamResourceDiscoveryAssociationsResult

        input = Types::DescribeIpamResourceDiscoveryAssociationsRequest.new(dry_run: dry_run, filters: filters, ipam_resource_discovery_association_ids: ipam_resource_discovery_association_ids, max_results: max_results, next_token: next_token)
        describe_ipam_resource_discovery_associations(input)
      end

      def describe_ipam_resource_discovery_associations(input : Types::DescribeIpamResourceDiscoveryAssociationsRequest) : Types::DescribeIpamResourceDiscoveryAssociationsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_IPAM_RESOURCE_DISCOVERY_ASSOCIATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeIpamResourceDiscoveryAssociationsResult, "DescribeIpamResourceDiscoveryAssociations")
      end

      # Get information about your IPAM scopes.

      def describe_ipam_scopes(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        ipam_scope_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeIpamScopesResult

        input = Types::DescribeIpamScopesRequest.new(dry_run: dry_run, filters: filters, ipam_scope_ids: ipam_scope_ids, max_results: max_results, next_token: next_token)
        describe_ipam_scopes(input)
      end

      def describe_ipam_scopes(input : Types::DescribeIpamScopesRequest) : Types::DescribeIpamScopesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_IPAM_SCOPES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeIpamScopesResult, "DescribeIpamScopes")
      end

      # Get information about your IPAM pools. For more information, see What is IPAM? in the Amazon VPC
      # IPAM User Guide .

      def describe_ipams(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        ipam_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeIpamsResult

        input = Types::DescribeIpamsRequest.new(dry_run: dry_run, filters: filters, ipam_ids: ipam_ids, max_results: max_results, next_token: next_token)
        describe_ipams(input)
      end

      def describe_ipams(input : Types::DescribeIpamsRequest) : Types::DescribeIpamsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_IPAMS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeIpamsResult, "DescribeIpams")
      end

      # Describes your IPv6 address pools.

      def describe_ipv6_pools(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        pool_ids : Array(String)? = nil
      ) : Types::DescribeIpv6PoolsResult

        input = Types::DescribeIpv6PoolsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, pool_ids: pool_ids)
        describe_ipv6_pools(input)
      end

      def describe_ipv6_pools(input : Types::DescribeIpv6PoolsRequest) : Types::DescribeIpv6PoolsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_IPV6_POOLS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeIpv6PoolsResult, "DescribeIpv6Pools")
      end

      # Describes the specified key pairs or all of your key pairs. For more information about key pairs,
      # see Amazon EC2 key pairs in the Amazon EC2 User Guide .

      def describe_key_pairs(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        include_public_key : Bool? = nil,
        key_names : Array(String)? = nil,
        key_pair_ids : Array(String)? = nil
      ) : Types::DescribeKeyPairsResult

        input = Types::DescribeKeyPairsRequest.new(dry_run: dry_run, filters: filters, include_public_key: include_public_key, key_names: key_names, key_pair_ids: key_pair_ids)
        describe_key_pairs(input)
      end

      def describe_key_pairs(input : Types::DescribeKeyPairsRequest) : Types::DescribeKeyPairsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_KEY_PAIRS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeKeyPairsResult, "DescribeKeyPairs")
      end

      # Describes one or more versions of a specified launch template. You can describe all versions,
      # individual versions, or a range of versions. You can also describe all the latest versions or all
      # the default versions of all the launch templates in your account.

      def describe_launch_template_versions(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        launch_template_id : String? = nil,
        launch_template_name : String? = nil,
        max_results : Int32? = nil,
        max_version : String? = nil,
        min_version : String? = nil,
        next_token : String? = nil,
        resolve_alias : Bool? = nil,
        versions : Array(String)? = nil
      ) : Types::DescribeLaunchTemplateVersionsResult

        input = Types::DescribeLaunchTemplateVersionsRequest.new(dry_run: dry_run, filters: filters, launch_template_id: launch_template_id, launch_template_name: launch_template_name, max_results: max_results, max_version: max_version, min_version: min_version, next_token: next_token, resolve_alias: resolve_alias, versions: versions)
        describe_launch_template_versions(input)
      end

      def describe_launch_template_versions(input : Types::DescribeLaunchTemplateVersionsRequest) : Types::DescribeLaunchTemplateVersionsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_LAUNCH_TEMPLATE_VERSIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeLaunchTemplateVersionsResult, "DescribeLaunchTemplateVersions")
      end

      # Describes one or more launch templates.

      def describe_launch_templates(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        launch_template_ids : Array(String)? = nil,
        launch_template_names : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeLaunchTemplatesResult

        input = Types::DescribeLaunchTemplatesRequest.new(dry_run: dry_run, filters: filters, launch_template_ids: launch_template_ids, launch_template_names: launch_template_names, max_results: max_results, next_token: next_token)
        describe_launch_templates(input)
      end

      def describe_launch_templates(input : Types::DescribeLaunchTemplatesRequest) : Types::DescribeLaunchTemplatesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_LAUNCH_TEMPLATES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeLaunchTemplatesResult, "DescribeLaunchTemplates")
      end

      # Describes the associations between virtual interface groups and local gateway route tables.

      def describe_local_gateway_route_table_virtual_interface_group_associations(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        local_gateway_route_table_virtual_interface_group_association_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociationsResult

        input = Types::DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociationsRequest.new(dry_run: dry_run, filters: filters, local_gateway_route_table_virtual_interface_group_association_ids: local_gateway_route_table_virtual_interface_group_association_ids, max_results: max_results, next_token: next_token)
        describe_local_gateway_route_table_virtual_interface_group_associations(input)
      end

      def describe_local_gateway_route_table_virtual_interface_group_associations(input : Types::DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociationsRequest) : Types::DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociationsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_LOCAL_GATEWAY_ROUTE_TABLE_VIRTUAL_INTERFACE_GROUP_ASSOCIATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociationsResult, "DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociations")
      end

      # Describes the specified associations between VPCs and local gateway route tables.

      def describe_local_gateway_route_table_vpc_associations(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        local_gateway_route_table_vpc_association_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeLocalGatewayRouteTableVpcAssociationsResult

        input = Types::DescribeLocalGatewayRouteTableVpcAssociationsRequest.new(dry_run: dry_run, filters: filters, local_gateway_route_table_vpc_association_ids: local_gateway_route_table_vpc_association_ids, max_results: max_results, next_token: next_token)
        describe_local_gateway_route_table_vpc_associations(input)
      end

      def describe_local_gateway_route_table_vpc_associations(input : Types::DescribeLocalGatewayRouteTableVpcAssociationsRequest) : Types::DescribeLocalGatewayRouteTableVpcAssociationsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_LOCAL_GATEWAY_ROUTE_TABLE_VPC_ASSOCIATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeLocalGatewayRouteTableVpcAssociationsResult, "DescribeLocalGatewayRouteTableVpcAssociations")
      end

      # Describes one or more local gateway route tables. By default, all local gateway route tables are
      # described. Alternatively, you can filter the results.

      def describe_local_gateway_route_tables(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        local_gateway_route_table_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeLocalGatewayRouteTablesResult

        input = Types::DescribeLocalGatewayRouteTablesRequest.new(dry_run: dry_run, filters: filters, local_gateway_route_table_ids: local_gateway_route_table_ids, max_results: max_results, next_token: next_token)
        describe_local_gateway_route_tables(input)
      end

      def describe_local_gateway_route_tables(input : Types::DescribeLocalGatewayRouteTablesRequest) : Types::DescribeLocalGatewayRouteTablesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_LOCAL_GATEWAY_ROUTE_TABLES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeLocalGatewayRouteTablesResult, "DescribeLocalGatewayRouteTables")
      end

      # Describes the specified local gateway virtual interface groups.

      def describe_local_gateway_virtual_interface_groups(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        local_gateway_virtual_interface_group_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeLocalGatewayVirtualInterfaceGroupsResult

        input = Types::DescribeLocalGatewayVirtualInterfaceGroupsRequest.new(dry_run: dry_run, filters: filters, local_gateway_virtual_interface_group_ids: local_gateway_virtual_interface_group_ids, max_results: max_results, next_token: next_token)
        describe_local_gateway_virtual_interface_groups(input)
      end

      def describe_local_gateway_virtual_interface_groups(input : Types::DescribeLocalGatewayVirtualInterfaceGroupsRequest) : Types::DescribeLocalGatewayVirtualInterfaceGroupsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_LOCAL_GATEWAY_VIRTUAL_INTERFACE_GROUPS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeLocalGatewayVirtualInterfaceGroupsResult, "DescribeLocalGatewayVirtualInterfaceGroups")
      end

      # Describes the specified local gateway virtual interfaces.

      def describe_local_gateway_virtual_interfaces(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        local_gateway_virtual_interface_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeLocalGatewayVirtualInterfacesResult

        input = Types::DescribeLocalGatewayVirtualInterfacesRequest.new(dry_run: dry_run, filters: filters, local_gateway_virtual_interface_ids: local_gateway_virtual_interface_ids, max_results: max_results, next_token: next_token)
        describe_local_gateway_virtual_interfaces(input)
      end

      def describe_local_gateway_virtual_interfaces(input : Types::DescribeLocalGatewayVirtualInterfacesRequest) : Types::DescribeLocalGatewayVirtualInterfacesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_LOCAL_GATEWAY_VIRTUAL_INTERFACES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeLocalGatewayVirtualInterfacesResult, "DescribeLocalGatewayVirtualInterfaces")
      end

      # Describes one or more local gateways. By default, all local gateways are described. Alternatively,
      # you can filter the results.

      def describe_local_gateways(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        local_gateway_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeLocalGatewaysResult

        input = Types::DescribeLocalGatewaysRequest.new(dry_run: dry_run, filters: filters, local_gateway_ids: local_gateway_ids, max_results: max_results, next_token: next_token)
        describe_local_gateways(input)
      end

      def describe_local_gateways(input : Types::DescribeLocalGatewaysRequest) : Types::DescribeLocalGatewaysResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_LOCAL_GATEWAYS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeLocalGatewaysResult, "DescribeLocalGateways")
      end

      # Describes the lock status for a snapshot.

      def describe_locked_snapshots(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        snapshot_ids : Array(String)? = nil
      ) : Types::DescribeLockedSnapshotsResult

        input = Types::DescribeLockedSnapshotsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, snapshot_ids: snapshot_ids)
        describe_locked_snapshots(input)
      end

      def describe_locked_snapshots(input : Types::DescribeLockedSnapshotsRequest) : Types::DescribeLockedSnapshotsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_LOCKED_SNAPSHOTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeLockedSnapshotsResult, "DescribeLockedSnapshots")
      end

      # Describes the specified EC2 Mac Dedicated Host or all of your EC2 Mac Dedicated Hosts.

      def describe_mac_hosts(
        filters : Array(Types::Filter)? = nil,
        host_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeMacHostsResult

        input = Types::DescribeMacHostsRequest.new(filters: filters, host_ids: host_ids, max_results: max_results, next_token: next_token)
        describe_mac_hosts(input)
      end

      def describe_mac_hosts(input : Types::DescribeMacHostsRequest) : Types::DescribeMacHostsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_MAC_HOSTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeMacHostsResult, "DescribeMacHosts")
      end

      # Describes a System Integrity Protection (SIP) modification task or volume ownership delegation task
      # for an Amazon EC2 Mac instance. For more information, see Configure SIP for Amazon EC2 instances in
      # the Amazon EC2 User Guide .

      def describe_mac_modification_tasks(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        mac_modification_task_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeMacModificationTasksResult

        input = Types::DescribeMacModificationTasksRequest.new(dry_run: dry_run, filters: filters, mac_modification_task_ids: mac_modification_task_ids, max_results: max_results, next_token: next_token)
        describe_mac_modification_tasks(input)
      end

      def describe_mac_modification_tasks(input : Types::DescribeMacModificationTasksRequest) : Types::DescribeMacModificationTasksResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_MAC_MODIFICATION_TASKS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeMacModificationTasksResult, "DescribeMacModificationTasks")
      end

      # Describes your managed prefix lists and any Amazon Web Services-managed prefix lists.

      def describe_managed_prefix_lists(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        prefix_list_ids : Array(String)? = nil
      ) : Types::DescribeManagedPrefixListsResult

        input = Types::DescribeManagedPrefixListsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, prefix_list_ids: prefix_list_ids)
        describe_managed_prefix_lists(input)
      end

      def describe_managed_prefix_lists(input : Types::DescribeManagedPrefixListsRequest) : Types::DescribeManagedPrefixListsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_MANAGED_PREFIX_LISTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeManagedPrefixListsResult, "DescribeManagedPrefixLists")
      end

      # This action is deprecated. Describes your Elastic IP addresses that are being moved from or being
      # restored to the EC2-Classic platform. This request does not return information about any other
      # Elastic IP addresses in your account.

      def describe_moving_addresses(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        public_ips : Array(String)? = nil
      ) : Types::DescribeMovingAddressesResult

        input = Types::DescribeMovingAddressesRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, public_ips: public_ips)
        describe_moving_addresses(input)
      end

      def describe_moving_addresses(input : Types::DescribeMovingAddressesRequest) : Types::DescribeMovingAddressesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_MOVING_ADDRESSES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeMovingAddressesResult, "DescribeMovingAddresses")
      end

      # Describes your NAT gateways. The default is to describe all your NAT gateways. Alternatively, you
      # can specify specific NAT gateway IDs or filter the results to include only the NAT gateways that
      # match specific criteria.

      def describe_nat_gateways(
        dry_run : Bool? = nil,
        filter : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        nat_gateway_ids : Array(String)? = nil,
        next_token : String? = nil
      ) : Types::DescribeNatGatewaysResult

        input = Types::DescribeNatGatewaysRequest.new(dry_run: dry_run, filter: filter, max_results: max_results, nat_gateway_ids: nat_gateway_ids, next_token: next_token)
        describe_nat_gateways(input)
      end

      def describe_nat_gateways(input : Types::DescribeNatGatewaysRequest) : Types::DescribeNatGatewaysResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_NAT_GATEWAYS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeNatGatewaysResult, "DescribeNatGateways")
      end

      # Describes your network ACLs. The default is to describe all your network ACLs. Alternatively, you
      # can specify specific network ACL IDs or filter the results to include only the network ACLs that
      # match specific criteria. For more information, see Network ACLs in the Amazon VPC User Guide .

      def describe_network_acls(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        network_acl_ids : Array(String)? = nil,
        next_token : String? = nil
      ) : Types::DescribeNetworkAclsResult

        input = Types::DescribeNetworkAclsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, network_acl_ids: network_acl_ids, next_token: next_token)
        describe_network_acls(input)
      end

      def describe_network_acls(input : Types::DescribeNetworkAclsRequest) : Types::DescribeNetworkAclsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_NETWORK_ACLS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeNetworkAclsResult, "DescribeNetworkAcls")
      end

      # Describes the specified Network Access Scope analyses.

      def describe_network_insights_access_scope_analyses(
        analysis_start_time_begin : Time? = nil,
        analysis_start_time_end : Time? = nil,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        network_insights_access_scope_analysis_ids : Array(String)? = nil,
        network_insights_access_scope_id : String? = nil,
        next_token : String? = nil
      ) : Types::DescribeNetworkInsightsAccessScopeAnalysesResult

        input = Types::DescribeNetworkInsightsAccessScopeAnalysesRequest.new(analysis_start_time_begin: analysis_start_time_begin, analysis_start_time_end: analysis_start_time_end, dry_run: dry_run, filters: filters, max_results: max_results, network_insights_access_scope_analysis_ids: network_insights_access_scope_analysis_ids, network_insights_access_scope_id: network_insights_access_scope_id, next_token: next_token)
        describe_network_insights_access_scope_analyses(input)
      end

      def describe_network_insights_access_scope_analyses(input : Types::DescribeNetworkInsightsAccessScopeAnalysesRequest) : Types::DescribeNetworkInsightsAccessScopeAnalysesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_NETWORK_INSIGHTS_ACCESS_SCOPE_ANALYSES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeNetworkInsightsAccessScopeAnalysesResult, "DescribeNetworkInsightsAccessScopeAnalyses")
      end

      # Describes the specified Network Access Scopes.

      def describe_network_insights_access_scopes(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        network_insights_access_scope_ids : Array(String)? = nil,
        next_token : String? = nil
      ) : Types::DescribeNetworkInsightsAccessScopesResult

        input = Types::DescribeNetworkInsightsAccessScopesRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, network_insights_access_scope_ids: network_insights_access_scope_ids, next_token: next_token)
        describe_network_insights_access_scopes(input)
      end

      def describe_network_insights_access_scopes(input : Types::DescribeNetworkInsightsAccessScopesRequest) : Types::DescribeNetworkInsightsAccessScopesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_NETWORK_INSIGHTS_ACCESS_SCOPES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeNetworkInsightsAccessScopesResult, "DescribeNetworkInsightsAccessScopes")
      end

      # Describes one or more of your network insights analyses.

      def describe_network_insights_analyses(
        analysis_end_time : Time? = nil,
        analysis_start_time : Time? = nil,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        network_insights_analysis_ids : Array(String)? = nil,
        network_insights_path_id : String? = nil,
        next_token : String? = nil
      ) : Types::DescribeNetworkInsightsAnalysesResult

        input = Types::DescribeNetworkInsightsAnalysesRequest.new(analysis_end_time: analysis_end_time, analysis_start_time: analysis_start_time, dry_run: dry_run, filters: filters, max_results: max_results, network_insights_analysis_ids: network_insights_analysis_ids, network_insights_path_id: network_insights_path_id, next_token: next_token)
        describe_network_insights_analyses(input)
      end

      def describe_network_insights_analyses(input : Types::DescribeNetworkInsightsAnalysesRequest) : Types::DescribeNetworkInsightsAnalysesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_NETWORK_INSIGHTS_ANALYSES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeNetworkInsightsAnalysesResult, "DescribeNetworkInsightsAnalyses")
      end

      # Describes one or more of your paths.

      def describe_network_insights_paths(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        network_insights_path_ids : Array(String)? = nil,
        next_token : String? = nil
      ) : Types::DescribeNetworkInsightsPathsResult

        input = Types::DescribeNetworkInsightsPathsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, network_insights_path_ids: network_insights_path_ids, next_token: next_token)
        describe_network_insights_paths(input)
      end

      def describe_network_insights_paths(input : Types::DescribeNetworkInsightsPathsRequest) : Types::DescribeNetworkInsightsPathsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_NETWORK_INSIGHTS_PATHS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeNetworkInsightsPathsResult, "DescribeNetworkInsightsPaths")
      end

      # Describes a network interface attribute. You can specify only one attribute at a time.

      def describe_network_interface_attribute(
        network_interface_id : String,
        attribute : String? = nil,
        dry_run : Bool? = nil
      ) : Types::DescribeNetworkInterfaceAttributeResult

        input = Types::DescribeNetworkInterfaceAttributeRequest.new(network_interface_id: network_interface_id, attribute: attribute, dry_run: dry_run)
        describe_network_interface_attribute(input)
      end

      def describe_network_interface_attribute(input : Types::DescribeNetworkInterfaceAttributeRequest) : Types::DescribeNetworkInterfaceAttributeResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_NETWORK_INTERFACE_ATTRIBUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeNetworkInterfaceAttributeResult, "DescribeNetworkInterfaceAttribute")
      end

      # Describes the permissions for your network interfaces.

      def describe_network_interface_permissions(
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        network_interface_permission_ids : Array(String)? = nil,
        next_token : String? = nil
      ) : Types::DescribeNetworkInterfacePermissionsResult

        input = Types::DescribeNetworkInterfacePermissionsRequest.new(filters: filters, max_results: max_results, network_interface_permission_ids: network_interface_permission_ids, next_token: next_token)
        describe_network_interface_permissions(input)
      end

      def describe_network_interface_permissions(input : Types::DescribeNetworkInterfacePermissionsRequest) : Types::DescribeNetworkInterfacePermissionsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_NETWORK_INTERFACE_PERMISSIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeNetworkInterfacePermissionsResult, "DescribeNetworkInterfacePermissions")
      end

      # Describes the specified network interfaces or all your network interfaces. If you have a large
      # number of network interfaces, the operation fails unless you use pagination or one of the following
      # filters: group-id , mac-address , private-dns-name , private-ip-address , subnet-id , or vpc-id . We
      # strongly recommend using only paginated requests. Unpaginated requests are susceptible to throttling
      # and timeouts.

      def describe_network_interfaces(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        network_interface_ids : Array(String)? = nil,
        next_token : String? = nil
      ) : Types::DescribeNetworkInterfacesResult

        input = Types::DescribeNetworkInterfacesRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, network_interface_ids: network_interface_ids, next_token: next_token)
        describe_network_interfaces(input)
      end

      def describe_network_interfaces(input : Types::DescribeNetworkInterfacesRequest) : Types::DescribeNetworkInterfacesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_NETWORK_INTERFACES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeNetworkInterfacesResult, "DescribeNetworkInterfaces")
      end

      # Describes the Outposts link aggregation groups (LAGs). LAGs are only available for second-generation
      # Outposts racks at this time.

      def describe_outpost_lags(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        outpost_lag_ids : Array(String)? = nil
      ) : Types::DescribeOutpostLagsResult

        input = Types::DescribeOutpostLagsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, outpost_lag_ids: outpost_lag_ids)
        describe_outpost_lags(input)
      end

      def describe_outpost_lags(input : Types::DescribeOutpostLagsRequest) : Types::DescribeOutpostLagsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_OUTPOST_LAGS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeOutpostLagsResult, "DescribeOutpostLags")
      end

      # Describes the specified placement groups or all of your placement groups. To describe a specific
      # placement group that is shared with your account, you must specify the ID of the placement group
      # using the GroupId parameter. Specifying the name of a shared placement group using the GroupNames
      # parameter will result in an error. For more information, see Placement groups in the Amazon EC2 User
      # Guide .

      def describe_placement_groups(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        group_ids : Array(String)? = nil,
        group_names : Array(String)? = nil
      ) : Types::DescribePlacementGroupsResult

        input = Types::DescribePlacementGroupsRequest.new(dry_run: dry_run, filters: filters, group_ids: group_ids, group_names: group_names)
        describe_placement_groups(input)
      end

      def describe_placement_groups(input : Types::DescribePlacementGroupsRequest) : Types::DescribePlacementGroupsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_PLACEMENT_GROUPS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribePlacementGroupsResult, "DescribePlacementGroups")
      end

      # Describes available Amazon Web Services services in a prefix list format, which includes the prefix
      # list name and prefix list ID of the service and the IP address range for the service.

      def describe_prefix_lists(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        prefix_list_ids : Array(String)? = nil
      ) : Types::DescribePrefixListsResult

        input = Types::DescribePrefixListsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, prefix_list_ids: prefix_list_ids)
        describe_prefix_lists(input)
      end

      def describe_prefix_lists(input : Types::DescribePrefixListsRequest) : Types::DescribePrefixListsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_PREFIX_LISTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribePrefixListsResult, "DescribePrefixLists")
      end

      # Describes the ID format settings for the root user and all IAM roles and IAM users that have
      # explicitly specified a longer ID (17-character ID) preference. By default, all IAM roles and IAM
      # users default to the same ID settings as the root user, unless they explicitly override the
      # settings. This request is useful for identifying those IAM users and IAM roles that have overridden
      # the default ID settings. The following resource types support longer IDs: bundle | conversion-task |
      # customer-gateway | dhcp-options | elastic-ip-allocation | elastic-ip-association | export-task |
      # flow-log | image | import-task | instance | internet-gateway | network-acl | network-acl-association
      # | network-interface | network-interface-attachment | prefix-list | reservation | route-table |
      # route-table-association | security-group | snapshot | subnet | subnet-cidr-block-association |
      # volume | vpc | vpc-cidr-block-association | vpc-endpoint | vpc-peering-connection | vpn-connection |
      # vpn-gateway .

      def describe_principal_id_format(
        dry_run : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        resources : Array(String)? = nil
      ) : Types::DescribePrincipalIdFormatResult

        input = Types::DescribePrincipalIdFormatRequest.new(dry_run: dry_run, max_results: max_results, next_token: next_token, resources: resources)
        describe_principal_id_format(input)
      end

      def describe_principal_id_format(input : Types::DescribePrincipalIdFormatRequest) : Types::DescribePrincipalIdFormatResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_PRINCIPAL_ID_FORMAT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribePrincipalIdFormatResult, "DescribePrincipalIdFormat")
      end

      # Describes the specified IPv4 address pools.

      def describe_public_ipv4_pools(
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        pool_ids : Array(String)? = nil
      ) : Types::DescribePublicIpv4PoolsResult

        input = Types::DescribePublicIpv4PoolsRequest.new(filters: filters, max_results: max_results, next_token: next_token, pool_ids: pool_ids)
        describe_public_ipv4_pools(input)
      end

      def describe_public_ipv4_pools(input : Types::DescribePublicIpv4PoolsRequest) : Types::DescribePublicIpv4PoolsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_PUBLIC_IPV4_POOLS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribePublicIpv4PoolsResult, "DescribePublicIpv4Pools")
      end

      # Describes the Regions that are enabled for your account, or all Regions. For a list of the Regions
      # supported by Amazon EC2, see Amazon EC2 service endpoints . For information about enabling and
      # disabling Regions for your account, see Specify which Amazon Web Services Regions your account can
      # use in the Amazon Web Services Account Management Reference Guide . The order of the elements in the
      # response, including those within nested structures, might vary. Applications should not assume the
      # elements appear in a particular order.

      def describe_regions(
        all_regions : Bool? = nil,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        region_names : Array(String)? = nil
      ) : Types::DescribeRegionsResult

        input = Types::DescribeRegionsRequest.new(all_regions: all_regions, dry_run: dry_run, filters: filters, region_names: region_names)
        describe_regions(input)
      end

      def describe_regions(input : Types::DescribeRegionsRequest) : Types::DescribeRegionsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_REGIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeRegionsResult, "DescribeRegions")
      end

      # Describes a root volume replacement task. For more information, see Replace a root volume in the
      # Amazon EC2 User Guide .

      def describe_replace_root_volume_tasks(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        replace_root_volume_task_ids : Array(String)? = nil
      ) : Types::DescribeReplaceRootVolumeTasksResult

        input = Types::DescribeReplaceRootVolumeTasksRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, replace_root_volume_task_ids: replace_root_volume_task_ids)
        describe_replace_root_volume_tasks(input)
      end

      def describe_replace_root_volume_tasks(input : Types::DescribeReplaceRootVolumeTasksRequest) : Types::DescribeReplaceRootVolumeTasksResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_REPLACE_ROOT_VOLUME_TASKS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeReplaceRootVolumeTasksResult, "DescribeReplaceRootVolumeTasks")
      end

      # Describes one or more of the Reserved Instances that you purchased. For more information about
      # Reserved Instances, see Reserved Instances in the Amazon EC2 User Guide . The order of the elements
      # in the response, including those within nested structures, might vary. Applications should not
      # assume the elements appear in a particular order.

      def describe_reserved_instances(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        offering_class : String? = nil,
        offering_type : String? = nil,
        reserved_instances_ids : Array(String)? = nil
      ) : Types::DescribeReservedInstancesResult

        input = Types::DescribeReservedInstancesRequest.new(dry_run: dry_run, filters: filters, offering_class: offering_class, offering_type: offering_type, reserved_instances_ids: reserved_instances_ids)
        describe_reserved_instances(input)
      end

      def describe_reserved_instances(input : Types::DescribeReservedInstancesRequest) : Types::DescribeReservedInstancesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_RESERVED_INSTANCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeReservedInstancesResult, "DescribeReservedInstances")
      end

      # Describes your account's Reserved Instance listings in the Reserved Instance Marketplace. The
      # Reserved Instance Marketplace matches sellers who want to resell Reserved Instance capacity that
      # they no longer need with buyers who want to purchase additional capacity. Reserved Instances bought
      # and sold through the Reserved Instance Marketplace work like any other Reserved Instances. As a
      # seller, you choose to list some or all of your Reserved Instances, and you specify the upfront price
      # to receive for them. Your Reserved Instances are then listed in the Reserved Instance Marketplace
      # and are available for purchase. As a buyer, you specify the configuration of the Reserved Instance
      # to purchase, and the Marketplace matches what you're searching for with what's available. The
      # Marketplace first sells the lowest priced Reserved Instances to you, and continues to sell available
      # Reserved Instance listings to you until your demand is met. You are charged based on the total price
      # of all of the listings that you purchase. For more information, see Sell in the Reserved Instance
      # Marketplace in the Amazon EC2 User Guide . The order of the elements in the response, including
      # those within nested structures, might vary. Applications should not assume the elements appear in a
      # particular order.

      def describe_reserved_instances_listings(
        filters : Array(Types::Filter)? = nil,
        reserved_instances_id : String? = nil,
        reserved_instances_listing_id : String? = nil
      ) : Types::DescribeReservedInstancesListingsResult

        input = Types::DescribeReservedInstancesListingsRequest.new(filters: filters, reserved_instances_id: reserved_instances_id, reserved_instances_listing_id: reserved_instances_listing_id)
        describe_reserved_instances_listings(input)
      end

      def describe_reserved_instances_listings(input : Types::DescribeReservedInstancesListingsRequest) : Types::DescribeReservedInstancesListingsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_RESERVED_INSTANCES_LISTINGS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeReservedInstancesListingsResult, "DescribeReservedInstancesListings")
      end

      # Describes the modifications made to your Reserved Instances. If no parameter is specified,
      # information about all your Reserved Instances modification requests is returned. If a modification
      # ID is specified, only information about the specific modification is returned. For more information,
      # see Modify Reserved Instances in the Amazon EC2 User Guide . The order of the elements in the
      # response, including those within nested structures, might vary. Applications should not assume the
      # elements appear in a particular order.

      def describe_reserved_instances_modifications(
        filters : Array(Types::Filter)? = nil,
        next_token : String? = nil,
        reserved_instances_modification_ids : Array(String)? = nil
      ) : Types::DescribeReservedInstancesModificationsResult

        input = Types::DescribeReservedInstancesModificationsRequest.new(filters: filters, next_token: next_token, reserved_instances_modification_ids: reserved_instances_modification_ids)
        describe_reserved_instances_modifications(input)
      end

      def describe_reserved_instances_modifications(input : Types::DescribeReservedInstancesModificationsRequest) : Types::DescribeReservedInstancesModificationsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_RESERVED_INSTANCES_MODIFICATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeReservedInstancesModificationsResult, "DescribeReservedInstancesModifications")
      end

      # Describes Reserved Instance offerings that are available for purchase. With Reserved Instances, you
      # purchase the right to launch instances for a period of time. During that time period, you do not
      # receive insufficient capacity errors, and you pay a lower usage rate than the rate charged for
      # On-Demand instances for the actual time used. If you have listed your own Reserved Instances for
      # sale in the Reserved Instance Marketplace, they will be excluded from these results. This is to
      # ensure that you do not purchase your own Reserved Instances. For more information, see Sell in the
      # Reserved Instance Marketplace in the Amazon EC2 User Guide . The order of the elements in the
      # response, including those within nested structures, might vary. Applications should not assume the
      # elements appear in a particular order.

      def describe_reserved_instances_offerings(
        availability_zone : String? = nil,
        availability_zone_id : String? = nil,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        include_marketplace : Bool? = nil,
        instance_tenancy : String? = nil,
        instance_type : String? = nil,
        max_duration : Int64? = nil,
        max_instance_count : Int32? = nil,
        max_results : Int32? = nil,
        min_duration : Int64? = nil,
        next_token : String? = nil,
        offering_class : String? = nil,
        offering_type : String? = nil,
        product_description : String? = nil,
        reserved_instances_offering_ids : Array(String)? = nil
      ) : Types::DescribeReservedInstancesOfferingsResult

        input = Types::DescribeReservedInstancesOfferingsRequest.new(availability_zone: availability_zone, availability_zone_id: availability_zone_id, dry_run: dry_run, filters: filters, include_marketplace: include_marketplace, instance_tenancy: instance_tenancy, instance_type: instance_type, max_duration: max_duration, max_instance_count: max_instance_count, max_results: max_results, min_duration: min_duration, next_token: next_token, offering_class: offering_class, offering_type: offering_type, product_description: product_description, reserved_instances_offering_ids: reserved_instances_offering_ids)
        describe_reserved_instances_offerings(input)
      end

      def describe_reserved_instances_offerings(input : Types::DescribeReservedInstancesOfferingsRequest) : Types::DescribeReservedInstancesOfferingsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_RESERVED_INSTANCES_OFFERINGS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeReservedInstancesOfferingsResult, "DescribeReservedInstancesOfferings")
      end

      # Describes one or more route server endpoints. A route server endpoint is an Amazon Web
      # Services-managed component inside a subnet that facilitates BGP (Border Gateway Protocol)
      # connections between your route server and your BGP peers. For more information see Dynamic routing
      # in your VPC with VPC Route Server in the Amazon VPC User Guide .

      def describe_route_server_endpoints(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        route_server_endpoint_ids : Array(String)? = nil
      ) : Types::DescribeRouteServerEndpointsResult

        input = Types::DescribeRouteServerEndpointsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, route_server_endpoint_ids: route_server_endpoint_ids)
        describe_route_server_endpoints(input)
      end

      def describe_route_server_endpoints(input : Types::DescribeRouteServerEndpointsRequest) : Types::DescribeRouteServerEndpointsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_ROUTE_SERVER_ENDPOINTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeRouteServerEndpointsResult, "DescribeRouteServerEndpoints")
      end

      # Describes one or more route server peers. A route server peer is a session between a route server
      # endpoint and the device deployed in Amazon Web Services (such as a firewall appliance or other
      # network security function running on an EC2 instance). The device must meet these requirements: Have
      # an elastic network interface in the VPC Support BGP (Border Gateway Protocol) Can initiate BGP
      # sessions For more information see Dynamic routing in your VPC with VPC Route Server in the Amazon
      # VPC User Guide .

      def describe_route_server_peers(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        route_server_peer_ids : Array(String)? = nil
      ) : Types::DescribeRouteServerPeersResult

        input = Types::DescribeRouteServerPeersRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, route_server_peer_ids: route_server_peer_ids)
        describe_route_server_peers(input)
      end

      def describe_route_server_peers(input : Types::DescribeRouteServerPeersRequest) : Types::DescribeRouteServerPeersResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_ROUTE_SERVER_PEERS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeRouteServerPeersResult, "DescribeRouteServerPeers")
      end

      # Describes one or more route servers. Amazon VPC Route Server simplifies routing for traffic between
      # workloads that are deployed within a VPC and its internet gateways. With this feature, VPC Route
      # Server dynamically updates VPC and internet gateway route tables with your preferred IPv4 or IPv6
      # routes to achieve routing fault tolerance for those workloads. This enables you to automatically
      # reroute traffic within a VPC, which increases the manageability of VPC routing and interoperability
      # with third-party workloads. Route server supports the follow route table types: VPC route tables not
      # associated with subnets Subnet route tables Internet gateway route tables Route server does not
      # support route tables associated with virtual private gateways. To propagate routes into a transit
      # gateway route table, use Transit Gateway Connect . For more information see Dynamic routing in your
      # VPC with VPC Route Server in the Amazon VPC User Guide .

      def describe_route_servers(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        route_server_ids : Array(String)? = nil
      ) : Types::DescribeRouteServersResult

        input = Types::DescribeRouteServersRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, route_server_ids: route_server_ids)
        describe_route_servers(input)
      end

      def describe_route_servers(input : Types::DescribeRouteServersRequest) : Types::DescribeRouteServersResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_ROUTE_SERVERS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeRouteServersResult, "DescribeRouteServers")
      end

      # Describes your route tables. The default is to describe all your route tables. Alternatively, you
      # can specify specific route table IDs or filter the results to include only the route tables that
      # match specific criteria. Each subnet in your VPC must be associated with a route table. If a subnet
      # is not explicitly associated with any route table, it is implicitly associated with the main route
      # table. This command does not return the subnet ID for implicit associations. For more information,
      # see Route tables in the Amazon VPC User Guide .

      def describe_route_tables(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        route_table_ids : Array(String)? = nil
      ) : Types::DescribeRouteTablesResult

        input = Types::DescribeRouteTablesRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, route_table_ids: route_table_ids)
        describe_route_tables(input)
      end

      def describe_route_tables(input : Types::DescribeRouteTablesRequest) : Types::DescribeRouteTablesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_ROUTE_TABLES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeRouteTablesResult, "DescribeRouteTables")
      end

      # Finds available schedules that meet the specified criteria. You can search for an available schedule
      # no more than 3 months in advance. You must meet the minimum required duration of 1,200 hours per
      # year. For example, the minimum daily schedule is 4 hours, the minimum weekly schedule is 24 hours,
      # and the minimum monthly schedule is 100 hours. After you find a schedule that meets your needs, call
      # PurchaseScheduledInstances to purchase Scheduled Instances with that schedule.

      def describe_scheduled_instance_availability(
        first_slot_start_time_range : Types::SlotDateTimeRangeRequest,
        recurrence : Types::ScheduledInstanceRecurrenceRequest,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        max_slot_duration_in_hours : Int32? = nil,
        min_slot_duration_in_hours : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeScheduledInstanceAvailabilityResult

        input = Types::DescribeScheduledInstanceAvailabilityRequest.new(first_slot_start_time_range: first_slot_start_time_range, recurrence: recurrence, dry_run: dry_run, filters: filters, max_results: max_results, max_slot_duration_in_hours: max_slot_duration_in_hours, min_slot_duration_in_hours: min_slot_duration_in_hours, next_token: next_token)
        describe_scheduled_instance_availability(input)
      end

      def describe_scheduled_instance_availability(input : Types::DescribeScheduledInstanceAvailabilityRequest) : Types::DescribeScheduledInstanceAvailabilityResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_SCHEDULED_INSTANCE_AVAILABILITY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeScheduledInstanceAvailabilityResult, "DescribeScheduledInstanceAvailability")
      end

      # Describes the specified Scheduled Instances or all your Scheduled Instances.

      def describe_scheduled_instances(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        scheduled_instance_ids : Array(String)? = nil,
        slot_start_time_range : Types::SlotStartTimeRangeRequest? = nil
      ) : Types::DescribeScheduledInstancesResult

        input = Types::DescribeScheduledInstancesRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, scheduled_instance_ids: scheduled_instance_ids, slot_start_time_range: slot_start_time_range)
        describe_scheduled_instances(input)
      end

      def describe_scheduled_instances(input : Types::DescribeScheduledInstancesRequest) : Types::DescribeScheduledInstancesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_SCHEDULED_INSTANCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeScheduledInstancesResult, "DescribeScheduledInstances")
      end

      # Describes the VPCs on the other side of a VPC peering or Transit Gateway connection that are
      # referencing the security groups you've specified in this request.

      def describe_security_group_references(
        group_id : Array(String),
        dry_run : Bool? = nil
      ) : Types::DescribeSecurityGroupReferencesResult

        input = Types::DescribeSecurityGroupReferencesRequest.new(group_id: group_id, dry_run: dry_run)
        describe_security_group_references(input)
      end

      def describe_security_group_references(input : Types::DescribeSecurityGroupReferencesRequest) : Types::DescribeSecurityGroupReferencesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_SECURITY_GROUP_REFERENCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeSecurityGroupReferencesResult, "DescribeSecurityGroupReferences")
      end

      # Describes one or more of your security group rules.

      def describe_security_group_rules(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        security_group_rule_ids : Array(String)? = nil
      ) : Types::DescribeSecurityGroupRulesResult

        input = Types::DescribeSecurityGroupRulesRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, security_group_rule_ids: security_group_rule_ids)
        describe_security_group_rules(input)
      end

      def describe_security_group_rules(input : Types::DescribeSecurityGroupRulesRequest) : Types::DescribeSecurityGroupRulesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_SECURITY_GROUP_RULES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeSecurityGroupRulesResult, "DescribeSecurityGroupRules")
      end

      # Describes security group VPC associations made with AssociateSecurityGroupVpc .

      def describe_security_group_vpc_associations(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeSecurityGroupVpcAssociationsResult

        input = Types::DescribeSecurityGroupVpcAssociationsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_security_group_vpc_associations(input)
      end

      def describe_security_group_vpc_associations(input : Types::DescribeSecurityGroupVpcAssociationsRequest) : Types::DescribeSecurityGroupVpcAssociationsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_SECURITY_GROUP_VPC_ASSOCIATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeSecurityGroupVpcAssociationsResult, "DescribeSecurityGroupVpcAssociations")
      end

      # Describes the specified security groups or all of your security groups.

      def describe_security_groups(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        group_ids : Array(String)? = nil,
        group_names : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeSecurityGroupsResult

        input = Types::DescribeSecurityGroupsRequest.new(dry_run: dry_run, filters: filters, group_ids: group_ids, group_names: group_names, max_results: max_results, next_token: next_token)
        describe_security_groups(input)
      end

      def describe_security_groups(input : Types::DescribeSecurityGroupsRequest) : Types::DescribeSecurityGroupsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_SECURITY_GROUPS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeSecurityGroupsResult, "DescribeSecurityGroups")
      end

      # Describes the Outpost service link virtual interfaces.

      def describe_service_link_virtual_interfaces(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        service_link_virtual_interface_ids : Array(String)? = nil
      ) : Types::DescribeServiceLinkVirtualInterfacesResult

        input = Types::DescribeServiceLinkVirtualInterfacesRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, service_link_virtual_interface_ids: service_link_virtual_interface_ids)
        describe_service_link_virtual_interfaces(input)
      end

      def describe_service_link_virtual_interfaces(input : Types::DescribeServiceLinkVirtualInterfacesRequest) : Types::DescribeServiceLinkVirtualInterfacesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_SERVICE_LINK_VIRTUAL_INTERFACES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeServiceLinkVirtualInterfacesResult, "DescribeServiceLinkVirtualInterfaces")
      end

      # Describes the specified attribute of the specified snapshot. You can specify only one attribute at a
      # time. For more information about EBS snapshots, see Amazon EBS snapshots in the Amazon EBS User
      # Guide .

      def describe_snapshot_attribute(
        attribute : String,
        snapshot_id : String,
        dry_run : Bool? = nil
      ) : Types::DescribeSnapshotAttributeResult

        input = Types::DescribeSnapshotAttributeRequest.new(attribute: attribute, snapshot_id: snapshot_id, dry_run: dry_run)
        describe_snapshot_attribute(input)
      end

      def describe_snapshot_attribute(input : Types::DescribeSnapshotAttributeRequest) : Types::DescribeSnapshotAttributeResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_SNAPSHOT_ATTRIBUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeSnapshotAttributeResult, "DescribeSnapshotAttribute")
      end

      # Describes the storage tier status of one or more Amazon EBS snapshots.

      def describe_snapshot_tier_status(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeSnapshotTierStatusResult

        input = Types::DescribeSnapshotTierStatusRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_snapshot_tier_status(input)
      end

      def describe_snapshot_tier_status(input : Types::DescribeSnapshotTierStatusRequest) : Types::DescribeSnapshotTierStatusResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_SNAPSHOT_TIER_STATUS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeSnapshotTierStatusResult, "DescribeSnapshotTierStatus")
      end

      # Describes the specified EBS snapshots available to you or all of the EBS snapshots available to you.
      # The snapshots available to you include public snapshots, private snapshots that you own, and private
      # snapshots owned by other Amazon Web Services accounts for which you have explicit create volume
      # permissions. The create volume permissions fall into the following categories: public : The owner of
      # the snapshot granted create volume permissions for the snapshot to the all group. All Amazon Web
      # Services accounts have create volume permissions for these snapshots. explicit : The owner of the
      # snapshot granted create volume permissions to a specific Amazon Web Services account. implicit : An
      # Amazon Web Services account has implicit create volume permissions for all snapshots it owns. The
      # list of snapshots returned can be filtered by specifying snapshot IDs, snapshot owners, or Amazon
      # Web Services accounts with create volume permissions. If no options are specified, Amazon EC2
      # returns all snapshots for which you have create volume permissions. If you specify one or more
      # snapshot IDs, only snapshots that have the specified IDs are returned. If you specify an invalid
      # snapshot ID, an error is returned. If you specify a snapshot ID for which you do not have access, it
      # is not included in the returned results. If you specify one or more snapshot owners using the
      # OwnerIds option, only snapshots from the specified owners and for which you have access are
      # returned. The results can include the Amazon Web Services account IDs of the specified owners,
      # amazon for snapshots owned by Amazon, or self for snapshots that you own. If you specify a list of
      # restorable users, only snapshots with create snapshot permissions for those users are returned. You
      # can specify Amazon Web Services account IDs (if you own the snapshots), self for snapshots for which
      # you own or have explicit permissions, or all for public snapshots. If you are describing a long list
      # of snapshots, we recommend that you paginate the output to make the list more manageable. For more
      # information, see Pagination . For more information about EBS snapshots, see Amazon EBS snapshots in
      # the Amazon EBS User Guide . We strongly recommend using only paginated requests. Unpaginated
      # requests are susceptible to throttling and timeouts.

      def describe_snapshots(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        owner_ids : Array(String)? = nil,
        restorable_by_user_ids : Array(String)? = nil,
        snapshot_ids : Array(String)? = nil
      ) : Types::DescribeSnapshotsResult

        input = Types::DescribeSnapshotsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, owner_ids: owner_ids, restorable_by_user_ids: restorable_by_user_ids, snapshot_ids: snapshot_ids)
        describe_snapshots(input)
      end

      def describe_snapshots(input : Types::DescribeSnapshotsRequest) : Types::DescribeSnapshotsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_SNAPSHOTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeSnapshotsResult, "DescribeSnapshots")
      end

      # Describes the data feed for Spot Instances. For more information, see Spot Instance data feed in the
      # Amazon EC2 User Guide .

      def describe_spot_datafeed_subscription(
        dry_run : Bool? = nil
      ) : Types::DescribeSpotDatafeedSubscriptionResult

        input = Types::DescribeSpotDatafeedSubscriptionRequest.new(dry_run: dry_run)
        describe_spot_datafeed_subscription(input)
      end

      def describe_spot_datafeed_subscription(input : Types::DescribeSpotDatafeedSubscriptionRequest) : Types::DescribeSpotDatafeedSubscriptionResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_SPOT_DATAFEED_SUBSCRIPTION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeSpotDatafeedSubscriptionResult, "DescribeSpotDatafeedSubscription")
      end

      # Describes the running instances for the specified Spot Fleet.

      def describe_spot_fleet_instances(
        spot_fleet_request_id : String,
        dry_run : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeSpotFleetInstancesResponse

        input = Types::DescribeSpotFleetInstancesRequest.new(spot_fleet_request_id: spot_fleet_request_id, dry_run: dry_run, max_results: max_results, next_token: next_token)
        describe_spot_fleet_instances(input)
      end

      def describe_spot_fleet_instances(input : Types::DescribeSpotFleetInstancesRequest) : Types::DescribeSpotFleetInstancesResponse
        request = Protocol::Ec2.build_request(Model::DESCRIBE_SPOT_FLEET_INSTANCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeSpotFleetInstancesResponse, "DescribeSpotFleetInstances")
      end

      # Describes the events for the specified Spot Fleet request during the specified time. Spot Fleet
      # events are delayed by up to 30 seconds before they can be described. This ensures that you can query
      # by the last evaluated time and not miss a recorded event. Spot Fleet events are available for 48
      # hours. For more information, see Monitor fleet events using Amazon EventBridge in the Amazon EC2
      # User Guide .

      def describe_spot_fleet_request_history(
        spot_fleet_request_id : String,
        start_time : Time,
        dry_run : Bool? = nil,
        event_type : String? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeSpotFleetRequestHistoryResponse

        input = Types::DescribeSpotFleetRequestHistoryRequest.new(spot_fleet_request_id: spot_fleet_request_id, start_time: start_time, dry_run: dry_run, event_type: event_type, max_results: max_results, next_token: next_token)
        describe_spot_fleet_request_history(input)
      end

      def describe_spot_fleet_request_history(input : Types::DescribeSpotFleetRequestHistoryRequest) : Types::DescribeSpotFleetRequestHistoryResponse
        request = Protocol::Ec2.build_request(Model::DESCRIBE_SPOT_FLEET_REQUEST_HISTORY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeSpotFleetRequestHistoryResponse, "DescribeSpotFleetRequestHistory")
      end

      # Describes your Spot Fleet requests. Spot Fleet requests are deleted 48 hours after they are canceled
      # and their instances are terminated.

      def describe_spot_fleet_requests(
        dry_run : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        spot_fleet_request_ids : Array(String)? = nil
      ) : Types::DescribeSpotFleetRequestsResponse

        input = Types::DescribeSpotFleetRequestsRequest.new(dry_run: dry_run, max_results: max_results, next_token: next_token, spot_fleet_request_ids: spot_fleet_request_ids)
        describe_spot_fleet_requests(input)
      end

      def describe_spot_fleet_requests(input : Types::DescribeSpotFleetRequestsRequest) : Types::DescribeSpotFleetRequestsResponse
        request = Protocol::Ec2.build_request(Model::DESCRIBE_SPOT_FLEET_REQUESTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeSpotFleetRequestsResponse, "DescribeSpotFleetRequests")
      end

      # Describes the specified Spot Instance requests. You can use DescribeSpotInstanceRequests to find a
      # running Spot Instance by examining the response. If the status of the Spot Instance is fulfilled ,
      # the instance ID appears in the response and contains the identifier of the instance. Alternatively,
      # you can use DescribeInstances with a filter to look for instances where the instance lifecycle is
      # spot . We recommend that you set MaxResults to a value between 5 and 1000 to limit the number of
      # items returned. This paginates the output, which makes the list more manageable and returns the
      # items faster. If the list of items exceeds your MaxResults value, then that number of items is
      # returned along with a NextToken value that can be passed to a subsequent
      # DescribeSpotInstanceRequests request to retrieve the remaining items. Spot Instance requests are
      # deleted four hours after they are canceled and their instances are terminated.

      def describe_spot_instance_requests(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        spot_instance_request_ids : Array(String)? = nil
      ) : Types::DescribeSpotInstanceRequestsResult

        input = Types::DescribeSpotInstanceRequestsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, spot_instance_request_ids: spot_instance_request_ids)
        describe_spot_instance_requests(input)
      end

      def describe_spot_instance_requests(input : Types::DescribeSpotInstanceRequestsRequest) : Types::DescribeSpotInstanceRequestsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_SPOT_INSTANCE_REQUESTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeSpotInstanceRequestsResult, "DescribeSpotInstanceRequests")
      end

      # Describes the Spot price history. For more information, see Spot Instance pricing history in the
      # Amazon EC2 User Guide . When you specify a start and end time, the operation returns the prices of
      # the instance types within that time range. It also returns the last price change before the start
      # time, which is the effective price as of the start time.

      def describe_spot_price_history(
        availability_zone : String? = nil,
        availability_zone_id : String? = nil,
        dry_run : Bool? = nil,
        end_time : Time? = nil,
        filters : Array(Types::Filter)? = nil,
        instance_types : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        product_descriptions : Array(String)? = nil,
        start_time : Time? = nil
      ) : Types::DescribeSpotPriceHistoryResult

        input = Types::DescribeSpotPriceHistoryRequest.new(availability_zone: availability_zone, availability_zone_id: availability_zone_id, dry_run: dry_run, end_time: end_time, filters: filters, instance_types: instance_types, max_results: max_results, next_token: next_token, product_descriptions: product_descriptions, start_time: start_time)
        describe_spot_price_history(input)
      end

      def describe_spot_price_history(input : Types::DescribeSpotPriceHistoryRequest) : Types::DescribeSpotPriceHistoryResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_SPOT_PRICE_HISTORY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeSpotPriceHistoryResult, "DescribeSpotPriceHistory")
      end

      # Describes the stale security group rules for security groups referenced across a VPC peering
      # connection, transit gateway connection, or with a security group VPC association. Rules are stale
      # when they reference a deleted security group. Rules can also be stale if they reference a security
      # group in a peer VPC for which the VPC peering connection has been deleted, across a transit gateway
      # where the transit gateway has been deleted (or the transit gateway security group referencing
      # feature has been disabled), or if a security group VPC association has been disassociated.

      def describe_stale_security_groups(
        vpc_id : String,
        dry_run : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeStaleSecurityGroupsResult

        input = Types::DescribeStaleSecurityGroupsRequest.new(vpc_id: vpc_id, dry_run: dry_run, max_results: max_results, next_token: next_token)
        describe_stale_security_groups(input)
      end

      def describe_stale_security_groups(input : Types::DescribeStaleSecurityGroupsRequest) : Types::DescribeStaleSecurityGroupsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_STALE_SECURITY_GROUPS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeStaleSecurityGroupsResult, "DescribeStaleSecurityGroups")
      end

      # Describes the progress of the AMI store tasks. You can describe the store tasks for specified AMIs.
      # If you don't specify the AMIs, you get a paginated list of store tasks from the last 31 days. For
      # each AMI task, the response indicates if the task is InProgress , Completed , or Failed . For tasks
      # InProgress , the response shows the estimated progress as a percentage. Tasks are listed in reverse
      # chronological order. Currently, only tasks from the past 31 days can be viewed. To use this API, you
      # must have the required permissions. For more information, see Permissions for storing and restoring
      # AMIs using S3 in the Amazon EC2 User Guide . For more information, see Store and restore an AMI
      # using S3 in the Amazon EC2 User Guide .

      def describe_store_image_tasks(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        image_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeStoreImageTasksResult

        input = Types::DescribeStoreImageTasksRequest.new(dry_run: dry_run, filters: filters, image_ids: image_ids, max_results: max_results, next_token: next_token)
        describe_store_image_tasks(input)
      end

      def describe_store_image_tasks(input : Types::DescribeStoreImageTasksRequest) : Types::DescribeStoreImageTasksResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_STORE_IMAGE_TASKS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeStoreImageTasksResult, "DescribeStoreImageTasks")
      end

      # Describes your subnets. The default is to describe all your subnets. Alternatively, you can specify
      # specific subnet IDs or filter the results to include only the subnets that match specific criteria.
      # For more information, see Subnets in the Amazon VPC User Guide .

      def describe_subnets(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        subnet_ids : Array(String)? = nil
      ) : Types::DescribeSubnetsResult

        input = Types::DescribeSubnetsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, subnet_ids: subnet_ids)
        describe_subnets(input)
      end

      def describe_subnets(input : Types::DescribeSubnetsRequest) : Types::DescribeSubnetsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_SUBNETS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeSubnetsResult, "DescribeSubnets")
      end

      # Describes the specified tags for your EC2 resources. For more information about tags, see Tag your
      # Amazon EC2 resources in the Amazon Elastic Compute Cloud User Guide . We strongly recommend using
      # only paginated requests. Unpaginated requests are susceptible to throttling and timeouts. The order
      # of the elements in the response, including those within nested structures, might vary. Applications
      # should not assume the elements appear in a particular order.

      def describe_tags(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeTagsResult

        input = Types::DescribeTagsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_tags(input)
      end

      def describe_tags(input : Types::DescribeTagsRequest) : Types::DescribeTagsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_TAGS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeTagsResult, "DescribeTags")
      end

      # Describe traffic mirror filters that determine the traffic that is mirrored.

      def describe_traffic_mirror_filter_rules(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        traffic_mirror_filter_id : String? = nil,
        traffic_mirror_filter_rule_ids : Array(String)? = nil
      ) : Types::DescribeTrafficMirrorFilterRulesResult

        input = Types::DescribeTrafficMirrorFilterRulesRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, traffic_mirror_filter_id: traffic_mirror_filter_id, traffic_mirror_filter_rule_ids: traffic_mirror_filter_rule_ids)
        describe_traffic_mirror_filter_rules(input)
      end

      def describe_traffic_mirror_filter_rules(input : Types::DescribeTrafficMirrorFilterRulesRequest) : Types::DescribeTrafficMirrorFilterRulesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_TRAFFIC_MIRROR_FILTER_RULES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeTrafficMirrorFilterRulesResult, "DescribeTrafficMirrorFilterRules")
      end

      # Describes one or more Traffic Mirror filters.

      def describe_traffic_mirror_filters(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        traffic_mirror_filter_ids : Array(String)? = nil
      ) : Types::DescribeTrafficMirrorFiltersResult

        input = Types::DescribeTrafficMirrorFiltersRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, traffic_mirror_filter_ids: traffic_mirror_filter_ids)
        describe_traffic_mirror_filters(input)
      end

      def describe_traffic_mirror_filters(input : Types::DescribeTrafficMirrorFiltersRequest) : Types::DescribeTrafficMirrorFiltersResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_TRAFFIC_MIRROR_FILTERS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeTrafficMirrorFiltersResult, "DescribeTrafficMirrorFilters")
      end

      # Describes one or more Traffic Mirror sessions. By default, all Traffic Mirror sessions are
      # described. Alternatively, you can filter the results.

      def describe_traffic_mirror_sessions(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        traffic_mirror_session_ids : Array(String)? = nil
      ) : Types::DescribeTrafficMirrorSessionsResult

        input = Types::DescribeTrafficMirrorSessionsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, traffic_mirror_session_ids: traffic_mirror_session_ids)
        describe_traffic_mirror_sessions(input)
      end

      def describe_traffic_mirror_sessions(input : Types::DescribeTrafficMirrorSessionsRequest) : Types::DescribeTrafficMirrorSessionsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_TRAFFIC_MIRROR_SESSIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeTrafficMirrorSessionsResult, "DescribeTrafficMirrorSessions")
      end

      # Information about one or more Traffic Mirror targets.

      def describe_traffic_mirror_targets(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        traffic_mirror_target_ids : Array(String)? = nil
      ) : Types::DescribeTrafficMirrorTargetsResult

        input = Types::DescribeTrafficMirrorTargetsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, traffic_mirror_target_ids: traffic_mirror_target_ids)
        describe_traffic_mirror_targets(input)
      end

      def describe_traffic_mirror_targets(input : Types::DescribeTrafficMirrorTargetsRequest) : Types::DescribeTrafficMirrorTargetsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_TRAFFIC_MIRROR_TARGETS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeTrafficMirrorTargetsResult, "DescribeTrafficMirrorTargets")
      end

      # Describes one or more attachments between resources and transit gateways. By default, all
      # attachments are described. Alternatively, you can filter the results by attachment ID, attachment
      # state, resource ID, or resource owner.

      def describe_transit_gateway_attachments(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        transit_gateway_attachment_ids : Array(String)? = nil
      ) : Types::DescribeTransitGatewayAttachmentsResult

        input = Types::DescribeTransitGatewayAttachmentsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, transit_gateway_attachment_ids: transit_gateway_attachment_ids)
        describe_transit_gateway_attachments(input)
      end

      def describe_transit_gateway_attachments(input : Types::DescribeTransitGatewayAttachmentsRequest) : Types::DescribeTransitGatewayAttachmentsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_TRANSIT_GATEWAY_ATTACHMENTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeTransitGatewayAttachmentsResult, "DescribeTransitGatewayAttachments")
      end

      # Describes one or more Connect peers.

      def describe_transit_gateway_connect_peers(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        transit_gateway_connect_peer_ids : Array(String)? = nil
      ) : Types::DescribeTransitGatewayConnectPeersResult

        input = Types::DescribeTransitGatewayConnectPeersRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, transit_gateway_connect_peer_ids: transit_gateway_connect_peer_ids)
        describe_transit_gateway_connect_peers(input)
      end

      def describe_transit_gateway_connect_peers(input : Types::DescribeTransitGatewayConnectPeersRequest) : Types::DescribeTransitGatewayConnectPeersResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_TRANSIT_GATEWAY_CONNECT_PEERS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeTransitGatewayConnectPeersResult, "DescribeTransitGatewayConnectPeers")
      end

      # Describes one or more Connect attachments.

      def describe_transit_gateway_connects(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        transit_gateway_attachment_ids : Array(String)? = nil
      ) : Types::DescribeTransitGatewayConnectsResult

        input = Types::DescribeTransitGatewayConnectsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, transit_gateway_attachment_ids: transit_gateway_attachment_ids)
        describe_transit_gateway_connects(input)
      end

      def describe_transit_gateway_connects(input : Types::DescribeTransitGatewayConnectsRequest) : Types::DescribeTransitGatewayConnectsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_TRANSIT_GATEWAY_CONNECTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeTransitGatewayConnectsResult, "DescribeTransitGatewayConnects")
      end

      # Describes one or more transit gateway metering policies.

      def describe_transit_gateway_metering_policies(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        transit_gateway_metering_policy_ids : Array(String)? = nil
      ) : Types::DescribeTransitGatewayMeteringPoliciesResult

        input = Types::DescribeTransitGatewayMeteringPoliciesRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, transit_gateway_metering_policy_ids: transit_gateway_metering_policy_ids)
        describe_transit_gateway_metering_policies(input)
      end

      def describe_transit_gateway_metering_policies(input : Types::DescribeTransitGatewayMeteringPoliciesRequest) : Types::DescribeTransitGatewayMeteringPoliciesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_TRANSIT_GATEWAY_METERING_POLICIES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeTransitGatewayMeteringPoliciesResult, "DescribeTransitGatewayMeteringPolicies")
      end

      # Describes one or more transit gateway multicast domains.

      def describe_transit_gateway_multicast_domains(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        transit_gateway_multicast_domain_ids : Array(String)? = nil
      ) : Types::DescribeTransitGatewayMulticastDomainsResult

        input = Types::DescribeTransitGatewayMulticastDomainsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, transit_gateway_multicast_domain_ids: transit_gateway_multicast_domain_ids)
        describe_transit_gateway_multicast_domains(input)
      end

      def describe_transit_gateway_multicast_domains(input : Types::DescribeTransitGatewayMulticastDomainsRequest) : Types::DescribeTransitGatewayMulticastDomainsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_TRANSIT_GATEWAY_MULTICAST_DOMAINS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeTransitGatewayMulticastDomainsResult, "DescribeTransitGatewayMulticastDomains")
      end

      # Describes your transit gateway peering attachments.

      def describe_transit_gateway_peering_attachments(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        transit_gateway_attachment_ids : Array(String)? = nil
      ) : Types::DescribeTransitGatewayPeeringAttachmentsResult

        input = Types::DescribeTransitGatewayPeeringAttachmentsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, transit_gateway_attachment_ids: transit_gateway_attachment_ids)
        describe_transit_gateway_peering_attachments(input)
      end

      def describe_transit_gateway_peering_attachments(input : Types::DescribeTransitGatewayPeeringAttachmentsRequest) : Types::DescribeTransitGatewayPeeringAttachmentsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_TRANSIT_GATEWAY_PEERING_ATTACHMENTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeTransitGatewayPeeringAttachmentsResult, "DescribeTransitGatewayPeeringAttachments")
      end

      # Describes one or more transit gateway route policy tables.

      def describe_transit_gateway_policy_tables(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        transit_gateway_policy_table_ids : Array(String)? = nil
      ) : Types::DescribeTransitGatewayPolicyTablesResult

        input = Types::DescribeTransitGatewayPolicyTablesRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, transit_gateway_policy_table_ids: transit_gateway_policy_table_ids)
        describe_transit_gateway_policy_tables(input)
      end

      def describe_transit_gateway_policy_tables(input : Types::DescribeTransitGatewayPolicyTablesRequest) : Types::DescribeTransitGatewayPolicyTablesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_TRANSIT_GATEWAY_POLICY_TABLES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeTransitGatewayPolicyTablesResult, "DescribeTransitGatewayPolicyTables")
      end

      # Describes one or more transit gateway route table advertisements.

      def describe_transit_gateway_route_table_announcements(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        transit_gateway_route_table_announcement_ids : Array(String)? = nil
      ) : Types::DescribeTransitGatewayRouteTableAnnouncementsResult

        input = Types::DescribeTransitGatewayRouteTableAnnouncementsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, transit_gateway_route_table_announcement_ids: transit_gateway_route_table_announcement_ids)
        describe_transit_gateway_route_table_announcements(input)
      end

      def describe_transit_gateway_route_table_announcements(input : Types::DescribeTransitGatewayRouteTableAnnouncementsRequest) : Types::DescribeTransitGatewayRouteTableAnnouncementsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_TRANSIT_GATEWAY_ROUTE_TABLE_ANNOUNCEMENTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeTransitGatewayRouteTableAnnouncementsResult, "DescribeTransitGatewayRouteTableAnnouncements")
      end

      # Describes one or more transit gateway route tables. By default, all transit gateway route tables are
      # described. Alternatively, you can filter the results.

      def describe_transit_gateway_route_tables(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        transit_gateway_route_table_ids : Array(String)? = nil
      ) : Types::DescribeTransitGatewayRouteTablesResult

        input = Types::DescribeTransitGatewayRouteTablesRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, transit_gateway_route_table_ids: transit_gateway_route_table_ids)
        describe_transit_gateway_route_tables(input)
      end

      def describe_transit_gateway_route_tables(input : Types::DescribeTransitGatewayRouteTablesRequest) : Types::DescribeTransitGatewayRouteTablesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_TRANSIT_GATEWAY_ROUTE_TABLES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeTransitGatewayRouteTablesResult, "DescribeTransitGatewayRouteTables")
      end

      # Describes one or more VPC attachments. By default, all VPC attachments are described. Alternatively,
      # you can filter the results.

      def describe_transit_gateway_vpc_attachments(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        transit_gateway_attachment_ids : Array(String)? = nil
      ) : Types::DescribeTransitGatewayVpcAttachmentsResult

        input = Types::DescribeTransitGatewayVpcAttachmentsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, transit_gateway_attachment_ids: transit_gateway_attachment_ids)
        describe_transit_gateway_vpc_attachments(input)
      end

      def describe_transit_gateway_vpc_attachments(input : Types::DescribeTransitGatewayVpcAttachmentsRequest) : Types::DescribeTransitGatewayVpcAttachmentsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_TRANSIT_GATEWAY_VPC_ATTACHMENTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeTransitGatewayVpcAttachmentsResult, "DescribeTransitGatewayVpcAttachments")
      end

      # Describes one or more transit gateways. By default, all transit gateways are described.
      # Alternatively, you can filter the results.

      def describe_transit_gateways(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        transit_gateway_ids : Array(String)? = nil
      ) : Types::DescribeTransitGatewaysResult

        input = Types::DescribeTransitGatewaysRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, transit_gateway_ids: transit_gateway_ids)
        describe_transit_gateways(input)
      end

      def describe_transit_gateways(input : Types::DescribeTransitGatewaysRequest) : Types::DescribeTransitGatewaysResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_TRANSIT_GATEWAYS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeTransitGatewaysResult, "DescribeTransitGateways")
      end

      # Describes one or more network interface trunk associations.

      def describe_trunk_interface_associations(
        association_ids : Array(String)? = nil,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeTrunkInterfaceAssociationsResult

        input = Types::DescribeTrunkInterfaceAssociationsRequest.new(association_ids: association_ids, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_trunk_interface_associations(input)
      end

      def describe_trunk_interface_associations(input : Types::DescribeTrunkInterfaceAssociationsRequest) : Types::DescribeTrunkInterfaceAssociationsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_TRUNK_INTERFACE_ASSOCIATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeTrunkInterfaceAssociationsResult, "DescribeTrunkInterfaceAssociations")
      end

      # Describes the specified Amazon Web Services Verified Access endpoints.

      def describe_verified_access_endpoints(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        verified_access_endpoint_ids : Array(String)? = nil,
        verified_access_group_id : String? = nil,
        verified_access_instance_id : String? = nil
      ) : Types::DescribeVerifiedAccessEndpointsResult

        input = Types::DescribeVerifiedAccessEndpointsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, verified_access_endpoint_ids: verified_access_endpoint_ids, verified_access_group_id: verified_access_group_id, verified_access_instance_id: verified_access_instance_id)
        describe_verified_access_endpoints(input)
      end

      def describe_verified_access_endpoints(input : Types::DescribeVerifiedAccessEndpointsRequest) : Types::DescribeVerifiedAccessEndpointsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VERIFIED_ACCESS_ENDPOINTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVerifiedAccessEndpointsResult, "DescribeVerifiedAccessEndpoints")
      end

      # Describes the specified Verified Access groups.

      def describe_verified_access_groups(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        verified_access_group_ids : Array(String)? = nil,
        verified_access_instance_id : String? = nil
      ) : Types::DescribeVerifiedAccessGroupsResult

        input = Types::DescribeVerifiedAccessGroupsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, verified_access_group_ids: verified_access_group_ids, verified_access_instance_id: verified_access_instance_id)
        describe_verified_access_groups(input)
      end

      def describe_verified_access_groups(input : Types::DescribeVerifiedAccessGroupsRequest) : Types::DescribeVerifiedAccessGroupsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VERIFIED_ACCESS_GROUPS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVerifiedAccessGroupsResult, "DescribeVerifiedAccessGroups")
      end

      # Describes the specified Amazon Web Services Verified Access instances.

      def describe_verified_access_instance_logging_configurations(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        verified_access_instance_ids : Array(String)? = nil
      ) : Types::DescribeVerifiedAccessInstanceLoggingConfigurationsResult

        input = Types::DescribeVerifiedAccessInstanceLoggingConfigurationsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, verified_access_instance_ids: verified_access_instance_ids)
        describe_verified_access_instance_logging_configurations(input)
      end

      def describe_verified_access_instance_logging_configurations(input : Types::DescribeVerifiedAccessInstanceLoggingConfigurationsRequest) : Types::DescribeVerifiedAccessInstanceLoggingConfigurationsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VERIFIED_ACCESS_INSTANCE_LOGGING_CONFIGURATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVerifiedAccessInstanceLoggingConfigurationsResult, "DescribeVerifiedAccessInstanceLoggingConfigurations")
      end

      # Describes the specified Amazon Web Services Verified Access instances.

      def describe_verified_access_instances(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        verified_access_instance_ids : Array(String)? = nil
      ) : Types::DescribeVerifiedAccessInstancesResult

        input = Types::DescribeVerifiedAccessInstancesRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, verified_access_instance_ids: verified_access_instance_ids)
        describe_verified_access_instances(input)
      end

      def describe_verified_access_instances(input : Types::DescribeVerifiedAccessInstancesRequest) : Types::DescribeVerifiedAccessInstancesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VERIFIED_ACCESS_INSTANCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVerifiedAccessInstancesResult, "DescribeVerifiedAccessInstances")
      end

      # Describes the specified Amazon Web Services Verified Access trust providers.

      def describe_verified_access_trust_providers(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        verified_access_trust_provider_ids : Array(String)? = nil
      ) : Types::DescribeVerifiedAccessTrustProvidersResult

        input = Types::DescribeVerifiedAccessTrustProvidersRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, verified_access_trust_provider_ids: verified_access_trust_provider_ids)
        describe_verified_access_trust_providers(input)
      end

      def describe_verified_access_trust_providers(input : Types::DescribeVerifiedAccessTrustProvidersRequest) : Types::DescribeVerifiedAccessTrustProvidersResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VERIFIED_ACCESS_TRUST_PROVIDERS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVerifiedAccessTrustProvidersResult, "DescribeVerifiedAccessTrustProviders")
      end

      # Describes the specified attribute of the specified volume. You can specify only one attribute at a
      # time. For more information about EBS volumes, see Amazon EBS volumes in the Amazon EBS User Guide .

      def describe_volume_attribute(
        attribute : String,
        volume_id : String,
        dry_run : Bool? = nil
      ) : Types::DescribeVolumeAttributeResult

        input = Types::DescribeVolumeAttributeRequest.new(attribute: attribute, volume_id: volume_id, dry_run: dry_run)
        describe_volume_attribute(input)
      end

      def describe_volume_attribute(input : Types::DescribeVolumeAttributeRequest) : Types::DescribeVolumeAttributeResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VOLUME_ATTRIBUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVolumeAttributeResult, "DescribeVolumeAttribute")
      end

      # Describes the status of the specified volumes. Volume status provides the result of the checks
      # performed on your volumes to determine events that can impair the performance of your volumes. The
      # performance of a volume can be affected if an issue occurs on the volume's underlying host. If the
      # volume's underlying host experiences a power outage or system issue, after the system is restored,
      # there could be data inconsistencies on the volume. Volume events notify you if this occurs. Volume
      # actions notify you if any action needs to be taken in response to the event. The
      # DescribeVolumeStatus operation provides the following information about the specified volumes:
      # Status : Reflects the current status of the volume. The possible values are ok , impaired , warning
      # , or insufficient-data . If all checks pass, the overall status of the volume is ok . If the check
      # fails, the overall status is impaired . If the status is insufficient-data , then the checks might
      # still be taking place on your volume at the time. We recommend that you retry the request. For more
      # information about volume status, see Monitor the status of your volumes in the Amazon EBS User Guide
      # . Events : Reflect the cause of a volume status and might require you to take action. For example,
      # if your volume returns an impaired status, then the volume event might be
      # potential-data-inconsistency . This means that your volume has been affected by an issue with the
      # underlying host, has all I/O operations disabled, and might have inconsistent data. Actions :
      # Reflect the actions you might have to take in response to an event. For example, if the status of
      # the volume is impaired and the volume event shows potential-data-inconsistency , then the action
      # shows enable-volume-io . This means that you may want to enable the I/O operations for the volume
      # and then check the volume for data consistency. For more information, see Work with an impaired EBS
      # volume . Volume status is based on the volume status checks, and does not reflect the volume state.
      # Therefore, volume status does not indicate volumes in the error state (for example, when a volume is
      # incapable of accepting I/O.) The order of the elements in the response, including those within
      # nested structures, might vary. Applications should not assume the elements appear in a particular
      # order.

      def describe_volume_status(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        volume_ids : Array(String)? = nil
      ) : Types::DescribeVolumeStatusResult

        input = Types::DescribeVolumeStatusRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, volume_ids: volume_ids)
        describe_volume_status(input)
      end

      def describe_volume_status(input : Types::DescribeVolumeStatusRequest) : Types::DescribeVolumeStatusResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VOLUME_STATUS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVolumeStatusResult, "DescribeVolumeStatus")
      end

      # Describes the specified EBS volumes or all of your EBS volumes. If you are describing a long list of
      # volumes, we recommend that you paginate the output to make the list more manageable. For more
      # information, see Pagination . For more information about EBS volumes, see Amazon EBS volumes in the
      # Amazon EBS User Guide . We strongly recommend using only paginated requests. Unpaginated requests
      # are susceptible to throttling and timeouts. The order of the elements in the response, including
      # those within nested structures, might vary. Applications should not assume the elements appear in a
      # particular order.

      def describe_volumes(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        volume_ids : Array(String)? = nil
      ) : Types::DescribeVolumesResult

        input = Types::DescribeVolumesRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, volume_ids: volume_ids)
        describe_volumes(input)
      end

      def describe_volumes(input : Types::DescribeVolumesRequest) : Types::DescribeVolumesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VOLUMES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVolumesResult, "DescribeVolumes")
      end

      # Describes the most recent volume modification request for the specified EBS volumes. For more
      # information, see Monitor the progress of volume modifications in the Amazon EBS User Guide .

      def describe_volumes_modifications(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        volume_ids : Array(String)? = nil
      ) : Types::DescribeVolumesModificationsResult

        input = Types::DescribeVolumesModificationsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, volume_ids: volume_ids)
        describe_volumes_modifications(input)
      end

      def describe_volumes_modifications(input : Types::DescribeVolumesModificationsRequest) : Types::DescribeVolumesModificationsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VOLUMES_MODIFICATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVolumesModificationsResult, "DescribeVolumesModifications")
      end

      # Describes the specified attribute of the specified VPC. You can specify only one attribute at a
      # time.

      def describe_vpc_attribute(
        attribute : String,
        vpc_id : String,
        dry_run : Bool? = nil
      ) : Types::DescribeVpcAttributeResult

        input = Types::DescribeVpcAttributeRequest.new(attribute: attribute, vpc_id: vpc_id, dry_run: dry_run)
        describe_vpc_attribute(input)
      end

      def describe_vpc_attribute(input : Types::DescribeVpcAttributeRequest) : Types::DescribeVpcAttributeResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VPC_ATTRIBUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVpcAttributeResult, "DescribeVpcAttribute")
      end

      # Describe VPC Block Public Access (BPA) exclusions. A VPC BPA exclusion is a mode that can be applied
      # to a single VPC or subnet that exempts it from the account’s BPA mode and will allow bidirectional
      # or egress-only access. You can create BPA exclusions for VPCs and subnets even when BPA is not
      # enabled on the account to ensure that there is no traffic disruption to the exclusions when VPC BPA
      # is turned on. To learn more about VPC BPA, see Block public access to VPCs and subnets in the Amazon
      # VPC User Guide .

      def describe_vpc_block_public_access_exclusions(
        dry_run : Bool? = nil,
        exclusion_ids : Array(String)? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeVpcBlockPublicAccessExclusionsResult

        input = Types::DescribeVpcBlockPublicAccessExclusionsRequest.new(dry_run: dry_run, exclusion_ids: exclusion_ids, filters: filters, max_results: max_results, next_token: next_token)
        describe_vpc_block_public_access_exclusions(input)
      end

      def describe_vpc_block_public_access_exclusions(input : Types::DescribeVpcBlockPublicAccessExclusionsRequest) : Types::DescribeVpcBlockPublicAccessExclusionsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VPC_BLOCK_PUBLIC_ACCESS_EXCLUSIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVpcBlockPublicAccessExclusionsResult, "DescribeVpcBlockPublicAccessExclusions")
      end

      # Describe VPC Block Public Access (BPA) options. VPC Block Public Access (BPA) enables you to block
      # resources in VPCs and subnets that you own in a Region from reaching or being reached from the
      # internet through internet gateways and egress-only internet gateways. To learn more about VPC BPA,
      # see Block public access to VPCs and subnets in the Amazon VPC User Guide .

      def describe_vpc_block_public_access_options(
        dry_run : Bool? = nil
      ) : Types::DescribeVpcBlockPublicAccessOptionsResult

        input = Types::DescribeVpcBlockPublicAccessOptionsRequest.new(dry_run: dry_run)
        describe_vpc_block_public_access_options(input)
      end

      def describe_vpc_block_public_access_options(input : Types::DescribeVpcBlockPublicAccessOptionsRequest) : Types::DescribeVpcBlockPublicAccessOptionsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VPC_BLOCK_PUBLIC_ACCESS_OPTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVpcBlockPublicAccessOptionsResult, "DescribeVpcBlockPublicAccessOptions")
      end

      # This action is deprecated. Describes the ClassicLink status of the specified VPCs.

      def describe_vpc_classic_link(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        vpc_ids : Array(String)? = nil
      ) : Types::DescribeVpcClassicLinkResult

        input = Types::DescribeVpcClassicLinkRequest.new(dry_run: dry_run, filters: filters, vpc_ids: vpc_ids)
        describe_vpc_classic_link(input)
      end

      def describe_vpc_classic_link(input : Types::DescribeVpcClassicLinkRequest) : Types::DescribeVpcClassicLinkResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VPC_CLASSIC_LINK, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVpcClassicLinkResult, "DescribeVpcClassicLink")
      end

      # This action is deprecated. Describes the ClassicLink DNS support status of one or more VPCs. If
      # enabled, the DNS hostname of a linked EC2-Classic instance resolves to its private IP address when
      # addressed from an instance in the VPC to which it's linked. Similarly, the DNS hostname of an
      # instance in a VPC resolves to its private IP address when addressed from a linked EC2-Classic
      # instance.

      def describe_vpc_classic_link_dns_support(
        max_results : Int32? = nil,
        next_token : String? = nil,
        vpc_ids : Array(String)? = nil
      ) : Types::DescribeVpcClassicLinkDnsSupportResult

        input = Types::DescribeVpcClassicLinkDnsSupportRequest.new(max_results: max_results, next_token: next_token, vpc_ids: vpc_ids)
        describe_vpc_classic_link_dns_support(input)
      end

      def describe_vpc_classic_link_dns_support(input : Types::DescribeVpcClassicLinkDnsSupportRequest) : Types::DescribeVpcClassicLinkDnsSupportResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VPC_CLASSIC_LINK_DNS_SUPPORT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVpcClassicLinkDnsSupportResult, "DescribeVpcClassicLinkDnsSupport")
      end

      # Describes one or more VPC Encryption Control configurations. VPC Encryption Control enables you to
      # enforce encryption for all data in transit within and between VPCs to meet compliance requirements
      # You can filter the results to return information about specific encryption controls or VPCs. For
      # more information, see Enforce VPC encryption in transit in the Amazon VPC User Guide .

      def describe_vpc_encryption_controls(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        vpc_encryption_control_ids : Array(String)? = nil,
        vpc_ids : Array(String)? = nil
      ) : Types::DescribeVpcEncryptionControlsResult

        input = Types::DescribeVpcEncryptionControlsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, vpc_encryption_control_ids: vpc_encryption_control_ids, vpc_ids: vpc_ids)
        describe_vpc_encryption_controls(input)
      end

      def describe_vpc_encryption_controls(input : Types::DescribeVpcEncryptionControlsRequest) : Types::DescribeVpcEncryptionControlsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VPC_ENCRYPTION_CONTROLS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVpcEncryptionControlsResult, "DescribeVpcEncryptionControls")
      end

      # Describes the VPC resources, VPC endpoint services, Amazon Lattice services, or service networks
      # associated with the VPC endpoint.

      def describe_vpc_endpoint_associations(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        vpc_endpoint_ids : Array(String)? = nil
      ) : Types::DescribeVpcEndpointAssociationsResult

        input = Types::DescribeVpcEndpointAssociationsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, vpc_endpoint_ids: vpc_endpoint_ids)
        describe_vpc_endpoint_associations(input)
      end

      def describe_vpc_endpoint_associations(input : Types::DescribeVpcEndpointAssociationsRequest) : Types::DescribeVpcEndpointAssociationsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VPC_ENDPOINT_ASSOCIATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVpcEndpointAssociationsResult, "DescribeVpcEndpointAssociations")
      end

      # Describes the connection notifications for VPC endpoints and VPC endpoint services.

      def describe_vpc_endpoint_connection_notifications(
        connection_notification_id : String? = nil,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeVpcEndpointConnectionNotificationsResult

        input = Types::DescribeVpcEndpointConnectionNotificationsRequest.new(connection_notification_id: connection_notification_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_vpc_endpoint_connection_notifications(input)
      end

      def describe_vpc_endpoint_connection_notifications(input : Types::DescribeVpcEndpointConnectionNotificationsRequest) : Types::DescribeVpcEndpointConnectionNotificationsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VPC_ENDPOINT_CONNECTION_NOTIFICATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVpcEndpointConnectionNotificationsResult, "DescribeVpcEndpointConnectionNotifications")
      end

      # Describes the VPC endpoint connections to your VPC endpoint services, including any endpoints that
      # are pending your acceptance.

      def describe_vpc_endpoint_connections(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeVpcEndpointConnectionsResult

        input = Types::DescribeVpcEndpointConnectionsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_vpc_endpoint_connections(input)
      end

      def describe_vpc_endpoint_connections(input : Types::DescribeVpcEndpointConnectionsRequest) : Types::DescribeVpcEndpointConnectionsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VPC_ENDPOINT_CONNECTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVpcEndpointConnectionsResult, "DescribeVpcEndpointConnections")
      end

      # Describes the VPC endpoint service configurations in your account (your services).

      def describe_vpc_endpoint_service_configurations(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        service_ids : Array(String)? = nil
      ) : Types::DescribeVpcEndpointServiceConfigurationsResult

        input = Types::DescribeVpcEndpointServiceConfigurationsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, service_ids: service_ids)
        describe_vpc_endpoint_service_configurations(input)
      end

      def describe_vpc_endpoint_service_configurations(input : Types::DescribeVpcEndpointServiceConfigurationsRequest) : Types::DescribeVpcEndpointServiceConfigurationsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VPC_ENDPOINT_SERVICE_CONFIGURATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVpcEndpointServiceConfigurationsResult, "DescribeVpcEndpointServiceConfigurations")
      end

      # Describes the principals (service consumers) that are permitted to discover your VPC endpoint
      # service. Principal ARNs with path components aren't supported.

      def describe_vpc_endpoint_service_permissions(
        service_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::DescribeVpcEndpointServicePermissionsResult

        input = Types::DescribeVpcEndpointServicePermissionsRequest.new(service_id: service_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        describe_vpc_endpoint_service_permissions(input)
      end

      def describe_vpc_endpoint_service_permissions(input : Types::DescribeVpcEndpointServicePermissionsRequest) : Types::DescribeVpcEndpointServicePermissionsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VPC_ENDPOINT_SERVICE_PERMISSIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVpcEndpointServicePermissionsResult, "DescribeVpcEndpointServicePermissions")
      end

      # Describes available services to which you can create a VPC endpoint. When the service provider and
      # the consumer have different accounts in multiple Availability Zones, and the consumer views the VPC
      # endpoint service information, the response only includes the common Availability Zones. For example,
      # when the service provider account uses us-east-1a and us-east-1c and the consumer uses us-east-1a
      # and us-east-1b , the response includes the VPC endpoint services in the common Availability Zone,
      # us-east-1a .

      def describe_vpc_endpoint_services(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        service_names : Array(String)? = nil,
        service_regions : Array(String)? = nil
      ) : Types::DescribeVpcEndpointServicesResult

        input = Types::DescribeVpcEndpointServicesRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, service_names: service_names, service_regions: service_regions)
        describe_vpc_endpoint_services(input)
      end

      def describe_vpc_endpoint_services(input : Types::DescribeVpcEndpointServicesRequest) : Types::DescribeVpcEndpointServicesResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VPC_ENDPOINT_SERVICES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVpcEndpointServicesResult, "DescribeVpcEndpointServices")
      end

      # Describes your VPC endpoints. The default is to describe all your VPC endpoints. Alternatively, you
      # can specify specific VPC endpoint IDs or filter the results to include only the VPC endpoints that
      # match specific criteria.

      def describe_vpc_endpoints(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        vpc_endpoint_ids : Array(String)? = nil
      ) : Types::DescribeVpcEndpointsResult

        input = Types::DescribeVpcEndpointsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, vpc_endpoint_ids: vpc_endpoint_ids)
        describe_vpc_endpoints(input)
      end

      def describe_vpc_endpoints(input : Types::DescribeVpcEndpointsRequest) : Types::DescribeVpcEndpointsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VPC_ENDPOINTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVpcEndpointsResult, "DescribeVpcEndpoints")
      end

      # Describes your VPC peering connections. The default is to describe all your VPC peering connections.
      # Alternatively, you can specify specific VPC peering connection IDs or filter the results to include
      # only the VPC peering connections that match specific criteria.

      def describe_vpc_peering_connections(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        vpc_peering_connection_ids : Array(String)? = nil
      ) : Types::DescribeVpcPeeringConnectionsResult

        input = Types::DescribeVpcPeeringConnectionsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, vpc_peering_connection_ids: vpc_peering_connection_ids)
        describe_vpc_peering_connections(input)
      end

      def describe_vpc_peering_connections(input : Types::DescribeVpcPeeringConnectionsRequest) : Types::DescribeVpcPeeringConnectionsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VPC_PEERING_CONNECTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVpcPeeringConnectionsResult, "DescribeVpcPeeringConnections")
      end

      # Describes your VPCs. The default is to describe all your VPCs. Alternatively, you can specify
      # specific VPC IDs or filter the results to include only the VPCs that match specific criteria.

      def describe_vpcs(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        vpc_ids : Array(String)? = nil
      ) : Types::DescribeVpcsResult

        input = Types::DescribeVpcsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, vpc_ids: vpc_ids)
        describe_vpcs(input)
      end

      def describe_vpcs(input : Types::DescribeVpcsRequest) : Types::DescribeVpcsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VPCS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVpcsResult, "DescribeVpcs")
      end

      # Describes one or more of your VPN concentrators.

      def describe_vpn_concentrators(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        vpn_concentrator_ids : Array(String)? = nil
      ) : Types::DescribeVpnConcentratorsResult

        input = Types::DescribeVpnConcentratorsRequest.new(dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token, vpn_concentrator_ids: vpn_concentrator_ids)
        describe_vpn_concentrators(input)
      end

      def describe_vpn_concentrators(input : Types::DescribeVpnConcentratorsRequest) : Types::DescribeVpnConcentratorsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VPN_CONCENTRATORS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVpnConcentratorsResult, "DescribeVpnConcentrators")
      end

      # Describes one or more of your VPN connections. For more information, see Amazon Web Services
      # Site-to-Site VPN in the Amazon Web Services Site-to-Site VPN User Guide .

      def describe_vpn_connections(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        vpn_connection_ids : Array(String)? = nil
      ) : Types::DescribeVpnConnectionsResult

        input = Types::DescribeVpnConnectionsRequest.new(dry_run: dry_run, filters: filters, vpn_connection_ids: vpn_connection_ids)
        describe_vpn_connections(input)
      end

      def describe_vpn_connections(input : Types::DescribeVpnConnectionsRequest) : Types::DescribeVpnConnectionsResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VPN_CONNECTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVpnConnectionsResult, "DescribeVpnConnections")
      end

      # Describes one or more of your virtual private gateways. For more information, see Amazon Web
      # Services Site-to-Site VPN in the Amazon Web Services Site-to-Site VPN User Guide .

      def describe_vpn_gateways(
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        vpn_gateway_ids : Array(String)? = nil
      ) : Types::DescribeVpnGatewaysResult

        input = Types::DescribeVpnGatewaysRequest.new(dry_run: dry_run, filters: filters, vpn_gateway_ids: vpn_gateway_ids)
        describe_vpn_gateways(input)
      end

      def describe_vpn_gateways(input : Types::DescribeVpnGatewaysRequest) : Types::DescribeVpnGatewaysResult
        request = Protocol::Ec2.build_request(Model::DESCRIBE_VPN_GATEWAYS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DescribeVpnGatewaysResult, "DescribeVpnGateways")
      end

      # This action is deprecated. Unlinks (detaches) a linked EC2-Classic instance from a VPC. After the
      # instance has been unlinked, the VPC security groups are no longer associated with it. An instance is
      # automatically unlinked from a VPC when it's stopped.

      def detach_classic_link_vpc(
        instance_id : String,
        vpc_id : String,
        dry_run : Bool? = nil
      ) : Types::DetachClassicLinkVpcResult

        input = Types::DetachClassicLinkVpcRequest.new(instance_id: instance_id, vpc_id: vpc_id, dry_run: dry_run)
        detach_classic_link_vpc(input)
      end

      def detach_classic_link_vpc(input : Types::DetachClassicLinkVpcRequest) : Types::DetachClassicLinkVpcResult
        request = Protocol::Ec2.build_request(Model::DETACH_CLASSIC_LINK_VPC, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DetachClassicLinkVpcResult, "DetachClassicLinkVpc")
      end

      # Detaches an internet gateway from a VPC, disabling connectivity between the internet and the VPC.
      # The VPC must not contain any running instances with Elastic IP addresses or public IPv4 addresses.

      def detach_internet_gateway(
        internet_gateway_id : String,
        vpc_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::DetachInternetGatewayRequest.new(internet_gateway_id: internet_gateway_id, vpc_id: vpc_id, dry_run: dry_run)
        detach_internet_gateway(input)
      end

      def detach_internet_gateway(input : Types::DetachInternetGatewayRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DETACH_INTERNET_GATEWAY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Detaches a network interface from an instance.

      def detach_network_interface(
        attachment_id : String,
        dry_run : Bool? = nil,
        force : Bool? = nil
      ) : Nil

        input = Types::DetachNetworkInterfaceRequest.new(attachment_id: attachment_id, dry_run: dry_run, force: force)
        detach_network_interface(input)
      end

      def detach_network_interface(input : Types::DetachNetworkInterfaceRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DETACH_NETWORK_INTERFACE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Detaches the specified Amazon Web Services Verified Access trust provider from the specified Amazon
      # Web Services Verified Access instance.

      def detach_verified_access_trust_provider(
        verified_access_instance_id : String,
        verified_access_trust_provider_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil
      ) : Types::DetachVerifiedAccessTrustProviderResult

        input = Types::DetachVerifiedAccessTrustProviderRequest.new(verified_access_instance_id: verified_access_instance_id, verified_access_trust_provider_id: verified_access_trust_provider_id, client_token: client_token, dry_run: dry_run)
        detach_verified_access_trust_provider(input)
      end

      def detach_verified_access_trust_provider(input : Types::DetachVerifiedAccessTrustProviderRequest) : Types::DetachVerifiedAccessTrustProviderResult
        request = Protocol::Ec2.build_request(Model::DETACH_VERIFIED_ACCESS_TRUST_PROVIDER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DetachVerifiedAccessTrustProviderResult, "DetachVerifiedAccessTrustProvider")
      end

      # Detaches an EBS volume from an instance. Make sure to unmount any file systems on the device within
      # your operating system before detaching the volume. Failure to do so can result in the volume
      # becoming stuck in the busy state while detaching. If this happens, detachment can be delayed
      # indefinitely until you unmount the volume, force detachment, reboot the instance, or all three. If
      # an EBS volume is the root device of an instance, it can't be detached while the instance is running.
      # To detach the root volume, stop the instance first. When a volume with an Amazon Web Services
      # Marketplace product code is detached from an instance, the product code is no longer associated with
      # the instance. You can't detach or force detach volumes that are attached to Amazon Web
      # Services-managed resources. Attempting to do this results in the UnsupportedOperationException
      # exception. For more information, see Detach an Amazon EBS volume in the Amazon EBS User Guide .

      def detach_volume(
        volume_id : String,
        device : String? = nil,
        dry_run : Bool? = nil,
        force : Bool? = nil,
        instance_id : String? = nil
      ) : Types::VolumeAttachment

        input = Types::DetachVolumeRequest.new(volume_id: volume_id, device: device, dry_run: dry_run, force: force, instance_id: instance_id)
        detach_volume(input)
      end

      def detach_volume(input : Types::DetachVolumeRequest) : Types::VolumeAttachment
        request = Protocol::Ec2.build_request(Model::DETACH_VOLUME, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::VolumeAttachment, "DetachVolume")
      end

      # Detaches a virtual private gateway from a VPC. You do this if you're planning to turn off the VPC
      # and not use it anymore. You can confirm a virtual private gateway has been completely detached from
      # a VPC by describing the virtual private gateway (any attachments to the virtual private gateway are
      # also described). You must wait for the attachment's state to switch to detached before you can
      # delete the VPC or attach a different VPC to the virtual private gateway.

      def detach_vpn_gateway(
        vpc_id : String,
        vpn_gateway_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::DetachVpnGatewayRequest.new(vpc_id: vpc_id, vpn_gateway_id: vpn_gateway_id, dry_run: dry_run)
        detach_vpn_gateway(input)
      end

      def detach_vpn_gateway(input : Types::DetachVpnGatewayRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DETACH_VPN_GATEWAY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Disables Elastic IP address transfer. For more information, see Transfer Elastic IP addresses in the
      # Amazon VPC User Guide .

      def disable_address_transfer(
        allocation_id : String,
        dry_run : Bool? = nil
      ) : Types::DisableAddressTransferResult

        input = Types::DisableAddressTransferRequest.new(allocation_id: allocation_id, dry_run: dry_run)
        disable_address_transfer(input)
      end

      def disable_address_transfer(input : Types::DisableAddressTransferRequest) : Types::DisableAddressTransferResult
        request = Protocol::Ec2.build_request(Model::DISABLE_ADDRESS_TRANSFER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisableAddressTransferResult, "DisableAddressTransfer")
      end

      # Disables Allowed AMIs for your account in the specified Amazon Web Services Region. When set to
      # disabled , the image criteria in your Allowed AMIs settings do not apply, and no restrictions are
      # placed on AMI discoverability or usage. Users in your account can launch instances using any public
      # AMI or AMI shared with your account. The Allowed AMIs feature does not restrict the AMIs owned by
      # your account. Regardless of the criteria you set, the AMIs created by your account will always be
      # discoverable and usable by users in your account. For more information, see Control the discovery
      # and use of AMIs in Amazon EC2 with Allowed AMIs in Amazon EC2 User Guide .

      def disable_allowed_images_settings(
        dry_run : Bool? = nil
      ) : Types::DisableAllowedImagesSettingsResult

        input = Types::DisableAllowedImagesSettingsRequest.new(dry_run: dry_run)
        disable_allowed_images_settings(input)
      end

      def disable_allowed_images_settings(input : Types::DisableAllowedImagesSettingsRequest) : Types::DisableAllowedImagesSettingsResult
        request = Protocol::Ec2.build_request(Model::DISABLE_ALLOWED_IMAGES_SETTINGS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisableAllowedImagesSettingsResult, "DisableAllowedImagesSettings")
      end

      # Disables Infrastructure Performance metric subscriptions.

      def disable_aws_network_performance_metric_subscription(
        destination : String? = nil,
        dry_run : Bool? = nil,
        metric : String? = nil,
        source : String? = nil,
        statistic : String? = nil
      ) : Types::DisableAwsNetworkPerformanceMetricSubscriptionResult

        input = Types::DisableAwsNetworkPerformanceMetricSubscriptionRequest.new(destination: destination, dry_run: dry_run, metric: metric, source: source, statistic: statistic)
        disable_aws_network_performance_metric_subscription(input)
      end

      def disable_aws_network_performance_metric_subscription(input : Types::DisableAwsNetworkPerformanceMetricSubscriptionRequest) : Types::DisableAwsNetworkPerformanceMetricSubscriptionResult
        request = Protocol::Ec2.build_request(Model::DISABLE_AWS_NETWORK_PERFORMANCE_METRIC_SUBSCRIPTION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisableAwsNetworkPerformanceMetricSubscriptionResult, "DisableAwsNetworkPerformanceMetricSubscription")
      end

      # Disables EC2 Capacity Manager for your account. This stops data ingestion and removes access to
      # capacity analytics and optimization recommendations. Previously collected data is retained but no
      # new data will be processed.

      def disable_capacity_manager(
        client_token : String? = nil,
        dry_run : Bool? = nil
      ) : Types::DisableCapacityManagerResult

        input = Types::DisableCapacityManagerRequest.new(client_token: client_token, dry_run: dry_run)
        disable_capacity_manager(input)
      end

      def disable_capacity_manager(input : Types::DisableCapacityManagerRequest) : Types::DisableCapacityManagerResult
        request = Protocol::Ec2.build_request(Model::DISABLE_CAPACITY_MANAGER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisableCapacityManagerResult, "DisableCapacityManager")
      end

      # Disables EBS encryption by default for your account in the current Region. After you disable
      # encryption by default, you can still create encrypted volumes by enabling encryption when you create
      # each volume. Disabling encryption by default does not change the encryption status of your existing
      # volumes. For more information, see Amazon EBS encryption in the Amazon EBS User Guide .

      def disable_ebs_encryption_by_default(
        dry_run : Bool? = nil
      ) : Types::DisableEbsEncryptionByDefaultResult

        input = Types::DisableEbsEncryptionByDefaultRequest.new(dry_run: dry_run)
        disable_ebs_encryption_by_default(input)
      end

      def disable_ebs_encryption_by_default(input : Types::DisableEbsEncryptionByDefaultRequest) : Types::DisableEbsEncryptionByDefaultResult
        request = Protocol::Ec2.build_request(Model::DISABLE_EBS_ENCRYPTION_BY_DEFAULT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisableEbsEncryptionByDefaultResult, "DisableEbsEncryptionByDefault")
      end

      # Discontinue Windows fast launch for a Windows AMI, and clean up existing pre-provisioned snapshots.
      # After you disable Windows fast launch, the AMI uses the standard launch process for each new
      # instance. Amazon EC2 must remove all pre-provisioned snapshots before you can enable Windows fast
      # launch again. You can only change these settings for Windows AMIs that you own or that have been
      # shared with you.

      def disable_fast_launch(
        image_id : String,
        dry_run : Bool? = nil,
        force : Bool? = nil
      ) : Types::DisableFastLaunchResult

        input = Types::DisableFastLaunchRequest.new(image_id: image_id, dry_run: dry_run, force: force)
        disable_fast_launch(input)
      end

      def disable_fast_launch(input : Types::DisableFastLaunchRequest) : Types::DisableFastLaunchResult
        request = Protocol::Ec2.build_request(Model::DISABLE_FAST_LAUNCH, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisableFastLaunchResult, "DisableFastLaunch")
      end

      # Disables fast snapshot restores for the specified snapshots in the specified Availability Zones.

      def disable_fast_snapshot_restores(
        source_snapshot_ids : Array(String),
        availability_zone_ids : Array(String)? = nil,
        availability_zones : Array(String)? = nil,
        dry_run : Bool? = nil
      ) : Types::DisableFastSnapshotRestoresResult

        input = Types::DisableFastSnapshotRestoresRequest.new(source_snapshot_ids: source_snapshot_ids, availability_zone_ids: availability_zone_ids, availability_zones: availability_zones, dry_run: dry_run)
        disable_fast_snapshot_restores(input)
      end

      def disable_fast_snapshot_restores(input : Types::DisableFastSnapshotRestoresRequest) : Types::DisableFastSnapshotRestoresResult
        request = Protocol::Ec2.build_request(Model::DISABLE_FAST_SNAPSHOT_RESTORES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisableFastSnapshotRestoresResult, "DisableFastSnapshotRestores")
      end

      # Sets the AMI state to disabled and removes all launch permissions from the AMI. A disabled AMI can't
      # be used for instance launches. A disabled AMI can't be shared. If an AMI was public or previously
      # shared, it is made private. If an AMI was shared with an Amazon Web Services account, organization,
      # or Organizational Unit, they lose access to the disabled AMI. A disabled AMI does not appear in
      # DescribeImages API calls by default. Only the AMI owner can disable an AMI. You can re-enable a
      # disabled AMI using EnableImage . For more information, see Disable an AMI in the Amazon EC2 User
      # Guide .

      def disable_image(
        image_id : String,
        dry_run : Bool? = nil
      ) : Types::DisableImageResult

        input = Types::DisableImageRequest.new(image_id: image_id, dry_run: dry_run)
        disable_image(input)
      end

      def disable_image(input : Types::DisableImageRequest) : Types::DisableImageResult
        request = Protocol::Ec2.build_request(Model::DISABLE_IMAGE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisableImageResult, "DisableImage")
      end

      # Disables block public access for AMIs at the account level in the specified Amazon Web Services
      # Region. This removes the block public access restriction from your account. With the restriction
      # removed, you can publicly share your AMIs in the specified Amazon Web Services Region. For more
      # information, see Block public access to your AMIs in the Amazon EC2 User Guide .

      def disable_image_block_public_access(
        dry_run : Bool? = nil
      ) : Types::DisableImageBlockPublicAccessResult

        input = Types::DisableImageBlockPublicAccessRequest.new(dry_run: dry_run)
        disable_image_block_public_access(input)
      end

      def disable_image_block_public_access(input : Types::DisableImageBlockPublicAccessRequest) : Types::DisableImageBlockPublicAccessResult
        request = Protocol::Ec2.build_request(Model::DISABLE_IMAGE_BLOCK_PUBLIC_ACCESS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisableImageBlockPublicAccessResult, "DisableImageBlockPublicAccess")
      end

      # Cancels the deprecation of the specified AMI. For more information, see Deprecate an Amazon EC2 AMI
      # in the Amazon EC2 User Guide .

      def disable_image_deprecation(
        image_id : String,
        dry_run : Bool? = nil
      ) : Types::DisableImageDeprecationResult

        input = Types::DisableImageDeprecationRequest.new(image_id: image_id, dry_run: dry_run)
        disable_image_deprecation(input)
      end

      def disable_image_deprecation(input : Types::DisableImageDeprecationRequest) : Types::DisableImageDeprecationResult
        request = Protocol::Ec2.build_request(Model::DISABLE_IMAGE_DEPRECATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisableImageDeprecationResult, "DisableImageDeprecation")
      end

      # Disables deregistration protection for an AMI. When deregistration protection is disabled, the AMI
      # can be deregistered. If you chose to include a 24-hour cooldown period when you enabled
      # deregistration protection for the AMI, then, when you disable deregistration protection, you won’t
      # immediately be able to deregister the AMI. For more information, see Protect an Amazon EC2 AMI from
      # deregistration in the Amazon EC2 User Guide .

      def disable_image_deregistration_protection(
        image_id : String,
        dry_run : Bool? = nil
      ) : Types::DisableImageDeregistrationProtectionResult

        input = Types::DisableImageDeregistrationProtectionRequest.new(image_id: image_id, dry_run: dry_run)
        disable_image_deregistration_protection(input)
      end

      def disable_image_deregistration_protection(input : Types::DisableImageDeregistrationProtectionRequest) : Types::DisableImageDeregistrationProtectionResult
        request = Protocol::Ec2.build_request(Model::DISABLE_IMAGE_DEREGISTRATION_PROTECTION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisableImageDeregistrationProtectionResult, "DisableImageDeregistrationProtection")
      end

      # Disable Amazon EC2 instances running in an SQL Server High Availability cluster from SQL Server High
      # Availability instance standby detection monitoring. Once disabled, Amazon Web Services no longer
      # monitors the metadata for the instances to determine whether they are active or standby nodes in the
      # SQL Server High Availability cluster.

      def disable_instance_sql_ha_standby_detections(
        instance_ids : Array(String),
        dry_run : Bool? = nil
      ) : Types::DisableInstanceSqlHaStandbyDetectionsResult

        input = Types::DisableInstanceSqlHaStandbyDetectionsRequest.new(instance_ids: instance_ids, dry_run: dry_run)
        disable_instance_sql_ha_standby_detections(input)
      end

      def disable_instance_sql_ha_standby_detections(input : Types::DisableInstanceSqlHaStandbyDetectionsRequest) : Types::DisableInstanceSqlHaStandbyDetectionsResult
        request = Protocol::Ec2.build_request(Model::DISABLE_INSTANCE_SQL_HA_STANDBY_DETECTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisableInstanceSqlHaStandbyDetectionsResult, "DisableInstanceSqlHaStandbyDetections")
      end

      # Disable the IPAM account. For more information, see Enable integration with Organizations in the
      # Amazon VPC IPAM User Guide .

      def disable_ipam_organization_admin_account(
        delegated_admin_account_id : String,
        dry_run : Bool? = nil
      ) : Types::DisableIpamOrganizationAdminAccountResult

        input = Types::DisableIpamOrganizationAdminAccountRequest.new(delegated_admin_account_id: delegated_admin_account_id, dry_run: dry_run)
        disable_ipam_organization_admin_account(input)
      end

      def disable_ipam_organization_admin_account(input : Types::DisableIpamOrganizationAdminAccountRequest) : Types::DisableIpamOrganizationAdminAccountResult
        request = Protocol::Ec2.build_request(Model::DISABLE_IPAM_ORGANIZATION_ADMIN_ACCOUNT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisableIpamOrganizationAdminAccountResult, "DisableIpamOrganizationAdminAccount")
      end

      # Disables an IPAM policy. An IPAM policy is a set of rules that define how public IPv4 addresses from
      # IPAM pools are allocated to Amazon Web Services resources. Each rule maps an Amazon Web Services
      # service to IPAM pools that the service will use to get IP addresses. A single policy can have
      # multiple rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run out of
      # addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied to an
      # individual Amazon Web Services account or an entity within Amazon Web Services Organizations.

      def disable_ipam_policy(
        ipam_policy_id : String,
        dry_run : Bool? = nil,
        organization_target_id : String? = nil
      ) : Types::DisableIpamPolicyResult

        input = Types::DisableIpamPolicyRequest.new(ipam_policy_id: ipam_policy_id, dry_run: dry_run, organization_target_id: organization_target_id)
        disable_ipam_policy(input)
      end

      def disable_ipam_policy(input : Types::DisableIpamPolicyRequest) : Types::DisableIpamPolicyResult
        request = Protocol::Ec2.build_request(Model::DISABLE_IPAM_POLICY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisableIpamPolicyResult, "DisableIpamPolicy")
      end

      # Disables route propagation from a route server to a specified route table. When enabled, route
      # server propagation installs the routes in the FIB on the route table you've specified. Route server
      # supports IPv4 and IPv6 route propagation. Amazon VPC Route Server simplifies routing for traffic
      # between workloads that are deployed within a VPC and its internet gateways. With this feature, VPC
      # Route Server dynamically updates VPC and internet gateway route tables with your preferred IPv4 or
      # IPv6 routes to achieve routing fault tolerance for those workloads. This enables you to
      # automatically reroute traffic within a VPC, which increases the manageability of VPC routing and
      # interoperability with third-party workloads. Route server supports the follow route table types: VPC
      # route tables not associated with subnets Subnet route tables Internet gateway route tables Route
      # server does not support route tables associated with virtual private gateways. To propagate routes
      # into a transit gateway route table, use Transit Gateway Connect . For more information see Dynamic
      # routing in your VPC with VPC Route Server in the Amazon VPC User Guide .

      def disable_route_server_propagation(
        route_server_id : String,
        route_table_id : String,
        dry_run : Bool? = nil
      ) : Types::DisableRouteServerPropagationResult

        input = Types::DisableRouteServerPropagationRequest.new(route_server_id: route_server_id, route_table_id: route_table_id, dry_run: dry_run)
        disable_route_server_propagation(input)
      end

      def disable_route_server_propagation(input : Types::DisableRouteServerPropagationRequest) : Types::DisableRouteServerPropagationResult
        request = Protocol::Ec2.build_request(Model::DISABLE_ROUTE_SERVER_PROPAGATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisableRouteServerPropagationResult, "DisableRouteServerPropagation")
      end

      # Disables access to the EC2 serial console of all instances for your account. By default, access to
      # the EC2 serial console is disabled for your account. For more information, see Manage account access
      # to the EC2 serial console in the Amazon EC2 User Guide .

      def disable_serial_console_access(
        dry_run : Bool? = nil
      ) : Types::DisableSerialConsoleAccessResult

        input = Types::DisableSerialConsoleAccessRequest.new(dry_run: dry_run)
        disable_serial_console_access(input)
      end

      def disable_serial_console_access(input : Types::DisableSerialConsoleAccessRequest) : Types::DisableSerialConsoleAccessResult
        request = Protocol::Ec2.build_request(Model::DISABLE_SERIAL_CONSOLE_ACCESS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisableSerialConsoleAccessResult, "DisableSerialConsoleAccess")
      end

      # Disables the block public access for snapshots setting at the account level for the specified Amazon
      # Web Services Region. After you disable block public access for snapshots in a Region, users can
      # publicly share snapshots in that Region. Enabling block public access for snapshots in
      # block-all-sharing mode does not change the permissions for snapshots that are already publicly
      # shared. Instead, it prevents these snapshots from be publicly visible and publicly accessible.
      # Therefore, the attributes for these snapshots still indicate that they are publicly shared, even
      # though they are not publicly available. If you disable block public access , these snapshots will
      # become publicly available again. For more information, see Block public access for snapshots in the
      # Amazon EBS User Guide .

      def disable_snapshot_block_public_access(
        dry_run : Bool? = nil
      ) : Types::DisableSnapshotBlockPublicAccessResult

        input = Types::DisableSnapshotBlockPublicAccessRequest.new(dry_run: dry_run)
        disable_snapshot_block_public_access(input)
      end

      def disable_snapshot_block_public_access(input : Types::DisableSnapshotBlockPublicAccessRequest) : Types::DisableSnapshotBlockPublicAccessResult
        request = Protocol::Ec2.build_request(Model::DISABLE_SNAPSHOT_BLOCK_PUBLIC_ACCESS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisableSnapshotBlockPublicAccessResult, "DisableSnapshotBlockPublicAccess")
      end

      # Disables the specified resource attachment from propagating routes to the specified propagation
      # route table.

      def disable_transit_gateway_route_table_propagation(
        transit_gateway_route_table_id : String,
        dry_run : Bool? = nil,
        transit_gateway_attachment_id : String? = nil,
        transit_gateway_route_table_announcement_id : String? = nil
      ) : Types::DisableTransitGatewayRouteTablePropagationResult

        input = Types::DisableTransitGatewayRouteTablePropagationRequest.new(transit_gateway_route_table_id: transit_gateway_route_table_id, dry_run: dry_run, transit_gateway_attachment_id: transit_gateway_attachment_id, transit_gateway_route_table_announcement_id: transit_gateway_route_table_announcement_id)
        disable_transit_gateway_route_table_propagation(input)
      end

      def disable_transit_gateway_route_table_propagation(input : Types::DisableTransitGatewayRouteTablePropagationRequest) : Types::DisableTransitGatewayRouteTablePropagationResult
        request = Protocol::Ec2.build_request(Model::DISABLE_TRANSIT_GATEWAY_ROUTE_TABLE_PROPAGATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisableTransitGatewayRouteTablePropagationResult, "DisableTransitGatewayRouteTablePropagation")
      end

      # Disables a virtual private gateway (VGW) from propagating routes to a specified route table of a
      # VPC.

      def disable_vgw_route_propagation(
        gateway_id : String,
        route_table_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::DisableVgwRoutePropagationRequest.new(gateway_id: gateway_id, route_table_id: route_table_id, dry_run: dry_run)
        disable_vgw_route_propagation(input)
      end

      def disable_vgw_route_propagation(input : Types::DisableVgwRoutePropagationRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DISABLE_VGW_ROUTE_PROPAGATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # This action is deprecated. Disables ClassicLink for a VPC. You cannot disable ClassicLink for a VPC
      # that has EC2-Classic instances linked to it.

      def disable_vpc_classic_link(
        vpc_id : String,
        dry_run : Bool? = nil
      ) : Types::DisableVpcClassicLinkResult

        input = Types::DisableVpcClassicLinkRequest.new(vpc_id: vpc_id, dry_run: dry_run)
        disable_vpc_classic_link(input)
      end

      def disable_vpc_classic_link(input : Types::DisableVpcClassicLinkRequest) : Types::DisableVpcClassicLinkResult
        request = Protocol::Ec2.build_request(Model::DISABLE_VPC_CLASSIC_LINK, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisableVpcClassicLinkResult, "DisableVpcClassicLink")
      end

      # This action is deprecated. Disables ClassicLink DNS support for a VPC. If disabled, DNS hostnames
      # resolve to public IP addresses when addressed between a linked EC2-Classic instance and instances in
      # the VPC to which it's linked. You must specify a VPC ID in the request.

      def disable_vpc_classic_link_dns_support(
        vpc_id : String? = nil
      ) : Types::DisableVpcClassicLinkDnsSupportResult

        input = Types::DisableVpcClassicLinkDnsSupportRequest.new(vpc_id: vpc_id)
        disable_vpc_classic_link_dns_support(input)
      end

      def disable_vpc_classic_link_dns_support(input : Types::DisableVpcClassicLinkDnsSupportRequest) : Types::DisableVpcClassicLinkDnsSupportResult
        request = Protocol::Ec2.build_request(Model::DISABLE_VPC_CLASSIC_LINK_DNS_SUPPORT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisableVpcClassicLinkDnsSupportResult, "DisableVpcClassicLinkDnsSupport")
      end

      # Disassociates an Elastic IP address from the instance or network interface it's associated with.
      # This is an idempotent operation. If you perform the operation more than once, Amazon EC2 doesn't
      # return an error. An address cannot be disassociated if the all of the following conditions are met:
      # Network interface has a publicDualStackDnsName publicDnsName Public IPv4 address is the primary
      # public IPv4 address Network interface only has one remaining public IPv4 address

      def disassociate_address(
        association_id : String? = nil,
        dry_run : Bool? = nil,
        public_ip : String? = nil
      ) : Nil

        input = Types::DisassociateAddressRequest.new(association_id: association_id, dry_run: dry_run, public_ip: public_ip)
        disassociate_address(input)
      end

      def disassociate_address(input : Types::DisassociateAddressRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DISASSOCIATE_ADDRESS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Cancels a pending request to assign billing of the unused capacity of a Capacity Reservation to a
      # consumer account, or revokes a request that has already been accepted. For more information, see
      # Billing assignment for shared Amazon EC2 Capacity Reservations .

      def disassociate_capacity_reservation_billing_owner(
        capacity_reservation_id : String,
        unused_reservation_billing_owner_id : String,
        dry_run : Bool? = nil
      ) : Types::DisassociateCapacityReservationBillingOwnerResult

        input = Types::DisassociateCapacityReservationBillingOwnerRequest.new(capacity_reservation_id: capacity_reservation_id, unused_reservation_billing_owner_id: unused_reservation_billing_owner_id, dry_run: dry_run)
        disassociate_capacity_reservation_billing_owner(input)
      end

      def disassociate_capacity_reservation_billing_owner(input : Types::DisassociateCapacityReservationBillingOwnerRequest) : Types::DisassociateCapacityReservationBillingOwnerResult
        request = Protocol::Ec2.build_request(Model::DISASSOCIATE_CAPACITY_RESERVATION_BILLING_OWNER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisassociateCapacityReservationBillingOwnerResult, "DisassociateCapacityReservationBillingOwner")
      end

      # Disassociates a target network from the specified Client VPN endpoint. When you disassociate the
      # last target network from a Client VPN, the following happens: The route that was automatically added
      # for the VPC is deleted All active client connections are terminated New client connections are
      # disallowed The Client VPN endpoint's status changes to pending-associate

      def disassociate_client_vpn_target_network(
        association_id : String,
        client_vpn_endpoint_id : String,
        dry_run : Bool? = nil
      ) : Types::DisassociateClientVpnTargetNetworkResult

        input = Types::DisassociateClientVpnTargetNetworkRequest.new(association_id: association_id, client_vpn_endpoint_id: client_vpn_endpoint_id, dry_run: dry_run)
        disassociate_client_vpn_target_network(input)
      end

      def disassociate_client_vpn_target_network(input : Types::DisassociateClientVpnTargetNetworkRequest) : Types::DisassociateClientVpnTargetNetworkResult
        request = Protocol::Ec2.build_request(Model::DISASSOCIATE_CLIENT_VPN_TARGET_NETWORK, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisassociateClientVpnTargetNetworkResult, "DisassociateClientVpnTargetNetwork")
      end

      # Disassociates an IAM role from an Certificate Manager (ACM) certificate. Disassociating an IAM role
      # from an ACM certificate removes the Amazon S3 object that contains the certificate, certificate
      # chain, and encrypted private key from the Amazon S3 bucket. It also revokes the IAM role's
      # permission to use the KMS key used to encrypt the private key. This effectively revokes the role's
      # permission to use the certificate.

      def disassociate_enclave_certificate_iam_role(
        certificate_arn : String,
        role_arn : String,
        dry_run : Bool? = nil
      ) : Types::DisassociateEnclaveCertificateIamRoleResult

        input = Types::DisassociateEnclaveCertificateIamRoleRequest.new(certificate_arn: certificate_arn, role_arn: role_arn, dry_run: dry_run)
        disassociate_enclave_certificate_iam_role(input)
      end

      def disassociate_enclave_certificate_iam_role(input : Types::DisassociateEnclaveCertificateIamRoleRequest) : Types::DisassociateEnclaveCertificateIamRoleResult
        request = Protocol::Ec2.build_request(Model::DISASSOCIATE_ENCLAVE_CERTIFICATE_IAM_ROLE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisassociateEnclaveCertificateIamRoleResult, "DisassociateEnclaveCertificateIamRole")
      end

      # Disassociates an IAM instance profile from a running or stopped instance. Use
      # DescribeIamInstanceProfileAssociations to get the association ID.

      def disassociate_iam_instance_profile(
        association_id : String
      ) : Types::DisassociateIamInstanceProfileResult

        input = Types::DisassociateIamInstanceProfileRequest.new(association_id: association_id)
        disassociate_iam_instance_profile(input)
      end

      def disassociate_iam_instance_profile(input : Types::DisassociateIamInstanceProfileRequest) : Types::DisassociateIamInstanceProfileResult
        request = Protocol::Ec2.build_request(Model::DISASSOCIATE_IAM_INSTANCE_PROFILE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisassociateIamInstanceProfileResult, "DisassociateIamInstanceProfile")
      end

      # Disassociates one or more targets from an event window. For more information, see Define event
      # windows for scheduled events in the Amazon EC2 User Guide .

      def disassociate_instance_event_window(
        association_target : Types::InstanceEventWindowDisassociationRequest,
        instance_event_window_id : String,
        dry_run : Bool? = nil
      ) : Types::DisassociateInstanceEventWindowResult

        input = Types::DisassociateInstanceEventWindowRequest.new(association_target: association_target, instance_event_window_id: instance_event_window_id, dry_run: dry_run)
        disassociate_instance_event_window(input)
      end

      def disassociate_instance_event_window(input : Types::DisassociateInstanceEventWindowRequest) : Types::DisassociateInstanceEventWindowResult
        request = Protocol::Ec2.build_request(Model::DISASSOCIATE_INSTANCE_EVENT_WINDOW, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisassociateInstanceEventWindowResult, "DisassociateInstanceEventWindow")
      end

      # Remove the association between your Autonomous System Number (ASN) and your BYOIP CIDR. You may want
      # to use this action to disassociate an ASN from a CIDR or if you want to swap ASNs. For more
      # information, see Tutorial: Bring your ASN to IPAM in the Amazon VPC IPAM guide .

      def disassociate_ipam_byoasn(
        asn : String,
        cidr : String,
        dry_run : Bool? = nil
      ) : Types::DisassociateIpamByoasnResult

        input = Types::DisassociateIpamByoasnRequest.new(asn: asn, cidr: cidr, dry_run: dry_run)
        disassociate_ipam_byoasn(input)
      end

      def disassociate_ipam_byoasn(input : Types::DisassociateIpamByoasnRequest) : Types::DisassociateIpamByoasnResult
        request = Protocol::Ec2.build_request(Model::DISASSOCIATE_IPAM_BYOASN, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisassociateIpamByoasnResult, "DisassociateIpamByoasn")
      end

      # Disassociates a resource discovery from an Amazon VPC IPAM. A resource discovery is an IPAM
      # component that enables IPAM to manage and monitor resources that belong to the owning account.

      def disassociate_ipam_resource_discovery(
        ipam_resource_discovery_association_id : String,
        dry_run : Bool? = nil
      ) : Types::DisassociateIpamResourceDiscoveryResult

        input = Types::DisassociateIpamResourceDiscoveryRequest.new(ipam_resource_discovery_association_id: ipam_resource_discovery_association_id, dry_run: dry_run)
        disassociate_ipam_resource_discovery(input)
      end

      def disassociate_ipam_resource_discovery(input : Types::DisassociateIpamResourceDiscoveryRequest) : Types::DisassociateIpamResourceDiscoveryResult
        request = Protocol::Ec2.build_request(Model::DISASSOCIATE_IPAM_RESOURCE_DISCOVERY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisassociateIpamResourceDiscoveryResult, "DisassociateIpamResourceDiscovery")
      end

      # Disassociates secondary Elastic IP addresses (EIPs) from a public NAT gateway. You cannot
      # disassociate your primary EIP. For more information, see Edit secondary IP address associations in
      # the Amazon VPC User Guide . While disassociating is in progress, you cannot associate/disassociate
      # additional EIPs while the connections are being drained. You are, however, allowed to delete the NAT
      # gateway. An EIP is released only at the end of MaxDrainDurationSeconds. It stays associated and
      # supports the existing connections but does not support any new connections (new connections are
      # distributed across the remaining associated EIPs). As the existing connections drain out, the EIPs
      # (and the corresponding private IP addresses mapped to them) are released.

      def disassociate_nat_gateway_address(
        association_ids : Array(String),
        nat_gateway_id : String,
        dry_run : Bool? = nil,
        max_drain_duration_seconds : Int32? = nil
      ) : Types::DisassociateNatGatewayAddressResult

        input = Types::DisassociateNatGatewayAddressRequest.new(association_ids: association_ids, nat_gateway_id: nat_gateway_id, dry_run: dry_run, max_drain_duration_seconds: max_drain_duration_seconds)
        disassociate_nat_gateway_address(input)
      end

      def disassociate_nat_gateway_address(input : Types::DisassociateNatGatewayAddressRequest) : Types::DisassociateNatGatewayAddressResult
        request = Protocol::Ec2.build_request(Model::DISASSOCIATE_NAT_GATEWAY_ADDRESS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisassociateNatGatewayAddressResult, "DisassociateNatGatewayAddress")
      end

      # Disassociates a route server from a VPC. A route server association is the connection established
      # between a route server and a VPC. For more information see Dynamic routing in your VPC with VPC
      # Route Server in the Amazon VPC User Guide .

      def disassociate_route_server(
        route_server_id : String,
        vpc_id : String,
        dry_run : Bool? = nil
      ) : Types::DisassociateRouteServerResult

        input = Types::DisassociateRouteServerRequest.new(route_server_id: route_server_id, vpc_id: vpc_id, dry_run: dry_run)
        disassociate_route_server(input)
      end

      def disassociate_route_server(input : Types::DisassociateRouteServerRequest) : Types::DisassociateRouteServerResult
        request = Protocol::Ec2.build_request(Model::DISASSOCIATE_ROUTE_SERVER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisassociateRouteServerResult, "DisassociateRouteServer")
      end

      # Disassociates a subnet or gateway from a route table. After you perform this action, the subnet no
      # longer uses the routes in the route table. Instead, it uses the routes in the VPC's main route
      # table. For more information about route tables, see Route tables in the Amazon VPC User Guide .

      def disassociate_route_table(
        association_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::DisassociateRouteTableRequest.new(association_id: association_id, dry_run: dry_run)
        disassociate_route_table(input)
      end

      def disassociate_route_table(input : Types::DisassociateRouteTableRequest) : Nil
        request = Protocol::Ec2.build_request(Model::DISASSOCIATE_ROUTE_TABLE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Disassociates a security group from a VPC. You cannot disassociate the security group if any Elastic
      # network interfaces in the associated VPC are still associated with the security group. Note that the
      # disassociation is asynchronous and you can check the status of the request with
      # DescribeSecurityGroupVpcAssociations .

      def disassociate_security_group_vpc(
        group_id : String,
        vpc_id : String,
        dry_run : Bool? = nil
      ) : Types::DisassociateSecurityGroupVpcResult

        input = Types::DisassociateSecurityGroupVpcRequest.new(group_id: group_id, vpc_id: vpc_id, dry_run: dry_run)
        disassociate_security_group_vpc(input)
      end

      def disassociate_security_group_vpc(input : Types::DisassociateSecurityGroupVpcRequest) : Types::DisassociateSecurityGroupVpcResult
        request = Protocol::Ec2.build_request(Model::DISASSOCIATE_SECURITY_GROUP_VPC, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisassociateSecurityGroupVpcResult, "DisassociateSecurityGroupVpc")
      end

      # Disassociates a CIDR block from a subnet. Currently, you can disassociate an IPv6 CIDR block only.
      # You must detach or delete all gateways and resources that are associated with the CIDR block before
      # you can disassociate it.

      def disassociate_subnet_cidr_block(
        association_id : String
      ) : Types::DisassociateSubnetCidrBlockResult

        input = Types::DisassociateSubnetCidrBlockRequest.new(association_id: association_id)
        disassociate_subnet_cidr_block(input)
      end

      def disassociate_subnet_cidr_block(input : Types::DisassociateSubnetCidrBlockRequest) : Types::DisassociateSubnetCidrBlockResult
        request = Protocol::Ec2.build_request(Model::DISASSOCIATE_SUBNET_CIDR_BLOCK, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisassociateSubnetCidrBlockResult, "DisassociateSubnetCidrBlock")
      end

      # Disassociates the specified subnets from the transit gateway multicast domain.

      def disassociate_transit_gateway_multicast_domain(
        subnet_ids : Array(String),
        transit_gateway_attachment_id : String,
        transit_gateway_multicast_domain_id : String,
        dry_run : Bool? = nil
      ) : Types::DisassociateTransitGatewayMulticastDomainResult

        input = Types::DisassociateTransitGatewayMulticastDomainRequest.new(subnet_ids: subnet_ids, transit_gateway_attachment_id: transit_gateway_attachment_id, transit_gateway_multicast_domain_id: transit_gateway_multicast_domain_id, dry_run: dry_run)
        disassociate_transit_gateway_multicast_domain(input)
      end

      def disassociate_transit_gateway_multicast_domain(input : Types::DisassociateTransitGatewayMulticastDomainRequest) : Types::DisassociateTransitGatewayMulticastDomainResult
        request = Protocol::Ec2.build_request(Model::DISASSOCIATE_TRANSIT_GATEWAY_MULTICAST_DOMAIN, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisassociateTransitGatewayMulticastDomainResult, "DisassociateTransitGatewayMulticastDomain")
      end

      # Removes the association between an an attachment and a policy table.

      def disassociate_transit_gateway_policy_table(
        transit_gateway_attachment_id : String,
        transit_gateway_policy_table_id : String,
        dry_run : Bool? = nil
      ) : Types::DisassociateTransitGatewayPolicyTableResult

        input = Types::DisassociateTransitGatewayPolicyTableRequest.new(transit_gateway_attachment_id: transit_gateway_attachment_id, transit_gateway_policy_table_id: transit_gateway_policy_table_id, dry_run: dry_run)
        disassociate_transit_gateway_policy_table(input)
      end

      def disassociate_transit_gateway_policy_table(input : Types::DisassociateTransitGatewayPolicyTableRequest) : Types::DisassociateTransitGatewayPolicyTableResult
        request = Protocol::Ec2.build_request(Model::DISASSOCIATE_TRANSIT_GATEWAY_POLICY_TABLE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisassociateTransitGatewayPolicyTableResult, "DisassociateTransitGatewayPolicyTable")
      end

      # Disassociates a resource attachment from a transit gateway route table.

      def disassociate_transit_gateway_route_table(
        transit_gateway_attachment_id : String,
        transit_gateway_route_table_id : String,
        dry_run : Bool? = nil
      ) : Types::DisassociateTransitGatewayRouteTableResult

        input = Types::DisassociateTransitGatewayRouteTableRequest.new(transit_gateway_attachment_id: transit_gateway_attachment_id, transit_gateway_route_table_id: transit_gateway_route_table_id, dry_run: dry_run)
        disassociate_transit_gateway_route_table(input)
      end

      def disassociate_transit_gateway_route_table(input : Types::DisassociateTransitGatewayRouteTableRequest) : Types::DisassociateTransitGatewayRouteTableResult
        request = Protocol::Ec2.build_request(Model::DISASSOCIATE_TRANSIT_GATEWAY_ROUTE_TABLE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisassociateTransitGatewayRouteTableResult, "DisassociateTransitGatewayRouteTable")
      end

      # Removes an association between a branch network interface with a trunk network interface.

      def disassociate_trunk_interface(
        association_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil
      ) : Types::DisassociateTrunkInterfaceResult

        input = Types::DisassociateTrunkInterfaceRequest.new(association_id: association_id, client_token: client_token, dry_run: dry_run)
        disassociate_trunk_interface(input)
      end

      def disassociate_trunk_interface(input : Types::DisassociateTrunkInterfaceRequest) : Types::DisassociateTrunkInterfaceResult
        request = Protocol::Ec2.build_request(Model::DISASSOCIATE_TRUNK_INTERFACE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisassociateTrunkInterfaceResult, "DisassociateTrunkInterface")
      end

      # Disassociates a CIDR block from a VPC. To disassociate the CIDR block, you must specify its
      # association ID. You can get the association ID by using DescribeVpcs . You must detach or delete all
      # gateways and resources that are associated with the CIDR block before you can disassociate it. You
      # cannot disassociate the CIDR block with which you originally created the VPC (the primary CIDR
      # block).

      def disassociate_vpc_cidr_block(
        association_id : String
      ) : Types::DisassociateVpcCidrBlockResult

        input = Types::DisassociateVpcCidrBlockRequest.new(association_id: association_id)
        disassociate_vpc_cidr_block(input)
      end

      def disassociate_vpc_cidr_block(input : Types::DisassociateVpcCidrBlockRequest) : Types::DisassociateVpcCidrBlockResult
        request = Protocol::Ec2.build_request(Model::DISASSOCIATE_VPC_CIDR_BLOCK, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::DisassociateVpcCidrBlockResult, "DisassociateVpcCidrBlock")
      end

      # Enables Elastic IP address transfer. For more information, see Transfer Elastic IP addresses in the
      # Amazon VPC User Guide .

      def enable_address_transfer(
        allocation_id : String,
        transfer_account_id : String,
        dry_run : Bool? = nil
      ) : Types::EnableAddressTransferResult

        input = Types::EnableAddressTransferRequest.new(allocation_id: allocation_id, transfer_account_id: transfer_account_id, dry_run: dry_run)
        enable_address_transfer(input)
      end

      def enable_address_transfer(input : Types::EnableAddressTransferRequest) : Types::EnableAddressTransferResult
        request = Protocol::Ec2.build_request(Model::ENABLE_ADDRESS_TRANSFER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::EnableAddressTransferResult, "EnableAddressTransfer")
      end

      # Enables Allowed AMIs for your account in the specified Amazon Web Services Region. Two values are
      # accepted: enabled : The image criteria in your Allowed AMIs settings are applied. As a result, only
      # AMIs matching these criteria are discoverable and can be used by your account to launch instances.
      # audit-mode : The image criteria in your Allowed AMIs settings are not applied. No restrictions are
      # placed on AMI discoverability or usage. Users in your account can launch instances using any public
      # AMI or AMI shared with your account. The purpose of audit-mode is to indicate which AMIs will be
      # affected when Allowed AMIs is enabled . In audit-mode , each AMI displays either "ImageAllowed":
      # true or "ImageAllowed": false to indicate whether the AMI will be discoverable and available to
      # users in the account when Allowed AMIs is enabled. The Allowed AMIs feature does not restrict the
      # AMIs owned by your account. Regardless of the criteria you set, the AMIs created by your account
      # will always be discoverable and usable by users in your account. For more information, see Control
      # the discovery and use of AMIs in Amazon EC2 with Allowed AMIs in Amazon EC2 User Guide .

      def enable_allowed_images_settings(
        allowed_images_settings_state : String,
        dry_run : Bool? = nil
      ) : Types::EnableAllowedImagesSettingsResult

        input = Types::EnableAllowedImagesSettingsRequest.new(allowed_images_settings_state: allowed_images_settings_state, dry_run: dry_run)
        enable_allowed_images_settings(input)
      end

      def enable_allowed_images_settings(input : Types::EnableAllowedImagesSettingsRequest) : Types::EnableAllowedImagesSettingsResult
        request = Protocol::Ec2.build_request(Model::ENABLE_ALLOWED_IMAGES_SETTINGS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::EnableAllowedImagesSettingsResult, "EnableAllowedImagesSettings")
      end

      # Enables Infrastructure Performance subscriptions.

      def enable_aws_network_performance_metric_subscription(
        destination : String? = nil,
        dry_run : Bool? = nil,
        metric : String? = nil,
        source : String? = nil,
        statistic : String? = nil
      ) : Types::EnableAwsNetworkPerformanceMetricSubscriptionResult

        input = Types::EnableAwsNetworkPerformanceMetricSubscriptionRequest.new(destination: destination, dry_run: dry_run, metric: metric, source: source, statistic: statistic)
        enable_aws_network_performance_metric_subscription(input)
      end

      def enable_aws_network_performance_metric_subscription(input : Types::EnableAwsNetworkPerformanceMetricSubscriptionRequest) : Types::EnableAwsNetworkPerformanceMetricSubscriptionResult
        request = Protocol::Ec2.build_request(Model::ENABLE_AWS_NETWORK_PERFORMANCE_METRIC_SUBSCRIPTION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::EnableAwsNetworkPerformanceMetricSubscriptionResult, "EnableAwsNetworkPerformanceMetricSubscription")
      end

      # Enables EC2 Capacity Manager for your account. This starts data ingestion for your EC2 capacity
      # usage across On-Demand, Spot, and Capacity Reservations. Initial data processing may take several
      # hours to complete.

      def enable_capacity_manager(
        client_token : String? = nil,
        dry_run : Bool? = nil,
        organizations_access : Bool? = nil
      ) : Types::EnableCapacityManagerResult

        input = Types::EnableCapacityManagerRequest.new(client_token: client_token, dry_run: dry_run, organizations_access: organizations_access)
        enable_capacity_manager(input)
      end

      def enable_capacity_manager(input : Types::EnableCapacityManagerRequest) : Types::EnableCapacityManagerResult
        request = Protocol::Ec2.build_request(Model::ENABLE_CAPACITY_MANAGER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::EnableCapacityManagerResult, "EnableCapacityManager")
      end

      # Enables EBS encryption by default for your account in the current Region. After you enable
      # encryption by default, the EBS volumes that you create are always encrypted, either using the
      # default KMS key or the KMS key that you specified when you created each volume. For more
      # information, see Amazon EBS encryption in the Amazon EBS User Guide . Enabling encryption by default
      # has no effect on the encryption status of your existing volumes. After you enable encryption by
      # default, you can no longer launch instances using instance types that do not support encryption. For
      # more information, see Supported instance types .

      def enable_ebs_encryption_by_default(
        dry_run : Bool? = nil
      ) : Types::EnableEbsEncryptionByDefaultResult

        input = Types::EnableEbsEncryptionByDefaultRequest.new(dry_run: dry_run)
        enable_ebs_encryption_by_default(input)
      end

      def enable_ebs_encryption_by_default(input : Types::EnableEbsEncryptionByDefaultRequest) : Types::EnableEbsEncryptionByDefaultResult
        request = Protocol::Ec2.build_request(Model::ENABLE_EBS_ENCRYPTION_BY_DEFAULT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::EnableEbsEncryptionByDefaultResult, "EnableEbsEncryptionByDefault")
      end

      # When you enable Windows fast launch for a Windows AMI, images are pre-provisioned, using snapshots
      # to launch instances up to 65% faster. To create the optimized Windows image, Amazon EC2 launches an
      # instance and runs through Sysprep steps, rebooting as required. Then it creates a set of reserved
      # snapshots that are used for subsequent launches. The reserved snapshots are automatically
      # replenished as they are used, depending on your settings for launch frequency. You can only change
      # these settings for Windows AMIs that you own or that have been shared with you.

      def enable_fast_launch(
        image_id : String,
        dry_run : Bool? = nil,
        launch_template : Types::FastLaunchLaunchTemplateSpecificationRequest? = nil,
        max_parallel_launches : Int32? = nil,
        resource_type : String? = nil,
        snapshot_configuration : Types::FastLaunchSnapshotConfigurationRequest? = nil
      ) : Types::EnableFastLaunchResult

        input = Types::EnableFastLaunchRequest.new(image_id: image_id, dry_run: dry_run, launch_template: launch_template, max_parallel_launches: max_parallel_launches, resource_type: resource_type, snapshot_configuration: snapshot_configuration)
        enable_fast_launch(input)
      end

      def enable_fast_launch(input : Types::EnableFastLaunchRequest) : Types::EnableFastLaunchResult
        request = Protocol::Ec2.build_request(Model::ENABLE_FAST_LAUNCH, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::EnableFastLaunchResult, "EnableFastLaunch")
      end

      # Enables fast snapshot restores for the specified snapshots in the specified Availability Zones. You
      # get the full benefit of fast snapshot restores after they enter the enabled state. For more
      # information, see Amazon EBS fast snapshot restore in the Amazon EBS User Guide .

      def enable_fast_snapshot_restores(
        source_snapshot_ids : Array(String),
        availability_zone_ids : Array(String)? = nil,
        availability_zones : Array(String)? = nil,
        dry_run : Bool? = nil
      ) : Types::EnableFastSnapshotRestoresResult

        input = Types::EnableFastSnapshotRestoresRequest.new(source_snapshot_ids: source_snapshot_ids, availability_zone_ids: availability_zone_ids, availability_zones: availability_zones, dry_run: dry_run)
        enable_fast_snapshot_restores(input)
      end

      def enable_fast_snapshot_restores(input : Types::EnableFastSnapshotRestoresRequest) : Types::EnableFastSnapshotRestoresResult
        request = Protocol::Ec2.build_request(Model::ENABLE_FAST_SNAPSHOT_RESTORES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::EnableFastSnapshotRestoresResult, "EnableFastSnapshotRestores")
      end

      # Re-enables a disabled AMI. The re-enabled AMI is marked as available and can be used for instance
      # launches, appears in describe operations, and can be shared. Amazon Web Services accounts,
      # organizations, and Organizational Units that lost access to the AMI when it was disabled do not
      # regain access automatically. Once the AMI is available, it can be shared with them again. Only the
      # AMI owner can re-enable a disabled AMI. For more information, see Disable an Amazon EC2 AMI in the
      # Amazon EC2 User Guide .

      def enable_image(
        image_id : String,
        dry_run : Bool? = nil
      ) : Types::EnableImageResult

        input = Types::EnableImageRequest.new(image_id: image_id, dry_run: dry_run)
        enable_image(input)
      end

      def enable_image(input : Types::EnableImageRequest) : Types::EnableImageResult
        request = Protocol::Ec2.build_request(Model::ENABLE_IMAGE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::EnableImageResult, "EnableImage")
      end

      # Enables block public access for AMIs at the account level in the specified Amazon Web Services
      # Region. This prevents the public sharing of your AMIs. However, if you already have public AMIs,
      # they will remain publicly available. The API can take up to 10 minutes to configure this setting.
      # During this time, if you run GetImageBlockPublicAccessState , the response will be unblocked . When
      # the API has completed the configuration, the response will be block-new-sharing . For more
      # information, see Block public access to your AMIs in the Amazon EC2 User Guide .

      def enable_image_block_public_access(
        image_block_public_access_state : String,
        dry_run : Bool? = nil
      ) : Types::EnableImageBlockPublicAccessResult

        input = Types::EnableImageBlockPublicAccessRequest.new(image_block_public_access_state: image_block_public_access_state, dry_run: dry_run)
        enable_image_block_public_access(input)
      end

      def enable_image_block_public_access(input : Types::EnableImageBlockPublicAccessRequest) : Types::EnableImageBlockPublicAccessResult
        request = Protocol::Ec2.build_request(Model::ENABLE_IMAGE_BLOCK_PUBLIC_ACCESS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::EnableImageBlockPublicAccessResult, "EnableImageBlockPublicAccess")
      end

      # Enables deprecation of the specified AMI at the specified date and time. For more information, see
      # Deprecate an AMI in the Amazon EC2 User Guide .

      def enable_image_deprecation(
        deprecate_at : Time,
        image_id : String,
        dry_run : Bool? = nil
      ) : Types::EnableImageDeprecationResult

        input = Types::EnableImageDeprecationRequest.new(deprecate_at: deprecate_at, image_id: image_id, dry_run: dry_run)
        enable_image_deprecation(input)
      end

      def enable_image_deprecation(input : Types::EnableImageDeprecationRequest) : Types::EnableImageDeprecationResult
        request = Protocol::Ec2.build_request(Model::ENABLE_IMAGE_DEPRECATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::EnableImageDeprecationResult, "EnableImageDeprecation")
      end

      # Enables deregistration protection for an AMI. When deregistration protection is enabled, the AMI
      # can't be deregistered. To allow the AMI to be deregistered, you must first disable deregistration
      # protection. For more information, see Protect an Amazon EC2 AMI from deregistration in the Amazon
      # EC2 User Guide .

      def enable_image_deregistration_protection(
        image_id : String,
        dry_run : Bool? = nil,
        with_cooldown : Bool? = nil
      ) : Types::EnableImageDeregistrationProtectionResult

        input = Types::EnableImageDeregistrationProtectionRequest.new(image_id: image_id, dry_run: dry_run, with_cooldown: with_cooldown)
        enable_image_deregistration_protection(input)
      end

      def enable_image_deregistration_protection(input : Types::EnableImageDeregistrationProtectionRequest) : Types::EnableImageDeregistrationProtectionResult
        request = Protocol::Ec2.build_request(Model::ENABLE_IMAGE_DEREGISTRATION_PROTECTION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::EnableImageDeregistrationProtectionResult, "EnableImageDeregistrationProtection")
      end

      # Enable Amazon EC2 instances running in an SQL Server High Availability cluster for SQL Server High
      # Availability instance standby detection monitoring. Once enabled, Amazon Web Services monitors the
      # metadata for the instances to determine whether they are active or standby nodes in the SQL Server
      # High Availability cluster. If the instances are determined to be standby failover nodes, Amazon Web
      # Services automatically applies SQL Server licensing fee waiver for those instances. To register an
      # instance, it must be running a Windows SQL Server license-included AMI and have the Amazon Web
      # Services Systems Manager agent installed and running. Only Windows Server 2019 and later and SQL
      # Server (Standard and Enterprise editions) 2017 and later are supported. For more information, see
      # Prerequisites for using SQL Server High Availability instance standby detection .

      def enable_instance_sql_ha_standby_detections(
        instance_ids : Array(String),
        dry_run : Bool? = nil,
        sql_server_credentials : String? = nil
      ) : Types::EnableInstanceSqlHaStandbyDetectionsResult

        input = Types::EnableInstanceSqlHaStandbyDetectionsRequest.new(instance_ids: instance_ids, dry_run: dry_run, sql_server_credentials: sql_server_credentials)
        enable_instance_sql_ha_standby_detections(input)
      end

      def enable_instance_sql_ha_standby_detections(input : Types::EnableInstanceSqlHaStandbyDetectionsRequest) : Types::EnableInstanceSqlHaStandbyDetectionsResult
        request = Protocol::Ec2.build_request(Model::ENABLE_INSTANCE_SQL_HA_STANDBY_DETECTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::EnableInstanceSqlHaStandbyDetectionsResult, "EnableInstanceSqlHaStandbyDetections")
      end

      # Enable an Organizations member account as the IPAM admin account. You cannot select the
      # Organizations management account as the IPAM admin account. For more information, see Enable
      # integration with Organizations in the Amazon VPC IPAM User Guide .

      def enable_ipam_organization_admin_account(
        delegated_admin_account_id : String,
        dry_run : Bool? = nil
      ) : Types::EnableIpamOrganizationAdminAccountResult

        input = Types::EnableIpamOrganizationAdminAccountRequest.new(delegated_admin_account_id: delegated_admin_account_id, dry_run: dry_run)
        enable_ipam_organization_admin_account(input)
      end

      def enable_ipam_organization_admin_account(input : Types::EnableIpamOrganizationAdminAccountRequest) : Types::EnableIpamOrganizationAdminAccountResult
        request = Protocol::Ec2.build_request(Model::ENABLE_IPAM_ORGANIZATION_ADMIN_ACCOUNT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::EnableIpamOrganizationAdminAccountResult, "EnableIpamOrganizationAdminAccount")
      end

      # Enables an IPAM policy. An IPAM policy is a set of rules that define how public IPv4 addresses from
      # IPAM pools are allocated to Amazon Web Services resources. Each rule maps an Amazon Web Services
      # service to IPAM pools that the service will use to get IP addresses. A single policy can have
      # multiple rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run out of
      # addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied to an
      # individual Amazon Web Services account or an entity within Amazon Web Services Organizations. For
      # more information, see Define public IPv4 allocation strategy with IPAM policies in the Amazon VPC
      # IPAM User Guide .

      def enable_ipam_policy(
        ipam_policy_id : String,
        dry_run : Bool? = nil,
        organization_target_id : String? = nil
      ) : Types::EnableIpamPolicyResult

        input = Types::EnableIpamPolicyRequest.new(ipam_policy_id: ipam_policy_id, dry_run: dry_run, organization_target_id: organization_target_id)
        enable_ipam_policy(input)
      end

      def enable_ipam_policy(input : Types::EnableIpamPolicyRequest) : Types::EnableIpamPolicyResult
        request = Protocol::Ec2.build_request(Model::ENABLE_IPAM_POLICY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::EnableIpamPolicyResult, "EnableIpamPolicy")
      end

      # Establishes a trust relationship between Reachability Analyzer and Organizations. This operation
      # must be performed by the management account for the organization. After you establish a trust
      # relationship, a user in the management account or a delegated administrator account can run a
      # cross-account analysis using resources from the member accounts.

      def enable_reachability_analyzer_organization_sharing(
        dry_run : Bool? = nil
      ) : Types::EnableReachabilityAnalyzerOrganizationSharingResult

        input = Types::EnableReachabilityAnalyzerOrganizationSharingRequest.new(dry_run: dry_run)
        enable_reachability_analyzer_organization_sharing(input)
      end

      def enable_reachability_analyzer_organization_sharing(input : Types::EnableReachabilityAnalyzerOrganizationSharingRequest) : Types::EnableReachabilityAnalyzerOrganizationSharingResult
        request = Protocol::Ec2.build_request(Model::ENABLE_REACHABILITY_ANALYZER_ORGANIZATION_SHARING, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::EnableReachabilityAnalyzerOrganizationSharingResult, "EnableReachabilityAnalyzerOrganizationSharing")
      end

      # Defines which route tables the route server can update with routes. When enabled, route server
      # propagation installs the routes in the FIB on the route table you've specified. Route server
      # supports IPv4 and IPv6 route propagation. For more information see Dynamic routing in your VPC with
      # VPC Route Server in the Amazon VPC User Guide .

      def enable_route_server_propagation(
        route_server_id : String,
        route_table_id : String,
        dry_run : Bool? = nil
      ) : Types::EnableRouteServerPropagationResult

        input = Types::EnableRouteServerPropagationRequest.new(route_server_id: route_server_id, route_table_id: route_table_id, dry_run: dry_run)
        enable_route_server_propagation(input)
      end

      def enable_route_server_propagation(input : Types::EnableRouteServerPropagationRequest) : Types::EnableRouteServerPropagationResult
        request = Protocol::Ec2.build_request(Model::ENABLE_ROUTE_SERVER_PROPAGATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::EnableRouteServerPropagationResult, "EnableRouteServerPropagation")
      end

      # Enables access to the EC2 serial console of all instances for your account. By default, access to
      # the EC2 serial console is disabled for your account. For more information, see Manage account access
      # to the EC2 serial console in the Amazon EC2 User Guide .

      def enable_serial_console_access(
        dry_run : Bool? = nil
      ) : Types::EnableSerialConsoleAccessResult

        input = Types::EnableSerialConsoleAccessRequest.new(dry_run: dry_run)
        enable_serial_console_access(input)
      end

      def enable_serial_console_access(input : Types::EnableSerialConsoleAccessRequest) : Types::EnableSerialConsoleAccessResult
        request = Protocol::Ec2.build_request(Model::ENABLE_SERIAL_CONSOLE_ACCESS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::EnableSerialConsoleAccessResult, "EnableSerialConsoleAccess")
      end

      # Enables or modifies the block public access for snapshots setting at the account level for the
      # specified Amazon Web Services Region. After you enable block public access for snapshots in a
      # Region, users can no longer request public sharing for snapshots in that Region. Snapshots that are
      # already publicly shared are either treated as private or they remain publicly shared, depending on
      # the State that you specify. Enabling block public access for snapshots in block all sharing mode
      # does not change the permissions for snapshots that are already publicly shared. Instead, it prevents
      # these snapshots from be publicly visible and publicly accessible. Therefore, the attributes for
      # these snapshots still indicate that they are publicly shared, even though they are not publicly
      # available. If you later disable block public access or change the mode to block new sharing , these
      # snapshots will become publicly available again. For more information, see Block public access for
      # snapshots in the Amazon EBS User Guide .

      def enable_snapshot_block_public_access(
        state : String,
        dry_run : Bool? = nil
      ) : Types::EnableSnapshotBlockPublicAccessResult

        input = Types::EnableSnapshotBlockPublicAccessRequest.new(state: state, dry_run: dry_run)
        enable_snapshot_block_public_access(input)
      end

      def enable_snapshot_block_public_access(input : Types::EnableSnapshotBlockPublicAccessRequest) : Types::EnableSnapshotBlockPublicAccessResult
        request = Protocol::Ec2.build_request(Model::ENABLE_SNAPSHOT_BLOCK_PUBLIC_ACCESS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::EnableSnapshotBlockPublicAccessResult, "EnableSnapshotBlockPublicAccess")
      end

      # Enables the specified attachment to propagate routes to the specified propagation route table.

      def enable_transit_gateway_route_table_propagation(
        transit_gateway_route_table_id : String,
        dry_run : Bool? = nil,
        transit_gateway_attachment_id : String? = nil,
        transit_gateway_route_table_announcement_id : String? = nil
      ) : Types::EnableTransitGatewayRouteTablePropagationResult

        input = Types::EnableTransitGatewayRouteTablePropagationRequest.new(transit_gateway_route_table_id: transit_gateway_route_table_id, dry_run: dry_run, transit_gateway_attachment_id: transit_gateway_attachment_id, transit_gateway_route_table_announcement_id: transit_gateway_route_table_announcement_id)
        enable_transit_gateway_route_table_propagation(input)
      end

      def enable_transit_gateway_route_table_propagation(input : Types::EnableTransitGatewayRouteTablePropagationRequest) : Types::EnableTransitGatewayRouteTablePropagationResult
        request = Protocol::Ec2.build_request(Model::ENABLE_TRANSIT_GATEWAY_ROUTE_TABLE_PROPAGATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::EnableTransitGatewayRouteTablePropagationResult, "EnableTransitGatewayRouteTablePropagation")
      end

      # Enables a virtual private gateway (VGW) to propagate routes to the specified route table of a VPC.

      def enable_vgw_route_propagation(
        gateway_id : String,
        route_table_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::EnableVgwRoutePropagationRequest.new(gateway_id: gateway_id, route_table_id: route_table_id, dry_run: dry_run)
        enable_vgw_route_propagation(input)
      end

      def enable_vgw_route_propagation(input : Types::EnableVgwRoutePropagationRequest) : Nil
        request = Protocol::Ec2.build_request(Model::ENABLE_VGW_ROUTE_PROPAGATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Enables I/O operations for a volume that had I/O operations disabled because the data on the volume
      # was potentially inconsistent.

      def enable_volume_io(
        volume_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::EnableVolumeIORequest.new(volume_id: volume_id, dry_run: dry_run)
        enable_volume_io(input)
      end

      def enable_volume_io(input : Types::EnableVolumeIORequest) : Nil
        request = Protocol::Ec2.build_request(Model::ENABLE_VOLUME_IO, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # This action is deprecated. Enables a VPC for ClassicLink. You can then link EC2-Classic instances to
      # your ClassicLink-enabled VPC to allow communication over private IP addresses. You cannot enable
      # your VPC for ClassicLink if any of your VPC route tables have existing routes for address ranges
      # within the 10.0.0.0/8 IP address range, excluding local routes for VPCs in the 10.0.0.0/16 and
      # 10.1.0.0/16 IP address ranges.

      def enable_vpc_classic_link(
        vpc_id : String,
        dry_run : Bool? = nil
      ) : Types::EnableVpcClassicLinkResult

        input = Types::EnableVpcClassicLinkRequest.new(vpc_id: vpc_id, dry_run: dry_run)
        enable_vpc_classic_link(input)
      end

      def enable_vpc_classic_link(input : Types::EnableVpcClassicLinkRequest) : Types::EnableVpcClassicLinkResult
        request = Protocol::Ec2.build_request(Model::ENABLE_VPC_CLASSIC_LINK, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::EnableVpcClassicLinkResult, "EnableVpcClassicLink")
      end

      # This action is deprecated. Enables a VPC to support DNS hostname resolution for ClassicLink. If
      # enabled, the DNS hostname of a linked EC2-Classic instance resolves to its private IP address when
      # addressed from an instance in the VPC to which it's linked. Similarly, the DNS hostname of an
      # instance in a VPC resolves to its private IP address when addressed from a linked EC2-Classic
      # instance. You must specify a VPC ID in the request.

      def enable_vpc_classic_link_dns_support(
        vpc_id : String? = nil
      ) : Types::EnableVpcClassicLinkDnsSupportResult

        input = Types::EnableVpcClassicLinkDnsSupportRequest.new(vpc_id: vpc_id)
        enable_vpc_classic_link_dns_support(input)
      end

      def enable_vpc_classic_link_dns_support(input : Types::EnableVpcClassicLinkDnsSupportRequest) : Types::EnableVpcClassicLinkDnsSupportResult
        request = Protocol::Ec2.build_request(Model::ENABLE_VPC_CLASSIC_LINK_DNS_SUPPORT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::EnableVpcClassicLinkDnsSupportResult, "EnableVpcClassicLinkDnsSupport")
      end

      # Downloads the client certificate revocation list for the specified Client VPN endpoint.

      def export_client_vpn_client_certificate_revocation_list(
        client_vpn_endpoint_id : String,
        dry_run : Bool? = nil
      ) : Types::ExportClientVpnClientCertificateRevocationListResult

        input = Types::ExportClientVpnClientCertificateRevocationListRequest.new(client_vpn_endpoint_id: client_vpn_endpoint_id, dry_run: dry_run)
        export_client_vpn_client_certificate_revocation_list(input)
      end

      def export_client_vpn_client_certificate_revocation_list(input : Types::ExportClientVpnClientCertificateRevocationListRequest) : Types::ExportClientVpnClientCertificateRevocationListResult
        request = Protocol::Ec2.build_request(Model::EXPORT_CLIENT_VPN_CLIENT_CERTIFICATE_REVOCATION_LIST, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ExportClientVpnClientCertificateRevocationListResult, "ExportClientVpnClientCertificateRevocationList")
      end

      # Downloads the contents of the Client VPN endpoint configuration file for the specified Client VPN
      # endpoint. The Client VPN endpoint configuration file includes the Client VPN endpoint and
      # certificate information clients need to establish a connection with the Client VPN endpoint.

      def export_client_vpn_client_configuration(
        client_vpn_endpoint_id : String,
        dry_run : Bool? = nil
      ) : Types::ExportClientVpnClientConfigurationResult

        input = Types::ExportClientVpnClientConfigurationRequest.new(client_vpn_endpoint_id: client_vpn_endpoint_id, dry_run: dry_run)
        export_client_vpn_client_configuration(input)
      end

      def export_client_vpn_client_configuration(input : Types::ExportClientVpnClientConfigurationRequest) : Types::ExportClientVpnClientConfigurationResult
        request = Protocol::Ec2.build_request(Model::EXPORT_CLIENT_VPN_CLIENT_CONFIGURATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ExportClientVpnClientConfigurationResult, "ExportClientVpnClientConfiguration")
      end

      # Exports an Amazon Machine Image (AMI) to a VM file. For more information, see Exporting a VM
      # directly from an Amazon Machine Image (AMI) in the VM Import/Export User Guide .

      def export_image(
        disk_image_format : String,
        image_id : String,
        s3_export_location : Types::ExportTaskS3LocationRequest,
        client_token : String? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        role_name : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::ExportImageResult

        input = Types::ExportImageRequest.new(disk_image_format: disk_image_format, image_id: image_id, s3_export_location: s3_export_location, client_token: client_token, description: description, dry_run: dry_run, role_name: role_name, tag_specifications: tag_specifications)
        export_image(input)
      end

      def export_image(input : Types::ExportImageRequest) : Types::ExportImageResult
        request = Protocol::Ec2.build_request(Model::EXPORT_IMAGE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ExportImageResult, "ExportImage")
      end

      # Exports routes from the specified transit gateway route table to the specified S3 bucket. By
      # default, all routes are exported. Alternatively, you can filter by CIDR range. The routes are saved
      # to the specified bucket in a JSON file. For more information, see Export route tables to Amazon S3
      # in the Amazon Web Services Transit Gateways Guide .

      def export_transit_gateway_routes(
        s3_bucket : String,
        transit_gateway_route_table_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil
      ) : Types::ExportTransitGatewayRoutesResult

        input = Types::ExportTransitGatewayRoutesRequest.new(s3_bucket: s3_bucket, transit_gateway_route_table_id: transit_gateway_route_table_id, dry_run: dry_run, filters: filters)
        export_transit_gateway_routes(input)
      end

      def export_transit_gateway_routes(input : Types::ExportTransitGatewayRoutesRequest) : Types::ExportTransitGatewayRoutesResult
        request = Protocol::Ec2.build_request(Model::EXPORT_TRANSIT_GATEWAY_ROUTES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ExportTransitGatewayRoutesResult, "ExportTransitGatewayRoutes")
      end

      # Exports the client configuration for a Verified Access instance.

      def export_verified_access_instance_client_configuration(
        verified_access_instance_id : String,
        dry_run : Bool? = nil
      ) : Types::ExportVerifiedAccessInstanceClientConfigurationResult

        input = Types::ExportVerifiedAccessInstanceClientConfigurationRequest.new(verified_access_instance_id: verified_access_instance_id, dry_run: dry_run)
        export_verified_access_instance_client_configuration(input)
      end

      def export_verified_access_instance_client_configuration(input : Types::ExportVerifiedAccessInstanceClientConfigurationRequest) : Types::ExportVerifiedAccessInstanceClientConfigurationResult
        request = Protocol::Ec2.build_request(Model::EXPORT_VERIFIED_ACCESS_INSTANCE_CLIENT_CONFIGURATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ExportVerifiedAccessInstanceClientConfigurationResult, "ExportVerifiedAccessInstanceClientConfiguration")
      end

      # Returns the currently negotiated security parameters for an active VPN tunnel, including IKE
      # version, DH groups, encryption algorithms, and integrity algorithms.

      def get_active_vpn_tunnel_status(
        vpn_connection_id : String,
        vpn_tunnel_outside_ip_address : String,
        dry_run : Bool? = nil
      ) : Types::GetActiveVpnTunnelStatusResult

        input = Types::GetActiveVpnTunnelStatusRequest.new(vpn_connection_id: vpn_connection_id, vpn_tunnel_outside_ip_address: vpn_tunnel_outside_ip_address, dry_run: dry_run)
        get_active_vpn_tunnel_status(input)
      end

      def get_active_vpn_tunnel_status(input : Types::GetActiveVpnTunnelStatusRequest) : Types::GetActiveVpnTunnelStatusResult
        request = Protocol::Ec2.build_request(Model::GET_ACTIVE_VPN_TUNNEL_STATUS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetActiveVpnTunnelStatusResult, "GetActiveVpnTunnelStatus")
      end

      # Gets the current state of the Allowed AMIs setting and the list of Allowed AMIs criteria at the
      # account level in the specified Region. The Allowed AMIs feature does not restrict the AMIs owned by
      # your account. Regardless of the criteria you set, the AMIs created by your account will always be
      # discoverable and usable by users in your account. For more information, see Control the discovery
      # and use of AMIs in Amazon EC2 with Allowed AMIs in Amazon EC2 User Guide .

      def get_allowed_images_settings(
        dry_run : Bool? = nil
      ) : Types::GetAllowedImagesSettingsResult

        input = Types::GetAllowedImagesSettingsRequest.new(dry_run: dry_run)
        get_allowed_images_settings(input)
      end

      def get_allowed_images_settings(input : Types::GetAllowedImagesSettingsRequest) : Types::GetAllowedImagesSettingsResult
        request = Protocol::Ec2.build_request(Model::GET_ALLOWED_IMAGES_SETTINGS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetAllowedImagesSettingsResult, "GetAllowedImagesSettings")
      end

      # Returns the IAM roles that are associated with the specified ACM (ACM) certificate. It also returns
      # the name of the Amazon S3 bucket and the Amazon S3 object key where the certificate, certificate
      # chain, and encrypted private key bundle are stored, and the ARN of the KMS key that's used to
      # encrypt the private key.

      def get_associated_enclave_certificate_iam_roles(
        certificate_arn : String,
        dry_run : Bool? = nil
      ) : Types::GetAssociatedEnclaveCertificateIamRolesResult

        input = Types::GetAssociatedEnclaveCertificateIamRolesRequest.new(certificate_arn: certificate_arn, dry_run: dry_run)
        get_associated_enclave_certificate_iam_roles(input)
      end

      def get_associated_enclave_certificate_iam_roles(input : Types::GetAssociatedEnclaveCertificateIamRolesRequest) : Types::GetAssociatedEnclaveCertificateIamRolesResult
        request = Protocol::Ec2.build_request(Model::GET_ASSOCIATED_ENCLAVE_CERTIFICATE_IAM_ROLES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetAssociatedEnclaveCertificateIamRolesResult, "GetAssociatedEnclaveCertificateIamRoles")
      end

      # Gets information about the IPv6 CIDR block associations for a specified IPv6 address pool.

      def get_associated_ipv6_pool_cidrs(
        pool_id : String,
        dry_run : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetAssociatedIpv6PoolCidrsResult

        input = Types::GetAssociatedIpv6PoolCidrsRequest.new(pool_id: pool_id, dry_run: dry_run, max_results: max_results, next_token: next_token)
        get_associated_ipv6_pool_cidrs(input)
      end

      def get_associated_ipv6_pool_cidrs(input : Types::GetAssociatedIpv6PoolCidrsRequest) : Types::GetAssociatedIpv6PoolCidrsResult
        request = Protocol::Ec2.build_request(Model::GET_ASSOCIATED_IPV6_POOL_CIDRS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetAssociatedIpv6PoolCidrsResult, "GetAssociatedIpv6PoolCidrs")
      end

      # Gets network performance data.

      def get_aws_network_performance_data(
        data_queries : Array(Types::DataQuery)? = nil,
        dry_run : Bool? = nil,
        end_time : Time? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        start_time : Time? = nil
      ) : Types::GetAwsNetworkPerformanceDataResult

        input = Types::GetAwsNetworkPerformanceDataRequest.new(data_queries: data_queries, dry_run: dry_run, end_time: end_time, max_results: max_results, next_token: next_token, start_time: start_time)
        get_aws_network_performance_data(input)
      end

      def get_aws_network_performance_data(input : Types::GetAwsNetworkPerformanceDataRequest) : Types::GetAwsNetworkPerformanceDataResult
        request = Protocol::Ec2.build_request(Model::GET_AWS_NETWORK_PERFORMANCE_DATA, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetAwsNetworkPerformanceDataResult, "GetAwsNetworkPerformanceData")
      end

      # Retrieves the current configuration and status of EC2 Capacity Manager for your account, including
      # enablement status, Organizations access settings, and data ingestion status.

      def get_capacity_manager_attributes(
        dry_run : Bool? = nil
      ) : Types::GetCapacityManagerAttributesResult

        input = Types::GetCapacityManagerAttributesRequest.new(dry_run: dry_run)
        get_capacity_manager_attributes(input)
      end

      def get_capacity_manager_attributes(input : Types::GetCapacityManagerAttributesRequest) : Types::GetCapacityManagerAttributesResult
        request = Protocol::Ec2.build_request(Model::GET_CAPACITY_MANAGER_ATTRIBUTES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetCapacityManagerAttributesResult, "GetCapacityManagerAttributes")
      end

      # Retrieves capacity usage metrics for your EC2 resources. Returns time-series data for metrics like
      # unused capacity, utilization rates, and costs across On-Demand, Spot, and Capacity Reservations.
      # Data can be grouped and filtered by various dimensions such as region, account, and instance family.

      def get_capacity_manager_metric_data(
        end_time : Time,
        metric_names : Array(String),
        period : Int32,
        start_time : Time,
        dry_run : Bool? = nil,
        filter_by : Array(Types::CapacityManagerCondition)? = nil,
        group_by : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetCapacityManagerMetricDataResult

        input = Types::GetCapacityManagerMetricDataRequest.new(end_time: end_time, metric_names: metric_names, period: period, start_time: start_time, dry_run: dry_run, filter_by: filter_by, group_by: group_by, max_results: max_results, next_token: next_token)
        get_capacity_manager_metric_data(input)
      end

      def get_capacity_manager_metric_data(input : Types::GetCapacityManagerMetricDataRequest) : Types::GetCapacityManagerMetricDataResult
        request = Protocol::Ec2.build_request(Model::GET_CAPACITY_MANAGER_METRIC_DATA, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetCapacityManagerMetricDataResult, "GetCapacityManagerMetricData")
      end

      # Retrieves the available dimension values for capacity metrics within a specified time range. This is
      # useful for discovering what accounts, regions, instance families, and other dimensions have data
      # available for filtering and grouping.

      def get_capacity_manager_metric_dimensions(
        end_time : Time,
        group_by : Array(String),
        metric_names : Array(String),
        start_time : Time,
        dry_run : Bool? = nil,
        filter_by : Array(Types::CapacityManagerCondition)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetCapacityManagerMetricDimensionsResult

        input = Types::GetCapacityManagerMetricDimensionsRequest.new(end_time: end_time, group_by: group_by, metric_names: metric_names, start_time: start_time, dry_run: dry_run, filter_by: filter_by, max_results: max_results, next_token: next_token)
        get_capacity_manager_metric_dimensions(input)
      end

      def get_capacity_manager_metric_dimensions(input : Types::GetCapacityManagerMetricDimensionsRequest) : Types::GetCapacityManagerMetricDimensionsResult
        request = Protocol::Ec2.build_request(Model::GET_CAPACITY_MANAGER_METRIC_DIMENSIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetCapacityManagerMetricDimensionsResult, "GetCapacityManagerMetricDimensions")
      end

      # Gets usage information about a Capacity Reservation. If the Capacity Reservation is shared, it shows
      # usage information for the Capacity Reservation owner and each Amazon Web Services account that is
      # currently using the shared capacity. If the Capacity Reservation is not shared, it shows only the
      # Capacity Reservation owner's usage.

      def get_capacity_reservation_usage(
        capacity_reservation_id : String,
        dry_run : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetCapacityReservationUsageResult

        input = Types::GetCapacityReservationUsageRequest.new(capacity_reservation_id: capacity_reservation_id, dry_run: dry_run, max_results: max_results, next_token: next_token)
        get_capacity_reservation_usage(input)
      end

      def get_capacity_reservation_usage(input : Types::GetCapacityReservationUsageRequest) : Types::GetCapacityReservationUsageResult
        request = Protocol::Ec2.build_request(Model::GET_CAPACITY_RESERVATION_USAGE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetCapacityReservationUsageResult, "GetCapacityReservationUsage")
      end

      # Describes the allocations from the specified customer-owned address pool.

      def get_coip_pool_usage(
        pool_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetCoipPoolUsageResult

        input = Types::GetCoipPoolUsageRequest.new(pool_id: pool_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        get_coip_pool_usage(input)
      end

      def get_coip_pool_usage(input : Types::GetCoipPoolUsageRequest) : Types::GetCoipPoolUsageResult
        request = Protocol::Ec2.build_request(Model::GET_COIP_POOL_USAGE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetCoipPoolUsageResult, "GetCoipPoolUsage")
      end

      # Gets the console output for the specified instance. For Linux instances, the instance console output
      # displays the exact console output that would normally be displayed on a physical monitor attached to
      # a computer. For Windows instances, the instance console output includes the last three system event
      # log errors. For more information, see Instance console output in the Amazon EC2 User Guide .

      def get_console_output(
        instance_id : String,
        dry_run : Bool? = nil,
        latest : Bool? = nil
      ) : Types::GetConsoleOutputResult

        input = Types::GetConsoleOutputRequest.new(instance_id: instance_id, dry_run: dry_run, latest: latest)
        get_console_output(input)
      end

      def get_console_output(input : Types::GetConsoleOutputRequest) : Types::GetConsoleOutputResult
        request = Protocol::Ec2.build_request(Model::GET_CONSOLE_OUTPUT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetConsoleOutputResult, "GetConsoleOutput")
      end

      # Retrieve a JPG-format screenshot of a running instance to help with troubleshooting. The returned
      # content is Base64-encoded. For more information, see Instance console output in the Amazon EC2 User
      # Guide .

      def get_console_screenshot(
        instance_id : String,
        dry_run : Bool? = nil,
        wake_up : Bool? = nil
      ) : Types::GetConsoleScreenshotResult

        input = Types::GetConsoleScreenshotRequest.new(instance_id: instance_id, dry_run: dry_run, wake_up: wake_up)
        get_console_screenshot(input)
      end

      def get_console_screenshot(input : Types::GetConsoleScreenshotRequest) : Types::GetConsoleScreenshotResult
        request = Protocol::Ec2.build_request(Model::GET_CONSOLE_SCREENSHOT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetConsoleScreenshotResult, "GetConsoleScreenshot")
      end

      # Retrieves a summary of the account status report. To view the full report, download it from the
      # Amazon S3 bucket where it was saved. Reports are accessible only when they have the complete status.
      # Reports with other statuses ( running , cancelled , or error ) are not available in the S3 bucket.
      # For more information about downloading objects from an S3 bucket, see Downloading objects in the
      # Amazon Simple Storage Service User Guide . For more information, see Generating the account status
      # report for declarative policies in the Amazon Web Services Organizations User Guide .

      def get_declarative_policies_report_summary(
        report_id : String,
        dry_run : Bool? = nil
      ) : Types::GetDeclarativePoliciesReportSummaryResult

        input = Types::GetDeclarativePoliciesReportSummaryRequest.new(report_id: report_id, dry_run: dry_run)
        get_declarative_policies_report_summary(input)
      end

      def get_declarative_policies_report_summary(input : Types::GetDeclarativePoliciesReportSummaryRequest) : Types::GetDeclarativePoliciesReportSummaryResult
        request = Protocol::Ec2.build_request(Model::GET_DECLARATIVE_POLICIES_REPORT_SUMMARY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetDeclarativePoliciesReportSummaryResult, "GetDeclarativePoliciesReportSummary")
      end

      # Describes the default credit option for CPU usage of a burstable performance instance family. For
      # more information, see Burstable performance instances in the Amazon EC2 User Guide .

      def get_default_credit_specification(
        instance_family : String,
        dry_run : Bool? = nil
      ) : Types::GetDefaultCreditSpecificationResult

        input = Types::GetDefaultCreditSpecificationRequest.new(instance_family: instance_family, dry_run: dry_run)
        get_default_credit_specification(input)
      end

      def get_default_credit_specification(input : Types::GetDefaultCreditSpecificationRequest) : Types::GetDefaultCreditSpecificationResult
        request = Protocol::Ec2.build_request(Model::GET_DEFAULT_CREDIT_SPECIFICATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetDefaultCreditSpecificationResult, "GetDefaultCreditSpecification")
      end

      # Describes the default KMS key for EBS encryption by default for your account in this Region. For
      # more information, see Amazon EBS encryption in the Amazon EBS User Guide .

      def get_ebs_default_kms_key_id(
        dry_run : Bool? = nil
      ) : Types::GetEbsDefaultKmsKeyIdResult

        input = Types::GetEbsDefaultKmsKeyIdRequest.new(dry_run: dry_run)
        get_ebs_default_kms_key_id(input)
      end

      def get_ebs_default_kms_key_id(input : Types::GetEbsDefaultKmsKeyIdRequest) : Types::GetEbsDefaultKmsKeyIdResult
        request = Protocol::Ec2.build_request(Model::GET_EBS_DEFAULT_KMS_KEY_ID, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetEbsDefaultKmsKeyIdResult, "GetEbsDefaultKmsKeyId")
      end

      # Describes whether EBS encryption by default is enabled for your account in the current Region. For
      # more information, see Amazon EBS encryption in the Amazon EBS User Guide .

      def get_ebs_encryption_by_default(
        dry_run : Bool? = nil
      ) : Types::GetEbsEncryptionByDefaultResult

        input = Types::GetEbsEncryptionByDefaultRequest.new(dry_run: dry_run)
        get_ebs_encryption_by_default(input)
      end

      def get_ebs_encryption_by_default(input : Types::GetEbsEncryptionByDefaultRequest) : Types::GetEbsEncryptionByDefaultResult
        request = Protocol::Ec2.build_request(Model::GET_EBS_ENCRYPTION_BY_DEFAULT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetEbsEncryptionByDefaultResult, "GetEbsEncryptionByDefault")
      end

      # Gets the enabled IPAM policy. An IPAM policy is a set of rules that define how public IPv4 addresses
      # from IPAM pools are allocated to Amazon Web Services resources. Each rule maps an Amazon Web
      # Services service to IPAM pools that the service will use to get IP addresses. A single policy can
      # have multiple rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run out
      # of addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied to
      # an individual Amazon Web Services account or an entity within Amazon Web Services Organizations.

      def get_enabled_ipam_policy(
        dry_run : Bool? = nil
      ) : Types::GetEnabledIpamPolicyResult

        input = Types::GetEnabledIpamPolicyRequest.new(dry_run: dry_run)
        get_enabled_ipam_policy(input)
      end

      def get_enabled_ipam_policy(input : Types::GetEnabledIpamPolicyRequest) : Types::GetEnabledIpamPolicyResult
        request = Protocol::Ec2.build_request(Model::GET_ENABLED_IPAM_POLICY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetEnabledIpamPolicyResult, "GetEnabledIpamPolicy")
      end

      # Generates a CloudFormation template that streamlines and automates the integration of VPC flow logs
      # with Amazon Athena. This make it easier for you to query and gain insights from VPC flow logs data.
      # Based on the information that you provide, we configure resources in the template to do the
      # following: Create a table in Athena that maps fields to a custom log format Create a Lambda function
      # that updates the table with new partitions on a daily, weekly, or monthly basis Create a table
      # partitioned between two timestamps in the past Create a set of named queries in Athena that you can
      # use to get started quickly GetFlowLogsIntegrationTemplate does not support integration between
      # Amazon Web Services Transit Gateway Flow Logs and Amazon Athena.

      def get_flow_logs_integration_template(
        config_delivery_s3_destination_arn : String,
        flow_log_id : String,
        integrate_services : Types::IntegrateServices,
        dry_run : Bool? = nil
      ) : Types::GetFlowLogsIntegrationTemplateResult

        input = Types::GetFlowLogsIntegrationTemplateRequest.new(config_delivery_s3_destination_arn: config_delivery_s3_destination_arn, flow_log_id: flow_log_id, integrate_services: integrate_services, dry_run: dry_run)
        get_flow_logs_integration_template(input)
      end

      def get_flow_logs_integration_template(input : Types::GetFlowLogsIntegrationTemplateRequest) : Types::GetFlowLogsIntegrationTemplateResult
        request = Protocol::Ec2.build_request(Model::GET_FLOW_LOGS_INTEGRATION_TEMPLATE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetFlowLogsIntegrationTemplateResult, "GetFlowLogsIntegrationTemplate")
      end

      # Lists the resource groups to which a Capacity Reservation has been added.

      def get_groups_for_capacity_reservation(
        capacity_reservation_id : String,
        dry_run : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetGroupsForCapacityReservationResult

        input = Types::GetGroupsForCapacityReservationRequest.new(capacity_reservation_id: capacity_reservation_id, dry_run: dry_run, max_results: max_results, next_token: next_token)
        get_groups_for_capacity_reservation(input)
      end

      def get_groups_for_capacity_reservation(input : Types::GetGroupsForCapacityReservationRequest) : Types::GetGroupsForCapacityReservationResult
        request = Protocol::Ec2.build_request(Model::GET_GROUPS_FOR_CAPACITY_RESERVATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetGroupsForCapacityReservationResult, "GetGroupsForCapacityReservation")
      end

      # Preview a reservation purchase with configurations that match those of your Dedicated Host. You must
      # have active Dedicated Hosts in your account before you purchase a reservation. This is a preview of
      # the PurchaseHostReservation action and does not result in the offering being purchased.

      def get_host_reservation_purchase_preview(
        host_id_set : Array(String),
        offering_id : String
      ) : Types::GetHostReservationPurchasePreviewResult

        input = Types::GetHostReservationPurchasePreviewRequest.new(host_id_set: host_id_set, offering_id: offering_id)
        get_host_reservation_purchase_preview(input)
      end

      def get_host_reservation_purchase_preview(input : Types::GetHostReservationPurchasePreviewRequest) : Types::GetHostReservationPurchasePreviewResult
        request = Protocol::Ec2.build_request(Model::GET_HOST_RESERVATION_PURCHASE_PREVIEW, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetHostReservationPurchasePreviewResult, "GetHostReservationPurchasePreview")
      end

      # Retrieves the ancestry chain of the specified AMI, tracing its lineage back to the root AMI. For
      # more information, see AMI ancestry in Amazon EC2 User Guide .

      def get_image_ancestry(
        image_id : String,
        dry_run : Bool? = nil
      ) : Types::GetImageAncestryResult

        input = Types::GetImageAncestryRequest.new(image_id: image_id, dry_run: dry_run)
        get_image_ancestry(input)
      end

      def get_image_ancestry(input : Types::GetImageAncestryRequest) : Types::GetImageAncestryResult
        request = Protocol::Ec2.build_request(Model::GET_IMAGE_ANCESTRY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetImageAncestryResult, "GetImageAncestry")
      end

      # Gets the current state of block public access for AMIs at the account level in the specified Amazon
      # Web Services Region. For more information, see Block public access to your AMIs in the Amazon EC2
      # User Guide .

      def get_image_block_public_access_state(
        dry_run : Bool? = nil
      ) : Types::GetImageBlockPublicAccessStateResult

        input = Types::GetImageBlockPublicAccessStateRequest.new(dry_run: dry_run)
        get_image_block_public_access_state(input)
      end

      def get_image_block_public_access_state(input : Types::GetImageBlockPublicAccessStateRequest) : Types::GetImageBlockPublicAccessStateResult
        request = Protocol::Ec2.build_request(Model::GET_IMAGE_BLOCK_PUBLIC_ACCESS_STATE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetImageBlockPublicAccessStateResult, "GetImageBlockPublicAccessState")
      end

      # Gets the default instance metadata service (IMDS) settings that are set at the account level in the
      # specified Amazon Web Services&#x2028; Region. For more information, see Order of precedence for
      # instance metadata options in the Amazon EC2 User Guide .

      def get_instance_metadata_defaults(
        dry_run : Bool? = nil
      ) : Types::GetInstanceMetadataDefaultsResult

        input = Types::GetInstanceMetadataDefaultsRequest.new(dry_run: dry_run)
        get_instance_metadata_defaults(input)
      end

      def get_instance_metadata_defaults(input : Types::GetInstanceMetadataDefaultsRequest) : Types::GetInstanceMetadataDefaultsResult
        request = Protocol::Ec2.build_request(Model::GET_INSTANCE_METADATA_DEFAULTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetInstanceMetadataDefaultsResult, "GetInstanceMetadataDefaults")
      end

      # Gets the public endorsement key associated with the Nitro Trusted Platform Module (NitroTPM) for the
      # specified instance.

      def get_instance_tpm_ek_pub(
        instance_id : String,
        key_format : String,
        key_type : String,
        dry_run : Bool? = nil
      ) : Types::GetInstanceTpmEkPubResult

        input = Types::GetInstanceTpmEkPubRequest.new(instance_id: instance_id, key_format: key_format, key_type: key_type, dry_run: dry_run)
        get_instance_tpm_ek_pub(input)
      end

      def get_instance_tpm_ek_pub(input : Types::GetInstanceTpmEkPubRequest) : Types::GetInstanceTpmEkPubResult
        request = Protocol::Ec2.build_request(Model::GET_INSTANCE_TPM_EK_PUB, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetInstanceTpmEkPubResult, "GetInstanceTpmEkPub")
      end

      # Returns a list of instance types with the specified instance attributes. You can use the response to
      # preview the instance types without launching instances. Note that the response does not consider
      # capacity. When you specify multiple parameters, you get instance types that satisfy all of the
      # specified parameters. If you specify multiple values for a parameter, you get instance types that
      # satisfy any of the specified values. For more information, see Preview instance types with specified
      # attributes , Specify attributes for instance type selection for EC2 Fleet or Spot Fleet , and Spot
      # placement score in the Amazon EC2 User Guide , and Creating mixed instance groups using
      # attribute-based instance type selection in the Amazon EC2 Auto Scaling User Guide .

      def get_instance_types_from_instance_requirements(
        architecture_types : Array(String),
        instance_requirements : Types::InstanceRequirementsRequest,
        virtualization_types : Array(String),
        context : String? = nil,
        dry_run : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetInstanceTypesFromInstanceRequirementsResult

        input = Types::GetInstanceTypesFromInstanceRequirementsRequest.new(architecture_types: architecture_types, instance_requirements: instance_requirements, virtualization_types: virtualization_types, context: context, dry_run: dry_run, max_results: max_results, next_token: next_token)
        get_instance_types_from_instance_requirements(input)
      end

      def get_instance_types_from_instance_requirements(input : Types::GetInstanceTypesFromInstanceRequirementsRequest) : Types::GetInstanceTypesFromInstanceRequirementsResult
        request = Protocol::Ec2.build_request(Model::GET_INSTANCE_TYPES_FROM_INSTANCE_REQUIREMENTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetInstanceTypesFromInstanceRequirementsResult, "GetInstanceTypesFromInstanceRequirements")
      end

      # A binary representation of the UEFI variable store. Only non-volatile variables are stored. This is
      # a base64 encoded and zlib compressed binary value that must be properly encoded. When you use
      # register-image to create an AMI, you can create an exact copy of your variable store by passing the
      # UEFI data in the UefiData parameter. You can modify the UEFI data by using the python-uefivars tool
      # on GitHub. You can use the tool to convert the UEFI data into a human-readable format (JSON), which
      # you can inspect and modify, and then convert back into the binary format to use with register-image.
      # For more information, see UEFI Secure Boot in the Amazon EC2 User Guide .

      def get_instance_uefi_data(
        instance_id : String,
        dry_run : Bool? = nil
      ) : Types::GetInstanceUefiDataResult

        input = Types::GetInstanceUefiDataRequest.new(instance_id: instance_id, dry_run: dry_run)
        get_instance_uefi_data(input)
      end

      def get_instance_uefi_data(input : Types::GetInstanceUefiDataRequest) : Types::GetInstanceUefiDataResult
        request = Protocol::Ec2.build_request(Model::GET_INSTANCE_UEFI_DATA, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetInstanceUefiDataResult, "GetInstanceUefiData")
      end

      # Retrieve historical information about a CIDR within an IPAM scope. For more information, see View
      # the history of IP addresses in the Amazon VPC IPAM User Guide .

      def get_ipam_address_history(
        cidr : String,
        ipam_scope_id : String,
        dry_run : Bool? = nil,
        end_time : Time? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        start_time : Time? = nil,
        vpc_id : String? = nil
      ) : Types::GetIpamAddressHistoryResult

        input = Types::GetIpamAddressHistoryRequest.new(cidr: cidr, ipam_scope_id: ipam_scope_id, dry_run: dry_run, end_time: end_time, max_results: max_results, next_token: next_token, start_time: start_time, vpc_id: vpc_id)
        get_ipam_address_history(input)
      end

      def get_ipam_address_history(input : Types::GetIpamAddressHistoryRequest) : Types::GetIpamAddressHistoryResult
        request = Protocol::Ec2.build_request(Model::GET_IPAM_ADDRESS_HISTORY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetIpamAddressHistoryResult, "GetIpamAddressHistory")
      end

      # Gets IPAM discovered accounts. A discovered account is an Amazon Web Services account that is
      # monitored under a resource discovery. If you have integrated IPAM with Amazon Web Services
      # Organizations, all accounts in the organization are discovered accounts. Only the IPAM account can
      # get all discovered accounts in the organization.

      def get_ipam_discovered_accounts(
        discovery_region : String,
        ipam_resource_discovery_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetIpamDiscoveredAccountsResult

        input = Types::GetIpamDiscoveredAccountsRequest.new(discovery_region: discovery_region, ipam_resource_discovery_id: ipam_resource_discovery_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        get_ipam_discovered_accounts(input)
      end

      def get_ipam_discovered_accounts(input : Types::GetIpamDiscoveredAccountsRequest) : Types::GetIpamDiscoveredAccountsResult
        request = Protocol::Ec2.build_request(Model::GET_IPAM_DISCOVERED_ACCOUNTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetIpamDiscoveredAccountsResult, "GetIpamDiscoveredAccounts")
      end

      # Gets the public IP addresses that have been discovered by IPAM.

      def get_ipam_discovered_public_addresses(
        address_region : String,
        ipam_resource_discovery_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetIpamDiscoveredPublicAddressesResult

        input = Types::GetIpamDiscoveredPublicAddressesRequest.new(address_region: address_region, ipam_resource_discovery_id: ipam_resource_discovery_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        get_ipam_discovered_public_addresses(input)
      end

      def get_ipam_discovered_public_addresses(input : Types::GetIpamDiscoveredPublicAddressesRequest) : Types::GetIpamDiscoveredPublicAddressesResult
        request = Protocol::Ec2.build_request(Model::GET_IPAM_DISCOVERED_PUBLIC_ADDRESSES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetIpamDiscoveredPublicAddressesResult, "GetIpamDiscoveredPublicAddresses")
      end

      # Returns the resource CIDRs that are monitored as part of a resource discovery. A discovered resource
      # is a resource CIDR monitored under a resource discovery. The following resources can be discovered:
      # VPCs, Public IPv4 pools, VPC subnets, and Elastic IP addresses.

      def get_ipam_discovered_resource_cidrs(
        ipam_resource_discovery_id : String,
        resource_region : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetIpamDiscoveredResourceCidrsResult

        input = Types::GetIpamDiscoveredResourceCidrsRequest.new(ipam_resource_discovery_id: ipam_resource_discovery_id, resource_region: resource_region, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        get_ipam_discovered_resource_cidrs(input)
      end

      def get_ipam_discovered_resource_cidrs(input : Types::GetIpamDiscoveredResourceCidrsRequest) : Types::GetIpamDiscoveredResourceCidrsResult
        request = Protocol::Ec2.build_request(Model::GET_IPAM_DISCOVERED_RESOURCE_CIDRS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetIpamDiscoveredResourceCidrsResult, "GetIpamDiscoveredResourceCidrs")
      end

      # Gets the allocation rules for an IPAM policy. An IPAM policy is a set of rules that define how
      # public IPv4 addresses from IPAM pools are allocated to Amazon Web Services resources. Each rule maps
      # an Amazon Web Services service to IPAM pools that the service will use to get IP addresses. A single
      # policy can have multiple rules and be applied to multiple Amazon Web Services Regions. If the IPAM
      # pool run out of addresses then the services fallback to Amazon-provided IP addresses. A policy can
      # be applied to an individual Amazon Web Services account or an entity within Amazon Web Services
      # Organizations. Allocation rules are optional configurations within an IPAM policy that map Amazon
      # Web Services resource types to specific IPAM pools. If no rules are defined, the resource types
      # default to using Amazon-provided IP addresses.

      def get_ipam_policy_allocation_rules(
        ipam_policy_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        locale : String? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        resource_type : String? = nil
      ) : Types::GetIpamPolicyAllocationRulesResult

        input = Types::GetIpamPolicyAllocationRulesRequest.new(ipam_policy_id: ipam_policy_id, dry_run: dry_run, filters: filters, locale: locale, max_results: max_results, next_token: next_token, resource_type: resource_type)
        get_ipam_policy_allocation_rules(input)
      end

      def get_ipam_policy_allocation_rules(input : Types::GetIpamPolicyAllocationRulesRequest) : Types::GetIpamPolicyAllocationRulesResult
        request = Protocol::Ec2.build_request(Model::GET_IPAM_POLICY_ALLOCATION_RULES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetIpamPolicyAllocationRulesResult, "GetIpamPolicyAllocationRules")
      end

      # Gets the Amazon Web Services Organizations targets for an IPAM policy. An IPAM policy is a set of
      # rules that define how public IPv4 addresses from IPAM pools are allocated to Amazon Web Services
      # resources. Each rule maps an Amazon Web Services service to IPAM pools that the service will use to
      # get IP addresses. A single policy can have multiple rules and be applied to multiple Amazon Web
      # Services Regions. If the IPAM pool run out of addresses then the services fallback to
      # Amazon-provided IP addresses. A policy can be applied to an individual Amazon Web Services account
      # or an entity within Amazon Web Services Organizations. A target can be an individual Amazon Web
      # Services account or an entity within an Amazon Web Services Organization to which an IPAM policy can
      # be applied.

      def get_ipam_policy_organization_targets(
        ipam_policy_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetIpamPolicyOrganizationTargetsResult

        input = Types::GetIpamPolicyOrganizationTargetsRequest.new(ipam_policy_id: ipam_policy_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        get_ipam_policy_organization_targets(input)
      end

      def get_ipam_policy_organization_targets(input : Types::GetIpamPolicyOrganizationTargetsRequest) : Types::GetIpamPolicyOrganizationTargetsResult
        request = Protocol::Ec2.build_request(Model::GET_IPAM_POLICY_ORGANIZATION_TARGETS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetIpamPolicyOrganizationTargetsResult, "GetIpamPolicyOrganizationTargets")
      end

      # Get a list of all the CIDR allocations in an IPAM pool. The Region you use should be the IPAM pool
      # locale. The locale is the Amazon Web Services Region where this IPAM pool is available for
      # allocations. If you use this action after AllocateIpamPoolCidr or ReleaseIpamPoolAllocation , note
      # that all EC2 API actions follow an eventual consistency model.

      def get_ipam_pool_allocations(
        ipam_pool_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        ipam_pool_allocation_id : String? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetIpamPoolAllocationsResult

        input = Types::GetIpamPoolAllocationsRequest.new(ipam_pool_id: ipam_pool_id, dry_run: dry_run, filters: filters, ipam_pool_allocation_id: ipam_pool_allocation_id, max_results: max_results, next_token: next_token)
        get_ipam_pool_allocations(input)
      end

      def get_ipam_pool_allocations(input : Types::GetIpamPoolAllocationsRequest) : Types::GetIpamPoolAllocationsResult
        request = Protocol::Ec2.build_request(Model::GET_IPAM_POOL_ALLOCATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetIpamPoolAllocationsResult, "GetIpamPoolAllocations")
      end

      # Get the CIDRs provisioned to an IPAM pool.

      def get_ipam_pool_cidrs(
        ipam_pool_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetIpamPoolCidrsResult

        input = Types::GetIpamPoolCidrsRequest.new(ipam_pool_id: ipam_pool_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        get_ipam_pool_cidrs(input)
      end

      def get_ipam_pool_cidrs(input : Types::GetIpamPoolCidrsRequest) : Types::GetIpamPoolCidrsResult
        request = Protocol::Ec2.build_request(Model::GET_IPAM_POOL_CIDRS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetIpamPoolCidrsResult, "GetIpamPoolCidrs")
      end

      # Retrieves the CIDR selection rules for an IPAM prefix list resolver. Use this operation to view the
      # business logic that determines which CIDRs are selected for synchronization with prefix lists.

      def get_ipam_prefix_list_resolver_rules(
        ipam_prefix_list_resolver_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetIpamPrefixListResolverRulesResult

        input = Types::GetIpamPrefixListResolverRulesRequest.new(ipam_prefix_list_resolver_id: ipam_prefix_list_resolver_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        get_ipam_prefix_list_resolver_rules(input)
      end

      def get_ipam_prefix_list_resolver_rules(input : Types::GetIpamPrefixListResolverRulesRequest) : Types::GetIpamPrefixListResolverRulesResult
        request = Protocol::Ec2.build_request(Model::GET_IPAM_PREFIX_LIST_RESOLVER_RULES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetIpamPrefixListResolverRulesResult, "GetIpamPrefixListResolverRules")
      end

      # Retrieves the CIDR entries for a specific version of an IPAM prefix list resolver. This shows the
      # actual CIDRs that were selected and synchronized at a particular point in time.

      def get_ipam_prefix_list_resolver_version_entries(
        ipam_prefix_list_resolver_id : String,
        ipam_prefix_list_resolver_version : Int64,
        dry_run : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetIpamPrefixListResolverVersionEntriesResult

        input = Types::GetIpamPrefixListResolverVersionEntriesRequest.new(ipam_prefix_list_resolver_id: ipam_prefix_list_resolver_id, ipam_prefix_list_resolver_version: ipam_prefix_list_resolver_version, dry_run: dry_run, max_results: max_results, next_token: next_token)
        get_ipam_prefix_list_resolver_version_entries(input)
      end

      def get_ipam_prefix_list_resolver_version_entries(input : Types::GetIpamPrefixListResolverVersionEntriesRequest) : Types::GetIpamPrefixListResolverVersionEntriesResult
        request = Protocol::Ec2.build_request(Model::GET_IPAM_PREFIX_LIST_RESOLVER_VERSION_ENTRIES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetIpamPrefixListResolverVersionEntriesResult, "GetIpamPrefixListResolverVersionEntries")
      end

      # Retrieves version information for an IPAM prefix list resolver. Each version is a snapshot of what
      # CIDRs matched your rules at that moment in time. The version number increments every time the CIDR
      # list changes due to infrastructure changes. Version example: Initial State (Version 1) Production
      # environment: vpc-prod-web (10.1.0.0/16) - tagged env=prod vpc-prod-db (10.2.0.0/16) - tagged
      # env=prod Resolver rule: Include all VPCs tagged env=prod Version 1 CIDRs: 10.1.0.0/16, 10.2.0.0/16
      # Infrastructure Change (Version 2) New VPC added: vpc-prod-api (10.3.0.0/16) - tagged env=prod IPAM
      # automatically detects the change and creates a new version. Version 2 CIDRs: 10.1.0.0/16,
      # 10.2.0.0/16, 10.3.0.0/16

      def get_ipam_prefix_list_resolver_versions(
        ipam_prefix_list_resolver_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        ipam_prefix_list_resolver_versions : Array(Int64)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetIpamPrefixListResolverVersionsResult

        input = Types::GetIpamPrefixListResolverVersionsRequest.new(ipam_prefix_list_resolver_id: ipam_prefix_list_resolver_id, dry_run: dry_run, filters: filters, ipam_prefix_list_resolver_versions: ipam_prefix_list_resolver_versions, max_results: max_results, next_token: next_token)
        get_ipam_prefix_list_resolver_versions(input)
      end

      def get_ipam_prefix_list_resolver_versions(input : Types::GetIpamPrefixListResolverVersionsRequest) : Types::GetIpamPrefixListResolverVersionsResult
        request = Protocol::Ec2.build_request(Model::GET_IPAM_PREFIX_LIST_RESOLVER_VERSIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetIpamPrefixListResolverVersionsResult, "GetIpamPrefixListResolverVersions")
      end

      # Returns resource CIDRs managed by IPAM in a given scope. If an IPAM is associated with more than one
      # resource discovery, the resource CIDRs across all of the resource discoveries is returned. A
      # resource discovery is an IPAM component that enables IPAM to manage and monitor resources that
      # belong to the owning account.

      def get_ipam_resource_cidrs(
        ipam_scope_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        ipam_pool_id : String? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        resource_id : String? = nil,
        resource_owner : String? = nil,
        resource_tag : Types::RequestIpamResourceTag? = nil,
        resource_type : String? = nil
      ) : Types::GetIpamResourceCidrsResult

        input = Types::GetIpamResourceCidrsRequest.new(ipam_scope_id: ipam_scope_id, dry_run: dry_run, filters: filters, ipam_pool_id: ipam_pool_id, max_results: max_results, next_token: next_token, resource_id: resource_id, resource_owner: resource_owner, resource_tag: resource_tag, resource_type: resource_type)
        get_ipam_resource_cidrs(input)
      end

      def get_ipam_resource_cidrs(input : Types::GetIpamResourceCidrsRequest) : Types::GetIpamResourceCidrsResult
        request = Protocol::Ec2.build_request(Model::GET_IPAM_RESOURCE_CIDRS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetIpamResourceCidrsResult, "GetIpamResourceCidrs")
      end

      # Retrieves the configuration data of the specified instance. You can use this data to create a launch
      # template. This action calls on other describe actions to get instance information. Depending on your
      # instance configuration, you may need to allow the following actions in your IAM policy:
      # DescribeSpotInstanceRequests , DescribeInstanceCreditSpecifications , DescribeVolumes , and
      # DescribeInstanceAttribute . Or, you can allow describe* depending on your instance requirements.

      def get_launch_template_data(
        instance_id : String,
        dry_run : Bool? = nil
      ) : Types::GetLaunchTemplateDataResult

        input = Types::GetLaunchTemplateDataRequest.new(instance_id: instance_id, dry_run: dry_run)
        get_launch_template_data(input)
      end

      def get_launch_template_data(input : Types::GetLaunchTemplateDataRequest) : Types::GetLaunchTemplateDataResult
        request = Protocol::Ec2.build_request(Model::GET_LAUNCH_TEMPLATE_DATA, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetLaunchTemplateDataResult, "GetLaunchTemplateData")
      end

      # Gets information about the resources that are associated with the specified managed prefix list.

      def get_managed_prefix_list_associations(
        prefix_list_id : String,
        dry_run : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetManagedPrefixListAssociationsResult

        input = Types::GetManagedPrefixListAssociationsRequest.new(prefix_list_id: prefix_list_id, dry_run: dry_run, max_results: max_results, next_token: next_token)
        get_managed_prefix_list_associations(input)
      end

      def get_managed_prefix_list_associations(input : Types::GetManagedPrefixListAssociationsRequest) : Types::GetManagedPrefixListAssociationsResult
        request = Protocol::Ec2.build_request(Model::GET_MANAGED_PREFIX_LIST_ASSOCIATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetManagedPrefixListAssociationsResult, "GetManagedPrefixListAssociations")
      end

      # Gets information about the entries for a specified managed prefix list.

      def get_managed_prefix_list_entries(
        prefix_list_id : String,
        dry_run : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        target_version : Int64? = nil
      ) : Types::GetManagedPrefixListEntriesResult

        input = Types::GetManagedPrefixListEntriesRequest.new(prefix_list_id: prefix_list_id, dry_run: dry_run, max_results: max_results, next_token: next_token, target_version: target_version)
        get_managed_prefix_list_entries(input)
      end

      def get_managed_prefix_list_entries(input : Types::GetManagedPrefixListEntriesRequest) : Types::GetManagedPrefixListEntriesResult
        request = Protocol::Ec2.build_request(Model::GET_MANAGED_PREFIX_LIST_ENTRIES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetManagedPrefixListEntriesResult, "GetManagedPrefixListEntries")
      end

      # Gets the findings for the specified Network Access Scope analysis.

      def get_network_insights_access_scope_analysis_findings(
        network_insights_access_scope_analysis_id : String,
        dry_run : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetNetworkInsightsAccessScopeAnalysisFindingsResult

        input = Types::GetNetworkInsightsAccessScopeAnalysisFindingsRequest.new(network_insights_access_scope_analysis_id: network_insights_access_scope_analysis_id, dry_run: dry_run, max_results: max_results, next_token: next_token)
        get_network_insights_access_scope_analysis_findings(input)
      end

      def get_network_insights_access_scope_analysis_findings(input : Types::GetNetworkInsightsAccessScopeAnalysisFindingsRequest) : Types::GetNetworkInsightsAccessScopeAnalysisFindingsResult
        request = Protocol::Ec2.build_request(Model::GET_NETWORK_INSIGHTS_ACCESS_SCOPE_ANALYSIS_FINDINGS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetNetworkInsightsAccessScopeAnalysisFindingsResult, "GetNetworkInsightsAccessScopeAnalysisFindings")
      end

      # Gets the content for the specified Network Access Scope.

      def get_network_insights_access_scope_content(
        network_insights_access_scope_id : String,
        dry_run : Bool? = nil
      ) : Types::GetNetworkInsightsAccessScopeContentResult

        input = Types::GetNetworkInsightsAccessScopeContentRequest.new(network_insights_access_scope_id: network_insights_access_scope_id, dry_run: dry_run)
        get_network_insights_access_scope_content(input)
      end

      def get_network_insights_access_scope_content(input : Types::GetNetworkInsightsAccessScopeContentRequest) : Types::GetNetworkInsightsAccessScopeContentResult
        request = Protocol::Ec2.build_request(Model::GET_NETWORK_INSIGHTS_ACCESS_SCOPE_CONTENT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetNetworkInsightsAccessScopeContentResult, "GetNetworkInsightsAccessScopeContent")
      end

      # Retrieves the encrypted administrator password for a running Windows instance. The Windows password
      # is generated at boot by the EC2Config service or EC2Launch scripts (Windows Server 2016 and later).
      # This usually only happens the first time an instance is launched. For more information, see
      # EC2Config and EC2Launch in the Amazon EC2 User Guide . For the EC2Config service, the password is
      # not generated for rebundled AMIs unless Ec2SetPassword is enabled before bundling. The password is
      # encrypted using the key pair that you specified when you launched the instance. You must provide the
      # corresponding key pair file. When you launch an instance, password generation and encryption may
      # take a few minutes. If you try to retrieve the password before it's available, the output returns an
      # empty string. We recommend that you wait up to 15 minutes after launching an instance before trying
      # to retrieve the generated password.

      def get_password_data(
        instance_id : String,
        dry_run : Bool? = nil
      ) : Types::GetPasswordDataResult

        input = Types::GetPasswordDataRequest.new(instance_id: instance_id, dry_run: dry_run)
        get_password_data(input)
      end

      def get_password_data(input : Types::GetPasswordDataRequest) : Types::GetPasswordDataResult
        request = Protocol::Ec2.build_request(Model::GET_PASSWORD_DATA, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetPasswordDataResult, "GetPasswordData")
      end

      # Returns a quote and exchange information for exchanging one or more specified Convertible Reserved
      # Instances for a new Convertible Reserved Instance. If the exchange cannot be performed, the reason
      # is returned in the response. Use AcceptReservedInstancesExchangeQuote to perform the exchange.

      def get_reserved_instances_exchange_quote(
        reserved_instance_ids : Array(String),
        dry_run : Bool? = nil,
        target_configurations : Array(Types::TargetConfigurationRequest)? = nil
      ) : Types::GetReservedInstancesExchangeQuoteResult

        input = Types::GetReservedInstancesExchangeQuoteRequest.new(reserved_instance_ids: reserved_instance_ids, dry_run: dry_run, target_configurations: target_configurations)
        get_reserved_instances_exchange_quote(input)
      end

      def get_reserved_instances_exchange_quote(input : Types::GetReservedInstancesExchangeQuoteRequest) : Types::GetReservedInstancesExchangeQuoteResult
        request = Protocol::Ec2.build_request(Model::GET_RESERVED_INSTANCES_EXCHANGE_QUOTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetReservedInstancesExchangeQuoteResult, "GetReservedInstancesExchangeQuote")
      end

      # Gets information about the associations for the specified route server. A route server association
      # is the connection established between a route server and a VPC. For more information see Dynamic
      # routing in your VPC with VPC Route Server in the Amazon VPC User Guide .

      def get_route_server_associations(
        route_server_id : String,
        dry_run : Bool? = nil
      ) : Types::GetRouteServerAssociationsResult

        input = Types::GetRouteServerAssociationsRequest.new(route_server_id: route_server_id, dry_run: dry_run)
        get_route_server_associations(input)
      end

      def get_route_server_associations(input : Types::GetRouteServerAssociationsRequest) : Types::GetRouteServerAssociationsResult
        request = Protocol::Ec2.build_request(Model::GET_ROUTE_SERVER_ASSOCIATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetRouteServerAssociationsResult, "GetRouteServerAssociations")
      end

      # Gets information about the route propagations for the specified route server. When enabled, route
      # server propagation installs the routes in the FIB on the route table you've specified. Route server
      # supports IPv4 and IPv6 route propagation. Amazon VPC Route Server simplifies routing for traffic
      # between workloads that are deployed within a VPC and its internet gateways. With this feature, VPC
      # Route Server dynamically updates VPC and internet gateway route tables with your preferred IPv4 or
      # IPv6 routes to achieve routing fault tolerance for those workloads. This enables you to
      # automatically reroute traffic within a VPC, which increases the manageability of VPC routing and
      # interoperability with third-party workloads. Route server supports the follow route table types: VPC
      # route tables not associated with subnets Subnet route tables Internet gateway route tables Route
      # server does not support route tables associated with virtual private gateways. To propagate routes
      # into a transit gateway route table, use Transit Gateway Connect .

      def get_route_server_propagations(
        route_server_id : String,
        dry_run : Bool? = nil,
        route_table_id : String? = nil
      ) : Types::GetRouteServerPropagationsResult

        input = Types::GetRouteServerPropagationsRequest.new(route_server_id: route_server_id, dry_run: dry_run, route_table_id: route_table_id)
        get_route_server_propagations(input)
      end

      def get_route_server_propagations(input : Types::GetRouteServerPropagationsRequest) : Types::GetRouteServerPropagationsResult
        request = Protocol::Ec2.build_request(Model::GET_ROUTE_SERVER_PROPAGATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetRouteServerPropagationsResult, "GetRouteServerPropagations")
      end

      # Gets the routing database for the specified route server. The Routing Information Base (RIB) serves
      # as a database that stores all the routing information and network topology data collected by a
      # router or routing system, such as routes learned from BGP peers. The RIB is constantly updated as
      # new routing information is received or existing routes change. This ensures that the route server
      # always has the most current view of the network topology and can make optimal routing decisions.
      # Amazon VPC Route Server simplifies routing for traffic between workloads that are deployed within a
      # VPC and its internet gateways. With this feature, VPC Route Server dynamically updates VPC and
      # internet gateway route tables with your preferred IPv4 or IPv6 routes to achieve routing fault
      # tolerance for those workloads. This enables you to automatically reroute traffic within a VPC, which
      # increases the manageability of VPC routing and interoperability with third-party workloads. Route
      # server supports the follow route table types: VPC route tables not associated with subnets Subnet
      # route tables Internet gateway route tables Route server does not support route tables associated
      # with virtual private gateways. To propagate routes into a transit gateway route table, use Transit
      # Gateway Connect .

      def get_route_server_routing_database(
        route_server_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetRouteServerRoutingDatabaseResult

        input = Types::GetRouteServerRoutingDatabaseRequest.new(route_server_id: route_server_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        get_route_server_routing_database(input)
      end

      def get_route_server_routing_database(input : Types::GetRouteServerRoutingDatabaseRequest) : Types::GetRouteServerRoutingDatabaseResult
        request = Protocol::Ec2.build_request(Model::GET_ROUTE_SERVER_ROUTING_DATABASE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetRouteServerRoutingDatabaseResult, "GetRouteServerRoutingDatabase")
      end

      # Gets security groups that can be associated by the Amazon Web Services account making the request
      # with network interfaces in the specified VPC.

      def get_security_groups_for_vpc(
        vpc_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetSecurityGroupsForVpcResult

        input = Types::GetSecurityGroupsForVpcRequest.new(vpc_id: vpc_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        get_security_groups_for_vpc(input)
      end

      def get_security_groups_for_vpc(input : Types::GetSecurityGroupsForVpcRequest) : Types::GetSecurityGroupsForVpcResult
        request = Protocol::Ec2.build_request(Model::GET_SECURITY_GROUPS_FOR_VPC, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetSecurityGroupsForVpcResult, "GetSecurityGroupsForVpc")
      end

      # Retrieves the access status of your account to the EC2 serial console of all instances. By default,
      # access to the EC2 serial console is disabled for your account. For more information, see Manage
      # account access to the EC2 serial console in the Amazon EC2 User Guide .

      def get_serial_console_access_status(
        dry_run : Bool? = nil
      ) : Types::GetSerialConsoleAccessStatusResult

        input = Types::GetSerialConsoleAccessStatusRequest.new(dry_run: dry_run)
        get_serial_console_access_status(input)
      end

      def get_serial_console_access_status(input : Types::GetSerialConsoleAccessStatusRequest) : Types::GetSerialConsoleAccessStatusResult
        request = Protocol::Ec2.build_request(Model::GET_SERIAL_CONSOLE_ACCESS_STATUS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetSerialConsoleAccessStatusResult, "GetSerialConsoleAccessStatus")
      end

      # Gets the current state of block public access for snapshots setting for the account and Region. For
      # more information, see Block public access for snapshots in the Amazon EBS User Guide .

      def get_snapshot_block_public_access_state(
        dry_run : Bool? = nil
      ) : Types::GetSnapshotBlockPublicAccessStateResult

        input = Types::GetSnapshotBlockPublicAccessStateRequest.new(dry_run: dry_run)
        get_snapshot_block_public_access_state(input)
      end

      def get_snapshot_block_public_access_state(input : Types::GetSnapshotBlockPublicAccessStateRequest) : Types::GetSnapshotBlockPublicAccessStateResult
        request = Protocol::Ec2.build_request(Model::GET_SNAPSHOT_BLOCK_PUBLIC_ACCESS_STATE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetSnapshotBlockPublicAccessStateResult, "GetSnapshotBlockPublicAccessState")
      end

      # Calculates the Spot placement score for a Region or Availability Zone based on the specified target
      # capacity and compute requirements. You can specify your compute requirements either by using
      # InstanceRequirementsWithMetadata and letting Amazon EC2 choose the optimal instance types to fulfill
      # your Spot request, or you can specify the instance types by using InstanceTypes . For more
      # information, see Spot placement score in the Amazon EC2 User Guide .

      def get_spot_placement_scores(
        target_capacity : Int32,
        dry_run : Bool? = nil,
        instance_requirements_with_metadata : Types::InstanceRequirementsWithMetadataRequest? = nil,
        instance_types : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        region_names : Array(String)? = nil,
        single_availability_zone : Bool? = nil,
        target_capacity_unit_type : String? = nil
      ) : Types::GetSpotPlacementScoresResult

        input = Types::GetSpotPlacementScoresRequest.new(target_capacity: target_capacity, dry_run: dry_run, instance_requirements_with_metadata: instance_requirements_with_metadata, instance_types: instance_types, max_results: max_results, next_token: next_token, region_names: region_names, single_availability_zone: single_availability_zone, target_capacity_unit_type: target_capacity_unit_type)
        get_spot_placement_scores(input)
      end

      def get_spot_placement_scores(input : Types::GetSpotPlacementScoresRequest) : Types::GetSpotPlacementScoresResult
        request = Protocol::Ec2.build_request(Model::GET_SPOT_PLACEMENT_SCORES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetSpotPlacementScoresResult, "GetSpotPlacementScores")
      end

      # Gets information about the subnet CIDR reservations.

      def get_subnet_cidr_reservations(
        subnet_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetSubnetCidrReservationsResult

        input = Types::GetSubnetCidrReservationsRequest.new(subnet_id: subnet_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        get_subnet_cidr_reservations(input)
      end

      def get_subnet_cidr_reservations(input : Types::GetSubnetCidrReservationsRequest) : Types::GetSubnetCidrReservationsResult
        request = Protocol::Ec2.build_request(Model::GET_SUBNET_CIDR_RESERVATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetSubnetCidrReservationsResult, "GetSubnetCidrReservations")
      end

      # Lists the route tables to which the specified resource attachment propagates routes.

      def get_transit_gateway_attachment_propagations(
        transit_gateway_attachment_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetTransitGatewayAttachmentPropagationsResult

        input = Types::GetTransitGatewayAttachmentPropagationsRequest.new(transit_gateway_attachment_id: transit_gateway_attachment_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        get_transit_gateway_attachment_propagations(input)
      end

      def get_transit_gateway_attachment_propagations(input : Types::GetTransitGatewayAttachmentPropagationsRequest) : Types::GetTransitGatewayAttachmentPropagationsResult
        request = Protocol::Ec2.build_request(Model::GET_TRANSIT_GATEWAY_ATTACHMENT_PROPAGATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetTransitGatewayAttachmentPropagationsResult, "GetTransitGatewayAttachmentPropagations")
      end

      # Retrieves the entries for a transit gateway metering policy.

      def get_transit_gateway_metering_policy_entries(
        transit_gateway_metering_policy_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetTransitGatewayMeteringPolicyEntriesResult

        input = Types::GetTransitGatewayMeteringPolicyEntriesRequest.new(transit_gateway_metering_policy_id: transit_gateway_metering_policy_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        get_transit_gateway_metering_policy_entries(input)
      end

      def get_transit_gateway_metering_policy_entries(input : Types::GetTransitGatewayMeteringPolicyEntriesRequest) : Types::GetTransitGatewayMeteringPolicyEntriesResult
        request = Protocol::Ec2.build_request(Model::GET_TRANSIT_GATEWAY_METERING_POLICY_ENTRIES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetTransitGatewayMeteringPolicyEntriesResult, "GetTransitGatewayMeteringPolicyEntries")
      end

      # Gets information about the associations for the transit gateway multicast domain.

      def get_transit_gateway_multicast_domain_associations(
        transit_gateway_multicast_domain_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetTransitGatewayMulticastDomainAssociationsResult

        input = Types::GetTransitGatewayMulticastDomainAssociationsRequest.new(transit_gateway_multicast_domain_id: transit_gateway_multicast_domain_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        get_transit_gateway_multicast_domain_associations(input)
      end

      def get_transit_gateway_multicast_domain_associations(input : Types::GetTransitGatewayMulticastDomainAssociationsRequest) : Types::GetTransitGatewayMulticastDomainAssociationsResult
        request = Protocol::Ec2.build_request(Model::GET_TRANSIT_GATEWAY_MULTICAST_DOMAIN_ASSOCIATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetTransitGatewayMulticastDomainAssociationsResult, "GetTransitGatewayMulticastDomainAssociations")
      end

      # Gets a list of the transit gateway policy table associations.

      def get_transit_gateway_policy_table_associations(
        transit_gateway_policy_table_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetTransitGatewayPolicyTableAssociationsResult

        input = Types::GetTransitGatewayPolicyTableAssociationsRequest.new(transit_gateway_policy_table_id: transit_gateway_policy_table_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        get_transit_gateway_policy_table_associations(input)
      end

      def get_transit_gateway_policy_table_associations(input : Types::GetTransitGatewayPolicyTableAssociationsRequest) : Types::GetTransitGatewayPolicyTableAssociationsResult
        request = Protocol::Ec2.build_request(Model::GET_TRANSIT_GATEWAY_POLICY_TABLE_ASSOCIATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetTransitGatewayPolicyTableAssociationsResult, "GetTransitGatewayPolicyTableAssociations")
      end

      # Returns a list of transit gateway policy table entries.

      def get_transit_gateway_policy_table_entries(
        transit_gateway_policy_table_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetTransitGatewayPolicyTableEntriesResult

        input = Types::GetTransitGatewayPolicyTableEntriesRequest.new(transit_gateway_policy_table_id: transit_gateway_policy_table_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        get_transit_gateway_policy_table_entries(input)
      end

      def get_transit_gateway_policy_table_entries(input : Types::GetTransitGatewayPolicyTableEntriesRequest) : Types::GetTransitGatewayPolicyTableEntriesResult
        request = Protocol::Ec2.build_request(Model::GET_TRANSIT_GATEWAY_POLICY_TABLE_ENTRIES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetTransitGatewayPolicyTableEntriesResult, "GetTransitGatewayPolicyTableEntries")
      end

      # Gets information about the prefix list references in a specified transit gateway route table.

      def get_transit_gateway_prefix_list_references(
        transit_gateway_route_table_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetTransitGatewayPrefixListReferencesResult

        input = Types::GetTransitGatewayPrefixListReferencesRequest.new(transit_gateway_route_table_id: transit_gateway_route_table_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        get_transit_gateway_prefix_list_references(input)
      end

      def get_transit_gateway_prefix_list_references(input : Types::GetTransitGatewayPrefixListReferencesRequest) : Types::GetTransitGatewayPrefixListReferencesResult
        request = Protocol::Ec2.build_request(Model::GET_TRANSIT_GATEWAY_PREFIX_LIST_REFERENCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetTransitGatewayPrefixListReferencesResult, "GetTransitGatewayPrefixListReferences")
      end

      # Gets information about the associations for the specified transit gateway route table.

      def get_transit_gateway_route_table_associations(
        transit_gateway_route_table_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetTransitGatewayRouteTableAssociationsResult

        input = Types::GetTransitGatewayRouteTableAssociationsRequest.new(transit_gateway_route_table_id: transit_gateway_route_table_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        get_transit_gateway_route_table_associations(input)
      end

      def get_transit_gateway_route_table_associations(input : Types::GetTransitGatewayRouteTableAssociationsRequest) : Types::GetTransitGatewayRouteTableAssociationsResult
        request = Protocol::Ec2.build_request(Model::GET_TRANSIT_GATEWAY_ROUTE_TABLE_ASSOCIATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetTransitGatewayRouteTableAssociationsResult, "GetTransitGatewayRouteTableAssociations")
      end

      # Gets information about the route table propagations for the specified transit gateway route table.

      def get_transit_gateway_route_table_propagations(
        transit_gateway_route_table_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetTransitGatewayRouteTablePropagationsResult

        input = Types::GetTransitGatewayRouteTablePropagationsRequest.new(transit_gateway_route_table_id: transit_gateway_route_table_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        get_transit_gateway_route_table_propagations(input)
      end

      def get_transit_gateway_route_table_propagations(input : Types::GetTransitGatewayRouteTablePropagationsRequest) : Types::GetTransitGatewayRouteTablePropagationsResult
        request = Protocol::Ec2.build_request(Model::GET_TRANSIT_GATEWAY_ROUTE_TABLE_PROPAGATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetTransitGatewayRouteTablePropagationsResult, "GetTransitGatewayRouteTablePropagations")
      end

      # Get the Verified Access policy associated with the endpoint.

      def get_verified_access_endpoint_policy(
        verified_access_endpoint_id : String,
        dry_run : Bool? = nil
      ) : Types::GetVerifiedAccessEndpointPolicyResult

        input = Types::GetVerifiedAccessEndpointPolicyRequest.new(verified_access_endpoint_id: verified_access_endpoint_id, dry_run: dry_run)
        get_verified_access_endpoint_policy(input)
      end

      def get_verified_access_endpoint_policy(input : Types::GetVerifiedAccessEndpointPolicyRequest) : Types::GetVerifiedAccessEndpointPolicyResult
        request = Protocol::Ec2.build_request(Model::GET_VERIFIED_ACCESS_ENDPOINT_POLICY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetVerifiedAccessEndpointPolicyResult, "GetVerifiedAccessEndpointPolicy")
      end

      # Gets the targets for the specified network CIDR endpoint for Verified Access.

      def get_verified_access_endpoint_targets(
        verified_access_endpoint_id : String,
        dry_run : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetVerifiedAccessEndpointTargetsResult

        input = Types::GetVerifiedAccessEndpointTargetsRequest.new(verified_access_endpoint_id: verified_access_endpoint_id, dry_run: dry_run, max_results: max_results, next_token: next_token)
        get_verified_access_endpoint_targets(input)
      end

      def get_verified_access_endpoint_targets(input : Types::GetVerifiedAccessEndpointTargetsRequest) : Types::GetVerifiedAccessEndpointTargetsResult
        request = Protocol::Ec2.build_request(Model::GET_VERIFIED_ACCESS_ENDPOINT_TARGETS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetVerifiedAccessEndpointTargetsResult, "GetVerifiedAccessEndpointTargets")
      end

      # Shows the contents of the Verified Access policy associated with the group.

      def get_verified_access_group_policy(
        verified_access_group_id : String,
        dry_run : Bool? = nil
      ) : Types::GetVerifiedAccessGroupPolicyResult

        input = Types::GetVerifiedAccessGroupPolicyRequest.new(verified_access_group_id: verified_access_group_id, dry_run: dry_run)
        get_verified_access_group_policy(input)
      end

      def get_verified_access_group_policy(input : Types::GetVerifiedAccessGroupPolicyRequest) : Types::GetVerifiedAccessGroupPolicyResult
        request = Protocol::Ec2.build_request(Model::GET_VERIFIED_ACCESS_GROUP_POLICY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetVerifiedAccessGroupPolicyResult, "GetVerifiedAccessGroupPolicy")
      end

      # Gets information about resources in a VPC that are blocking encryption enforcement. For more
      # information, see Enforce VPC encryption in transit in the Amazon VPC User Guide .

      def get_vpc_resources_blocking_encryption_enforcement(
        vpc_id : String,
        dry_run : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetVpcResourcesBlockingEncryptionEnforcementResult

        input = Types::GetVpcResourcesBlockingEncryptionEnforcementRequest.new(vpc_id: vpc_id, dry_run: dry_run, max_results: max_results, next_token: next_token)
        get_vpc_resources_blocking_encryption_enforcement(input)
      end

      def get_vpc_resources_blocking_encryption_enforcement(input : Types::GetVpcResourcesBlockingEncryptionEnforcementRequest) : Types::GetVpcResourcesBlockingEncryptionEnforcementResult
        request = Protocol::Ec2.build_request(Model::GET_VPC_RESOURCES_BLOCKING_ENCRYPTION_ENFORCEMENT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetVpcResourcesBlockingEncryptionEnforcementResult, "GetVpcResourcesBlockingEncryptionEnforcement")
      end

      # Download an Amazon Web Services-provided sample configuration file to be used with the customer
      # gateway device specified for your Site-to-Site VPN connection.

      def get_vpn_connection_device_sample_configuration(
        vpn_connection_device_type_id : String,
        vpn_connection_id : String,
        dry_run : Bool? = nil,
        internet_key_exchange_version : String? = nil,
        sample_type : String? = nil
      ) : Types::GetVpnConnectionDeviceSampleConfigurationResult

        input = Types::GetVpnConnectionDeviceSampleConfigurationRequest.new(vpn_connection_device_type_id: vpn_connection_device_type_id, vpn_connection_id: vpn_connection_id, dry_run: dry_run, internet_key_exchange_version: internet_key_exchange_version, sample_type: sample_type)
        get_vpn_connection_device_sample_configuration(input)
      end

      def get_vpn_connection_device_sample_configuration(input : Types::GetVpnConnectionDeviceSampleConfigurationRequest) : Types::GetVpnConnectionDeviceSampleConfigurationResult
        request = Protocol::Ec2.build_request(Model::GET_VPN_CONNECTION_DEVICE_SAMPLE_CONFIGURATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetVpnConnectionDeviceSampleConfigurationResult, "GetVpnConnectionDeviceSampleConfiguration")
      end

      # Obtain a list of customer gateway devices for which sample configuration files can be provided. The
      # request has no additional parameters. You can also see the list of device types with sample
      # configuration files available under Your customer gateway device in the Amazon Web Services
      # Site-to-Site VPN User Guide .

      def get_vpn_connection_device_types(
        dry_run : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::GetVpnConnectionDeviceTypesResult

        input = Types::GetVpnConnectionDeviceTypesRequest.new(dry_run: dry_run, max_results: max_results, next_token: next_token)
        get_vpn_connection_device_types(input)
      end

      def get_vpn_connection_device_types(input : Types::GetVpnConnectionDeviceTypesRequest) : Types::GetVpnConnectionDeviceTypesResult
        request = Protocol::Ec2.build_request(Model::GET_VPN_CONNECTION_DEVICE_TYPES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetVpnConnectionDeviceTypesResult, "GetVpnConnectionDeviceTypes")
      end

      # Get details of available tunnel endpoint maintenance.

      def get_vpn_tunnel_replacement_status(
        vpn_connection_id : String,
        vpn_tunnel_outside_ip_address : String,
        dry_run : Bool? = nil
      ) : Types::GetVpnTunnelReplacementStatusResult

        input = Types::GetVpnTunnelReplacementStatusRequest.new(vpn_connection_id: vpn_connection_id, vpn_tunnel_outside_ip_address: vpn_tunnel_outside_ip_address, dry_run: dry_run)
        get_vpn_tunnel_replacement_status(input)
      end

      def get_vpn_tunnel_replacement_status(input : Types::GetVpnTunnelReplacementStatusRequest) : Types::GetVpnTunnelReplacementStatusResult
        request = Protocol::Ec2.build_request(Model::GET_VPN_TUNNEL_REPLACEMENT_STATUS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::GetVpnTunnelReplacementStatusResult, "GetVpnTunnelReplacementStatus")
      end

      # Uploads a client certificate revocation list to the specified Client VPN endpoint. Uploading a
      # client certificate revocation list overwrites the existing client certificate revocation list.
      # Uploading a client certificate revocation list resets existing client connections.

      def import_client_vpn_client_certificate_revocation_list(
        certificate_revocation_list : String,
        client_vpn_endpoint_id : String,
        dry_run : Bool? = nil
      ) : Types::ImportClientVpnClientCertificateRevocationListResult

        input = Types::ImportClientVpnClientCertificateRevocationListRequest.new(certificate_revocation_list: certificate_revocation_list, client_vpn_endpoint_id: client_vpn_endpoint_id, dry_run: dry_run)
        import_client_vpn_client_certificate_revocation_list(input)
      end

      def import_client_vpn_client_certificate_revocation_list(input : Types::ImportClientVpnClientCertificateRevocationListRequest) : Types::ImportClientVpnClientCertificateRevocationListResult
        request = Protocol::Ec2.build_request(Model::IMPORT_CLIENT_VPN_CLIENT_CERTIFICATE_REVOCATION_LIST, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ImportClientVpnClientCertificateRevocationListResult, "ImportClientVpnClientCertificateRevocationList")
      end

      # To import your virtual machines (VMs) with a console-based experience, you can use the Import
      # virtual machine images to Amazon Web Services template in the Migration Hub Orchestrator console .
      # For more information, see the Migration Hub Orchestrator User Guide . Import single or multi-volume
      # disk images or EBS snapshots into an Amazon Machine Image (AMI). Amazon Web Services VM
      # Import/Export strongly recommends specifying a value for either the --license-type or
      # --usage-operation parameter when you create a new VM Import task. This ensures your operating system
      # is licensed appropriately and your billing is optimized. For more information, see Importing a VM as
      # an image using VM Import/Export in the VM Import/Export User Guide .

      def import_image(
        architecture : String? = nil,
        boot_mode : String? = nil,
        client_data : Types::ClientData? = nil,
        client_token : String? = nil,
        description : String? = nil,
        disk_containers : Array(Types::ImageDiskContainer)? = nil,
        dry_run : Bool? = nil,
        encrypted : Bool? = nil,
        hypervisor : String? = nil,
        kms_key_id : String? = nil,
        license_specifications : Array(Types::ImportImageLicenseConfigurationRequest)? = nil,
        license_type : String? = nil,
        platform : String? = nil,
        role_name : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        usage_operation : String? = nil
      ) : Types::ImportImageResult

        input = Types::ImportImageRequest.new(architecture: architecture, boot_mode: boot_mode, client_data: client_data, client_token: client_token, description: description, disk_containers: disk_containers, dry_run: dry_run, encrypted: encrypted, hypervisor: hypervisor, kms_key_id: kms_key_id, license_specifications: license_specifications, license_type: license_type, platform: platform, role_name: role_name, tag_specifications: tag_specifications, usage_operation: usage_operation)
        import_image(input)
      end

      def import_image(input : Types::ImportImageRequest) : Types::ImportImageResult
        request = Protocol::Ec2.build_request(Model::IMPORT_IMAGE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ImportImageResult, "ImportImage")
      end

      # We recommend that you use the ImportImage API instead. For more information, see Importing a VM as
      # an image using VM Import/Export in the VM Import/Export User Guide . Creates an import instance task
      # using metadata from the specified disk image. This API action supports only single-volume VMs. To
      # import multi-volume VMs, use ImportImage instead. For information about the import manifest
      # referenced by this API action, see VM Import Manifest . This API action is not supported by the
      # Command Line Interface (CLI).

      def import_instance(
        platform : String,
        description : String? = nil,
        disk_images : Array(Types::DiskImage)? = nil,
        dry_run : Bool? = nil,
        launch_specification : Types::ImportInstanceLaunchSpecification? = nil
      ) : Types::ImportInstanceResult

        input = Types::ImportInstanceRequest.new(platform: platform, description: description, disk_images: disk_images, dry_run: dry_run, launch_specification: launch_specification)
        import_instance(input)
      end

      def import_instance(input : Types::ImportInstanceRequest) : Types::ImportInstanceResult
        request = Protocol::Ec2.build_request(Model::IMPORT_INSTANCE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ImportInstanceResult, "ImportInstance")
      end

      # Imports the public key from an RSA or ED25519 key pair that you created using a third-party tool.
      # You give Amazon Web Services only the public key. The private key is never transferred between you
      # and Amazon Web Services. For more information about the requirements for importing a key pair, see
      # Create a key pair and import the public key to Amazon EC2 in the Amazon EC2 User Guide .

      def import_key_pair(
        key_name : String,
        public_key_material : Bytes,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::ImportKeyPairResult

        input = Types::ImportKeyPairRequest.new(key_name: key_name, public_key_material: public_key_material, dry_run: dry_run, tag_specifications: tag_specifications)
        import_key_pair(input)
      end

      def import_key_pair(input : Types::ImportKeyPairRequest) : Types::ImportKeyPairResult
        request = Protocol::Ec2.build_request(Model::IMPORT_KEY_PAIR, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ImportKeyPairResult, "ImportKeyPair")
      end

      # Imports a disk into an EBS snapshot. For more information, see Importing a disk as a snapshot using
      # VM Import/Export in the VM Import/Export User Guide .

      def import_snapshot(
        client_data : Types::ClientData? = nil,
        client_token : String? = nil,
        description : String? = nil,
        disk_container : Types::SnapshotDiskContainer? = nil,
        dry_run : Bool? = nil,
        encrypted : Bool? = nil,
        kms_key_id : String? = nil,
        role_name : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::ImportSnapshotResult

        input = Types::ImportSnapshotRequest.new(client_data: client_data, client_token: client_token, description: description, disk_container: disk_container, dry_run: dry_run, encrypted: encrypted, kms_key_id: kms_key_id, role_name: role_name, tag_specifications: tag_specifications)
        import_snapshot(input)
      end

      def import_snapshot(input : Types::ImportSnapshotRequest) : Types::ImportSnapshotResult
        request = Protocol::Ec2.build_request(Model::IMPORT_SNAPSHOT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ImportSnapshotResult, "ImportSnapshot")
      end

      # This API action supports only single-volume VMs. To import multi-volume VMs, use ImportImage
      # instead. To import a disk to a snapshot, use ImportSnapshot instead. Creates an import volume task
      # using metadata from the specified disk image. For information about the import manifest referenced
      # by this API action, see VM Import Manifest . This API action is not supported by the Command Line
      # Interface (CLI).

      def import_volume(
        image : Types::DiskImageDetail,
        volume : Types::VolumeDetail,
        availability_zone : String? = nil,
        availability_zone_id : String? = nil,
        description : String? = nil,
        dry_run : Bool? = nil
      ) : Types::ImportVolumeResult

        input = Types::ImportVolumeRequest.new(image: image, volume: volume, availability_zone: availability_zone, availability_zone_id: availability_zone_id, description: description, dry_run: dry_run)
        import_volume(input)
      end

      def import_volume(input : Types::ImportVolumeRequest) : Types::ImportVolumeResult
        request = Protocol::Ec2.build_request(Model::IMPORT_VOLUME, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ImportVolumeResult, "ImportVolume")
      end

      # Lists one or more AMIs that are currently in the Recycle Bin. For more information, see Recycle Bin
      # in the Amazon EC2 User Guide .

      def list_images_in_recycle_bin(
        dry_run : Bool? = nil,
        image_ids : Array(String)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::ListImagesInRecycleBinResult

        input = Types::ListImagesInRecycleBinRequest.new(dry_run: dry_run, image_ids: image_ids, max_results: max_results, next_token: next_token)
        list_images_in_recycle_bin(input)
      end

      def list_images_in_recycle_bin(input : Types::ListImagesInRecycleBinRequest) : Types::ListImagesInRecycleBinResult
        request = Protocol::Ec2.build_request(Model::LIST_IMAGES_IN_RECYCLE_BIN, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ListImagesInRecycleBinResult, "ListImagesInRecycleBin")
      end

      # Lists one or more snapshots that are currently in the Recycle Bin.

      def list_snapshots_in_recycle_bin(
        dry_run : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        snapshot_ids : Array(String)? = nil
      ) : Types::ListSnapshotsInRecycleBinResult

        input = Types::ListSnapshotsInRecycleBinRequest.new(dry_run: dry_run, max_results: max_results, next_token: next_token, snapshot_ids: snapshot_ids)
        list_snapshots_in_recycle_bin(input)
      end

      def list_snapshots_in_recycle_bin(input : Types::ListSnapshotsInRecycleBinRequest) : Types::ListSnapshotsInRecycleBinResult
        request = Protocol::Ec2.build_request(Model::LIST_SNAPSHOTS_IN_RECYCLE_BIN, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ListSnapshotsInRecycleBinResult, "ListSnapshotsInRecycleBin")
      end

      # Lists one or more volumes that are currently in the Recycle Bin.

      def list_volumes_in_recycle_bin(
        dry_run : Bool? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil,
        volume_ids : Array(String)? = nil
      ) : Types::ListVolumesInRecycleBinResult

        input = Types::ListVolumesInRecycleBinRequest.new(dry_run: dry_run, max_results: max_results, next_token: next_token, volume_ids: volume_ids)
        list_volumes_in_recycle_bin(input)
      end

      def list_volumes_in_recycle_bin(input : Types::ListVolumesInRecycleBinRequest) : Types::ListVolumesInRecycleBinResult
        request = Protocol::Ec2.build_request(Model::LIST_VOLUMES_IN_RECYCLE_BIN, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ListVolumesInRecycleBinResult, "ListVolumesInRecycleBin")
      end

      # Locks an Amazon EBS snapshot in either governance or compliance mode to protect it against
      # accidental or malicious deletions for a specific duration. A locked snapshot can't be deleted. You
      # can also use this action to modify the lock settings for a snapshot that is already locked. The
      # allowed modifications depend on the lock mode and lock state: If the snapshot is locked in
      # governance mode, you can modify the lock mode and the lock duration or lock expiration date. If the
      # snapshot is locked in compliance mode and it is in the cooling-off period, you can modify the lock
      # mode and the lock duration or lock expiration date. If the snapshot is locked in compliance mode and
      # the cooling-off period has lapsed, you can only increase the lock duration or extend the lock
      # expiration date.

      def lock_snapshot(
        lock_mode : String,
        snapshot_id : String,
        cool_off_period : Int32? = nil,
        dry_run : Bool? = nil,
        expiration_date : Time? = nil,
        lock_duration : Int32? = nil
      ) : Types::LockSnapshotResult

        input = Types::LockSnapshotRequest.new(lock_mode: lock_mode, snapshot_id: snapshot_id, cool_off_period: cool_off_period, dry_run: dry_run, expiration_date: expiration_date, lock_duration: lock_duration)
        lock_snapshot(input)
      end

      def lock_snapshot(input : Types::LockSnapshotRequest) : Types::LockSnapshotResult
        request = Protocol::Ec2.build_request(Model::LOCK_SNAPSHOT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::LockSnapshotResult, "LockSnapshot")
      end

      # Modifies an attribute of the specified Elastic IP address. For requirements, see Using reverse DNS
      # for email applications .

      def modify_address_attribute(
        allocation_id : String,
        domain_name : String? = nil,
        dry_run : Bool? = nil
      ) : Types::ModifyAddressAttributeResult

        input = Types::ModifyAddressAttributeRequest.new(allocation_id: allocation_id, domain_name: domain_name, dry_run: dry_run)
        modify_address_attribute(input)
      end

      def modify_address_attribute(input : Types::ModifyAddressAttributeRequest) : Types::ModifyAddressAttributeResult
        request = Protocol::Ec2.build_request(Model::MODIFY_ADDRESS_ATTRIBUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyAddressAttributeResult, "ModifyAddressAttribute")
      end

      # Changes the opt-in status of the specified zone group for your account.

      def modify_availability_zone_group(
        group_name : String,
        opt_in_status : String,
        dry_run : Bool? = nil
      ) : Types::ModifyAvailabilityZoneGroupResult

        input = Types::ModifyAvailabilityZoneGroupRequest.new(group_name: group_name, opt_in_status: opt_in_status, dry_run: dry_run)
        modify_availability_zone_group(input)
      end

      def modify_availability_zone_group(input : Types::ModifyAvailabilityZoneGroupRequest) : Types::ModifyAvailabilityZoneGroupResult
        request = Protocol::Ec2.build_request(Model::MODIFY_AVAILABILITY_ZONE_GROUP, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyAvailabilityZoneGroupResult, "ModifyAvailabilityZoneGroup")
      end

      # Modifies a Capacity Reservation's capacity, instance eligibility, and the conditions under which it
      # is to be released. You can't modify a Capacity Reservation's instance type, EBS optimization,
      # platform, instance store settings, Availability Zone, or tenancy. If you need to modify any of these
      # attributes, we recommend that you cancel the Capacity Reservation, and then create a new one with
      # the required attributes. For more information, see Modify an active Capacity Reservation . The
      # allowed modifications depend on the state of the Capacity Reservation: assessing or scheduled state
      # - You can modify the tags only. pending state - You can't modify the Capacity Reservation in any
      # way. active state but still within the commitment duration - You can't decrease the instance count
      # or set an end date that is within the commitment duration. All other modifications are allowed.
      # active state with no commitment duration or elapsed commitment duration - All modifications are
      # allowed. expired , cancelled , unsupported , or failed state - You can't modify the Capacity
      # Reservation in any way.

      def modify_capacity_reservation(
        capacity_reservation_id : String,
        accept : Bool? = nil,
        additional_info : String? = nil,
        dry_run : Bool? = nil,
        end_date : Time? = nil,
        end_date_type : String? = nil,
        instance_count : Int32? = nil,
        instance_match_criteria : String? = nil
      ) : Types::ModifyCapacityReservationResult

        input = Types::ModifyCapacityReservationRequest.new(capacity_reservation_id: capacity_reservation_id, accept: accept, additional_info: additional_info, dry_run: dry_run, end_date: end_date, end_date_type: end_date_type, instance_count: instance_count, instance_match_criteria: instance_match_criteria)
        modify_capacity_reservation(input)
      end

      def modify_capacity_reservation(input : Types::ModifyCapacityReservationRequest) : Types::ModifyCapacityReservationResult
        request = Protocol::Ec2.build_request(Model::MODIFY_CAPACITY_RESERVATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyCapacityReservationResult, "ModifyCapacityReservation")
      end

      # Modifies a Capacity Reservation Fleet. When you modify the total target capacity of a Capacity
      # Reservation Fleet, the Fleet automatically creates new Capacity Reservations, or modifies or cancels
      # existing Capacity Reservations in the Fleet to meet the new total target capacity. When you modify
      # the end date for the Fleet, the end dates for all of the individual Capacity Reservations in the
      # Fleet are updated accordingly.

      def modify_capacity_reservation_fleet(
        capacity_reservation_fleet_id : String,
        dry_run : Bool? = nil,
        end_date : Time? = nil,
        remove_end_date : Bool? = nil,
        total_target_capacity : Int32? = nil
      ) : Types::ModifyCapacityReservationFleetResult

        input = Types::ModifyCapacityReservationFleetRequest.new(capacity_reservation_fleet_id: capacity_reservation_fleet_id, dry_run: dry_run, end_date: end_date, remove_end_date: remove_end_date, total_target_capacity: total_target_capacity)
        modify_capacity_reservation_fleet(input)
      end

      def modify_capacity_reservation_fleet(input : Types::ModifyCapacityReservationFleetRequest) : Types::ModifyCapacityReservationFleetResult
        request = Protocol::Ec2.build_request(Model::MODIFY_CAPACITY_RESERVATION_FLEET, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyCapacityReservationFleetResult, "ModifyCapacityReservationFleet")
      end

      # Modifies the specified Client VPN endpoint. Modifying the DNS server resets existing client
      # connections.

      def modify_client_vpn_endpoint(
        client_vpn_endpoint_id : String,
        client_connect_options : Types::ClientConnectOptions? = nil,
        client_login_banner_options : Types::ClientLoginBannerOptions? = nil,
        client_route_enforcement_options : Types::ClientRouteEnforcementOptions? = nil,
        connection_log_options : Types::ConnectionLogOptions? = nil,
        description : String? = nil,
        disconnect_on_session_timeout : Bool? = nil,
        dns_servers : Types::DnsServersOptionsModifyStructure? = nil,
        dry_run : Bool? = nil,
        security_group_ids : Array(String)? = nil,
        self_service_portal : String? = nil,
        server_certificate_arn : String? = nil,
        session_timeout_hours : Int32? = nil,
        split_tunnel : Bool? = nil,
        vpc_id : String? = nil,
        vpn_port : Int32? = nil
      ) : Types::ModifyClientVpnEndpointResult

        input = Types::ModifyClientVpnEndpointRequest.new(client_vpn_endpoint_id: client_vpn_endpoint_id, client_connect_options: client_connect_options, client_login_banner_options: client_login_banner_options, client_route_enforcement_options: client_route_enforcement_options, connection_log_options: connection_log_options, description: description, disconnect_on_session_timeout: disconnect_on_session_timeout, dns_servers: dns_servers, dry_run: dry_run, security_group_ids: security_group_ids, self_service_portal: self_service_portal, server_certificate_arn: server_certificate_arn, session_timeout_hours: session_timeout_hours, split_tunnel: split_tunnel, vpc_id: vpc_id, vpn_port: vpn_port)
        modify_client_vpn_endpoint(input)
      end

      def modify_client_vpn_endpoint(input : Types::ModifyClientVpnEndpointRequest) : Types::ModifyClientVpnEndpointResult
        request = Protocol::Ec2.build_request(Model::MODIFY_CLIENT_VPN_ENDPOINT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyClientVpnEndpointResult, "ModifyClientVpnEndpoint")
      end

      # Modifies the default credit option for CPU usage of burstable performance instances. The default
      # credit option is set at the account level per Amazon Web Services Region, and is specified per
      # instance family. All new burstable performance instances in the account launch using the default
      # credit option. ModifyDefaultCreditSpecification is an asynchronous operation, which works at an
      # Amazon Web Services Region level and modifies the credit option for each Availability Zone. All
      # zones in a Region are updated within five minutes. But if instances are launched during this
      # operation, they might not get the new credit option until the zone is updated. To verify whether the
      # update has occurred, you can call GetDefaultCreditSpecification and check DefaultCreditSpecification
      # for updates. For more information, see Burstable performance instances in the Amazon EC2 User Guide
      # .

      def modify_default_credit_specification(
        cpu_credits : String,
        instance_family : String,
        dry_run : Bool? = nil
      ) : Types::ModifyDefaultCreditSpecificationResult

        input = Types::ModifyDefaultCreditSpecificationRequest.new(cpu_credits: cpu_credits, instance_family: instance_family, dry_run: dry_run)
        modify_default_credit_specification(input)
      end

      def modify_default_credit_specification(input : Types::ModifyDefaultCreditSpecificationRequest) : Types::ModifyDefaultCreditSpecificationResult
        request = Protocol::Ec2.build_request(Model::MODIFY_DEFAULT_CREDIT_SPECIFICATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyDefaultCreditSpecificationResult, "ModifyDefaultCreditSpecification")
      end

      # Changes the default KMS key for EBS encryption by default for your account in this Region. Amazon
      # Web Services creates a unique Amazon Web Services managed KMS key in each Region for use with
      # encryption by default. If you change the default KMS key to a symmetric customer managed KMS key, it
      # is used instead of the Amazon Web Services managed KMS key. Amazon EBS does not support asymmetric
      # KMS keys. If you delete or disable the customer managed KMS key that you specified for use with
      # encryption by default, your instances will fail to launch. For more information, see Amazon EBS
      # encryption in the Amazon EBS User Guide .

      def modify_ebs_default_kms_key_id(
        kms_key_id : String,
        dry_run : Bool? = nil
      ) : Types::ModifyEbsDefaultKmsKeyIdResult

        input = Types::ModifyEbsDefaultKmsKeyIdRequest.new(kms_key_id: kms_key_id, dry_run: dry_run)
        modify_ebs_default_kms_key_id(input)
      end

      def modify_ebs_default_kms_key_id(input : Types::ModifyEbsDefaultKmsKeyIdRequest) : Types::ModifyEbsDefaultKmsKeyIdResult
        request = Protocol::Ec2.build_request(Model::MODIFY_EBS_DEFAULT_KMS_KEY_ID, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyEbsDefaultKmsKeyIdResult, "ModifyEbsDefaultKmsKeyId")
      end

      # Modifies the specified EC2 Fleet. You can only modify an EC2 Fleet request of type maintain . While
      # the EC2 Fleet is being modified, it is in the modifying state. To scale up your EC2 Fleet, increase
      # its target capacity. The EC2 Fleet launches the additional Spot Instances according to the
      # allocation strategy for the EC2 Fleet request. If the allocation strategy is lowest-price , the EC2
      # Fleet launches instances using the Spot Instance pool with the lowest price. If the allocation
      # strategy is diversified , the EC2 Fleet distributes the instances across the Spot Instance pools. If
      # the allocation strategy is capacity-optimized , EC2 Fleet launches instances from Spot Instance
      # pools with optimal capacity for the number of instances that are launching. To scale down your EC2
      # Fleet, decrease its target capacity. First, the EC2 Fleet cancels any open requests that exceed the
      # new target capacity. You can request that the EC2 Fleet terminate Spot Instances until the size of
      # the fleet no longer exceeds the new target capacity. If the allocation strategy is lowest-price ,
      # the EC2 Fleet terminates the instances with the highest price per unit. If the allocation strategy
      # is capacity-optimized , the EC2 Fleet terminates the instances in the Spot Instance pools that have
      # the least available Spot Instance capacity. If the allocation strategy is diversified , the EC2
      # Fleet terminates instances across the Spot Instance pools. Alternatively, you can request that the
      # EC2 Fleet keep the fleet at its current size, but not replace any Spot Instances that are
      # interrupted or that you terminate manually. If you are finished with your EC2 Fleet for now, but
      # will use it again later, you can set the target capacity to 0.

      def modify_fleet(
        fleet_id : String,
        context : String? = nil,
        dry_run : Bool? = nil,
        excess_capacity_termination_policy : String? = nil,
        launch_template_configs : Array(Types::FleetLaunchTemplateConfigRequest)? = nil,
        target_capacity_specification : Types::TargetCapacitySpecificationRequest? = nil
      ) : Types::ModifyFleetResult

        input = Types::ModifyFleetRequest.new(fleet_id: fleet_id, context: context, dry_run: dry_run, excess_capacity_termination_policy: excess_capacity_termination_policy, launch_template_configs: launch_template_configs, target_capacity_specification: target_capacity_specification)
        modify_fleet(input)
      end

      def modify_fleet(input : Types::ModifyFleetRequest) : Types::ModifyFleetResult
        request = Protocol::Ec2.build_request(Model::MODIFY_FLEET, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyFleetResult, "ModifyFleet")
      end

      # Modifies the specified attribute of the specified Amazon FPGA Image (AFI).

      def modify_fpga_image_attribute(
        fpga_image_id : String,
        attribute : String? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        load_permission : Types::LoadPermissionModifications? = nil,
        name : String? = nil,
        operation_type : String? = nil,
        product_codes : Array(String)? = nil,
        user_groups : Array(String)? = nil,
        user_ids : Array(String)? = nil
      ) : Types::ModifyFpgaImageAttributeResult

        input = Types::ModifyFpgaImageAttributeRequest.new(fpga_image_id: fpga_image_id, attribute: attribute, description: description, dry_run: dry_run, load_permission: load_permission, name: name, operation_type: operation_type, product_codes: product_codes, user_groups: user_groups, user_ids: user_ids)
        modify_fpga_image_attribute(input)
      end

      def modify_fpga_image_attribute(input : Types::ModifyFpgaImageAttributeRequest) : Types::ModifyFpgaImageAttributeResult
        request = Protocol::Ec2.build_request(Model::MODIFY_FPGA_IMAGE_ATTRIBUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyFpgaImageAttributeResult, "ModifyFpgaImageAttribute")
      end

      # Modify the auto-placement setting of a Dedicated Host. When auto-placement is enabled, any instances
      # that you launch with a tenancy of host but without a specific host ID are placed onto any available
      # Dedicated Host in your account that has auto-placement enabled. When auto-placement is disabled, you
      # need to provide a host ID to have the instance launch onto a specific host. If no host ID is
      # provided, the instance is launched onto a suitable host with auto-placement enabled. You can also
      # use this API action to modify a Dedicated Host to support either multiple instance types in an
      # instance family, or to support a specific instance type only.

      def modify_hosts(
        host_ids : Array(String),
        auto_placement : String? = nil,
        host_maintenance : String? = nil,
        host_recovery : String? = nil,
        instance_family : String? = nil,
        instance_type : String? = nil
      ) : Types::ModifyHostsResult

        input = Types::ModifyHostsRequest.new(host_ids: host_ids, auto_placement: auto_placement, host_maintenance: host_maintenance, host_recovery: host_recovery, instance_family: instance_family, instance_type: instance_type)
        modify_hosts(input)
      end

      def modify_hosts(input : Types::ModifyHostsRequest) : Types::ModifyHostsResult
        request = Protocol::Ec2.build_request(Model::MODIFY_HOSTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyHostsResult, "ModifyHosts")
      end

      # Modifies the ID format for the specified resource on a per-Region basis. You can specify that
      # resources should receive longer IDs (17-character IDs) when they are created. This request can only
      # be used to modify longer ID settings for resource types that are within the opt-in period. Resources
      # currently in their opt-in period include: bundle | conversion-task | customer-gateway | dhcp-options
      # | elastic-ip-allocation | elastic-ip-association | export-task | flow-log | image | import-task |
      # internet-gateway | network-acl | network-acl-association | network-interface |
      # network-interface-attachment | prefix-list | route-table | route-table-association | security-group
      # | subnet | subnet-cidr-block-association | vpc | vpc-cidr-block-association | vpc-endpoint |
      # vpc-peering-connection | vpn-connection | vpn-gateway . This setting applies to the IAM user who
      # makes the request; it does not apply to the entire Amazon Web Services account. By default, an IAM
      # user defaults to the same settings as the root user. If you're using this action as the root user,
      # then these settings apply to the entire account, unless an IAM user explicitly overrides these
      # settings for themselves. For more information, see Resource IDs in the Amazon Elastic Compute Cloud
      # User Guide . Resources created with longer IDs are visible to all IAM roles and users, regardless of
      # these settings and provided that they have permission to use the relevant Describe command for the
      # resource type.

      def modify_id_format(
        resource : String,
        use_long_ids : Bool
      ) : Nil

        input = Types::ModifyIdFormatRequest.new(resource: resource, use_long_ids: use_long_ids)
        modify_id_format(input)
      end

      def modify_id_format(input : Types::ModifyIdFormatRequest) : Nil
        request = Protocol::Ec2.build_request(Model::MODIFY_ID_FORMAT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Modifies the ID format of a resource for a specified IAM user, IAM role, or the root user for an
      # account; or all IAM users, IAM roles, and the root user for an account. You can specify that
      # resources should receive longer IDs (17-character IDs) when they are created. This request can only
      # be used to modify longer ID settings for resource types that are within the opt-in period. Resources
      # currently in their opt-in period include: bundle | conversion-task | customer-gateway | dhcp-options
      # | elastic-ip-allocation | elastic-ip-association | export-task | flow-log | image | import-task |
      # internet-gateway | network-acl | network-acl-association | network-interface |
      # network-interface-attachment | prefix-list | route-table | route-table-association | security-group
      # | subnet | subnet-cidr-block-association | vpc | vpc-cidr-block-association | vpc-endpoint |
      # vpc-peering-connection | vpn-connection | vpn-gateway . For more information, see Resource IDs in
      # the Amazon Elastic Compute Cloud User Guide . This setting applies to the principal specified in the
      # request; it does not apply to the principal that makes the request. Resources created with longer
      # IDs are visible to all IAM roles and users, regardless of these settings and provided that they have
      # permission to use the relevant Describe command for the resource type.

      def modify_identity_id_format(
        principal_arn : String,
        resource : String,
        use_long_ids : Bool
      ) : Nil

        input = Types::ModifyIdentityIdFormatRequest.new(principal_arn: principal_arn, resource: resource, use_long_ids: use_long_ids)
        modify_identity_id_format(input)
      end

      def modify_identity_id_format(input : Types::ModifyIdentityIdFormatRequest) : Nil
        request = Protocol::Ec2.build_request(Model::MODIFY_IDENTITY_ID_FORMAT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Modifies the specified attribute of the specified AMI. You can specify only one attribute at a time.
      # To specify the attribute, you can use the Attribute parameter, or one of the following parameters:
      # Description , ImdsSupport , or LaunchPermission . Images with an Amazon Web Services Marketplace
      # product code cannot be made public. To enable the SriovNetSupport enhanced networking attribute of
      # an image, enable SriovNetSupport on an instance and create an AMI from the instance.

      def modify_image_attribute(
        image_id : String,
        attribute : String? = nil,
        description : Types::AttributeValue? = nil,
        dry_run : Bool? = nil,
        imds_support : Types::AttributeValue? = nil,
        launch_permission : Types::LaunchPermissionModifications? = nil,
        operation_type : String? = nil,
        organization_arns : Array(String)? = nil,
        organizational_unit_arns : Array(String)? = nil,
        product_codes : Array(String)? = nil,
        user_groups : Array(String)? = nil,
        user_ids : Array(String)? = nil,
        value : String? = nil
      ) : Nil

        input = Types::ModifyImageAttributeRequest.new(image_id: image_id, attribute: attribute, description: description, dry_run: dry_run, imds_support: imds_support, launch_permission: launch_permission, operation_type: operation_type, organization_arns: organization_arns, organizational_unit_arns: organizational_unit_arns, product_codes: product_codes, user_groups: user_groups, user_ids: user_ids, value: value)
        modify_image_attribute(input)
      end

      def modify_image_attribute(input : Types::ModifyImageAttributeRequest) : Nil
        request = Protocol::Ec2.build_request(Model::MODIFY_IMAGE_ATTRIBUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Modifies the specified attribute of the specified instance. You can specify only one attribute at a
      # time. Note: Using this action to change the security groups associated with an elastic network
      # interface (ENI) attached to an instance can result in an error if the instance has more than one
      # ENI. To change the security groups associated with an ENI attached to an instance that has multiple
      # ENIs, we recommend that you use the ModifyNetworkInterfaceAttribute action. To modify some
      # attributes, the instance must be stopped. For more information, see Modify a stopped instance in the
      # Amazon EC2 User Guide .

      def modify_instance_attribute(
        instance_id : String,
        attribute : String? = nil,
        block_device_mappings : Array(Types::InstanceBlockDeviceMappingSpecification)? = nil,
        disable_api_stop : Types::AttributeBooleanValue? = nil,
        disable_api_termination : Types::AttributeBooleanValue? = nil,
        dry_run : Bool? = nil,
        ebs_optimized : Types::AttributeBooleanValue? = nil,
        ena_support : Types::AttributeBooleanValue? = nil,
        groups : Array(String)? = nil,
        instance_initiated_shutdown_behavior : Types::AttributeValue? = nil,
        instance_type : Types::AttributeValue? = nil,
        kernel : Types::AttributeValue? = nil,
        ramdisk : Types::AttributeValue? = nil,
        source_dest_check : Types::AttributeBooleanValue? = nil,
        sriov_net_support : Types::AttributeValue? = nil,
        user_data : Types::BlobAttributeValue? = nil,
        value : String? = nil
      ) : Nil

        input = Types::ModifyInstanceAttributeRequest.new(instance_id: instance_id, attribute: attribute, block_device_mappings: block_device_mappings, disable_api_stop: disable_api_stop, disable_api_termination: disable_api_termination, dry_run: dry_run, ebs_optimized: ebs_optimized, ena_support: ena_support, groups: groups, instance_initiated_shutdown_behavior: instance_initiated_shutdown_behavior, instance_type: instance_type, kernel: kernel, ramdisk: ramdisk, source_dest_check: source_dest_check, sriov_net_support: sriov_net_support, user_data: user_data, value: value)
        modify_instance_attribute(input)
      end

      def modify_instance_attribute(input : Types::ModifyInstanceAttributeRequest) : Nil
        request = Protocol::Ec2.build_request(Model::MODIFY_INSTANCE_ATTRIBUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Modifies the Capacity Reservation settings for a stopped instance. Use this action to configure an
      # instance to target a specific Capacity Reservation, run in any open Capacity Reservation with
      # matching attributes, run in On-Demand Instance capacity, or only run in a Capacity Reservation.

      def modify_instance_capacity_reservation_attributes(
        capacity_reservation_specification : Types::CapacityReservationSpecification,
        instance_id : String,
        dry_run : Bool? = nil
      ) : Types::ModifyInstanceCapacityReservationAttributesResult

        input = Types::ModifyInstanceCapacityReservationAttributesRequest.new(capacity_reservation_specification: capacity_reservation_specification, instance_id: instance_id, dry_run: dry_run)
        modify_instance_capacity_reservation_attributes(input)
      end

      def modify_instance_capacity_reservation_attributes(input : Types::ModifyInstanceCapacityReservationAttributesRequest) : Types::ModifyInstanceCapacityReservationAttributesResult
        request = Protocol::Ec2.build_request(Model::MODIFY_INSTANCE_CAPACITY_RESERVATION_ATTRIBUTES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyInstanceCapacityReservationAttributesResult, "ModifyInstanceCapacityReservationAttributes")
      end

      # Modifies the specified EC2 Instance Connect Endpoint. For more information, see Modify an EC2
      # Instance Connect Endpoint in the Amazon EC2 User Guide .

      def modify_instance_connect_endpoint(
        instance_connect_endpoint_id : String,
        dry_run : Bool? = nil,
        ip_address_type : String? = nil,
        preserve_client_ip : Bool? = nil,
        security_group_ids : Array(String)? = nil
      ) : Types::ModifyInstanceConnectEndpointResult

        input = Types::ModifyInstanceConnectEndpointRequest.new(instance_connect_endpoint_id: instance_connect_endpoint_id, dry_run: dry_run, ip_address_type: ip_address_type, preserve_client_ip: preserve_client_ip, security_group_ids: security_group_ids)
        modify_instance_connect_endpoint(input)
      end

      def modify_instance_connect_endpoint(input : Types::ModifyInstanceConnectEndpointRequest) : Types::ModifyInstanceConnectEndpointResult
        request = Protocol::Ec2.build_request(Model::MODIFY_INSTANCE_CONNECT_ENDPOINT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyInstanceConnectEndpointResult, "ModifyInstanceConnectEndpoint")
      end

      # By default, all vCPUs for the instance type are active when you launch an instance. When you
      # configure the number of active vCPUs for the instance, it can help you save on licensing costs and
      # optimize performance. The base cost of the instance remains unchanged. The number of active vCPUs
      # equals the number of threads per CPU core multiplied by the number of cores. The instance must be in
      # a Stopped state before you make changes. Some instance type options do not support this capability.
      # For more information, see Supported CPU options in the Amazon EC2 User Guide .

      def modify_instance_cpu_options(
        core_count : Int32,
        instance_id : String,
        threads_per_core : Int32,
        dry_run : Bool? = nil
      ) : Types::ModifyInstanceCpuOptionsResult

        input = Types::ModifyInstanceCpuOptionsRequest.new(core_count: core_count, instance_id: instance_id, threads_per_core: threads_per_core, dry_run: dry_run)
        modify_instance_cpu_options(input)
      end

      def modify_instance_cpu_options(input : Types::ModifyInstanceCpuOptionsRequest) : Types::ModifyInstanceCpuOptionsResult
        request = Protocol::Ec2.build_request(Model::MODIFY_INSTANCE_CPU_OPTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyInstanceCpuOptionsResult, "ModifyInstanceCpuOptions")
      end

      # Modifies the credit option for CPU usage on a running or stopped burstable performance instance. The
      # credit options are standard and unlimited . For more information, see Burstable performance
      # instances in the Amazon EC2 User Guide .

      def modify_instance_credit_specification(
        instance_credit_specifications : Array(Types::InstanceCreditSpecificationRequest),
        client_token : String? = nil,
        dry_run : Bool? = nil
      ) : Types::ModifyInstanceCreditSpecificationResult

        input = Types::ModifyInstanceCreditSpecificationRequest.new(instance_credit_specifications: instance_credit_specifications, client_token: client_token, dry_run: dry_run)
        modify_instance_credit_specification(input)
      end

      def modify_instance_credit_specification(input : Types::ModifyInstanceCreditSpecificationRequest) : Types::ModifyInstanceCreditSpecificationResult
        request = Protocol::Ec2.build_request(Model::MODIFY_INSTANCE_CREDIT_SPECIFICATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyInstanceCreditSpecificationResult, "ModifyInstanceCreditSpecification")
      end

      # Modifies the start time for a scheduled Amazon EC2 instance event.

      def modify_instance_event_start_time(
        instance_event_id : String,
        instance_id : String,
        not_before : Time,
        dry_run : Bool? = nil
      ) : Types::ModifyInstanceEventStartTimeResult

        input = Types::ModifyInstanceEventStartTimeRequest.new(instance_event_id: instance_event_id, instance_id: instance_id, not_before: not_before, dry_run: dry_run)
        modify_instance_event_start_time(input)
      end

      def modify_instance_event_start_time(input : Types::ModifyInstanceEventStartTimeRequest) : Types::ModifyInstanceEventStartTimeResult
        request = Protocol::Ec2.build_request(Model::MODIFY_INSTANCE_EVENT_START_TIME, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyInstanceEventStartTimeResult, "ModifyInstanceEventStartTime")
      end

      # Modifies the specified event window. You can define either a set of time ranges or a cron expression
      # when modifying the event window, but not both. To modify the targets associated with the event
      # window, use the AssociateInstanceEventWindow and DisassociateInstanceEventWindow API. If Amazon Web
      # Services has already scheduled an event, modifying an event window won't change the time of the
      # scheduled event. For more information, see Define event windows for scheduled events in the Amazon
      # EC2 User Guide .

      def modify_instance_event_window(
        instance_event_window_id : String,
        cron_expression : String? = nil,
        dry_run : Bool? = nil,
        name : String? = nil,
        time_ranges : Array(Types::InstanceEventWindowTimeRangeRequest)? = nil
      ) : Types::ModifyInstanceEventWindowResult

        input = Types::ModifyInstanceEventWindowRequest.new(instance_event_window_id: instance_event_window_id, cron_expression: cron_expression, dry_run: dry_run, name: name, time_ranges: time_ranges)
        modify_instance_event_window(input)
      end

      def modify_instance_event_window(input : Types::ModifyInstanceEventWindowRequest) : Types::ModifyInstanceEventWindowResult
        request = Protocol::Ec2.build_request(Model::MODIFY_INSTANCE_EVENT_WINDOW, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyInstanceEventWindowResult, "ModifyInstanceEventWindow")
      end

      # Modifies the recovery behavior of your instance to disable simplified automatic recovery or set the
      # recovery behavior to default. The default configuration will not enable simplified automatic
      # recovery for an unsupported instance type. For more information, see Simplified automatic recovery .
      # Modifies the reboot migration behavior during a user-initiated reboot of an instance that has a
      # pending system-reboot event. For more information, see Enable or disable reboot migration .

      def modify_instance_maintenance_options(
        instance_id : String,
        auto_recovery : String? = nil,
        dry_run : Bool? = nil,
        reboot_migration : String? = nil
      ) : Types::ModifyInstanceMaintenanceOptionsResult

        input = Types::ModifyInstanceMaintenanceOptionsRequest.new(instance_id: instance_id, auto_recovery: auto_recovery, dry_run: dry_run, reboot_migration: reboot_migration)
        modify_instance_maintenance_options(input)
      end

      def modify_instance_maintenance_options(input : Types::ModifyInstanceMaintenanceOptionsRequest) : Types::ModifyInstanceMaintenanceOptionsResult
        request = Protocol::Ec2.build_request(Model::MODIFY_INSTANCE_MAINTENANCE_OPTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyInstanceMaintenanceOptionsResult, "ModifyInstanceMaintenanceOptions")
      end

      # Modifies the default instance metadata service (IMDS) settings at the account level in the specified
      # Amazon Web Services&#x2028; Region. To remove a parameter's account-level default setting, specify
      # no-preference . If an account-level setting is cleared with no-preference , then the instance launch
      # considers the other instance metadata settings. For more information, see Order of precedence for
      # instance metadata options in the Amazon EC2 User Guide .

      def modify_instance_metadata_defaults(
        dry_run : Bool? = nil,
        http_endpoint : String? = nil,
        http_put_response_hop_limit : Int32? = nil,
        http_tokens : String? = nil,
        instance_metadata_tags : String? = nil
      ) : Types::ModifyInstanceMetadataDefaultsResult

        input = Types::ModifyInstanceMetadataDefaultsRequest.new(dry_run: dry_run, http_endpoint: http_endpoint, http_put_response_hop_limit: http_put_response_hop_limit, http_tokens: http_tokens, instance_metadata_tags: instance_metadata_tags)
        modify_instance_metadata_defaults(input)
      end

      def modify_instance_metadata_defaults(input : Types::ModifyInstanceMetadataDefaultsRequest) : Types::ModifyInstanceMetadataDefaultsResult
        request = Protocol::Ec2.build_request(Model::MODIFY_INSTANCE_METADATA_DEFAULTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyInstanceMetadataDefaultsResult, "ModifyInstanceMetadataDefaults")
      end

      # Modify the instance metadata parameters on a running or stopped instance. When you modify the
      # parameters on a stopped instance, they are applied when the instance is started. When you modify the
      # parameters on a running instance, the API responds with a state of “pending”. After the parameter
      # modifications are successfully applied to the instance, the state of the modifications changes from
      # “pending” to “applied” in subsequent describe-instances API calls. For more information, see
      # Instance metadata and user data in the Amazon EC2 User Guide .

      def modify_instance_metadata_options(
        instance_id : String,
        dry_run : Bool? = nil,
        http_endpoint : String? = nil,
        http_protocol_ipv6 : String? = nil,
        http_put_response_hop_limit : Int32? = nil,
        http_tokens : String? = nil,
        instance_metadata_tags : String? = nil
      ) : Types::ModifyInstanceMetadataOptionsResult

        input = Types::ModifyInstanceMetadataOptionsRequest.new(instance_id: instance_id, dry_run: dry_run, http_endpoint: http_endpoint, http_protocol_ipv6: http_protocol_ipv6, http_put_response_hop_limit: http_put_response_hop_limit, http_tokens: http_tokens, instance_metadata_tags: instance_metadata_tags)
        modify_instance_metadata_options(input)
      end

      def modify_instance_metadata_options(input : Types::ModifyInstanceMetadataOptionsRequest) : Types::ModifyInstanceMetadataOptionsResult
        request = Protocol::Ec2.build_request(Model::MODIFY_INSTANCE_METADATA_OPTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyInstanceMetadataOptionsResult, "ModifyInstanceMetadataOptions")
      end

      # Change the configuration of the network performance options for an existing instance.

      def modify_instance_network_performance_options(
        bandwidth_weighting : String,
        instance_id : String,
        dry_run : Bool? = nil
      ) : Types::ModifyInstanceNetworkPerformanceResult

        input = Types::ModifyInstanceNetworkPerformanceRequest.new(bandwidth_weighting: bandwidth_weighting, instance_id: instance_id, dry_run: dry_run)
        modify_instance_network_performance_options(input)
      end

      def modify_instance_network_performance_options(input : Types::ModifyInstanceNetworkPerformanceRequest) : Types::ModifyInstanceNetworkPerformanceResult
        request = Protocol::Ec2.build_request(Model::MODIFY_INSTANCE_NETWORK_PERFORMANCE_OPTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyInstanceNetworkPerformanceResult, "ModifyInstanceNetworkPerformanceOptions")
      end

      # Modifies the placement attributes for a specified instance. You can do the following: Modify the
      # affinity between an instance and a Dedicated Host . When affinity is set to host and the instance is
      # not associated with a specific Dedicated Host, the next time the instance is started, it is
      # automatically associated with the host on which it lands. If the instance is restarted or rebooted,
      # this relationship persists. Change the Dedicated Host with which an instance is associated. Change
      # the instance tenancy of an instance. Move an instance to or from a placement group . At least one
      # attribute for affinity, host ID, tenancy, or placement group name must be specified in the request.
      # Affinity and tenancy can be modified in the same request. To modify the host ID, tenancy, placement
      # group, or partition for an instance, the instance must be in the stopped state.

      def modify_instance_placement(
        instance_id : String,
        affinity : String? = nil,
        group_id : String? = nil,
        group_name : String? = nil,
        host_id : String? = nil,
        host_resource_group_arn : String? = nil,
        partition_number : Int32? = nil,
        tenancy : String? = nil
      ) : Types::ModifyInstancePlacementResult

        input = Types::ModifyInstancePlacementRequest.new(instance_id: instance_id, affinity: affinity, group_id: group_id, group_name: group_name, host_id: host_id, host_resource_group_arn: host_resource_group_arn, partition_number: partition_number, tenancy: tenancy)
        modify_instance_placement(input)
      end

      def modify_instance_placement(input : Types::ModifyInstancePlacementRequest) : Types::ModifyInstancePlacementResult
        request = Protocol::Ec2.build_request(Model::MODIFY_INSTANCE_PLACEMENT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyInstancePlacementResult, "ModifyInstancePlacement")
      end

      # Modify the configurations of an IPAM.

      def modify_ipam(
        ipam_id : String,
        add_operating_regions : Array(Types::AddIpamOperatingRegion)? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        enable_private_gua : Bool? = nil,
        metered_account : String? = nil,
        remove_operating_regions : Array(Types::RemoveIpamOperatingRegion)? = nil,
        tier : String? = nil
      ) : Types::ModifyIpamResult

        input = Types::ModifyIpamRequest.new(ipam_id: ipam_id, add_operating_regions: add_operating_regions, description: description, dry_run: dry_run, enable_private_gua: enable_private_gua, metered_account: metered_account, remove_operating_regions: remove_operating_regions, tier: tier)
        modify_ipam(input)
      end

      def modify_ipam(input : Types::ModifyIpamRequest) : Types::ModifyIpamResult
        request = Protocol::Ec2.build_request(Model::MODIFY_IPAM, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyIpamResult, "ModifyIpam")
      end

      # Modifies the allocation rules in an IPAM policy. An IPAM policy is a set of rules that define how
      # public IPv4 addresses from IPAM pools are allocated to Amazon Web Services resources. Each rule maps
      # an Amazon Web Services service to IPAM pools that the service will use to get IP addresses. A single
      # policy can have multiple rules and be applied to multiple Amazon Web Services Regions. If the IPAM
      # pool run out of addresses then the services fallback to Amazon-provided IP addresses. A policy can
      # be applied to an individual Amazon Web Services account or an entity within Amazon Web Services
      # Organizations. Allocation rules are optional configurations within an IPAM policy that map Amazon
      # Web Services resource types to specific IPAM pools. If no rules are defined, the resource types
      # default to using Amazon-provided IP addresses.

      def modify_ipam_policy_allocation_rules(
        ipam_policy_id : String,
        locale : String,
        resource_type : String,
        allocation_rules : Array(Types::IpamPolicyAllocationRuleRequest)? = nil,
        dry_run : Bool? = nil
      ) : Types::ModifyIpamPolicyAllocationRulesResult

        input = Types::ModifyIpamPolicyAllocationRulesRequest.new(ipam_policy_id: ipam_policy_id, locale: locale, resource_type: resource_type, allocation_rules: allocation_rules, dry_run: dry_run)
        modify_ipam_policy_allocation_rules(input)
      end

      def modify_ipam_policy_allocation_rules(input : Types::ModifyIpamPolicyAllocationRulesRequest) : Types::ModifyIpamPolicyAllocationRulesResult
        request = Protocol::Ec2.build_request(Model::MODIFY_IPAM_POLICY_ALLOCATION_RULES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyIpamPolicyAllocationRulesResult, "ModifyIpamPolicyAllocationRules")
      end

      # Modify the configurations of an IPAM pool. For more information, see Modify a pool in the Amazon VPC
      # IPAM User Guide .

      def modify_ipam_pool(
        ipam_pool_id : String,
        add_allocation_resource_tags : Array(Types::RequestIpamResourceTag)? = nil,
        allocation_default_netmask_length : Int32? = nil,
        allocation_max_netmask_length : Int32? = nil,
        allocation_min_netmask_length : Int32? = nil,
        auto_import : Bool? = nil,
        clear_allocation_default_netmask_length : Bool? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        remove_allocation_resource_tags : Array(Types::RequestIpamResourceTag)? = nil
      ) : Types::ModifyIpamPoolResult

        input = Types::ModifyIpamPoolRequest.new(ipam_pool_id: ipam_pool_id, add_allocation_resource_tags: add_allocation_resource_tags, allocation_default_netmask_length: allocation_default_netmask_length, allocation_max_netmask_length: allocation_max_netmask_length, allocation_min_netmask_length: allocation_min_netmask_length, auto_import: auto_import, clear_allocation_default_netmask_length: clear_allocation_default_netmask_length, description: description, dry_run: dry_run, remove_allocation_resource_tags: remove_allocation_resource_tags)
        modify_ipam_pool(input)
      end

      def modify_ipam_pool(input : Types::ModifyIpamPoolRequest) : Types::ModifyIpamPoolResult
        request = Protocol::Ec2.build_request(Model::MODIFY_IPAM_POOL, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyIpamPoolResult, "ModifyIpamPool")
      end

      # Modifies an IPAM prefix list resolver. You can update the description and CIDR selection rules.
      # Changes to rules will trigger re-evaluation and potential updates to associated prefix lists.

      def modify_ipam_prefix_list_resolver(
        ipam_prefix_list_resolver_id : String,
        description : String? = nil,
        dry_run : Bool? = nil,
        rules : Array(Types::IpamPrefixListResolverRuleRequest)? = nil
      ) : Types::ModifyIpamPrefixListResolverResult

        input = Types::ModifyIpamPrefixListResolverRequest.new(ipam_prefix_list_resolver_id: ipam_prefix_list_resolver_id, description: description, dry_run: dry_run, rules: rules)
        modify_ipam_prefix_list_resolver(input)
      end

      def modify_ipam_prefix_list_resolver(input : Types::ModifyIpamPrefixListResolverRequest) : Types::ModifyIpamPrefixListResolverResult
        request = Protocol::Ec2.build_request(Model::MODIFY_IPAM_PREFIX_LIST_RESOLVER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyIpamPrefixListResolverResult, "ModifyIpamPrefixListResolver")
      end

      # Modifies an IPAM prefix list resolver target. You can update version tracking settings and the
      # desired version of the target prefix list.

      def modify_ipam_prefix_list_resolver_target(
        ipam_prefix_list_resolver_target_id : String,
        client_token : String? = nil,
        desired_version : Int64? = nil,
        dry_run : Bool? = nil,
        track_latest_version : Bool? = nil
      ) : Types::ModifyIpamPrefixListResolverTargetResult

        input = Types::ModifyIpamPrefixListResolverTargetRequest.new(ipam_prefix_list_resolver_target_id: ipam_prefix_list_resolver_target_id, client_token: client_token, desired_version: desired_version, dry_run: dry_run, track_latest_version: track_latest_version)
        modify_ipam_prefix_list_resolver_target(input)
      end

      def modify_ipam_prefix_list_resolver_target(input : Types::ModifyIpamPrefixListResolverTargetRequest) : Types::ModifyIpamPrefixListResolverTargetResult
        request = Protocol::Ec2.build_request(Model::MODIFY_IPAM_PREFIX_LIST_RESOLVER_TARGET, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyIpamPrefixListResolverTargetResult, "ModifyIpamPrefixListResolverTarget")
      end

      # Modify a resource CIDR. You can use this action to transfer resource CIDRs between scopes and ignore
      # resource CIDRs that you do not want to manage. If set to false, the resource will not be tracked for
      # overlap, it cannot be auto-imported into a pool, and it will be removed from any pool it has an
      # allocation in. For more information, see Move resource CIDRs between scopes and Change the
      # monitoring state of resource CIDRs in the Amazon VPC IPAM User Guide .

      def modify_ipam_resource_cidr(
        current_ipam_scope_id : String,
        monitored : Bool,
        resource_cidr : String,
        resource_id : String,
        resource_region : String,
        destination_ipam_scope_id : String? = nil,
        dry_run : Bool? = nil
      ) : Types::ModifyIpamResourceCidrResult

        input = Types::ModifyIpamResourceCidrRequest.new(current_ipam_scope_id: current_ipam_scope_id, monitored: monitored, resource_cidr: resource_cidr, resource_id: resource_id, resource_region: resource_region, destination_ipam_scope_id: destination_ipam_scope_id, dry_run: dry_run)
        modify_ipam_resource_cidr(input)
      end

      def modify_ipam_resource_cidr(input : Types::ModifyIpamResourceCidrRequest) : Types::ModifyIpamResourceCidrResult
        request = Protocol::Ec2.build_request(Model::MODIFY_IPAM_RESOURCE_CIDR, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyIpamResourceCidrResult, "ModifyIpamResourceCidr")
      end

      # Modifies a resource discovery. A resource discovery is an IPAM component that enables IPAM to manage
      # and monitor resources that belong to the owning account.

      def modify_ipam_resource_discovery(
        ipam_resource_discovery_id : String,
        add_operating_regions : Array(Types::AddIpamOperatingRegion)? = nil,
        add_organizational_unit_exclusions : Array(Types::AddIpamOrganizationalUnitExclusion)? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        remove_operating_regions : Array(Types::RemoveIpamOperatingRegion)? = nil,
        remove_organizational_unit_exclusions : Array(Types::RemoveIpamOrganizationalUnitExclusion)? = nil
      ) : Types::ModifyIpamResourceDiscoveryResult

        input = Types::ModifyIpamResourceDiscoveryRequest.new(ipam_resource_discovery_id: ipam_resource_discovery_id, add_operating_regions: add_operating_regions, add_organizational_unit_exclusions: add_organizational_unit_exclusions, description: description, dry_run: dry_run, remove_operating_regions: remove_operating_regions, remove_organizational_unit_exclusions: remove_organizational_unit_exclusions)
        modify_ipam_resource_discovery(input)
      end

      def modify_ipam_resource_discovery(input : Types::ModifyIpamResourceDiscoveryRequest) : Types::ModifyIpamResourceDiscoveryResult
        request = Protocol::Ec2.build_request(Model::MODIFY_IPAM_RESOURCE_DISCOVERY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyIpamResourceDiscoveryResult, "ModifyIpamResourceDiscovery")
      end

      # Modify an IPAM scope.

      def modify_ipam_scope(
        ipam_scope_id : String,
        description : String? = nil,
        dry_run : Bool? = nil,
        external_authority_configuration : Types::ExternalAuthorityConfiguration? = nil,
        remove_external_authority_configuration : Bool? = nil
      ) : Types::ModifyIpamScopeResult

        input = Types::ModifyIpamScopeRequest.new(ipam_scope_id: ipam_scope_id, description: description, dry_run: dry_run, external_authority_configuration: external_authority_configuration, remove_external_authority_configuration: remove_external_authority_configuration)
        modify_ipam_scope(input)
      end

      def modify_ipam_scope(input : Types::ModifyIpamScopeRequest) : Types::ModifyIpamScopeResult
        request = Protocol::Ec2.build_request(Model::MODIFY_IPAM_SCOPE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyIpamScopeResult, "ModifyIpamScope")
      end

      # Modifies a launch template. You can specify which version of the launch template to set as the
      # default version. When launching an instance, the default version applies when a launch template
      # version is not specified.

      def modify_launch_template(
        client_token : String? = nil,
        default_version : String? = nil,
        dry_run : Bool? = nil,
        launch_template_id : String? = nil,
        launch_template_name : String? = nil
      ) : Types::ModifyLaunchTemplateResult

        input = Types::ModifyLaunchTemplateRequest.new(client_token: client_token, default_version: default_version, dry_run: dry_run, launch_template_id: launch_template_id, launch_template_name: launch_template_name)
        modify_launch_template(input)
      end

      def modify_launch_template(input : Types::ModifyLaunchTemplateRequest) : Types::ModifyLaunchTemplateResult
        request = Protocol::Ec2.build_request(Model::MODIFY_LAUNCH_TEMPLATE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyLaunchTemplateResult, "ModifyLaunchTemplate")
      end

      # Modifies the specified local gateway route.

      def modify_local_gateway_route(
        local_gateway_route_table_id : String,
        destination_cidr_block : String? = nil,
        destination_prefix_list_id : String? = nil,
        dry_run : Bool? = nil,
        local_gateway_virtual_interface_group_id : String? = nil,
        network_interface_id : String? = nil
      ) : Types::ModifyLocalGatewayRouteResult

        input = Types::ModifyLocalGatewayRouteRequest.new(local_gateway_route_table_id: local_gateway_route_table_id, destination_cidr_block: destination_cidr_block, destination_prefix_list_id: destination_prefix_list_id, dry_run: dry_run, local_gateway_virtual_interface_group_id: local_gateway_virtual_interface_group_id, network_interface_id: network_interface_id)
        modify_local_gateway_route(input)
      end

      def modify_local_gateway_route(input : Types::ModifyLocalGatewayRouteRequest) : Types::ModifyLocalGatewayRouteResult
        request = Protocol::Ec2.build_request(Model::MODIFY_LOCAL_GATEWAY_ROUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyLocalGatewayRouteResult, "ModifyLocalGatewayRoute")
      end

      # Modifies the specified managed prefix list. Adding or removing entries in a prefix list creates a
      # new version of the prefix list. Changing the name of the prefix list does not affect the version. If
      # you specify a current version number that does not match the true current version number, the
      # request fails.

      def modify_managed_prefix_list(
        prefix_list_id : String,
        add_entries : Array(Types::AddPrefixListEntry)? = nil,
        current_version : Int64? = nil,
        dry_run : Bool? = nil,
        ipam_prefix_list_resolver_sync_enabled : Bool? = nil,
        max_entries : Int32? = nil,
        prefix_list_name : String? = nil,
        remove_entries : Array(Types::RemovePrefixListEntry)? = nil
      ) : Types::ModifyManagedPrefixListResult

        input = Types::ModifyManagedPrefixListRequest.new(prefix_list_id: prefix_list_id, add_entries: add_entries, current_version: current_version, dry_run: dry_run, ipam_prefix_list_resolver_sync_enabled: ipam_prefix_list_resolver_sync_enabled, max_entries: max_entries, prefix_list_name: prefix_list_name, remove_entries: remove_entries)
        modify_managed_prefix_list(input)
      end

      def modify_managed_prefix_list(input : Types::ModifyManagedPrefixListRequest) : Types::ModifyManagedPrefixListResult
        request = Protocol::Ec2.build_request(Model::MODIFY_MANAGED_PREFIX_LIST, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyManagedPrefixListResult, "ModifyManagedPrefixList")
      end

      # Modifies the specified network interface attribute. You can specify only one attribute at a time.
      # You can use this action to attach and detach security groups from an existing EC2 instance.

      def modify_network_interface_attribute(
        network_interface_id : String,
        associate_public_ip_address : Bool? = nil,
        associated_subnet_ids : Array(String)? = nil,
        attachment : Types::NetworkInterfaceAttachmentChanges? = nil,
        connection_tracking_specification : Types::ConnectionTrackingSpecificationRequest? = nil,
        description : Types::AttributeValue? = nil,
        dry_run : Bool? = nil,
        ena_srd_specification : Types::EnaSrdSpecification? = nil,
        enable_primary_ipv6 : Bool? = nil,
        groups : Array(String)? = nil,
        source_dest_check : Types::AttributeBooleanValue? = nil
      ) : Nil

        input = Types::ModifyNetworkInterfaceAttributeRequest.new(network_interface_id: network_interface_id, associate_public_ip_address: associate_public_ip_address, associated_subnet_ids: associated_subnet_ids, attachment: attachment, connection_tracking_specification: connection_tracking_specification, description: description, dry_run: dry_run, ena_srd_specification: ena_srd_specification, enable_primary_ipv6: enable_primary_ipv6, groups: groups, source_dest_check: source_dest_check)
        modify_network_interface_attribute(input)
      end

      def modify_network_interface_attribute(input : Types::ModifyNetworkInterfaceAttributeRequest) : Nil
        request = Protocol::Ec2.build_request(Model::MODIFY_NETWORK_INTERFACE_ATTRIBUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Modifies the options for instance hostnames for the specified instance.

      def modify_private_dns_name_options(
        instance_id : String,
        dry_run : Bool? = nil,
        enable_resource_name_dns_aaaa_record : Bool? = nil,
        enable_resource_name_dns_a_record : Bool? = nil,
        private_dns_hostname_type : String? = nil
      ) : Types::ModifyPrivateDnsNameOptionsResult

        input = Types::ModifyPrivateDnsNameOptionsRequest.new(instance_id: instance_id, dry_run: dry_run, enable_resource_name_dns_aaaa_record: enable_resource_name_dns_aaaa_record, enable_resource_name_dns_a_record: enable_resource_name_dns_a_record, private_dns_hostname_type: private_dns_hostname_type)
        modify_private_dns_name_options(input)
      end

      def modify_private_dns_name_options(input : Types::ModifyPrivateDnsNameOptionsRequest) : Types::ModifyPrivateDnsNameOptionsResult
        request = Protocol::Ec2.build_request(Model::MODIFY_PRIVATE_DNS_NAME_OPTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyPrivateDnsNameOptionsResult, "ModifyPrivateDnsNameOptions")
      end

      # Modify public hostname options for a network interface. For more information, see EC2 instance
      # hostnames, DNS names, and domains in the Amazon EC2 User Guide .

      def modify_public_ip_dns_name_options(
        hostname_type : String,
        network_interface_id : String,
        dry_run : Bool? = nil
      ) : Types::ModifyPublicIpDnsNameOptionsResult

        input = Types::ModifyPublicIpDnsNameOptionsRequest.new(hostname_type: hostname_type, network_interface_id: network_interface_id, dry_run: dry_run)
        modify_public_ip_dns_name_options(input)
      end

      def modify_public_ip_dns_name_options(input : Types::ModifyPublicIpDnsNameOptionsRequest) : Types::ModifyPublicIpDnsNameOptionsResult
        request = Protocol::Ec2.build_request(Model::MODIFY_PUBLIC_IP_DNS_NAME_OPTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyPublicIpDnsNameOptionsResult, "ModifyPublicIpDnsNameOptions")
      end

      # Modifies the configuration of your Reserved Instances, such as the Availability Zone, instance
      # count, or instance type. The Reserved Instances to be modified must be identical, except for
      # Availability Zone, network platform, and instance type. For more information, see Modify Reserved
      # Instances in the Amazon EC2 User Guide .

      def modify_reserved_instances(
        reserved_instances_ids : Array(String),
        target_configurations : Array(Types::ReservedInstancesConfiguration),
        client_token : String? = nil
      ) : Types::ModifyReservedInstancesResult

        input = Types::ModifyReservedInstancesRequest.new(reserved_instances_ids: reserved_instances_ids, target_configurations: target_configurations, client_token: client_token)
        modify_reserved_instances(input)
      end

      def modify_reserved_instances(input : Types::ModifyReservedInstancesRequest) : Types::ModifyReservedInstancesResult
        request = Protocol::Ec2.build_request(Model::MODIFY_RESERVED_INSTANCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyReservedInstancesResult, "ModifyReservedInstances")
      end

      # Modifies the configuration of an existing route server. Amazon VPC Route Server simplifies routing
      # for traffic between workloads that are deployed within a VPC and its internet gateways. With this
      # feature, VPC Route Server dynamically updates VPC and internet gateway route tables with your
      # preferred IPv4 or IPv6 routes to achieve routing fault tolerance for those workloads. This enables
      # you to automatically reroute traffic within a VPC, which increases the manageability of VPC routing
      # and interoperability with third-party workloads. Route server supports the follow route table types:
      # VPC route tables not associated with subnets Subnet route tables Internet gateway route tables Route
      # server does not support route tables associated with virtual private gateways. To propagate routes
      # into a transit gateway route table, use Transit Gateway Connect . For more information see Dynamic
      # routing in your VPC with VPC Route Server in the Amazon VPC User Guide .

      def modify_route_server(
        route_server_id : String,
        dry_run : Bool? = nil,
        persist_routes : String? = nil,
        persist_routes_duration : Int64? = nil,
        sns_notifications_enabled : Bool? = nil
      ) : Types::ModifyRouteServerResult

        input = Types::ModifyRouteServerRequest.new(route_server_id: route_server_id, dry_run: dry_run, persist_routes: persist_routes, persist_routes_duration: persist_routes_duration, sns_notifications_enabled: sns_notifications_enabled)
        modify_route_server(input)
      end

      def modify_route_server(input : Types::ModifyRouteServerRequest) : Types::ModifyRouteServerResult
        request = Protocol::Ec2.build_request(Model::MODIFY_ROUTE_SERVER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyRouteServerResult, "ModifyRouteServer")
      end

      # Modifies the rules of a security group.

      def modify_security_group_rules(
        group_id : String,
        security_group_rules : Array(Types::SecurityGroupRuleUpdate),
        dry_run : Bool? = nil
      ) : Types::ModifySecurityGroupRulesResult

        input = Types::ModifySecurityGroupRulesRequest.new(group_id: group_id, security_group_rules: security_group_rules, dry_run: dry_run)
        modify_security_group_rules(input)
      end

      def modify_security_group_rules(input : Types::ModifySecurityGroupRulesRequest) : Types::ModifySecurityGroupRulesResult
        request = Protocol::Ec2.build_request(Model::MODIFY_SECURITY_GROUP_RULES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifySecurityGroupRulesResult, "ModifySecurityGroupRules")
      end

      # Adds or removes permission settings for the specified snapshot. You may add or remove specified
      # Amazon Web Services account IDs from a snapshot's list of create volume permissions, but you cannot
      # do both in a single operation. If you need to both add and remove account IDs for a snapshot, you
      # must use multiple operations. You can make up to 500 modifications to a snapshot in a single
      # operation. Encrypted snapshots and snapshots with Amazon Web Services Marketplace product codes
      # cannot be made public. Snapshots encrypted with your default KMS key cannot be shared with other
      # accounts. For more information about modifying snapshot permissions, see Share a snapshot in the
      # Amazon EBS User Guide .

      def modify_snapshot_attribute(
        snapshot_id : String,
        attribute : String? = nil,
        create_volume_permission : Types::CreateVolumePermissionModifications? = nil,
        dry_run : Bool? = nil,
        group_names : Array(String)? = nil,
        operation_type : String? = nil,
        user_ids : Array(String)? = nil
      ) : Nil

        input = Types::ModifySnapshotAttributeRequest.new(snapshot_id: snapshot_id, attribute: attribute, create_volume_permission: create_volume_permission, dry_run: dry_run, group_names: group_names, operation_type: operation_type, user_ids: user_ids)
        modify_snapshot_attribute(input)
      end

      def modify_snapshot_attribute(input : Types::ModifySnapshotAttributeRequest) : Nil
        request = Protocol::Ec2.build_request(Model::MODIFY_SNAPSHOT_ATTRIBUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Archives an Amazon EBS snapshot. When you archive a snapshot, it is converted to a full snapshot
      # that includes all of the blocks of data that were written to the volume at the time the snapshot was
      # created, and moved from the standard tier to the archive tier. For more information, see Archive
      # Amazon EBS snapshots in the Amazon EBS User Guide .

      def modify_snapshot_tier(
        snapshot_id : String,
        dry_run : Bool? = nil,
        storage_tier : String? = nil
      ) : Types::ModifySnapshotTierResult

        input = Types::ModifySnapshotTierRequest.new(snapshot_id: snapshot_id, dry_run: dry_run, storage_tier: storage_tier)
        modify_snapshot_tier(input)
      end

      def modify_snapshot_tier(input : Types::ModifySnapshotTierRequest) : Types::ModifySnapshotTierResult
        request = Protocol::Ec2.build_request(Model::MODIFY_SNAPSHOT_TIER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifySnapshotTierResult, "ModifySnapshotTier")
      end

      # Modifies the specified Spot Fleet request. You can only modify a Spot Fleet request of type maintain
      # . While the Spot Fleet request is being modified, it is in the modifying state. To scale up your
      # Spot Fleet, increase its target capacity. The Spot Fleet launches the additional Spot Instances
      # according to the allocation strategy for the Spot Fleet request. If the allocation strategy is
      # lowestPrice , the Spot Fleet launches instances using the Spot Instance pool with the lowest price.
      # If the allocation strategy is diversified , the Spot Fleet distributes the instances across the Spot
      # Instance pools. If the allocation strategy is capacityOptimized , Spot Fleet launches instances from
      # Spot Instance pools with optimal capacity for the number of instances that are launching. To scale
      # down your Spot Fleet, decrease its target capacity. First, the Spot Fleet cancels any open requests
      # that exceed the new target capacity. You can request that the Spot Fleet terminate Spot Instances
      # until the size of the fleet no longer exceeds the new target capacity. If the allocation strategy is
      # lowestPrice , the Spot Fleet terminates the instances with the highest price per unit. If the
      # allocation strategy is capacityOptimized , the Spot Fleet terminates the instances in the Spot
      # Instance pools that have the least available Spot Instance capacity. If the allocation strategy is
      # diversified , the Spot Fleet terminates instances across the Spot Instance pools. Alternatively, you
      # can request that the Spot Fleet keep the fleet at its current size, but not replace any Spot
      # Instances that are interrupted or that you terminate manually. If you are finished with your Spot
      # Fleet for now, but will use it again later, you can set the target capacity to 0.

      def modify_spot_fleet_request(
        spot_fleet_request_id : String,
        context : String? = nil,
        excess_capacity_termination_policy : String? = nil,
        launch_template_configs : Array(Types::LaunchTemplateConfig)? = nil,
        on_demand_target_capacity : Int32? = nil,
        target_capacity : Int32? = nil
      ) : Types::ModifySpotFleetRequestResponse

        input = Types::ModifySpotFleetRequestRequest.new(spot_fleet_request_id: spot_fleet_request_id, context: context, excess_capacity_termination_policy: excess_capacity_termination_policy, launch_template_configs: launch_template_configs, on_demand_target_capacity: on_demand_target_capacity, target_capacity: target_capacity)
        modify_spot_fleet_request(input)
      end

      def modify_spot_fleet_request(input : Types::ModifySpotFleetRequestRequest) : Types::ModifySpotFleetRequestResponse
        request = Protocol::Ec2.build_request(Model::MODIFY_SPOT_FLEET_REQUEST, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifySpotFleetRequestResponse, "ModifySpotFleetRequest")
      end

      # Modifies a subnet attribute. You can only modify one attribute at a time. Use this action to modify
      # subnets on Amazon Web Services Outposts. To modify a subnet on an Outpost rack, set both
      # MapCustomerOwnedIpOnLaunch and CustomerOwnedIpv4Pool . These two parameters act as a single
      # attribute. To modify a subnet on an Outpost server, set either EnableLniAtDeviceIndex or
      # DisableLniAtDeviceIndex . For more information about Amazon Web Services Outposts, see the
      # following: Outpost servers Outpost racks

      def modify_subnet_attribute(
        subnet_id : String,
        assign_ipv6_address_on_creation : Types::AttributeBooleanValue? = nil,
        customer_owned_ipv4_pool : String? = nil,
        disable_lni_at_device_index : Types::AttributeBooleanValue? = nil,
        enable_dns64 : Types::AttributeBooleanValue? = nil,
        enable_lni_at_device_index : Int32? = nil,
        enable_resource_name_dns_aaaa_record_on_launch : Types::AttributeBooleanValue? = nil,
        enable_resource_name_dns_a_record_on_launch : Types::AttributeBooleanValue? = nil,
        map_customer_owned_ip_on_launch : Types::AttributeBooleanValue? = nil,
        map_public_ip_on_launch : Types::AttributeBooleanValue? = nil,
        private_dns_hostname_type_on_launch : String? = nil
      ) : Nil

        input = Types::ModifySubnetAttributeRequest.new(subnet_id: subnet_id, assign_ipv6_address_on_creation: assign_ipv6_address_on_creation, customer_owned_ipv4_pool: customer_owned_ipv4_pool, disable_lni_at_device_index: disable_lni_at_device_index, enable_dns64: enable_dns64, enable_lni_at_device_index: enable_lni_at_device_index, enable_resource_name_dns_aaaa_record_on_launch: enable_resource_name_dns_aaaa_record_on_launch, enable_resource_name_dns_a_record_on_launch: enable_resource_name_dns_a_record_on_launch, map_customer_owned_ip_on_launch: map_customer_owned_ip_on_launch, map_public_ip_on_launch: map_public_ip_on_launch, private_dns_hostname_type_on_launch: private_dns_hostname_type_on_launch)
        modify_subnet_attribute(input)
      end

      def modify_subnet_attribute(input : Types::ModifySubnetAttributeRequest) : Nil
        request = Protocol::Ec2.build_request(Model::MODIFY_SUBNET_ATTRIBUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Allows or restricts mirroring network services. By default, Amazon DNS network services are not
      # eligible for Traffic Mirror. Use AddNetworkServices to add network services to a Traffic Mirror
      # filter. When a network service is added to the Traffic Mirror filter, all traffic related to that
      # network service will be mirrored. When you no longer want to mirror network services, use
      # RemoveNetworkServices to remove the network services from the Traffic Mirror filter.

      def modify_traffic_mirror_filter_network_services(
        traffic_mirror_filter_id : String,
        add_network_services : Array(String)? = nil,
        dry_run : Bool? = nil,
        remove_network_services : Array(String)? = nil
      ) : Types::ModifyTrafficMirrorFilterNetworkServicesResult

        input = Types::ModifyTrafficMirrorFilterNetworkServicesRequest.new(traffic_mirror_filter_id: traffic_mirror_filter_id, add_network_services: add_network_services, dry_run: dry_run, remove_network_services: remove_network_services)
        modify_traffic_mirror_filter_network_services(input)
      end

      def modify_traffic_mirror_filter_network_services(input : Types::ModifyTrafficMirrorFilterNetworkServicesRequest) : Types::ModifyTrafficMirrorFilterNetworkServicesResult
        request = Protocol::Ec2.build_request(Model::MODIFY_TRAFFIC_MIRROR_FILTER_NETWORK_SERVICES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyTrafficMirrorFilterNetworkServicesResult, "ModifyTrafficMirrorFilterNetworkServices")
      end

      # Modifies the specified Traffic Mirror rule. DestinationCidrBlock and SourceCidrBlock must both be an
      # IPv4 range or an IPv6 range.

      def modify_traffic_mirror_filter_rule(
        traffic_mirror_filter_rule_id : String,
        description : String? = nil,
        destination_cidr_block : String? = nil,
        destination_port_range : Types::TrafficMirrorPortRangeRequest? = nil,
        dry_run : Bool? = nil,
        protocol : Int32? = nil,
        remove_fields : Array(String)? = nil,
        rule_action : String? = nil,
        rule_number : Int32? = nil,
        source_cidr_block : String? = nil,
        source_port_range : Types::TrafficMirrorPortRangeRequest? = nil,
        traffic_direction : String? = nil
      ) : Types::ModifyTrafficMirrorFilterRuleResult

        input = Types::ModifyTrafficMirrorFilterRuleRequest.new(traffic_mirror_filter_rule_id: traffic_mirror_filter_rule_id, description: description, destination_cidr_block: destination_cidr_block, destination_port_range: destination_port_range, dry_run: dry_run, protocol: protocol, remove_fields: remove_fields, rule_action: rule_action, rule_number: rule_number, source_cidr_block: source_cidr_block, source_port_range: source_port_range, traffic_direction: traffic_direction)
        modify_traffic_mirror_filter_rule(input)
      end

      def modify_traffic_mirror_filter_rule(input : Types::ModifyTrafficMirrorFilterRuleRequest) : Types::ModifyTrafficMirrorFilterRuleResult
        request = Protocol::Ec2.build_request(Model::MODIFY_TRAFFIC_MIRROR_FILTER_RULE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyTrafficMirrorFilterRuleResult, "ModifyTrafficMirrorFilterRule")
      end

      # Modifies a Traffic Mirror session.

      def modify_traffic_mirror_session(
        traffic_mirror_session_id : String,
        description : String? = nil,
        dry_run : Bool? = nil,
        packet_length : Int32? = nil,
        remove_fields : Array(String)? = nil,
        session_number : Int32? = nil,
        traffic_mirror_filter_id : String? = nil,
        traffic_mirror_target_id : String? = nil,
        virtual_network_id : Int32? = nil
      ) : Types::ModifyTrafficMirrorSessionResult

        input = Types::ModifyTrafficMirrorSessionRequest.new(traffic_mirror_session_id: traffic_mirror_session_id, description: description, dry_run: dry_run, packet_length: packet_length, remove_fields: remove_fields, session_number: session_number, traffic_mirror_filter_id: traffic_mirror_filter_id, traffic_mirror_target_id: traffic_mirror_target_id, virtual_network_id: virtual_network_id)
        modify_traffic_mirror_session(input)
      end

      def modify_traffic_mirror_session(input : Types::ModifyTrafficMirrorSessionRequest) : Types::ModifyTrafficMirrorSessionResult
        request = Protocol::Ec2.build_request(Model::MODIFY_TRAFFIC_MIRROR_SESSION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyTrafficMirrorSessionResult, "ModifyTrafficMirrorSession")
      end

      # Modifies the specified transit gateway. When you modify a transit gateway, the modified options are
      # applied to new transit gateway attachments only. Your existing transit gateway attachments are not
      # modified.

      def modify_transit_gateway(
        transit_gateway_id : String,
        description : String? = nil,
        dry_run : Bool? = nil,
        options : Types::ModifyTransitGatewayOptions? = nil
      ) : Types::ModifyTransitGatewayResult

        input = Types::ModifyTransitGatewayRequest.new(transit_gateway_id: transit_gateway_id, description: description, dry_run: dry_run, options: options)
        modify_transit_gateway(input)
      end

      def modify_transit_gateway(input : Types::ModifyTransitGatewayRequest) : Types::ModifyTransitGatewayResult
        request = Protocol::Ec2.build_request(Model::MODIFY_TRANSIT_GATEWAY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyTransitGatewayResult, "ModifyTransitGateway")
      end

      # Modifies a transit gateway metering policy.

      def modify_transit_gateway_metering_policy(
        transit_gateway_metering_policy_id : String,
        add_middlebox_attachment_ids : Array(String)? = nil,
        dry_run : Bool? = nil,
        remove_middlebox_attachment_ids : Array(String)? = nil
      ) : Types::ModifyTransitGatewayMeteringPolicyResult

        input = Types::ModifyTransitGatewayMeteringPolicyRequest.new(transit_gateway_metering_policy_id: transit_gateway_metering_policy_id, add_middlebox_attachment_ids: add_middlebox_attachment_ids, dry_run: dry_run, remove_middlebox_attachment_ids: remove_middlebox_attachment_ids)
        modify_transit_gateway_metering_policy(input)
      end

      def modify_transit_gateway_metering_policy(input : Types::ModifyTransitGatewayMeteringPolicyRequest) : Types::ModifyTransitGatewayMeteringPolicyResult
        request = Protocol::Ec2.build_request(Model::MODIFY_TRANSIT_GATEWAY_METERING_POLICY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyTransitGatewayMeteringPolicyResult, "ModifyTransitGatewayMeteringPolicy")
      end

      # Modifies a reference (route) to a prefix list in a specified transit gateway route table.

      def modify_transit_gateway_prefix_list_reference(
        prefix_list_id : String,
        transit_gateway_route_table_id : String,
        blackhole : Bool? = nil,
        dry_run : Bool? = nil,
        transit_gateway_attachment_id : String? = nil
      ) : Types::ModifyTransitGatewayPrefixListReferenceResult

        input = Types::ModifyTransitGatewayPrefixListReferenceRequest.new(prefix_list_id: prefix_list_id, transit_gateway_route_table_id: transit_gateway_route_table_id, blackhole: blackhole, dry_run: dry_run, transit_gateway_attachment_id: transit_gateway_attachment_id)
        modify_transit_gateway_prefix_list_reference(input)
      end

      def modify_transit_gateway_prefix_list_reference(input : Types::ModifyTransitGatewayPrefixListReferenceRequest) : Types::ModifyTransitGatewayPrefixListReferenceResult
        request = Protocol::Ec2.build_request(Model::MODIFY_TRANSIT_GATEWAY_PREFIX_LIST_REFERENCE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyTransitGatewayPrefixListReferenceResult, "ModifyTransitGatewayPrefixListReference")
      end

      # Modifies the specified VPC attachment.

      def modify_transit_gateway_vpc_attachment(
        transit_gateway_attachment_id : String,
        add_subnet_ids : Array(String)? = nil,
        dry_run : Bool? = nil,
        options : Types::ModifyTransitGatewayVpcAttachmentRequestOptions? = nil,
        remove_subnet_ids : Array(String)? = nil
      ) : Types::ModifyTransitGatewayVpcAttachmentResult

        input = Types::ModifyTransitGatewayVpcAttachmentRequest.new(transit_gateway_attachment_id: transit_gateway_attachment_id, add_subnet_ids: add_subnet_ids, dry_run: dry_run, options: options, remove_subnet_ids: remove_subnet_ids)
        modify_transit_gateway_vpc_attachment(input)
      end

      def modify_transit_gateway_vpc_attachment(input : Types::ModifyTransitGatewayVpcAttachmentRequest) : Types::ModifyTransitGatewayVpcAttachmentResult
        request = Protocol::Ec2.build_request(Model::MODIFY_TRANSIT_GATEWAY_VPC_ATTACHMENT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyTransitGatewayVpcAttachmentResult, "ModifyTransitGatewayVpcAttachment")
      end

      # Modifies the configuration of the specified Amazon Web Services Verified Access endpoint.

      def modify_verified_access_endpoint(
        verified_access_endpoint_id : String,
        cidr_options : Types::ModifyVerifiedAccessEndpointCidrOptions? = nil,
        client_token : String? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        load_balancer_options : Types::ModifyVerifiedAccessEndpointLoadBalancerOptions? = nil,
        network_interface_options : Types::ModifyVerifiedAccessEndpointEniOptions? = nil,
        rds_options : Types::ModifyVerifiedAccessEndpointRdsOptions? = nil,
        verified_access_group_id : String? = nil
      ) : Types::ModifyVerifiedAccessEndpointResult

        input = Types::ModifyVerifiedAccessEndpointRequest.new(verified_access_endpoint_id: verified_access_endpoint_id, cidr_options: cidr_options, client_token: client_token, description: description, dry_run: dry_run, load_balancer_options: load_balancer_options, network_interface_options: network_interface_options, rds_options: rds_options, verified_access_group_id: verified_access_group_id)
        modify_verified_access_endpoint(input)
      end

      def modify_verified_access_endpoint(input : Types::ModifyVerifiedAccessEndpointRequest) : Types::ModifyVerifiedAccessEndpointResult
        request = Protocol::Ec2.build_request(Model::MODIFY_VERIFIED_ACCESS_ENDPOINT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyVerifiedAccessEndpointResult, "ModifyVerifiedAccessEndpoint")
      end

      # Modifies the specified Amazon Web Services Verified Access endpoint policy.

      def modify_verified_access_endpoint_policy(
        verified_access_endpoint_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        policy_document : String? = nil,
        policy_enabled : Bool? = nil,
        sse_specification : Types::VerifiedAccessSseSpecificationRequest? = nil
      ) : Types::ModifyVerifiedAccessEndpointPolicyResult

        input = Types::ModifyVerifiedAccessEndpointPolicyRequest.new(verified_access_endpoint_id: verified_access_endpoint_id, client_token: client_token, dry_run: dry_run, policy_document: policy_document, policy_enabled: policy_enabled, sse_specification: sse_specification)
        modify_verified_access_endpoint_policy(input)
      end

      def modify_verified_access_endpoint_policy(input : Types::ModifyVerifiedAccessEndpointPolicyRequest) : Types::ModifyVerifiedAccessEndpointPolicyResult
        request = Protocol::Ec2.build_request(Model::MODIFY_VERIFIED_ACCESS_ENDPOINT_POLICY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyVerifiedAccessEndpointPolicyResult, "ModifyVerifiedAccessEndpointPolicy")
      end

      # Modifies the specified Amazon Web Services Verified Access group configuration.

      def modify_verified_access_group(
        verified_access_group_id : String,
        client_token : String? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        verified_access_instance_id : String? = nil
      ) : Types::ModifyVerifiedAccessGroupResult

        input = Types::ModifyVerifiedAccessGroupRequest.new(verified_access_group_id: verified_access_group_id, client_token: client_token, description: description, dry_run: dry_run, verified_access_instance_id: verified_access_instance_id)
        modify_verified_access_group(input)
      end

      def modify_verified_access_group(input : Types::ModifyVerifiedAccessGroupRequest) : Types::ModifyVerifiedAccessGroupResult
        request = Protocol::Ec2.build_request(Model::MODIFY_VERIFIED_ACCESS_GROUP, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyVerifiedAccessGroupResult, "ModifyVerifiedAccessGroup")
      end

      # Modifies the specified Amazon Web Services Verified Access group policy.

      def modify_verified_access_group_policy(
        verified_access_group_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        policy_document : String? = nil,
        policy_enabled : Bool? = nil,
        sse_specification : Types::VerifiedAccessSseSpecificationRequest? = nil
      ) : Types::ModifyVerifiedAccessGroupPolicyResult

        input = Types::ModifyVerifiedAccessGroupPolicyRequest.new(verified_access_group_id: verified_access_group_id, client_token: client_token, dry_run: dry_run, policy_document: policy_document, policy_enabled: policy_enabled, sse_specification: sse_specification)
        modify_verified_access_group_policy(input)
      end

      def modify_verified_access_group_policy(input : Types::ModifyVerifiedAccessGroupPolicyRequest) : Types::ModifyVerifiedAccessGroupPolicyResult
        request = Protocol::Ec2.build_request(Model::MODIFY_VERIFIED_ACCESS_GROUP_POLICY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyVerifiedAccessGroupPolicyResult, "ModifyVerifiedAccessGroupPolicy")
      end

      # Modifies the configuration of the specified Amazon Web Services Verified Access instance.

      def modify_verified_access_instance(
        verified_access_instance_id : String,
        cidr_endpoints_custom_sub_domain : String? = nil,
        client_token : String? = nil,
        description : String? = nil,
        dry_run : Bool? = nil
      ) : Types::ModifyVerifiedAccessInstanceResult

        input = Types::ModifyVerifiedAccessInstanceRequest.new(verified_access_instance_id: verified_access_instance_id, cidr_endpoints_custom_sub_domain: cidr_endpoints_custom_sub_domain, client_token: client_token, description: description, dry_run: dry_run)
        modify_verified_access_instance(input)
      end

      def modify_verified_access_instance(input : Types::ModifyVerifiedAccessInstanceRequest) : Types::ModifyVerifiedAccessInstanceResult
        request = Protocol::Ec2.build_request(Model::MODIFY_VERIFIED_ACCESS_INSTANCE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyVerifiedAccessInstanceResult, "ModifyVerifiedAccessInstance")
      end

      # Modifies the logging configuration for the specified Amazon Web Services Verified Access instance.

      def modify_verified_access_instance_logging_configuration(
        access_logs : Types::VerifiedAccessLogOptions,
        verified_access_instance_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil
      ) : Types::ModifyVerifiedAccessInstanceLoggingConfigurationResult

        input = Types::ModifyVerifiedAccessInstanceLoggingConfigurationRequest.new(access_logs: access_logs, verified_access_instance_id: verified_access_instance_id, client_token: client_token, dry_run: dry_run)
        modify_verified_access_instance_logging_configuration(input)
      end

      def modify_verified_access_instance_logging_configuration(input : Types::ModifyVerifiedAccessInstanceLoggingConfigurationRequest) : Types::ModifyVerifiedAccessInstanceLoggingConfigurationResult
        request = Protocol::Ec2.build_request(Model::MODIFY_VERIFIED_ACCESS_INSTANCE_LOGGING_CONFIGURATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyVerifiedAccessInstanceLoggingConfigurationResult, "ModifyVerifiedAccessInstanceLoggingConfiguration")
      end

      # Modifies the configuration of the specified Amazon Web Services Verified Access trust provider.

      def modify_verified_access_trust_provider(
        verified_access_trust_provider_id : String,
        client_token : String? = nil,
        description : String? = nil,
        device_options : Types::ModifyVerifiedAccessTrustProviderDeviceOptions? = nil,
        dry_run : Bool? = nil,
        native_application_oidc_options : Types::ModifyVerifiedAccessNativeApplicationOidcOptions? = nil,
        oidc_options : Types::ModifyVerifiedAccessTrustProviderOidcOptions? = nil,
        sse_specification : Types::VerifiedAccessSseSpecificationRequest? = nil
      ) : Types::ModifyVerifiedAccessTrustProviderResult

        input = Types::ModifyVerifiedAccessTrustProviderRequest.new(verified_access_trust_provider_id: verified_access_trust_provider_id, client_token: client_token, description: description, device_options: device_options, dry_run: dry_run, native_application_oidc_options: native_application_oidc_options, oidc_options: oidc_options, sse_specification: sse_specification)
        modify_verified_access_trust_provider(input)
      end

      def modify_verified_access_trust_provider(input : Types::ModifyVerifiedAccessTrustProviderRequest) : Types::ModifyVerifiedAccessTrustProviderResult
        request = Protocol::Ec2.build_request(Model::MODIFY_VERIFIED_ACCESS_TRUST_PROVIDER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyVerifiedAccessTrustProviderResult, "ModifyVerifiedAccessTrustProvider")
      end

      # You can modify several parameters of an existing EBS volume, including volume size, volume type, and
      # IOPS capacity. If your EBS volume is attached to a current-generation EC2 instance type, you might
      # be able to apply these changes without stopping the instance or detaching the volume from it. For
      # more information about modifying EBS volumes, see Amazon EBS Elastic Volumes in the Amazon EBS User
      # Guide . When you complete a resize operation on your volume, you need to extend the volume's
      # file-system size to take advantage of the new storage capacity. For more information, see Extend the
      # file system . For more information, see Monitor the progress of volume modifications in the Amazon
      # EBS User Guide . With previous-generation instance types, resizing an EBS volume might require
      # detaching and reattaching the volume or stopping and restarting the instance. After you initiate a
      # volume modification, you must wait for that modification to reach the completed state before you can
      # initiate another modification for the same volume. You can modify a volume up to four times within a
      # rolling 24-hour period, as long as the volume is in the in-use or available state, and all previous
      # modifications for that volume are completed . If you exceed this limit, you get an error message
      # that indicates when you can perform your next modification.

      def modify_volume(
        volume_id : String,
        dry_run : Bool? = nil,
        iops : Int32? = nil,
        multi_attach_enabled : Bool? = nil,
        size : Int32? = nil,
        throughput : Int32? = nil,
        volume_type : String? = nil
      ) : Types::ModifyVolumeResult

        input = Types::ModifyVolumeRequest.new(volume_id: volume_id, dry_run: dry_run, iops: iops, multi_attach_enabled: multi_attach_enabled, size: size, throughput: throughput, volume_type: volume_type)
        modify_volume(input)
      end

      def modify_volume(input : Types::ModifyVolumeRequest) : Types::ModifyVolumeResult
        request = Protocol::Ec2.build_request(Model::MODIFY_VOLUME, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyVolumeResult, "ModifyVolume")
      end

      # Modifies a volume attribute. By default, all I/O operations for the volume are suspended when the
      # data on the volume is determined to be potentially inconsistent, to prevent undetectable, latent
      # data corruption. The I/O access to the volume can be resumed by first enabling I/O access and then
      # checking the data consistency on your volume. You can change the default behavior to resume I/O
      # operations. We recommend that you change this only for boot volumes or for volumes that are
      # stateless or disposable.

      def modify_volume_attribute(
        volume_id : String,
        auto_enable_io : Types::AttributeBooleanValue? = nil,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::ModifyVolumeAttributeRequest.new(volume_id: volume_id, auto_enable_io: auto_enable_io, dry_run: dry_run)
        modify_volume_attribute(input)
      end

      def modify_volume_attribute(input : Types::ModifyVolumeAttributeRequest) : Nil
        request = Protocol::Ec2.build_request(Model::MODIFY_VOLUME_ATTRIBUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Modifies the specified attribute of the specified VPC.

      def modify_vpc_attribute(
        vpc_id : String,
        enable_dns_hostnames : Types::AttributeBooleanValue? = nil,
        enable_dns_support : Types::AttributeBooleanValue? = nil,
        enable_network_address_usage_metrics : Types::AttributeBooleanValue? = nil
      ) : Nil

        input = Types::ModifyVpcAttributeRequest.new(vpc_id: vpc_id, enable_dns_hostnames: enable_dns_hostnames, enable_dns_support: enable_dns_support, enable_network_address_usage_metrics: enable_network_address_usage_metrics)
        modify_vpc_attribute(input)
      end

      def modify_vpc_attribute(input : Types::ModifyVpcAttributeRequest) : Nil
        request = Protocol::Ec2.build_request(Model::MODIFY_VPC_ATTRIBUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Modify VPC Block Public Access (BPA) exclusions. A VPC BPA exclusion is a mode that can be applied
      # to a single VPC or subnet that exempts it from the account’s BPA mode and will allow bidirectional
      # or egress-only access. You can create BPA exclusions for VPCs and subnets even when BPA is not
      # enabled on the account to ensure that there is no traffic disruption to the exclusions when VPC BPA
      # is turned on.

      def modify_vpc_block_public_access_exclusion(
        exclusion_id : String,
        internet_gateway_exclusion_mode : String,
        dry_run : Bool? = nil
      ) : Types::ModifyVpcBlockPublicAccessExclusionResult

        input = Types::ModifyVpcBlockPublicAccessExclusionRequest.new(exclusion_id: exclusion_id, internet_gateway_exclusion_mode: internet_gateway_exclusion_mode, dry_run: dry_run)
        modify_vpc_block_public_access_exclusion(input)
      end

      def modify_vpc_block_public_access_exclusion(input : Types::ModifyVpcBlockPublicAccessExclusionRequest) : Types::ModifyVpcBlockPublicAccessExclusionResult
        request = Protocol::Ec2.build_request(Model::MODIFY_VPC_BLOCK_PUBLIC_ACCESS_EXCLUSION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyVpcBlockPublicAccessExclusionResult, "ModifyVpcBlockPublicAccessExclusion")
      end

      # Modify VPC Block Public Access (BPA) options. VPC Block Public Access (BPA) enables you to block
      # resources in VPCs and subnets that you own in a Region from reaching or being reached from the
      # internet through internet gateways and egress-only internet gateways. To learn more about VPC BPA,
      # see Block public access to VPCs and subnets in the Amazon VPC User Guide .

      def modify_vpc_block_public_access_options(
        internet_gateway_block_mode : String,
        dry_run : Bool? = nil
      ) : Types::ModifyVpcBlockPublicAccessOptionsResult

        input = Types::ModifyVpcBlockPublicAccessOptionsRequest.new(internet_gateway_block_mode: internet_gateway_block_mode, dry_run: dry_run)
        modify_vpc_block_public_access_options(input)
      end

      def modify_vpc_block_public_access_options(input : Types::ModifyVpcBlockPublicAccessOptionsRequest) : Types::ModifyVpcBlockPublicAccessOptionsResult
        request = Protocol::Ec2.build_request(Model::MODIFY_VPC_BLOCK_PUBLIC_ACCESS_OPTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyVpcBlockPublicAccessOptionsResult, "ModifyVpcBlockPublicAccessOptions")
      end

      # Modifies the encryption control configuration for a VPC. You can update the encryption mode and
      # exclusion settings for various gateway types and peering connections. For more information, see
      # Enforce VPC encryption in transit in the Amazon VPC User Guide .

      def modify_vpc_encryption_control(
        vpc_encryption_control_id : String,
        dry_run : Bool? = nil,
        egress_only_internet_gateway_exclusion : String? = nil,
        elastic_file_system_exclusion : String? = nil,
        internet_gateway_exclusion : String? = nil,
        lambda_exclusion : String? = nil,
        mode : String? = nil,
        nat_gateway_exclusion : String? = nil,
        virtual_private_gateway_exclusion : String? = nil,
        vpc_lattice_exclusion : String? = nil,
        vpc_peering_exclusion : String? = nil
      ) : Types::ModifyVpcEncryptionControlResult

        input = Types::ModifyVpcEncryptionControlRequest.new(vpc_encryption_control_id: vpc_encryption_control_id, dry_run: dry_run, egress_only_internet_gateway_exclusion: egress_only_internet_gateway_exclusion, elastic_file_system_exclusion: elastic_file_system_exclusion, internet_gateway_exclusion: internet_gateway_exclusion, lambda_exclusion: lambda_exclusion, mode: mode, nat_gateway_exclusion: nat_gateway_exclusion, virtual_private_gateway_exclusion: virtual_private_gateway_exclusion, vpc_lattice_exclusion: vpc_lattice_exclusion, vpc_peering_exclusion: vpc_peering_exclusion)
        modify_vpc_encryption_control(input)
      end

      def modify_vpc_encryption_control(input : Types::ModifyVpcEncryptionControlRequest) : Types::ModifyVpcEncryptionControlResult
        request = Protocol::Ec2.build_request(Model::MODIFY_VPC_ENCRYPTION_CONTROL, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyVpcEncryptionControlResult, "ModifyVpcEncryptionControl")
      end

      # Modifies attributes of a specified VPC endpoint. The attributes that you can modify depend on the
      # type of VPC endpoint (interface, gateway, or Gateway Load Balancer). For more information, see the
      # Amazon Web Services PrivateLink Guide .

      def modify_vpc_endpoint(
        vpc_endpoint_id : String,
        add_route_table_ids : Array(String)? = nil,
        add_security_group_ids : Array(String)? = nil,
        add_subnet_ids : Array(String)? = nil,
        dns_options : Types::DnsOptionsSpecification? = nil,
        dry_run : Bool? = nil,
        ip_address_type : String? = nil,
        policy_document : String? = nil,
        private_dns_enabled : Bool? = nil,
        remove_route_table_ids : Array(String)? = nil,
        remove_security_group_ids : Array(String)? = nil,
        remove_subnet_ids : Array(String)? = nil,
        reset_policy : Bool? = nil,
        subnet_configurations : Array(Types::SubnetConfiguration)? = nil
      ) : Types::ModifyVpcEndpointResult

        input = Types::ModifyVpcEndpointRequest.new(vpc_endpoint_id: vpc_endpoint_id, add_route_table_ids: add_route_table_ids, add_security_group_ids: add_security_group_ids, add_subnet_ids: add_subnet_ids, dns_options: dns_options, dry_run: dry_run, ip_address_type: ip_address_type, policy_document: policy_document, private_dns_enabled: private_dns_enabled, remove_route_table_ids: remove_route_table_ids, remove_security_group_ids: remove_security_group_ids, remove_subnet_ids: remove_subnet_ids, reset_policy: reset_policy, subnet_configurations: subnet_configurations)
        modify_vpc_endpoint(input)
      end

      def modify_vpc_endpoint(input : Types::ModifyVpcEndpointRequest) : Types::ModifyVpcEndpointResult
        request = Protocol::Ec2.build_request(Model::MODIFY_VPC_ENDPOINT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyVpcEndpointResult, "ModifyVpcEndpoint")
      end

      # Modifies a connection notification for VPC endpoint or VPC endpoint service. You can change the SNS
      # topic for the notification, or the events for which to be notified.

      def modify_vpc_endpoint_connection_notification(
        connection_notification_id : String,
        connection_events : Array(String)? = nil,
        connection_notification_arn : String? = nil,
        dry_run : Bool? = nil
      ) : Types::ModifyVpcEndpointConnectionNotificationResult

        input = Types::ModifyVpcEndpointConnectionNotificationRequest.new(connection_notification_id: connection_notification_id, connection_events: connection_events, connection_notification_arn: connection_notification_arn, dry_run: dry_run)
        modify_vpc_endpoint_connection_notification(input)
      end

      def modify_vpc_endpoint_connection_notification(input : Types::ModifyVpcEndpointConnectionNotificationRequest) : Types::ModifyVpcEndpointConnectionNotificationResult
        request = Protocol::Ec2.build_request(Model::MODIFY_VPC_ENDPOINT_CONNECTION_NOTIFICATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyVpcEndpointConnectionNotificationResult, "ModifyVpcEndpointConnectionNotification")
      end

      # Modifies the attributes of the specified VPC endpoint service configuration. If you set or modify
      # the private DNS name, you must prove that you own the private DNS domain name.

      def modify_vpc_endpoint_service_configuration(
        service_id : String,
        acceptance_required : Bool? = nil,
        add_gateway_load_balancer_arns : Array(String)? = nil,
        add_network_load_balancer_arns : Array(String)? = nil,
        add_supported_ip_address_types : Array(String)? = nil,
        add_supported_regions : Array(String)? = nil,
        dry_run : Bool? = nil,
        private_dns_name : String? = nil,
        remove_gateway_load_balancer_arns : Array(String)? = nil,
        remove_network_load_balancer_arns : Array(String)? = nil,
        remove_private_dns_name : Bool? = nil,
        remove_supported_ip_address_types : Array(String)? = nil,
        remove_supported_regions : Array(String)? = nil
      ) : Types::ModifyVpcEndpointServiceConfigurationResult

        input = Types::ModifyVpcEndpointServiceConfigurationRequest.new(service_id: service_id, acceptance_required: acceptance_required, add_gateway_load_balancer_arns: add_gateway_load_balancer_arns, add_network_load_balancer_arns: add_network_load_balancer_arns, add_supported_ip_address_types: add_supported_ip_address_types, add_supported_regions: add_supported_regions, dry_run: dry_run, private_dns_name: private_dns_name, remove_gateway_load_balancer_arns: remove_gateway_load_balancer_arns, remove_network_load_balancer_arns: remove_network_load_balancer_arns, remove_private_dns_name: remove_private_dns_name, remove_supported_ip_address_types: remove_supported_ip_address_types, remove_supported_regions: remove_supported_regions)
        modify_vpc_endpoint_service_configuration(input)
      end

      def modify_vpc_endpoint_service_configuration(input : Types::ModifyVpcEndpointServiceConfigurationRequest) : Types::ModifyVpcEndpointServiceConfigurationResult
        request = Protocol::Ec2.build_request(Model::MODIFY_VPC_ENDPOINT_SERVICE_CONFIGURATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyVpcEndpointServiceConfigurationResult, "ModifyVpcEndpointServiceConfiguration")
      end

      # Modifies the payer responsibility for your VPC endpoint service.

      def modify_vpc_endpoint_service_payer_responsibility(
        payer_responsibility : String,
        service_id : String,
        dry_run : Bool? = nil
      ) : Types::ModifyVpcEndpointServicePayerResponsibilityResult

        input = Types::ModifyVpcEndpointServicePayerResponsibilityRequest.new(payer_responsibility: payer_responsibility, service_id: service_id, dry_run: dry_run)
        modify_vpc_endpoint_service_payer_responsibility(input)
      end

      def modify_vpc_endpoint_service_payer_responsibility(input : Types::ModifyVpcEndpointServicePayerResponsibilityRequest) : Types::ModifyVpcEndpointServicePayerResponsibilityResult
        request = Protocol::Ec2.build_request(Model::MODIFY_VPC_ENDPOINT_SERVICE_PAYER_RESPONSIBILITY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyVpcEndpointServicePayerResponsibilityResult, "ModifyVpcEndpointServicePayerResponsibility")
      end

      # Modifies the permissions for your VPC endpoint service. You can add or remove permissions for
      # service consumers (Amazon Web Services accounts, users, and IAM roles) to connect to your endpoint
      # service. Principal ARNs with path components aren't supported. If you grant permissions to all
      # principals, the service is public. Any users who know the name of a public service can send a
      # request to attach an endpoint. If the service does not require manual approval, attachments are
      # automatically approved.

      def modify_vpc_endpoint_service_permissions(
        service_id : String,
        add_allowed_principals : Array(String)? = nil,
        dry_run : Bool? = nil,
        remove_allowed_principals : Array(String)? = nil
      ) : Types::ModifyVpcEndpointServicePermissionsResult

        input = Types::ModifyVpcEndpointServicePermissionsRequest.new(service_id: service_id, add_allowed_principals: add_allowed_principals, dry_run: dry_run, remove_allowed_principals: remove_allowed_principals)
        modify_vpc_endpoint_service_permissions(input)
      end

      def modify_vpc_endpoint_service_permissions(input : Types::ModifyVpcEndpointServicePermissionsRequest) : Types::ModifyVpcEndpointServicePermissionsResult
        request = Protocol::Ec2.build_request(Model::MODIFY_VPC_ENDPOINT_SERVICE_PERMISSIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyVpcEndpointServicePermissionsResult, "ModifyVpcEndpointServicePermissions")
      end

      # Modifies the VPC peering connection options on one side of a VPC peering connection. If the peered
      # VPCs are in the same Amazon Web Services account, you can enable DNS resolution for queries from the
      # local VPC. This ensures that queries from the local VPC resolve to private IP addresses in the peer
      # VPC. This option is not available if the peered VPCs are in different Amazon Web Services accounts
      # or different Regions. For peered VPCs in different Amazon Web Services accounts, each Amazon Web
      # Services account owner must initiate a separate request to modify the peering connection options.
      # For inter-region peering connections, you must use the Region for the requester VPC to modify the
      # requester VPC peering options and the Region for the accepter VPC to modify the accepter VPC peering
      # options. To verify which VPCs are the accepter and the requester for a VPC peering connection, use
      # the DescribeVpcPeeringConnections command.

      def modify_vpc_peering_connection_options(
        vpc_peering_connection_id : String,
        accepter_peering_connection_options : Types::PeeringConnectionOptionsRequest? = nil,
        dry_run : Bool? = nil,
        requester_peering_connection_options : Types::PeeringConnectionOptionsRequest? = nil
      ) : Types::ModifyVpcPeeringConnectionOptionsResult

        input = Types::ModifyVpcPeeringConnectionOptionsRequest.new(vpc_peering_connection_id: vpc_peering_connection_id, accepter_peering_connection_options: accepter_peering_connection_options, dry_run: dry_run, requester_peering_connection_options: requester_peering_connection_options)
        modify_vpc_peering_connection_options(input)
      end

      def modify_vpc_peering_connection_options(input : Types::ModifyVpcPeeringConnectionOptionsRequest) : Types::ModifyVpcPeeringConnectionOptionsResult
        request = Protocol::Ec2.build_request(Model::MODIFY_VPC_PEERING_CONNECTION_OPTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyVpcPeeringConnectionOptionsResult, "ModifyVpcPeeringConnectionOptions")
      end

      # Modifies the instance tenancy attribute of the specified VPC. You can change the instance tenancy
      # attribute of a VPC to default only. You cannot change the instance tenancy attribute to dedicated .
      # After you modify the tenancy of the VPC, any new instances that you launch into the VPC have a
      # tenancy of default , unless you specify otherwise during launch. The tenancy of any existing
      # instances in the VPC is not affected. For more information, see Dedicated Instances in the Amazon
      # EC2 User Guide .

      def modify_vpc_tenancy(
        instance_tenancy : String,
        vpc_id : String,
        dry_run : Bool? = nil
      ) : Types::ModifyVpcTenancyResult

        input = Types::ModifyVpcTenancyRequest.new(instance_tenancy: instance_tenancy, vpc_id: vpc_id, dry_run: dry_run)
        modify_vpc_tenancy(input)
      end

      def modify_vpc_tenancy(input : Types::ModifyVpcTenancyRequest) : Types::ModifyVpcTenancyResult
        request = Protocol::Ec2.build_request(Model::MODIFY_VPC_TENANCY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyVpcTenancyResult, "ModifyVpcTenancy")
      end

      # Modifies the customer gateway or the target gateway of an Amazon Web Services Site-to-Site VPN
      # connection. To modify the target gateway, the following migration options are available: An existing
      # virtual private gateway to a new virtual private gateway An existing virtual private gateway to a
      # transit gateway An existing transit gateway to a new transit gateway An existing transit gateway to
      # a virtual private gateway Before you perform the migration to the new gateway, you must configure
      # the new gateway. Use CreateVpnGateway to create a virtual private gateway, or CreateTransitGateway
      # to create a transit gateway. This step is required when you migrate from a virtual private gateway
      # with static routes to a transit gateway. You must delete the static routes before you migrate to the
      # new gateway. Keep a copy of the static route before you delete it. You will need to add back these
      # routes to the transit gateway after the VPN connection migration is complete. After you migrate to
      # the new gateway, you might need to modify your VPC route table. Use CreateRoute and DeleteRoute to
      # make the changes described in Update VPC route tables in the Amazon Web Services Site-to-Site VPN
      # User Guide . When the new gateway is a transit gateway, modify the transit gateway route table to
      # allow traffic between the VPC and the Amazon Web Services Site-to-Site VPN connection. Use
      # CreateTransitGatewayRoute to add the routes. If you deleted VPN static routes, you must add the
      # static routes to the transit gateway route table. After you perform this operation, the VPN
      # endpoint's IP addresses on the Amazon Web Services side and the tunnel options remain intact. Your
      # Amazon Web Services Site-to-Site VPN connection will be temporarily unavailable for a brief period
      # while we provision the new endpoints.

      def modify_vpn_connection(
        vpn_connection_id : String,
        customer_gateway_id : String? = nil,
        dry_run : Bool? = nil,
        transit_gateway_id : String? = nil,
        vpn_gateway_id : String? = nil
      ) : Types::ModifyVpnConnectionResult

        input = Types::ModifyVpnConnectionRequest.new(vpn_connection_id: vpn_connection_id, customer_gateway_id: customer_gateway_id, dry_run: dry_run, transit_gateway_id: transit_gateway_id, vpn_gateway_id: vpn_gateway_id)
        modify_vpn_connection(input)
      end

      def modify_vpn_connection(input : Types::ModifyVpnConnectionRequest) : Types::ModifyVpnConnectionResult
        request = Protocol::Ec2.build_request(Model::MODIFY_VPN_CONNECTION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyVpnConnectionResult, "ModifyVpnConnection")
      end

      # Modifies the connection options for your Site-to-Site VPN connection. When you modify the VPN
      # connection options, the VPN endpoint IP addresses on the Amazon Web Services side do not change, and
      # the tunnel options do not change. Your VPN connection will be temporarily unavailable for a brief
      # period while the VPN connection is updated.

      def modify_vpn_connection_options(
        vpn_connection_id : String,
        dry_run : Bool? = nil,
        local_ipv4_network_cidr : String? = nil,
        local_ipv6_network_cidr : String? = nil,
        remote_ipv4_network_cidr : String? = nil,
        remote_ipv6_network_cidr : String? = nil
      ) : Types::ModifyVpnConnectionOptionsResult

        input = Types::ModifyVpnConnectionOptionsRequest.new(vpn_connection_id: vpn_connection_id, dry_run: dry_run, local_ipv4_network_cidr: local_ipv4_network_cidr, local_ipv6_network_cidr: local_ipv6_network_cidr, remote_ipv4_network_cidr: remote_ipv4_network_cidr, remote_ipv6_network_cidr: remote_ipv6_network_cidr)
        modify_vpn_connection_options(input)
      end

      def modify_vpn_connection_options(input : Types::ModifyVpnConnectionOptionsRequest) : Types::ModifyVpnConnectionOptionsResult
        request = Protocol::Ec2.build_request(Model::MODIFY_VPN_CONNECTION_OPTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyVpnConnectionOptionsResult, "ModifyVpnConnectionOptions")
      end

      # Modifies the VPN tunnel endpoint certificate.

      def modify_vpn_tunnel_certificate(
        vpn_connection_id : String,
        vpn_tunnel_outside_ip_address : String,
        dry_run : Bool? = nil
      ) : Types::ModifyVpnTunnelCertificateResult

        input = Types::ModifyVpnTunnelCertificateRequest.new(vpn_connection_id: vpn_connection_id, vpn_tunnel_outside_ip_address: vpn_tunnel_outside_ip_address, dry_run: dry_run)
        modify_vpn_tunnel_certificate(input)
      end

      def modify_vpn_tunnel_certificate(input : Types::ModifyVpnTunnelCertificateRequest) : Types::ModifyVpnTunnelCertificateResult
        request = Protocol::Ec2.build_request(Model::MODIFY_VPN_TUNNEL_CERTIFICATE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyVpnTunnelCertificateResult, "ModifyVpnTunnelCertificate")
      end

      # Modifies the options for a VPN tunnel in an Amazon Web Services Site-to-Site VPN connection. You can
      # modify multiple options for a tunnel in a single request, but you can only modify one tunnel at a
      # time. For more information, see Site-to-Site VPN tunnel options for your Site-to-Site VPN connection
      # in the Amazon Web Services Site-to-Site VPN User Guide .

      def modify_vpn_tunnel_options(
        tunnel_options : Types::ModifyVpnTunnelOptionsSpecification,
        vpn_connection_id : String,
        vpn_tunnel_outside_ip_address : String,
        dry_run : Bool? = nil,
        pre_shared_key_storage : String? = nil,
        skip_tunnel_replacement : Bool? = nil
      ) : Types::ModifyVpnTunnelOptionsResult

        input = Types::ModifyVpnTunnelOptionsRequest.new(tunnel_options: tunnel_options, vpn_connection_id: vpn_connection_id, vpn_tunnel_outside_ip_address: vpn_tunnel_outside_ip_address, dry_run: dry_run, pre_shared_key_storage: pre_shared_key_storage, skip_tunnel_replacement: skip_tunnel_replacement)
        modify_vpn_tunnel_options(input)
      end

      def modify_vpn_tunnel_options(input : Types::ModifyVpnTunnelOptionsRequest) : Types::ModifyVpnTunnelOptionsResult
        request = Protocol::Ec2.build_request(Model::MODIFY_VPN_TUNNEL_OPTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ModifyVpnTunnelOptionsResult, "ModifyVpnTunnelOptions")
      end

      # Enables detailed monitoring for a running instance. Otherwise, basic monitoring is enabled. For more
      # information, see Monitor your instances using CloudWatch in the Amazon EC2 User Guide . To disable
      # detailed monitoring, see UnmonitorInstances .

      def monitor_instances(
        instance_ids : Array(String),
        dry_run : Bool? = nil
      ) : Types::MonitorInstancesResult

        input = Types::MonitorInstancesRequest.new(instance_ids: instance_ids, dry_run: dry_run)
        monitor_instances(input)
      end

      def monitor_instances(input : Types::MonitorInstancesRequest) : Types::MonitorInstancesResult
        request = Protocol::Ec2.build_request(Model::MONITOR_INSTANCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::MonitorInstancesResult, "MonitorInstances")
      end

      # This action is deprecated. Moves an Elastic IP address from the EC2-Classic platform to the EC2-VPC
      # platform. The Elastic IP address must be allocated to your account for more than 24 hours, and it
      # must not be associated with an instance. After the Elastic IP address is moved, it is no longer
      # available for use in the EC2-Classic platform. You cannot move an Elastic IP address that was
      # originally allocated for use in the EC2-VPC platform to the EC2-Classic platform.

      def move_address_to_vpc(
        public_ip : String,
        dry_run : Bool? = nil
      ) : Types::MoveAddressToVpcResult

        input = Types::MoveAddressToVpcRequest.new(public_ip: public_ip, dry_run: dry_run)
        move_address_to_vpc(input)
      end

      def move_address_to_vpc(input : Types::MoveAddressToVpcRequest) : Types::MoveAddressToVpcResult
        request = Protocol::Ec2.build_request(Model::MOVE_ADDRESS_TO_VPC, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::MoveAddressToVpcResult, "MoveAddressToVpc")
      end

      # Move a BYOIPv4 CIDR to IPAM from a public IPv4 pool. If you already have a BYOIPv4 CIDR with Amazon
      # Web Services, you can move the CIDR to IPAM from a public IPv4 pool. You cannot move an IPv6 CIDR to
      # IPAM. If you are bringing a new IP address to Amazon Web Services for the first time, complete the
      # steps in Tutorial: BYOIP address CIDRs to IPAM .

      def move_byoip_cidr_to_ipam(
        cidr : String,
        ipam_pool_id : String,
        ipam_pool_owner : String,
        dry_run : Bool? = nil
      ) : Types::MoveByoipCidrToIpamResult

        input = Types::MoveByoipCidrToIpamRequest.new(cidr: cidr, ipam_pool_id: ipam_pool_id, ipam_pool_owner: ipam_pool_owner, dry_run: dry_run)
        move_byoip_cidr_to_ipam(input)
      end

      def move_byoip_cidr_to_ipam(input : Types::MoveByoipCidrToIpamRequest) : Types::MoveByoipCidrToIpamResult
        request = Protocol::Ec2.build_request(Model::MOVE_BYOIP_CIDR_TO_IPAM, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::MoveByoipCidrToIpamResult, "MoveByoipCidrToIpam")
      end

      # Move available capacity from a source Capacity Reservation to a destination Capacity Reservation.
      # The source Capacity Reservation and the destination Capacity Reservation must be active , owned by
      # your Amazon Web Services account, and share the following: Instance type Platform Availability Zone
      # Tenancy Placement group Capacity Reservation end time - At specific time or Manually .

      def move_capacity_reservation_instances(
        destination_capacity_reservation_id : String,
        instance_count : Int32,
        source_capacity_reservation_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil
      ) : Types::MoveCapacityReservationInstancesResult

        input = Types::MoveCapacityReservationInstancesRequest.new(destination_capacity_reservation_id: destination_capacity_reservation_id, instance_count: instance_count, source_capacity_reservation_id: source_capacity_reservation_id, client_token: client_token, dry_run: dry_run)
        move_capacity_reservation_instances(input)
      end

      def move_capacity_reservation_instances(input : Types::MoveCapacityReservationInstancesRequest) : Types::MoveCapacityReservationInstancesResult
        request = Protocol::Ec2.build_request(Model::MOVE_CAPACITY_RESERVATION_INSTANCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::MoveCapacityReservationInstancesResult, "MoveCapacityReservationInstances")
      end

      # Provisions an IPv4 or IPv6 address range for use with your Amazon Web Services resources through
      # bring your own IP addresses (BYOIP) and creates a corresponding address pool. After the address
      # range is provisioned, it is ready to be advertised. Amazon Web Services verifies that you own the
      # address range and are authorized to advertise it. You must ensure that the address range is
      # registered to you and that you created an RPKI ROA to authorize Amazon ASNs 16509 and 14618 to
      # advertise the address range. For more information, see Bring your own IP addresses (BYOIP) in the
      # Amazon EC2 User Guide . Provisioning an address range is an asynchronous operation, so the call
      # returns immediately, but the address range is not ready to use until its status changes from
      # pending-provision to provisioned . For more information, see Onboard your address range .

      def provision_byoip_cidr(
        cidr : String,
        cidr_authorization_context : Types::CidrAuthorizationContext? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        multi_region : Bool? = nil,
        network_border_group : String? = nil,
        pool_tag_specifications : Array(Types::TagSpecification)? = nil,
        publicly_advertisable : Bool? = nil
      ) : Types::ProvisionByoipCidrResult

        input = Types::ProvisionByoipCidrRequest.new(cidr: cidr, cidr_authorization_context: cidr_authorization_context, description: description, dry_run: dry_run, multi_region: multi_region, network_border_group: network_border_group, pool_tag_specifications: pool_tag_specifications, publicly_advertisable: publicly_advertisable)
        provision_byoip_cidr(input)
      end

      def provision_byoip_cidr(input : Types::ProvisionByoipCidrRequest) : Types::ProvisionByoipCidrResult
        request = Protocol::Ec2.build_request(Model::PROVISION_BYOIP_CIDR, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ProvisionByoipCidrResult, "ProvisionByoipCidr")
      end

      # Provisions your Autonomous System Number (ASN) for use in your Amazon Web Services account. This
      # action requires authorization context for Amazon to bring the ASN to an Amazon Web Services account.
      # For more information, see Tutorial: Bring your ASN to IPAM in the Amazon VPC IPAM guide .

      def provision_ipam_byoasn(
        asn : String,
        asn_authorization_context : Types::AsnAuthorizationContext,
        ipam_id : String,
        dry_run : Bool? = nil
      ) : Types::ProvisionIpamByoasnResult

        input = Types::ProvisionIpamByoasnRequest.new(asn: asn, asn_authorization_context: asn_authorization_context, ipam_id: ipam_id, dry_run: dry_run)
        provision_ipam_byoasn(input)
      end

      def provision_ipam_byoasn(input : Types::ProvisionIpamByoasnRequest) : Types::ProvisionIpamByoasnResult
        request = Protocol::Ec2.build_request(Model::PROVISION_IPAM_BYOASN, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ProvisionIpamByoasnResult, "ProvisionIpamByoasn")
      end

      # Provision a CIDR to an IPAM pool. You can use this action to provision new CIDRs to a top-level pool
      # or to transfer a CIDR from a top-level pool to a pool within it. For more information, see Provision
      # CIDRs to pools in the Amazon VPC IPAM User Guide .

      def provision_ipam_pool_cidr(
        ipam_pool_id : String,
        cidr : String? = nil,
        cidr_authorization_context : Types::IpamCidrAuthorizationContext? = nil,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        ipam_external_resource_verification_token_id : String? = nil,
        netmask_length : Int32? = nil,
        verification_method : String? = nil
      ) : Types::ProvisionIpamPoolCidrResult

        input = Types::ProvisionIpamPoolCidrRequest.new(ipam_pool_id: ipam_pool_id, cidr: cidr, cidr_authorization_context: cidr_authorization_context, client_token: client_token, dry_run: dry_run, ipam_external_resource_verification_token_id: ipam_external_resource_verification_token_id, netmask_length: netmask_length, verification_method: verification_method)
        provision_ipam_pool_cidr(input)
      end

      def provision_ipam_pool_cidr(input : Types::ProvisionIpamPoolCidrRequest) : Types::ProvisionIpamPoolCidrResult
        request = Protocol::Ec2.build_request(Model::PROVISION_IPAM_POOL_CIDR, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ProvisionIpamPoolCidrResult, "ProvisionIpamPoolCidr")
      end

      # Provision a CIDR to a public IPv4 pool. For more information about IPAM, see What is IPAM? in the
      # Amazon VPC IPAM User Guide .

      def provision_public_ipv4_pool_cidr(
        ipam_pool_id : String,
        netmask_length : Int32,
        pool_id : String,
        dry_run : Bool? = nil,
        network_border_group : String? = nil
      ) : Types::ProvisionPublicIpv4PoolCidrResult

        input = Types::ProvisionPublicIpv4PoolCidrRequest.new(ipam_pool_id: ipam_pool_id, netmask_length: netmask_length, pool_id: pool_id, dry_run: dry_run, network_border_group: network_border_group)
        provision_public_ipv4_pool_cidr(input)
      end

      def provision_public_ipv4_pool_cidr(input : Types::ProvisionPublicIpv4PoolCidrRequest) : Types::ProvisionPublicIpv4PoolCidrResult
        request = Protocol::Ec2.build_request(Model::PROVISION_PUBLIC_IPV4_POOL_CIDR, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ProvisionPublicIpv4PoolCidrResult, "ProvisionPublicIpv4PoolCidr")
      end

      # Purchase the Capacity Block for use with your account. With Capacity Blocks you ensure GPU capacity
      # is available for machine learning (ML) workloads. You must specify the ID of the Capacity Block
      # offering you are purchasing.

      def purchase_capacity_block(
        capacity_block_offering_id : String,
        instance_platform : String,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::PurchaseCapacityBlockResult

        input = Types::PurchaseCapacityBlockRequest.new(capacity_block_offering_id: capacity_block_offering_id, instance_platform: instance_platform, dry_run: dry_run, tag_specifications: tag_specifications)
        purchase_capacity_block(input)
      end

      def purchase_capacity_block(input : Types::PurchaseCapacityBlockRequest) : Types::PurchaseCapacityBlockResult
        request = Protocol::Ec2.build_request(Model::PURCHASE_CAPACITY_BLOCK, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::PurchaseCapacityBlockResult, "PurchaseCapacityBlock")
      end

      # Purchase the Capacity Block extension for use with your account. You must specify the ID of the
      # Capacity Block extension offering you are purchasing.

      def purchase_capacity_block_extension(
        capacity_block_extension_offering_id : String,
        capacity_reservation_id : String,
        dry_run : Bool? = nil
      ) : Types::PurchaseCapacityBlockExtensionResult

        input = Types::PurchaseCapacityBlockExtensionRequest.new(capacity_block_extension_offering_id: capacity_block_extension_offering_id, capacity_reservation_id: capacity_reservation_id, dry_run: dry_run)
        purchase_capacity_block_extension(input)
      end

      def purchase_capacity_block_extension(input : Types::PurchaseCapacityBlockExtensionRequest) : Types::PurchaseCapacityBlockExtensionResult
        request = Protocol::Ec2.build_request(Model::PURCHASE_CAPACITY_BLOCK_EXTENSION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::PurchaseCapacityBlockExtensionResult, "PurchaseCapacityBlockExtension")
      end

      # Purchase a reservation with configurations that match those of your Dedicated Host. You must have
      # active Dedicated Hosts in your account before you purchase a reservation. This action results in the
      # specified reservation being purchased and charged to your account.

      def purchase_host_reservation(
        host_id_set : Array(String),
        offering_id : String,
        client_token : String? = nil,
        currency_code : String? = nil,
        limit_price : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::PurchaseHostReservationResult

        input = Types::PurchaseHostReservationRequest.new(host_id_set: host_id_set, offering_id: offering_id, client_token: client_token, currency_code: currency_code, limit_price: limit_price, tag_specifications: tag_specifications)
        purchase_host_reservation(input)
      end

      def purchase_host_reservation(input : Types::PurchaseHostReservationRequest) : Types::PurchaseHostReservationResult
        request = Protocol::Ec2.build_request(Model::PURCHASE_HOST_RESERVATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::PurchaseHostReservationResult, "PurchaseHostReservation")
      end

      # Purchases a Reserved Instance for use with your account. With Reserved Instances, you pay a lower
      # hourly rate compared to On-Demand instance pricing. Use DescribeReservedInstancesOfferings to get a
      # list of Reserved Instance offerings that match your specifications. After you've purchased a
      # Reserved Instance, you can check for your new Reserved Instance with DescribeReservedInstances . To
      # queue a purchase for a future date and time, specify a purchase time. If you do not specify a
      # purchase time, the default is the current time. For more information, see Reserved Instances and
      # Sell in the Reserved Instance Marketplace in the Amazon EC2 User Guide .

      def purchase_reserved_instances_offering(
        instance_count : Int32,
        reserved_instances_offering_id : String,
        dry_run : Bool? = nil,
        limit_price : Types::ReservedInstanceLimitPrice? = nil,
        purchase_time : Time? = nil
      ) : Types::PurchaseReservedInstancesOfferingResult

        input = Types::PurchaseReservedInstancesOfferingRequest.new(instance_count: instance_count, reserved_instances_offering_id: reserved_instances_offering_id, dry_run: dry_run, limit_price: limit_price, purchase_time: purchase_time)
        purchase_reserved_instances_offering(input)
      end

      def purchase_reserved_instances_offering(input : Types::PurchaseReservedInstancesOfferingRequest) : Types::PurchaseReservedInstancesOfferingResult
        request = Protocol::Ec2.build_request(Model::PURCHASE_RESERVED_INSTANCES_OFFERING, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::PurchaseReservedInstancesOfferingResult, "PurchaseReservedInstancesOffering")
      end

      # You can no longer purchase Scheduled Instances. Purchases the Scheduled Instances with the specified
      # schedule. Scheduled Instances enable you to purchase Amazon EC2 compute capacity by the hour for a
      # one-year term. Before you can purchase a Scheduled Instance, you must call
      # DescribeScheduledInstanceAvailability to check for available schedules and obtain a purchase token.
      # After you purchase a Scheduled Instance, you must call RunScheduledInstances during each scheduled
      # time period. After you purchase a Scheduled Instance, you can't cancel, modify, or resell your
      # purchase.

      def purchase_scheduled_instances(
        purchase_requests : Array(Types::PurchaseRequest),
        client_token : String? = nil,
        dry_run : Bool? = nil
      ) : Types::PurchaseScheduledInstancesResult

        input = Types::PurchaseScheduledInstancesRequest.new(purchase_requests: purchase_requests, client_token: client_token, dry_run: dry_run)
        purchase_scheduled_instances(input)
      end

      def purchase_scheduled_instances(input : Types::PurchaseScheduledInstancesRequest) : Types::PurchaseScheduledInstancesResult
        request = Protocol::Ec2.build_request(Model::PURCHASE_SCHEDULED_INSTANCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::PurchaseScheduledInstancesResult, "PurchaseScheduledInstances")
      end

      # Requests a reboot of the specified instances. This operation is asynchronous; it only queues a
      # request to reboot the specified instances. The operation succeeds if the instances are valid and
      # belong to you. Requests to reboot terminated instances are ignored. If an instance does not cleanly
      # shut down within a few minutes, Amazon EC2 performs a hard reboot. For more information about
      # troubleshooting, see Troubleshoot an unreachable instance in the Amazon EC2 User Guide .

      def reboot_instances(
        instance_ids : Array(String),
        dry_run : Bool? = nil
      ) : Nil

        input = Types::RebootInstancesRequest.new(instance_ids: instance_ids, dry_run: dry_run)
        reboot_instances(input)
      end

      def reboot_instances(input : Types::RebootInstancesRequest) : Nil
        request = Protocol::Ec2.build_request(Model::REBOOT_INSTANCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Registers an AMI. When you're creating an instance-store backed AMI, registering the AMI is the
      # final step in the creation process. For more information about creating AMIs, see Create an AMI from
      # a snapshot and Create an instance-store backed AMI in the Amazon EC2 User Guide . If needed, you can
      # deregister an AMI at any time. Any modifications you make to an AMI backed by an instance store
      # volume invalidates its registration. If you make changes to an image, deregister the previous image
      # and register the new image. Register a snapshot of a root device volume You can use RegisterImage to
      # create an Amazon EBS-backed Linux AMI from a snapshot of a root device volume. You specify the
      # snapshot using a block device mapping. You can't set the encryption state of the volume using the
      # block device mapping. If the snapshot is encrypted, or encryption by default is enabled, the root
      # volume of an instance launched from the AMI is encrypted. For more information, see Create an AMI
      # from a snapshot and Use encryption with EBS-backed AMIs in the Amazon EC2 User Guide . Amazon Web
      # Services Marketplace product codes If any snapshots have Amazon Web Services Marketplace product
      # codes, they are copied to the new AMI. In most cases, AMIs for Windows, RedHat, SUSE, and SQL Server
      # require correct licensing information to be present on the AMI. For more information, see Understand
      # AMI billing information in the Amazon EC2 User Guide . When creating an AMI from a snapshot, the
      # RegisterImage operation derives the correct billing information from the snapshot's metadata, but
      # this requires the appropriate metadata to be present. To verify if the correct billing information
      # was applied, check the PlatformDetails field on the new AMI. If the field is empty or doesn't match
      # the expected operating system code (for example, Windows, RedHat, SUSE, or SQL), the AMI creation
      # was unsuccessful, and you should discard the AMI and instead create the AMI from an instance. For
      # more information, see Create an AMI from an instance in the Amazon EC2 User Guide . If you purchase
      # a Reserved Instance to apply to an On-Demand Instance that was launched from an AMI with a billing
      # product code, make sure that the Reserved Instance has the matching billing product code. If you
      # purchase a Reserved Instance without the matching billing product code, the Reserved Instance is not
      # applied to the On-Demand Instance. For information about how to obtain the platform details and
      # billing information of an AMI, see Understand AMI billing information in the Amazon EC2 User Guide .

      def register_image(
        name : String,
        architecture : String? = nil,
        billing_products : Array(String)? = nil,
        block_device_mappings : Array(Types::BlockDeviceMapping)? = nil,
        boot_mode : String? = nil,
        description : String? = nil,
        dry_run : Bool? = nil,
        ena_support : Bool? = nil,
        image_location : String? = nil,
        imds_support : String? = nil,
        kernel_id : String? = nil,
        ramdisk_id : String? = nil,
        root_device_name : String? = nil,
        sriov_net_support : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        tpm_support : String? = nil,
        uefi_data : String? = nil,
        virtualization_type : String? = nil
      ) : Types::RegisterImageResult

        input = Types::RegisterImageRequest.new(name: name, architecture: architecture, billing_products: billing_products, block_device_mappings: block_device_mappings, boot_mode: boot_mode, description: description, dry_run: dry_run, ena_support: ena_support, image_location: image_location, imds_support: imds_support, kernel_id: kernel_id, ramdisk_id: ramdisk_id, root_device_name: root_device_name, sriov_net_support: sriov_net_support, tag_specifications: tag_specifications, tpm_support: tpm_support, uefi_data: uefi_data, virtualization_type: virtualization_type)
        register_image(input)
      end

      def register_image(input : Types::RegisterImageRequest) : Types::RegisterImageResult
        request = Protocol::Ec2.build_request(Model::REGISTER_IMAGE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::RegisterImageResult, "RegisterImage")
      end

      # Registers a set of tag keys to include in scheduled event notifications for your resources. To
      # remove tags, use DeregisterInstanceEventNotificationAttributes .

      def register_instance_event_notification_attributes(
        instance_tag_attribute : Types::RegisterInstanceTagAttributeRequest,
        dry_run : Bool? = nil
      ) : Types::RegisterInstanceEventNotificationAttributesResult

        input = Types::RegisterInstanceEventNotificationAttributesRequest.new(instance_tag_attribute: instance_tag_attribute, dry_run: dry_run)
        register_instance_event_notification_attributes(input)
      end

      def register_instance_event_notification_attributes(input : Types::RegisterInstanceEventNotificationAttributesRequest) : Types::RegisterInstanceEventNotificationAttributesResult
        request = Protocol::Ec2.build_request(Model::REGISTER_INSTANCE_EVENT_NOTIFICATION_ATTRIBUTES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::RegisterInstanceEventNotificationAttributesResult, "RegisterInstanceEventNotificationAttributes")
      end

      # Registers members (network interfaces) with the transit gateway multicast group. A member is a
      # network interface associated with a supported EC2 instance that receives multicast traffic. For more
      # information, see Multicast on transit gateways in the Amazon Web Services Transit Gateways Guide .
      # After you add the members, use SearchTransitGatewayMulticastGroups to verify that the members were
      # added to the transit gateway multicast group.

      def register_transit_gateway_multicast_group_members(
        network_interface_ids : Array(String),
        transit_gateway_multicast_domain_id : String,
        dry_run : Bool? = nil,
        group_ip_address : String? = nil
      ) : Types::RegisterTransitGatewayMulticastGroupMembersResult

        input = Types::RegisterTransitGatewayMulticastGroupMembersRequest.new(network_interface_ids: network_interface_ids, transit_gateway_multicast_domain_id: transit_gateway_multicast_domain_id, dry_run: dry_run, group_ip_address: group_ip_address)
        register_transit_gateway_multicast_group_members(input)
      end

      def register_transit_gateway_multicast_group_members(input : Types::RegisterTransitGatewayMulticastGroupMembersRequest) : Types::RegisterTransitGatewayMulticastGroupMembersResult
        request = Protocol::Ec2.build_request(Model::REGISTER_TRANSIT_GATEWAY_MULTICAST_GROUP_MEMBERS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::RegisterTransitGatewayMulticastGroupMembersResult, "RegisterTransitGatewayMulticastGroupMembers")
      end

      # Registers sources (network interfaces) with the specified transit gateway multicast group. A
      # multicast source is a network interface attached to a supported instance that sends multicast
      # traffic. For more information about supported instances, see Multicast on transit gateways in the
      # Amazon Web Services Transit Gateways Guide . After you add the source, use
      # SearchTransitGatewayMulticastGroups to verify that the source was added to the multicast group.

      def register_transit_gateway_multicast_group_sources(
        network_interface_ids : Array(String),
        transit_gateway_multicast_domain_id : String,
        dry_run : Bool? = nil,
        group_ip_address : String? = nil
      ) : Types::RegisterTransitGatewayMulticastGroupSourcesResult

        input = Types::RegisterTransitGatewayMulticastGroupSourcesRequest.new(network_interface_ids: network_interface_ids, transit_gateway_multicast_domain_id: transit_gateway_multicast_domain_id, dry_run: dry_run, group_ip_address: group_ip_address)
        register_transit_gateway_multicast_group_sources(input)
      end

      def register_transit_gateway_multicast_group_sources(input : Types::RegisterTransitGatewayMulticastGroupSourcesRequest) : Types::RegisterTransitGatewayMulticastGroupSourcesResult
        request = Protocol::Ec2.build_request(Model::REGISTER_TRANSIT_GATEWAY_MULTICAST_GROUP_SOURCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::RegisterTransitGatewayMulticastGroupSourcesResult, "RegisterTransitGatewayMulticastGroupSources")
      end

      # Rejects a request to assign billing of the available capacity of a shared Capacity Reservation to
      # your account. For more information, see Billing assignment for shared Amazon EC2 Capacity
      # Reservations .

      def reject_capacity_reservation_billing_ownership(
        capacity_reservation_id : String,
        dry_run : Bool? = nil
      ) : Types::RejectCapacityReservationBillingOwnershipResult

        input = Types::RejectCapacityReservationBillingOwnershipRequest.new(capacity_reservation_id: capacity_reservation_id, dry_run: dry_run)
        reject_capacity_reservation_billing_ownership(input)
      end

      def reject_capacity_reservation_billing_ownership(input : Types::RejectCapacityReservationBillingOwnershipRequest) : Types::RejectCapacityReservationBillingOwnershipResult
        request = Protocol::Ec2.build_request(Model::REJECT_CAPACITY_RESERVATION_BILLING_OWNERSHIP, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::RejectCapacityReservationBillingOwnershipResult, "RejectCapacityReservationBillingOwnership")
      end

      # Rejects a request to associate cross-account subnets with a transit gateway multicast domain.

      def reject_transit_gateway_multicast_domain_associations(
        dry_run : Bool? = nil,
        subnet_ids : Array(String)? = nil,
        transit_gateway_attachment_id : String? = nil,
        transit_gateway_multicast_domain_id : String? = nil
      ) : Types::RejectTransitGatewayMulticastDomainAssociationsResult

        input = Types::RejectTransitGatewayMulticastDomainAssociationsRequest.new(dry_run: dry_run, subnet_ids: subnet_ids, transit_gateway_attachment_id: transit_gateway_attachment_id, transit_gateway_multicast_domain_id: transit_gateway_multicast_domain_id)
        reject_transit_gateway_multicast_domain_associations(input)
      end

      def reject_transit_gateway_multicast_domain_associations(input : Types::RejectTransitGatewayMulticastDomainAssociationsRequest) : Types::RejectTransitGatewayMulticastDomainAssociationsResult
        request = Protocol::Ec2.build_request(Model::REJECT_TRANSIT_GATEWAY_MULTICAST_DOMAIN_ASSOCIATIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::RejectTransitGatewayMulticastDomainAssociationsResult, "RejectTransitGatewayMulticastDomainAssociations")
      end

      # Rejects a transit gateway peering attachment request.

      def reject_transit_gateway_peering_attachment(
        transit_gateway_attachment_id : String,
        dry_run : Bool? = nil
      ) : Types::RejectTransitGatewayPeeringAttachmentResult

        input = Types::RejectTransitGatewayPeeringAttachmentRequest.new(transit_gateway_attachment_id: transit_gateway_attachment_id, dry_run: dry_run)
        reject_transit_gateway_peering_attachment(input)
      end

      def reject_transit_gateway_peering_attachment(input : Types::RejectTransitGatewayPeeringAttachmentRequest) : Types::RejectTransitGatewayPeeringAttachmentResult
        request = Protocol::Ec2.build_request(Model::REJECT_TRANSIT_GATEWAY_PEERING_ATTACHMENT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::RejectTransitGatewayPeeringAttachmentResult, "RejectTransitGatewayPeeringAttachment")
      end

      # Rejects a request to attach a VPC to a transit gateway. The VPC attachment must be in the
      # pendingAcceptance state. Use DescribeTransitGatewayVpcAttachments to view your pending VPC
      # attachment requests. Use AcceptTransitGatewayVpcAttachment to accept a VPC attachment request.

      def reject_transit_gateway_vpc_attachment(
        transit_gateway_attachment_id : String,
        dry_run : Bool? = nil
      ) : Types::RejectTransitGatewayVpcAttachmentResult

        input = Types::RejectTransitGatewayVpcAttachmentRequest.new(transit_gateway_attachment_id: transit_gateway_attachment_id, dry_run: dry_run)
        reject_transit_gateway_vpc_attachment(input)
      end

      def reject_transit_gateway_vpc_attachment(input : Types::RejectTransitGatewayVpcAttachmentRequest) : Types::RejectTransitGatewayVpcAttachmentResult
        request = Protocol::Ec2.build_request(Model::REJECT_TRANSIT_GATEWAY_VPC_ATTACHMENT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::RejectTransitGatewayVpcAttachmentResult, "RejectTransitGatewayVpcAttachment")
      end

      # Rejects VPC endpoint connection requests to your VPC endpoint service.

      def reject_vpc_endpoint_connections(
        service_id : String,
        vpc_endpoint_ids : Array(String),
        dry_run : Bool? = nil
      ) : Types::RejectVpcEndpointConnectionsResult

        input = Types::RejectVpcEndpointConnectionsRequest.new(service_id: service_id, vpc_endpoint_ids: vpc_endpoint_ids, dry_run: dry_run)
        reject_vpc_endpoint_connections(input)
      end

      def reject_vpc_endpoint_connections(input : Types::RejectVpcEndpointConnectionsRequest) : Types::RejectVpcEndpointConnectionsResult
        request = Protocol::Ec2.build_request(Model::REJECT_VPC_ENDPOINT_CONNECTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::RejectVpcEndpointConnectionsResult, "RejectVpcEndpointConnections")
      end

      # Rejects a VPC peering connection request. The VPC peering connection must be in the
      # pending-acceptance state. Use the DescribeVpcPeeringConnections request to view your outstanding VPC
      # peering connection requests. To delete an active VPC peering connection, or to delete a VPC peering
      # connection request that you initiated, use DeleteVpcPeeringConnection .

      def reject_vpc_peering_connection(
        vpc_peering_connection_id : String,
        dry_run : Bool? = nil
      ) : Types::RejectVpcPeeringConnectionResult

        input = Types::RejectVpcPeeringConnectionRequest.new(vpc_peering_connection_id: vpc_peering_connection_id, dry_run: dry_run)
        reject_vpc_peering_connection(input)
      end

      def reject_vpc_peering_connection(input : Types::RejectVpcPeeringConnectionRequest) : Types::RejectVpcPeeringConnectionResult
        request = Protocol::Ec2.build_request(Model::REJECT_VPC_PEERING_CONNECTION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::RejectVpcPeeringConnectionResult, "RejectVpcPeeringConnection")
      end

      # Releases the specified Elastic IP address. [Default VPC] Releasing an Elastic IP address
      # automatically disassociates it from any instance that it's associated with. Alternatively, you can
      # disassociate an Elastic IP address without releasing it. [Nondefault VPC] You must disassociate the
      # Elastic IP address before you can release it. Otherwise, Amazon EC2 returns an error (
      # InvalidIPAddress.InUse ). After releasing an Elastic IP address, it is released to the IP address
      # pool. Be sure to update your DNS records and any servers or devices that communicate with the
      # address. If you attempt to release an Elastic IP address that you already released, you'll get an
      # AuthFailure error if the address is already allocated to another Amazon Web Services account. After
      # you release an Elastic IP address, you might be able to recover it. For more information, see
      # Release an Elastic IP address .

      def release_address(
        allocation_id : String? = nil,
        dry_run : Bool? = nil,
        network_border_group : String? = nil,
        public_ip : String? = nil
      ) : Nil

        input = Types::ReleaseAddressRequest.new(allocation_id: allocation_id, dry_run: dry_run, network_border_group: network_border_group, public_ip: public_ip)
        release_address(input)
      end

      def release_address(input : Types::ReleaseAddressRequest) : Nil
        request = Protocol::Ec2.build_request(Model::RELEASE_ADDRESS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # When you no longer want to use an On-Demand Dedicated Host it can be released. On-Demand billing is
      # stopped and the host goes into released state. The host ID of Dedicated Hosts that have been
      # released can no longer be specified in another request, for example, to modify the host. You must
      # stop or terminate all instances on a host before it can be released. When Dedicated Hosts are
      # released, it may take some time for them to stop counting toward your limit and you may receive
      # capacity errors when trying to allocate new Dedicated Hosts. Wait a few minutes and then try again.
      # Released hosts still appear in a DescribeHosts response.

      def release_hosts(
        host_ids : Array(String)
      ) : Types::ReleaseHostsResult

        input = Types::ReleaseHostsRequest.new(host_ids: host_ids)
        release_hosts(input)
      end

      def release_hosts(input : Types::ReleaseHostsRequest) : Types::ReleaseHostsResult
        request = Protocol::Ec2.build_request(Model::RELEASE_HOSTS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ReleaseHostsResult, "ReleaseHosts")
      end

      # Release an allocation within an IPAM pool. The Region you use should be the IPAM pool locale. The
      # locale is the Amazon Web Services Region where this IPAM pool is available for allocations. You can
      # only use this action to release manual allocations. To remove an allocation for a resource without
      # deleting the resource, set its monitored state to false using ModifyIpamResourceCidr . For more
      # information, see Release an allocation in the Amazon VPC IPAM User Guide . All EC2 API actions
      # follow an eventual consistency model.

      def release_ipam_pool_allocation(
        cidr : String,
        ipam_pool_allocation_id : String,
        ipam_pool_id : String,
        dry_run : Bool? = nil
      ) : Types::ReleaseIpamPoolAllocationResult

        input = Types::ReleaseIpamPoolAllocationRequest.new(cidr: cidr, ipam_pool_allocation_id: ipam_pool_allocation_id, ipam_pool_id: ipam_pool_id, dry_run: dry_run)
        release_ipam_pool_allocation(input)
      end

      def release_ipam_pool_allocation(input : Types::ReleaseIpamPoolAllocationRequest) : Types::ReleaseIpamPoolAllocationResult
        request = Protocol::Ec2.build_request(Model::RELEASE_IPAM_POOL_ALLOCATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ReleaseIpamPoolAllocationResult, "ReleaseIpamPoolAllocation")
      end

      # Replaces an IAM instance profile for the specified running instance. You can use this action to
      # change the IAM instance profile that's associated with an instance without having to disassociate
      # the existing IAM instance profile first. Use DescribeIamInstanceProfileAssociations to get the
      # association ID.

      def replace_iam_instance_profile_association(
        association_id : String,
        iam_instance_profile : Types::IamInstanceProfileSpecification
      ) : Types::ReplaceIamInstanceProfileAssociationResult

        input = Types::ReplaceIamInstanceProfileAssociationRequest.new(association_id: association_id, iam_instance_profile: iam_instance_profile)
        replace_iam_instance_profile_association(input)
      end

      def replace_iam_instance_profile_association(input : Types::ReplaceIamInstanceProfileAssociationRequest) : Types::ReplaceIamInstanceProfileAssociationResult
        request = Protocol::Ec2.build_request(Model::REPLACE_IAM_INSTANCE_PROFILE_ASSOCIATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ReplaceIamInstanceProfileAssociationResult, "ReplaceIamInstanceProfileAssociation")
      end

      # Sets or replaces the criteria for Allowed AMIs. The Allowed AMIs feature does not restrict the AMIs
      # owned by your account. Regardless of the criteria you set, the AMIs created by your account will
      # always be discoverable and usable by users in your account. For more information, see Control the
      # discovery and use of AMIs in Amazon EC2 with Allowed AMIs in Amazon EC2 User Guide .

      def replace_image_criteria_in_allowed_images_settings(
        dry_run : Bool? = nil,
        image_criteria : Array(Types::ImageCriterionRequest)? = nil
      ) : Types::ReplaceImageCriteriaInAllowedImagesSettingsResult

        input = Types::ReplaceImageCriteriaInAllowedImagesSettingsRequest.new(dry_run: dry_run, image_criteria: image_criteria)
        replace_image_criteria_in_allowed_images_settings(input)
      end

      def replace_image_criteria_in_allowed_images_settings(input : Types::ReplaceImageCriteriaInAllowedImagesSettingsRequest) : Types::ReplaceImageCriteriaInAllowedImagesSettingsResult
        request = Protocol::Ec2.build_request(Model::REPLACE_IMAGE_CRITERIA_IN_ALLOWED_IMAGES_SETTINGS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ReplaceImageCriteriaInAllowedImagesSettingsResult, "ReplaceImageCriteriaInAllowedImagesSettings")
      end

      # Changes which network ACL a subnet is associated with. By default when you create a subnet, it's
      # automatically associated with the default network ACL. For more information, see Network ACLs in the
      # Amazon VPC User Guide . This is an idempotent operation.

      def replace_network_acl_association(
        association_id : String,
        network_acl_id : String,
        dry_run : Bool? = nil
      ) : Types::ReplaceNetworkAclAssociationResult

        input = Types::ReplaceNetworkAclAssociationRequest.new(association_id: association_id, network_acl_id: network_acl_id, dry_run: dry_run)
        replace_network_acl_association(input)
      end

      def replace_network_acl_association(input : Types::ReplaceNetworkAclAssociationRequest) : Types::ReplaceNetworkAclAssociationResult
        request = Protocol::Ec2.build_request(Model::REPLACE_NETWORK_ACL_ASSOCIATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ReplaceNetworkAclAssociationResult, "ReplaceNetworkAclAssociation")
      end

      # Replaces an entry (rule) in a network ACL. For more information, see Network ACLs in the Amazon VPC
      # User Guide .

      def replace_network_acl_entry(
        egress : Bool,
        network_acl_id : String,
        protocol : String,
        rule_action : String,
        rule_number : Int32,
        cidr_block : String? = nil,
        dry_run : Bool? = nil,
        icmp_type_code : Types::IcmpTypeCode? = nil,
        ipv6_cidr_block : String? = nil,
        port_range : Types::PortRange? = nil
      ) : Nil

        input = Types::ReplaceNetworkAclEntryRequest.new(egress: egress, network_acl_id: network_acl_id, protocol: protocol, rule_action: rule_action, rule_number: rule_number, cidr_block: cidr_block, dry_run: dry_run, icmp_type_code: icmp_type_code, ipv6_cidr_block: ipv6_cidr_block, port_range: port_range)
        replace_network_acl_entry(input)
      end

      def replace_network_acl_entry(input : Types::ReplaceNetworkAclEntryRequest) : Nil
        request = Protocol::Ec2.build_request(Model::REPLACE_NETWORK_ACL_ENTRY, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Replaces an existing route within a route table in a VPC. You must specify either a destination CIDR
      # block or a prefix list ID. You must also specify exactly one of the resources from the parameter
      # list, or reset the local route to its default target. For more information, see Route tables in the
      # Amazon VPC User Guide .

      def replace_route(
        route_table_id : String,
        carrier_gateway_id : String? = nil,
        core_network_arn : String? = nil,
        destination_cidr_block : String? = nil,
        destination_ipv6_cidr_block : String? = nil,
        destination_prefix_list_id : String? = nil,
        dry_run : Bool? = nil,
        egress_only_internet_gateway_id : String? = nil,
        gateway_id : String? = nil,
        instance_id : String? = nil,
        local_gateway_id : String? = nil,
        local_target : Bool? = nil,
        nat_gateway_id : String? = nil,
        network_interface_id : String? = nil,
        odb_network_arn : String? = nil,
        transit_gateway_id : String? = nil,
        vpc_endpoint_id : String? = nil,
        vpc_peering_connection_id : String? = nil
      ) : Nil

        input = Types::ReplaceRouteRequest.new(route_table_id: route_table_id, carrier_gateway_id: carrier_gateway_id, core_network_arn: core_network_arn, destination_cidr_block: destination_cidr_block, destination_ipv6_cidr_block: destination_ipv6_cidr_block, destination_prefix_list_id: destination_prefix_list_id, dry_run: dry_run, egress_only_internet_gateway_id: egress_only_internet_gateway_id, gateway_id: gateway_id, instance_id: instance_id, local_gateway_id: local_gateway_id, local_target: local_target, nat_gateway_id: nat_gateway_id, network_interface_id: network_interface_id, odb_network_arn: odb_network_arn, transit_gateway_id: transit_gateway_id, vpc_endpoint_id: vpc_endpoint_id, vpc_peering_connection_id: vpc_peering_connection_id)
        replace_route(input)
      end

      def replace_route(input : Types::ReplaceRouteRequest) : Nil
        request = Protocol::Ec2.build_request(Model::REPLACE_ROUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Changes the route table associated with a given subnet, internet gateway, or virtual private gateway
      # in a VPC. After the operation completes, the subnet or gateway uses the routes in the new route
      # table. For more information about route tables, see Route tables in the Amazon VPC User Guide . You
      # can also use this operation to change which table is the main route table in the VPC. Specify the
      # main route table's association ID and the route table ID of the new main route table.

      def replace_route_table_association(
        association_id : String,
        route_table_id : String,
        dry_run : Bool? = nil
      ) : Types::ReplaceRouteTableAssociationResult

        input = Types::ReplaceRouteTableAssociationRequest.new(association_id: association_id, route_table_id: route_table_id, dry_run: dry_run)
        replace_route_table_association(input)
      end

      def replace_route_table_association(input : Types::ReplaceRouteTableAssociationRequest) : Types::ReplaceRouteTableAssociationResult
        request = Protocol::Ec2.build_request(Model::REPLACE_ROUTE_TABLE_ASSOCIATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ReplaceRouteTableAssociationResult, "ReplaceRouteTableAssociation")
      end

      # Replaces the specified route in the specified transit gateway route table.

      def replace_transit_gateway_route(
        destination_cidr_block : String,
        transit_gateway_route_table_id : String,
        blackhole : Bool? = nil,
        dry_run : Bool? = nil,
        transit_gateway_attachment_id : String? = nil
      ) : Types::ReplaceTransitGatewayRouteResult

        input = Types::ReplaceTransitGatewayRouteRequest.new(destination_cidr_block: destination_cidr_block, transit_gateway_route_table_id: transit_gateway_route_table_id, blackhole: blackhole, dry_run: dry_run, transit_gateway_attachment_id: transit_gateway_attachment_id)
        replace_transit_gateway_route(input)
      end

      def replace_transit_gateway_route(input : Types::ReplaceTransitGatewayRouteRequest) : Types::ReplaceTransitGatewayRouteResult
        request = Protocol::Ec2.build_request(Model::REPLACE_TRANSIT_GATEWAY_ROUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ReplaceTransitGatewayRouteResult, "ReplaceTransitGatewayRoute")
      end

      # Trigger replacement of specified VPN tunnel.

      def replace_vpn_tunnel(
        vpn_connection_id : String,
        vpn_tunnel_outside_ip_address : String,
        apply_pending_maintenance : Bool? = nil,
        dry_run : Bool? = nil
      ) : Types::ReplaceVpnTunnelResult

        input = Types::ReplaceVpnTunnelRequest.new(vpn_connection_id: vpn_connection_id, vpn_tunnel_outside_ip_address: vpn_tunnel_outside_ip_address, apply_pending_maintenance: apply_pending_maintenance, dry_run: dry_run)
        replace_vpn_tunnel(input)
      end

      def replace_vpn_tunnel(input : Types::ReplaceVpnTunnelRequest) : Types::ReplaceVpnTunnelResult
        request = Protocol::Ec2.build_request(Model::REPLACE_VPN_TUNNEL, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ReplaceVpnTunnelResult, "ReplaceVpnTunnel")
      end

      # Submits feedback about the status of an instance. The instance must be in the running state. If your
      # experience with the instance differs from the instance status returned by DescribeInstanceStatus ,
      # use ReportInstanceStatus to report your experience with the instance. Amazon EC2 collects this
      # information to improve the accuracy of status checks. Use of this action does not change the value
      # returned by DescribeInstanceStatus .

      def report_instance_status(
        instances : Array(String),
        reason_codes : Array(String),
        status : String,
        description : String? = nil,
        dry_run : Bool? = nil,
        end_time : Time? = nil,
        start_time : Time? = nil
      ) : Nil

        input = Types::ReportInstanceStatusRequest.new(instances: instances, reason_codes: reason_codes, status: status, description: description, dry_run: dry_run, end_time: end_time, start_time: start_time)
        report_instance_status(input)
      end

      def report_instance_status(input : Types::ReportInstanceStatusRequest) : Nil
        request = Protocol::Ec2.build_request(Model::REPORT_INSTANCE_STATUS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Creates a Spot Fleet request. The Spot Fleet request specifies the total target capacity and the
      # On-Demand target capacity. Amazon EC2 calculates the difference between the total capacity and
      # On-Demand capacity, and launches the difference as Spot capacity. You can submit a single request
      # that includes multiple launch specifications that vary by instance type, AMI, Availability Zone, or
      # subnet. By default, the Spot Fleet requests Spot Instances in the Spot Instance pool where the price
      # per unit is the lowest. Each launch specification can include its own instance weighting that
      # reflects the value of the instance type to your application workload. Alternatively, you can specify
      # that the Spot Fleet distribute the target capacity across the Spot pools included in its launch
      # specifications. By ensuring that the Spot Instances in your Spot Fleet are in different Spot pools,
      # you can improve the availability of your fleet. You can specify tags for the Spot Fleet request and
      # instances launched by the fleet. You cannot tag other resource types in a Spot Fleet request because
      # only the spot-fleet-request and instance resource types are supported. For more information, see
      # Spot Fleet requests in the Amazon EC2 User Guide . We strongly discourage using the RequestSpotFleet
      # API because it is a legacy API with no planned investment. For options for requesting Spot
      # Instances, see Which is the best Spot request method to use? in the Amazon EC2 User Guide .

      def request_spot_fleet(
        spot_fleet_request_config : Types::SpotFleetRequestConfigData,
        dry_run : Bool? = nil
      ) : Types::RequestSpotFleetResponse

        input = Types::RequestSpotFleetRequest.new(spot_fleet_request_config: spot_fleet_request_config, dry_run: dry_run)
        request_spot_fleet(input)
      end

      def request_spot_fleet(input : Types::RequestSpotFleetRequest) : Types::RequestSpotFleetResponse
        request = Protocol::Ec2.build_request(Model::REQUEST_SPOT_FLEET, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::RequestSpotFleetResponse, "RequestSpotFleet")
      end

      # Creates a Spot Instance request. For more information, see Work with Spot Instance in the Amazon EC2
      # User Guide . We strongly discourage using the RequestSpotInstances API because it is a legacy API
      # with no planned investment. For options for requesting Spot Instances, see Which is the best Spot
      # request method to use? in the Amazon EC2 User Guide .

      def request_spot_instances(
        availability_zone_group : String? = nil,
        block_duration_minutes : Int32? = nil,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        instance_count : Int32? = nil,
        instance_interruption_behavior : String? = nil,
        launch_group : String? = nil,
        launch_specification : Types::RequestSpotLaunchSpecification? = nil,
        spot_price : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        type : String? = nil,
        valid_from : Time? = nil,
        valid_until : Time? = nil
      ) : Types::RequestSpotInstancesResult

        input = Types::RequestSpotInstancesRequest.new(availability_zone_group: availability_zone_group, block_duration_minutes: block_duration_minutes, client_token: client_token, dry_run: dry_run, instance_count: instance_count, instance_interruption_behavior: instance_interruption_behavior, launch_group: launch_group, launch_specification: launch_specification, spot_price: spot_price, tag_specifications: tag_specifications, type: type, valid_from: valid_from, valid_until: valid_until)
        request_spot_instances(input)
      end

      def request_spot_instances(input : Types::RequestSpotInstancesRequest) : Types::RequestSpotInstancesResult
        request = Protocol::Ec2.build_request(Model::REQUEST_SPOT_INSTANCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::RequestSpotInstancesResult, "RequestSpotInstances")
      end

      # Resets the attribute of the specified IP address. For requirements, see Using reverse DNS for email
      # applications .

      def reset_address_attribute(
        allocation_id : String,
        attribute : String,
        dry_run : Bool? = nil
      ) : Types::ResetAddressAttributeResult

        input = Types::ResetAddressAttributeRequest.new(allocation_id: allocation_id, attribute: attribute, dry_run: dry_run)
        reset_address_attribute(input)
      end

      def reset_address_attribute(input : Types::ResetAddressAttributeRequest) : Types::ResetAddressAttributeResult
        request = Protocol::Ec2.build_request(Model::RESET_ADDRESS_ATTRIBUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ResetAddressAttributeResult, "ResetAddressAttribute")
      end

      # Resets the default KMS key for EBS encryption for your account in this Region to the Amazon Web
      # Services managed KMS key for EBS. After resetting the default KMS key to the Amazon Web Services
      # managed KMS key, you can continue to encrypt by a customer managed KMS key by specifying it when you
      # create the volume. For more information, see Amazon EBS encryption in the Amazon EBS User Guide .

      def reset_ebs_default_kms_key_id(
        dry_run : Bool? = nil
      ) : Types::ResetEbsDefaultKmsKeyIdResult

        input = Types::ResetEbsDefaultKmsKeyIdRequest.new(dry_run: dry_run)
        reset_ebs_default_kms_key_id(input)
      end

      def reset_ebs_default_kms_key_id(input : Types::ResetEbsDefaultKmsKeyIdRequest) : Types::ResetEbsDefaultKmsKeyIdResult
        request = Protocol::Ec2.build_request(Model::RESET_EBS_DEFAULT_KMS_KEY_ID, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ResetEbsDefaultKmsKeyIdResult, "ResetEbsDefaultKmsKeyId")
      end

      # Resets the specified attribute of the specified Amazon FPGA Image (AFI) to its default value. You
      # can only reset the load permission attribute.

      def reset_fpga_image_attribute(
        fpga_image_id : String,
        attribute : String? = nil,
        dry_run : Bool? = nil
      ) : Types::ResetFpgaImageAttributeResult

        input = Types::ResetFpgaImageAttributeRequest.new(fpga_image_id: fpga_image_id, attribute: attribute, dry_run: dry_run)
        reset_fpga_image_attribute(input)
      end

      def reset_fpga_image_attribute(input : Types::ResetFpgaImageAttributeRequest) : Types::ResetFpgaImageAttributeResult
        request = Protocol::Ec2.build_request(Model::RESET_FPGA_IMAGE_ATTRIBUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::ResetFpgaImageAttributeResult, "ResetFpgaImageAttribute")
      end

      # Resets an attribute of an AMI to its default value.

      def reset_image_attribute(
        attribute : String,
        image_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::ResetImageAttributeRequest.new(attribute: attribute, image_id: image_id, dry_run: dry_run)
        reset_image_attribute(input)
      end

      def reset_image_attribute(input : Types::ResetImageAttributeRequest) : Nil
        request = Protocol::Ec2.build_request(Model::RESET_IMAGE_ATTRIBUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Resets an attribute of an instance to its default value. To reset the kernel or ramdisk , the
      # instance must be in a stopped state. To reset the sourceDestCheck , the instance can be either
      # running or stopped. The sourceDestCheck attribute controls whether source/destination checking is
      # enabled. The default value is true , which means checking is enabled. This value must be false for a
      # NAT instance to perform NAT. For more information, see NAT instances in the Amazon VPC User Guide .

      def reset_instance_attribute(
        attribute : String,
        instance_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::ResetInstanceAttributeRequest.new(attribute: attribute, instance_id: instance_id, dry_run: dry_run)
        reset_instance_attribute(input)
      end

      def reset_instance_attribute(input : Types::ResetInstanceAttributeRequest) : Nil
        request = Protocol::Ec2.build_request(Model::RESET_INSTANCE_ATTRIBUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Resets a network interface attribute. You can specify only one attribute at a time.

      def reset_network_interface_attribute(
        network_interface_id : String,
        dry_run : Bool? = nil,
        source_dest_check : String? = nil
      ) : Nil

        input = Types::ResetNetworkInterfaceAttributeRequest.new(network_interface_id: network_interface_id, dry_run: dry_run, source_dest_check: source_dest_check)
        reset_network_interface_attribute(input)
      end

      def reset_network_interface_attribute(input : Types::ResetNetworkInterfaceAttributeRequest) : Nil
        request = Protocol::Ec2.build_request(Model::RESET_NETWORK_INTERFACE_ATTRIBUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Resets permission settings for the specified snapshot. For more information about modifying snapshot
      # permissions, see Share a snapshot in the Amazon EBS User Guide .

      def reset_snapshot_attribute(
        attribute : String,
        snapshot_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::ResetSnapshotAttributeRequest.new(attribute: attribute, snapshot_id: snapshot_id, dry_run: dry_run)
        reset_snapshot_attribute(input)
      end

      def reset_snapshot_attribute(input : Types::ResetSnapshotAttributeRequest) : Nil
        request = Protocol::Ec2.build_request(Model::RESET_SNAPSHOT_ATTRIBUTE, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # This action is deprecated. Restores an Elastic IP address that was previously moved to the EC2-VPC
      # platform back to the EC2-Classic platform. You cannot move an Elastic IP address that was originally
      # allocated for use in EC2-VPC. The Elastic IP address must not be associated with an instance or
      # network interface.

      def restore_address_to_classic(
        public_ip : String,
        dry_run : Bool? = nil
      ) : Types::RestoreAddressToClassicResult

        input = Types::RestoreAddressToClassicRequest.new(public_ip: public_ip, dry_run: dry_run)
        restore_address_to_classic(input)
      end

      def restore_address_to_classic(input : Types::RestoreAddressToClassicRequest) : Types::RestoreAddressToClassicResult
        request = Protocol::Ec2.build_request(Model::RESTORE_ADDRESS_TO_CLASSIC, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::RestoreAddressToClassicResult, "RestoreAddressToClassic")
      end

      # Restores an AMI from the Recycle Bin. For more information, see Recover deleted Amazon EBS snapshots
      # and EBS-back AMIs with Recycle Bin in the Amazon EC2 User Guide .

      def restore_image_from_recycle_bin(
        image_id : String,
        dry_run : Bool? = nil
      ) : Types::RestoreImageFromRecycleBinResult

        input = Types::RestoreImageFromRecycleBinRequest.new(image_id: image_id, dry_run: dry_run)
        restore_image_from_recycle_bin(input)
      end

      def restore_image_from_recycle_bin(input : Types::RestoreImageFromRecycleBinRequest) : Types::RestoreImageFromRecycleBinResult
        request = Protocol::Ec2.build_request(Model::RESTORE_IMAGE_FROM_RECYCLE_BIN, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::RestoreImageFromRecycleBinResult, "RestoreImageFromRecycleBin")
      end

      # Restores the entries from a previous version of a managed prefix list to a new version of the prefix
      # list.

      def restore_managed_prefix_list_version(
        current_version : Int64,
        prefix_list_id : String,
        previous_version : Int64,
        dry_run : Bool? = nil
      ) : Types::RestoreManagedPrefixListVersionResult

        input = Types::RestoreManagedPrefixListVersionRequest.new(current_version: current_version, prefix_list_id: prefix_list_id, previous_version: previous_version, dry_run: dry_run)
        restore_managed_prefix_list_version(input)
      end

      def restore_managed_prefix_list_version(input : Types::RestoreManagedPrefixListVersionRequest) : Types::RestoreManagedPrefixListVersionResult
        request = Protocol::Ec2.build_request(Model::RESTORE_MANAGED_PREFIX_LIST_VERSION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::RestoreManagedPrefixListVersionResult, "RestoreManagedPrefixListVersion")
      end

      # Restores a snapshot from the Recycle Bin. For more information, see Restore snapshots from the
      # Recycle Bin in the Amazon EBS User Guide .

      def restore_snapshot_from_recycle_bin(
        snapshot_id : String,
        dry_run : Bool? = nil
      ) : Types::RestoreSnapshotFromRecycleBinResult

        input = Types::RestoreSnapshotFromRecycleBinRequest.new(snapshot_id: snapshot_id, dry_run: dry_run)
        restore_snapshot_from_recycle_bin(input)
      end

      def restore_snapshot_from_recycle_bin(input : Types::RestoreSnapshotFromRecycleBinRequest) : Types::RestoreSnapshotFromRecycleBinResult
        request = Protocol::Ec2.build_request(Model::RESTORE_SNAPSHOT_FROM_RECYCLE_BIN, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::RestoreSnapshotFromRecycleBinResult, "RestoreSnapshotFromRecycleBin")
      end

      # Restores an archived Amazon EBS snapshot for use temporarily or permanently, or modifies the restore
      # period or restore type for a snapshot that was previously temporarily restored. For more information
      # see Restore an archived snapshot and modify the restore period or restore type for a temporarily
      # restored snapshot in the Amazon EBS User Guide .

      def restore_snapshot_tier(
        snapshot_id : String,
        dry_run : Bool? = nil,
        permanent_restore : Bool? = nil,
        temporary_restore_days : Int32? = nil
      ) : Types::RestoreSnapshotTierResult

        input = Types::RestoreSnapshotTierRequest.new(snapshot_id: snapshot_id, dry_run: dry_run, permanent_restore: permanent_restore, temporary_restore_days: temporary_restore_days)
        restore_snapshot_tier(input)
      end

      def restore_snapshot_tier(input : Types::RestoreSnapshotTierRequest) : Types::RestoreSnapshotTierResult
        request = Protocol::Ec2.build_request(Model::RESTORE_SNAPSHOT_TIER, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::RestoreSnapshotTierResult, "RestoreSnapshotTier")
      end

      # Restores a volume from the Recycle Bin. For more information, see Restore volumes from the Recycle
      # Bin in the Amazon EBS User Guide .

      def restore_volume_from_recycle_bin(
        volume_id : String,
        dry_run : Bool? = nil
      ) : Types::RestoreVolumeFromRecycleBinResult

        input = Types::RestoreVolumeFromRecycleBinRequest.new(volume_id: volume_id, dry_run: dry_run)
        restore_volume_from_recycle_bin(input)
      end

      def restore_volume_from_recycle_bin(input : Types::RestoreVolumeFromRecycleBinRequest) : Types::RestoreVolumeFromRecycleBinResult
        request = Protocol::Ec2.build_request(Model::RESTORE_VOLUME_FROM_RECYCLE_BIN, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::RestoreVolumeFromRecycleBinResult, "RestoreVolumeFromRecycleBin")
      end

      # Removes an ingress authorization rule from a Client VPN endpoint.

      def revoke_client_vpn_ingress(
        client_vpn_endpoint_id : String,
        target_network_cidr : String,
        access_group_id : String? = nil,
        dry_run : Bool? = nil,
        revoke_all_groups : Bool? = nil
      ) : Types::RevokeClientVpnIngressResult

        input = Types::RevokeClientVpnIngressRequest.new(client_vpn_endpoint_id: client_vpn_endpoint_id, target_network_cidr: target_network_cidr, access_group_id: access_group_id, dry_run: dry_run, revoke_all_groups: revoke_all_groups)
        revoke_client_vpn_ingress(input)
      end

      def revoke_client_vpn_ingress(input : Types::RevokeClientVpnIngressRequest) : Types::RevokeClientVpnIngressResult
        request = Protocol::Ec2.build_request(Model::REVOKE_CLIENT_VPN_INGRESS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::RevokeClientVpnIngressResult, "RevokeClientVpnIngress")
      end

      # Removes the specified outbound (egress) rules from the specified security group. You can specify
      # rules using either rule IDs or security group rule properties. If you use rule properties, the
      # values that you specify (for example, ports) must match the existing rule's values exactly. Each
      # rule has a protocol, from and to ports, and destination (CIDR range, security group, or prefix
      # list). For the TCP and UDP protocols, you must also specify the destination port or range of ports.
      # For the ICMP protocol, you must also specify the ICMP type and code. If the security group rule has
      # a description, you do not need to specify the description to revoke the rule. For a default VPC, if
      # the values you specify do not match the existing rule's values, no error is returned, and the output
      # describes the security group rules that were not revoked. Amazon Web Services recommends that you
      # describe the security group to verify that the rules were removed. Rule changes are propagated to
      # instances within the security group as quickly as possible. However, a small delay might occur.

      def revoke_security_group_egress(
        group_id : String,
        cidr_ip : String? = nil,
        dry_run : Bool? = nil,
        from_port : Int32? = nil,
        ip_permissions : Array(Types::IpPermission)? = nil,
        ip_protocol : String? = nil,
        security_group_rule_ids : Array(String)? = nil,
        source_security_group_name : String? = nil,
        source_security_group_owner_id : String? = nil,
        to_port : Int32? = nil
      ) : Types::RevokeSecurityGroupEgressResult

        input = Types::RevokeSecurityGroupEgressRequest.new(group_id: group_id, cidr_ip: cidr_ip, dry_run: dry_run, from_port: from_port, ip_permissions: ip_permissions, ip_protocol: ip_protocol, security_group_rule_ids: security_group_rule_ids, source_security_group_name: source_security_group_name, source_security_group_owner_id: source_security_group_owner_id, to_port: to_port)
        revoke_security_group_egress(input)
      end

      def revoke_security_group_egress(input : Types::RevokeSecurityGroupEgressRequest) : Types::RevokeSecurityGroupEgressResult
        request = Protocol::Ec2.build_request(Model::REVOKE_SECURITY_GROUP_EGRESS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::RevokeSecurityGroupEgressResult, "RevokeSecurityGroupEgress")
      end

      # Removes the specified inbound (ingress) rules from a security group. You can specify rules using
      # either rule IDs or security group rule properties. If you use rule properties, the values that you
      # specify (for example, ports) must match the existing rule's values exactly. Each rule has a
      # protocol, from and to ports, and source (CIDR range, security group, or prefix list). For the TCP
      # and UDP protocols, you must also specify the destination port or range of ports. For the ICMP
      # protocol, you must also specify the ICMP type and code. If the security group rule has a
      # description, you do not need to specify the description to revoke the rule. For a default VPC, if
      # the values you specify do not match the existing rule's values, no error is returned, and the output
      # describes the security group rules that were not revoked. For a non-default VPC, if the values you
      # specify do not match the existing rule's values, an InvalidPermission.NotFound client error is
      # returned, and no rules are revoked. Amazon Web Services recommends that you describe the security
      # group to verify that the rules were removed. Rule changes are propagated to instances within the
      # security group as quickly as possible. However, a small delay might occur.

      def revoke_security_group_ingress(
        cidr_ip : String? = nil,
        dry_run : Bool? = nil,
        from_port : Int32? = nil,
        group_id : String? = nil,
        group_name : String? = nil,
        ip_permissions : Array(Types::IpPermission)? = nil,
        ip_protocol : String? = nil,
        security_group_rule_ids : Array(String)? = nil,
        source_security_group_name : String? = nil,
        source_security_group_owner_id : String? = nil,
        to_port : Int32? = nil
      ) : Types::RevokeSecurityGroupIngressResult

        input = Types::RevokeSecurityGroupIngressRequest.new(cidr_ip: cidr_ip, dry_run: dry_run, from_port: from_port, group_id: group_id, group_name: group_name, ip_permissions: ip_permissions, ip_protocol: ip_protocol, security_group_rule_ids: security_group_rule_ids, source_security_group_name: source_security_group_name, source_security_group_owner_id: source_security_group_owner_id, to_port: to_port)
        revoke_security_group_ingress(input)
      end

      def revoke_security_group_ingress(input : Types::RevokeSecurityGroupIngressRequest) : Types::RevokeSecurityGroupIngressResult
        request = Protocol::Ec2.build_request(Model::REVOKE_SECURITY_GROUP_INGRESS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::RevokeSecurityGroupIngressResult, "RevokeSecurityGroupIngress")
      end

      # Launches the specified number of instances using an AMI for which you have permissions. You can
      # specify a number of options, or leave the default options. The following rules apply: If you don't
      # specify a subnet ID, we choose a default subnet from your default VPC for you. If you don't have a
      # default VPC, you must specify a subnet ID in the request. All instances have a network interface
      # with a primary private IPv4 address. If you don't specify this address, we choose one from the IPv4
      # range of your subnet. Not all instance types support IPv6 addresses. For more information, see
      # Instance types . If you don't specify a security group ID, we use the default security group for the
      # VPC. For more information, see Security groups . If any of the AMIs have a product code attached for
      # which the user has not subscribed, the request fails. You can create a launch template , which is a
      # resource that contains the parameters to launch an instance. When you launch an instance using
      # RunInstances , you can specify the launch template instead of specifying the launch parameters. To
      # ensure faster instance launches, break up large requests into smaller batches. For example, create
      # five separate launch requests for 100 instances each instead of one launch request for 500
      # instances. RunInstances is subject to both request rate limiting and resource rate limiting. For
      # more information, see Request throttling . An instance is ready for you to use when it's in the
      # running state. You can check the state of your instance using DescribeInstances . You can tag
      # instances and EBS volumes during launch, after launch, or both. For more information, see CreateTags
      # and Tagging your Amazon EC2 resources . Linux instances have access to the public key of the key
      # pair at boot. You can use this key to provide secure access to the instance. Amazon EC2 public
      # images use this feature to provide secure access without passwords. For more information, see Key
      # pairs . For troubleshooting, see What to do if an instance immediately terminates , and
      # Troubleshooting connecting to your instance .

      def run_instances(
        max_count : Int32,
        min_count : Int32,
        additional_info : String? = nil,
        block_device_mappings : Array(Types::BlockDeviceMapping)? = nil,
        capacity_reservation_specification : Types::CapacityReservationSpecification? = nil,
        client_token : String? = nil,
        cpu_options : Types::CpuOptionsRequest? = nil,
        credit_specification : Types::CreditSpecificationRequest? = nil,
        disable_api_stop : Bool? = nil,
        disable_api_termination : Bool? = nil,
        dry_run : Bool? = nil,
        ebs_optimized : Bool? = nil,
        elastic_gpu_specification : Array(Types::ElasticGpuSpecification)? = nil,
        elastic_inference_accelerators : Array(Types::ElasticInferenceAccelerator)? = nil,
        enable_primary_ipv6 : Bool? = nil,
        enclave_options : Types::EnclaveOptionsRequest? = nil,
        hibernation_options : Types::HibernationOptionsRequest? = nil,
        iam_instance_profile : Types::IamInstanceProfileSpecification? = nil,
        image_id : String? = nil,
        instance_initiated_shutdown_behavior : String? = nil,
        instance_market_options : Types::InstanceMarketOptionsRequest? = nil,
        instance_type : String? = nil,
        ipv6_address_count : Int32? = nil,
        ipv6_addresses : Array(Types::InstanceIpv6Address)? = nil,
        kernel_id : String? = nil,
        key_name : String? = nil,
        launch_template : Types::LaunchTemplateSpecification? = nil,
        license_specifications : Array(Types::LicenseConfigurationRequest)? = nil,
        maintenance_options : Types::InstanceMaintenanceOptionsRequest? = nil,
        metadata_options : Types::InstanceMetadataOptionsRequest? = nil,
        monitoring : Types::RunInstancesMonitoringEnabled? = nil,
        network_interfaces : Array(Types::InstanceNetworkInterfaceSpecification)? = nil,
        network_performance_options : Types::InstanceNetworkPerformanceOptionsRequest? = nil,
        operator : Types::OperatorRequest? = nil,
        placement : Types::Placement? = nil,
        private_dns_name_options : Types::PrivateDnsNameOptionsRequest? = nil,
        private_ip_address : String? = nil,
        ramdisk_id : String? = nil,
        security_group_ids : Array(String)? = nil,
        security_groups : Array(String)? = nil,
        subnet_id : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil,
        user_data : String? = nil
      ) : Types::Reservation

        input = Types::RunInstancesRequest.new(max_count: max_count, min_count: min_count, additional_info: additional_info, block_device_mappings: block_device_mappings, capacity_reservation_specification: capacity_reservation_specification, client_token: client_token, cpu_options: cpu_options, credit_specification: credit_specification, disable_api_stop: disable_api_stop, disable_api_termination: disable_api_termination, dry_run: dry_run, ebs_optimized: ebs_optimized, elastic_gpu_specification: elastic_gpu_specification, elastic_inference_accelerators: elastic_inference_accelerators, enable_primary_ipv6: enable_primary_ipv6, enclave_options: enclave_options, hibernation_options: hibernation_options, iam_instance_profile: iam_instance_profile, image_id: image_id, instance_initiated_shutdown_behavior: instance_initiated_shutdown_behavior, instance_market_options: instance_market_options, instance_type: instance_type, ipv6_address_count: ipv6_address_count, ipv6_addresses: ipv6_addresses, kernel_id: kernel_id, key_name: key_name, launch_template: launch_template, license_specifications: license_specifications, maintenance_options: maintenance_options, metadata_options: metadata_options, monitoring: monitoring, network_interfaces: network_interfaces, network_performance_options: network_performance_options, operator: operator, placement: placement, private_dns_name_options: private_dns_name_options, private_ip_address: private_ip_address, ramdisk_id: ramdisk_id, security_group_ids: security_group_ids, security_groups: security_groups, subnet_id: subnet_id, tag_specifications: tag_specifications, user_data: user_data)
        run_instances(input)
      end

      def run_instances(input : Types::RunInstancesRequest) : Types::Reservation
        request = Protocol::Ec2.build_request(Model::RUN_INSTANCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::Reservation, "RunInstances")
      end

      # Launches the specified Scheduled Instances. Before you can launch a Scheduled Instance, you must
      # purchase it and obtain an identifier using PurchaseScheduledInstances . You must launch a Scheduled
      # Instance during its scheduled time period. You can't stop or reboot a Scheduled Instance, but you
      # can terminate it as needed. If you terminate a Scheduled Instance before the current scheduled time
      # period ends, you can launch it again after a few minutes.

      def run_scheduled_instances(
        launch_specification : Types::ScheduledInstancesLaunchSpecification,
        scheduled_instance_id : String,
        client_token : String? = nil,
        dry_run : Bool? = nil,
        instance_count : Int32? = nil
      ) : Types::RunScheduledInstancesResult

        input = Types::RunScheduledInstancesRequest.new(launch_specification: launch_specification, scheduled_instance_id: scheduled_instance_id, client_token: client_token, dry_run: dry_run, instance_count: instance_count)
        run_scheduled_instances(input)
      end

      def run_scheduled_instances(input : Types::RunScheduledInstancesRequest) : Types::RunScheduledInstancesResult
        request = Protocol::Ec2.build_request(Model::RUN_SCHEDULED_INSTANCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::RunScheduledInstancesResult, "RunScheduledInstances")
      end

      # Searches for routes in the specified local gateway route table.

      def search_local_gateway_routes(
        local_gateway_route_table_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::SearchLocalGatewayRoutesResult

        input = Types::SearchLocalGatewayRoutesRequest.new(local_gateway_route_table_id: local_gateway_route_table_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        search_local_gateway_routes(input)
      end

      def search_local_gateway_routes(input : Types::SearchLocalGatewayRoutesRequest) : Types::SearchLocalGatewayRoutesResult
        request = Protocol::Ec2.build_request(Model::SEARCH_LOCAL_GATEWAY_ROUTES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::SearchLocalGatewayRoutesResult, "SearchLocalGatewayRoutes")
      end

      # Searches one or more transit gateway multicast groups and returns the group membership information.

      def search_transit_gateway_multicast_groups(
        transit_gateway_multicast_domain_id : String,
        dry_run : Bool? = nil,
        filters : Array(Types::Filter)? = nil,
        max_results : Int32? = nil,
        next_token : String? = nil
      ) : Types::SearchTransitGatewayMulticastGroupsResult

        input = Types::SearchTransitGatewayMulticastGroupsRequest.new(transit_gateway_multicast_domain_id: transit_gateway_multicast_domain_id, dry_run: dry_run, filters: filters, max_results: max_results, next_token: next_token)
        search_transit_gateway_multicast_groups(input)
      end

      def search_transit_gateway_multicast_groups(input : Types::SearchTransitGatewayMulticastGroupsRequest) : Types::SearchTransitGatewayMulticastGroupsResult
        request = Protocol::Ec2.build_request(Model::SEARCH_TRANSIT_GATEWAY_MULTICAST_GROUPS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::SearchTransitGatewayMulticastGroupsResult, "SearchTransitGatewayMulticastGroups")
      end

      # Searches for routes in the specified transit gateway route table.

      def search_transit_gateway_routes(
        filters : Array(Types::Filter),
        transit_gateway_route_table_id : String,
        dry_run : Bool? = nil,
        max_results : Int32? = nil
      ) : Types::SearchTransitGatewayRoutesResult

        input = Types::SearchTransitGatewayRoutesRequest.new(filters: filters, transit_gateway_route_table_id: transit_gateway_route_table_id, dry_run: dry_run, max_results: max_results)
        search_transit_gateway_routes(input)
      end

      def search_transit_gateway_routes(input : Types::SearchTransitGatewayRoutesRequest) : Types::SearchTransitGatewayRoutesResult
        request = Protocol::Ec2.build_request(Model::SEARCH_TRANSIT_GATEWAY_ROUTES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::SearchTransitGatewayRoutesResult, "SearchTransitGatewayRoutes")
      end

      # Sends a diagnostic interrupt to the specified Amazon EC2 instance to trigger a kernel panic (on
      # Linux instances), or a blue screen / stop error (on Windows instances). For instances based on Intel
      # and AMD processors, the interrupt is received as a non-maskable interrupt (NMI). In general, the
      # operating system crashes and reboots when a kernel panic or stop error is triggered. The operating
      # system can also be configured to perform diagnostic tasks, such as generating a memory dump file,
      # loading a secondary kernel, or obtaining a call trace. Before sending a diagnostic interrupt to your
      # instance, ensure that its operating system is configured to perform the required diagnostic tasks.
      # For more information about configuring your operating system to generate a crash dump when a kernel
      # panic or stop error occurs, see Send a diagnostic interrupt (for advanced users) in the Amazon EC2
      # User Guide .

      def send_diagnostic_interrupt(
        instance_id : String,
        dry_run : Bool? = nil
      ) : Nil

        input = Types::SendDiagnosticInterruptRequest.new(instance_id: instance_id, dry_run: dry_run)
        send_diagnostic_interrupt(input)
      end

      def send_diagnostic_interrupt(input : Types::SendDiagnosticInterruptRequest) : Nil
        request = Protocol::Ec2.build_request(Model::SEND_DIAGNOSTIC_INTERRUPT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Generates an account status report. The report is generated asynchronously, and can take several
      # hours to complete. The report provides the current status of all attributes supported by declarative
      # policies for the accounts within the specified scope. The scope is determined by the specified
      # TargetId , which can represent an individual account, or all the accounts that fall under the
      # specified organizational unit (OU) or root (the entire Amazon Web Services Organization). The report
      # is saved to your specified S3 bucket, using the following path structure (with the capitalized
      # placeholders representing your specific values):
      # s3://AMZN-S3-DEMO-BUCKET/YOUR-OPTIONAL-S3-PREFIX/ec2_TARGETID_REPORTID_YYYYMMDDTHHMMZ.csv
      # Prerequisites for generating a report The StartDeclarativePoliciesReport API can only be called by
      # the management account or delegated administrators for the organization. An S3 bucket must be
      # available before generating the report (you can create a new one or use an existing one), it must be
      # in the same Region where the report generation request is made, and it must have an appropriate
      # bucket policy. For a sample S3 policy, see Sample Amazon S3 policy under Examples . Trusted access
      # must be enabled for the service for which the declarative policy will enforce a baseline
      # configuration. If you use the Amazon Web Services Organizations console, this is done automatically
      # when you enable declarative policies. The API uses the following service principal to identify the
      # EC2 service: ec2.amazonaws.com . For more information on how to enable trusted access with the
      # Amazon Web Services CLI and Amazon Web Services SDKs, see Using Organizations with other Amazon Web
      # Services services in the Amazon Web Services Organizations User Guide . Only one report per
      # organization can be generated at a time. Attempting to generate a report while another is in
      # progress will result in an error. For more information, including the required IAM permissions to
      # run this API, see Generating the account status report for declarative policies in the Amazon Web
      # Services Organizations User Guide .

      def start_declarative_policies_report(
        s3_bucket : String,
        target_id : String,
        dry_run : Bool? = nil,
        s3_prefix : String? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::StartDeclarativePoliciesReportResult

        input = Types::StartDeclarativePoliciesReportRequest.new(s3_bucket: s3_bucket, target_id: target_id, dry_run: dry_run, s3_prefix: s3_prefix, tag_specifications: tag_specifications)
        start_declarative_policies_report(input)
      end

      def start_declarative_policies_report(input : Types::StartDeclarativePoliciesReportRequest) : Types::StartDeclarativePoliciesReportResult
        request = Protocol::Ec2.build_request(Model::START_DECLARATIVE_POLICIES_REPORT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::StartDeclarativePoliciesReportResult, "StartDeclarativePoliciesReport")
      end

      # Starts an Amazon EBS-backed instance that you've previously stopped. Instances that use Amazon EBS
      # volumes as their root devices can be quickly stopped and started. When an instance is stopped, the
      # compute resources are released and you are not billed for instance usage. However, your root
      # partition Amazon EBS volume remains and continues to persist your data, and you are charged for
      # Amazon EBS volume usage. You can restart your instance at any time. Every time you start your
      # instance, Amazon EC2 charges a one-minute minimum for instance usage, and thereafter charges per
      # second for instance usage. Before stopping an instance, make sure it is in a state from which it can
      # be restarted. Stopping an instance does not preserve data stored in RAM. Performing this operation
      # on an instance that uses an instance store as its root device returns an error. If you attempt to
      # start a T3 instance with host tenancy and the unlimited CPU credit option, the request fails. The
      # unlimited CPU credit option is not supported on Dedicated Hosts. Before you start the instance,
      # either change its CPU credit option to standard , or change its tenancy to default or dedicated .
      # For more information, see Stop and start Amazon EC2 instances in the Amazon EC2 User Guide .

      def start_instances(
        instance_ids : Array(String),
        additional_info : String? = nil,
        dry_run : Bool? = nil
      ) : Types::StartInstancesResult

        input = Types::StartInstancesRequest.new(instance_ids: instance_ids, additional_info: additional_info, dry_run: dry_run)
        start_instances(input)
      end

      def start_instances(input : Types::StartInstancesRequest) : Types::StartInstancesResult
        request = Protocol::Ec2.build_request(Model::START_INSTANCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::StartInstancesResult, "StartInstances")
      end

      # Starts analyzing the specified Network Access Scope.

      def start_network_insights_access_scope_analysis(
        client_token : String,
        network_insights_access_scope_id : String,
        dry_run : Bool? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::StartNetworkInsightsAccessScopeAnalysisResult

        input = Types::StartNetworkInsightsAccessScopeAnalysisRequest.new(client_token: client_token, network_insights_access_scope_id: network_insights_access_scope_id, dry_run: dry_run, tag_specifications: tag_specifications)
        start_network_insights_access_scope_analysis(input)
      end

      def start_network_insights_access_scope_analysis(input : Types::StartNetworkInsightsAccessScopeAnalysisRequest) : Types::StartNetworkInsightsAccessScopeAnalysisResult
        request = Protocol::Ec2.build_request(Model::START_NETWORK_INSIGHTS_ACCESS_SCOPE_ANALYSIS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::StartNetworkInsightsAccessScopeAnalysisResult, "StartNetworkInsightsAccessScopeAnalysis")
      end

      # Starts analyzing the specified path. If the path is reachable, the operation returns the shortest
      # feasible path.

      def start_network_insights_analysis(
        client_token : String,
        network_insights_path_id : String,
        additional_accounts : Array(String)? = nil,
        dry_run : Bool? = nil,
        filter_in_arns : Array(String)? = nil,
        filter_out_arns : Array(String)? = nil,
        tag_specifications : Array(Types::TagSpecification)? = nil
      ) : Types::StartNetworkInsightsAnalysisResult

        input = Types::StartNetworkInsightsAnalysisRequest.new(client_token: client_token, network_insights_path_id: network_insights_path_id, additional_accounts: additional_accounts, dry_run: dry_run, filter_in_arns: filter_in_arns, filter_out_arns: filter_out_arns, tag_specifications: tag_specifications)
        start_network_insights_analysis(input)
      end

      def start_network_insights_analysis(input : Types::StartNetworkInsightsAnalysisRequest) : Types::StartNetworkInsightsAnalysisResult
        request = Protocol::Ec2.build_request(Model::START_NETWORK_INSIGHTS_ANALYSIS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::StartNetworkInsightsAnalysisResult, "StartNetworkInsightsAnalysis")
      end

      # Initiates the verification process to prove that the service provider owns the private DNS name
      # domain for the endpoint service. The service provider must successfully perform the verification
      # before the consumer can use the name to access the service. Before the service provider runs this
      # command, they must add a record to the DNS server.

      def start_vpc_endpoint_service_private_dns_verification(
        service_id : String,
        dry_run : Bool? = nil
      ) : Types::StartVpcEndpointServicePrivateDnsVerificationResult

        input = Types::StartVpcEndpointServicePrivateDnsVerificationRequest.new(service_id: service_id, dry_run: dry_run)
        start_vpc_endpoint_service_private_dns_verification(input)
      end

      def start_vpc_endpoint_service_private_dns_verification(input : Types::StartVpcEndpointServicePrivateDnsVerificationRequest) : Types::StartVpcEndpointServicePrivateDnsVerificationResult
        request = Protocol::Ec2.build_request(Model::START_VPC_ENDPOINT_SERVICE_PRIVATE_DNS_VERIFICATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::StartVpcEndpointServicePrivateDnsVerificationResult, "StartVpcEndpointServicePrivateDnsVerification")
      end

      # Stops an Amazon EBS-backed instance. You can restart your instance at any time using the
      # StartInstances API. For more information, see Stop and start Amazon EC2 instances in the Amazon EC2
      # User Guide . When you stop or hibernate an instance, we shut it down. By default, this includes a
      # graceful operating system (OS) shutdown. To bypass the graceful shutdown, use the skipOsShutdown
      # parameter; however, this might risk data integrity. You can use the StopInstances operation together
      # with the Hibernate parameter to hibernate an instance if the instance is enabled for hibernation and
      # meets the hibernation prerequisites . Stopping an instance doesn't preserve data stored in RAM,
      # while hibernation does. If hibernation fails, a normal shutdown occurs. For more information, see
      # Hibernate your Amazon EC2 instance in the Amazon EC2 User Guide . If your instance appears stuck in
      # the stopping state, there might be an issue with the underlying host computer. You can use the
      # StopInstances operation together with the Force parameter to force stop your instance. For more
      # information, see Troubleshoot Amazon EC2 instance stop issues in the Amazon EC2 User Guide .
      # Stopping and hibernating an instance differs from rebooting or terminating it. For example, a
      # stopped or hibernated instance retains its root volume and any data volumes, unlike terminated
      # instances where these volumes are automatically deleted. For more information about the differences
      # between stopping, hibernating, rebooting, and terminating instances, see Amazon EC2 instance state
      # changes in the Amazon EC2 User Guide . We don't charge for instance usage or data transfer fees when
      # an instance is stopped. However, the root volume and any data volumes remain and continue to persist
      # your data, and you're charged for volume usage. Every time you start your instance, Amazon EC2
      # charges a one-minute minimum for instance usage, followed by per-second billing. You can't stop or
      # hibernate instance store-backed instances.

      def stop_instances(
        instance_ids : Array(String),
        dry_run : Bool? = nil,
        force : Bool? = nil,
        hibernate : Bool? = nil,
        skip_os_shutdown : Bool? = nil
      ) : Types::StopInstancesResult

        input = Types::StopInstancesRequest.new(instance_ids: instance_ids, dry_run: dry_run, force: force, hibernate: hibernate, skip_os_shutdown: skip_os_shutdown)
        stop_instances(input)
      end

      def stop_instances(input : Types::StopInstancesRequest) : Types::StopInstancesResult
        request = Protocol::Ec2.build_request(Model::STOP_INSTANCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::StopInstancesResult, "StopInstances")
      end

      # Terminates active Client VPN endpoint connections. This action can be used to terminate a specific
      # client connection, or up to five connections established by a specific user.

      def terminate_client_vpn_connections(
        client_vpn_endpoint_id : String,
        connection_id : String? = nil,
        dry_run : Bool? = nil,
        username : String? = nil
      ) : Types::TerminateClientVpnConnectionsResult

        input = Types::TerminateClientVpnConnectionsRequest.new(client_vpn_endpoint_id: client_vpn_endpoint_id, connection_id: connection_id, dry_run: dry_run, username: username)
        terminate_client_vpn_connections(input)
      end

      def terminate_client_vpn_connections(input : Types::TerminateClientVpnConnectionsRequest) : Types::TerminateClientVpnConnectionsResult
        request = Protocol::Ec2.build_request(Model::TERMINATE_CLIENT_VPN_CONNECTIONS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::TerminateClientVpnConnectionsResult, "TerminateClientVpnConnections")
      end

      # Terminates (deletes) the specified instances. This operation is idempotent ; if you terminate an
      # instance more than once, each call succeeds. Terminating an instance is permanent and irreversible.
      # After you terminate an instance, you can no longer connect to it, and it can't be recovered. All
      # attached Amazon EBS volumes that are configured to be deleted on termination are also permanently
      # deleted and can't be recovered. All data stored on instance store volumes is permanently lost. For
      # more information, see How instance termination works . Before you terminate an instance, ensure that
      # you have backed up all data that you need to retain after the termination to persistent storage. If
      # you specify multiple instances and the request fails (for example, because of a single incorrect
      # instance ID), none of the instances are terminated. If you terminate multiple instances across
      # multiple Availability Zones, and one or more of the specified instances are enabled for termination
      # protection, the request fails with the following results: The specified instances that are in the
      # same Availability Zone as the protected instance are not terminated. The specified instances that
      # are in different Availability Zones, where no other specified instances are protected, are
      # successfully terminated. For example, say you have the following instances: Instance A: us-east-1a ;
      # Not protected Instance B: us-east-1a ; Not protected Instance C: us-east-1b ; Protected Instance D:
      # us-east-1b ; not protected If you attempt to terminate all of these instances in the same request,
      # the request reports failure with the following results: Instance A and Instance B are successfully
      # terminated because none of the specified instances in us-east-1a are enabled for termination
      # protection. Instance C and Instance D fail to terminate because at least one of the specified
      # instances in us-east-1b (Instance C) is enabled for termination protection. Terminated instances
      # remain visible after termination (for approximately one hour). By default, Amazon EC2 deletes all
      # EBS volumes that were attached when the instance launched. Volumes attached after instance launch
      # continue running. By default, the TerminateInstances operation includes a graceful operating system
      # (OS) shutdown. To bypass the graceful shutdown, use the skipOsShutdown parameter; however, this
      # might risk data integrity. You can stop, start, and terminate EBS-backed instances. You can only
      # terminate instance store-backed instances. What happens to an instance differs if you stop or
      # terminate it. For example, when you stop an instance, the root device and any other devices attached
      # to the instance persist. When you terminate an instance, any attached EBS volumes with the
      # DeleteOnTermination block device mapping parameter set to true are automatically deleted. For more
      # information about the differences between stopping and terminating instances, see Amazon EC2
      # instance state changes in the Amazon EC2 User Guide . When you terminate an instance, we attempt to
      # terminate it forcibly after a short while. If your instance appears stuck in the shutting-down state
      # after a period of time, there might be an issue with the underlying host computer. For more
      # information about terminating and troubleshooting terminating your instances, see Terminate Amazon
      # EC2 instances and Troubleshooting terminating your instance in the Amazon EC2 User Guide .

      def terminate_instances(
        instance_ids : Array(String),
        dry_run : Bool? = nil,
        force : Bool? = nil,
        skip_os_shutdown : Bool? = nil
      ) : Types::TerminateInstancesResult

        input = Types::TerminateInstancesRequest.new(instance_ids: instance_ids, dry_run: dry_run, force: force, skip_os_shutdown: skip_os_shutdown)
        terminate_instances(input)
      end

      def terminate_instances(input : Types::TerminateInstancesRequest) : Types::TerminateInstancesResult
        request = Protocol::Ec2.build_request(Model::TERMINATE_INSTANCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::TerminateInstancesResult, "TerminateInstances")
      end

      # Unassigns the specified IPv6 addresses or Prefix Delegation prefixes from a network interface.

      def unassign_ipv6_addresses(
        network_interface_id : String,
        ipv6_addresses : Array(String)? = nil,
        ipv6_prefixes : Array(String)? = nil
      ) : Types::UnassignIpv6AddressesResult

        input = Types::UnassignIpv6AddressesRequest.new(network_interface_id: network_interface_id, ipv6_addresses: ipv6_addresses, ipv6_prefixes: ipv6_prefixes)
        unassign_ipv6_addresses(input)
      end

      def unassign_ipv6_addresses(input : Types::UnassignIpv6AddressesRequest) : Types::UnassignIpv6AddressesResult
        request = Protocol::Ec2.build_request(Model::UNASSIGN_IPV6_ADDRESSES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::UnassignIpv6AddressesResult, "UnassignIpv6Addresses")
      end

      # Unassigns the specified secondary private IP addresses or IPv4 Prefix Delegation prefixes from a
      # network interface.

      def unassign_private_ip_addresses(
        network_interface_id : String,
        ipv4_prefixes : Array(String)? = nil,
        private_ip_addresses : Array(String)? = nil
      ) : Nil

        input = Types::UnassignPrivateIpAddressesRequest.new(network_interface_id: network_interface_id, ipv4_prefixes: ipv4_prefixes, private_ip_addresses: private_ip_addresses)
        unassign_private_ip_addresses(input)
      end

      def unassign_private_ip_addresses(input : Types::UnassignPrivateIpAddressesRequest) : Nil
        request = Protocol::Ec2.build_request(Model::UNASSIGN_PRIVATE_IP_ADDRESSES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        nil
      end

      # Unassigns secondary private IPv4 addresses from a private NAT gateway. You cannot unassign your
      # primary private IP. For more information, see Edit secondary IP address associations in the Amazon
      # VPC User Guide . While unassigning is in progress, you cannot assign/unassign additional IP
      # addresses while the connections are being drained. You are, however, allowed to delete the NAT
      # gateway. A private IP address will only be released at the end of MaxDrainDurationSeconds. The
      # private IP addresses stay associated and support the existing connections, but do not support any
      # new connections (new connections are distributed across the remaining assigned private IP address).
      # After the existing connections drain out, the private IP addresses are released.

      def unassign_private_nat_gateway_address(
        nat_gateway_id : String,
        private_ip_addresses : Array(String),
        dry_run : Bool? = nil,
        max_drain_duration_seconds : Int32? = nil
      ) : Types::UnassignPrivateNatGatewayAddressResult

        input = Types::UnassignPrivateNatGatewayAddressRequest.new(nat_gateway_id: nat_gateway_id, private_ip_addresses: private_ip_addresses, dry_run: dry_run, max_drain_duration_seconds: max_drain_duration_seconds)
        unassign_private_nat_gateway_address(input)
      end

      def unassign_private_nat_gateway_address(input : Types::UnassignPrivateNatGatewayAddressRequest) : Types::UnassignPrivateNatGatewayAddressResult
        request = Protocol::Ec2.build_request(Model::UNASSIGN_PRIVATE_NAT_GATEWAY_ADDRESS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::UnassignPrivateNatGatewayAddressResult, "UnassignPrivateNatGatewayAddress")
      end

      # Unlocks a snapshot that is locked in governance mode or that is locked in compliance mode but still
      # in the cooling-off period. You can't unlock a snapshot that is locked in compliance mode after the
      # cooling-off period has expired.

      def unlock_snapshot(
        snapshot_id : String,
        dry_run : Bool? = nil
      ) : Types::UnlockSnapshotResult

        input = Types::UnlockSnapshotRequest.new(snapshot_id: snapshot_id, dry_run: dry_run)
        unlock_snapshot(input)
      end

      def unlock_snapshot(input : Types::UnlockSnapshotRequest) : Types::UnlockSnapshotResult
        request = Protocol::Ec2.build_request(Model::UNLOCK_SNAPSHOT, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::UnlockSnapshotResult, "UnlockSnapshot")
      end

      # Disables detailed monitoring for a running instance. For more information, see Monitoring your
      # instances and volumes in the Amazon EC2 User Guide .

      def unmonitor_instances(
        instance_ids : Array(String),
        dry_run : Bool? = nil
      ) : Types::UnmonitorInstancesResult

        input = Types::UnmonitorInstancesRequest.new(instance_ids: instance_ids, dry_run: dry_run)
        unmonitor_instances(input)
      end

      def unmonitor_instances(input : Types::UnmonitorInstancesRequest) : Types::UnmonitorInstancesResult
        request = Protocol::Ec2.build_request(Model::UNMONITOR_INSTANCES, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::UnmonitorInstancesResult, "UnmonitorInstances")
      end

      # Updates the Organizations access setting for EC2 Capacity Manager. This controls whether Capacity
      # Manager can aggregate data from all accounts in your Amazon Web Services Organization or only from
      # the current account.

      def update_capacity_manager_organizations_access(
        organizations_access : Bool,
        client_token : String? = nil,
        dry_run : Bool? = nil
      ) : Types::UpdateCapacityManagerOrganizationsAccessResult

        input = Types::UpdateCapacityManagerOrganizationsAccessRequest.new(organizations_access: organizations_access, client_token: client_token, dry_run: dry_run)
        update_capacity_manager_organizations_access(input)
      end

      def update_capacity_manager_organizations_access(input : Types::UpdateCapacityManagerOrganizationsAccessRequest) : Types::UpdateCapacityManagerOrganizationsAccessResult
        request = Protocol::Ec2.build_request(Model::UPDATE_CAPACITY_MANAGER_ORGANIZATIONS_ACCESS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::UpdateCapacityManagerOrganizationsAccessResult, "UpdateCapacityManagerOrganizationsAccess")
      end

      # Modifies the number of instances allocated to an interruptible reservation, allowing you to add more
      # capacity or reclaim capacity to your source Capacity Reservation.

      def update_interruptible_capacity_reservation_allocation(
        capacity_reservation_id : String,
        target_instance_count : Int32,
        dry_run : Bool? = nil
      ) : Types::UpdateInterruptibleCapacityReservationAllocationResult

        input = Types::UpdateInterruptibleCapacityReservationAllocationRequest.new(capacity_reservation_id: capacity_reservation_id, target_instance_count: target_instance_count, dry_run: dry_run)
        update_interruptible_capacity_reservation_allocation(input)
      end

      def update_interruptible_capacity_reservation_allocation(input : Types::UpdateInterruptibleCapacityReservationAllocationRequest) : Types::UpdateInterruptibleCapacityReservationAllocationResult
        request = Protocol::Ec2.build_request(Model::UPDATE_INTERRUPTIBLE_CAPACITY_RESERVATION_ALLOCATION, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::UpdateInterruptibleCapacityReservationAllocationResult, "UpdateInterruptibleCapacityReservationAllocation")
      end

      # Updates the description of an egress (outbound) security group rule. You can replace an existing
      # description, or add a description to a rule that did not have one previously. You can remove a
      # description for a security group rule by omitting the description parameter in the request.

      def update_security_group_rule_descriptions_egress(
        dry_run : Bool? = nil,
        group_id : String? = nil,
        group_name : String? = nil,
        ip_permissions : Array(Types::IpPermission)? = nil,
        security_group_rule_descriptions : Array(Types::SecurityGroupRuleDescription)? = nil
      ) : Types::UpdateSecurityGroupRuleDescriptionsEgressResult

        input = Types::UpdateSecurityGroupRuleDescriptionsEgressRequest.new(dry_run: dry_run, group_id: group_id, group_name: group_name, ip_permissions: ip_permissions, security_group_rule_descriptions: security_group_rule_descriptions)
        update_security_group_rule_descriptions_egress(input)
      end

      def update_security_group_rule_descriptions_egress(input : Types::UpdateSecurityGroupRuleDescriptionsEgressRequest) : Types::UpdateSecurityGroupRuleDescriptionsEgressResult
        request = Protocol::Ec2.build_request(Model::UPDATE_SECURITY_GROUP_RULE_DESCRIPTIONS_EGRESS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::UpdateSecurityGroupRuleDescriptionsEgressResult, "UpdateSecurityGroupRuleDescriptionsEgress")
      end

      # Updates the description of an ingress (inbound) security group rule. You can replace an existing
      # description, or add a description to a rule that did not have one previously. You can remove a
      # description for a security group rule by omitting the description parameter in the request.

      def update_security_group_rule_descriptions_ingress(
        dry_run : Bool? = nil,
        group_id : String? = nil,
        group_name : String? = nil,
        ip_permissions : Array(Types::IpPermission)? = nil,
        security_group_rule_descriptions : Array(Types::SecurityGroupRuleDescription)? = nil
      ) : Types::UpdateSecurityGroupRuleDescriptionsIngressResult

        input = Types::UpdateSecurityGroupRuleDescriptionsIngressRequest.new(dry_run: dry_run, group_id: group_id, group_name: group_name, ip_permissions: ip_permissions, security_group_rule_descriptions: security_group_rule_descriptions)
        update_security_group_rule_descriptions_ingress(input)
      end

      def update_security_group_rule_descriptions_ingress(input : Types::UpdateSecurityGroupRuleDescriptionsIngressRequest) : Types::UpdateSecurityGroupRuleDescriptionsIngressResult
        request = Protocol::Ec2.build_request(Model::UPDATE_SECURITY_GROUP_RULE_DESCRIPTIONS_INGRESS, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::UpdateSecurityGroupRuleDescriptionsIngressResult, "UpdateSecurityGroupRuleDescriptionsIngress")
      end

      # Stops advertising an address range that is provisioned as an address pool. You can perform this
      # operation at most once every 10 seconds, even if you specify different address ranges each time. It
      # can take a few minutes before traffic to the specified addresses stops routing to Amazon Web
      # Services because of BGP propagation delays.

      def withdraw_byoip_cidr(
        cidr : String,
        dry_run : Bool? = nil
      ) : Types::WithdrawByoipCidrResult

        input = Types::WithdrawByoipCidrRequest.new(cidr: cidr, dry_run: dry_run)
        withdraw_byoip_cidr(input)
      end

      def withdraw_byoip_cidr(input : Types::WithdrawByoipCidrRequest) : Types::WithdrawByoipCidrResult
        request = Protocol::Ec2.build_request(Model::WITHDRAW_BYOIP_CIDR, input, endpoint)
        request = request.with_headers(endpoint_headers)
        response = runtime.execute(request)
        raise Protocol::Ec2.parse_error(response) if response.status >= 300
        Protocol::Ec2.parse_response(response, Types::WithdrawByoipCidrResult, "WithdrawByoipCidr")
      end
    end
  end
end
