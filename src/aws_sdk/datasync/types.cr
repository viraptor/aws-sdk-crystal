require "json"
require "time"

module AwsSdk
  module DataSync
    module Types

      # Represents a single entry in a list (or array) of DataSync agents when you call the ListAgents
      # operation.

      struct AgentListEntry
        include JSON::Serializable

        # The Amazon Resource Name (ARN) of a DataSync agent.

        @[JSON::Field(key: "AgentArn")]
        getter agent_arn : String?

        # The name of an agent.

        @[JSON::Field(key: "Name")]
        getter name : String?

        # The platform-related details about the agent, such as the version number.

        @[JSON::Field(key: "Platform")]
        getter platform : Types::Platform?

        # The status of an agent. If the status is ONLINE , the agent is configured properly and ready to use.
        # If the status is OFFLINE , the agent has been out of contact with DataSync for five minutes or
        # longer. This can happen for a few reasons. For more information, see What do I do if my agent is
        # offline?

        @[JSON::Field(key: "Status")]
        getter status : String?

        def initialize(
          @agent_arn : String? = nil,
          @name : String? = nil,
          @platform : Types::Platform? = nil,
          @status : String? = nil
        )
        end
      end

      # The shared access signature (SAS) configuration that allows DataSync to access your Microsoft Azure
      # Blob Storage. For more information, see SAS tokens for accessing your Azure Blob Storage.

      struct AzureBlobSasConfiguration
        include JSON::Serializable

        # Specifies a SAS token that provides permissions to access your Azure Blob Storage. The token is part
        # of the SAS URI string that comes after the storage resource URI and a question mark. A token looks
        # something like this:
        # sp=r&amp;st=2023-12-20T14:54:52Z&amp;se=2023-12-20T22:54:52Z&amp;spr=https&amp;sv=2021-06-08&amp;sr=c&amp;sig=aBBKDWQvyuVcTPH9EBp%2FXTI9E%2F%2Fmq171%2BZU178wcwqU%3D

        @[JSON::Field(key: "Token")]
        getter token : String

        def initialize(
          @token : String
        )
        end
      end

      # CancelTaskExecutionRequest

      struct CancelTaskExecutionRequest
        include JSON::Serializable

        # The Amazon Resource Name (ARN) of the task execution to stop.

        @[JSON::Field(key: "TaskExecutionArn")]
        getter task_execution_arn : String

        def initialize(
          @task_execution_arn : String
        )
        end
      end


      struct CancelTaskExecutionResponse
        include JSON::Serializable

        def initialize
        end
      end

      # Specifies configuration information for a DataSync-managed secret, such as an authentication token,
      # secret key, password, or Kerberos keytab that DataSync uses to access a specific storage location,
      # with a customer-managed KMS key. You can use either CmkSecretConfig or CustomSecretConfig to provide
      # credentials for a CreateLocation request. Do not provide both parameters for the same request.

      struct CmkSecretConfig
        include JSON::Serializable

        # Specifies the ARN for the customer-managed KMS key that DataSync uses to encrypt the
        # DataSync-managed secret stored for SecretArn . DataSync provides this key to Secrets Manager.

        @[JSON::Field(key: "KmsKeyArn")]
        getter kms_key_arn : String?

        # Specifies the ARN for the DataSync-managed Secrets Manager secret that that is used to access a
        # specific storage location. This property is generated by DataSync and is read-only. DataSync
        # encrypts this secret with the KMS key that you specify for KmsKeyArn .

        @[JSON::Field(key: "SecretArn")]
        getter secret_arn : String?

        def initialize(
          @kms_key_arn : String? = nil,
          @secret_arn : String? = nil
        )
        end
      end

      # CreateAgentRequest

      struct CreateAgentRequest
        include JSON::Serializable

        # Specifies your DataSync agent's activation key. If you don't have an activation key, see Activating
        # your agent .

        @[JSON::Field(key: "ActivationKey")]
        getter activation_key : String

        # Specifies a name for your agent. We recommend specifying a name that you can remember.

        @[JSON::Field(key: "AgentName")]
        getter agent_name : String?

        # Specifies the Amazon Resource Name (ARN) of the security group that allows traffic between your
        # agent and VPC service endpoint. You can only specify one ARN.

        @[JSON::Field(key: "SecurityGroupArns")]
        getter security_group_arns : Array(String)?

        # Specifies the ARN of the subnet where your VPC service endpoint is located. You can only specify one
        # ARN.

        @[JSON::Field(key: "SubnetArns")]
        getter subnet_arns : Array(String)?

        # Specifies labels that help you categorize, filter, and search for your Amazon Web Services
        # resources. We recommend creating at least one tag for your agent.

        @[JSON::Field(key: "Tags")]
        getter tags : Array(Types::TagListEntry)?

        # Specifies the ID of the VPC service endpoint that you're using. For example, a VPC endpoint ID looks
        # like vpce-01234d5aff67890e1 . The VPC service endpoint you use must include the DataSync service
        # name (for example, com.amazonaws.us-east-2.datasync ).

        @[JSON::Field(key: "VpcEndpointId")]
        getter vpc_endpoint_id : String?

        def initialize(
          @activation_key : String,
          @agent_name : String? = nil,
          @security_group_arns : Array(String)? = nil,
          @subnet_arns : Array(String)? = nil,
          @tags : Array(Types::TagListEntry)? = nil,
          @vpc_endpoint_id : String? = nil
        )
        end
      end

      # CreateAgentResponse

      struct CreateAgentResponse
        include JSON::Serializable

        # The ARN of the agent that you just activated. Use the ListAgents operation to return a list of
        # agents in your Amazon Web Services account and Amazon Web Services Region.

        @[JSON::Field(key: "AgentArn")]
        getter agent_arn : String?

        def initialize(
          @agent_arn : String? = nil
        )
        end
      end


      struct CreateLocationAzureBlobRequest
        include JSON::Serializable

        # Specifies the authentication method DataSync uses to access your Azure Blob Storage. DataSync can
        # access blob storage using a shared access signature (SAS).

        @[JSON::Field(key: "AuthenticationType")]
        getter authentication_type : String

        # Specifies the URL of the Azure Blob Storage container involved in your transfer.

        @[JSON::Field(key: "ContainerUrl")]
        getter container_url : String

        # Specifies the access tier that you want your objects or files transferred into. This only applies
        # when using the location as a transfer destination. For more information, see Access tiers .

        @[JSON::Field(key: "AccessTier")]
        getter access_tier : String?

        # (Optional) Specifies the Amazon Resource Name (ARN) of the DataSync agent that can connect with your
        # Azure Blob Storage container. If you are setting up an agentless cross-cloud transfer, you do not
        # need to specify a value for this parameter. You can specify more than one agent. For more
        # information, see Using multiple agents for your transfer . Make sure you configure this parameter
        # correctly when you first create your storage location. You cannot add or remove agents from a
        # storage location after you create it.

        @[JSON::Field(key: "AgentArns")]
        getter agent_arns : Array(String)?

        # Specifies the type of blob that you want your objects or files to be when transferring them into
        # Azure Blob Storage. Currently, DataSync only supports moving data into Azure Blob Storage as block
        # blobs. For more information on blob types, see the Azure Blob Storage documentation .

        @[JSON::Field(key: "BlobType")]
        getter blob_type : String?

        # Specifies configuration information for a DataSync-managed secret, which includes the authentication
        # token that DataSync uses to access a specific AzureBlob storage location, with a customer-managed
        # KMS key. When you include this parameter as part of a CreateLocationAzureBlob request, you provide
        # only the KMS key ARN. DataSync uses this KMS key together with the authentication token you specify
        # for SasConfiguration to create a DataSync-managed secret to store the location access credentials.
        # Make sure that DataSync has permission to access the KMS key that you specify. You can use either
        # CmkSecretConfig (with SasConfiguration ) or CustomSecretConfig (without SasConfiguration ) to
        # provide credentials for a CreateLocationAzureBlob request. Do not provide both parameters for the
        # same request.

        @[JSON::Field(key: "CmkSecretConfig")]
        getter cmk_secret_config : Types::CmkSecretConfig?

        # Specifies configuration information for a customer-managed Secrets Manager secret where the
        # authentication token for an AzureBlob storage location is stored in plain text, in Secrets Manager.
        # This configuration includes the secret ARN, and the ARN for an IAM role that provides access to the
        # secret. You can use either CmkSecretConfig (with SasConfiguration ) or CustomSecretConfig (without
        # SasConfiguration ) to provide credentials for a CreateLocationAzureBlob request. Do not provide both
        # parameters for the same request.

        @[JSON::Field(key: "CustomSecretConfig")]
        getter custom_secret_config : Types::CustomSecretConfig?

        # Specifies the SAS configuration that allows DataSync to access your Azure Blob Storage. If you
        # provide an authentication token using SasConfiguration , but do not provide secret configuration
        # details using CmkSecretConfig or CustomSecretConfig , then DataSync stores the token using your
        # Amazon Web Services account's secrets manager secret.

        @[JSON::Field(key: "SasConfiguration")]
        getter sas_configuration : Types::AzureBlobSasConfiguration?

        # Specifies path segments if you want to limit your transfer to a virtual directory in your container
        # (for example, /my/images ).

        @[JSON::Field(key: "Subdirectory")]
        getter subdirectory : String?

        # Specifies labels that help you categorize, filter, and search for your Amazon Web Services
        # resources. We recommend creating at least a name tag for your transfer location.

        @[JSON::Field(key: "Tags")]
        getter tags : Array(Types::TagListEntry)?

        def initialize(
          @authentication_type : String,
          @container_url : String,
          @access_tier : String? = nil,
          @agent_arns : Array(String)? = nil,
          @blob_type : String? = nil,
          @cmk_secret_config : Types::CmkSecretConfig? = nil,
          @custom_secret_config : Types::CustomSecretConfig? = nil,
          @sas_configuration : Types::AzureBlobSasConfiguration? = nil,
          @subdirectory : String? = nil,
          @tags : Array(Types::TagListEntry)? = nil
        )
        end
      end


      struct CreateLocationAzureBlobResponse
        include JSON::Serializable

        # The ARN of the Azure Blob Storage transfer location that you created.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String?

        def initialize(
          @location_arn : String? = nil
        )
        end
      end

      # CreateLocationEfsRequest

      struct CreateLocationEfsRequest
        include JSON::Serializable

        # Specifies the subnet and security groups DataSync uses to connect to one of your Amazon EFS file
        # system's mount targets .

        @[JSON::Field(key: "Ec2Config")]
        getter ec2_config : Types::Ec2Config

        # Specifies the ARN for your Amazon EFS file system.

        @[JSON::Field(key: "EfsFilesystemArn")]
        getter efs_filesystem_arn : String

        # Specifies the Amazon Resource Name (ARN) of the access point that DataSync uses to mount your Amazon
        # EFS file system. For more information, see Accessing restricted file systems .

        @[JSON::Field(key: "AccessPointArn")]
        getter access_point_arn : String?

        # Specifies an Identity and Access Management (IAM) role that allows DataSync to access your Amazon
        # EFS file system. For information on creating this role, see Creating a DataSync IAM role for file
        # system access .

        @[JSON::Field(key: "FileSystemAccessRoleArn")]
        getter file_system_access_role_arn : String?

        # Specifies whether you want DataSync to use Transport Layer Security (TLS) 1.2 encryption when it
        # transfers data to or from your Amazon EFS file system. If you specify an access point using
        # AccessPointArn or an IAM role using FileSystemAccessRoleArn , you must set this parameter to TLS1_2
        # .

        @[JSON::Field(key: "InTransitEncryption")]
        getter in_transit_encryption : String?

        # Specifies a mount path for your Amazon EFS file system. This is where DataSync reads or writes data
        # on your file system (depending on if this is a source or destination location). By default, DataSync
        # uses the root directory (or access point if you provide one by using AccessPointArn ). You can also
        # include subdirectories using forward slashes (for example, /path/to/folder ).

        @[JSON::Field(key: "Subdirectory")]
        getter subdirectory : String?

        # Specifies the key-value pair that represents a tag that you want to add to the resource. The value
        # can be an empty string. This value helps you manage, filter, and search for your resources. We
        # recommend that you create a name tag for your location.

        @[JSON::Field(key: "Tags")]
        getter tags : Array(Types::TagListEntry)?

        def initialize(
          @ec2_config : Types::Ec2Config,
          @efs_filesystem_arn : String,
          @access_point_arn : String? = nil,
          @file_system_access_role_arn : String? = nil,
          @in_transit_encryption : String? = nil,
          @subdirectory : String? = nil,
          @tags : Array(Types::TagListEntry)? = nil
        )
        end
      end

      # CreateLocationEfs

      struct CreateLocationEfsResponse
        include JSON::Serializable

        # The Amazon Resource Name (ARN) of the Amazon EFS file system location that you create.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String?

        def initialize(
          @location_arn : String? = nil
        )
        end
      end


      struct CreateLocationFsxLustreRequest
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of the FSx for Lustre file system.

        @[JSON::Field(key: "FsxFilesystemArn")]
        getter fsx_filesystem_arn : String

        # Specifies the Amazon Resource Names (ARNs) of up to five security groups that provide access to your
        # FSx for Lustre file system. The security groups must be able to access the file system's ports. The
        # file system must also allow access from the security groups. For information about file system
        # access, see the Amazon FSx for Lustre User Guide .

        @[JSON::Field(key: "SecurityGroupArns")]
        getter security_group_arns : Array(String)

        # Specifies a mount path for your FSx for Lustre file system. The path can include subdirectories.
        # When the location is used as a source, DataSync reads data from the mount path. When the location is
        # used as a destination, DataSync writes data to the mount path. If you don't include this parameter,
        # DataSync uses the file system's root directory ( / ).

        @[JSON::Field(key: "Subdirectory")]
        getter subdirectory : String?

        # Specifies labels that help you categorize, filter, and search for your Amazon Web Services
        # resources. We recommend creating at least a name tag for your location.

        @[JSON::Field(key: "Tags")]
        getter tags : Array(Types::TagListEntry)?

        def initialize(
          @fsx_filesystem_arn : String,
          @security_group_arns : Array(String),
          @subdirectory : String? = nil,
          @tags : Array(Types::TagListEntry)? = nil
        )
        end
      end


      struct CreateLocationFsxLustreResponse
        include JSON::Serializable

        # The Amazon Resource Name (ARN) of the FSx for Lustre file system location that you created.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String?

        def initialize(
          @location_arn : String? = nil
        )
        end
      end


      struct CreateLocationFsxOntapRequest
        include JSON::Serializable


        @[JSON::Field(key: "Protocol")]
        getter protocol : Types::FsxProtocol

        # Specifies the Amazon EC2 security groups that provide access to your file system's preferred subnet.
        # The security groups must allow outbound traffic on the following ports (depending on the protocol
        # you use): Network File System (NFS) : TCP ports 111, 635, and 2049 Server Message Block (SMB) : TCP
        # port 445 Your file system's security groups must also allow inbound traffic on the same ports.

        @[JSON::Field(key: "SecurityGroupArns")]
        getter security_group_arns : Array(String)

        # Specifies the ARN of the storage virtual machine (SVM) in your file system where you want to copy
        # data to or from.

        @[JSON::Field(key: "StorageVirtualMachineArn")]
        getter storage_virtual_machine_arn : String

        # Specifies a path to the file share in the SVM where you want to transfer data to or from. You can
        # specify a junction path (also known as a mount point), qtree path (for NFS file shares), or share
        # name (for SMB file shares). For example, your mount path might be /vol1 , /vol1/tree1 , or /share1 .
        # Don't specify a junction path in the SVM's root volume. For more information, see Managing FSx for
        # ONTAP storage virtual machines in the Amazon FSx for NetApp ONTAP User Guide .

        @[JSON::Field(key: "Subdirectory")]
        getter subdirectory : String?

        # Specifies labels that help you categorize, filter, and search for your Amazon Web Services
        # resources. We recommend creating at least a name tag for your location.

        @[JSON::Field(key: "Tags")]
        getter tags : Array(Types::TagListEntry)?

        def initialize(
          @protocol : Types::FsxProtocol,
          @security_group_arns : Array(String),
          @storage_virtual_machine_arn : String,
          @subdirectory : String? = nil,
          @tags : Array(Types::TagListEntry)? = nil
        )
        end
      end


      struct CreateLocationFsxOntapResponse
        include JSON::Serializable

        # Specifies the ARN of the FSx for ONTAP file system location that you create.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String?

        def initialize(
          @location_arn : String? = nil
        )
        end
      end


      struct CreateLocationFsxOpenZfsRequest
        include JSON::Serializable

        # The Amazon Resource Name (ARN) of the FSx for OpenZFS file system.

        @[JSON::Field(key: "FsxFilesystemArn")]
        getter fsx_filesystem_arn : String

        # The type of protocol that DataSync uses to access your file system.

        @[JSON::Field(key: "Protocol")]
        getter protocol : Types::FsxProtocol

        # The ARNs of the security groups that are used to configure the FSx for OpenZFS file system.

        @[JSON::Field(key: "SecurityGroupArns")]
        getter security_group_arns : Array(String)

        # A subdirectory in the location's path that must begin with /fsx . DataSync uses this subdirectory to
        # read or write data (depending on whether the file system is a source or destination location).

        @[JSON::Field(key: "Subdirectory")]
        getter subdirectory : String?

        # The key-value pair that represents a tag that you want to add to the resource. The value can be an
        # empty string. This value helps you manage, filter, and search for your resources. We recommend that
        # you create a name tag for your location.

        @[JSON::Field(key: "Tags")]
        getter tags : Array(Types::TagListEntry)?

        def initialize(
          @fsx_filesystem_arn : String,
          @protocol : Types::FsxProtocol,
          @security_group_arns : Array(String),
          @subdirectory : String? = nil,
          @tags : Array(Types::TagListEntry)? = nil
        )
        end
      end


      struct CreateLocationFsxOpenZfsResponse
        include JSON::Serializable

        # The ARN of the FSx for OpenZFS file system location that you created.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String?

        def initialize(
          @location_arn : String? = nil
        )
        end
      end


      struct CreateLocationFsxWindowsRequest
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) for the FSx for Windows File Server file system.

        @[JSON::Field(key: "FsxFilesystemArn")]
        getter fsx_filesystem_arn : String

        # Specifies the password of the user with the permissions to mount and access the files, folders, and
        # file metadata in your FSx for Windows File Server file system.

        @[JSON::Field(key: "Password")]
        getter password : String

        # Specifies the ARNs of the Amazon EC2 security groups that provide access to your file system's
        # preferred subnet. The security groups that you specify must be able to communicate with your file
        # system's security groups. For information about configuring security groups for file system access,
        # see the Amazon FSx for Windows File Server User Guide . If you choose a security group that doesn't
        # allow connections from within itself, do one of the following: Configure the security group to allow
        # it to communicate within itself. Choose a different security group that can communicate with the
        # mount target's security group.

        @[JSON::Field(key: "SecurityGroupArns")]
        getter security_group_arns : Array(String)

        # Specifies the user with the permissions to mount and access the files, folders, and file metadata in
        # your FSx for Windows File Server file system. For information about choosing a user with the right
        # level of access for your transfer, see required permissions for FSx for Windows File Server
        # locations.

        @[JSON::Field(key: "User")]
        getter user : String

        # Specifies the name of the Windows domain that the FSx for Windows File Server file system belongs
        # to. If you have multiple Active Directory domains in your environment, configuring this parameter
        # makes sure that DataSync connects to the right file system.

        @[JSON::Field(key: "Domain")]
        getter domain : String?

        # Specifies a mount path for your file system using forward slashes. This is where DataSync reads or
        # writes data (depending on if this is a source or destination location).

        @[JSON::Field(key: "Subdirectory")]
        getter subdirectory : String?

        # Specifies labels that help you categorize, filter, and search for your Amazon Web Services
        # resources. We recommend creating at least a name tag for your location.

        @[JSON::Field(key: "Tags")]
        getter tags : Array(Types::TagListEntry)?

        def initialize(
          @fsx_filesystem_arn : String,
          @password : String,
          @security_group_arns : Array(String),
          @user : String,
          @domain : String? = nil,
          @subdirectory : String? = nil,
          @tags : Array(Types::TagListEntry)? = nil
        )
        end
      end


      struct CreateLocationFsxWindowsResponse
        include JSON::Serializable

        # The ARN of the FSx for Windows File Server file system location you created.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String?

        def initialize(
          @location_arn : String? = nil
        )
        end
      end


      struct CreateLocationHdfsRequest
        include JSON::Serializable

        # The Amazon Resource Names (ARNs) of the DataSync agents that can connect to your HDFS cluster.

        @[JSON::Field(key: "AgentArns")]
        getter agent_arns : Array(String)

        # The type of authentication used to determine the identity of the user.

        @[JSON::Field(key: "AuthenticationType")]
        getter authentication_type : String

        # The NameNode that manages the HDFS namespace. The NameNode performs operations such as opening,
        # closing, and renaming files and directories. The NameNode contains the information to map blocks of
        # data to the DataNodes. You can use only one NameNode.

        @[JSON::Field(key: "NameNodes")]
        getter name_nodes : Array(Types::HdfsNameNode)

        # The size of data blocks to write into the HDFS cluster. The block size must be a multiple of 512
        # bytes. The default block size is 128 mebibytes (MiB).

        @[JSON::Field(key: "BlockSize")]
        getter block_size : Int32?

        # The Kerberos key table (keytab) that contains mappings between the defined Kerberos principal and
        # the encrypted keys. You can load the keytab from a file by providing the file's address. If KERBEROS
        # is specified for AuthenticationType , this parameter is required.

        @[JSON::Field(key: "KerberosKeytab")]
        getter kerberos_keytab : Bytes?

        # The krb5.conf file that contains the Kerberos configuration information. You can load the krb5.conf
        # file by providing the file's address. If you're using the CLI, it performs the base64 encoding for
        # you. Otherwise, provide the base64-encoded text. If KERBEROS is specified for AuthenticationType ,
        # this parameter is required.

        @[JSON::Field(key: "KerberosKrb5Conf")]
        getter kerberos_krb5_conf : Bytes?

        # The Kerberos principal with access to the files and folders on the HDFS cluster. If KERBEROS is
        # specified for AuthenticationType , this parameter is required.

        @[JSON::Field(key: "KerberosPrincipal")]
        getter kerberos_principal : String?

        # The URI of the HDFS cluster's Key Management Server (KMS).

        @[JSON::Field(key: "KmsKeyProviderUri")]
        getter kms_key_provider_uri : String?

        # The Quality of Protection (QOP) configuration specifies the Remote Procedure Call (RPC) and data
        # transfer protection settings configured on the Hadoop Distributed File System (HDFS) cluster. If
        # QopConfiguration isn't specified, RpcProtection and DataTransferProtection default to PRIVACY . If
        # you set RpcProtection or DataTransferProtection , the other parameter assumes the same value.

        @[JSON::Field(key: "QopConfiguration")]
        getter qop_configuration : Types::QopConfiguration?

        # The number of DataNodes to replicate the data to when writing to the HDFS cluster. By default, data
        # is replicated to three DataNodes.

        @[JSON::Field(key: "ReplicationFactor")]
        getter replication_factor : Int32?

        # The user name used to identify the client on the host operating system. If SIMPLE is specified for
        # AuthenticationType , this parameter is required.

        @[JSON::Field(key: "SimpleUser")]
        getter simple_user : String?

        # A subdirectory in the HDFS cluster. This subdirectory is used to read data from or write data to the
        # HDFS cluster. If the subdirectory isn't specified, it will default to / .

        @[JSON::Field(key: "Subdirectory")]
        getter subdirectory : String?

        # The key-value pair that represents the tag that you want to add to the location. The value can be an
        # empty string. We recommend using tags to name your resources.

        @[JSON::Field(key: "Tags")]
        getter tags : Array(Types::TagListEntry)?

        def initialize(
          @agent_arns : Array(String),
          @authentication_type : String,
          @name_nodes : Array(Types::HdfsNameNode),
          @block_size : Int32? = nil,
          @kerberos_keytab : Bytes? = nil,
          @kerberos_krb5_conf : Bytes? = nil,
          @kerberos_principal : String? = nil,
          @kms_key_provider_uri : String? = nil,
          @qop_configuration : Types::QopConfiguration? = nil,
          @replication_factor : Int32? = nil,
          @simple_user : String? = nil,
          @subdirectory : String? = nil,
          @tags : Array(Types::TagListEntry)? = nil
        )
        end
      end


      struct CreateLocationHdfsResponse
        include JSON::Serializable

        # The ARN of the source HDFS cluster location that you create.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String?

        def initialize(
          @location_arn : String? = nil
        )
        end
      end

      # CreateLocationNfsRequest

      struct CreateLocationNfsRequest
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of the DataSync agent that can connect to your NFS file
        # server. You can specify more than one agent. For more information, see Using multiple DataSync
        # agents .

        @[JSON::Field(key: "OnPremConfig")]
        getter on_prem_config : Types::OnPremConfig

        # Specifies the DNS name or IP address (IPv4 or IPv6) of the NFS file server that your DataSync agent
        # connects to.

        @[JSON::Field(key: "ServerHostname")]
        getter server_hostname : String

        # Specifies the export path in your NFS file server that you want DataSync to mount. This path (or a
        # subdirectory of the path) is where DataSync transfers data to or from. For information on
        # configuring an export for DataSync, see Accessing NFS file servers .

        @[JSON::Field(key: "Subdirectory")]
        getter subdirectory : String

        # Specifies the options that DataSync can use to mount your NFS file server.

        @[JSON::Field(key: "MountOptions")]
        getter mount_options : Types::NfsMountOptions?

        # Specifies labels that help you categorize, filter, and search for your Amazon Web Services
        # resources. We recommend creating at least a name tag for your location.

        @[JSON::Field(key: "Tags")]
        getter tags : Array(Types::TagListEntry)?

        def initialize(
          @on_prem_config : Types::OnPremConfig,
          @server_hostname : String,
          @subdirectory : String,
          @mount_options : Types::NfsMountOptions? = nil,
          @tags : Array(Types::TagListEntry)? = nil
        )
        end
      end

      # CreateLocationNfsResponse

      struct CreateLocationNfsResponse
        include JSON::Serializable

        # The ARN of the transfer location that you created for your NFS file server.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String?

        def initialize(
          @location_arn : String? = nil
        )
        end
      end

      # CreateLocationObjectStorageRequest

      struct CreateLocationObjectStorageRequest
        include JSON::Serializable

        # Specifies the name of the object storage bucket involved in the transfer.

        @[JSON::Field(key: "BucketName")]
        getter bucket_name : String

        # Specifies the domain name or IP address (IPv4 or IPv6) of the object storage server that your
        # DataSync agent connects to.

        @[JSON::Field(key: "ServerHostname")]
        getter server_hostname : String

        # Specifies the access key (for example, a user name) if credentials are required to authenticate with
        # the object storage server.

        @[JSON::Field(key: "AccessKey")]
        getter access_key : String?

        # (Optional) Specifies the Amazon Resource Names (ARNs) of the DataSync agents that can connect with
        # your object storage system. If you are setting up an agentless cross-cloud transfer, you do not need
        # to specify a value for this parameter. Make sure you configure this parameter correctly when you
        # first create your storage location. You cannot add or remove agents from a storage location after
        # you create it.

        @[JSON::Field(key: "AgentArns")]
        getter agent_arns : Array(String)?

        # Specifies configuration information for a DataSync-managed secret, which includes the SecretKey that
        # DataSync uses to access a specific object storage location, with a customer-managed KMS key. When
        # you include this parameter as part of a CreateLocationObjectStorage request, you provide only the
        # KMS key ARN. DataSync uses this KMS key together with the value you specify for the SecretKey
        # parameter to create a DataSync-managed secret to store the location access credentials. Make sure
        # that DataSync has permission to access the KMS key that you specify. You can use either
        # CmkSecretConfig (with SecretKey ) or CustomSecretConfig (without SecretKey ) to provide credentials
        # for a CreateLocationObjectStorage request. Do not provide both parameters for the same request.

        @[JSON::Field(key: "CmkSecretConfig")]
        getter cmk_secret_config : Types::CmkSecretConfig?

        # Specifies configuration information for a customer-managed Secrets Manager secret where the secret
        # key for a specific object storage location is stored in plain text, in Secrets Manager. This
        # configuration includes the secret ARN, and the ARN for an IAM role that provides access to the
        # secret. You can use either CmkSecretConfig (with SecretKey ) or CustomSecretConfig (without
        # SecretKey ) to provide credentials for a CreateLocationObjectStorage request. Do not provide both
        # parameters for the same request.

        @[JSON::Field(key: "CustomSecretConfig")]
        getter custom_secret_config : Types::CustomSecretConfig?

        # Specifies the secret key (for example, a password) if credentials are required to authenticate with
        # the object storage server. If you provide a secret using SecretKey , but do not provide secret
        # configuration details using CmkSecretConfig or CustomSecretConfig , then DataSync stores the token
        # using your Amazon Web Services account's Secrets Manager secret.

        @[JSON::Field(key: "SecretKey")]
        getter secret_key : String?

        # Specifies a certificate chain for DataSync to authenticate with your object storage system if the
        # system uses a private or self-signed certificate authority (CA). You must specify a single .pem file
        # with a full certificate chain (for example, file:///home/user/.ssh/object_storage_certificates.pem
        # ). The certificate chain might include: The object storage system's certificate All intermediate
        # certificates (if there are any) The root certificate of the signing CA You can concatenate your
        # certificates into a .pem file (which can be up to 32768 bytes before base64 encoding). The following
        # example cat command creates an object_storage_certificates.pem file that includes three
        # certificates: cat object_server_certificate.pem intermediate_certificate.pem ca_root_certificate.pem
        # &gt; object_storage_certificates.pem To use this parameter, configure ServerProtocol to HTTPS .

        @[JSON::Field(key: "ServerCertificate")]
        getter server_certificate : Bytes?

        # Specifies the port that your object storage server accepts inbound network traffic on (for example,
        # port 443).

        @[JSON::Field(key: "ServerPort")]
        getter server_port : Int32?

        # Specifies the protocol that your object storage server uses to communicate. If not specified, the
        # default value is HTTPS .

        @[JSON::Field(key: "ServerProtocol")]
        getter server_protocol : String?

        # Specifies the object prefix for your object storage server. If this is a source location, DataSync
        # only copies objects with this prefix. If this is a destination location, DataSync writes all objects
        # with this prefix.

        @[JSON::Field(key: "Subdirectory")]
        getter subdirectory : String?

        # Specifies the key-value pair that represents a tag that you want to add to the resource. Tags can
        # help you manage, filter, and search for your resources. We recommend creating a name tag for your
        # location.

        @[JSON::Field(key: "Tags")]
        getter tags : Array(Types::TagListEntry)?

        def initialize(
          @bucket_name : String,
          @server_hostname : String,
          @access_key : String? = nil,
          @agent_arns : Array(String)? = nil,
          @cmk_secret_config : Types::CmkSecretConfig? = nil,
          @custom_secret_config : Types::CustomSecretConfig? = nil,
          @secret_key : String? = nil,
          @server_certificate : Bytes? = nil,
          @server_port : Int32? = nil,
          @server_protocol : String? = nil,
          @subdirectory : String? = nil,
          @tags : Array(Types::TagListEntry)? = nil
        )
        end
      end

      # CreateLocationObjectStorageResponse

      struct CreateLocationObjectStorageResponse
        include JSON::Serializable

        # Specifies the ARN of the object storage system location that you create.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String?

        def initialize(
          @location_arn : String? = nil
        )
        end
      end

      # CreateLocationS3Request

      struct CreateLocationS3Request
        include JSON::Serializable

        # Specifies the ARN of the S3 bucket that you want to use as a location. (When creating your DataSync
        # task later, you specify whether this location is a transfer source or destination.) If your S3
        # bucket is located on an Outposts resource, you must specify an Amazon S3 access point. For more
        # information, see Managing data access with Amazon S3 access points in the Amazon S3 User Guide .

        @[JSON::Field(key: "S3BucketArn")]
        getter s3_bucket_arn : String


        @[JSON::Field(key: "S3Config")]
        getter s3_config : Types::S3Config

        # (Amazon S3 on Outposts only) Specifies the Amazon Resource Name (ARN) of the DataSync agent on your
        # Outpost. For more information, see Deploy your DataSync agent on Outposts .

        @[JSON::Field(key: "AgentArns")]
        getter agent_arns : Array(String)?

        # Specifies the storage class that you want your objects to use when Amazon S3 is a transfer
        # destination. For buckets in Amazon Web Services Regions, the storage class defaults to STANDARD .
        # For buckets on Outposts, the storage class defaults to OUTPOSTS . For more information, see Storage
        # class considerations with Amazon S3 transfers .

        @[JSON::Field(key: "S3StorageClass")]
        getter s3_storage_class : String?

        # Specifies a prefix in the S3 bucket that DataSync reads from or writes to (depending on whether the
        # bucket is a source or destination location). DataSync can't transfer objects with a prefix that
        # begins with a slash ( / ) or includes // , /./ , or /../ patterns. For example: /photos
        # photos//2006/January photos/./2006/February photos/../2006/March

        @[JSON::Field(key: "Subdirectory")]
        getter subdirectory : String?

        # Specifies labels that help you categorize, filter, and search for your Amazon Web Services
        # resources. We recommend creating at least a name tag for your transfer location.

        @[JSON::Field(key: "Tags")]
        getter tags : Array(Types::TagListEntry)?

        def initialize(
          @s3_bucket_arn : String,
          @s3_config : Types::S3Config,
          @agent_arns : Array(String)? = nil,
          @s3_storage_class : String? = nil,
          @subdirectory : String? = nil,
          @tags : Array(Types::TagListEntry)? = nil
        )
        end
      end

      # CreateLocationS3Response

      struct CreateLocationS3Response
        include JSON::Serializable

        # The ARN of the S3 location that you created.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String?

        def initialize(
          @location_arn : String? = nil
        )
        end
      end

      # CreateLocationSmbRequest

      struct CreateLocationSmbRequest
        include JSON::Serializable

        # Specifies the DataSync agent (or agents) that can connect to your SMB file server. You specify an
        # agent by using its Amazon Resource Name (ARN).

        @[JSON::Field(key: "AgentArns")]
        getter agent_arns : Array(String)

        # Specifies the domain name or IP address (IPv4 or IPv6) of the SMB file server that your DataSync
        # agent connects to. If you're using Kerberos authentication, you must specify a domain name.

        @[JSON::Field(key: "ServerHostname")]
        getter server_hostname : String

        # Specifies the name of the share exported by your SMB file server where DataSync will read or write
        # data. You can include a subdirectory in the share path (for example, /path/to/subdirectory ). Make
        # sure that other SMB clients in your network can also mount this path. To copy all data in the
        # subdirectory, DataSync must be able to mount the SMB share and access all of its data. For more
        # information, see Providing DataSync access to SMB file servers .

        @[JSON::Field(key: "Subdirectory")]
        getter subdirectory : String

        # Specifies the authentication protocol that DataSync uses to connect to your SMB file server.
        # DataSync supports NTLM (default) and KERBEROS authentication. For more information, see Providing
        # DataSync access to SMB file servers .

        @[JSON::Field(key: "AuthenticationType")]
        getter authentication_type : String?

        # Specifies configuration information for a DataSync-managed secret, either a Password or
        # KerberosKeytab (for NTLM (default) and KERBEROS authentication types, respectively) that DataSync
        # uses to access a specific SMB storage location, with a customer-managed KMS key. When you include
        # this parameter as part of a CreateLocationSmbRequest request, you provide only the KMS key ARN.
        # DataSync uses this KMS key together with either the Password or KerberosKeytab you specify to create
        # a DataSync-managed secret to store the location access credentials. Make sure that DataSync has
        # permission to access the KMS key that you specify. You can use either CmkSecretConfig (with either
        # Password or KerberosKeytab ) or CustomSecretConfig (without any Password and KerberosKeytab ) to
        # provide credentials for a CreateLocationSmbRequest request. Do not provide both CmkSecretConfig and
        # CustomSecretConfig parameters for the same request.

        @[JSON::Field(key: "CmkSecretConfig")]
        getter cmk_secret_config : Types::CmkSecretConfig?

        # Specifies configuration information for a customer-managed Secrets Manager secret where the SMB
        # storage location credentials is stored in Secrets Manager as plain text (for Password ) or binary
        # (for KerberosKeytab ). This configuration includes the secret ARN, and the ARN for an IAM role that
        # provides access to the secret. You can use either CmkSecretConfig (with SasConfiguration ) or
        # CustomSecretConfig (without SasConfiguration ) to provide credentials for a CreateLocationSmbRequest
        # request. Do not provide both parameters for the same request.

        @[JSON::Field(key: "CustomSecretConfig")]
        getter custom_secret_config : Types::CustomSecretConfig?

        # Specifies the IPv4 or IPv6 addresses for the DNS servers that your SMB file server belongs to. This
        # parameter applies only if AuthenticationType is set to KERBEROS . If you have multiple domains in
        # your environment, configuring this parameter makes sure that DataSync connects to the right SMB file
        # server.

        @[JSON::Field(key: "DnsIpAddresses")]
        getter dns_ip_addresses : Array(String)?

        # Specifies the Windows domain name that your SMB file server belongs to. This parameter applies only
        # if AuthenticationType is set to NTLM . If you have multiple domains in your environment, configuring
        # this parameter makes sure that DataSync connects to the right file server.

        @[JSON::Field(key: "Domain")]
        getter domain : String?

        # Specifies your Kerberos key table (keytab) file, which includes mappings between your Kerberos
        # principal and encryption keys. To avoid task execution errors, make sure that the Kerberos principal
        # that you use to create the keytab file matches exactly what you specify for KerberosPrincipal .

        @[JSON::Field(key: "KerberosKeytab")]
        getter kerberos_keytab : Bytes?

        # Specifies a Kerberos configuration file ( krb5.conf ) that defines your Kerberos realm
        # configuration. The file must be base64 encoded. If you're using the CLI, the encoding is done for
        # you.

        @[JSON::Field(key: "KerberosKrb5Conf")]
        getter kerberos_krb5_conf : Bytes?

        # Specifies a Kerberos principal, which is an identity in your Kerberos realm that has permission to
        # access the files, folders, and file metadata in your SMB file server. A Kerberos principal might
        # look like HOST/kerberosuser@MYDOMAIN.ORG . Principal names are case sensitive. Your DataSync task
        # execution will fail if the principal that you specify for this parameter doesn’t exactly match the
        # principal that you use to create the keytab file.

        @[JSON::Field(key: "KerberosPrincipal")]
        getter kerberos_principal : String?

        # Specifies the version of the SMB protocol that DataSync uses to access your SMB file server.

        @[JSON::Field(key: "MountOptions")]
        getter mount_options : Types::SmbMountOptions?

        # Specifies the password of the user who can mount your SMB file server and has permission to access
        # the files and folders involved in your transfer. This parameter applies only if AuthenticationType
        # is set to NTLM .

        @[JSON::Field(key: "Password")]
        getter password : String?

        # Specifies labels that help you categorize, filter, and search for your Amazon Web Services
        # resources. We recommend creating at least a name tag for your location.

        @[JSON::Field(key: "Tags")]
        getter tags : Array(Types::TagListEntry)?

        # Specifies the user that can mount and access the files, folders, and file metadata in your SMB file
        # server. This parameter applies only if AuthenticationType is set to NTLM . For information about
        # choosing a user with the right level of access for your transfer, see Providing DataSync access to
        # SMB file servers .

        @[JSON::Field(key: "User")]
        getter user : String?

        def initialize(
          @agent_arns : Array(String),
          @server_hostname : String,
          @subdirectory : String,
          @authentication_type : String? = nil,
          @cmk_secret_config : Types::CmkSecretConfig? = nil,
          @custom_secret_config : Types::CustomSecretConfig? = nil,
          @dns_ip_addresses : Array(String)? = nil,
          @domain : String? = nil,
          @kerberos_keytab : Bytes? = nil,
          @kerberos_krb5_conf : Bytes? = nil,
          @kerberos_principal : String? = nil,
          @mount_options : Types::SmbMountOptions? = nil,
          @password : String? = nil,
          @tags : Array(Types::TagListEntry)? = nil,
          @user : String? = nil
        )
        end
      end

      # CreateLocationSmbResponse

      struct CreateLocationSmbResponse
        include JSON::Serializable

        # The ARN of the SMB location that you created.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String?

        def initialize(
          @location_arn : String? = nil
        )
        end
      end

      # CreateTaskRequest

      struct CreateTaskRequest
        include JSON::Serializable

        # Specifies the ARN of your transfer's destination location.

        @[JSON::Field(key: "DestinationLocationArn")]
        getter destination_location_arn : String

        # Specifies the ARN of your transfer's source location.

        @[JSON::Field(key: "SourceLocationArn")]
        getter source_location_arn : String

        # Specifies the Amazon Resource Name (ARN) of an Amazon CloudWatch log group for monitoring your task.
        # For Enhanced mode tasks, you don't need to specify anything. DataSync automatically sends logs to a
        # CloudWatch log group named /aws/datasync .

        @[JSON::Field(key: "CloudWatchLogGroupArn")]
        getter cloud_watch_log_group_arn : String?

        # Specifies exclude filters that define the files, objects, and folders in your source location that
        # you don't want DataSync to transfer. For more information and examples, see Specifying what DataSync
        # transfers by using filters .

        @[JSON::Field(key: "Excludes")]
        getter excludes : Array(Types::FilterRule)?

        # Specifies include filters that define the files, objects, and folders in your source location that
        # you want DataSync to transfer. For more information and examples, see Specifying what DataSync
        # transfers by using filters .

        @[JSON::Field(key: "Includes")]
        getter includes : Array(Types::FilterRule)?

        # Configures a manifest, which is a list of files or objects that you want DataSync to transfer. For
        # more information and configuration examples, see Specifying what DataSync transfers by using a
        # manifest . When using this parameter, your caller identity (the role that you're using DataSync
        # with) must have the iam:PassRole permission. The AWSDataSyncFullAccess policy includes this
        # permission.

        @[JSON::Field(key: "ManifestConfig")]
        getter manifest_config : Types::ManifestConfig?

        # Specifies the name of your task.

        @[JSON::Field(key: "Name")]
        getter name : String?

        # Specifies your task's settings, such as preserving file metadata, verifying data integrity, among
        # other options.

        @[JSON::Field(key: "Options")]
        getter options : Types::Options?

        # Specifies a schedule for when you want your task to run. For more information, see Scheduling your
        # task .

        @[JSON::Field(key: "Schedule")]
        getter schedule : Types::TaskSchedule?

        # Specifies the tags that you want to apply to your task. Tags are key-value pairs that help you
        # manage, filter, and search for your DataSync resources.

        @[JSON::Field(key: "Tags")]
        getter tags : Array(Types::TagListEntry)?

        # Specifies one of the following task modes for your data transfer: ENHANCED - Transfer virtually
        # unlimited numbers of objects with higher performance than Basic mode. Enhanced mode tasks optimize
        # the data transfer process by listing, preparing, transferring, and verifying data in parallel.
        # Enhanced mode is currently available for transfers between Amazon S3 locations, transfers between
        # Azure Blob and Amazon S3 without an agent, and transfers between other clouds and Amazon S3 without
        # an agent. To create an Enhanced mode task, the IAM role that you use to call the CreateTask
        # operation must have the iam:CreateServiceLinkedRole permission. BASIC (default) - Transfer files or
        # objects between Amazon Web Services storage and all other supported DataSync locations. Basic mode
        # tasks are subject to quotas on the number of files, objects, and directories in a dataset. Basic
        # mode sequentially prepares, transfers, and verifies data, making it slower than Enhanced mode for
        # most workloads. For more information, see Understanding task mode differences .

        @[JSON::Field(key: "TaskMode")]
        getter task_mode : String?

        # Specifies how you want to configure a task report, which provides detailed information about your
        # DataSync transfer. For more information, see Monitoring your DataSync transfers with task reports .
        # When using this parameter, your caller identity (the role that you're using DataSync with) must have
        # the iam:PassRole permission. The AWSDataSyncFullAccess policy includes this permission.

        @[JSON::Field(key: "TaskReportConfig")]
        getter task_report_config : Types::TaskReportConfig?

        def initialize(
          @destination_location_arn : String,
          @source_location_arn : String,
          @cloud_watch_log_group_arn : String? = nil,
          @excludes : Array(Types::FilterRule)? = nil,
          @includes : Array(Types::FilterRule)? = nil,
          @manifest_config : Types::ManifestConfig? = nil,
          @name : String? = nil,
          @options : Types::Options? = nil,
          @schedule : Types::TaskSchedule? = nil,
          @tags : Array(Types::TagListEntry)? = nil,
          @task_mode : String? = nil,
          @task_report_config : Types::TaskReportConfig? = nil
        )
        end
      end

      # CreateTaskResponse

      struct CreateTaskResponse
        include JSON::Serializable

        # The Amazon Resource Name (ARN) of the task.

        @[JSON::Field(key: "TaskArn")]
        getter task_arn : String?

        def initialize(
          @task_arn : String? = nil
        )
        end
      end

      # Specifies configuration information for a customer-managed Secrets Manager secret where a storage
      # location credentials is stored in Secrets Manager as plain text (for authentication token, secret
      # key, or password) or as binary (for Kerberos keytab). This configuration includes the secret ARN,
      # and the ARN for an IAM role that provides access to the secret. You can use either CmkSecretConfig
      # or CustomSecretConfig to provide credentials for a CreateLocation request. Do not provide both
      # parameters for the same request.

      struct CustomSecretConfig
        include JSON::Serializable

        # Specifies the ARN for the Identity and Access Management role that DataSync uses to access the
        # secret specified for SecretArn .

        @[JSON::Field(key: "SecretAccessRoleArn")]
        getter secret_access_role_arn : String?

        # Specifies the ARN for an Secrets Manager secret.

        @[JSON::Field(key: "SecretArn")]
        getter secret_arn : String?

        def initialize(
          @secret_access_role_arn : String? = nil,
          @secret_arn : String? = nil
        )
        end
      end

      # DeleteAgentRequest

      struct DeleteAgentRequest
        include JSON::Serializable

        # The Amazon Resource Name (ARN) of the agent to delete. Use the ListAgents operation to return a list
        # of agents for your account and Amazon Web Services Region.

        @[JSON::Field(key: "AgentArn")]
        getter agent_arn : String

        def initialize(
          @agent_arn : String
        )
        end
      end


      struct DeleteAgentResponse
        include JSON::Serializable

        def initialize
        end
      end

      # DeleteLocation

      struct DeleteLocationRequest
        include JSON::Serializable

        # The Amazon Resource Name (ARN) of the location to delete.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String

        def initialize(
          @location_arn : String
        )
        end
      end


      struct DeleteLocationResponse
        include JSON::Serializable

        def initialize
        end
      end

      # DeleteTask

      struct DeleteTaskRequest
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of the task that you want to delete.

        @[JSON::Field(key: "TaskArn")]
        getter task_arn : String

        def initialize(
          @task_arn : String
        )
        end
      end


      struct DeleteTaskResponse
        include JSON::Serializable

        def initialize
        end
      end

      # DescribeAgent

      struct DescribeAgentRequest
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of the DataSync agent that you want information about.

        @[JSON::Field(key: "AgentArn")]
        getter agent_arn : String

        def initialize(
          @agent_arn : String
        )
        end
      end

      # DescribeAgentResponse

      struct DescribeAgentResponse
        include JSON::Serializable

        # The ARN of the agent.

        @[JSON::Field(key: "AgentArn")]
        getter agent_arn : String?

        # The time that the agent was activated .

        @[JSON::Field(key: "CreationTime", converter: AwsSdk::Runtime::UnixTimestampConverter)]
        getter creation_time : Time?

        # The type of service endpoint that your agent is connected to.

        @[JSON::Field(key: "EndpointType")]
        getter endpoint_type : String?

        # The last time that the agent was communicating with the DataSync service.

        @[JSON::Field(key: "LastConnectionTime", converter: AwsSdk::Runtime::UnixTimestampConverter)]
        getter last_connection_time : Time?

        # The name of the agent.

        @[JSON::Field(key: "Name")]
        getter name : String?

        # The platform-related details about the agent, such as the version number.

        @[JSON::Field(key: "Platform")]
        getter platform : Types::Platform?

        # The network configuration that the agent uses when connecting to a VPC service endpoint .

        @[JSON::Field(key: "PrivateLinkConfig")]
        getter private_link_config : Types::PrivateLinkConfig?

        # The status of the agent. If the status is ONLINE , the agent is configured properly and ready to
        # use. If the status is OFFLINE , the agent has been out of contact with DataSync for five minutes or
        # longer. This can happen for a few reasons. For more information, see What do I do if my agent is
        # offline?

        @[JSON::Field(key: "Status")]
        getter status : String?

        def initialize(
          @agent_arn : String? = nil,
          @creation_time : Time? = nil,
          @endpoint_type : String? = nil,
          @last_connection_time : Time? = nil,
          @name : String? = nil,
          @platform : Types::Platform? = nil,
          @private_link_config : Types::PrivateLinkConfig? = nil,
          @status : String? = nil
        )
        end
      end


      struct DescribeLocationAzureBlobRequest
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of your Azure Blob Storage transfer location.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String

        def initialize(
          @location_arn : String
        )
        end
      end


      struct DescribeLocationAzureBlobResponse
        include JSON::Serializable

        # The access tier that you want your objects or files transferred into. This only applies when using
        # the location as a transfer destination. For more information, see Access tiers .

        @[JSON::Field(key: "AccessTier")]
        getter access_tier : String?

        # The ARNs of the DataSync agents that can connect with your Azure Blob Storage container.

        @[JSON::Field(key: "AgentArns")]
        getter agent_arns : Array(String)?

        # The authentication method DataSync uses to access your Azure Blob Storage. DataSync can access blob
        # storage using a shared access signature (SAS).

        @[JSON::Field(key: "AuthenticationType")]
        getter authentication_type : String?

        # The type of blob that you want your objects or files to be when transferring them into Azure Blob
        # Storage. Currently, DataSync only supports moving data into Azure Blob Storage as block blobs. For
        # more information on blob types, see the Azure Blob Storage documentation .

        @[JSON::Field(key: "BlobType")]
        getter blob_type : String?

        # Describes configuration information for a DataSync-managed secret, such as an authentication token
        # that DataSync uses to access a specific storage location, with a customer-managed KMS key.

        @[JSON::Field(key: "CmkSecretConfig")]
        getter cmk_secret_config : Types::CmkSecretConfig?

        # The time that your Azure Blob Storage transfer location was created.

        @[JSON::Field(key: "CreationTime", converter: AwsSdk::Runtime::UnixTimestampConverter)]
        getter creation_time : Time?

        # Describes configuration information for a customer-managed secret, such as an authentication token
        # that DataSync uses to access a specific storage location, with a customer-managed KMS key.

        @[JSON::Field(key: "CustomSecretConfig")]
        getter custom_secret_config : Types::CustomSecretConfig?

        # The ARN of your Azure Blob Storage transfer location.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String?

        # The URL of the Azure Blob Storage container involved in your transfer.

        @[JSON::Field(key: "LocationUri")]
        getter location_uri : String?

        # Describes configuration information for a DataSync-managed secret, such as an authentication token
        # that DataSync uses to access a specific storage location. DataSync uses the default Amazon Web
        # Services-managed KMS key to encrypt this secret in Secrets Manager.

        @[JSON::Field(key: "ManagedSecretConfig")]
        getter managed_secret_config : Types::ManagedSecretConfig?

        def initialize(
          @access_tier : String? = nil,
          @agent_arns : Array(String)? = nil,
          @authentication_type : String? = nil,
          @blob_type : String? = nil,
          @cmk_secret_config : Types::CmkSecretConfig? = nil,
          @creation_time : Time? = nil,
          @custom_secret_config : Types::CustomSecretConfig? = nil,
          @location_arn : String? = nil,
          @location_uri : String? = nil,
          @managed_secret_config : Types::ManagedSecretConfig? = nil
        )
        end
      end

      # DescribeLocationEfsRequest

      struct DescribeLocationEfsRequest
        include JSON::Serializable

        # The Amazon Resource Name (ARN) of the Amazon EFS file system location that you want information
        # about.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String

        def initialize(
          @location_arn : String
        )
        end
      end

      # DescribeLocationEfsResponse

      struct DescribeLocationEfsResponse
        include JSON::Serializable

        # The ARN of the access point that DataSync uses to access the Amazon EFS file system. For more
        # information, see Accessing restricted file systems .

        @[JSON::Field(key: "AccessPointArn")]
        getter access_point_arn : String?

        # The time that the location was created.

        @[JSON::Field(key: "CreationTime", converter: AwsSdk::Runtime::UnixTimestampConverter)]
        getter creation_time : Time?


        @[JSON::Field(key: "Ec2Config")]
        getter ec2_config : Types::Ec2Config?

        # The Identity and Access Management (IAM) role that allows DataSync to access your Amazon EFS file
        # system. For more information, see Creating a DataSync IAM role for file system access .

        @[JSON::Field(key: "FileSystemAccessRoleArn")]
        getter file_system_access_role_arn : String?

        # Indicates whether DataSync uses Transport Layer Security (TLS) encryption when transferring data to
        # or from the Amazon EFS file system.

        @[JSON::Field(key: "InTransitEncryption")]
        getter in_transit_encryption : String?

        # The ARN of the Amazon EFS file system location.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String?

        # The URL of the Amazon EFS file system location.

        @[JSON::Field(key: "LocationUri")]
        getter location_uri : String?

        def initialize(
          @access_point_arn : String? = nil,
          @creation_time : Time? = nil,
          @ec2_config : Types::Ec2Config? = nil,
          @file_system_access_role_arn : String? = nil,
          @in_transit_encryption : String? = nil,
          @location_arn : String? = nil,
          @location_uri : String? = nil
        )
        end
      end


      struct DescribeLocationFsxLustreRequest
        include JSON::Serializable

        # The Amazon Resource Name (ARN) of the FSx for Lustre location to describe.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String

        def initialize(
          @location_arn : String
        )
        end
      end


      struct DescribeLocationFsxLustreResponse
        include JSON::Serializable

        # The time that the FSx for Lustre location was created.

        @[JSON::Field(key: "CreationTime", converter: AwsSdk::Runtime::UnixTimestampConverter)]
        getter creation_time : Time?

        # The Amazon Resource Name (ARN) of the FSx for Lustre location that was described.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String?

        # The URI of the FSx for Lustre location that was described.

        @[JSON::Field(key: "LocationUri")]
        getter location_uri : String?

        # The Amazon Resource Names (ARNs) of the security groups that are configured for the FSx for Lustre
        # file system.

        @[JSON::Field(key: "SecurityGroupArns")]
        getter security_group_arns : Array(String)?

        def initialize(
          @creation_time : Time? = nil,
          @location_arn : String? = nil,
          @location_uri : String? = nil,
          @security_group_arns : Array(String)? = nil
        )
        end
      end


      struct DescribeLocationFsxOntapRequest
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of the FSx for ONTAP file system location that you want
        # information about.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String

        def initialize(
          @location_arn : String
        )
        end
      end


      struct DescribeLocationFsxOntapResponse
        include JSON::Serializable

        # The time that the location was created.

        @[JSON::Field(key: "CreationTime", converter: AwsSdk::Runtime::UnixTimestampConverter)]
        getter creation_time : Time?

        # The ARN of the FSx for ONTAP file system.

        @[JSON::Field(key: "FsxFilesystemArn")]
        getter fsx_filesystem_arn : String?

        # The ARN of the FSx for ONTAP file system location.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String?

        # The uniform resource identifier (URI) of the FSx for ONTAP file system location.

        @[JSON::Field(key: "LocationUri")]
        getter location_uri : String?


        @[JSON::Field(key: "Protocol")]
        getter protocol : Types::FsxProtocol?

        # The security groups that DataSync uses to access your FSx for ONTAP file system.

        @[JSON::Field(key: "SecurityGroupArns")]
        getter security_group_arns : Array(String)?

        # The ARN of the storage virtual machine (SVM) on your FSx for ONTAP file system where you're copying
        # data to or from.

        @[JSON::Field(key: "StorageVirtualMachineArn")]
        getter storage_virtual_machine_arn : String?

        def initialize(
          @creation_time : Time? = nil,
          @fsx_filesystem_arn : String? = nil,
          @location_arn : String? = nil,
          @location_uri : String? = nil,
          @protocol : Types::FsxProtocol? = nil,
          @security_group_arns : Array(String)? = nil,
          @storage_virtual_machine_arn : String? = nil
        )
        end
      end


      struct DescribeLocationFsxOpenZfsRequest
        include JSON::Serializable

        # The Amazon Resource Name (ARN) of the FSx for OpenZFS location to describe.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String

        def initialize(
          @location_arn : String
        )
        end
      end


      struct DescribeLocationFsxOpenZfsResponse
        include JSON::Serializable

        # The time that the FSx for OpenZFS location was created.

        @[JSON::Field(key: "CreationTime", converter: AwsSdk::Runtime::UnixTimestampConverter)]
        getter creation_time : Time?

        # The ARN of the FSx for OpenZFS location that was described.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String?

        # The uniform resource identifier (URI) of the FSx for OpenZFS location that was described. Example:
        # fsxz://us-west-2.fs-1234567890abcdef02/fsx/folderA/folder

        @[JSON::Field(key: "LocationUri")]
        getter location_uri : String?

        # The type of protocol that DataSync uses to access your file system.

        @[JSON::Field(key: "Protocol")]
        getter protocol : Types::FsxProtocol?

        # The ARNs of the security groups that are configured for the FSx for OpenZFS file system.

        @[JSON::Field(key: "SecurityGroupArns")]
        getter security_group_arns : Array(String)?

        def initialize(
          @creation_time : Time? = nil,
          @location_arn : String? = nil,
          @location_uri : String? = nil,
          @protocol : Types::FsxProtocol? = nil,
          @security_group_arns : Array(String)? = nil
        )
        end
      end


      struct DescribeLocationFsxWindowsRequest
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of the FSx for Windows File Server location.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String

        def initialize(
          @location_arn : String
        )
        end
      end


      struct DescribeLocationFsxWindowsResponse
        include JSON::Serializable

        # The time that the FSx for Windows File Server location was created.

        @[JSON::Field(key: "CreationTime", converter: AwsSdk::Runtime::UnixTimestampConverter)]
        getter creation_time : Time?

        # The name of the Microsoft Active Directory domain that the FSx for Windows File Server file system
        # belongs to.

        @[JSON::Field(key: "Domain")]
        getter domain : String?

        # The ARN of the FSx for Windows File Server location.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String?

        # The uniform resource identifier (URI) of the FSx for Windows File Server location.

        @[JSON::Field(key: "LocationUri")]
        getter location_uri : String?

        # The ARNs of the Amazon EC2 security groups that provide access to your file system's preferred
        # subnet. For information about configuring security groups for file system access, see the Amazon FSx
        # for Windows File Server User Guide .

        @[JSON::Field(key: "SecurityGroupArns")]
        getter security_group_arns : Array(String)?

        # The user with the permissions to mount and access the FSx for Windows File Server file system.

        @[JSON::Field(key: "User")]
        getter user : String?

        def initialize(
          @creation_time : Time? = nil,
          @domain : String? = nil,
          @location_arn : String? = nil,
          @location_uri : String? = nil,
          @security_group_arns : Array(String)? = nil,
          @user : String? = nil
        )
        end
      end


      struct DescribeLocationHdfsRequest
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of the HDFS location.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String

        def initialize(
          @location_arn : String
        )
        end
      end


      struct DescribeLocationHdfsResponse
        include JSON::Serializable

        # The ARNs of the DataSync agents that can connect with your HDFS cluster.

        @[JSON::Field(key: "AgentArns")]
        getter agent_arns : Array(String)?

        # The type of authentication used to determine the identity of the user.

        @[JSON::Field(key: "AuthenticationType")]
        getter authentication_type : String?

        # The size of the data blocks to write into the HDFS cluster.

        @[JSON::Field(key: "BlockSize")]
        getter block_size : Int32?

        # The time that the HDFS location was created.

        @[JSON::Field(key: "CreationTime", converter: AwsSdk::Runtime::UnixTimestampConverter)]
        getter creation_time : Time?

        # The Kerberos principal with access to the files and folders on the HDFS cluster. This parameter is
        # used if the AuthenticationType is defined as KERBEROS .

        @[JSON::Field(key: "KerberosPrincipal")]
        getter kerberos_principal : String?

        # The URI of the HDFS cluster's Key Management Server (KMS).

        @[JSON::Field(key: "KmsKeyProviderUri")]
        getter kms_key_provider_uri : String?

        # The ARN of the HDFS location.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String?

        # The URI of the HDFS location.

        @[JSON::Field(key: "LocationUri")]
        getter location_uri : String?

        # The NameNode that manages the HDFS namespace.

        @[JSON::Field(key: "NameNodes")]
        getter name_nodes : Array(Types::HdfsNameNode)?

        # The Quality of Protection (QOP) configuration, which specifies the Remote Procedure Call (RPC) and
        # data transfer protection settings configured on the HDFS cluster.

        @[JSON::Field(key: "QopConfiguration")]
        getter qop_configuration : Types::QopConfiguration?

        # The number of DataNodes to replicate the data to when writing to the HDFS cluster.

        @[JSON::Field(key: "ReplicationFactor")]
        getter replication_factor : Int32?

        # The user name to identify the client on the host operating system. This parameter is used if the
        # AuthenticationType is defined as SIMPLE .

        @[JSON::Field(key: "SimpleUser")]
        getter simple_user : String?

        def initialize(
          @agent_arns : Array(String)? = nil,
          @authentication_type : String? = nil,
          @block_size : Int32? = nil,
          @creation_time : Time? = nil,
          @kerberos_principal : String? = nil,
          @kms_key_provider_uri : String? = nil,
          @location_arn : String? = nil,
          @location_uri : String? = nil,
          @name_nodes : Array(Types::HdfsNameNode)? = nil,
          @qop_configuration : Types::QopConfiguration? = nil,
          @replication_factor : Int32? = nil,
          @simple_user : String? = nil
        )
        end
      end

      # DescribeLocationNfsRequest

      struct DescribeLocationNfsRequest
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of the NFS location that you want information about.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String

        def initialize(
          @location_arn : String
        )
        end
      end

      # DescribeLocationNfsResponse

      struct DescribeLocationNfsResponse
        include JSON::Serializable

        # The time when the NFS location was created.

        @[JSON::Field(key: "CreationTime", converter: AwsSdk::Runtime::UnixTimestampConverter)]
        getter creation_time : Time?

        # The ARN of the NFS location.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String?

        # The URI of the NFS location.

        @[JSON::Field(key: "LocationUri")]
        getter location_uri : String?

        # The mount options that DataSync uses to mount your NFS file server.

        @[JSON::Field(key: "MountOptions")]
        getter mount_options : Types::NfsMountOptions?


        @[JSON::Field(key: "OnPremConfig")]
        getter on_prem_config : Types::OnPremConfig?

        def initialize(
          @creation_time : Time? = nil,
          @location_arn : String? = nil,
          @location_uri : String? = nil,
          @mount_options : Types::NfsMountOptions? = nil,
          @on_prem_config : Types::OnPremConfig? = nil
        )
        end
      end

      # DescribeLocationObjectStorageRequest

      struct DescribeLocationObjectStorageRequest
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of the object storage system location.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String

        def initialize(
          @location_arn : String
        )
        end
      end

      # DescribeLocationObjectStorageResponse

      struct DescribeLocationObjectStorageResponse
        include JSON::Serializable

        # The access key (for example, a user name) required to authenticate with the object storage system.

        @[JSON::Field(key: "AccessKey")]
        getter access_key : String?

        # The ARNs of the DataSync agents that can connect with your object storage system.

        @[JSON::Field(key: "AgentArns")]
        getter agent_arns : Array(String)?

        # Describes configuration information for a DataSync-managed secret, such as an authentication token
        # or set of credentials that DataSync uses to access a specific transfer location, and a
        # customer-managed KMS key.

        @[JSON::Field(key: "CmkSecretConfig")]
        getter cmk_secret_config : Types::CmkSecretConfig?

        # The time that the location was created.

        @[JSON::Field(key: "CreationTime", converter: AwsSdk::Runtime::UnixTimestampConverter)]
        getter creation_time : Time?

        # Describes configuration information for a customer-managed secret, such as an authentication token
        # or set of credentials that DataSync uses to access a specific transfer location, and a
        # customer-managed KMS key.

        @[JSON::Field(key: "CustomSecretConfig")]
        getter custom_secret_config : Types::CustomSecretConfig?

        # The ARN of the object storage system location.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String?

        # The URI of the object storage system location.

        @[JSON::Field(key: "LocationUri")]
        getter location_uri : String?

        # Describes configuration information for a DataSync-managed secret, such as an authentication token
        # or set of credentials that DataSync uses to access a specific transfer location. DataSync uses the
        # default Amazon Web Services-managed KMS key to encrypt this secret in Secrets Manager.

        @[JSON::Field(key: "ManagedSecretConfig")]
        getter managed_secret_config : Types::ManagedSecretConfig?

        # The certificate chain for DataSync to authenticate with your object storage system if the system
        # uses a private or self-signed certificate authority (CA).

        @[JSON::Field(key: "ServerCertificate")]
        getter server_certificate : Bytes?

        # The port that your object storage server accepts inbound network traffic on (for example, port 443).

        @[JSON::Field(key: "ServerPort")]
        getter server_port : Int32?

        # The protocol that your object storage system uses to communicate.

        @[JSON::Field(key: "ServerProtocol")]
        getter server_protocol : String?

        def initialize(
          @access_key : String? = nil,
          @agent_arns : Array(String)? = nil,
          @cmk_secret_config : Types::CmkSecretConfig? = nil,
          @creation_time : Time? = nil,
          @custom_secret_config : Types::CustomSecretConfig? = nil,
          @location_arn : String? = nil,
          @location_uri : String? = nil,
          @managed_secret_config : Types::ManagedSecretConfig? = nil,
          @server_certificate : Bytes? = nil,
          @server_port : Int32? = nil,
          @server_protocol : String? = nil
        )
        end
      end

      # DescribeLocationS3Request

      struct DescribeLocationS3Request
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of the Amazon S3 location.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String

        def initialize(
          @location_arn : String
        )
        end
      end

      # DescribeLocationS3Response

      struct DescribeLocationS3Response
        include JSON::Serializable

        # The ARNs of the DataSync agents deployed on your Outpost when using working with Amazon S3 on
        # Outposts. For more information, see Deploy your DataSync agent on Outposts .

        @[JSON::Field(key: "AgentArns")]
        getter agent_arns : Array(String)?

        # The time that the Amazon S3 location was created.

        @[JSON::Field(key: "CreationTime", converter: AwsSdk::Runtime::UnixTimestampConverter)]
        getter creation_time : Time?

        # The ARN of the Amazon S3 location.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String?

        # The URL of the Amazon S3 location that was described.

        @[JSON::Field(key: "LocationUri")]
        getter location_uri : String?


        @[JSON::Field(key: "S3Config")]
        getter s3_config : Types::S3Config?

        # When Amazon S3 is a destination location, this is the storage class that you chose for your objects.
        # Some storage classes have behaviors that can affect your Amazon S3 storage costs. For more
        # information, see Storage class considerations with Amazon S3 transfers .

        @[JSON::Field(key: "S3StorageClass")]
        getter s3_storage_class : String?

        def initialize(
          @agent_arns : Array(String)? = nil,
          @creation_time : Time? = nil,
          @location_arn : String? = nil,
          @location_uri : String? = nil,
          @s3_config : Types::S3Config? = nil,
          @s3_storage_class : String? = nil
        )
        end
      end

      # DescribeLocationSmbRequest

      struct DescribeLocationSmbRequest
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of the SMB location that you want information about.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String

        def initialize(
          @location_arn : String
        )
        end
      end

      # DescribeLocationSmbResponse

      struct DescribeLocationSmbResponse
        include JSON::Serializable

        # The ARNs of the DataSync agents that can connect with your SMB file server.

        @[JSON::Field(key: "AgentArns")]
        getter agent_arns : Array(String)?

        # The authentication protocol that DataSync uses to connect to your SMB file server.

        @[JSON::Field(key: "AuthenticationType")]
        getter authentication_type : String?

        # Describes configuration information for a DataSync-managed secret, such as a Password or
        # KerberosKeytab that DataSync uses to access a specific storage location, with a customer-managed KMS
        # key.

        @[JSON::Field(key: "CmkSecretConfig")]
        getter cmk_secret_config : Types::CmkSecretConfig?

        # The time that the SMB location was created.

        @[JSON::Field(key: "CreationTime", converter: AwsSdk::Runtime::UnixTimestampConverter)]
        getter creation_time : Time?

        # Describes configuration information for a customer-managed secret, such as a Password or
        # KerberosKeytab that DataSync uses to access a specific storage location, with a customer-managed KMS
        # key.

        @[JSON::Field(key: "CustomSecretConfig")]
        getter custom_secret_config : Types::CustomSecretConfig?

        # The IPv4 or IPv6 addresses for the DNS servers that your SMB file server belongs to. This element
        # applies only if AuthenticationType is set to KERBEROS .

        @[JSON::Field(key: "DnsIpAddresses")]
        getter dns_ip_addresses : Array(String)?

        # The name of the Windows domain that the SMB file server belongs to. This element applies only if
        # AuthenticationType is set to NTLM .

        @[JSON::Field(key: "Domain")]
        getter domain : String?

        # The Kerberos principal that has permission to access the files, folders, and file metadata in your
        # SMB file server.

        @[JSON::Field(key: "KerberosPrincipal")]
        getter kerberos_principal : String?

        # The ARN of the SMB location.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String?

        # The URI of the SMB location.

        @[JSON::Field(key: "LocationUri")]
        getter location_uri : String?

        # Describes configuration information for a DataSync-managed secret, such as a Password or
        # KerberosKeytab that DataSync uses to access a specific storage location. DataSync uses the default
        # Amazon Web Services-managed KMS key to encrypt this secret in Secrets Manager.

        @[JSON::Field(key: "ManagedSecretConfig")]
        getter managed_secret_config : Types::ManagedSecretConfig?

        # The SMB protocol version that DataSync uses to access your SMB file server.

        @[JSON::Field(key: "MountOptions")]
        getter mount_options : Types::SmbMountOptions?

        # The user that can mount and access the files, folders, and file metadata in your SMB file server.
        # This element applies only if AuthenticationType is set to NTLM .

        @[JSON::Field(key: "User")]
        getter user : String?

        def initialize(
          @agent_arns : Array(String)? = nil,
          @authentication_type : String? = nil,
          @cmk_secret_config : Types::CmkSecretConfig? = nil,
          @creation_time : Time? = nil,
          @custom_secret_config : Types::CustomSecretConfig? = nil,
          @dns_ip_addresses : Array(String)? = nil,
          @domain : String? = nil,
          @kerberos_principal : String? = nil,
          @location_arn : String? = nil,
          @location_uri : String? = nil,
          @managed_secret_config : Types::ManagedSecretConfig? = nil,
          @mount_options : Types::SmbMountOptions? = nil,
          @user : String? = nil
        )
        end
      end

      # DescribeTaskExecutionRequest

      struct DescribeTaskExecutionRequest
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of the task execution that you want information about.

        @[JSON::Field(key: "TaskExecutionArn")]
        getter task_execution_arn : String

        def initialize(
          @task_execution_arn : String
        )
        end
      end

      # DescribeTaskExecutionResponse

      struct DescribeTaskExecutionResponse
        include JSON::Serializable

        # The number of physical bytes that DataSync transfers over the network after compression (if
        # compression is possible). This number is typically less than BytesTransferred unless the data isn't
        # compressible.

        @[JSON::Field(key: "BytesCompressed")]
        getter bytes_compressed : Int64?

        # The number of bytes that DataSync sends to the network before compression (if compression is
        # possible). For the number of bytes transferred over the network, see BytesCompressed .

        @[JSON::Field(key: "BytesTransferred")]
        getter bytes_transferred : Int64?

        # The number of logical bytes that DataSync actually writes to the destination location.

        @[JSON::Field(key: "BytesWritten")]
        getter bytes_written : Int64?

        # The time that the transfer task ends.

        @[JSON::Field(key: "EndTime", converter: AwsSdk::Runtime::UnixTimestampConverter)]
        getter end_time : Time?

        # The number of logical bytes that DataSync expects to write to the destination location.

        @[JSON::Field(key: "EstimatedBytesToTransfer")]
        getter estimated_bytes_to_transfer : Int64?

        # The number of files, objects, and directories that DataSync expects to delete in your destination
        # location. If you don't configure your task to delete data in the destination that isn't in the
        # source , the value is always 0 . For Enhanced mode tasks , this counter only includes files or
        # objects. Directories are counted in EstimatedFoldersToDelete .

        @[JSON::Field(key: "EstimatedFilesToDelete")]
        getter estimated_files_to_delete : Int64?

        # The number of files, objects, and directories that DataSync expects to transfer over the network.
        # This value is calculated while DataSync prepares the transfer. How this gets calculated depends
        # primarily on your task’s transfer mode configuration: If TranserMode is set to CHANGED - The
        # calculation is based on comparing the content of the source and destination locations and
        # determining the difference that needs to be transferred. The difference can include: Anything that's
        # added or modified at the source location. Anything that's in both locations and modified at the
        # destination after an initial transfer (unless OverwriteMode is set to NEVER ). (Basic task mode
        # only) The number of items that DataSync expects to delete (if PreserveDeletedFiles is set to REMOVE
        # ). If TranserMode is set to ALL - The calculation is based only on the items that DataSync finds at
        # the source location. For Enhanced mode tasks , this counter only includes files or objects.
        # Directories are counted in EstimatedFoldersToTransfer .

        @[JSON::Field(key: "EstimatedFilesToTransfer")]
        getter estimated_files_to_transfer : Int64?

        # The number of directories that DataSync expects to delete in your destination location. If you don't
        # configure your task to delete data in the destination that isn't in the source , the value is always
        # 0 . Applies only to Enhanced mode tasks .

        @[JSON::Field(key: "EstimatedFoldersToDelete")]
        getter estimated_folders_to_delete : Int64?

        # The number of directories that DataSync expects to transfer over the network. This value is
        # calculated as DataSync prepares directories to transfer. How this gets calculated depends primarily
        # on your task’s transfer mode configuration: If TranserMode is set to CHANGED - The calculation is
        # based on comparing the content of the source and destination locations and determining the
        # difference that needs to be transferred. The difference can include: Anything that's added or
        # modified at the source location. Anything that's in both locations and modified at the destination
        # after an initial transfer (unless OverwriteMode is set to NEVER ). If TranserMode is set to ALL -
        # The calculation is based only on the items that DataSync finds at the source location. Applies only
        # to Enhanced mode tasks .

        @[JSON::Field(key: "EstimatedFoldersToTransfer")]
        getter estimated_folders_to_transfer : Int64?

        # A list of filter rules that exclude specific data during your transfer. For more information and
        # examples, see Filtering data transferred by DataSync .

        @[JSON::Field(key: "Excludes")]
        getter excludes : Array(Types::FilterRule)?

        # The number of files, objects, and directories that DataSync actually deletes in your destination
        # location. If you don't configure your task to delete data in the destination that isn't in the
        # source , the value is always 0 . For Enhanced mode tasks , this counter only includes files or
        # objects. Directories are counted in FoldersDeleted .

        @[JSON::Field(key: "FilesDeleted")]
        getter files_deleted : Int64?

        # The number of files or objects that DataSync fails to prepare, transfer, verify, and delete during
        # your task execution. Applies only to Enhanced mode tasks .

        @[JSON::Field(key: "FilesFailed")]
        getter files_failed : Types::TaskExecutionFilesFailedDetail?

        # The number of files or objects that DataSync finds at your locations. Applies only to Enhanced mode
        # tasks .

        @[JSON::Field(key: "FilesListed")]
        getter files_listed : Types::TaskExecutionFilesListedDetail?

        # The number of files or objects that DataSync will attempt to transfer after comparing your source
        # and destination locations. Applies only to Enhanced mode tasks . This counter isn't applicable if
        # you configure your task to transfer all data . In that scenario, DataSync copies everything from the
        # source to the destination without comparing differences between the locations.

        @[JSON::Field(key: "FilesPrepared")]
        getter files_prepared : Int64?

        # The number of files, objects, and directories that DataSync skips during your transfer. For Enhanced
        # mode tasks , this counter only includes files or objects. Directories are counted in FoldersSkipped
        # .

        @[JSON::Field(key: "FilesSkipped")]
        getter files_skipped : Int64?

        # The number of files, objects, and directories that DataSync actually transfers over the network.
        # This value is updated periodically during your task execution when something is read from the source
        # and sent over the network. If DataSync fails to transfer something, this value can be less than
        # EstimatedFilesToTransfer . In some cases, this value can also be greater than
        # EstimatedFilesToTransfer . This element is implementation-specific for some location types, so don't
        # use it as an exact indication of what's transferring or to monitor your task execution. For Enhanced
        # mode tasks , this counter only includes files or objects. Directories are counted in
        # FoldersTransferred .

        @[JSON::Field(key: "FilesTransferred")]
        getter files_transferred : Int64?

        # The number of files, objects, and directories that DataSync verifies during your transfer. When you
        # configure your task to verify only the data that's transferred , DataSync doesn't verify directories
        # in some situations or files that fail to transfer. For Enhanced mode tasks , this counter only
        # includes files or objects. Directories are counted in FoldersVerified .

        @[JSON::Field(key: "FilesVerified")]
        getter files_verified : Int64?

        # The number of directories that DataSync actually deletes in your destination location. If you don't
        # configure your task to delete data in the destination that isn't in the source , the value is always
        # 0 . Applies only to Enhanced mode tasks .

        @[JSON::Field(key: "FoldersDeleted")]
        getter folders_deleted : Int64?

        # The number of directories that DataSync fails to list, prepare, transfer, verify, and delete during
        # your task execution. Applies only to Enhanced mode tasks .

        @[JSON::Field(key: "FoldersFailed")]
        getter folders_failed : Types::TaskExecutionFoldersFailedDetail?

        # The number of directories that DataSync finds at your locations. Applies only to Enhanced mode tasks
        # .

        @[JSON::Field(key: "FoldersListed")]
        getter folders_listed : Types::TaskExecutionFoldersListedDetail?

        # The number of directories that DataSync will attempt to transfer after comparing your source and
        # destination locations. Applies only to Enhanced mode tasks . This counter isn't applicable if you
        # configure your task to transfer all data . In that scenario, DataSync copies everything from the
        # source to the destination without comparing differences between the locations.

        @[JSON::Field(key: "FoldersPrepared")]
        getter folders_prepared : Int64?

        # The number of directories that DataSync skips during your transfer. Applies only to Enhanced mode
        # tasks .

        @[JSON::Field(key: "FoldersSkipped")]
        getter folders_skipped : Int64?

        # The number of directories that DataSync actually transfers over the network. This value is updated
        # periodically during your task execution when something is read from the source and sent over the
        # network. If DataSync fails to transfer something, this value can be less than
        # EstimatedFoldersToTransfer . In some cases, this value can also be greater than
        # EstimatedFoldersToTransfer . Applies only to Enhanced mode tasks .

        @[JSON::Field(key: "FoldersTransferred")]
        getter folders_transferred : Int64?

        # The number of directories that DataSync verifies during your transfer. Applies only to Enhanced mode
        # tasks .

        @[JSON::Field(key: "FoldersVerified")]
        getter folders_verified : Int64?

        # A list of filter rules that include specific data during your transfer. For more information and
        # examples, see Filtering data transferred by DataSync .

        @[JSON::Field(key: "Includes")]
        getter includes : Array(Types::FilterRule)?

        # The time that the task execution actually begins. For non-queued tasks, LaunchTime and StartTime are
        # typically the same. For queued tasks, LaunchTime is typically later than StartTime because
        # previously queued tasks must finish running before newer tasks can begin.

        @[JSON::Field(key: "LaunchTime", converter: AwsSdk::Runtime::UnixTimestampConverter)]
        getter launch_time : Time?

        # The configuration of the manifest that lists the files or objects to transfer. For more information,
        # see Specifying what DataSync transfers by using a manifest .

        @[JSON::Field(key: "ManifestConfig")]
        getter manifest_config : Types::ManifestConfig?


        @[JSON::Field(key: "Options")]
        getter options : Types::Options?

        # Indicates whether DataSync generated a complete task report for your transfer.

        @[JSON::Field(key: "ReportResult")]
        getter report_result : Types::ReportResult?

        # The result of the task execution.

        @[JSON::Field(key: "Result")]
        getter result : Types::TaskExecutionResultDetail?

        # The time that DataSync sends the request to start the task execution. For non-queued tasks,
        # LaunchTime and StartTime are typically the same. For queued tasks, LaunchTime is typically later
        # than StartTime because previously queued tasks must finish running before newer tasks can begin.

        @[JSON::Field(key: "StartTime", converter: AwsSdk::Runtime::UnixTimestampConverter)]
        getter start_time : Time?

        # The status of the task execution.

        @[JSON::Field(key: "Status")]
        getter status : String?

        # The ARN of the task execution that you wanted information about. TaskExecutionArn is hierarchical
        # and includes TaskArn for the task that was executed. For example, a TaskExecution value with the ARN
        # arn:aws:datasync:us-east-1:111222333444:task/task-0208075f79cedf4a2/execution/exec-08ef1e88ec491019b
        # executed the task with the ARN arn:aws:datasync:us-east-1:111222333444:task/task-0208075f79cedf4a2 .

        @[JSON::Field(key: "TaskExecutionArn")]
        getter task_execution_arn : String?

        # The task mode that you're using. For more information, see Choosing a task mode for your data
        # transfer .

        @[JSON::Field(key: "TaskMode")]
        getter task_mode : String?

        # The configuration of your task report, which provides detailed information about for your DataSync
        # transfer. For more information, see Creating a task report .

        @[JSON::Field(key: "TaskReportConfig")]
        getter task_report_config : Types::TaskReportConfig?

        def initialize(
          @bytes_compressed : Int64? = nil,
          @bytes_transferred : Int64? = nil,
          @bytes_written : Int64? = nil,
          @end_time : Time? = nil,
          @estimated_bytes_to_transfer : Int64? = nil,
          @estimated_files_to_delete : Int64? = nil,
          @estimated_files_to_transfer : Int64? = nil,
          @estimated_folders_to_delete : Int64? = nil,
          @estimated_folders_to_transfer : Int64? = nil,
          @excludes : Array(Types::FilterRule)? = nil,
          @files_deleted : Int64? = nil,
          @files_failed : Types::TaskExecutionFilesFailedDetail? = nil,
          @files_listed : Types::TaskExecutionFilesListedDetail? = nil,
          @files_prepared : Int64? = nil,
          @files_skipped : Int64? = nil,
          @files_transferred : Int64? = nil,
          @files_verified : Int64? = nil,
          @folders_deleted : Int64? = nil,
          @folders_failed : Types::TaskExecutionFoldersFailedDetail? = nil,
          @folders_listed : Types::TaskExecutionFoldersListedDetail? = nil,
          @folders_prepared : Int64? = nil,
          @folders_skipped : Int64? = nil,
          @folders_transferred : Int64? = nil,
          @folders_verified : Int64? = nil,
          @includes : Array(Types::FilterRule)? = nil,
          @launch_time : Time? = nil,
          @manifest_config : Types::ManifestConfig? = nil,
          @options : Types::Options? = nil,
          @report_result : Types::ReportResult? = nil,
          @result : Types::TaskExecutionResultDetail? = nil,
          @start_time : Time? = nil,
          @status : String? = nil,
          @task_execution_arn : String? = nil,
          @task_mode : String? = nil,
          @task_report_config : Types::TaskReportConfig? = nil
        )
        end
      end

      # DescribeTaskRequest

      struct DescribeTaskRequest
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of the transfer task that you want information about.

        @[JSON::Field(key: "TaskArn")]
        getter task_arn : String

        def initialize(
          @task_arn : String
        )
        end
      end

      # DescribeTaskResponse

      struct DescribeTaskResponse
        include JSON::Serializable

        # The Amazon Resource Name (ARN) of an Amazon CloudWatch log group for monitoring your task. For more
        # information, see Monitoring data transfers with CloudWatch Logs .

        @[JSON::Field(key: "CloudWatchLogGroupArn")]
        getter cloud_watch_log_group_arn : String?

        # The time that the task was created.

        @[JSON::Field(key: "CreationTime", converter: AwsSdk::Runtime::UnixTimestampConverter)]
        getter creation_time : Time?

        # The ARN of the most recent task execution.

        @[JSON::Field(key: "CurrentTaskExecutionArn")]
        getter current_task_execution_arn : String?

        # The ARN of your transfer's destination location.

        @[JSON::Field(key: "DestinationLocationArn")]
        getter destination_location_arn : String?

        # The ARNs of the network interfaces that DataSync created for your destination location.

        @[JSON::Field(key: "DestinationNetworkInterfaceArns")]
        getter destination_network_interface_arns : Array(String)?

        # If there's an issue with your task, you can use the error code to help you troubleshoot the problem.
        # For more information, see Troubleshooting issues with DataSync transfers .

        @[JSON::Field(key: "ErrorCode")]
        getter error_code : String?

        # If there's an issue with your task, you can use the error details to help you troubleshoot the
        # problem. For more information, see Troubleshooting issues with DataSync transfers .

        @[JSON::Field(key: "ErrorDetail")]
        getter error_detail : String?

        # The exclude filters that define the files, objects, and folders in your source location that you
        # don't want DataSync to transfer. For more information and examples, see Specifying what DataSync
        # transfers by using filters .

        @[JSON::Field(key: "Excludes")]
        getter excludes : Array(Types::FilterRule)?

        # The include filters that define the files, objects, and folders in your source location that you
        # want DataSync to transfer. For more information and examples, see Specifying what DataSync transfers
        # by using filters .

        @[JSON::Field(key: "Includes")]
        getter includes : Array(Types::FilterRule)?

        # The configuration of the manifest that lists the files or objects that you want DataSync to
        # transfer. For more information, see Specifying what DataSync transfers by using a manifest .

        @[JSON::Field(key: "ManifestConfig")]
        getter manifest_config : Types::ManifestConfig?

        # The name of your task.

        @[JSON::Field(key: "Name")]
        getter name : String?

        # The task's settings. For example, what file metadata gets preserved, how data integrity gets
        # verified at the end of your transfer, bandwidth limits, among other options.

        @[JSON::Field(key: "Options")]
        getter options : Types::Options?

        # The schedule for when you want your task to run. For more information, see Scheduling your task .

        @[JSON::Field(key: "Schedule")]
        getter schedule : Types::TaskSchedule?

        # The details about your task schedule .

        @[JSON::Field(key: "ScheduleDetails")]
        getter schedule_details : Types::TaskScheduleDetails?

        # The ARN of your transfer's source location.

        @[JSON::Field(key: "SourceLocationArn")]
        getter source_location_arn : String?

        # The ARNs of the network interfaces that DataSync created for your source location.

        @[JSON::Field(key: "SourceNetworkInterfaceArns")]
        getter source_network_interface_arns : Array(String)?

        # The status of your task. For information about what each status means, see Task statuses .

        @[JSON::Field(key: "Status")]
        getter status : String?

        # The ARN of your task.

        @[JSON::Field(key: "TaskArn")]
        getter task_arn : String?

        # The task mode that you're using. For more information, see Choosing a task mode for your data
        # transfer .

        @[JSON::Field(key: "TaskMode")]
        getter task_mode : String?

        # The configuration of your task report, which provides detailed information about your DataSync
        # transfer. For more information, see Monitoring your DataSync transfers with task reports .

        @[JSON::Field(key: "TaskReportConfig")]
        getter task_report_config : Types::TaskReportConfig?

        def initialize(
          @cloud_watch_log_group_arn : String? = nil,
          @creation_time : Time? = nil,
          @current_task_execution_arn : String? = nil,
          @destination_location_arn : String? = nil,
          @destination_network_interface_arns : Array(String)? = nil,
          @error_code : String? = nil,
          @error_detail : String? = nil,
          @excludes : Array(Types::FilterRule)? = nil,
          @includes : Array(Types::FilterRule)? = nil,
          @manifest_config : Types::ManifestConfig? = nil,
          @name : String? = nil,
          @options : Types::Options? = nil,
          @schedule : Types::TaskSchedule? = nil,
          @schedule_details : Types::TaskScheduleDetails? = nil,
          @source_location_arn : String? = nil,
          @source_network_interface_arns : Array(String)? = nil,
          @status : String? = nil,
          @task_arn : String? = nil,
          @task_mode : String? = nil,
          @task_report_config : Types::TaskReportConfig? = nil
        )
        end
      end

      # The subnet and security groups that DataSync uses to connect to one of your Amazon EFS file system's
      # mount targets .

      struct Ec2Config
        include JSON::Serializable

        # Specifies the Amazon Resource Names (ARNs) of the security groups associated with an Amazon EFS file
        # system's mount target.

        @[JSON::Field(key: "SecurityGroupArns")]
        getter security_group_arns : Array(String)

        # Specifies the ARN of a subnet where DataSync creates the network interfaces for managing traffic
        # during your transfer. The subnet must be located: In the same virtual private cloud (VPC) as the
        # Amazon EFS file system. In the same Availability Zone as at least one mount target for the Amazon
        # EFS file system. You don't need to specify a subnet that includes a file system mount target.

        @[JSON::Field(key: "SubnetArn")]
        getter subnet_arn : String

        def initialize(
          @security_group_arns : Array(String),
          @subnet_arn : String
        )
        end
      end

      # Specifies which files, folders, and objects to include or exclude when transferring files from
      # source to destination.

      struct FilterRule
        include JSON::Serializable

        # The type of filter rule to apply. DataSync only supports the SIMPLE_PATTERN rule type.

        @[JSON::Field(key: "FilterType")]
        getter filter_type : String?

        # A single filter string that consists of the patterns to include or exclude. The patterns are
        # delimited by "|" (that is, a pipe), for example: /folder1|/folder2

        @[JSON::Field(key: "Value")]
        getter value : String?

        def initialize(
          @filter_type : String? = nil,
          @value : String? = nil
        )
        end
      end

      # Specifies the data transfer protocol that DataSync uses to access your Amazon FSx file system.

      struct FsxProtocol
        include JSON::Serializable

        # Specifies the Network File System (NFS) protocol configuration that DataSync uses to access your FSx
        # for OpenZFS file system or FSx for ONTAP file system's storage virtual machine (SVM).

        @[JSON::Field(key: "NFS")]
        getter nfs : Types::FsxProtocolNfs?

        # Specifies the Server Message Block (SMB) protocol configuration that DataSync uses to access your
        # FSx for ONTAP file system's SVM.

        @[JSON::Field(key: "SMB")]
        getter smb : Types::FsxProtocolSmb?

        def initialize(
          @nfs : Types::FsxProtocolNfs? = nil,
          @smb : Types::FsxProtocolSmb? = nil
        )
        end
      end

      # Specifies the Network File System (NFS) protocol configuration that DataSync uses to access your FSx
      # for OpenZFS file system or FSx for ONTAP file system's storage virtual machine (SVM).

      struct FsxProtocolNfs
        include JSON::Serializable


        @[JSON::Field(key: "MountOptions")]
        getter mount_options : Types::NfsMountOptions?

        def initialize(
          @mount_options : Types::NfsMountOptions? = nil
        )
        end
      end

      # Specifies the Server Message Block (SMB) protocol configuration that DataSync uses to access your
      # Amazon FSx for NetApp ONTAP file system's storage virtual machine (SVM). For more information, see
      # Providing DataSync access to FSx for ONTAP file systems .

      struct FsxProtocolSmb
        include JSON::Serializable

        # Specifies the password of a user who has permission to access your SVM.

        @[JSON::Field(key: "Password")]
        getter password : String

        # Specifies a user that can mount and access the files, folders, and metadata in your SVM. For
        # information about choosing a user with the right level of access for your transfer, see Using the
        # SMB protocol .

        @[JSON::Field(key: "User")]
        getter user : String

        # Specifies the name of the Windows domain that your storage virtual machine (SVM) belongs to. If you
        # have multiple domains in your environment, configuring this setting makes sure that DataSync
        # connects to the right SVM. If you have multiple Active Directory domains in your environment,
        # configuring this parameter makes sure that DataSync connects to the right SVM.

        @[JSON::Field(key: "Domain")]
        getter domain : String?


        @[JSON::Field(key: "MountOptions")]
        getter mount_options : Types::SmbMountOptions?

        def initialize(
          @password : String,
          @user : String,
          @domain : String? = nil,
          @mount_options : Types::SmbMountOptions? = nil
        )
        end
      end

      # Specifies the data transfer protocol that DataSync uses to access your Amazon FSx file system. You
      # can't update the Network File System (NFS) protocol configuration for FSx for ONTAP locations.
      # DataSync currently only supports NFS version 3 with this location type.

      struct FsxUpdateProtocol
        include JSON::Serializable


        @[JSON::Field(key: "NFS")]
        getter nfs : Types::FsxProtocolNfs?

        # Specifies the Server Message Block (SMB) protocol configuration that DataSync uses to access your
        # FSx for ONTAP file system's storage virtual machine (SVM).

        @[JSON::Field(key: "SMB")]
        getter smb : Types::FsxUpdateProtocolSmb?

        def initialize(
          @nfs : Types::FsxProtocolNfs? = nil,
          @smb : Types::FsxUpdateProtocolSmb? = nil
        )
        end
      end

      # Specifies the Server Message Block (SMB) protocol configuration that DataSync uses to access your
      # Amazon FSx for NetApp ONTAP file system's storage virtual machine (SVM). For more information, see
      # Providing DataSync access to FSx for ONTAP file systems .

      struct FsxUpdateProtocolSmb
        include JSON::Serializable

        # Specifies the name of the Windows domain that your storage virtual machine (SVM) belongs to. If you
        # have multiple Active Directory domains in your environment, configuring this parameter makes sure
        # that DataSync connects to the right SVM.

        @[JSON::Field(key: "Domain")]
        getter domain : String?


        @[JSON::Field(key: "MountOptions")]
        getter mount_options : Types::SmbMountOptions?

        # Specifies the password of a user who has permission to access your SVM.

        @[JSON::Field(key: "Password")]
        getter password : String?

        # Specifies a user that can mount and access the files, folders, and metadata in your SVM. For
        # information about choosing a user with the right level of access for your transfer, see Using the
        # SMB protocol .

        @[JSON::Field(key: "User")]
        getter user : String?

        def initialize(
          @domain : String? = nil,
          @mount_options : Types::SmbMountOptions? = nil,
          @password : String? = nil,
          @user : String? = nil
        )
        end
      end

      # The NameNode of the Hadoop Distributed File System (HDFS). The NameNode manages the file system's
      # namespace. The NameNode performs operations such as opening, closing, and renaming files and
      # directories. The NameNode contains the information to map blocks of data to the DataNodes.

      struct HdfsNameNode
        include JSON::Serializable

        # The hostname of the NameNode in the HDFS cluster. This value is the IP address or Domain Name
        # Service (DNS) name of the NameNode. An agent that's installed on-premises uses this hostname to
        # communicate with the NameNode in the network.

        @[JSON::Field(key: "Hostname")]
        getter hostname : String

        # The port that the NameNode uses to listen to client requests.

        @[JSON::Field(key: "Port")]
        getter port : Int32

        def initialize(
          @hostname : String,
          @port : Int32
        )
        end
      end

      # This exception is thrown when an error occurs in the DataSync service.

      struct InternalException
        include JSON::Serializable


        @[JSON::Field(key: "errorCode")]
        getter error_code : String?


        @[JSON::Field(key: "message")]
        getter message : String?

        def initialize(
          @error_code : String? = nil,
          @message : String? = nil
        )
        end
      end

      # This exception is thrown when the client submits a malformed request.

      struct InvalidRequestException
        include JSON::Serializable


        @[JSON::Field(key: "datasyncErrorCode")]
        getter datasync_error_code : String?


        @[JSON::Field(key: "errorCode")]
        getter error_code : String?


        @[JSON::Field(key: "message")]
        getter message : String?

        def initialize(
          @datasync_error_code : String? = nil,
          @error_code : String? = nil,
          @message : String? = nil
        )
        end
      end

      # ListAgentsRequest

      struct ListAgentsRequest
        include JSON::Serializable

        # Specifies the maximum number of DataSync agents to list in a response. By default, a response shows
        # a maximum of 100 agents.

        @[JSON::Field(key: "MaxResults")]
        getter max_results : Int32?

        # Specifies an opaque string that indicates the position to begin the next list of results in the
        # response.

        @[JSON::Field(key: "NextToken")]
        getter next_token : String?

        def initialize(
          @max_results : Int32? = nil,
          @next_token : String? = nil
        )
        end
      end

      # ListAgentsResponse

      struct ListAgentsResponse
        include JSON::Serializable

        # A list of DataSync agents in your Amazon Web Services account in the Amazon Web Services Region
        # specified in the request. The list is ordered by the agents' Amazon Resource Names (ARNs).

        @[JSON::Field(key: "Agents")]
        getter agents : Array(Types::AgentListEntry)?

        # The opaque string that indicates the position to begin the next list of results in the response.

        @[JSON::Field(key: "NextToken")]
        getter next_token : String?

        def initialize(
          @agents : Array(Types::AgentListEntry)? = nil,
          @next_token : String? = nil
        )
        end
      end

      # ListLocationsRequest

      struct ListLocationsRequest
        include JSON::Serializable

        # You can use API filters to narrow down the list of resources returned by ListLocations . For
        # example, to retrieve all tasks on a specific source location, you can use ListLocations with filter
        # name LocationType S3 and Operator Equals .

        @[JSON::Field(key: "Filters")]
        getter filters : Array(Types::LocationFilter)?

        # The maximum number of locations to return.

        @[JSON::Field(key: "MaxResults")]
        getter max_results : Int32?

        # An opaque string that indicates the position at which to begin the next list of locations.

        @[JSON::Field(key: "NextToken")]
        getter next_token : String?

        def initialize(
          @filters : Array(Types::LocationFilter)? = nil,
          @max_results : Int32? = nil,
          @next_token : String? = nil
        )
        end
      end

      # ListLocationsResponse

      struct ListLocationsResponse
        include JSON::Serializable

        # An array that contains a list of locations.

        @[JSON::Field(key: "Locations")]
        getter locations : Array(Types::LocationListEntry)?

        # An opaque string that indicates the position at which to begin returning the next list of locations.

        @[JSON::Field(key: "NextToken")]
        getter next_token : String?

        def initialize(
          @locations : Array(Types::LocationListEntry)? = nil,
          @next_token : String? = nil
        )
        end
      end

      # ListTagsForResourceRequest

      struct ListTagsForResourceRequest
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of the resource that you want tag information on.

        @[JSON::Field(key: "ResourceArn")]
        getter resource_arn : String

        # Specifies how many results that you want in the response.

        @[JSON::Field(key: "MaxResults")]
        getter max_results : Int32?

        # Specifies an opaque string that indicates the position to begin the next list of results in the
        # response.

        @[JSON::Field(key: "NextToken")]
        getter next_token : String?

        def initialize(
          @resource_arn : String,
          @max_results : Int32? = nil,
          @next_token : String? = nil
        )
        end
      end

      # ListTagsForResourceResponse

      struct ListTagsForResourceResponse
        include JSON::Serializable

        # The opaque string that indicates the position to begin the next list of results in the response.

        @[JSON::Field(key: "NextToken")]
        getter next_token : String?

        # An array of tags applied to the specified resource.

        @[JSON::Field(key: "Tags")]
        getter tags : Array(Types::TagListEntry)?

        def initialize(
          @next_token : String? = nil,
          @tags : Array(Types::TagListEntry)? = nil
        )
        end
      end

      # ListTaskExecutions

      struct ListTaskExecutionsRequest
        include JSON::Serializable

        # Specifies how many results you want in the response.

        @[JSON::Field(key: "MaxResults")]
        getter max_results : Int32?

        # Specifies an opaque string that indicates the position at which to begin the next list of results in
        # the response.

        @[JSON::Field(key: "NextToken")]
        getter next_token : String?

        # Specifies the Amazon Resource Name (ARN) of the task that you want execution information about.

        @[JSON::Field(key: "TaskArn")]
        getter task_arn : String?

        def initialize(
          @max_results : Int32? = nil,
          @next_token : String? = nil,
          @task_arn : String? = nil
        )
        end
      end

      # ListTaskExecutionsResponse

      struct ListTaskExecutionsResponse
        include JSON::Serializable

        # The opaque string that indicates the position to begin the next list of results in the response.

        @[JSON::Field(key: "NextToken")]
        getter next_token : String?

        # A list of the task's executions.

        @[JSON::Field(key: "TaskExecutions")]
        getter task_executions : Array(Types::TaskExecutionListEntry)?

        def initialize(
          @next_token : String? = nil,
          @task_executions : Array(Types::TaskExecutionListEntry)? = nil
        )
        end
      end

      # ListTasksRequest

      struct ListTasksRequest
        include JSON::Serializable

        # You can use API filters to narrow down the list of resources returned by ListTasks . For example, to
        # retrieve all tasks on a specific source location, you can use ListTasks with filter name LocationId
        # and Operator Equals with the ARN for the location.

        @[JSON::Field(key: "Filters")]
        getter filters : Array(Types::TaskFilter)?

        # The maximum number of tasks to return.

        @[JSON::Field(key: "MaxResults")]
        getter max_results : Int32?

        # An opaque string that indicates the position at which to begin the next list of tasks.

        @[JSON::Field(key: "NextToken")]
        getter next_token : String?

        def initialize(
          @filters : Array(Types::TaskFilter)? = nil,
          @max_results : Int32? = nil,
          @next_token : String? = nil
        )
        end
      end

      # ListTasksResponse

      struct ListTasksResponse
        include JSON::Serializable

        # An opaque string that indicates the position at which to begin returning the next list of tasks.

        @[JSON::Field(key: "NextToken")]
        getter next_token : String?

        # A list of all the tasks that are returned.

        @[JSON::Field(key: "Tasks")]
        getter tasks : Array(Types::TaskListEntry)?

        def initialize(
          @next_token : String? = nil,
          @tasks : Array(Types::TaskListEntry)? = nil
        )
        end
      end

      # Narrow down the list of resources returned by ListLocations . For example, to see all your Amazon S3
      # locations, create a filter using "Name": "LocationType" , "Operator": "Equals" , and "Values": "S3"
      # . For more information, see filtering resources .

      struct LocationFilter
        include JSON::Serializable

        # The name of the filter being used. Each API call supports a list of filters that are available for
        # it (for example, LocationType for ListLocations ).

        @[JSON::Field(key: "Name")]
        getter name : String

        # The operator that is used to compare filter values (for example, Equals or Contains ).

        @[JSON::Field(key: "Operator")]
        getter operator : String

        # The values that you want to filter for. For example, you might want to display only Amazon S3
        # locations.

        @[JSON::Field(key: "Values")]
        getter values : Array(String)

        def initialize(
          @name : String,
          @operator : String,
          @values : Array(String)
        )
        end
      end

      # Represents a single entry in a list of locations. LocationListEntry returns an array that contains a
      # list of locations when the ListLocations operation is called.

      struct LocationListEntry
        include JSON::Serializable

        # The Amazon Resource Name (ARN) of the location. For Network File System (NFS) or Amazon EFS, the
        # location is the export path. For Amazon S3, the location is the prefix path that you want to mount
        # and use as the root of the location.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String?

        # Represents a list of URIs of a location. LocationUri returns an array that contains a list of
        # locations when the ListLocations operation is called. Format: TYPE://GLOBAL_ID/SUBDIR . TYPE
        # designates the type of location (for example, nfs or s3 ). GLOBAL_ID is the globally unique
        # identifier of the resource that backs the location. An example for EFS is us-east-2.fs-abcd1234 . An
        # example for Amazon S3 is the bucket name, such as myBucket . An example for NFS is a valid IPv4 or
        # IPv6 address or a hostname that is compliant with DNS. SUBDIR is a valid file system path, delimited
        # by forward slashes as is the *nix convention. For NFS and Amazon EFS, it's the export path to mount
        # the location. For Amazon S3, it's the prefix path that you mount to and treat as the root of the
        # location.

        @[JSON::Field(key: "LocationUri")]
        getter location_uri : String?

        def initialize(
          @location_arn : String? = nil,
          @location_uri : String? = nil
        )
        end
      end

      # Specifies configuration information for a DataSync-managed secret, such as an authentication token
      # or set of credentials that DataSync uses to access a specific transfer location. DataSync uses the
      # default Amazon Web Services-managed KMS key to encrypt this secret in Secrets Manager.

      struct ManagedSecretConfig
        include JSON::Serializable

        # Specifies the ARN for an Secrets Manager secret.

        @[JSON::Field(key: "SecretArn")]
        getter secret_arn : String?

        def initialize(
          @secret_arn : String? = nil
        )
        end
      end

      # Configures a manifest, which is a list of files or objects that you want DataSync to transfer. For
      # more information and configuration examples, see Specifying what DataSync transfers by using a
      # manifest .

      struct ManifestConfig
        include JSON::Serializable

        # Specifies what DataSync uses the manifest for.

        @[JSON::Field(key: "Action")]
        getter action : String?

        # Specifies the file format of your manifest. For more information, see Creating a manifest .

        @[JSON::Field(key: "Format")]
        getter format : String?

        # Specifies the manifest that you want DataSync to use and where it's hosted. You must specify this
        # parameter if you're configuring a new manifest on or after February 7, 2024. If you don't, you'll
        # get a 400 status code and ValidationException error stating that you're missing the IAM role for
        # DataSync to access the S3 bucket where you're hosting your manifest. For more information, see
        # Providing DataSync access to your manifest .

        @[JSON::Field(key: "Source")]
        getter source : Types::SourceManifestConfig?

        def initialize(
          @action : String? = nil,
          @format : String? = nil,
          @source : Types::SourceManifestConfig? = nil
        )
        end
      end

      # Specifies how DataSync can access a location using the NFS protocol.

      struct NfsMountOptions
        include JSON::Serializable

        # Specifies the NFS version that you want DataSync to use when mounting your NFS share. If the server
        # refuses to use the version specified, the task fails. You can specify the following options:
        # AUTOMATIC (default): DataSync chooses NFS version 4.1. NFS3 : Stateless protocol version that allows
        # for asynchronous writes on the server. NFSv4_0 : Stateful, firewall-friendly protocol version that
        # supports delegations and pseudo file systems. NFSv4_1 : Stateful protocol version that supports
        # sessions, directory delegations, and parallel data processing. NFS version 4.1 also includes all
        # features available in version 4.0. DataSync currently only supports NFS version 3 with Amazon FSx
        # for NetApp ONTAP locations.

        @[JSON::Field(key: "Version")]
        getter version : String?

        def initialize(
          @version : String? = nil
        )
        end
      end

      # The DataSync agents that can connect to your Network File System (NFS) file server.

      struct OnPremConfig
        include JSON::Serializable

        # The Amazon Resource Names (ARNs) of the DataSync agents that can connect to your NFS file server.
        # You can specify more than one agent. For more information, see Using multiple DataSync agents .

        @[JSON::Field(key: "AgentArns")]
        getter agent_arns : Array(String)

        def initialize(
          @agent_arns : Array(String)
        )
        end
      end

      # Indicates how your transfer task is configured. These options include how DataSync handles files,
      # objects, and their associated metadata during your transfer. You also can specify how to verify data
      # integrity, set bandwidth limits for your task, among other options. Each option has a default value.
      # Unless you need to, you don't have to configure any option before calling StartTaskExecution . You
      # also can override your task options for each task execution. For example, you might want to adjust
      # the LogLevel for an individual execution.

      struct Options
        include JSON::Serializable

        # Specifies whether to preserve metadata indicating the last time a file was read or written to. The
        # behavior of Atime isn't fully standard across platforms, so DataSync can only do this on a
        # best-effort basis. BEST_EFFORT (default) - DataSync attempts to preserve the original Atime
        # attribute on all source files (that is, the version before the PREPARING steps of the task
        # execution). This option is recommended. NONE - Ignores Atime . If Atime is set to BEST_EFFORT ,
        # Mtime must be set to PRESERVE . If Atime is set to NONE , Mtime must also be NONE .

        @[JSON::Field(key: "Atime")]
        getter atime : String?

        # Limits the bandwidth used by a DataSync task. For example, if you want DataSync to use a maximum of
        # 1 MB, set this value to 1048576 ( =1024*1024 ).

        @[JSON::Field(key: "BytesPerSecond")]
        getter bytes_per_second : Int64?

        # Specifies the POSIX group ID (GID) of the file's owners. INT_VALUE (default) - Preserves the integer
        # value of user ID (UID) and GID, which is recommended. NONE - Ignores UID and GID. For more
        # information, see Understanding how DataSync handles file and object metadata .

        @[JSON::Field(key: "Gid")]
        getter gid : String?

        # Specifies the type of logs that DataSync publishes to a Amazon CloudWatch Logs log group. To specify
        # the log group, see CloudWatchLogGroupArn . BASIC - Publishes logs with only basic information (such
        # as transfer errors). TRANSFER - Publishes logs for all files or objects that your DataSync task
        # transfers and performs data-integrity checks on. OFF - No logs are published.

        @[JSON::Field(key: "LogLevel")]
        getter log_level : String?

        # Specifies whether to preserve metadata indicating the last time that a file was written to before
        # the PREPARING step of your task execution. This option is required when you need to run the a task
        # more than once. PRESERVE (default) - Preserves original Mtime , which is recommended. NONE - Ignores
        # Mtime . If Mtime is set to PRESERVE , Atime must be set to BEST_EFFORT . If Mtime is set to NONE ,
        # Atime must also be set to NONE .

        @[JSON::Field(key: "Mtime")]
        getter mtime : String?

        # Specifies whether you want DataSync to PRESERVE object tags (default behavior) when transferring
        # between object storage systems. If you want your DataSync task to ignore object tags, specify the
        # NONE value.

        @[JSON::Field(key: "ObjectTags")]
        getter object_tags : String?

        # Specifies whether DataSync should modify or preserve data at the destination location. ALWAYS
        # (default) - DataSync modifies data in the destination location when source data (including metadata)
        # has changed. If DataSync overwrites objects, you might incur additional charges for certain Amazon
        # S3 storage classes (for example, for retrieval or early deletion). For more information, see Storage
        # class considerations with Amazon S3 transfers . NEVER - DataSync doesn't overwrite data in the
        # destination location even if the source data has changed. You can use this option to protect against
        # overwriting changes made to files or objects in the destination.

        @[JSON::Field(key: "OverwriteMode")]
        getter overwrite_mode : String?

        # Specifies which users or groups can access a file for a specific purpose such as reading, writing,
        # or execution of the file. For more information, see Understanding how DataSync handles file and
        # object metadata . PRESERVE (default) - Preserves POSIX-style permissions, which is recommended. NONE
        # - Ignores POSIX-style permissions. DataSync can preserve extant permissions of a source location.

        @[JSON::Field(key: "PosixPermissions")]
        getter posix_permissions : String?

        # Specifies whether files in the destination location that don't exist in the source should be
        # preserved. This option can affect your Amazon S3 storage cost. If your task deletes objects, you
        # might incur minimum storage duration charges for certain storage classes. For detailed information,
        # see Considerations when working with Amazon S3 storage classes in DataSync . PRESERVE (default) -
        # Ignores such destination files, which is recommended. REMOVE - Deletes destination files that aren’t
        # present in the source. If you set this parameter to REMOVE , you can't set TransferMode to ALL .
        # When you transfer all data, DataSync doesn't scan your destination location and doesn't know what to
        # delete.

        @[JSON::Field(key: "PreserveDeletedFiles")]
        getter preserve_deleted_files : String?

        # Specifies whether DataSync should preserve the metadata of block and character devices in the source
        # location and recreate the files with that device name and metadata on the destination. DataSync
        # copies only the name and metadata of such devices. DataSync can't copy the actual contents of these
        # devices because they're nonterminal and don't return an end-of-file (EOF) marker. NONE (default) -
        # Ignores special devices (recommended). PRESERVE - Preserves character and block device metadata.
        # This option currently isn't supported for Amazon EFS.

        @[JSON::Field(key: "PreserveDevices")]
        getter preserve_devices : String?

        # Specifies which components of the SMB security descriptor are copied from source to destination
        # objects. This value is only used for transfers between SMB and Amazon FSx for Windows File Server
        # locations or between two FSx for Windows File Server locations. For more information, see
        # Understanding how DataSync handles file and object metadata . OWNER_DACL (default) - For each copied
        # object, DataSync copies the following metadata: The object owner. NTFS discretionary access control
        # lists (DACLs), which determine whether to grant access to an object. DataSync won't copy NTFS system
        # access control lists (SACLs) with this option. OWNER_DACL_SACL - For each copied object, DataSync
        # copies the following metadata: The object owner. NTFS discretionary access control lists (DACLs),
        # which determine whether to grant access to an object. SACLs, which are used by administrators to log
        # attempts to access a secured object. Copying SACLs requires granting additional permissions to the
        # Windows user that DataSync uses to access your SMB location. For information about choosing a user
        # with the right permissions, see required permissions for SMB , FSx for Windows File Server , or FSx
        # for ONTAP (depending on the type of location in your transfer). NONE - None of the SMB security
        # descriptor components are copied. Destination objects are owned by the user that was provided for
        # accessing the destination location. DACLs and SACLs are set based on the destination server’s
        # configuration.

        @[JSON::Field(key: "SecurityDescriptorCopyFlags")]
        getter security_descriptor_copy_flags : String?

        # Specifies whether your transfer tasks should be put into a queue during certain scenarios when
        # running multiple tasks . This is ENABLED by default.

        @[JSON::Field(key: "TaskQueueing")]
        getter task_queueing : String?

        # Specifies whether DataSync transfers only the data (including metadata) that differs between
        # locations following an initial copy or transfers all data every time you run the task. If you're
        # planning on recurring transfers, you might only want to transfer what's changed since your previous
        # task execution. CHANGED (default) - After your initial full transfer, DataSync copies only the data
        # and metadata that differs between the source and destination location. ALL - DataSync copies
        # everything in the source to the destination without comparing differences between the locations.

        @[JSON::Field(key: "TransferMode")]
        getter transfer_mode : String?

        # Specifies the POSIX user ID (UID) of the file's owner. INT_VALUE (default) - Preserves the integer
        # value of UID and group ID (GID), which is recommended. NONE - Ignores UID and GID. For more
        # information, see Metadata copied by DataSync .

        @[JSON::Field(key: "Uid")]
        getter uid : String?

        # Specifies if and how DataSync checks the integrity of your data at the end of your transfer.
        # ONLY_FILES_TRANSFERRED (recommended) - DataSync calculates the checksum of transferred data
        # (including metadata) at the source location. At the end of the transfer, DataSync then compares this
        # checksum to the checksum calculated on that data at the destination. This is the default option for
        # Enhanced mode tasks . We recommend this option when transferring to S3 Glacier Flexible Retrieval or
        # S3 Glacier Deep Archive storage classes. For more information, see Storage class considerations with
        # Amazon S3 locations . POINT_IN_TIME_CONSISTENT - At the end of the transfer, DataSync checks the
        # entire source and destination to verify that both locations are fully synchronized. The is the
        # default option for Basic mode tasks and isn't currently supported with Enhanced mode tasks. If you
        # use a manifest , DataSync only scans and verifies what's listed in the manifest. You can't use this
        # option when transferring to S3 Glacier Flexible Retrieval or S3 Glacier Deep Archive storage
        # classes. For more information, see Storage class considerations with Amazon S3 locations . NONE -
        # DataSync performs data integrity checks only during your transfer. Unlike other options, there's no
        # additional verification at the end of your transfer.

        @[JSON::Field(key: "VerifyMode")]
        getter verify_mode : String?

        def initialize(
          @atime : String? = nil,
          @bytes_per_second : Int64? = nil,
          @gid : String? = nil,
          @log_level : String? = nil,
          @mtime : String? = nil,
          @object_tags : String? = nil,
          @overwrite_mode : String? = nil,
          @posix_permissions : String? = nil,
          @preserve_deleted_files : String? = nil,
          @preserve_devices : String? = nil,
          @security_descriptor_copy_flags : String? = nil,
          @task_queueing : String? = nil,
          @transfer_mode : String? = nil,
          @uid : String? = nil,
          @verify_mode : String? = nil
        )
        end
      end

      # The platform-related details about the DataSync agent, such as the version number.

      struct Platform
        include JSON::Serializable

        # The version of the DataSync agent.

        @[JSON::Field(key: "Version")]
        getter version : String?

        def initialize(
          @version : String? = nil
        )
        end
      end

      # Specifies how your DataSync agent connects to Amazon Web Services using a virtual private cloud
      # (VPC) service endpoint . An agent that uses a VPC endpoint isn't accessible over the public
      # internet.

      struct PrivateLinkConfig
        include JSON::Serializable

        # Specifies the VPC endpoint provided by Amazon Web Services PrivateLink that your agent connects to.

        @[JSON::Field(key: "PrivateLinkEndpoint")]
        getter private_link_endpoint : String?

        # Specifies the Amazon Resource Names (ARN) of the security group that provides DataSync access to
        # your VPC endpoint. You can only specify one ARN.

        @[JSON::Field(key: "SecurityGroupArns")]
        getter security_group_arns : Array(String)?

        # Specifies the ARN of the subnet where your VPC endpoint is located. You can only specify one ARN.

        @[JSON::Field(key: "SubnetArns")]
        getter subnet_arns : Array(String)?

        # Specifies the ID of the VPC endpoint that your agent connects to.

        @[JSON::Field(key: "VpcEndpointId")]
        getter vpc_endpoint_id : String?

        def initialize(
          @private_link_endpoint : String? = nil,
          @security_group_arns : Array(String)? = nil,
          @subnet_arns : Array(String)? = nil,
          @vpc_endpoint_id : String? = nil
        )
        end
      end

      # The Quality of Protection (QOP) configuration specifies the Remote Procedure Call (RPC) and data
      # transfer privacy settings configured on the Hadoop Distributed File System (HDFS) cluster.

      struct QopConfiguration
        include JSON::Serializable

        # The data transfer protection setting configured on the HDFS cluster. This setting corresponds to
        # your dfs.data.transfer.protection setting in the hdfs-site.xml file on your Hadoop cluster.

        @[JSON::Field(key: "DataTransferProtection")]
        getter data_transfer_protection : String?

        # The RPC protection setting configured on the HDFS cluster. This setting corresponds to your
        # hadoop.rpc.protection setting in your core-site.xml file on your Hadoop cluster.

        @[JSON::Field(key: "RpcProtection")]
        getter rpc_protection : String?

        def initialize(
          @data_transfer_protection : String? = nil,
          @rpc_protection : String? = nil
        )
        end
      end

      # Specifies where DataSync uploads your task report .

      struct ReportDestination
        include JSON::Serializable

        # Specifies the Amazon S3 bucket where DataSync uploads your task report.

        @[JSON::Field(key: "S3")]
        getter s3 : Types::ReportDestinationS3?

        def initialize(
          @s3 : Types::ReportDestinationS3? = nil
        )
        end
      end

      # Specifies the Amazon S3 bucket where DataSync uploads your task report .

      struct ReportDestinationS3
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of the IAM policy that allows DataSync to upload a task
        # report to your S3 bucket. For more information, see Allowing DataSync to upload a task report to an
        # Amazon S3 bucket .

        @[JSON::Field(key: "BucketAccessRoleArn")]
        getter bucket_access_role_arn : String

        # Specifies the ARN of the S3 bucket where DataSync uploads your report.

        @[JSON::Field(key: "S3BucketArn")]
        getter s3_bucket_arn : String

        # Specifies a bucket prefix for your report.

        @[JSON::Field(key: "Subdirectory")]
        getter subdirectory : String?

        def initialize(
          @bucket_access_role_arn : String,
          @s3_bucket_arn : String,
          @subdirectory : String? = nil
        )
        end
      end

      # Specifies the level of detail for a particular aspect of your DataSync task report .

      struct ReportOverride
        include JSON::Serializable

        # Specifies whether your task report includes errors only or successes and errors. For example, your
        # report might mostly include only what didn't go well in your transfer ( ERRORS_ONLY ). At the same
        # time, you want to verify that your task filter is working correctly. In this situation, you can get
        # a list of what files DataSync successfully skipped and if something transferred that you didn't to
        # transfer ( SUCCESSES_AND_ERRORS ).

        @[JSON::Field(key: "ReportLevel")]
        getter report_level : String?

        def initialize(
          @report_level : String? = nil
        )
        end
      end

      # The level of detail included in each aspect of your DataSync task report .

      struct ReportOverrides
        include JSON::Serializable

        # Specifies the level of reporting for the files, objects, and directories that DataSync attempted to
        # delete in your destination location. This only applies if you configure your task to delete data in
        # the destination that isn't in the source.

        @[JSON::Field(key: "Deleted")]
        getter deleted : Types::ReportOverride?

        # Specifies the level of reporting for the files, objects, and directories that DataSync attempted to
        # skip during your transfer.

        @[JSON::Field(key: "Skipped")]
        getter skipped : Types::ReportOverride?

        # Specifies the level of reporting for the files, objects, and directories that DataSync attempted to
        # transfer.

        @[JSON::Field(key: "Transferred")]
        getter transferred : Types::ReportOverride?

        # Specifies the level of reporting for the files, objects, and directories that DataSync attempted to
        # verify at the end of your transfer.

        @[JSON::Field(key: "Verified")]
        getter verified : Types::ReportOverride?

        def initialize(
          @deleted : Types::ReportOverride? = nil,
          @skipped : Types::ReportOverride? = nil,
          @transferred : Types::ReportOverride? = nil,
          @verified : Types::ReportOverride? = nil
        )
        end
      end

      # Indicates whether DataSync created a complete task report for your transfer.

      struct ReportResult
        include JSON::Serializable

        # Indicates the code associated with the error if DataSync can't create a complete report.

        @[JSON::Field(key: "ErrorCode")]
        getter error_code : String?

        # Provides details about issues creating a report.

        @[JSON::Field(key: "ErrorDetail")]
        getter error_detail : String?

        # Indicates whether DataSync is still working on your report, created a report, or can't create a
        # complete report.

        @[JSON::Field(key: "Status")]
        getter status : String?

        def initialize(
          @error_code : String? = nil,
          @error_detail : String? = nil,
          @status : String? = nil
        )
        end
      end

      # Specifies the Amazon Resource Name (ARN) of the Identity and Access Management (IAM) role that
      # DataSync uses to access your S3 bucket. For more information, see Providing DataSync access to S3
      # buckets .

      struct S3Config
        include JSON::Serializable

        # Specifies the ARN of the IAM role that DataSync uses to access your S3 bucket.

        @[JSON::Field(key: "BucketAccessRoleArn")]
        getter bucket_access_role_arn : String

        def initialize(
          @bucket_access_role_arn : String
        )
        end
      end

      # Specifies the S3 bucket where you're hosting the manifest that you want DataSync to use. For more
      # information and configuration examples, see Specifying what DataSync transfers by using a manifest .

      struct S3ManifestConfig
        include JSON::Serializable

        # Specifies the Identity and Access Management (IAM) role that allows DataSync to access your
        # manifest. For more information, see Providing DataSync access to your manifest .

        @[JSON::Field(key: "BucketAccessRoleArn")]
        getter bucket_access_role_arn : String

        # Specifies the Amazon S3 object key of your manifest. This can include a prefix (for example,
        # prefix/my-manifest.csv ).

        @[JSON::Field(key: "ManifestObjectPath")]
        getter manifest_object_path : String

        # Specifies the Amazon Resource Name (ARN) of the S3 bucket where you're hosting your manifest.

        @[JSON::Field(key: "S3BucketArn")]
        getter s3_bucket_arn : String

        # Specifies the object version ID of the manifest that you want DataSync to use. If you don't set
        # this, DataSync uses the latest version of the object.

        @[JSON::Field(key: "ManifestObjectVersionId")]
        getter manifest_object_version_id : String?

        def initialize(
          @bucket_access_role_arn : String,
          @manifest_object_path : String,
          @s3_bucket_arn : String,
          @manifest_object_version_id : String? = nil
        )
        end
      end

      # Specifies the version of the Server Message Block (SMB) protocol that DataSync uses to access an SMB
      # file server.

      struct SmbMountOptions
        include JSON::Serializable

        # By default, DataSync automatically chooses an SMB protocol version based on negotiation with your
        # SMB file server. You also can configure DataSync to use a specific SMB version, but we recommend
        # doing this only if DataSync has trouble negotiating with the SMB file server automatically. These
        # are the following options for configuring the SMB version: AUTOMATIC (default): DataSync and the SMB
        # file server negotiate the highest version of SMB that they mutually support between 2.1 and 3.1.1.
        # This is the recommended option. If you instead choose a specific version that your file server
        # doesn't support, you may get an Operation Not Supported error. SMB3 : Restricts the protocol
        # negotiation to only SMB version 3.0.2. SMB2 : Restricts the protocol negotiation to only SMB version
        # 2.1. SMB2_0 : Restricts the protocol negotiation to only SMB version 2.0. SMB1 : Restricts the
        # protocol negotiation to only SMB version 1.0. The SMB1 option isn't available when creating an
        # Amazon FSx for NetApp ONTAP location .

        @[JSON::Field(key: "Version")]
        getter version : String?

        def initialize(
          @version : String? = nil
        )
        end
      end

      # Specifies the manifest that you want DataSync to use and where it's hosted. For more information and
      # configuration examples, see Specifying what DataSync transfers by using a manifest .

      struct SourceManifestConfig
        include JSON::Serializable

        # Specifies the S3 bucket where you're hosting your manifest.

        @[JSON::Field(key: "S3")]
        getter s3 : Types::S3ManifestConfig

        def initialize(
          @s3 : Types::S3ManifestConfig
        )
        end
      end

      # StartTaskExecutionRequest

      struct StartTaskExecutionRequest
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of the task that you want to start.

        @[JSON::Field(key: "TaskArn")]
        getter task_arn : String

        # Specifies a list of filter rules that determines which files to exclude from a task. The list
        # contains a single filter string that consists of the patterns to exclude. The patterns are delimited
        # by "|" (that is, a pipe), for example, "/folder1|/folder2" .

        @[JSON::Field(key: "Excludes")]
        getter excludes : Array(Types::FilterRule)?

        # Specifies a list of filter rules that determines which files to include when running a task. The
        # pattern should contain a single filter string that consists of the patterns to include. The patterns
        # are delimited by "|" (that is, a pipe), for example, "/folder1|/folder2" .

        @[JSON::Field(key: "Includes")]
        getter includes : Array(Types::FilterRule)?

        # Configures a manifest, which is a list of files or objects that you want DataSync to transfer. For
        # more information and configuration examples, see Specifying what DataSync transfers by using a
        # manifest . When using this parameter, your caller identity (the role that you're using DataSync
        # with) must have the iam:PassRole permission. The AWSDataSyncFullAccess policy includes this
        # permission. To remove a manifest configuration, specify this parameter with an empty value.

        @[JSON::Field(key: "ManifestConfig")]
        getter manifest_config : Types::ManifestConfig?


        @[JSON::Field(key: "OverrideOptions")]
        getter override_options : Types::Options?

        # Specifies the tags that you want to apply to the Amazon Resource Name (ARN) representing the task
        # execution. Tags are key-value pairs that help you manage, filter, and search for your DataSync
        # resources.

        @[JSON::Field(key: "Tags")]
        getter tags : Array(Types::TagListEntry)?

        # Specifies how you want to configure a task report, which provides detailed information about your
        # DataSync transfer. For more information, see Monitoring your DataSync transfers with task reports .
        # When using this parameter, your caller identity (the role that you're using DataSync with) must have
        # the iam:PassRole permission. The AWSDataSyncFullAccess policy includes this permission. To remove a
        # task report configuration, specify this parameter as empty.

        @[JSON::Field(key: "TaskReportConfig")]
        getter task_report_config : Types::TaskReportConfig?

        def initialize(
          @task_arn : String,
          @excludes : Array(Types::FilterRule)? = nil,
          @includes : Array(Types::FilterRule)? = nil,
          @manifest_config : Types::ManifestConfig? = nil,
          @override_options : Types::Options? = nil,
          @tags : Array(Types::TagListEntry)? = nil,
          @task_report_config : Types::TaskReportConfig? = nil
        )
        end
      end

      # StartTaskExecutionResponse

      struct StartTaskExecutionResponse
        include JSON::Serializable

        # The ARN of the running task execution.

        @[JSON::Field(key: "TaskExecutionArn")]
        getter task_execution_arn : String?

        def initialize(
          @task_execution_arn : String? = nil
        )
        end
      end

      # A key-value pair representing a single tag that's been applied to an Amazon Web Services resource.

      struct TagListEntry
        include JSON::Serializable

        # The key for an Amazon Web Services resource tag.

        @[JSON::Field(key: "Key")]
        getter key : String

        # The value for an Amazon Web Services resource tag.

        @[JSON::Field(key: "Value")]
        getter value : String?

        def initialize(
          @key : String,
          @value : String? = nil
        )
        end
      end

      # TagResourceRequest

      struct TagResourceRequest
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of the resource to apply the tag to.

        @[JSON::Field(key: "ResourceArn")]
        getter resource_arn : String

        # Specifies the tags that you want to apply to the resource.

        @[JSON::Field(key: "Tags")]
        getter tags : Array(Types::TagListEntry)

        def initialize(
          @resource_arn : String,
          @tags : Array(Types::TagListEntry)
        )
        end
      end


      struct TagResourceResponse
        include JSON::Serializable

        def initialize
        end
      end

      # The number of files or objects that DataSync fails to prepare, transfer, verify, and delete during
      # your task execution. Applies only to Enhanced mode tasks .

      struct TaskExecutionFilesFailedDetail
        include JSON::Serializable

        # The number of files or objects that DataSync fails to delete during your task execution.

        @[JSON::Field(key: "Delete")]
        getter delete : Int64?

        # The number of files or objects that DataSync fails to prepare during your task execution.

        @[JSON::Field(key: "Prepare")]
        getter prepare : Int64?

        # The number of files or objects that DataSync fails to transfer during your task execution.

        @[JSON::Field(key: "Transfer")]
        getter transfer : Int64?

        # The number of files or objects that DataSync fails to verify during your task execution.

        @[JSON::Field(key: "Verify")]
        getter verify : Int64?

        def initialize(
          @delete : Int64? = nil,
          @prepare : Int64? = nil,
          @transfer : Int64? = nil,
          @verify : Int64? = nil
        )
        end
      end

      # The number of files or objects that DataSync finds at your locations. Applies only to Enhanced mode
      # tasks .

      struct TaskExecutionFilesListedDetail
        include JSON::Serializable

        # The number of files or objects that DataSync finds at your destination location. This counter is
        # only applicable if you configure your task to delete data in the destination that isn't in the
        # source.

        @[JSON::Field(key: "AtDestinationForDelete")]
        getter at_destination_for_delete : Int64?

        # The number of files or objects that DataSync finds at your source location. With a manifest ,
        # DataSync lists only what's in your manifest (and not everything at your source location). With an
        # include filter , DataSync lists only what matches the filter at your source location. With an
        # exclude filter, DataSync lists everything at your source location before applying the filter.

        @[JSON::Field(key: "AtSource")]
        getter at_source : Int64?

        def initialize(
          @at_destination_for_delete : Int64? = nil,
          @at_source : Int64? = nil
        )
        end
      end

      # The number of directories that DataSync fails to list, prepare, transfer, verify, and delete during
      # your task execution. Applies only to Enhanced mode tasks .

      struct TaskExecutionFoldersFailedDetail
        include JSON::Serializable

        # The number of directories that DataSync fails to delete during your task execution.

        @[JSON::Field(key: "Delete")]
        getter delete : Int64?

        # The number of directories that DataSync fails to list during your task execution.

        @[JSON::Field(key: "List")]
        getter list : Int64?

        # The number of directories that DataSync fails to prepare during your task execution.

        @[JSON::Field(key: "Prepare")]
        getter prepare : Int64?

        # The number of directories that DataSync fails to transfer during your task execution.

        @[JSON::Field(key: "Transfer")]
        getter transfer : Int64?

        # The number of directories that DataSync fails to verify during your task execution.

        @[JSON::Field(key: "Verify")]
        getter verify : Int64?

        def initialize(
          @delete : Int64? = nil,
          @list : Int64? = nil,
          @prepare : Int64? = nil,
          @transfer : Int64? = nil,
          @verify : Int64? = nil
        )
        end
      end

      # The number of directories that DataSync finds at your locations. Applies only to Enhanced mode tasks
      # .

      struct TaskExecutionFoldersListedDetail
        include JSON::Serializable

        # The number of directories that DataSync finds at your destination location. This counter is only
        # applicable if you configure your task to delete data in the destination that isn't in the source.

        @[JSON::Field(key: "AtDestinationForDelete")]
        getter at_destination_for_delete : Int64?

        # The number of directories that DataSync finds at your source location. With a manifest , DataSync
        # lists only what's in your manifest (and not everything at your source location). With an include
        # filter , DataSync lists only what matches the filter at your source location. With an exclude
        # filter, DataSync lists everything at your source location before applying the filter.

        @[JSON::Field(key: "AtSource")]
        getter at_source : Int64?

        def initialize(
          @at_destination_for_delete : Int64? = nil,
          @at_source : Int64? = nil
        )
        end
      end

      # Represents a single entry in a list of DataSync task executions that's returned with the
      # ListTaskExecutions operation.

      struct TaskExecutionListEntry
        include JSON::Serializable

        # The status of a task execution. For more information, see Task execution statuses .

        @[JSON::Field(key: "Status")]
        getter status : String?

        # The Amazon Resource Name (ARN) of a task execution.

        @[JSON::Field(key: "TaskExecutionArn")]
        getter task_execution_arn : String?

        # The task mode that you're using. For more information, see Choosing a task mode for your data
        # transfer .

        @[JSON::Field(key: "TaskMode")]
        getter task_mode : String?

        def initialize(
          @status : String? = nil,
          @task_execution_arn : String? = nil,
          @task_mode : String? = nil
        )
        end
      end

      # Provides detailed information about the result of your DataSync task execution.

      struct TaskExecutionResultDetail
        include JSON::Serializable

        # An error that DataSync encountered during your task execution. You can use this information to help
        # troubleshoot issues .

        @[JSON::Field(key: "ErrorCode")]
        getter error_code : String?

        # The detailed description of an error that DataSync encountered during your task execution. You can
        # use this information to help troubleshoot issues .

        @[JSON::Field(key: "ErrorDetail")]
        getter error_detail : String?

        # The time in milliseconds that your task execution was in the PREPARING step. For more information,
        # see Task execution statuses . For Enhanced mode tasks, the value is always 0 . For more information,
        # see How DataSync prepares your data transfer .

        @[JSON::Field(key: "PrepareDuration")]
        getter prepare_duration : Int64?

        # The status of the PREPARING step for your task execution. For more information, see Task execution
        # statuses .

        @[JSON::Field(key: "PrepareStatus")]
        getter prepare_status : String?

        # The time in milliseconds that your task execution ran.

        @[JSON::Field(key: "TotalDuration")]
        getter total_duration : Int64?

        # The time in milliseconds that your task execution was in the TRANSFERRING step. For more
        # information, see Task execution statuses . For Enhanced mode tasks, the value is always 0 . For more
        # information, see How DataSync transfers your data .

        @[JSON::Field(key: "TransferDuration")]
        getter transfer_duration : Int64?

        # The status of the TRANSFERRING step for your task execution. For more information, see Task
        # execution statuses .

        @[JSON::Field(key: "TransferStatus")]
        getter transfer_status : String?

        # The time in milliseconds that your task execution was in the VERIFYING step. For more information,
        # see Task execution statuses . For Enhanced mode tasks, the value is always 0 . For more information,
        # see How DataSync verifies your data's integrity .

        @[JSON::Field(key: "VerifyDuration")]
        getter verify_duration : Int64?

        # The status of the VERIFYING step for your task execution. For more information, see Task execution
        # statuses .

        @[JSON::Field(key: "VerifyStatus")]
        getter verify_status : String?

        def initialize(
          @error_code : String? = nil,
          @error_detail : String? = nil,
          @prepare_duration : Int64? = nil,
          @prepare_status : String? = nil,
          @total_duration : Int64? = nil,
          @transfer_duration : Int64? = nil,
          @transfer_status : String? = nil,
          @verify_duration : Int64? = nil,
          @verify_status : String? = nil
        )
        end
      end

      # You can use API filters to narrow down the list of resources returned by ListTasks . For example, to
      # retrieve all tasks on a source location, you can use ListTasks with filter name LocationId and
      # Operator Equals with the ARN for the location. For more information, see filtering DataSync
      # resources .

      struct TaskFilter
        include JSON::Serializable

        # The name of the filter being used. Each API call supports a list of filters that are available for
        # it. For example, LocationId for ListTasks .

        @[JSON::Field(key: "Name")]
        getter name : String

        # The operator that is used to compare filter values (for example, Equals or Contains ).

        @[JSON::Field(key: "Operator")]
        getter operator : String

        # The values that you want to filter for. For example, you might want to display only tasks for a
        # specific destination location.

        @[JSON::Field(key: "Values")]
        getter values : Array(String)

        def initialize(
          @name : String,
          @operator : String,
          @values : Array(String)
        )
        end
      end

      # Represents a single entry in a list of tasks. TaskListEntry returns an array that contains a list of
      # tasks when the ListTasks operation is called. A task includes the source and destination file
      # systems to sync and the options to use for the tasks.

      struct TaskListEntry
        include JSON::Serializable

        # The name of the task.

        @[JSON::Field(key: "Name")]
        getter name : String?

        # The status of the task.

        @[JSON::Field(key: "Status")]
        getter status : String?

        # The Amazon Resource Name (ARN) of the task.

        @[JSON::Field(key: "TaskArn")]
        getter task_arn : String?

        # The task mode that you're using. For more information, see Choosing a task mode for your data
        # transfer .

        @[JSON::Field(key: "TaskMode")]
        getter task_mode : String?

        def initialize(
          @name : String? = nil,
          @status : String? = nil,
          @task_arn : String? = nil,
          @task_mode : String? = nil
        )
        end
      end

      # Specifies how you want to configure a task report, which provides detailed information about for
      # your DataSync transfer. For more information, see Task reports .

      struct TaskReportConfig
        include JSON::Serializable

        # Specifies the Amazon S3 bucket where DataSync uploads your task report. For more information, see
        # Task reports .

        @[JSON::Field(key: "Destination")]
        getter destination : Types::ReportDestination?

        # Specifies whether your task report includes the new version of each object transferred into an S3
        # bucket. This only applies if you enable versioning on your bucket . Keep in mind that setting this
        # to INCLUDE can increase the duration of your task execution.

        @[JSON::Field(key: "ObjectVersionIds")]
        getter object_version_ids : String?

        # Specifies the type of task report that you want: SUMMARY_ONLY : Provides necessary details about
        # your task, including the number of files, objects, and directories transferred and transfer
        # duration. STANDARD : Provides complete details about your task, including a full list of files,
        # objects, and directories that were transferred, skipped, verified, and more.

        @[JSON::Field(key: "OutputType")]
        getter output_type : String?

        # Customizes the reporting level for aspects of your task report. For example, your report might
        # generally only include errors, but you could specify that you want a list of successes and errors
        # just for the files that DataSync attempted to delete in your destination location.

        @[JSON::Field(key: "Overrides")]
        getter overrides : Types::ReportOverrides?

        # Specifies whether you want your task report to include only what went wrong with your transfer or a
        # list of what succeeded and didn't. ERRORS_ONLY : A report shows what DataSync was unable to
        # transfer, skip, verify, and delete. SUCCESSES_AND_ERRORS : A report shows what DataSync was able and
        # unable to transfer, skip, verify, and delete.

        @[JSON::Field(key: "ReportLevel")]
        getter report_level : String?

        def initialize(
          @destination : Types::ReportDestination? = nil,
          @object_version_ids : String? = nil,
          @output_type : String? = nil,
          @overrides : Types::ReportOverrides? = nil,
          @report_level : String? = nil
        )
        end
      end

      # Configures your DataSync task to run on a schedule (at a minimum interval of 1 hour).

      struct TaskSchedule
        include JSON::Serializable

        # Specifies your task schedule by using a cron or rate expression. Use cron expressions for task
        # schedules that run on a specific time and day. For example, the following cron expression creates a
        # task schedule that runs at 8 AM on the first Wednesday of every month: cron(0 8 * * 3#1) Use rate
        # expressions for task schedules that run on a regular interval. For example, the following rate
        # expression creates a task schedule that runs every 12 hours: rate(12 hours) For information about
        # cron and rate expression syntax, see the Amazon EventBridge User Guide .

        @[JSON::Field(key: "ScheduleExpression")]
        getter schedule_expression : String

        # Specifies whether to enable or disable your task schedule. Your schedule is enabled by default, but
        # there can be situations where you need to disable it. For example, you might need to pause a
        # recurring transfer to fix an issue with your task or perform maintenance on your storage system.
        # DataSync might disable your schedule automatically if your task fails repeatedly with the same
        # error. For more information, see TaskScheduleDetails .

        @[JSON::Field(key: "Status")]
        getter status : String?

        def initialize(
          @schedule_expression : String,
          @status : String? = nil
        )
        end
      end

      # Provides information about your DataSync task schedule .

      struct TaskScheduleDetails
        include JSON::Serializable

        # Indicates how your task schedule was disabled. USER - Your schedule was manually disabled by using
        # the UpdateTask operation or DataSync console. SERVICE - Your schedule was automatically disabled by
        # DataSync because the task failed repeatedly with the same error.

        @[JSON::Field(key: "DisabledBy")]
        getter disabled_by : String?

        # Provides a reason if the task schedule is disabled. If your schedule is disabled by USER , you see a
        # Manually disabled by user. message. If your schedule is disabled by SERVICE , you see an error
        # message to help you understand why the task keeps failing. For information on resolving DataSync
        # errors, see Troubleshooting issues with DataSync transfers .

        @[JSON::Field(key: "DisabledReason")]
        getter disabled_reason : String?

        # Indicates the last time the status of your task schedule changed. For example, if DataSync
        # automatically disables your schedule because of a repeated error, you can see when the schedule was
        # disabled.

        @[JSON::Field(key: "StatusUpdateTime", converter: AwsSdk::Runtime::UnixTimestampConverter)]
        getter status_update_time : Time?

        def initialize(
          @disabled_by : String? = nil,
          @disabled_reason : String? = nil,
          @status_update_time : Time? = nil
        )
        end
      end

      # UntagResourceRequest

      struct UntagResourceRequest
        include JSON::Serializable

        # Specifies the keys in the tags that you want to remove.

        @[JSON::Field(key: "Keys")]
        getter keys : Array(String)

        # Specifies the Amazon Resource Name (ARN) of the resource to remove the tags from.

        @[JSON::Field(key: "ResourceArn")]
        getter resource_arn : String

        def initialize(
          @keys : Array(String),
          @resource_arn : String
        )
        end
      end


      struct UntagResourceResponse
        include JSON::Serializable

        def initialize
        end
      end

      # UpdateAgentRequest

      struct UpdateAgentRequest
        include JSON::Serializable

        # The Amazon Resource Name (ARN) of the agent to update.

        @[JSON::Field(key: "AgentArn")]
        getter agent_arn : String

        # The name that you want to use to configure the agent.

        @[JSON::Field(key: "Name")]
        getter name : String?

        def initialize(
          @agent_arn : String,
          @name : String? = nil
        )
        end
      end


      struct UpdateAgentResponse
        include JSON::Serializable

        def initialize
        end
      end


      struct UpdateLocationAzureBlobRequest
        include JSON::Serializable

        # Specifies the ARN of the Azure Blob Storage transfer location that you're updating.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String

        # Specifies the access tier that you want your objects or files transferred into. This only applies
        # when using the location as a transfer destination. For more information, see Access tiers .

        @[JSON::Field(key: "AccessTier")]
        getter access_tier : String?

        # (Optional) Specifies the Amazon Resource Name (ARN) of the DataSync agent that can connect with your
        # Azure Blob Storage container. If you are setting up an agentless cross-cloud transfer, you do not
        # need to specify a value for this parameter. You can specify more than one agent. For more
        # information, see Using multiple agents for your transfer . You cannot add or remove agents from a
        # storage location after you initially create it.

        @[JSON::Field(key: "AgentArns")]
        getter agent_arns : Array(String)?

        # Specifies the authentication method DataSync uses to access your Azure Blob Storage. DataSync can
        # access blob storage using a shared access signature (SAS).

        @[JSON::Field(key: "AuthenticationType")]
        getter authentication_type : String?

        # Specifies the type of blob that you want your objects or files to be when transferring them into
        # Azure Blob Storage. Currently, DataSync only supports moving data into Azure Blob Storage as block
        # blobs. For more information on blob types, see the Azure Blob Storage documentation .

        @[JSON::Field(key: "BlobType")]
        getter blob_type : String?

        # Specifies configuration information for a DataSync-managed secret, such as an authentication token
        # or set of credentials that DataSync uses to access a specific transfer location, and a
        # customer-managed KMS key.

        @[JSON::Field(key: "CmkSecretConfig")]
        getter cmk_secret_config : Types::CmkSecretConfig?

        # Specifies configuration information for a customer-managed secret, such as an authentication token
        # or set of credentials that DataSync uses to access a specific transfer location, and a
        # customer-managed KMS key.

        @[JSON::Field(key: "CustomSecretConfig")]
        getter custom_secret_config : Types::CustomSecretConfig?

        # Specifies the SAS configuration that allows DataSync to access your Azure Blob Storage.

        @[JSON::Field(key: "SasConfiguration")]
        getter sas_configuration : Types::AzureBlobSasConfiguration?

        # Specifies path segments if you want to limit your transfer to a virtual directory in your container
        # (for example, /my/images ).

        @[JSON::Field(key: "Subdirectory")]
        getter subdirectory : String?

        def initialize(
          @location_arn : String,
          @access_tier : String? = nil,
          @agent_arns : Array(String)? = nil,
          @authentication_type : String? = nil,
          @blob_type : String? = nil,
          @cmk_secret_config : Types::CmkSecretConfig? = nil,
          @custom_secret_config : Types::CustomSecretConfig? = nil,
          @sas_configuration : Types::AzureBlobSasConfiguration? = nil,
          @subdirectory : String? = nil
        )
        end
      end


      struct UpdateLocationAzureBlobResponse
        include JSON::Serializable

        def initialize
        end
      end


      struct UpdateLocationEfsRequest
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of the Amazon EFS transfer location that you're updating.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String

        # Specifies the Amazon Resource Name (ARN) of the access point that DataSync uses to mount your Amazon
        # EFS file system. For more information, see Accessing restricted Amazon EFS file systems .

        @[JSON::Field(key: "AccessPointArn")]
        getter access_point_arn : String?

        # Specifies an Identity and Access Management (IAM) role that allows DataSync to access your Amazon
        # EFS file system. For information on creating this role, see Creating a DataSync IAM role for Amazon
        # EFS file system access .

        @[JSON::Field(key: "FileSystemAccessRoleArn")]
        getter file_system_access_role_arn : String?

        # Specifies whether you want DataSync to use Transport Layer Security (TLS) 1.2 encryption when it
        # transfers data to or from your Amazon EFS file system. If you specify an access point using
        # AccessPointArn or an IAM role using FileSystemAccessRoleArn , you must set this parameter to TLS1_2
        # .

        @[JSON::Field(key: "InTransitEncryption")]
        getter in_transit_encryption : String?

        # Specifies a mount path for your Amazon EFS file system. This is where DataSync reads or writes data
        # on your file system (depending on if this is a source or destination location). By default, DataSync
        # uses the root directory (or access point if you provide one by using AccessPointArn ). You can also
        # include subdirectories using forward slashes (for example, /path/to/folder ).

        @[JSON::Field(key: "Subdirectory")]
        getter subdirectory : String?

        def initialize(
          @location_arn : String,
          @access_point_arn : String? = nil,
          @file_system_access_role_arn : String? = nil,
          @in_transit_encryption : String? = nil,
          @subdirectory : String? = nil
        )
        end
      end


      struct UpdateLocationEfsResponse
        include JSON::Serializable

        def initialize
        end
      end


      struct UpdateLocationFsxLustreRequest
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of the FSx for Lustre transfer location that you're
        # updating.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String

        # Specifies a mount path for your FSx for Lustre file system. The path can include subdirectories.
        # When the location is used as a source, DataSync reads data from the mount path. When the location is
        # used as a destination, DataSync writes data to the mount path. If you don't include this parameter,
        # DataSync uses the file system's root directory ( / ).

        @[JSON::Field(key: "Subdirectory")]
        getter subdirectory : String?

        def initialize(
          @location_arn : String,
          @subdirectory : String? = nil
        )
        end
      end


      struct UpdateLocationFsxLustreResponse
        include JSON::Serializable

        def initialize
        end
      end


      struct UpdateLocationFsxOntapRequest
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of the FSx for ONTAP transfer location that you're
        # updating.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String

        # Specifies the data transfer protocol that DataSync uses to access your Amazon FSx file system.

        @[JSON::Field(key: "Protocol")]
        getter protocol : Types::FsxUpdateProtocol?

        # Specifies a path to the file share in the storage virtual machine (SVM) where you want to transfer
        # data to or from. You can specify a junction path (also known as a mount point), qtree path (for NFS
        # file shares), or share name (for SMB file shares). For example, your mount path might be /vol1 ,
        # /vol1/tree1 , or /share1 . Don't specify a junction path in the SVM's root volume. For more
        # information, see Managing FSx for ONTAP storage virtual machines in the Amazon FSx for NetApp ONTAP
        # User Guide .

        @[JSON::Field(key: "Subdirectory")]
        getter subdirectory : String?

        def initialize(
          @location_arn : String,
          @protocol : Types::FsxUpdateProtocol? = nil,
          @subdirectory : String? = nil
        )
        end
      end


      struct UpdateLocationFsxOntapResponse
        include JSON::Serializable

        def initialize
        end
      end


      struct UpdateLocationFsxOpenZfsRequest
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of the FSx for OpenZFS transfer location that you're
        # updating.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String


        @[JSON::Field(key: "Protocol")]
        getter protocol : Types::FsxProtocol?

        # Specifies a subdirectory in the location's path that must begin with /fsx . DataSync uses this
        # subdirectory to read or write data (depending on whether the file system is a source or destination
        # location).

        @[JSON::Field(key: "Subdirectory")]
        getter subdirectory : String?

        def initialize(
          @location_arn : String,
          @protocol : Types::FsxProtocol? = nil,
          @subdirectory : String? = nil
        )
        end
      end


      struct UpdateLocationFsxOpenZfsResponse
        include JSON::Serializable

        def initialize
        end
      end


      struct UpdateLocationFsxWindowsRequest
        include JSON::Serializable

        # Specifies the ARN of the FSx for Windows File Server transfer location that you're updating.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String

        # Specifies the name of the Windows domain that your FSx for Windows File Server file system belongs
        # to. If you have multiple Active Directory domains in your environment, configuring this parameter
        # makes sure that DataSync connects to the right file system.

        @[JSON::Field(key: "Domain")]
        getter domain : String?

        # Specifies the password of the user with the permissions to mount and access the files, folders, and
        # file metadata in your FSx for Windows File Server file system.

        @[JSON::Field(key: "Password")]
        getter password : String?

        # Specifies a mount path for your file system using forward slashes. DataSync uses this subdirectory
        # to read or write data (depending on whether the file system is a source or destination location).

        @[JSON::Field(key: "Subdirectory")]
        getter subdirectory : String?

        # Specifies the user with the permissions to mount and access the files, folders, and file metadata in
        # your FSx for Windows File Server file system. For information about choosing a user with the right
        # level of access for your transfer, see required permissions for FSx for Windows File Server
        # locations.

        @[JSON::Field(key: "User")]
        getter user : String?

        def initialize(
          @location_arn : String,
          @domain : String? = nil,
          @password : String? = nil,
          @subdirectory : String? = nil,
          @user : String? = nil
        )
        end
      end


      struct UpdateLocationFsxWindowsResponse
        include JSON::Serializable

        def initialize
        end
      end


      struct UpdateLocationHdfsRequest
        include JSON::Serializable

        # The Amazon Resource Name (ARN) of the source HDFS cluster location.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String

        # The Amazon Resource Names (ARNs) of the DataSync agents that can connect to your HDFS cluster.

        @[JSON::Field(key: "AgentArns")]
        getter agent_arns : Array(String)?

        # The type of authentication used to determine the identity of the user.

        @[JSON::Field(key: "AuthenticationType")]
        getter authentication_type : String?

        # The size of the data blocks to write into the HDFS cluster.

        @[JSON::Field(key: "BlockSize")]
        getter block_size : Int32?

        # The Kerberos key table (keytab) that contains mappings between the defined Kerberos principal and
        # the encrypted keys. You can load the keytab from a file by providing the file's address.

        @[JSON::Field(key: "KerberosKeytab")]
        getter kerberos_keytab : Bytes?

        # The krb5.conf file that contains the Kerberos configuration information. You can load the krb5.conf
        # file by providing the file's address. If you're using the CLI, it performs the base64 encoding for
        # you. Otherwise, provide the base64-encoded text.

        @[JSON::Field(key: "KerberosKrb5Conf")]
        getter kerberos_krb5_conf : Bytes?

        # The Kerberos principal with access to the files and folders on the HDFS cluster.

        @[JSON::Field(key: "KerberosPrincipal")]
        getter kerberos_principal : String?

        # The URI of the HDFS cluster's Key Management Server (KMS).

        @[JSON::Field(key: "KmsKeyProviderUri")]
        getter kms_key_provider_uri : String?

        # The NameNode that manages the HDFS namespace. The NameNode performs operations such as opening,
        # closing, and renaming files and directories. The NameNode contains the information to map blocks of
        # data to the DataNodes. You can use only one NameNode.

        @[JSON::Field(key: "NameNodes")]
        getter name_nodes : Array(Types::HdfsNameNode)?

        # The Quality of Protection (QOP) configuration specifies the Remote Procedure Call (RPC) and data
        # transfer privacy settings configured on the Hadoop Distributed File System (HDFS) cluster.

        @[JSON::Field(key: "QopConfiguration")]
        getter qop_configuration : Types::QopConfiguration?

        # The number of DataNodes to replicate the data to when writing to the HDFS cluster.

        @[JSON::Field(key: "ReplicationFactor")]
        getter replication_factor : Int32?

        # The user name used to identify the client on the host operating system.

        @[JSON::Field(key: "SimpleUser")]
        getter simple_user : String?

        # A subdirectory in the HDFS cluster. This subdirectory is used to read data from or write data to the
        # HDFS cluster.

        @[JSON::Field(key: "Subdirectory")]
        getter subdirectory : String?

        def initialize(
          @location_arn : String,
          @agent_arns : Array(String)? = nil,
          @authentication_type : String? = nil,
          @block_size : Int32? = nil,
          @kerberos_keytab : Bytes? = nil,
          @kerberos_krb5_conf : Bytes? = nil,
          @kerberos_principal : String? = nil,
          @kms_key_provider_uri : String? = nil,
          @name_nodes : Array(Types::HdfsNameNode)? = nil,
          @qop_configuration : Types::QopConfiguration? = nil,
          @replication_factor : Int32? = nil,
          @simple_user : String? = nil,
          @subdirectory : String? = nil
        )
        end
      end


      struct UpdateLocationHdfsResponse
        include JSON::Serializable

        def initialize
        end
      end


      struct UpdateLocationNfsRequest
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of the NFS transfer location that you want to update.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String


        @[JSON::Field(key: "MountOptions")]
        getter mount_options : Types::NfsMountOptions?


        @[JSON::Field(key: "OnPremConfig")]
        getter on_prem_config : Types::OnPremConfig?

        # Specifies the DNS name or IP address (IPv4 or IPv6) of the NFS file server that your DataSync agent
        # connects to.

        @[JSON::Field(key: "ServerHostname")]
        getter server_hostname : String?

        # Specifies the export path in your NFS file server that you want DataSync to mount. This path (or a
        # subdirectory of the path) is where DataSync transfers data to or from. For information on
        # configuring an export for DataSync, see Accessing NFS file servers .

        @[JSON::Field(key: "Subdirectory")]
        getter subdirectory : String?

        def initialize(
          @location_arn : String,
          @mount_options : Types::NfsMountOptions? = nil,
          @on_prem_config : Types::OnPremConfig? = nil,
          @server_hostname : String? = nil,
          @subdirectory : String? = nil
        )
        end
      end


      struct UpdateLocationNfsResponse
        include JSON::Serializable

        def initialize
        end
      end


      struct UpdateLocationObjectStorageRequest
        include JSON::Serializable

        # Specifies the ARN of the object storage system location that you're updating.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String

        # Specifies the access key (for example, a user name) if credentials are required to authenticate with
        # the object storage server.

        @[JSON::Field(key: "AccessKey")]
        getter access_key : String?

        # (Optional) Specifies the Amazon Resource Names (ARNs) of the DataSync agents that can connect with
        # your object storage system. If you are setting up an agentless cross-cloud transfer, you do not need
        # to specify a value for this parameter. You cannot add or remove agents from a storage location after
        # you initially create it.

        @[JSON::Field(key: "AgentArns")]
        getter agent_arns : Array(String)?

        # Specifies configuration information for a DataSync-managed secret, such as an authentication token
        # or set of credentials that DataSync uses to access a specific transfer location, and a
        # customer-managed KMS key.

        @[JSON::Field(key: "CmkSecretConfig")]
        getter cmk_secret_config : Types::CmkSecretConfig?

        # Specifies configuration information for a customer-managed secret, such as an authentication token
        # or set of credentials that DataSync uses to access a specific transfer location, and a
        # customer-managed KMS key.

        @[JSON::Field(key: "CustomSecretConfig")]
        getter custom_secret_config : Types::CustomSecretConfig?

        # Specifies the secret key (for example, a password) if credentials are required to authenticate with
        # the object storage server. If you provide a secret using SecretKey , but do not provide secret
        # configuration details using CmkSecretConfig or CustomSecretConfig , then DataSync stores the token
        # using your Amazon Web Services account's Secrets Manager secret.

        @[JSON::Field(key: "SecretKey")]
        getter secret_key : String?

        # Specifies a certificate chain for DataSync to authenticate with your object storage system if the
        # system uses a private or self-signed certificate authority (CA). You must specify a single .pem file
        # with a full certificate chain (for example, file:///home/user/.ssh/object_storage_certificates.pem
        # ). The certificate chain might include: The object storage system's certificate All intermediate
        # certificates (if there are any) The root certificate of the signing CA You can concatenate your
        # certificates into a .pem file (which can be up to 32768 bytes before base64 encoding). The following
        # example cat command creates an object_storage_certificates.pem file that includes three
        # certificates: cat object_server_certificate.pem intermediate_certificate.pem ca_root_certificate.pem
        # &gt; object_storage_certificates.pem To use this parameter, configure ServerProtocol to HTTPS .
        # Updating this parameter doesn't interfere with tasks that you have in progress.

        @[JSON::Field(key: "ServerCertificate")]
        getter server_certificate : Bytes?

        # Specifies the domain name or IP address (IPv4 or IPv6) of the object storage server that your
        # DataSync agent connects to.

        @[JSON::Field(key: "ServerHostname")]
        getter server_hostname : String?

        # Specifies the port that your object storage server accepts inbound network traffic on (for example,
        # port 443).

        @[JSON::Field(key: "ServerPort")]
        getter server_port : Int32?

        # Specifies the protocol that your object storage server uses to communicate.

        @[JSON::Field(key: "ServerProtocol")]
        getter server_protocol : String?

        # Specifies the object prefix for your object storage server. If this is a source location, DataSync
        # only copies objects with this prefix. If this is a destination location, DataSync writes all objects
        # with this prefix.

        @[JSON::Field(key: "Subdirectory")]
        getter subdirectory : String?

        def initialize(
          @location_arn : String,
          @access_key : String? = nil,
          @agent_arns : Array(String)? = nil,
          @cmk_secret_config : Types::CmkSecretConfig? = nil,
          @custom_secret_config : Types::CustomSecretConfig? = nil,
          @secret_key : String? = nil,
          @server_certificate : Bytes? = nil,
          @server_hostname : String? = nil,
          @server_port : Int32? = nil,
          @server_protocol : String? = nil,
          @subdirectory : String? = nil
        )
        end
      end


      struct UpdateLocationObjectStorageResponse
        include JSON::Serializable

        def initialize
        end
      end


      struct UpdateLocationS3Request
        include JSON::Serializable

        # Specifies the Amazon Resource Name (ARN) of the Amazon S3 transfer location that you're updating.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String


        @[JSON::Field(key: "S3Config")]
        getter s3_config : Types::S3Config?

        # Specifies the storage class that you want your objects to use when Amazon S3 is a transfer
        # destination. For buckets in Amazon Web Services Regions, the storage class defaults to STANDARD .
        # For buckets on Outposts, the storage class defaults to OUTPOSTS . For more information, see Storage
        # class considerations with Amazon S3 transfers .

        @[JSON::Field(key: "S3StorageClass")]
        getter s3_storage_class : String?

        # Specifies a prefix in the S3 bucket that DataSync reads from or writes to (depending on whether the
        # bucket is a source or destination location). DataSync can't transfer objects with a prefix that
        # begins with a slash ( / ) or includes // , /./ , or /../ patterns. For example: /photos
        # photos//2006/January photos/./2006/February photos/../2006/March

        @[JSON::Field(key: "Subdirectory")]
        getter subdirectory : String?

        def initialize(
          @location_arn : String,
          @s3_config : Types::S3Config? = nil,
          @s3_storage_class : String? = nil,
          @subdirectory : String? = nil
        )
        end
      end


      struct UpdateLocationS3Response
        include JSON::Serializable

        def initialize
        end
      end


      struct UpdateLocationSmbRequest
        include JSON::Serializable

        # Specifies the ARN of the SMB location that you want to update.

        @[JSON::Field(key: "LocationArn")]
        getter location_arn : String

        # Specifies the DataSync agent (or agents) that can connect to your SMB file server. You specify an
        # agent by using its Amazon Resource Name (ARN).

        @[JSON::Field(key: "AgentArns")]
        getter agent_arns : Array(String)?

        # Specifies the authentication protocol that DataSync uses to connect to your SMB file server.
        # DataSync supports NTLM (default) and KERBEROS authentication. For more information, see Providing
        # DataSync access to SMB file servers .

        @[JSON::Field(key: "AuthenticationType")]
        getter authentication_type : String?

        # Specifies configuration information for a DataSync-managed secret, such as a Password or
        # KerberosKeytab or set of credentials that DataSync uses to access a specific transfer location, and
        # a customer-managed KMS key.

        @[JSON::Field(key: "CmkSecretConfig")]
        getter cmk_secret_config : Types::CmkSecretConfig?

        # Specifies configuration information for a customer-managed secret, such as a Password or
        # KerberosKeytab or set of credentials that DataSync uses to access a specific transfer location, and
        # a customer-managed KMS key.

        @[JSON::Field(key: "CustomSecretConfig")]
        getter custom_secret_config : Types::CustomSecretConfig?

        # Specifies the IP addresses (IPv4 or IPv6) for the DNS servers that your SMB file server belongs to.
        # This parameter applies only if AuthenticationType is set to KERBEROS . If you have multiple domains
        # in your environment, configuring this parameter makes sure that DataSync connects to the right SMB
        # file server.

        @[JSON::Field(key: "DnsIpAddresses")]
        getter dns_ip_addresses : Array(String)?

        # Specifies the Windows domain name that your SMB file server belongs to. This parameter applies only
        # if AuthenticationType is set to NTLM . If you have multiple domains in your environment, configuring
        # this parameter makes sure that DataSync connects to the right file server.

        @[JSON::Field(key: "Domain")]
        getter domain : String?

        # Specifies your Kerberos key table (keytab) file, which includes mappings between your Kerberos
        # principal and encryption keys. To avoid task execution errors, make sure that the Kerberos principal
        # that you use to create the keytab file matches exactly what you specify for KerberosPrincipal .

        @[JSON::Field(key: "KerberosKeytab")]
        getter kerberos_keytab : Bytes?

        # Specifies a Kerberos configuration file ( krb5.conf ) that defines your Kerberos realm
        # configuration. The file must be base64 encoded. If you're using the CLI, the encoding is done for
        # you.

        @[JSON::Field(key: "KerberosKrb5Conf")]
        getter kerberos_krb5_conf : Bytes?

        # Specifies a Kerberos prinicpal, which is an identity in your Kerberos realm that has permission to
        # access the files, folders, and file metadata in your SMB file server. A Kerberos principal might
        # look like HOST/kerberosuser@MYDOMAIN.ORG . Principal names are case sensitive. Your DataSync task
        # execution will fail if the principal that you specify for this parameter doesn’t exactly match the
        # principal that you use to create the keytab file.

        @[JSON::Field(key: "KerberosPrincipal")]
        getter kerberos_principal : String?


        @[JSON::Field(key: "MountOptions")]
        getter mount_options : Types::SmbMountOptions?

        # Specifies the password of the user who can mount your SMB file server and has permission to access
        # the files and folders involved in your transfer. This parameter applies only if AuthenticationType
        # is set to NTLM .

        @[JSON::Field(key: "Password")]
        getter password : String?

        # Specifies the domain name or IP address (IPv4 or IPv6) of the SMB file server that your DataSync
        # agent connects to. If you're using Kerberos authentication, you must specify a domain name.

        @[JSON::Field(key: "ServerHostname")]
        getter server_hostname : String?

        # Specifies the name of the share exported by your SMB file server where DataSync will read or write
        # data. You can include a subdirectory in the share path (for example, /path/to/subdirectory ). Make
        # sure that other SMB clients in your network can also mount this path. To copy all data in the
        # specified subdirectory, DataSync must be able to mount the SMB share and access all of its data. For
        # more information, see Providing DataSync access to SMB file servers .

        @[JSON::Field(key: "Subdirectory")]
        getter subdirectory : String?

        # Specifies the user name that can mount your SMB file server and has permission to access the files
        # and folders involved in your transfer. This parameter applies only if AuthenticationType is set to
        # NTLM . For information about choosing a user with the right level of access for your transfer, see
        # Providing DataSync access to SMB file servers .

        @[JSON::Field(key: "User")]
        getter user : String?

        def initialize(
          @location_arn : String,
          @agent_arns : Array(String)? = nil,
          @authentication_type : String? = nil,
          @cmk_secret_config : Types::CmkSecretConfig? = nil,
          @custom_secret_config : Types::CustomSecretConfig? = nil,
          @dns_ip_addresses : Array(String)? = nil,
          @domain : String? = nil,
          @kerberos_keytab : Bytes? = nil,
          @kerberos_krb5_conf : Bytes? = nil,
          @kerberos_principal : String? = nil,
          @mount_options : Types::SmbMountOptions? = nil,
          @password : String? = nil,
          @server_hostname : String? = nil,
          @subdirectory : String? = nil,
          @user : String? = nil
        )
        end
      end


      struct UpdateLocationSmbResponse
        include JSON::Serializable

        def initialize
        end
      end


      struct UpdateTaskExecutionRequest
        include JSON::Serializable


        @[JSON::Field(key: "Options")]
        getter options : Types::Options

        # Specifies the Amazon Resource Name (ARN) of the task execution that you're updating.

        @[JSON::Field(key: "TaskExecutionArn")]
        getter task_execution_arn : String

        def initialize(
          @options : Types::Options,
          @task_execution_arn : String
        )
        end
      end


      struct UpdateTaskExecutionResponse
        include JSON::Serializable

        def initialize
        end
      end

      # UpdateTaskResponse

      struct UpdateTaskRequest
        include JSON::Serializable

        # Specifies the ARN of the task that you want to update.

        @[JSON::Field(key: "TaskArn")]
        getter task_arn : String

        # Specifies the Amazon Resource Name (ARN) of an Amazon CloudWatch log group for monitoring your task.
        # For Enhanced mode tasks, you must use /aws/datasync as your log group name. For example:
        # arn:aws:logs:us-east-1:111222333444:log-group:/aws/datasync:* For more information, see Monitoring
        # data transfers with CloudWatch Logs .

        @[JSON::Field(key: "CloudWatchLogGroupArn")]
        getter cloud_watch_log_group_arn : String?

        # Specifies exclude filters that define the files, objects, and folders in your source location that
        # you don't want DataSync to transfer. For more information and examples, see Specifying what DataSync
        # transfers by using filters .

        @[JSON::Field(key: "Excludes")]
        getter excludes : Array(Types::FilterRule)?

        # Specifies include filters define the files, objects, and folders in your source location that you
        # want DataSync to transfer. For more information and examples, see Specifying what DataSync transfers
        # by using filters .

        @[JSON::Field(key: "Includes")]
        getter includes : Array(Types::FilterRule)?

        # Configures a manifest, which is a list of files or objects that you want DataSync to transfer. For
        # more information and configuration examples, see Specifying what DataSync transfers by using a
        # manifest . When using this parameter, your caller identity (the IAM role that you're using DataSync
        # with) must have the iam:PassRole permission. The AWSDataSyncFullAccess policy includes this
        # permission. To remove a manifest configuration, specify this parameter as empty.

        @[JSON::Field(key: "ManifestConfig")]
        getter manifest_config : Types::ManifestConfig?

        # Specifies the name of your task.

        @[JSON::Field(key: "Name")]
        getter name : String?


        @[JSON::Field(key: "Options")]
        getter options : Types::Options?

        # Specifies a schedule for when you want your task to run. For more information, see Scheduling your
        # task .

        @[JSON::Field(key: "Schedule")]
        getter schedule : Types::TaskSchedule?

        # Specifies how you want to configure a task report, which provides detailed information about your
        # DataSync transfer. For more information, see Monitoring your DataSync transfers with task reports .
        # When using this parameter, your caller identity (the IAM role that you're using DataSync with) must
        # have the iam:PassRole permission. The AWSDataSyncFullAccess policy includes this permission. To
        # remove a task report configuration, specify this parameter as empty.

        @[JSON::Field(key: "TaskReportConfig")]
        getter task_report_config : Types::TaskReportConfig?

        def initialize(
          @task_arn : String,
          @cloud_watch_log_group_arn : String? = nil,
          @excludes : Array(Types::FilterRule)? = nil,
          @includes : Array(Types::FilterRule)? = nil,
          @manifest_config : Types::ManifestConfig? = nil,
          @name : String? = nil,
          @options : Types::Options? = nil,
          @schedule : Types::TaskSchedule? = nil,
          @task_report_config : Types::TaskReportConfig? = nil
        )
        end
      end


      struct UpdateTaskResponse
        include JSON::Serializable

        def initialize
        end
      end
    end
  end
end
